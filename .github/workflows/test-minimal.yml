name: Test Minimal

on:
  push:
    branches: [fix/ci-workflow-errors]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "Test workflow running"
