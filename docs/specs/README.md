# 规格文档

本目录存放当前目标态工程契约，不承载历史过程和代码实现证据。

| 文档 | 用途 |
|---|---|
| [domain-model.md](domain-model.md) | 业务对象、字段、状态、枚举、统计口径 |
| [api-contract.md](api-contract.md) | API、权限、请求和响应契约 |

维护规则：

- 只写当前目标态，不写历史迁移过程。
- 不放代码路径和测试文件路径。
- 历史字段对照、访谈记录和评审记录放入 `docs/archive/requirements-decisions/`。
