# 工具元数据说明

仓库根目录包含少量用于本地工具与会话面板的元数据配置文件。
这些文件依赖固定路径加载，**请保留在根目录**，避免移动导致工具失效。

## 目录与文件用途

- `labels/`：标签体系配置（颜色、层级、可选值等）
- `statuses/`：状态配置与图标资源
- `sources/`：MCP 数据源与连接配置
- `views.json`：会话/任务视图过滤规则

如需新增或调整配置，请同步更新本说明文档。
