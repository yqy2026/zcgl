# 土地物业资产运营管理系统 Context

本文件记录土地物业资产运营管理系统的领域语言，确保 PRD、契约文档和实现讨论使用同一套业务词义。

## Language

**权限并集**:
多角色用户在没有命中显式拒绝时，按各角色允许执行的动作合并得到的权限集合。
_Avoid_: 多角色权限绝对并集

**显式拒绝优先**:
当用户命中明确拒绝的授权或数据策略时，拒绝结论覆盖其他角色带来的允许权限；这是 MVP 的基础安全语义。
_Avoid_: Deny-Overrides

**高级多角色冲突策略配置**:
未来用于配置和解释复杂多角色冲突的扩展能力，不等同于 MVP 已有的显式拒绝优先语义。
_Avoid_: Deny-Overrides 策略框架

**权限管理员授予边界**:
`perm_admin`（权限管理员）「不可查看业务数据」是**强约束而非默认**:它不得授予任何带业务数据范围的「业务角色」(如 `asset_manager`/`project_manager`/运营/业务审核类)，含授予自己;业务角色只能由 `admin`/`system_admin` 授予。否则 `perm_admin` 可 `POST /users/{自己}/roles` 自授业务角色、缓存刷新后看全量业务数据——§5/Q6 的「完全隔离」即被单人捅穿。这是**静态角色授予边界**，按角色「业务/管理」分类在 service 层（`rbac_service.assign_role_to_user`）校验，违规拒绝并审计;它**不是职责互斥矩阵**，与 [[资产确认（两步生命周期）]] 所属的「MVP 不做职责互斥」(ADR-0002,豁免的是双人共谋)不冲突——本边界拦的是单人越过明文安全边界的自我提权，性质是「兑现承诺」不是「增强防共谋」。见 ADR-0009。
_Avoid_: perm_admin 可授任意角色、perm_admin 自授业务角色、完全隔离=默认不分配、授予边界=职责互斥、业务角色 perm_admin 可授予、仅端点层校验授予

**运营方单一权威轴（随项目派生）**:
资产的「运营方」`manager_party_id` 不是资产自带的独立可填字段，而是 **= 资产当前有效项目的运营方派生值**；运营方权威轴唯一落在项目（Project），资产挪项目则运营方随之改变、数据范围（manager 绑定谁能看）自动跟随。**产权方恰好相反**：`owner_party_id` 是资产内在权属事实，与归在哪个项目解耦，挪项目不改变所有权（资产身份锚在产权方——`owner_party_id` 内在必填、挪项目不变；`asset_code` 按产权方编码段自动生成、生成后冻结、系统生成只读，**段来自 owner 的 `party_code` 派生**（与 project_code/group_code 同一套 `_build_*_code_segment` 机制，ADR-0018 统一段来源、撤原拟的专用 `asset_code_prefix` 字段），**已决策待实施见 ADR-0017/0018**——现状为 `nullable` 选填、零生成器的过度声明，真实唯一/重名锚暂为 `asset_name`）。二者性质不同，故**非对称**——运营方派生、产权方独立必填。`Asset.project_id` 选填，**无项目资产 = 无运营方**，对运营方绑定用户一律不可见，但产权方绑定视角照常可见（还没归入运营项目 = 还没人在运营它）。这把 REQ-PRJ-001「项目绑定运营管理方、通过资产间接关联产权方」落到字段形态上。见 ADR-0010。
_Avoid_: 资产独立可填运营方、Asset.manager_party_id 自由设置、有项目时校验等于项目运营方（不如直接派生）、产权方随项目派生、运营方挂资产、按资产字段算 manager 数据范围、asset_code 已按产权方生成（实为待实施 ADR-0017）、asset_code 用户手填、asset_code 段靠名字派生、owner 更正重编 asset_code、新增 asset_code_prefix 专用字段（ADR-0018 撤销改用 party_code 派生段）、project_code 按年月段（应按运营方 party_code 段，ADR-0018）

**产权方**:
Party 在资产、项目或合同关系中承担产权归属的一种业务角色；MVP 使用 Party 主档表达产权方。产权方是资产的内在权属事实，挂在资产本身（`owner_party_id` 独立必填），与 [[运营方单一权威轴（随项目派生）]] 的运营方相对——运营方随项目派生，产权方不随项目变。
_Avoid_: 权属方

**权属方管理**:
旧 Ownership 独立管理模块，不等同于 Party 的产权方角色；当前不纳入 MVP 正式需求基线。
_Avoid_: 产权方管理

**产权证管理**:
记录、维护和核验证明资产权属的证照信息；该能力属于 MVP，可从资产详情关联同一本产权证覆盖的多个资产，但需要与 Party 产权方角色和旧 Ownership 模块明确拆分。产权证证号是全局唯一业务键；同一本证覆盖多个资产时，应在同一产权证记录上关联多个资产，不得以相同证号创建多条产权证。重复证号不是新产权证路径，而是既有产权证资产关联维护入口；从另一个资产录入同一证号时，可以在当前资产详情页展示既有产权证摘要并完成追加关联确认，不强制跳转到产权证详情页，但追加该资产关联必须由用户显式确认，不自动绑定。若重复证号由本次扫描件上传触发，本次扫描件应作为既有产权证的附件候选，默认随追加关联一并保留，但仍需用户确认，用户可取消追加以避免重复附件；但当既有产权证当前没有任何附件时，不能取消追加本次扫描件。产权证与资产是多对多平等关联，MVP 不区分主资产和附属资产；展示可按资产名称或编码排序。产权证必须至少关联一个既有资产，不作为孤立证照记录保存；用户可追加或移除资产关联，但不得移除最后一个资产关联。产权证统一使用“附件”承载证照事实底座，不再区分“证照扫描件”和“其他附件”。用户可追加、替换或删除附件，但正式产权证任何时候至少保留一份附件；附件上传仅接受 PDF、JPG/JPEG、PNG，单个文件大小上限为 20MB；附件上传后不自动触发扫描件解析，上传附件和解析辅助补录是两个独立动作，用户需要解析时再手动触发；同一次操作可多选上传多个附件，但每个文件独立校验类型和大小，不合规文件不得进入附件列表，合规文件可继续保留；附件应支持轻量在线预览，PDF 可内嵌预览，JPG/PNG 可图片预览；预览跟随产权证查看权限，且不单独记录操作日志；下载是独立资料外流动作，下载按钮和下载接口必须校验产权证附件下载权限，不得仅凭产权证查看权限放行；MVP 不做批注、旋转、裁剪、全文搜索或 OCR 原文高亮；预览失败只提示“预览失败，可下载查看”，不阻断产权证保存、附件保留、扫描件解析或人工核验。替换附件时新附件取代旧附件，MVP 不保留被替换旧附件；删除附件不要求填写或记录删除原因；下载附件需要记录轻量操作日志，记录谁在什么时候下载了哪个附件，但不要求填写下载原因，也不记录下载来源页面、入口、客户端 IP 或设备信息，不触发审批或告警；下载日志仅进入系统操作日志/审计日志查询，不在产权证详情页默认展示；MVP 只支持单个附件下载，不提供多附件打包下载；系统操作日志可按产权证号筛选产权证附件下载或变更记录，附件文件名仅作为日志展示信息，不作为 MVP 筛选条件；MVP 不做附件版本管理、替换审批、差异追踪，不新增附件备注字段，也不为附件新增“来源于哪次资产详情维护”的业务字段；如需追溯附件追加、替换、删除或下载动作，使用通用操作日志；其中追加、替换、删除日志记录附件 ID、文件名、动作类型、操作人和操作时间，不记录操作原因，不保留被替换或删除的旧文件内容，也不做附件差异记录。附件上传可按同名或相同文件哈希做轻量重复提示；疑似重复时只提示“疑似重复附件”，不向用户展示具体命中依据；默认不追加，但用户可显式选择追加；选择追加即视为确认，不要求二次确认弹窗，不作为保存阻断。MVP 不做产权证附件总容量配额、多附件打包下载、批量解析、批量确认或复杂上传队列治理。MVP 不做核验状态（`is_verified` 已删，见 [[产权证核验状态（MVP 已删除）]] 与 ADR-0005）。产权证权利人与关联资产当前主产权主体不一致时，按关联资产逐个生成数据质量风险提示，不阻断保存。产权证数据质量风险只在资产详情产权证区域、产权证详情/编辑页和项目风险摘要中展示，不进入全局搜索，也不产生独立处置流程；风险由当前产权证数据实时派生，不能手工关闭、忽略或标记已处理，只能通过补齐证照信息或修正权利人/资产关联自然消除（MVP 无核验，`is_verified` 已删，见 ADR-0005）；扫描件解析出的坐落或地址未匹配到既有资产时，不得直接创建并关联新资产，也不得保存为无资产关联的产权证。**正式保存硬门槛(5 条)**:证号全局唯一且非空、≥1 既有资产关联、≥1 附件、权利人引用已审核 Party、**坐落/地址非空**(不动产权/房屋/土地证类型必填,其他证照类型按证号;地址 2026-06-18 grill 新增,扩 ADR-0005 当时列举的 4 条);证载面积/期限/限制信息为空只 `warning` 不阻断。**现状缺口(待收口)**:手动创建 API(`api/v1/assets/property_certificate.py`)误把 `validate_extracted_fields`(本为解析字段提示)当保存硬闸门——它卡了地址(对)但**漏卡 ≥1资产/≥1附件/权利人已审核**,而 `service.create_certificate` 裸 dump 无门槛校验,证号唯一仅靠 DB 约束硬报错;须把保存闸门改为只卡这 5 条硬门槛(解析提示≠保存门禁)。另:`validator.py` 的 `validate_certificate_data` 及 18 位/面积/发证日期/到期日期必填实例方法是**生产零引用、仅测试调用的僵尸代码**,编码减法前旧硬必填、与 PRD 放宽冲突,按「删而非冻」连同其「保佑违规」测试一并删。
_Avoid_: 权属方管理、解析创建资产、全局产权证搜索、产权证风险处置流程、孤立产权证、删除最后一个资产关联、重复证号新建产权证、自动追加资产关联、解析字段校验当保存闸门、面积/期限硬必填、证号18位硬必填、validate_certificate_data 接回保存路径、地址降为 warning

**产权证解析入口**:
产权证解析入口分为新建与既有两种：新建产权证时，可在解析会话中上传一份 PDF/JPEG/PNG 作为会话级临时文件，人工确认并满足正式保存硬门槛后，产权证、资产/权利人关系和正式附件一次完成；取消、失败、超时或放弃时清理临时文件，不产生草稿产权证或孤儿附件。解析既有产权证时，只能选择当前产权证正式附件列表中的附件，不承担上传新文件能力；需要解析新文件时，先经产权证附件入口追加，再手动触发解析。解析确认页不提供保存草稿或稍后继续确认；用户可以取消解析，之后重新解析或手工补录。确认成功后自动返回产权证详情或编辑页并展示保存成功提示，不保留确认结果页。对同一附件重新解析时，生成新的临时解析结果，旧的未确认候选直接废弃；已确认写入产权证主数据的字段不受重新解析影响。解析候选与当前已保存字段冲突时，已保存字段默认保留；解析候选只作为差异提示，用户逐字段确认或修正后才写入。低置信字段必须被逐项处理，处理方式只能是确认候选值、修正为手工值，或在非必填字段上显式选择暂不填写或留空；未处理的低置信字段不得直接提交确认。用户可在确认页补齐解析未识别出的目标对象字段，字段来源按手工录入记录为 `manual`；但不得新增不属于目标对象字段契约的临时字段。用户可对非必填解析字段选择暂不填写或留空，空值按人工确认写入字段来源快照；必填字段仍必须补齐后才能确认保存。
_Avoid_: 既有产权证解析入口重新上传新文件、新建产权证临时文件先落正式附件、孤儿附件、产权证草稿态、脱离附件事实底座解析、解析草稿箱、稍后继续确认、解析确认结果页、低置信字段不处理直接提交、确认页新增临时字段、保留未确认候选版本、解析候选自动覆盖已保存字段

**产权证核验状态（MVP 已删除）**:
原 `is_verified` 布尔「核验状态」MVP **彻底删除**（见 ADR-0005）。删除理由:后端零消费（只在导入时写 `False`，无任何逻辑读取），其唯一「消费者」——「未核验」数据质量风险——**从未实现**；又因已决定不存核验人/时间，它是个匿名、不可审计、卡不住任何事的手工勾，两头不靠。产权证质量风险只剩两个**客观**触发:证照信息不完整、权利人与关联资产当前主产权主体不一致。核验若将来要做属 vNext，须一并设计追责与消费场景。
_Avoid_: is_verified、核验状态、核验门禁、未核验风险、核验人字段、核验时间字段、解析自动置核验、产权证核验徽标

**产权证权利人**:
产权证上承载权利的一方，MVP 必须绑定到已审核 Party；证载权利人名称只能作为快照信息。产权证权利人与关联资产当前主产权主体不一致时只提示风险，不作为保存门禁。扫描件解析出的权利人名称未匹配到已审核 Party 时，不得直接创建并写入产权证。
_Avoid_: 自由文本权利人、解析创建权利人

**合同补录**:
MVP 中对全部合同采用盖章扫描件上传和必要经营字段补录的方式记录合同事实，用于项目合同关系、台账和风险提示；合同必须保留至少一份盖章扫描件作为合同事实底座；**补录即生效**事实底座，不经提审/审核步骤，不通过 BPM 合同审批流（提审/审核/联审/反审核/制审分离整体降级 vNext，同 [[资产审批流（MVP 已移除）]] 尺度，见 ADR-0013）或主合同覆盖门禁限制录入。注意区分：MVP **仍保留最小合同生命周期**——见 [[最小合同生命周期]]，它承载 REQ-RNT-005 更正与台账到期，不是审批流。
_Avoid_: 合同审批流程、合同提审、合同审核、合同联审、合同反审核、制审分离、合同待审态、review_status 审批列、合同补录无任何状态

**最小合同生命周期**:
合同在 MVP 保留的、**非审批流**的最小状态：`草稿`（纠错草稿/录入中）、`生效`、`已到期`、`已终止`。补录即落 `生效`；**存储轴只有 `{草稿, 生效, 已终止}`**——`已到期` 按 `effective_to < today` **派生**（展示状态、非落库写入值，无到期 job、无 `expire` 操作/审计事件，同 [[逾期（派生口径）]]），`已终止` 由显式终止动作写入并留痕；台账生成器直接比对 `effective_to` 判定何时停生成未来条目（§4.14），生效中合同受删除保护。`ContractAuditLog` 只记显式操作 `{terminate, void, start_correction, finalize_correction}`（定稿 `finalize_correction` 触发台账重算、§8 须审计），不记派生的到期与审批流动作（审批流已随 ADR-0013 删）。`草稿 → 定稿` 一步承载 [[合同补录]] 与 REQ-RNT-005 更正，纠错草稿发起门禁绑生命周期状态（只能从生效合同发起），不依赖审批列。它**不是** ADR-0013 删掉的提审/审核/联审/反审核审批流——那套（含 `ContractReviewStatus`、`待审` 态、`_requires_joint_review` 联审）已整体降级 vNext。区分尺度同 [[资产确认（两步生命周期）]] vs [[资产审批流（MVP 已移除）]]：留的是承重的最小状态，删的是路由审批流。
_Avoid_: 最小生命周期=审批流、保留待审态、保留 review_status、补录走提审审核、纠错门禁依赖 review_status、已到期落库物化、到期定时任务写状态、expire 当审计动作、已到期算操作留痕

**合同关系锁单项目（代理合同跨项目靠共享扫描件）**:
`ContractGroup` 恒锁**单项目**（承租/代理一律如此），覆盖资产同属该项目，service 层校验——这是 [[台账（账本语义·归属固化）]] 固化项目唯一、§6.3「合同→资产→项目」归属链不断的前提。现实里**代理模式跨项目的只是委托协议（产权方↔运营方）**——直租/上游/下游合同恒单项目（承租天然项目内；2026-06-19 grill 收窄 ADR-0012 原「一份代理合同」泛指，避免实施时把直租也做成跨项目），但委托跨项目落在**物理盖章扫描件层**：同一份委托协议扫描件**只传一次**，在覆盖的每个项目下各建一条 [[合同关系（建模对象）]]/合同记录**共享引用同一扫描件**，不建跨项目合同关系、不把一条 `Contract` 挂多个合同组（`Contract.contract_group_id` 单值、合同经 `ContractGroup.project_id` 归属项目）。两条一致性硬校验（service 层）：`Contract.asset_ids ⊆ ContractGroup.asset_ids`（留空=覆盖本关系全部资产/整租，台账固化回退组 asset_ids）；合同组覆盖资产的 `asset.project_id` 须 = 组 `project_id`（现状 `_ensure_assets_not_bound_to_other_groups` 只拦跨项目组间冲突、漏比对资产自身项目，待补）。**跨项目的是那张合同，不是合同关系，更不是台账条目**——钱不跨项目，每条直租租金/服务费台账落唯一项目、服务费计入该项目应收。一份代理合同在 N 个项目各建一条关系是正当的建模摩擦（合同关系本就不要求零摩擦），低摩擦那头由「扫描件只传一次、共享引用」兜。被否决：改 `Contract` 多对多挂多组 / 合同组代理模式跨项目（破单项目不变量，把 ADR-0011 与 §6.3 全拖下水）。**合同号按项目复合唯一（2026-06-18 grill 补）**：现状 `contracts.contract_number` 全局 `unique=True`，与「N 个项目各建一条同号记录」直接冲突（同号第二条被 DB 拒、机制落不了地）——改 `UNIQUE(contract_number, project_id)`，N 条同号靠 project 区分；共享扫描件单存一份文档行、N 记录引用，替换即对所有覆盖项目生效、删除受「每合同 ≥1 扫描件」保护；更正按记录独立只重算本项目台账、不联动兄弟记录；不建显式「逻辑合同」聚合（靠同号 + 共享扫描件发现）。**客户双指标安全（2026-06-19 grill）**：跨项目只发生在委托协议，而 [[客户双指标（视图口径 + 仅终端客户）]] 只数直租/下游 lessee 合同、二者恒单项目，故全局/owner 汇总不会把跨项目兄弟记录算重，domain-model §6「客户合同数按合同 ID 去重」逻辑正确、无需改按 `contract_number` 逻辑合同去重。见 ADR-0012。
_Avoid_: 合同组跨项目、代理合同建跨项目合同关系、Contract 多对多挂多合同组、台账跨项目合并金额、代理合同重复上传扫描件、承租合同关系跨项目、直租/上游/下游跨项目、按合同 ID 去重把跨项目委托算进客户合同数、代理合同项目归属留空、contract_number 全局唯一、同号兄弟记录联动更正、为跨项目代理合同建逻辑合同聚合对象

**合同关系（建模对象）**:
归属项目、承载一笔经营关系的用户可见对象（由 `ContractGroup` 派生投影），包含模式、主体、资产、结算和风险。**建合同关系是一个建模动作，与 [[合同补录]] 的零摩擦预期不同**：合同补录追求"先把扫描件和必要字段存进来"的低摩擦，而建合同关系需要先想清这笔经营关系的结构（模式、主体、资产范围、结算口径）。二者摩擦预期本就不同，不要求合同关系也零摩擦。其 `settlement_rule`（`cycle / settlement_mode / amount_rule / payment_rule` 等）**创建时选填、可缓填**：允许先建关系、补录合同后再在编辑页补结算规则；保存不因其缺失而阻断。「软必填」这个含糊词作废——保存要么拦要么不拦，没有中间态,MVP 选「不拦」。台账实际依据组内合同条款明细（`ContractRentTerm` / `AgencyAgreementDetail`）生成，而非这个汇总 JSON，`settlement_rule` 不参与任何保存门禁或台账计算。结构先保留（五键 JSON），因为 `settlement_mode` / `payment_rule` 可含合同条款表达不了的运营约定（分润、归集口径）；是否进一步与同样「MVP 只存不用」的 `revenue_share_rule` 合并是另一话题，MVP 不动。**代码对齐**：`ContractGroupCreate.settlement_rule` 由 `Field(...)` 改可空、DB 去 `NOT NULL`；并拔除 `predecessor_group_id`「续签用」残留（对齐「MVP 不提供续签」、`RENEWAL` 已删）。
_Avoid_: 合同关系零摩擦、settlement_rule 创建时硬必填、软必填、settlement_rule 纯条款派生、建合同关系=随手补录、settlement_rule 进保存门禁、predecessor_group_id 续签残留

**扫描件解析辅助补录**:
系统对合同或产权证盖章扫描件执行 OCR/AI 解析，提取关键字段并预填补录表单；解析结果必须经用户人工确认或修正后才能写入业务主数据。解析会话只是临时工作区，确认、取消、失败或放弃后不作为长期记录保留；解析确认页不提供保存草稿或稍后继续确认；产权证解析确认成功后自动返回产权证详情或编辑页并展示保存成功提示，不保留确认结果页；长期事实以合同或产权证主数据、附件和字段来源快照为准，不记录解析工具、模型、供应商、置信度、确认时间、页码、文本片段或截图区域等解析元信息和字段级来源证据。解析失败、超时或低置信度不得阻断用户完全手工补录；产权证解析失败或低置信字段较多时不生成待办或任务，只在当前解析确认页和产权证编辑页提示，可手工补录或重新解析；对同一产权证附件重新解析时，旧的未确认解析候选直接废弃，不保留候选版本对比；已确认写入主数据的字段不受影响。低置信字段必须高亮并被用户逐项处理，处理方式只能是确认候选值、修正为手工值，或在非必填字段上显式选择暂不填写或留空；未处理的低置信字段不得直接提交确认。MVP 只支持字段级确认，不提供“一键采用全部高置信无冲突字段”或批量采纳解析候选；非必填解析字段允许用户选择暂不填写或留空，并按 `manual` 记录空值来源；必填字段不得通过留空完成确认保存。用户可在确认页补齐解析未识别出的目标对象字段，字段来源记为 `manual`；确认载荷不得包含不属于目标对象字段契约的临时字段。确认页可以主动列出少量 OCR/AI 未识别但建议补全的非必填经营关键字段，合同限签订日期、付款周期、备注类关键经营字段，产权证限证载面积、期限、限制信息；不展开目标对象全部可选字段。建议补全字段是数据质量提示，不属于解析确认硬门槛；用户未填写时不阻断确认保存，空值按 `manual` 进入字段来源快照，后续可在普通编辑页补齐；保存后仍为空时，可形成补录完整性提示。解析候选与已保存字段冲突时，不自动覆盖已有值，应作为差异提示由用户逐字段确认或修正；关键字段确认时应展示轻量来源证据，辅助用户核对原件，但确认后不长期保存这些证据。最终保存时需记录字段来源快照，区分手工录入、解析预填确认和解析预填修正；解析确认载荷中凡写入目标字段的值都必须有对应字段来源，缺失来源应阻断确认保存。普通非解析编辑不属于解析确认链路，若缺少字段来源可由服务端按手工录入补为 `manual`。解析出的主体、权利人或资产未匹配到既有合格主档时，只能提示用户先完成主档维护后再选择。
_Avoid_: 自动落库、解析门禁、解析创建 Party、解析创建资产、解析失败生成待办、低置信生成任务、低置信字段不处理直接提交、确认页新增临时字段、解析草稿箱、稍后继续确认、解析确认结果页、保留未确认候选版本、解析候选版本对比、批量采纳解析候选、一键采用高置信字段、长期保存字段级来源证据、复杂版本 diff、全文标注编辑器、解析会话审计档案、解析模型审计、解析确认来源缺失静默保存、确认页全量编辑器

**手工补齐字段**:
在扫描件解析确认页中，OCR/AI 未识别出候选值但属于合同或产权证目标对象字段契约、由用户直接填写的字段；确认页需要显示手工补齐或来源标记。保存后该字段归入手工录入来源，只在编辑或补录来源上下文按需查看，不进入业务详情主视图。MVP 不区分“纯手工录入”和“解析未识别后手工补齐”的字段来源枚举，二者长期来源值统一为 `manual`；UI 可按当前场景显示“手工录入”或“手工补齐”。确认页主动提示的手工补齐字段只覆盖少量建议补全字段，不替代合同或产权证普通编辑页；建议字段未填不阻断保存。
_Avoid_: 临时自定义字段、业务详情主视图来源徽标、OCR 未识别字段长期证据、手工来源子类型、确认页全字段补录

**补录完整性提示**:
合同或产权证建议补全字段为空时，由当前业务数据实时派生的轻量质量提示。合同范围限签订日期、付款周期和备注类关键经营字段；产权证范围限证载面积、期限和限制信息。该提示只在详情或编辑上下文提醒用户后续补齐，不生成任务、待办或审批，不作为保存门禁。
_Avoid_: 保存门禁、审批、任务、待办、手工关闭

**催缴管理（MVP 已删除）**:
后端曾建成的催缴工单模块（`CollectionRecord`、`PENDING / IN_PROGRESS / SUCCESS` 催缴状态机、催缴成功率统计），MVP **彻底删除**（删而非冻，见 ADR-0006）。删除理由:(1) 从未进入需求基线——PRD 与 api-contract 均无催缴条目，前端只有孤儿类型文件，无任何页面消费;(2) 本质是任务/待办外壳，与 MVP「不生成任务、待办」的产品哲学冲突;(3)「催缴成功率」是没人消费的 KPI，与已删的 [[台账覆盖率（MVP 已删除）]] 同病。逾期处理的真实闭环是:台账逾期查询 → 线下催收 → 台账实收登记（REQ-RNT-006）。催收分派与跟踪如将来需要，属 vNext 另行设计。
_Avoid_: 催缴工单、催缴状态机、催缴成功率、催缴记录、收款任务、催缴待办

**上线成功指标（删二留一）**:
§3.2 上线后三个月成功指标只保留「关键报表口径一致性问题 ≤ 2 个/月」，认定规则:口径类缺陷以 issue 标签登记，按确认为真实口径不一致的缺陷数逐月计数，由产品负责人月度复盘认定;纯人工流程，不建系统功能或报表。「资产台账完整率 ≥ 98%」已删——无口径定义，与 [[台账覆盖率（MVP 已删除）]] 同构，数据完整性由导入模板校验、[[资产确认（两步生命周期）]] 和 [[补录完整性提示]] 机制保障，不靠事后指标。「核心流程平均处理时长下降 ≥ 40%」已删——没有线下基线即不可证伪。MVP 的真实成功标准是 §3.1 一票否决目标加 §9 G1-G3。ADR-0002 中对「资产台账完整率 ≥ 98%」的引用是历史决策记录，不回溯改写。
_Avoid_: 资产台账完整率、处理时长下降指标、为 KPI 建精确口径、口径问题自动计数报表

**台账覆盖率（MVP 已删除）**:
原 §3.2 成功指标「合同台账覆盖率 ≥ 95%」及 domain-model §6 的覆盖率口径，MVP **彻底删除**——不当成功指标、不在 §6 定义、不建计算或报表。删除理由:(1) 与已建成的 `补偿任务`(自动扫描补齐缺失台账)重叠到自证满分，信息量低;(2) 是 KPI 非功能，为没人天天看的数字建精确口径属过度建模;(3) 数据完整度由"台账自动生成 + 补偿任务补齐"机制保障，不靠这个事后指标。原 §6 口径还存在条目级/合同级量纲矛盾，一并消失。
_Avoid_: 台账覆盖率、合同台账覆盖率、覆盖率分母、覆盖率报表

**客户双指标（视图口径 + 仅终端客户）**:
G3 经营统计的客户主体数 / 客户合同数。两条约束:(1) **属分析视图口径,禁止 `binding_type=all` 混合**——多绑定用户看客户指标必须先选定 owner 或 manager 视图,系统按该视图计算;`all` 并集去重只保留给"常规客户列表"(供浏览),不进经营统计口径。这把 PRD §5 两条看似冲突的规则分工:**列表可并集,分析必选视图**。(2) **经营指标的"客户"只算终端客户(外部)**——上游/委托等内部合同的对方(运营方/产权方)不计入客户双指标,只有下游 lessee 和直租 lessee 算;内部主体可在客户列表以"内部"标签出现,但不污染经营指标分母。客户主体数按 Party 去重、客户合同数按合同 ID 去重不变。**「内部」一词有两个互不相同的义项,不得互相套用**:(a) 经营指标里「内部对手方不计入」的「内部」= **合同角色**(上游/委托对手方);(b) 客户列表 `customer_type` 标签的「内部」= **视角相对的自己人**(客户主体落在查看者绑定主体内)。指标过滤**只认 (a) 合同角色**,与 `customer_type` 标签无关——关联公司作为下游/直租终端承租方时照常计入经营客户数,即使列表把它标为「内部」。`customer_type` 是纯展示标签,不参与任何指标计算。**单视图口径规则约束的是客户双指标本身,无论它在哪个端点暴露**:`/analytics/comprehensive` 等综合分析端点对 `scope_mode=all` **硬拒绝**(专门页,本应先选视图);但 `/projects/{id}/analytics` 这类客户双指标只是载荷一部分(还有资产/合同关系/收付款/风险/收入拆分)的高频端点,改**靶向抑制**——`all` 口径下只把客户双指标字段置空并带「需选 owner/manager 视图」标记,其余按 `party_filter` 照常渲染,不整体 400 拒绝(否则多绑定用户每次开项目页都被顶回,UX 退化)。收入拆分按合同 `revenue_mode` 派生、perspective-safe,不受 `all` 影响照常显示;只有会跨视图重复计数的客户双指标需要抑制。**现状缺口**(待收口):综合分析端点已装 `_assert_analytics_customer_metrics_perspective` 守卫,项目分析端点漏装,`all` 口径下客户双指标在 owner+manager 混合下被算出——正是本规则禁止的。**与档案计数区分（2026-06-19 grill）**:单视图铁律只管**分析端点**会跨视图重复计数的客户双指标聚合;`CustomerProfile.historical_contract_count` 是 per-客户的**列表/档案字段**,按数据范围并集、合同 ID 去重计(同一合同两绑定均可见也只算一次),`all` 模式合法、**不套**单视图拒绝。配套:`CustomerProfile.binding_type` 增 `all` 值(经 owner+manager 两绑定均可见的并集行),列表按 `customer_party_id` 去重一客户一行。别把列表并集口径与分析单视图口径互相套用(同列表「内部」≠ 指标「内部」的双义陷阱)。
_Avoid_: 客户指标用 all 混视图、项目分析端点漏视图守卫、项目页客户指标整体硬拒绝退化 UX、抑制收入拆分、内部对手方计入经营客户数、列表口径=分析口径、用 customer_type 标签过滤经营指标、把列表「内部」等同指标「内部」、historical_contract_count 套单视图拒绝、把档案计数当分析客户双指标、binding_type 单值无 all、all 列表按 binding 分行

**主体历史身份（不合并 + 名称快照固化）**:
主体的**当前身份可变**（Party 主档 live，改名即时生效），但**历史引用的身份事实冻结**——分层承载，同 [[台账（账本语义·归属固化）]] 的历史冻结哲学（ADR-0011）。两条决策（ADR-0020）：(1) REQ-PTY-001 的**「去重」= 建档时查重提示**（防重复建档），MVP **不做主体合并**——[[客户双指标（视图口径 + 仅终端客户）]] 按 Party **live 去重**，合并会回溯改写历史客户主体数/合同数、撞 §3.2，合并归 vNext（届时须先定义是否回溯改写历史口径）。(2) §6.1「签署时主体名称快照」由 Contract `lessor_name_snapshot` / `lessee_name_snapshot` 承载，定稿（补录即生效 / `finalize_correction`）时从当时主档名固化、**不随主档改名回写**，覆盖全部合同类型；`LeaseContractDetail.tenant_name` 退为展示冗余、从快照同步，不另立真相轴。客户**列表**显示当前主档名、**历史合同详情**显示快照名——§6.1 两句各落对应字段。实现已落地：Contract 快照列、定稿写入/纠错定稿刷新、存量回填与 `tenant_name` 同步均已收口；MVP 仍无主体合并端点。
_Avoid_: MVP 合并主体、去重=合并已存在主体、合并回溯改写历史客户口径、名称快照无字段、快照只放 LeaseContractDetail.tenant_name、快照随主档改名回写、历史合同显示当前主档名

**候选匹配**:
系统根据扫描件解析出的主体名称、权利人名称、地址或坐落，在已审核 Party 和既有 Asset 中推荐可能匹配对象；候选匹配只辅助用户选择，不因高置信度自动写入 Party 或 Asset 引用。
_Avoid_: 自动绑定、自动选择主档

**合同上下游（收入/成本标记）**:
合同的 `group_relation_type`（上游/下游/委托/直租）只是**收入与成本归集的方向标记**，不是要被监管的"覆盖"门。承租模式下游租金计入收入、上游租金计入成本，代理模式直租派生服务费收入——这些**全靠每份合同的 `group_relation_type` 计算，不依赖任何上下游配对**。MVP **不做主合同覆盖风险**：终端合同是否被主合同在资产范围/期间上盖全，不计算、不提示、不阻断。
**MVP 删除 `ContractRelation` 逐对配对表**：上下游不再记录"这份下游具体对应哪份上游"，只靠每份合同的 `group_relation_type` 表达方向；投影的 `primary_contract_ids` / `terminal_contract_ids` 改为按组内 `group_relation_type` 派生（上游/委托 → primary，下游/直租 → terminal），不再逐条配对。逐对配对唯一不可替代的能力（逐租约毛利/分润匹配、逐对覆盖）正是 MVP 不做的；保留它只会给补录增加"指定对哪份上游"的纯摩擦。`ContractRelationType.RENEWAL`（续签关系）随表一并删除，对齐"MVP 不提供续签"。逐租约毛利/分润若将来需要，届时重建配对。
_Avoid_: 主合同覆盖、覆盖风险、missing_primary_contract、coverage_conflict、上下游覆盖门禁、按配对算收入成本、ContractRelation 配对表、续签关系、RENEWAL、补录时手填上下游配对

**当前成本/当前收益（沿归属链汇总）**:
盈亏不靠"上游合同↔下游合同配对"表达，而是沿 **合同 → 资产 → 项目** 的归属链自然汇总：上游/委托台账落成本，下游/直租台账落收益，在共享同一批资产/项目处相减即盈亏。**"当前成本/当前收益"只放项目级（及合同关系级），不放资产级**：资产级会被迫把多资产合同的合同级金额分摊到单个资产，违反 §6「多资产合同金额：合同级口径，不按资产分摊」。资产详情只**列出**关联合同及各自金额（清单，不拆分），不给"该资产分到多少成本/收益"的数。项目是主业务轴，项目级盈亏即可满足"这个项目现在赚是亏"。
_Avoid_: 资产级当前成本/收益、按资产分摊合同金额、靠合同配对算盈亏、资产详情显示拆分成本

**台账（账本语义·归属固化）**:
台账是**账本（历史记录）**，不是实时情况列表。每条 `ContractLedgerEntry` 钉在账期 `year_month` 上，是绑定时点的不可变财务事实（`paid`/`partial` 冻结只读，见 [[台账重算（已收条目不可变）]]、ADR-0008）；「当前欠多少、当前出租率、现在归谁」是从账本 + 当前主数据派生的**实时投影**，与账本本身是两回事。因此**条目必须自带归属**：生成（及重算新建未收条目）时把归属 `project_id` / `owner_party_id` / `operator_party_id`（来自其单项目合同组）与 `asset_ids`（取生成该条目的 `Contract.asset_ids`，留空整租则回退组 `asset_ids`，见 [[合同关系锁单项目（代理合同跨项目靠共享扫描件）]]）**固化写死**在条目上，之后 re-org（资产/合同组挪项目，见 [[运营方单一权威轴（随项目派生）]]、ADR-0010）不回写。项目/产权方维度的历史与分析口径**只读条目固化归属聚合，禁止 join 合同组/资产的当前归属回算历史**——否则把账本当实时列表读，挪个资产就把去年的项目收入改写了，正是 §3.2「关键报表口径一致性」最难查的违反。固化 `asset_ids` 只表达「这条账当时对应哪些资产」（支撑「该资产的台账历史」检索），**金额仍是合同级，不按资产分摊**（沿用 [[当前成本/当前收益（沿归属链汇总）]] 与 §6 口径）。当前归属可变（挂合同组、随 re-org 演进）与历史事实不可变（每条目自冻）分层承载。`ServiceFeeLedger` 同理，继承来源租金台账的固化归属。见 ADR-0011。
_Avoid_: 台账=实时情况列表、历史口径 join 当前 project_id/owner_party_id、re-org 改写历史台账归属、条目只带 contract_id 靠现查归属、为历史回放建资产归属时间线表、合同组有台账就冻结禁改、固化 asset_ids 按资产分摊金额

**台账重算（已收条目不可变）**:
合同更正后台账重算的真实规则:**只调整未收条目**——`unpaid` 按新条款改金额/日期、不再覆盖的月份置 `voided`(作废);**`paid`/`partial`(已收/部分已收,即 `paid_amount > 0`)条目系统不自动改写或作废**,自动跳过,以免破坏实收与收缴率口径(跳过判据是 `paid_amount > 0`,不读可漂移的状态枚举——`unpaid/partial/paid` 本身已改为按 `paid_amount` 派生,见 ADR-0019)。被跳过的已收条目的「需人工处理」**不是手工标记位**(那是 [[逾期（派生口径）]] 与产权证 `is_verified` 已踩过的覆辙):靠两条机制提醒——(1) 重算结果当场在前端列出本次跳过条目;(2) 持久**派生风险** `ledger_stale_after_correction`,当 `paid`/`partial` 条目的 `amount_due`/`due_date` 与当前合同条款重算目标不一致或已不在当前条款账期内时由数据实时派生,在合同关系与项目风险摘要展示,人工对账修正后自然消除,不能手工关闭(见 ADR-0008)。已收口后的护栏是:禁止退回到 `skipped_entries` 仅 HTTP 返回但前端不渲染、无持久派生信号的孤儿响应值模式。`voided` 仅由系统重算/更正流程写入。**MVP 没有「冲销」机制**——「冲销」原是有名无实的空壳词(会计红字反向条目,属财务总账,Out of Scope,历史上一直「留到 M3」未建),已从 domain-model §2 等处删除;已收条目如需更正由人工在台账上处理,不做自动红冲。台账的合法操作只有:重算、作废(系统写)、重建。
_Avoid_: 冲销、红字冲销、台账冲销、自动改写已收条目、自动作废已收台账、冲销状态、reversed 台账状态、已收条目陈旧手工标记位、skipped_entries 仅返回不落地、needs_manual_review 布尔字段

**逾期（派生口径）**:
台账条目「逾期」不是登记状态，而是由当前数据实时派生的口径:`due_date` 已过且未收清（`paid_amount < amount_due`）且状态非 `voided` 即逾期。用户在 [[台账重算（已收条目不可变）]] 所属的实收登记里**只登记实收金额 `paid_amount`（外加显式作废动作）**——`unpaid / partial / paid` 由 `paid_amount` 对 `amount_due` **纯派生**（`==0`→unpaid、`0<…<amount_due`→partial、`>=amount_due`→paid），不让用户手填、不存可漂移标记位；`voided` 是唯一非派生状态，仅系统重算/作废写入（见 ADR-0019）。`overdue` 同理已从登记状态枚举删除（见 ADR-0007）。减免/结清少收不靠翻状态表达（属财务总账 Out of Scope），需要时走 `amount_due` 更正。全系统的逾期统计、逾期筛选、`payment_overdue` 项目风险和逾期通知必须统一按本口径派生，不依赖人工翻状态，也不允许出现无 `due_date` 过滤的「逾期」统计。宽限期若将来需要，是给派生规则加参数（如 `due_date + N 天`），不是恢复手工标记。
_Avoid_: overdue 登记态、手工标记逾期、payment_status=overdue、手填 unpaid/partial/paid、payment_status 与 paid_amount 双写漂移、登记 paid 但 paid_amount=0、用 payment_status 表达减免结清、自动翻态定时任务、未过滤 due_date 的逾期统计

**代理服务费实收（挂钩直租租金实收派生）**:
代理模式服务费的应收/实收**不独立登记**，而是从**直租租金台账**派生（[[台账（账本语义·归属固化）]] 的 `ServiceFeeLedger`）：`amount_due = 直租租金 amount_due × service_fee_ratio`，`paid_amount` 按来源直租租金条目的实收派生，`payment_status` 按服务费金额派生（来源租金台账 `voided` 时同步为 `voided`；与 [[逾期（派生口径）]]、ADR-0019 同一「单一真相轴」原则——钱的事实只在直租租金条目登记一次，服务费全派生）。**业务依据（2026-06-19 grill）**：运营方与产权方对账，只有产权方实际收到的租金才结算代理费，故服务费实收天然挂钩直租租金实收、不旱涝保收。运营方在系统里登记的「直租租金实收」反映的是**对账确认的产权方到账结果**（非运营方自己的现金）——这解释了为何运营方会在一笔自己并非收款主体的租金上登记实收。**被否决 (B)**：服务费按独立管理费旱涝保收、自带独立实收登记轴——与对账结算现实不符，且会再开一条可漂移的实收轴，撞单一真相轴。
_Avoid_: 服务费独立实收登记、服务费旱涝保收、服务费 paid_amount 手填、服务费实收脱离直租租金实收、ServiceFeeLedger 独立 payment_status 登记轴

**消息通知（提醒非工单）**:
站内通知是面向用户的业务提醒通道，五类:合同即将到期、合同已到期、付款到期提醒、付款逾期、系统通知。通知是**提醒不是工单**——只有已读/未读，无处理闭环、无状态机、不承载待办或任务语义（与 [[催缴管理（MVP 已删除）]] 的工单外壳区分;ADR-0002 拆的是审批通知这一**类型**，不是通知**通道**本身）。逾期提醒按 [[逾期（派生口径）]] 触发，不依赖人工翻态。**幂等按档位（不是按未读、更不是按天）**:幂等键 =（接收人, 对象, 类型, **优先级档位**），同档位不重复生成，跨档位升级各发一次——逾期按 `days_overdue` 派生 NORMAL→HIGH(≥7天)→URGENT(≥30天)，合同/付款到期按剩余天数派生 30→15→7 天档位；档位由数据实时派生、不存标记位（口径同 [[逾期（派生口径）]]、ADR-0007）。这条同时修两个老毛病:(1) `check_payment_overdue`/`check_payment_due_soon` 原用 `created_since=today` **按天重发**,一条逾期天天刷屏,撞「提醒非工单」;(2) `check_contract_expiry` 原用 `require_unread=True` **纯未读幂等**,首条 30 天 NORMAL 未读就**漏发** 7 天 URGENT 升级(同 `CONTRACT_EXPIRING` 类型被去重塌缩)。两者统一为档位幂等:不刷屏、不漏升级,整个生命周期最多 ~3 条递进提醒。通知正文含合同号、租户名等业务数据，是业务数据外发渠道:接收人必须按主体绑定数据范围过滤，用户只收到其数据范围内对象的业务通知，不得全员广播。**系统通知是唯一可豁免数据范围、广播全员的类型,前提是内容中立**:仅 `admin`/`system_admin` 可发,服务端强制不挂任何业务实体(无 `related_entity_id`)、不含 PII,违反即拒绝创建;系统通知为管理员手动一次性创建,不进定时扫描、不做档位幂等(那套只管派生类业务提醒),MVP 仅站内、不推企业微信,只有已读/未读、可删除、无处理闭环。**现状缺口**:`SYSTEM_NOTICE` 枚举已声明但零生产者(无创建端点/service,只有 4 类业务扫描),是待补的幽灵类型——MVP 决定建最小生产者而非删(yellowUp 确认有公告需求),内容中立硬约束是 §8 广播豁免成立的前提。企业微信推送是可选通道,配置开关控制,**按接收人定向推企业微信应用消息(`touser`+`agentid`),不走群机器人广播**——推给本人天然落在其数据范围内,§8 自洽(原 `WECOM_WEBHOOK_URL` 群广播把业务正文外泄给全群、且每接收人推一遍 N 倍刷屏,是错误实现,删);它是**个人消息提醒不是企业微信待办**(无完成态/处理闭环,接待办应用 = 给通知接闭环,撞 [[催缴管理（MVP 已删除）]]/ADR-0006);应用消息集成(`corpid`/`agentid`/`secret` + 系统用户↔企业微信 userid 映射)接通前推送开关默认关、先只发站内(先止血)。推送失败不影响站内通知创建,不做送达重试保障(见 ADR-0016)。MVP 不做通知模板配置、用户自定义订阅规则、短信或邮件通道。
_Avoid_: 通知工单化、通知处理闭环、通知指派、全员广播业务通知、通知模板配置、自定义订阅规则、通知送达保障、按天重发刷屏、纯未读幂等漏发升级、created_since=today 去重、档位塌缩、企业微信群机器人推业务正文、WECOM_WEBHOOK_URL 群广播、企业微信待办闭环、每接收人推一遍企业微信、系统通知挂业务实体、系统通知含 PII、非 admin 发系统通知、系统通知走扫描/档位幂等、SYSTEM_NOTICE 幽灵枚举无生产者

**资产确认（两步生命周期）**:
资产数据是否已被确认的质量控制，承载于 `Asset.review_status`（`draft / pending / approved / reversed`）。语义是「数据已确认，可对外展示或用于计算」。MVP 采用两步:制单人提交 `draft → pending`，确认 `pending → approved`；**确认仅按复核权限门控，不限制 `审核人 = 提交人`**（2026-06-10 决策取消职责互斥：任何持复核权限者均可确认，含提交人本人，同一人有权限可一人走完两步）。支持批量提交与批量审核（导入资产统一落 `draft`，批量转 `pending`、批量转 `approved`）。`review_by` 记录确认人，是可反审核再重审（`approved → reversed → pending`）的可逆循环。`approved` / `reversed` 状态下禁止编辑业务字段，须先反审核。它**不依赖也不再使用** [[资产审批流（MVP 已移除）]] 那套流程引擎，只是 `review_status` 字段上的轻量两步控制。**MVP 不做职责互斥**；vNext「可配置职责分离体系」如将来需要再引入可配置互斥矩阵。Party 同样是权限门控的轻量生命周期（[[轻量提交标记]]），与资产的差异只在是否可反审，不在职责互斥；**Project 无 review 生命周期**（孤儿列已删，见 ADR-0014）。
_Avoid_: 审核人≠提交人硬约束、职责互斥、反共谋、走 ApprovalInstance、draft 直连 approved（仍须经 pending）、逐条手工复核大批导入

**轻量提交标记**:
**Party** 的 `review_status`（draft/pending/approved/rejected）表达「这条主体主档是否已提交确认」，是权限门控的轻量生命周期，单人有权限即可完成，不校验审核人≠提交人——与 [[资产确认（两步生命周期）]] 在职责互斥上一致（都不互斥）。Party review **真承重**：编辑/删除门禁（待审/已审不可改不可删）+ `assert_parties_approved`（合同补录、产权证权利人引用前要求关联主体全部已审核，背 REQ-PTY-002）。**Project 不在此列——其 review 生命周期是零消费孤儿列，MVP 已删（见 ADR-0014）**；项目只是资产的业务管理归集桶，无下游要求「项目已审」。Asset 用可逆 `reversed`（台账/统计事实底座，留反审重审闭环），Party 驳回后保留 `rejected` 态，可编辑后重新提审、不提供反审闭环——差异只在是否可反审，不在职责互斥。
_Avoid_: 主体审核人≠提交人、Project 有 review 生命周期、Project 支持驳回/反审、三对象 review 必须统一、Party review 是孤儿可删

**资产审批流（MVP 已移除）**:
曾经的 REQ-APR-001「资产审批流第一阶段」及 `ApprovalInstance` / `ApprovalTaskSnapshot` / `ApprovalActionLog` 路由审批机制，MVP 已决定**彻底删除**(连表带代码及 `notification_service` 的审批通知一并拆除)，资产不再有独立的路由审批流。删除而非冻结的原则:接入了外部副作用(通知)的死代码要拆干净，区别于纯静态骨架的 [[权属方管理]]（冻结保留）。资产的管控由 [[资产确认（两步复核）]] 承担;变更级审批(出租/出售/调价分别路由)若需要，属 vNext 流程引擎能力，届时重建。
_Avoid_: 资产路由审批、ApprovalInstance、对象级审批留在 MVP、冻结而非删除
