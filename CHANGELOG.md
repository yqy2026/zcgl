# 变更日志 (Changelog)

## [Unreleased] - 2026-03-06

### 2026-03-21
- fix(ci): 修复 `Frontend E2E Tests` 最后一处 `access-denied` 红灯。`frontend/tests/e2e/security/access-denied.spec.ts` 中的普通用户权限拒绝用例此前假设数据库里必然已有一个非管理员角色；在全新 CI 数据库上该假设不成立，导致 Chromium/Firefox/WebKit 三端统一报 `No non-admin role is available for access-denied E2E.`。现已将用例改为自给自足：若 `/api/v1/roles` 列表里不存在任何非管理员角色，则先以管理员会话调用 `/api/v1/roles` 创建一个最小 global role，再继续创建普通用户并执行 admin-only API 拒绝断言。验证：`cd frontend && pnpm type-check:e2e`（PASS）。
- fix(ci): 修复 import 前端 E2E 运行态的 `pdf_import_sessions` PostgreSQL enum 漂移。`backend/src/models/pdf_import_session.py` 中 `PDFImportSession.status/current_step` 与 `SessionLog.step` 之前使用了默认原生 `Enum(...)` 映射，运行中的 async SQLAlchemy/asyncpg 会按 `sessionstatus` / `processingstep` 原生 enum 类型发送参数，而现有 Alembic 基线只创建了字符串列，导致 import 前端链路触发 `type "sessionstatus" does not exist` 并间接把 `/api/v1/pdf-import/upload` 打成 503。现已改为 `native_enum=False` 且使用 `.value` 持久化标签，与 `e4c9e4968dd7_initial_schema_creation.py` 中 `status/current_step/step` 的 `String(30/50)` DDL 保持一致；新增模型级回归 [test_pdf_import_session_model.py](/home/y/projects/zcgl/.worktrees/ci-baseline-restoration/backend/tests/unit/models/test_pdf_import_session_model.py) 锁定这 3 个列的非原生 enum + 小写 value 绑定策略。验证：`cd backend && uv run pytest tests/unit/models/test_pdf_import_session_model.py --no-cov -q`（3 passed）；本地静态检查枚举绑定结果已确认为 `native_enum=False` 且 `bind(sample)` 输出 `uploading/file_upload`。
- fix(ci): 继续修复 PR #44 的前端 E2E 基线。`.github/workflows/ci.yml` 的 `Frontend E2E Tests` 浏览器安装从 `chromium` 扩展为 `chromium firefox webkit`，修复 CI matrix 中 Firefox/WebKit 直接因浏览器未安装而启动失败；`frontend/tests/e2e/user/import-guardrails.spec.ts` 将两条合同 Excel 导入断言改为当前退休页语义（显式展示“租赁前端模块已退休”，且不再暴露 Excel 导入入口），`frontend/tests/e2e/legacy-contract/import-success.spec.ts` 删除已退休的 Excel 成功链路，只保留仍存在的 PDF 导入成功路径；`frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 的首条成功用例改为 mock `confirm-import` 与列表查询，稳定验证前端跳转与列表展示，不再与后端导入 E2E 重复耦合；`Makefile` 与 `scripts/dev/run_import_e2e.sh` 的前端 import 套件路径同步从已删除的 `tests/e2e/rental/import-success.spec.ts` 切换为 `tests/e2e/legacy-contract/import-success.spec.ts`；`backend/tests/unit/config/test_ci_workflow.py` 新增回归，锁定 import 前端 spec 路径必须真实存在、frontend-e2e job 必须安装完整浏览器矩阵。验证：`cd backend && uv run pytest tests/unit/config/test_ci_workflow.py --no-cov -q`（6 passed）；`cd frontend && pnpm type-check:e2e`（PASS）；`bash -n scripts/dev/run_import_e2e.sh`（PASS）；本地以临时 `zcgl_import_e2e_test` 库执行 `scripts/dev/run_import_e2e.sh` 时 backend import E2E `23 passed`，前端 Playwright 本地复跑受 dev server 未成功监听 `127.0.0.1:5173` 影响未形成有效结论，最终以 GitHub Actions 新一轮结果为准。
- fix(ci): 修复 PR #44 第二轮 E2E 阶段红灯。`.github/workflows/ci.yml` 中 `backend-e2e`、`frontend-e2e`、`import-e2e` 的测试数据库统一改为 `zcgl_e2e_test` / `zcgl_import_e2e_test`，与 `backend/tests/conftest.py` 的“仅允许 *_test 数据库被 destructive reset/seed”安全守卫对齐，恢复 E2E 前置的 schema reset 与基线枚举种子初始化；`Makefile` 的 `test-e2e-import-backend` 与 `scripts/dev/run_import_e2e.sh` 同步移除对已删除 `tests/e2e/test_excel_import_e2e.py` 的陈旧引用；`backend/tests/unit/config/test_ci_workflow.py` 新增回归，锁定 E2E job 使用 dedicated `*_test` 数据库名、import E2E 目标仅引用真实存在的后端 spec。验证：`cd backend && uv run pytest tests/unit/config/test_ci_workflow.py --no-cov -q`（4 passed）；`make test-e2e-import-backend`（不再报 missing file，23 skipped）；临时 PG16 + `zcgl_e2e_test` 环境下 `cd backend && TEST_DATABASE_URL=... E2E_TEST_DATABASE_URL=... uv run pytest tests/e2e/test_asset_lifecycle_e2e.py::test_asset_complete_lifecycle_e2e --no-cov -q`（1 passed）。
- fix(ci): 继续收口 PR #44 的 workflow 配置漏口。`.github/workflows/ci.yml` 为所有会执行 `uv run alembic upgrade head` 的 job（`backend-test`、`backend-e2e`、`make-quality-gate`、`frontend-e2e`、`import-e2e`、`integration`）统一补齐 CI 专用 `SECRET_KEY` 与 `PHASE4_TENANT_NOT_NULL_DECISION=B`，修复测试数据库初始化先后被 `Settings.SECRET_KEY` 缺失和 Phase4 tenant 决策门阻断的问题；同时将前端覆盖率检查、artifact 上传和 Codecov 路径从失效的 `frontend/coverage/*` / `frontend/test-results.json` 改为 `test-results/frontend/{coverage,reports}/...`，与 `frontend/vitest.config.ts` 的真实输出目录对齐；新增 `backend/tests/unit/config/test_ci_workflow.py` 回归测试锁定上述约束。验证：`cd backend && uv run pytest tests/unit/config/test_ci_workflow.py --no-cov -q`（2 passed）；GitHub Actions 失败日志已确认原始根因分别为 `SECRET_KEY` 缺失、`PHASE4_TENANT_NOT_NULL_DECISION` 缺失链，以及 `coverage/coverage-summary.json` 不存在。
- docs(plan): 新增 `docs/plans/2026-03-21-ci-baseline-restoration-plan.md`，将当前 CI 基线恢复拆为独立工作流：先修 `.github/workflows/security.yml` 中失效的 Trivy action pin，再按 <=20 文件批次治理 `backend/src/**` 的 Ruff format 漂移与 `frontend/src/**` 的 Oxfmt 漂移；同步更新 `docs/plans/README.md` 索引，明确该修复需独立 PR 进行，避免与 `pr/2026-03-17-perspective-final` 的业务补丁混改。
- docs(plan): `2026-03-21-ci-baseline-restoration-plan.md` 已在实施完成后移入 `docs/archive/backend-plans/`，`docs/plans/README.md` 同步从活跃方案移除并登记为 ✅ 已完成，满足活跃方案目录只保留进行中/搁置项的治理规则。
- fix(ci): `security.yml` 中的 Trivy workflow pin 已从 GitHub 无法解析的 `aquasecurity/trivy-action@0.24.0` 切换为 commit-pinned `57a97c7e7821a5776cebc9bb87c984fa69cba8f1`（对应 `v0.35.0`），保留现有 `scan-type/scan-ref/format/output` 输入不变，恢复 Security Scanning 工作流的可解析性。
- style(backend): 已按 6 个批次清理 `backend/src/**` 的 Ruff format 基线漂移，共修正 120 个后端源码文件；本地验证 `cd backend && uv run ruff format --check src && uv run ruff check src` 已通过，消除 `Backend Lint & Type Check` 在 formatting step 的既有红灯。
- style(frontend): 已修正 `frontend/src/**` 下 4 个 Oxfmt 漂移文件：`src/components/Analytics/AnalyticsStatsCard.tsx`、`src/pages/Assets/AssetAnalyticsPage.tsx`、`src/services/analyticsExportService.ts`、`src/services/__tests__/analyticsExportService.ana001.test.ts`；本地验证 `pnpm format:check && pnpm lint && pnpm type-check && pnpm type-check:e2e && pnpm check:route-authz` 已通过。
- test(legacy-contract): 修正 3 个 legacy-contract 退休测试文件的过时断言，统一将“旧包已退休”定义为“模块完全不存在”或“仅保留空 namespace 占位”两种状态都可接受，消除测试对已删除 `src.api.v1.rent_contracts`、`src.services.rent_contract`、`tests.integration.services.rent_contract` 的自相矛盾要求。验证：`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_legacy_contract_runtime_retired.py tests/unit/services/legacy_contract/test_legacy_main_service_retired.py tests/unit/services/legacy_contract/test_package_hygiene.py`（20 passed）。
- fix(ci): 继续收口 backend `mypy` 门禁，修复 15 个后端文件中的类型债务，包括：PDF 渲染可选依赖的 `Callable | None` 注解、合同/资产历史与审核日志 ORM 构造、资产计数查询对真实结果与 AsyncMock 的兼容读取、主体/资产 service 的 nullable 与 tuple key typing、PDF 导入 confirm payload 的必填 schema 字段补齐，以及合同/项目/资产 API route 的返回注解与 response_model 对齐。验证：`cd backend && uv run mypy src/ --config-file=pyproject.toml`（PASS）；`DATABASE_URL="postgresql+psycopg://user:pass@localhost:5432/zcgl_test" TEST_DATABASE_URL="postgresql+psycopg://user:pass@localhost:5432/zcgl_test" make check`（PASS）。
- fix(ci): 继续收口 backend `Bandit` 门禁，修复/显式约束 13 个后端文件中的静态安全告警：枚举 children 清理逻辑移除 `except: continue`，缓存/追踪器/资产导入移除 `assert` 依赖，认证服务用 `AuthTokenKind` 消除 token string 误报，`pdf_to_images` 改为记录 debug 日志而非 `except: pass`，备份服务对 `subprocess` 使用改为带解释的精确 nosec，迁移脚本中的动态 SQL 统一增加标识符校验并在必要处改写为 SQLAlchemy 表达式。验证：`cd backend && uv run bandit -r src -f txt`（No issues identified）；`DATABASE_URL="postgresql+psycopg://user:pass@localhost:5432/zcgl_test" TEST_DATABASE_URL="postgresql+psycopg://user:pass@localhost:5432/zcgl_test" make check`（PASS）。

### 2026-03-12
- feat(party): 完成 REQ-PTY-001/002 本轮主体主档 Gap 修复闭环。后端已收口 `PartyReviewStatus` 为 `draft/pending/approved/reversed`、驳回回草稿、`PENDING/APPROVED` 编辑/删除守卫、`deleted_at` 软删除、`PartyReviewLog` 审计日志、删除前资产/合同/租赁引用保护，以及 `(party_type, code)` 友好去重；前端新增系统管理入口 `/system/parties` 与主体详情页 `/system/parties/:id`，补齐主体列表、详情编辑、提审/通过/驳回操作和菜单/面包屑/路由配置；新增前端回归测试 `frontend/src/pages/System/__tests__/PartyPages.test.tsx`、`frontend/src/services/__tests__/partyService.test.ts`，并同步 SSOT 需求证据与方案归档。
- feat(analytics-ui): ANA-001 经营口径字段前端完整对接。`AnalyticsStatsCard.tsx` 新增 `RevenueStatsGrid` 组件（总收入/自营租金收入/代理服务费收入/客户主体数/客户合同数 + 口径版本 Tag）；`AssetAnalyticsPage.tsx` 在财务指标下方新增「经营口径」Card；`useAssetAnalytics.ts` 导出数据补入 6 个 ANA-001 字段；`analyticsExportService.ts` Excel/CSV/HTML 三种导出格式全部补入经营口径数据。TypeScript 类型检查通过。
- feat(asset): 完成 REQ-AST-003 资产审核与反审核闭环。新增 `AssetReviewLog` 审计日志模型与迁移 `backend/alembic/versions/20260311_req_ast_003_asset_review.py`，资产审核状态从 `draft/pending/approved/reversed` 完整流转，`backend/src/services/asset/asset_service.py` 增加提审/通过/驳回/反审核/重提审、审核日志查询，以及 `APPROVED/PENDING` 编辑与删除守卫；`backend/src/api/v1/assets/assets.py` 新增 6 个审核相关端点；合同提审在 `backend/src/services/contract/contract_group_service.py` / `backend/src/api/v1/contracts/contract_groups.py` 增加资产审核软警告并通过 `X-Asset-Review-Warnings` 返回；review follow-up 继续补齐两项稳定性修复：5 个资产审核动作统一把 `StaleDataError` 转换为 409 冲突语义，合同组批量提审返回聚合后的 `warnings` 列表，避免软警告静默丢失；SSOT 文档与方案归档同步更新。新增测试：`backend/tests/unit/services/asset/test_asset_review.py`、`backend/tests/unit/api/v1/test_asset_review_api.py`、`backend/tests/unit/models/test_asset_review_status.py`、`backend/tests/unit/migration/test_req_ast_003_asset_review_migration.py`。

### 2026-03-11
- docs(plans): REQ-AST-003 资产审核与反审核技术方案——状态机 5 条转换（DRAFT→PENDING→APPROVED、PENDING→DRAFT 驳回、APPROVED→REVERSED 反审核、REVERSED→PENDING 重提审）+ 编辑/删除守卫（APPROVED/PENDING 禁止编辑和删除）+ `AssetReviewLog` 审计日志表（含 context JSON）+ 合同提审资产软警告（`submit_review` 改返回 tuple + X-Asset-Review-Warnings header）+ `AssetReviewStatus.REJECTED→REVERSED` 对齐合同侧命名 + 合同保持实时引用资产不做快照 + 反审核不联动合同状态；15 个文件涉及（6 新建 + 9 修改）；28 个测试用例（T-01~21 service + TA-01~05 api + TM-01~02 model）。复核修正 6 个问题（P1~P6）。需求状态 📋→🚧。
- fix(ledger): 继续收口批量更新状态约束——`batch_update_status()` 在 service 层新增完整支付状态白名单，内部调用者不能再绕过 API schema 写入任意未知状态。
- test(ledger): 补充聚合查询 service 边界回归——新增 `year_month_start > year_month_end` 单测，锁定 service 层账期范围校验不回退。
- fix(ledger): 收口台账 review follow-up——`query_ledger_entries()` 在 service 层补“至少一个核心筛选条件 + 账期范围”校验，`batch_update_status()` 在 service 层禁止 `voided` 绕过，REQ 代码证据中的旧批量更新路径同步修正。
- feat(ledger): 完成 REQ-RNT-006 M2 台账跨合同聚合查询与变更重算——新增 `GET /api/v1/ledger/entries` 与 `POST /api/v1/contracts/{contract_id}/ledger/recalculate`，`payment_status` 新增系统保留值 `voided` 并禁止批量接口直接写入；`ledger_service_v2` 补充聚合查询与重算能力，需求证据/字段附录同步更新，新增 `test_ledger_aggregate_query.py`、`test_ledger_recalculate.py`、`test_ledger_api.py`。
- docs(plans): 台账 M2 技术方案复核修订——修复 voided 唯一约束冲突、补充触发机制说明、完善 include_voided/due_date/路由注册/筛选约束，测试用例 15→19
- docs(plans): 台账 M2 方案二次收口（小米 review）——文件清单补 req-spec/CHANGELOG、重算补 due_date 刷新、路由注册链路精确描述、voided 设为系统保留状态，测试用例 19→22

### 🛠️ 本次变更 (Current Changes)

- 测试基建回归修复（2026-03-11）：① `backend/tests/e2e/factories.py` 的资产创建工厂已从旧 `address` 输入切换为当前口径要求的 `address_detail`，修复资产生命周期 e2e 在创建阶段返回 `422` 的问题；② `backend/tests/conftest.py` 与 `backend/tests/unit/conftest.py` 的 PostgreSQL schema reset 现在会先终止同库其他连接，再执行 `DROP SCHEMA public`，避免测试库清理被锁等待；③ `backend/tests/e2e/conftest.py::db_tables` 不再重复执行 Alembic/`create_all`，避免与根级 `setup_test_database` 预热流程互相打架，修复单条 e2e 运行时的重复迁移问题。验证：`tests/e2e/test_asset_lifecycle_e2e.py::test_asset_complete_lifecycle_e2e` 在显式测试库环境下通过。

- 迁移枚举 cast 回归修复（2026-03-11）：修复 `backend/alembic/versions/20260306_m2_legacy_contract_backfill.py` 在 PostgreSQL 上执行 legacy backfill 时，`CASE` 表达式返回 text 导致 enum 列写入失败的问题。对 `revenue_mode`、`contract_direction`、`group_relation_type`、`status`、`review_status`、`relation_type` 六处表达式补显式 `::enum_type` cast，并新增回归测试 `backend/tests/unit/migration/test_contract_group_legacy_backfill_migration.py::test_upgrade_sql_should_cast_case_results_to_enum_types` 锁定 SQL 语义，防止全新库迁移链再次在该步骤失败。

- 合同组切换阻断问题彻底修复（2026-03-11，review follow-up）：1) 新增 Alembic 数据迁移 `backend/alembic/versions/20260306_m2_legacy_contract_backfill.py`，将 legacy `rent_contracts / rent_contract_assets / rent_terms / rent_ledger` 回填进新 `contract_groups / contracts / lease_contract_details / agency_agreement_details / contract_assets / contract_group_assets / contract_rent_terms / contract_ledger_entries / contract_relations`，为历史升级库补齐仓库可见的 M1b 路径；2) `20260307_m2_collection_records_contract_fk.py` 改为先重写 `collection_records.ledger_id/contract_id` 到新体系、再校验引用完整性、最后重建 FK，避免 legacy `rent_ledger`/`rent_contracts` 引用在升级时直接炸掉；3) `20260307_m2_contract_number_on_contracts.py` 改为“先加可空列 -> 从 legacy 合同回填 -> 兜底生成 `AUTO-<contract_id>` -> 再收紧 NOT NULL + unique index”，修复非空库升级失败；4) 前端 PDF 导入确认页与 `pdfImportService` 新增 `agency_detail` 类型/表单/payload，`AGENCY` 模式可显式提交 `service_fee_ratio / fee_calculation_base / agency_scope`，后端单测同步锁定正向导入链路；5) 新增迁移单测 `test_contract_group_legacy_backfill_migration.py`、`test_collection_records_contract_fk_migration.py`、`test_contract_number_on_contracts_migration.py`，并扩充 `frontend/src/pages/Contract/__tests__/ContractImportReview.confirm-context.test.tsx`、`frontend/src/services/__tests__/pdfImportService.test.ts`、`backend/tests/unit/services/document/test_pdf_import_service.py`；6) legacy backfill 进一步按新运行时语义收口：代理组内 `LEASE_DOWNSTREAM` 现映射为 `DIRECT_LEASE`，合同关系映射为 `AGENCY_DIRECT`，并在迁移末尾加 fail-closed 断言，阻止代理/承租语义错配数据落库；7) `ContractImportReview` 对 `fee_calculation_base` 改为严格枚举校验，提交链路不再把非法值静默回退成 `actual_received`。

- REQ-PTY-001 / REQ-PTY-002 最小闭环（2026-03-11）：① `Party` 主档新增审核字段 `review_status/review_by/reviewed_at/review_reason`，创建默认 `draft`；② 新增主体审核接口 `POST /api/v1/parties/{party_id}/submit-review`、`approve-review`、`reject-review`；③ `ContractGroupService.submit_review()` 新增合同提审前主体审核门禁，校验 `lessor/lessee + operator/owner` 关联主体全部为 `approved`，否则阻断提审；④ 新增失败测试锁定门禁与审核流：`test_party_service.py`、`test_contract_group_service.py`、`test_party_api.py`。

- REQ-ANA-001 最小闭环（2026-03-11）：① `AnalyticsService` 新增经营口径计算：仅统计 `ACTIVE` 且主体审核通过的合同；按 `ContractGroup.revenue_mode` + `group_relation_type` 计算 `total_income / self_operated_rent_income / agency_service_income`；按 `customer_party_id`/`contract_id` 计算客户双指标；返回 `metrics_version=req-ana-001-v1`。② `/api/v1/analytics/comprehensive` 与 `/api/v1/analytics/export` 现在都包含新口径字段；CSV 导出内容中可见版本标识。③ 前端 `analytics` 类型与服务适配已补充上述字段，保持旧页面兼容。④ 新增/更新测试：`test_analytics_service.py`、`test_analytics.py`。

- SSOT 残差复核修订（2026-03-11）：① `docs/requirements-specification.md` 将 `REQ-AST-002` 追踪矩阵状态从 `🚧` 同步为 `✅`，并补足代码证据到 `asset_management_history.py` / `asset_service.py` / `assets.py` 的历史关系链路；② `docs/features/requirements-appendix-fields.md` §7.1 项目遗留工程字段拟处理策略统一改为“直接下线删除（当前 As-Built 仍存在，待物理收口）”，消除与正文 `REQ-PRJ-001` 的口径冲突。


- REQ-RNT-003 验收收口（2026-03-11）：① `requirements-specification.md` 将 REQ-RNT-003 状态从 🚧 改为 ✅，补全代码证据（`ContractLifecycleStatus` 枚举 + 6 个生命周期端点 + `ContractAuditLog` + `calculate_derived_status` + 组批量提审）；② 追踪矩阵 REQ-RNT-003 行同步更新为 ✅；③ 清理 REQ-AST-002 代码证据中的重复条目。

- REQ-RNT-006 台账 M2/M3 分期定义（2026-03-11）：① `requirements-specification.md` REQ-RNT-006 验收拆分为 M2（初次生成 + 跨合同聚合查询 + 批量状态更新 + 变更重算）和 M3（导出 + 补偿任务 + 代理服务费台账），字段映射更新为 §3.10 ContractLedgerEntry 口径；② §10.1 里程碑 M2 改为"合同生命周期 + 台账核心正确性"，M3 追加"台账运营增强"；③ `requirements-appendix-fields.md` §11.2 补充 M2/M3 分期说明（变更重算/聚合查询/导出/补偿任务/服务费台账）。
- REQ-AST-002 资产关系可追踪落地（2026-03-11）：五项 Gap 修复。① `Asset.project` relationship 加 `valid_to IS NULL` 过滤，确保只返回当前有效项目绑定（不再随机取历史行）。② `ProjectAsset` 新增 partial unique index `uq_project_assets_active_asset`（`asset_id WHERE valid_to IS NULL`），数据库层面保证同一时刻一个资产只有一个活跃项目。③ `update_asset` 新增经营方变更自动留痕：检测 `manager_party_id` 变化时，自动关闭旧 `AssetManagementHistory` 记录并创建新记录。④ 新增 `CRUDAssetManagementHistory`（`crud/asset_management_history.py`）提供 `create`/`close_active`/`get_by_asset_id` 三个方法。⑤ 新增两个关系历史查询 API：`GET /api/v1/assets/{id}/management-history`、`GET /api/v1/assets/{id}/project-history`，均受 ABAC 资产读权限守护。Alembic 迁移 `20260311_req_ast_002_active_project_unique`。9 个单元测试覆盖全部 Gap，3571 个既有测试无回归。

- 六项口径歧义消歧 + SSOT 全量对齐（2026-03-11）：跨 `requirements-specification.md` 与 `requirements-appendix-fields.md` 统一口径。① REQ-RNT-003：合同组改为纯容器无独立生命周期，`derived_status` 由组内合同派生（`筹备中/生效中/已结束`），审核在合同级进行，对齐附录 §8。② REQ-RNT-006：台账触发粒度从"合同组待审→生效"改为"合同 `status → 生效` 时由 `approve()` 同事务触发"，对齐附录 §11.2。③ REQ-AST-002：明确资产同一时点只允许一个当前有效项目、一个主产权主体，其余为历史关系；附录 Asset 字段 `project_ids` → `project_id`(string)、`owner_party_ids` → `owner_party_id`(string)，§6.1/§6.3/§6.4/§6.5 全部同步收敛。④ REQ-ANA-001：补充客户双指标去重口径，`customer_entity_count` 按 `customer_party_id` 去重，`customer_contract_count` 按 `contract_id` 去重；附录 §3.9 同步标注。⑤ REQ-PTY-002：追加验收条件——草稿 Party 允许挂在草稿合同/合同组上，但合同进入 `待审` 时系统校验所有关联 Party 必须 `已审核`，否则阻断提审。⑥ Q3（视角为强过滤）经核实正文已明确，无需修改。⑦ REQ-RNT-004：标题从"合同组主审与关键合同联审"改为"关键合同联审"，描述对齐"审核在合同级进行"口径；§15 访谈冻结结论中审核策略同步修订。

- M2 旧租赁前端入口退休第一批（2026-03-07）：新增 `frontend/src/pages/Rental/LegacyRentalRetiredPage.tsx` 作为显式退休页；`frontend/src/routes/AppRoutes.tsx` 已将 9 个旧 `/rental/*` 路由统一切到该页，并通过 `capabilityGuardBypass` 断开对旧 `rent_contract/ledger` 权限资源的运行时依赖；新增路由守卫测试 `frontend/src/routes/__tests__/AppRoutes.legacy-rental-retired.test.ts`，并同步收窄 `frontend/src/routes/__tests__/AppRoutes.authz-metadata.test.ts` 的旧租赁权限断言。

- M2 旧租赁前端入口退休第二批（2026-03-07）：导航配置已收口到退休态。`frontend/src/config/menuConfig.tsx` 的租赁菜单从 4 个旧入口缩减为 1 个“旧租赁前端已退休”入口，`getSelectedKeys()` 会把所有 `/rental/*` 路径折回 `/rental/contracts`；`frontend/src/config/breadcrumb.ts` 已将旧租赁列表、导入、详情、续签、台账、统计等面包屑统一改为退休态文案；新增回归测试 `frontend/src/config/__tests__/rental-retired-navigation.test.ts` 锁定菜单、选中态和面包屑不再暴露旧功能入口。

- M2 旧租赁前端入口退休第三批（2026-03-07）：继续切断活跃运行时消费者。`frontend/src/hooks/useSmartPreload.tsx` 已删除旧 `/rental/contracts` 预热配置与对应懒加载入口，避免继续预拉取已退休页面；`frontend/src/pages/System/NotificationCenter.tsx` 的合同类通知点击行为已从旧合同详情跳转改为显式提示“旧租赁前端入口已退休，请改走新合同流程”，资产类通知仍保持正常跳转；新增/更新测试 `frontend/src/hooks/__tests__/useSmartPreload.test.tsx`、`frontend/src/pages/System/__tests__/NotificationCenter.legacy-contract-navigation-retired.test.tsx` 锁定这两处不再回退。

- M2 旧租赁前端入口退休第四批（2026-03-07）：`frontend/src/services/apiHealthCheck.ts` 的关键接口探针已从旧 `/rental-contracts/contracts` 切到新 `/contract-groups`，避免监控面板继续把已退休合同列表当作核心健康检查依赖；新增测试 `frontend/src/services/__tests__/apiHealthCheck.contract-groups.test.ts` 锁定新旧探针切换不回退。

- M2 旧租赁前端入口退休第五批（2026-03-07）：`frontend/src/pages/System/TemplateManagementPage.tsx` 已把“租赁合同导入模板”显式标记为 `deprecated`，描述改为退休提示；点击该模板下载时不再调用旧 `rentContractExcelService`，而是直接提示“旧租赁合同导入模板已退休，请改走新合同导入流程”；新增测试 `frontend/src/pages/System/__tests__/TemplateManagementPage.legacy-contract-template-retired.test.tsx` 锁定合同模板退休而资产模板下载链路保持可用。

- M2 旧租赁前端入口退休第六批（2026-03-07）：`frontend/src/pages/Ownership/OwnershipDetailPage.tsx` 已删除对旧 `rentContractService` 的运行时依赖，不再请求旧合同列表与权属财务统计接口；页面中的“关联合同”和财务汇总区域改为显式迁移提示，避免权属详情继续触发已退休的 `/rental-contracts/*` 链路；新增测试 `frontend/src/pages/Ownership/__tests__/OwnershipDetailPage.legacy-contract-retired.test.tsx` 锁定旧依赖不会回退且页面会展示迁移态文案。

- M2 旧租赁前端入口退休第七批（2026-03-07）：`frontend/src/constants/routes.ts` 的租赁分支配置已从“租赁管理/合同列表/租赁统计”收口为统一退休态标题 `旧租赁前端已退休`，并把对应 `permissions.resource` 从旧 `rent_contract` 切到中性的 `contract:read`，避免常量层继续向活跃运行时暴露旧合同权限语义；新增测试 `frontend/src/constants/__tests__/routes.rental-retired.test.ts` 锁定该配置不回退。

- M2 SSOT 全量审查修复完成（2026-03-06）：完成 11 项跨文档 SSOT 漂移修复。① `requirements-specification.md`：REQ-RNT-003 代码证据去死链（`lifecycle_service.py` → `contract_group_service.py`）；REQ-RNT-006 触发粒度从"合同组状态"提升到"合同 `status` 由 PENDING_REVIEW→ACTIVE"，字段映射从旧 §3.4 RentContract 更新为 Contract + ContractRentTerm + ContractLedgerEntry，M2/M3 边界显式拆分，代码证据更新为 `ledger_v2.py`/`ledger_service_v2.py`；追踪矩阵两行同步。② `docs/plans/2026-03-06-m2-contract-lifecycle-and-ledger.md`：ContractAuditLog 补 `related_entry_id` 字段；void 状态说明补"ACTIVE 合同必须先 terminate 再 void"；台账生成逻辑补步骤 1（读 LeaseContractDetail 获取 payment_cycle），完整 7 步编号；T-03 测试描述精确化（条目数 = 各 RentTerm 阶段覆盖自然月数之和）；T-08 测试描述扩展（非 DRAFT 均跳过）；`ledger_v2.py` 路由注册方式补全。③ `docs/features/requirements-appendix-fields.md`：升版至 v0.4，新增 §3.5.1 ContractRentTerm（10 字段）、§3.10 ContractLedgerEntry（13 字段）、§3.11 ContractAuditLog（12 字段）；§11.2 台账生成更新为 M2 目标表名（`ContractRentTerm`/`contract_ledger_entries`），明确代理合同跳过规则。

- M2 合同生命周期与台账方案审阅归档（2026-03-06）：新增 `docs/archive/reviews/README.md` 与 `docs/archive/reviews/2026-03-06-m2-contract-lifecycle-and-ledger-review.md`，对 `docs/plans/2026-03-06-m2-contract-lifecycle-and-ledger.md` 做系统性复核。审阅结论为暂不建议直接执行，归档记录 4 个高风险问题（`REQ-RNT-003/004/005/006` 与现行 SSOT/附录不一致或能力缩窄）及 2 个中风险问题（路由装配路径不明确、兼容策略与 0→1 约束冲突）。

- REQ-AST-004 收尾与 G1 项目页补全（2026-03-06）：① REQ-AST-004 完整实现：后端 `CRUDContract.get_active_by_asset_id` + `AsyncAssetService.get_asset_lease_summary` + `GET /api/v1/assets/{id}/lease-summary`；前端 `AssetDetailPage` 租赁情况卡片（出租率大字、按 group_relation_type 四类汇总表、客户摘要、月份切换）；11 个单元测试全部通过。② API 层 `normalize_summary_period` 重复调用已移除，归一化职责统一到 service 层。③ 补 `test_excludes_non_active_status` 和 `test_deleted_asset_raises_not_found` 两个缺失测试用例。④ 需求文档 REQ-AST-004 状态 📋→✅，补代码证据；追踪矩阵同步；方案文件归档至 `docs/archive/backend-plans/`。⑤ **G1 项目页补全**：`ProjectDetailPage` 新增"租赁情况（合同口径）"卡片，通过 `AssetLeaseSummaryRow` 子组件对每个关联资产并行调用 `getAssetLeaseSummary`，展示上游/下游/委托/直租合同数、出租率、客户摘要（含客户数+Tooltip 展开名称），支持月份切换联动。

- 组织数据权限隔离修复计划落地（2026-03-06）：完成 `docs/plans/2026-03-02-party-scope-isolation-fix-plan.md` 全量排查与补测。① §5.1 主路径（API→Service→CRUD party scope 收敛）已全部贯通；② §5.2 全量集合端点评估：角色列表已有完整 party 过滤链路；操作日志统计为全局聚合（不需隔离）；组织架构列表/LLM Prompt 列表/用户列表确认为全局资源，在各接口 docstring 补充"Party 隔离策略"说明，防止未来误判；③ §6 补测 3 条：`test_list_parties_cross_user_isolation`（跨用户隔离）、`test_list_parties_headquarters_expands_to_manager_descendants`（headquarters 子树展开）、`test_list_parties_returns_empty_when_scope_resolver_raises`（异常 fail-closed）；同步修复 `_create_user` 辅助函数以确保多用户场景电话唯一性；全部 10 条 test_party_api 测试通过。计划状态更新为 🔄（资产/合同/项目跨主体集成测试待后续补充）。

- REQ-AST-004 方案审阅修正（2026-03-06）：深入审阅 `docs/plans/2026-03-05-req-ast-004-asset-lease-summary.md` 发现并修正 7 处与实际代码不符的错误：① 枚举类名由 `ContractStatus` → `ContractLifecycleStatus`；② 去掉不存在的 `EXPIRING`/`RENEWED` 成员，`ACTIVE_STATUSES` 改为 `{ContractLifecycleStatus.ACTIVE}`；③ `DataStatusValues.CONTRACT_NORMAL` 不存在，改用字符串 `"正常"`；④ `asset_crud.get_by_id_async` → `asset_crud.get_async(db, id=asset_id, include_deleted=True)`；⑤ 删除未定义的 `_as_decimal()`，改用 `x or Decimal("0")` 惯用写法；⑥ CRUD 查询补加 `selectinload(Contract.lessee_party)`（客户摘要取名必需）；⑦ `OUTBOUND_TYPES` 改用枚举成员，`lessee_name` 取名链补回退策略（`tenant_name` → `lessee_party.name` → `"未知租户"`）。同步：`docs/plans/README.md` AST-004 状态 ⏸→🔄；`docs/requirements-specification.md` REQ-AST-004 状态 📋→🚧，追踪矩阵同步。

- 项目与资产“非兼容”收口（2026-03-06）：按 0→1 阶段原则移除临时兼容。1) `backend/src/schemas/project.py` 在 `ProjectBase` 恢复 `project_code` 严格校验（`PRJ-<4-12位大写字母数字>-<6位序号>`），`ProjectResponse` 不再容忍 legacy code；2) `backend/src/services/asset/asset_service.py` 的 `create_asset` 移除仅传 `address` 的回退逻辑，`address_detail` 缺失时统一返回 `422/required_for_address_composition`；3) 同步更新单测：`backend/tests/unit/schemas/test_project_schema.py`、`backend/tests/unit/services/asset/test_asset_service.py`；4) 删除误提交产物 `backend/e()`、`backend/ycopg`。

- REQ-RNT-001 收尾（2026-03-06）：M1–M3 全量完成，60/60 单元测试通过（36 service + 13 model + 11 API layering）。plan 文档归档至 `docs/archive/backend-plans/2026-03-05-req-rnt-001-contract-group.md`；`docs/plans/README.md` 移除活跃条目并补入已归档索引；`docs/requirements-specification.md` REQ-RNT-001 状态修正为 ✅（含追踪矩阵第 501 行）。

- 项目关系持久化阻断回归修复（2026-03-06，review follow-up）：`backend/src/services/project/service.py` 的 `_replace_project_owner_relations` 改为安全 no-op，不再对已在 Phase4 删除的 `project_ownership_relations` 执行 `DELETE/INSERT`，从而避免项目创建/更新在已迁移库上触发 `UndefinedTable`，并同步消除 `party_id` 误写 `ownership_id` 的 ID 域错配风险。对应回归测试更新为禁止触表断言：`backend/tests/unit/services/project/test_project_service.py::TestCreateProject::test_create_project_should_not_touch_relation_table_when_party_relations_provided`、`backend/tests/unit/services/project/test_project_service.py::TestUpdateProject::test_update_project_should_not_touch_relation_table_when_party_relations_provided`。验证：`cd backend && uv run pytest --no-cov -q tests/unit/services/project/test_project_service.py -k "party_relations_provided"`（PASS）。

- 核心回归修复（2026-03-06，review follow-up）：修复项目关系表与资产地址提交两个阻断问题。1) `backend/src/services/project/service.py` 的 `_replace_project_owner_relations` 在 `party_relations` 归一化后为空时直接短路，不再执行 `delete(ProjectOwnershipRelation)`，避免迁移后 `project_ownership_relations` 已删除场景下 create/update 因无关系输入触发 `UndefinedTable`；并新增回归测试 `backend/tests/unit/services/project/test_project_service.py::TestCreateProject::test_create_project_should_not_touch_relation_table_when_party_relations_missing` 与 `backend/tests/unit/services/project/test_project_service.py::TestUpdateProject::test_update_project_should_not_touch_relation_table_when_party_relations_empty`。2) `frontend/src/components/Forms/AssetForm.tsx` 与 `frontend/src/components/Forms/Asset/AssetBasicInfoSection.tsx` 提交链路改为以 `address_detail` 为必填并写入 payload（同时保留 `address` 回填兼容），`frontend/src/types/asset.ts` 同步补齐结构化地址字段类型，新增回归测试 `frontend/src/components/Forms/__tests__/AssetForm.test.tsx::提交时应包含 address_detail 字段`。

- REQ-RNT-001 ORM 枚举存储策略修复（2026-03-06）：`src/models/contract_group.py` 全部 6 个 `Enum()` 列补充 `values_callable=lambda e: [m.name for m in e]`，使 ORM 显式声明以成员 **name**（LEASE/ACTIVE/…）存储，与迁移 DDL 字面量完全对齐。原代码依赖 SQLAlchemy 版本相关的 fallback 行为（先尝试 value 匹配再尝试 name 回退），存在版本升级后静默失效的风险；service 层虽防御性地手动传 `.name` 字符串使写操作暂时安全，但读取侧的 result_processor 仍依赖 fallback。
  - **反思：mock 测试的系统性盲区**：项目所有 CRUD 层均被 mock，SQLAlchemy 的 `bind_processor`（写入侧）和 `result_processor`（读取侧）从不实际执行，导致 ORM 配置类 bug 被完全掩盖，直到集成测试或生产才暴露。应对策略：对"mock 穿不透"的 ORM 配置，用**模型静态内省测试**（不需要真实 DB）补位。
  - 新建 `backend/tests/unit/models/test_contract_group_model.py`（13 cases）：① `TestEnumColumnStorageStrategy`（7 cases）：静态检查 6 个枚举列的 `column.type.enums` 与各 enum 成员 name 列表一致；② `TestEnumLabelsMatchMigrationDDL`（6 cases）：将 ORM 标签与迁移文件中的字面量进行交叉核对，防止手写迁移笔误偏差；③ 文件头详细记录 mock 盲区原因与解决原则，供后续新增枚举列时参考。验证：13 passed；全量 4507 passed，0 failed。

深度审查 M1+M2+M3 全链路，修复 5 类共 9 处问题，4491 unit tests passed。
  - **BUG-1（P1）`list_contracts_in_group` 绕层 + 无存在性检查**：`src/api/v1/rent_contracts/contract_groups.py` 原直接调 `contract_crud.list_by_group`，非法 group_id 返回 200 空数组。新增 `ContractGroupService.list_contracts_in_group`（先查 group 是否存在，不存在抛 `ResourceNotFoundError`），端点改为委托 service。
  - **BUG-2（P1）`delete_contract` 绕层 + 缺业务规则守卫**：原端点直接调 `contract_crud.soft_delete`，无"生效中合同不可删"的业务保护。新增 `ContractGroupService.soft_delete_contract`（存在性检查 + ACTIVE 状态拦截），端点改为委托 service。
  - **BUG-3（P2）CRUD 枚举裸字符串比较**：`src/crud/contract.py` `count_active_in_group` 中 `Contract.status == "ACTIVE"` 改为 `ContractLifecycleStatus.ACTIVE`，防止枚举实现变更时静默失效；同步导入 `ContractLifecycleStatus`。
  - **BUG-4（P2）422 测试逻辑路径错误**：`tests/unit/api/v1/test_contract_groups_layering.py` payload 使用枚举 name（`"LESSOR"/"UPSTREAM"`），导致 422 由 Pydantic schema 校验触发而非 group_id mismatch。修正为枚举 value（`"出租"/"上游"`），测试验证正确路径。
  - **BUG-5（P2）测试辅助函数 `_make_contract` 类型错误**：`group_relation_type` 被赋值为 `GroupRelationType.UPSTREAM.name`（字符串 `"UPSTREAM"`），与 service 中的枚举比较 `== GroupRelationType.UPSTREAM`（value `"上游"`）永远不等，导致 `upstream_contract_ids` 在所有 mock 测试中始终为空。修正为直接赋枚举成员，并增加 `group_relation_type` 参数以支持自定义。
  - **M-1 内联 import 移顶层**：`from fastapi import HTTPException` 和 `from ....crud.contract import contract_crud` 的函数体内联 import 统一提到模块顶层（其中 crud import 随 BUG-1/BUG-2 修复一并去除）。
  - **M-2 冗余 `model_validate`**：`list_contract_groups` 中 `ContractGroupListItem.model_validate(item)` 的 item 已是 `ContractGroupListItem`，改为直接 `.model_dump()`。
  - **M-3 schema description 有误**：`ContractGroupCreate.revenue_mode` 的 description 写 `"LEASE/AGENCY"`（枚举 name），实际 API 接受 `"lease/agency"`（value），修正为 `"lease / agency"`。
  - **M-4 可空字段无法清回 null**：`update_contract_group` 的 `if x is not None` 守卫导致客户端无法将 `effective_to/risk_tags` 等可空字段清回 null。改用 `model_fields_set` 判断字段是否被显式传入，同时保留 `settlement_rule is not None` 校验（DB NOT NULL 字段不允许清空）。
  - **分层约束测试加强**：`test_module_should_not_import_crud_directly` 改为检查全文无 `contract_group_crud`/`contract_crud`；`test_module_calls_contract_group_service_for_all_mutations` 新增对 `list_contracts_in_group` 和 `soft_delete_contract` 的断言。

- 核心回归修复（2026-03-06）：修复 2 个用户可见回归 + 1 个输入校验缺口。1) `backend/src/services/asset/asset_service.py`：`create_asset` 地址生成逻辑改为“优先使用地址子字段拼接；若子字段不足但请求含 legacy `address`，回退使用 `address`；两者都缺失才返回 422”，恢复历史 address-only 创建兼容，同时保留无地址来源 fail-fast；并新增回归测试 `backend/tests/unit/services/asset/test_asset_service.py::TestCreateAsset::test_create_asset_allows_legacy_address_without_address_detail`。2) `backend/src/schemas/project.py`：将 `project_code` 严格格式校验收敛到写入侧（`ProjectCreate`/`ProjectUpdate`），`ProjectResponse` 不再因存量 legacy code 在响应序列化阶段报错，避免升级后项目列表/详情 500；并新增回归测试 `backend/tests/unit/schemas/test_project_schema.py::test_project_response_accepts_legacy_project_code`。3) `backend/src/api/v1/rent_contracts/contract_groups.py`：创建合同组时补充 `owner_party_id` 与 `operator_party_id` 同级的主体存在性预校验，非法 owner 输入直接返回 `404/party not found`，避免误归类为 500；并新增回归测试 `backend/tests/unit/api/v1/test_contract_groups_layering.py::test_create_contract_group_404_when_owner_party_missing`。验证：`cd backend && uv run pytest --no-cov tests/unit/services/asset/test_asset_service.py tests/unit/schemas/test_project_schema.py tests/unit/api/v1/test_contract_groups_layering.py -q`（74 passed）。

- 集成回归补齐（2026-03-06，继续）：1) `backend/tests/integration/test_asset_lifecycle.py` 将 address-only 用例从“返回 422”更新为“允许创建并回退使用 legacy `address`”，新增断言覆盖创建响应与详情读取均保留该地址；2) `backend/tests/integration/api/test_project_api_real.py` 新增 `test_project_list_and_detail_should_tolerate_legacy_project_code`，验证数据库存在历史 `project_code`（非 `PRJ-*` 格式）时，`GET /api/v1/projects` 与 `GET /api/v1/projects/{id}` 仍返回 200 且可正确序列化输出。验证：`cd backend && uv run pytest --no-cov tests/integration/test_asset_lifecycle.py -k "address_only_should_use_legacy_address_fallback" -q`（1 passed）；`cd backend && uv run pytest --no-cov tests/integration/api/test_project_api_real.py -k "legacy_project_code" -q`（1 passed）。

- 集成回归补齐（2026-03-06，继续）：新增 2 条边界用例。1) `backend/tests/integration/test_asset_lifecycle.py::TestAssetLifecycle::test_asset_create_with_blank_legacy_address_should_return_422`，覆盖“仅传空白 `address` 且无 `address_detail`”必须返回 `422/VALIDATION_ERROR`，避免 legacy fallback 误放大；2) `backend/tests/integration/api/test_project_api_real.py::test_project_search_should_tolerate_legacy_project_code`，覆盖历史 `project_code` 在 `POST /api/v1/projects/search` 路径下也可稳定序列化返回。验证：`cd backend && uv run pytest --no-cov tests/integration/test_asset_lifecycle.py -k "blank_legacy_address_should_return_422" -q`（1 passed）；`cd backend && uv run pytest --no-cov tests/integration/api/test_project_api_real.py -k "search_should_tolerate_legacy_project_code" -q`（1 passed）。

- 集成测试稳态收敛（2026-03-06，继续）：`backend/tests/integration/api/test_project_api_real.py` 新增 `_request_with_reauth` 辅助，在请求遇到偶发 `401`（日志表现为 `token not yet valid (iat)`）时自动重登录并重试一次，避免测试环境时间抖动导致假失败；相关 legacy code 列表/详情/搜索用例改用该 helper 发请求。最终联跑验证（显式注入测试库连接串）：`cd backend && TEST_DATABASE_URL='postgresql+psycopg://postgres:asdf@localhost:5432/zcgl_test' uv run pytest --no-cov tests/integration/test_asset_lifecycle.py tests/integration/api/test_project_api_real.py -k "address_only_should_use_legacy_address_fallback or blank_legacy_address_should_return_422 or legacy_project_code or search_should_tolerate_legacy_project_code" -q`（4 passed, 4 deselected）。

- REQ-RNT-001 M3 落地（2026-03-05）：合同组 API 端点。① 新建 `backend/src/api/v1/rent_contracts/contract_groups.py`（9 条路由：`POST/GET /contract-groups`、`GET/PUT/DELETE /contract-groups/{id}`、`POST/GET /contract-groups/{id}/contracts`、`GET/DELETE /contracts/{id}`；API 层负责调 `party_service.get_party` 获取 `operator_party_code`，再调 `contract_group_service.generate_group_code`；全部端点接入 `require_authz`，写操作 re-raise `BaseBusinessError`）；② `backend/src/api/v1/rent_contracts/__init__.py` 暴露 `contract_groups_router`；③ `backend/src/api/v1/__init__.py` 注册 `contract_groups_router`（无额外前缀，路径由 router 内定义）；④ 新建测试 `backend/tests/unit/api/v1/test_contract_groups_layering.py`（10 cases：静态约束 5 + 行为 5，覆盖分层不绕 CRUD、require_authz 接入、party_service 生成 code、404 party 缺失、group_id 不一致 422、ResourceNotFoundError→404、路径覆盖）。同步修复 M2 复核发现的 5 处问题（已在后台整合）：5 处 `ResourceNotFoundError` 参数修正、`RevenueMode[member]` KeyError 修复为直接使用 ORM 枚举成员、`.name` 比较修复为枚举成员比较（upstream/downstream 聚合）、`get_group_detail` 双重加载消除、`_SETTLEMENT_RULE_REQUIRED_KEYS` 死代码删除。验证：`uv run ruff check src/api/v1/rent_contracts/contract_groups.py`（PASS）；`uv run pytest tests/unit/api/v1/test_contract_groups_layering.py --no-cov -q`（10 passed）；`uv run pytest -m unit --no-cov -q`（4479 passed，3 skipped，0 failed）。文档联动：`docs/requirements-specification.md` REQ-RNT-001 状态更新为 🚧 并补全代码证据路径；需求追踪矩阵同步更新。

- `useProject` 行为回归补齐（2026-03-05，继续）：`frontend/src/hooks/__tests__/useProject.test.ts` 新增 2 条行为断言，覆盖 `useProjectStatistics` 必须返回新契约键 `total_projects/active_projects`（并显式断言旧键 `total/active` 不存在），以及 `useProjectList` 必须按 `page_size` 消费分页并回填 `pagination.pageSize/current/total`。验证：`pnpm -C frontend exec vitest run src/hooks/__tests__/useProject.test.ts`（PASS，12 passed）；`pnpm -C frontend type-check`（PASS）。
- 前端 `useProject` 测试契约收口（2026-03-05，继续）：`frontend/src/hooks/__tests__/useProject.test.ts` 的 `projectService` mock 全量切换到新字段主链（`project_name/project_code/status`、`page_size/pages`、`total_projects/active_projects`），并移除 `name/code/is_active/size/total/active` 等 legacy mock 数据，避免测试层继续掩盖旧契约。验证：`pnpm -C frontend exec vitest run src/hooks/__tests__/useProject.test.ts`（PASS）。
- REQ-RNT-001 M2 落地（2026-03-05）：合同组 CRUD + Service + Schema + 单元测试。① 新建 `backend/src/schemas/contract_group.py`（`SettlementRuleSchema`（5键校验 + 周期枚举）、`ContractGroupCreate/Update/ListItem/Detail`、`ContractCreate/Summary/Detail`、`LeaseDetailCreate/Response`、`AgencyDetailCreate/Response`、`ContractRelationCreate`）；② 新建 `backend/src/crud/contract_group.py`（`CRUDContractGroup`：`create/get/get_by_code/update/soft_delete/list_by_filters/count_by_operator_month/_replace_assets`）；③ 新建 `backend/src/crud/contract.py`（`CRUDContract`：`create/get/update/soft_delete/list_by_group/count_active_in_group/_replace_assets`，同步创建 `LeaseContractDetail`/`AgencyAgreementDetail`）；④ 新建 `backend/src/services/rent_contract/contract_group_service.py`（纯函数：`calculate_derived_status`、`validate_revenue_mode_compatibility`、`validate_sign_date_for_status`、`_build_operator_code_segment`；Service 类：`generate_group_code`、`create_contract_group`、`update_contract_group`、`get_group_detail`、`list_groups`、`soft_delete_group`、`add_contract_to_group`、`get_contract_detail`）；⑤ 新建测试 `backend/tests/unit/services/rent_contract/test_contract_group_service.py`（36 cases，覆盖 B1/B2/B3/B4/B5/B6/B7/B8/B12 所有边界）。验证：`uv run ruff check`（PASS）；`uv run pytest test_contract_group_service.py --no-cov -q`（36 passed）；`uv run pytest -m unit --no-cov -q`（4479 passed，3 skipped，0 failed）。

- REQ-RNT-001 M1 落地（2026-03-05）：五层合同模型 ORM + DDL 建表，并完成迁移链全面复核与枚举DDL修正。① 新增 `backend/src/models/contract_group.py`（`ContractGroup`/`Contract`/`LeaseContractDetail`/`AgencyAgreementDetail`/`ContractRelation` 五个模型类及六个枚举）；② `backend/src/models/associations.py` 新增 `contract_group_assets`/`contract_assets` 关联表；③ `backend/src/models/__init__.py` 导出全部新符号；④ 新增 Alembic 迁移 `backend/alembic/versions/20260305_contract_group_m1.py`（DDL，建 7 张新表）；⑤ 新增合并迁移 `backend/alembic/versions/cca6e52486f4_merge_phase4_party_and_contract_group_m1.py`，合并 `phase4_drop_legacy_party_columns` 分支，主库升至单一 head `cca6e52486f4`；⑥ **DDL enum 值修正**：迁移文件原误用中文/小写值（如 `"出租"/"承租"/"lease"/"agency"/"upstream_downstream"` 等），全部修正为 SQLAlchemy 默认的 Python 成员名（`"LESSOR"/"LESSEE"/"LEASE"/"AGENCY"/"UPSTREAM_DOWNSTREAM"` 等），`server_default` 同步从 `"草稿"` 修正为 `"DRAFT"`，与 ORM 实际写库行为一致；⑦ 在全新临时数据库 `zcgl_migration_verify` 上从零跑完整迁移链至 `cca6e52486f4`（`PHASE4_TENANT_NOT_NULL_DECISION=B` 跳过旧决策门，全部 Running upgrade 成功无 ERROR），验证 6 个枚举类型均以成员名入库。验证：`uv run ruff check`（PASS）；`uv run pytest tests/unit/models/ --no-cov -q`（148 passed）；alembic upgrade head 从零到 `cca6e52486f4` PASS（新建空库验证）。

- REQ-RNT-001 技术方案撰写（2026-03-05）：新增 `docs/plans/2026-03-05-req-rnt-001-contract-group.md`，覆盖五层合同模型重构（ContractGroup + Contract + LeaseContractDetail + AgencyAgreementDetail + ContractRelation）的完整技术方案，包含 DDL 设计、ORM 结构、旧表迁移路径、Service/CRUD/API 设计、分三阶段实施计划（M1 ORM迁移 → M2 CRUD+Service+Schema → M3 API端点）、12 类边界情况与测试要点。`docs/plans/README.md` 同步注册新活跃方案。

- 需求口径对齐（两种运营模式 + 四类合同角色，2026-03-05）：确定资产运营业务完整合同模型并回写 SSOT 文档。核心决策：① 两种运营模式——承租模式（`revenue_mode=lease`）和代理模式（`revenue_mode=agency`）；② 四类合同角色通过 `Contract.group_relation_type`（而非已废弃的 `ContractType` 枚举）区分：`上游`（内部入约/承租模式）、`下游`（对外出约/承租模式）、`委托`（内部入约/代理模式）、`直租`（对外出约/代理模式）；③ `ContractType` 枚举已标注废弃（§10.1，2026-03-03），本次不新增值；④ 客户摘要仅展示对外出约（`下游`、`直租`）的 `lessee_party` 姓名，内部入约不计入；⑤ MVP 约束：一个资产只属于一种运营模式；⑥ REQ-AST-004 标记为 ⏸ 阻塞，前置 REQ-RNT-001（五层合同模型）完成后才可实现。文档联动：`docs/requirements-specification.md`（REQ-AST-004/REQ-RNT-002 验收条件改用 `group_relation_type`）、`docs/features/requirements-appendix-fields.md`（§3.4 `group_relation_type` 枚举 `终端→直租`、§3.7 `agency_terminal→agency_direct`）、`docs/plans/2026-03-05-req-ast-004-asset-lease-summary.md`（全文重写为基于 `group_relation_type`，状态改为 ⏸）。

- 项目关系更新路径回归补齐（2026-03-05，继续）：为 `update_project` 新增 `party_relations` 替换行为测试，覆盖“传入关系时先清空旧关系再写入新关系（含重复 party 去重）”与“未传 `party_relations` 时不触发关系表写入”两条分支。涉及：`backend/tests/unit/services/project/test_project_service.py`。验证：`cd backend && uv run pytest --no-cov -q tests/unit/services/project/test_project_service.py -k "update_project_should_replace_party_relations_when_provided or update_project_should_not_replace_party_relations_when_not_provided"`（PASS）。
- ~~需求文档口径对齐（REQ-AST-004，2026-03-05，已作废）：此条目描述的中间状态已被同日后续修正完全推翻，`requirements-specification.md` 最终未采用 `ContractType V2` 口径，而是改用 `group_relation_type` 体系（见上方"两种运营模式 + 四类合同角色"条目）。保留仅供历史追溯。~~
- 前端测试并发稳定性修复（2026-03-05）：`frontend/vitest.config.ts` 将本地与 CI 统一为 `maxWorkers=1/minWorkers=1`，避免全量测试在多 worker 下出现随机超时与并发 mock 污染。验证：`cd frontend && corepack pnpm test`（PASS，串行全量通过）；`make check`（PASS）。
- 项目响应契约继续“不兼容”收口（2026-03-05，继续）：`backend/src/schemas/project.py` 的 `ProjectResponse` 移除 `ownership_relations` 对外字段，仅保留 `party_relations`；响应序列化仍从已加载的 ORM 关系对象安全映射到 `party_relations`，但不再暴露 legacy 键。同步更新 schema 单测：`backend/tests/unit/schemas/test_project_schema.py`、`backend/tests/unit/schemas/test_response_safety.py`。验证：`cd backend && uv run ruff check src/schemas/project.py tests/unit/schemas/test_project_schema.py tests/unit/schemas/test_response_safety.py`（PASS）；`cd backend && uv run pytest --no-cov -q tests/unit/schemas/test_project_schema.py tests/unit/schemas/test_response_safety.py`（PASS）；`cd backend && uv run pytest --no-cov -q tests/unit/services/project/test_project_service.py tests/unit/api/v1/test_project.py`（PASS）。
- 项目契约继续“不兼容”收口（2026-03-05，继续）：1) 项目下拉筛选参数从 `is_active` 全链路切换为 `status`：`backend/src/api/v1/assets/project.py`、`backend/src/services/project/service.py`、`frontend/src/services/projectService.ts`、`frontend/src/hooks/useProject.ts`、`frontend/src/components/Project/ProjectSelect.tsx`、`frontend/src/components/Forms/OwnershipForm.tsx`；2) 对应测试同步更新并新增接口透传断言：`backend/tests/unit/services/project/test_project_service.py`、`backend/tests/unit/api/v1/test_project.py`、`frontend/src/services/__tests__/projectService.test.ts`；3) 前端项目列表移除 legacy 回退逻辑（不再读取 `ownership_relations`、不再用 `assets.length` 兜底），并从 `frontend/src/types/project.ts` 删除 `ownership_relations` 字段定义，`frontend/src/components/Project/ProjectList.tsx` 与其测试同步改为仅依赖新契约主链。验证：`cd backend && uv run ruff check src/services/project/service.py src/api/v1/assets/project.py tests/unit/services/project/test_project_service.py tests/unit/api/v1/test_project.py`（PASS）；`cd backend && uv run pytest --no-cov -q tests/unit/services/project/test_project_service.py -k "dropdown_options"`（PASS）；`cd backend && uv run pytest --no-cov -q tests/unit/api/v1/test_project.py -k "project_dropdown_options_uses_status_filter or project_statistics_returns_new_contract_fields"`（PASS）；`cd frontend && pnpm exec vitest run src/services/__tests__/projectService.test.ts src/components/Project/__tests__/ProjectSelect.test.tsx src/components/Project/__tests__/ProjectList.test.tsx`（PASS）；`pnpm -C frontend type-check`（PASS）。
- 项目 owner-party 映射回归测试稳定性修复（2026-03-05）：修复 `backend/tests/unit/api/v1/test_project.py::TestListProjects::test_list_projects_owner_party_filter_should_support_party_id_mapping` 在重复执行时触发 `uq_parties_party_type_code` 唯一约束冲突的问题。测试改为优先复用 `Ownership` 自动生成且 `external_ref` 对齐的 `Party`，仅在缺失时再创建兜底映射主体，避免重复插入同码 `Party`。验证：`cd backend && uv run ruff check tests/unit/api/v1/test_project.py`（PASS）；`cd backend && uv run pytest --no-cov -q tests/unit/api/v1/test_project.py::TestListProjects::test_list_projects_owner_party_filter_should_support_party_id_mapping`（当前环境因缺失 `TEST_DATABASE_URL` 被 skip）。
- 项目统计契约字段收口（2026-03-05，继续收口）：后端项目统计统一为 `total_projects/active_projects`。1) `backend/src/schemas/project.py` 的 `ProjectStatisticsResponse` 删除 `total_count/active_count` 旧键，改为新键；2) `backend/src/api/v1/assets/project.py` 的 `/projects/stats/overview` 增加 `response_model=ProjectStatisticsResponse`，显式锁定响应结构；3) 新增 `backend/tests/unit/api/v1/test_project.py::TestProjectStatistics::test_get_project_statistics_returns_new_contract_fields`，断言返回仅使用新键。验证：`cd backend && uv run pytest --no-cov -q tests/unit/api/v1/test_project.py -k "project_statistics_returns_new_contract_fields"`（PASS）、`cd backend && uv run ruff check src/schemas/project.py src/api/v1/assets/project.py tests/unit/api/v1/test_project.py`（PASS）。
- REQ-PRJ-002（项目详情按有效资产汇总）落地完成（2026-03-05）：1) 后端新增 `GET /api/v1/projects/{project_id}/assets`（`backend/src/api/v1/assets/project.py`），并在 `backend/src/services/project/service.py` 增加 `get_project_active_assets`，按 `project_assets.valid_to IS NULL` + `assets.data_status='正常'` 汇总并返回面积统计；2) `backend/src/schemas/project.py` 新增 `ProjectAssetSummary`、`ProjectActiveAssetsResponse`；3) 前端 `frontend/src/pages/Project/ProjectDetailPage.tsx` 改为调用 `projectService.getProjectAssets`，统计值改读后端 `summary`；4) 前端补齐类型与服务方法（`frontend/src/types/project.ts`、`frontend/src/services/projectService.ts`、`frontend/src/constants/api.ts`）；5) 新增回归测试 `backend/tests/unit/services/project/test_project_service.py::TestGetProjectActiveAssets`、`backend/tests/unit/api/v1/test_project.py::TestGetProjectActiveAssets`、`frontend/src/services/__tests__/projectService.test.ts`。文档联动：`docs/requirements-specification.md` 将 REQ-PRJ-002 状态更新为 ✅ 并补代码证据；方案归档至 `docs/archive/backend-plans/2026-03-05-req-prj-002-project-active-assets.md`，并更新 `docs/plans/README.md`。验证：`cd backend && uv run pytest --no-cov -q tests/unit/services/project/test_project_service.py::TestGetProjectActiveAssets tests/unit/api/v1/test_project.py::TestGetProjectActiveAssets`（PASS）；`cd backend && uv run ruff check src/services/project/service.py src/api/v1/assets/project.py src/schemas/project.py tests/unit/services/project/test_project_service.py tests/unit/api/v1/test_project.py`（PASS）；`cd frontend && pnpm test src/services/__tests__/projectService.test.ts`（PASS）；`cd frontend && pnpm type-check`（PASS）。
- 前端 UI 门禁修复（2026-03-05）：修复 `scan:px:all-strict` 在 `frontend/src/pages/System/UserManagementPage.module.css` 的 3 处模块样式 `px` 违规，将 `min-width: 320px/220px/120px` 改为等价 `20rem/13.75rem/7.5rem`，保持布局语义不变。验证：`cd frontend && corepack pnpm scan:px:all-strict`（PASS）。补充验证：`make check` 已通过 lint/type-check/docs-lint，但在 `test-frontend` 阶段仍存在历史超时与 worker 启动超时失败（非本次变更引入）。
- 项目下拉接口返回字段去兼容（2026-03-05，继续收口）：`backend/src/services/project/service.py` 的 `get_project_dropdown_options` 不再返回 legacy 键 `name/code/is_active`，仅输出 `id/project_name/project_code`；并在 `backend/tests/unit/services/project/test_project_service.py` 增加断言锁定该契约。验证：`cd backend && uv run pytest --no-cov -q tests/unit/services/project/test_project_service.py -k "dropdown_options_accepts_legacy_is_active_argument"`（PASS）。
- 项目列表 `owner_party_id` 筛选链路修复（2026-03-05，review follow-up）：修复“前端发送 `owner_party_id` 但后端未生效”问题。1) `backend/src/api/v1/assets/project.py` 的 `GET /api/v1/projects` 查询参数从 `ownership_id` 切换为 `owner_party_id` 并透传到 `ProjectSearchRequest`；2) `backend/src/schemas/project.py` 的 `ProjectSearchRequest` 同步收口为 `owner_party_id`；3) `backend/src/crud/project.py` 实现真实过滤逻辑：基于 `ProjectOwnershipRelation`（仅 `is_active=true`）对子查询 `project_id` 进行约束，替换原 `pass` 占位分支；4) 新增复现与行为回归测试 `backend/tests/unit/api/v1/test_project.py::TestListProjects::test_list_projects_passes_owner_party_id_filter`（参数透传）与 `backend/tests/unit/api/v1/test_project.py::TestListProjects::test_list_projects_owner_party_filter_should_limit_results`（结果过滤）；5) `frontend/src/types/project.ts` 为 `ProjectSearchRequest` 增加 `owner_party_id`，与后端 `/api/v1/projects/search` 契约对齐，并在 `frontend/src/services/__tests__/projectService.test.ts` 新增 `searchProjects` 透传断言；6) 修复 Party/Ownership ID 不同场景的漏筛选：`backend/src/crud/project.py` 新增 `Party.external_ref` 映射路径（`owner_party_id=Party.id` 也能匹配 `project_ownership_relations.ownership_id`），并新增回归测试 `backend/tests/unit/api/v1/test_project.py::TestListProjects::test_list_projects_owner_party_filter_should_support_party_id_mapping` 与 `backend/tests/unit/api/v1/test_project.py::TestSearchProjects::test_search_projects_owner_party_filter_should_support_party_id_mapping`（覆盖 POST `/api/v1/projects/search`）；7) 补齐 `POST /api/v1/projects/search` 额外边界回归：`owner_party_id` 为空白时 fail-closed、无映射时空结果、`owner_party_id + status` 取交集（`backend/tests/unit/api/v1/test_project.py`）。验证：`cd backend && uv run pytest --no-cov tests/unit/api/v1/test_project.py::TestListProjects -q`（PASS）、`cd backend && uv run pytest --no-cov tests/unit/api/v1/test_project.py::TestSearchProjects -q`（PASS）、`cd backend && uv run pytest --no-cov tests/unit/crud/test_project.py::TestCRUDProjectSearch -q`（PASS）、`cd backend && uv run pytest --no-cov tests/unit/services/project/test_project_service.py -q`（PASS）、`cd backend && uv run pytest --no-cov tests/unit/migration/test_project_field_enrichment_m1_migration.py -q`（PASS）、`cd backend && uv run ruff check src/api/v1/assets/project.py src/schemas/project.py src/crud/project.py tests/unit/api/v1/test_project.py`（PASS）、`pnpm -C frontend type-check`（PASS）、`pnpm -C frontend exec vitest run src/services/__tests__/projectService.test.ts`（PASS）。
- 项目列表 owner-party GET 边界回归补齐（2026-03-05，继续）：`backend/tests/unit/api/v1/test_project.py::TestListProjects` 增加 3 个边界用例：1) `owner_party_id + status` 组合筛选应取交集；2) 非法 `owner_party_id`（如 `%25%25%25invalid%%%25`）应 fail-closed 返回空列表；3) `owner_party_id` 筛选下分页返回 `total/page/items` 一致。补充复核：`cd backend && uv run pytest --no-cov -q tests/unit/api/v1/test_project.py`（PASS, 38 passed）；`cd backend && uv run ruff check tests/unit/api/v1/test_project.py tests/unit/migration/test_project_field_enrichment_m1_migration.py`（PASS）；`pnpm -C frontend exec vitest run src/services/__tests__/projectService.test.ts`（PASS, 47 passed）；`pnpm -C frontend type-check`（PASS）；`pnpm -C frontend build`（PASS）。
- 项目契约非兼容收口（2026-03-05，review follow-up）：移除前端项目模块的兼容层，统一新契约字段并删除 legacy 入口。1) `frontend/src/types/project.ts` 删除 `name/code/is_active/project_status/ownership_id/short_name` 等旧字段定义，仅保留 `project_name/project_code/status` 主链；2) `frontend/src/services/projectService.ts` 移除旧字段归一化与旧参数双写（不再发送 `project_status/ownership_id`，删除 `getProjectsByOwnership`）；3) `frontend/src/hooks/useProject.ts` 查询参数从 `is_active` 切换为 `status`；4) `frontend/src/services/__tests__/projectService.test.ts` 更新为“禁止发送 legacy 字段”断言，删除已废弃 API 的测试。验证：`pnpm -C frontend type-check`（PASS）、`pnpm -C frontend exec vitest run src/services/__tests__/projectService.test.ts src/components/Project/__tests__/ProjectDetail.test.tsx src/components/Project/__tests__/ProjectList.test.tsx src/components/Project/__tests__/ProjectSelect.test.tsx src/components/Forms/__tests__/ProjectForm.test.tsx`（PASS，70 passed）。
- 项目字段增强复核阻断项修复（2026-03-05）：完成 4 项阻断问题闭环。1) `frontend/src/types/project.ts` + `frontend/src/services/projectService.ts` 增加新旧字段双向兼容（`project_name/project_code/status` 与 `name/code/is_active` 同时可读写），恢复项目页/表单/下拉的类型与运行时兼容；2) `frontend/src/services/projectService.ts` 的列表查询参数补齐 `project_status` 兼容键（与 `status` 同步发送），避免旧后端过滤失效；3) `backend/src/services/project/service.py` 恢复 `get_project_dropdown_options(..., is_active=...)` 兼容签名并在 `backend/src/api/v1/assets/project.py` 透传，消除 dropdown 500 风险；4) `backend/alembic/versions/20260305_project_field_enrichment_m1.py` 保留英文状态值（`active/paused/completed/terminated`）不被降级为 `planning`。新增回归测试：`backend/tests/unit/migration/test_project_field_enrichment_m1_migration.py`、`backend/tests/unit/services/project/test_project_service.py`、`frontend/src/services/__tests__/projectService.test.ts`。验证：`pnpm -C frontend type-check`（PASS）、`pnpm -C frontend vitest run src/services/__tests__/projectService.test.ts -t "legacy project_status"`（PASS）、`cd backend && uv run pytest --no-cov -q tests/unit/migration/test_project_field_enrichment_m1_migration.py tests/unit/services/project/test_project_service.py -k "project_dropdown_options_accepts_legacy_is_active_argument or preserve_existing_english_status_values"`（PASS）、`cd backend && uv run pytest --no-cov -q tests/unit/api/v1/test_project.py -k "list_projects_with_status_filter"`（PASS）、`cd backend && uv run ruff check src/services/project/service.py src/api/v1/assets/project.py alembic/versions/20260305_project_field_enrichment_m1.py tests/unit/migration/test_project_field_enrichment_m1_migration.py tests/unit/services/project/test_project_service.py`（PASS）。
- 前端项目契约“不兼容迁移”收敛（2026-03-05）：彻底移除 `name/code/is_active/description/short_name` 在项目模块中的调用，统一切换为 `project_name/project_code/status`。本次变更覆盖 `ProjectForm`、`ProjectList`、`ProjectDetail`、`ProjectDetailPage`、`ProjectSelect`、`projectService`、`OwnershipForm` 及测试工厂 `projectFactory`，并补齐 `frontend/src/types/project.ts` 的 `ProjectCreate/ProjectUpdate/ProjectDeleteResponse` 字段定义。验证：`pnpm -C frontend type-check`（PASS）。
- 代码复核修复（REQ-PRJ-001 M1 post-review）：深度复核发现并修复 6 项问题。**CRITICAL×2**：① `backend/src/api/v1/assets/project.py` `list_projects` 路由向 `ProjectSearchRequest` 传入废弃字段（`is_active/project_type/project_status/city/ownership_entity`），Pydantic v2 静默丢弃导致 `status` 过滤实际无效，已修复为传 `status=status`；② 同文件 `get_project_options` 路由调用 `get_project_dropdown_options(db, is_active=is_active, ...)` 而 service 无此参数，运行时 `TypeError`，已移除该 kwarg（service 底层已用 `data_status='正常'` 过滤）。**MINOR×4**：③ `backend/src/services/project/service.py` 删除 `_generate_name_code` 死代码（始终 `return None`，含大量过时注释）；④ `backend/alembic/versions/20260305_project_field_enrichment_m1.py` 注释 "DROP 22 个废弃列" 实际列表 25 项，改为 25；⑤ `docs/requirements-specification.md` 需求追踪矩阵 §11 缺 REQ-PRJ-001/REQ-PRJ-002 行，已补入；⑥ `frontend/src/types/project.ts` `ProjectStatisticsResponse` 字段名 `total_count/active_count` 与后端返回 `total_projects/active_projects` 不匹配，已修正。验证：`uv run pytest -m unit --no-cov -q`（4418 passed，3 skipped）。
- REQ-PRJ-001 字段收口（M1）：Project 表字段完整收口至运营管理语义规格。1) `backend/src/models/project.py` 新增 `ProjectStatus`/`ProjectReviewStatus` 枚举，字段从 37 列收口至规格字段：`project_name`/`project_code`/`status`/`manager_party_id`/`review_status`/`review_by`/`reviewed_at`/`review_reason`/`data_status` + 审计字段；2) `backend/alembic/versions/20260305_project_field_enrichment_m1.py` 执行 RENAME(`name→project_name`, `code→project_code`, `project_status→status`)、数据迁移中文枚举→英文枚举、ADD 4 review 列、DROP 22 个工程管理/投资/描述/地址残留列；3) `backend/src/schemas/project.py` 重写 `ProjectBase/Create/Update/Response/SearchRequest`，新增 `status`/`project_code` 格式校验，`ProjectCreate` 保留 `organization_id` 辅助字段用于推断 `manager_party_id`；4) `backend/src/crud/project.py` 全字段引用更新；5) `backend/src/crud/field_whitelist.py` `ProjectWhitelist` 同步更新；6) `backend/src/services/project/service.py` 更新 `create_project`/`toggle_status`，重写 `generate_project_code`（新格式 `PRJ-YYYYMM-NNNNNN`），移除 `_generate_name_code`；7) `frontend/src/types/project.ts` 接口全部重写；8) `frontend/src/services/projectService.ts` 字段引用更新；9) 修复对应 7 个测试文件（service/model/schema/api）共 39 个用例。验证：`uv run pytest tests/unit/ --no-cov -q`（4418 passed，3 skipped）。
- REQ-AST-001 字段收口（M1）：补全 2026-02-28 访谈冻结的资产字段规格。1) `backend/src/models/asset.py` 新增 `AssetForm`/`SpatialLevel`/`BusinessUsage`/`AssetReviewStatus` 四个枚举类；`property_name` 改名为 `asset_name`；新增 `asset_code`、`asset_form`、`spatial_level`、`business_usage`、`province_code`、`city_code`、`district_code`、`address_detail`、`review_status`/`review_by`/`reviewed_at`/`review_reason` 共 12 个字段；`address` 改为系统拼接只读，不对外开放直写。2) `backend/src/schemas/asset.py` `AssetBase`/`AssetUpdate` 同步：移除可写 `address/property_name`，加入地址拆字段和分类三字段，`AssetResponseBase` 增加所有新只读字段；`address_detail` 增 trim + 5-200 校验器。3) `backend/src/services/asset/asset_service.py` 新增 `_compose_address` 辅助函数，`create_asset`/`update_asset` 写入时自动拼接 `address`。4) `backend/alembic/versions/20260305_asset_field_enrichment_m1.py` 迁移文件：`RENAME property_name → asset_name`，ADD 12 新列，存量数据占位填充，`asset_code`/`asset_form` 加唯一/普通索引。5) 全局 sed 替换：后端 20 个源文件 + 37 个测试文件，前端 45 个文件中 `property_name` → `asset_name`。验证：`uv run pytest tests/unit/ --no-cov -q`（4415 passed，3 skipped）。
- 资产创建地址校验修复（2026-03-05）：修复 `create_asset` 在 `address_detail/address` 同时缺失时穿透到数据库 `NOT NULL` 的问题。`backend/src/services/asset/asset_service.py` 改为创建阶段必须成功拼接 `address`，否则返回 `422 validation_error`（`address_detail=required_for_address_composition`）；并在 `backend/tests/unit/services/asset/test_asset_service.py` 新增回归用例 `test_create_asset_should_fail_fast_when_address_sources_missing`，锁定“服务层 fail-fast，且不触发 CRUD create”。验证：`cd backend && uv run pytest --no-cov -q tests/unit/services/asset/test_asset_service.py`（60 passed）。
- 资产字段增强迁移回填修复（2026-03-05）：修复 `20260305_asset_field_enrichment_m1` 迁移将 `address`（可能是加密密文）截断后写入明文字段 `address_detail` 的数据污染风险。`backend/alembic/versions/20260305_asset_field_enrichment_m1.py` 移除 `address_detail = LEFT(address, 200)` 回填，仅保留分类字段占位更新；新增 `backend/tests/unit/migration/test_asset_field_enrichment_m1_migration.py` 回归测试 `test_upgrade_should_not_backfill_address_detail_from_legacy_address` 锁定该约束。验证：`cd backend && uv run pytest --no-cov -q tests/unit/migration/test_asset_field_enrichment_m1_migration.py`（2 passed）。
- 资产地址 API 回归测试补齐（2026-03-05）：在 `backend/tests/integration/test_asset_lifecycle.py` 新增两条集成用例：1) `test_asset_create_with_address_only_should_return_422`，覆盖仅传 `address`、缺失 `address_detail` 返回 `422` 且 `field_errors.address_detail` 包含 `required_for_address_composition`；2) `test_asset_create_should_ignore_manual_address_and_use_composed_value`，覆盖“传入伪造 `address` + 合法 `address_detail`”时系统仍以拼接结果落库。由于当前分支存在 Alembic 多 head，`backend/tests/integration/conftest.py` 增加升级失败回退 `create_all` 兜底（与 unit fixture 策略一致），避免集成测试环境初始化被多 head 阻断。验证：`cd backend && uv run pytest --no-cov -q tests/integration/test_asset_lifecycle.py -k "address_only_should_return_422 or ignore_manual_address_and_use_composed_value"`（2 passed）；`cd backend && uv run ruff check tests/integration/test_asset_lifecycle.py tests/integration/conftest.py`（PASS）。

## [Unreleased] - 2026-03-04

### 🛠️ 本次修复 (Current Fixes)

- SSOT 工作流门禁增强（2026-03-04）：扩展 `scripts/check_requirements_authority.py` 新增两项检查：① 代码证据死链检测；② `docs/plans/` 已完结方案残留检测。新增 `scripts/check_field_drift.py` 提供字段规格 vs ORM 漂移报告。更新 `Makefile` 将 `docs-lint` 纳入 `make check` 全量门禁。在 `AGENTS.md` 新增"需求 → 代码 SSOT 工作流"章节，明确唯一真相链路、`make check` 门禁表、`docs-lint` 三项检查说明、字段漂移报告解读与新增功能必须遵守的关联动作。验证：`make docs-lint`（3 项检查全部 PASS）。

- AGENTS 规范回放（2026-03-04）：在远端基线上回放被冲突跳过的文档子集，更新 `AGENTS.md` 为 0→1 开发期执行约束（恢复 6 条协作规则），并统一后端环境/测试/迁移口径为 `uv sync` + `uv run`，同步修正排障与安全警告中的命令示例。

- 后端类型安全子集回放（2026-03-04）：在与远端主干冲突后按“小步可验证”策略回放 `refactor(backend)` 的安全改动，更新 `backend/src/core/exception_handler.py`（`ErrorDetails` 改为只读 `Mapping`、细化 `ErrorDetailDict`、异常详情序列化与 helper 入参类型收敛）和 `backend/src/crud/asset_support.py`（Result Protocol + awaitable 解析收敛、`encrypt_data/decrypt_data` overload）；保持行为不变。验证：`cd backend && uv run --extra dev python -m ruff check src/core/exception_handler.py src/crud/asset_support.py`（PASS）；`cd backend && uv run pytest --no-cov tests/unit/core/test_exception_handling.py tests/unit/crud/test_asset.py -q`（33 passed, 2 skipped）。

- E2E Day 19 产权证确认导入非法日期 fail-fast 与前端严格日期标准化回归（2026-03-04）：继续补齐日期边界语义。1) `backend/src/services/property_certificate/service.py` 在 `confirm_import` 增加日期字段预校验（`registration_date` / `land_use_term_start` / `land_use_term_end`），非空且不满足 `YYYY-MM-DD / YYYY/MM/DD / YYYY年MM月DD日` 时返回 `BusinessValidationError`（`VALIDATION_ERROR` + `field_errors`），避免非法日期静默入库为 `null`；2) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_rejects_invalid_date_format_e2e`，锁定非法日期返回 `422/VALIDATION_ERROR`；3) `frontend/src/components/PropertyCertificate/PropertyCertificateReview.tsx` 日期标准化改为严格校验（避免 `dayjs` 宽松解析将 `2026-13-01` 纠偏成 `2027-01-01`），不可识别日期统一归一为 `null`；4) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should null invalid date strings before confirm submit`，锁定非法日期载荷清洗为 `null`。验证：`cd backend && ./.venv/bin/ruff check src/services/property_certificate/service.py tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`make test-e2e-import-backend`（后端 `27 passed`）；`make test-e2e-import-frontend`（前端 `20 passed`）。

- E2E Day 18 产权证日期字段多格式解析与前端提交标准化回归（2026-03-04）：补齐确认导入日期语义并修复前端字段丢失风险。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_parses_mixed_date_formats_e2e`，覆盖 `registration_date=YYYY/MM/DD`、`land_use_term_start=YYYY年MM月DD日`、`land_use_term_end=YYYY-MM-DD` 在确认导入后均标准化落库并可在详情返回 `YYYY-MM-DD`；2) `frontend/src/components/PropertyCertificate/PropertyCertificateReview.tsx` 将确认提交改为“`extracted_data` 与表单值合并后提交”，并对 `registration_date/land_use_term_start/land_use_term_end` 做 `YYYY-MM-DD` 标准化，避免未展示字段丢失及日期对象序列化偏差；3) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should normalize mixed date formats before confirm submit`，锁定确认请求载荷中的三类日期字段均被标准化。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`set -a; . backend/.env >/dev/null 2>&1; set +a; make test-e2e-import`（后端 `26 passed` + 前端 `19 passed`）。

- E2E Day 17 产权证确认导入重试不可变语义与匹配切换载荷回归（2026-03-04）：继续补齐确认导入高频交互边界。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_keeps_existing_relations_on_retry_e2e`，锁定同一 `certificate_number` 首次创建后，后续携带不同 `owners/asset_link_id` 的重复确认不会改写既有权利人关系与资产关联；2) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should submit last selected matched asset linkage`，覆盖多匹配资产场景下“后选覆盖前选”，确认载荷以最后一次点击为准；3) `frontend/tests/e2e/helpers/auth.ts` 将 `loginAsAdmin` 升级为 `loginWithCredentialRetry` 并在候选凭证失败后清理认证状态，降低长链路 E2E 认证抖动。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`set -a; . backend/.env >/dev/null 2>&1; set +a; make test-e2e-import`（后端 `25 passed` + 前端 `18 passed`）。

- E2E Day 16 产权证导入资产关联标准化与“匹配未选择”默认新建语义回归（2026-03-04）：继续补齐确认导入边界。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_trims_asset_link_id_before_linking_e2e`，锁定 `should_create_new_asset=false` 时即使 `asset_link_id` 含前后空格也会标准化后正确写入 `property_cert_assets`；2) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should keep create-new-asset payload when matches exist but user does not select`，覆盖“存在 `asset_matches` 但用户未点击任一匹配项”时确认载荷仍必须为 `should_create_new_asset=true`、`asset_link_id=null`、`asset_ids=[]`。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`set -a; . backend/.env >/dev/null 2>&1; set +a; make test-e2e-import`（后端 `24 passed` + 前端 `17 passed`）。

- E2E Day 15 产权证导入权利主体映射与默认建资产载荷回归（2026-03-04）：继续补齐确认导入语义。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_maps_owner_parties_with_single_primary_e2e`，覆盖 `owners` 中有效 `party_id` 映射为 `OWNER` 关系且仅首个有效主体为 `is_primary=true`；2) 同文件将 Day 13/14 的资产关联断言统一收敛为 `property_cert_assets` 关联表查询，避免依赖详情响应中的 `asset_ids` 展示口径导致误判；3) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should submit create-new-asset payload when no matched asset is selected`，锁定无匹配资产时确认请求必须提交 `should_create_new_asset=true`、`asset_link_id=null`、`asset_ids=[]`。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`set -a; . backend/.env >/dev/null 2>&1; set +a; make test-e2e-import`（后端 `23 passed` + 前端 `16 passed`）。

- E2E Day 14 产权证“选择已有资产”关联语义回归（2026-03-03）：继续补齐确认导入主链路。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_links_asset_when_existing_asset_selected_e2e`，验证 `should_create_new_asset=false` 且传入 `asset_link_id` 时，`property_cert_assets` 关联表正确写入目标资产；2) 同文件将 `test_property_certificate_confirm_import_ignores_asset_links_when_create_new_asset_e2e` 断言收敛到关联表，锁定 `should_create_new_asset=true` 时不得产生资产关联；3) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should submit existing asset linkage when selecting a matched asset`，覆盖审核页选择匹配资产后提交载荷必须包含 `asset_link_id`、`asset_ids` 且 `should_create_new_asset=false`。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`set -a; . backend/.env >/dev/null 2>&1; set +a; make test-e2e-import`（后端 `22 passed` + 前端 `15 passed`）。

- E2E Day 13 产权证确认资产关联语义与后端校验失败可恢复回归（2026-03-03）：继续补齐产权证导入“确认导入”关键边界。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_ignores_asset_links_when_create_new_asset_e2e`，验证当 `should_create_new_asset=true` 时即使传入 `asset_ids/asset_link_id` 也不得误关联现有资产；2) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should stay on review when confirm returns validation error`，覆盖确认导入返回 `422/VALIDATION_ERROR` 时页面不跳转、停留审核页并提示 `创建失败，请重试`。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`set -a; . backend/.env >/dev/null 2>&1; set +a; make test-e2e-import`（后端 `21 passed` + 前端 `14 passed`）。

- E2E Day 12 导入确认拒绝路径与前端审核必填防线回归（2026-03-03）：继续补齐产权证导入“确认阶段”边界语义。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_upload_rejects_unsupported_extension_e2e`，锁定产权证上传非法扩展名返回 `400/HTTP_400`；2) 同文件新增 `test_property_certificate_confirm_import_rejects_blank_certificate_number_e2e`，锁定空白 `certificate_number` 在确认导入阶段被拒绝并返回 `422/VALIDATION_ERROR`；3) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should block confirm request when certificate number is empty`，覆盖审核页清空证书编号后前端表单拦截且不得发起 `confirm-import` 请求。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`make test-e2e-import`（后端 `20 passed` + 前端 `13 passed`）。

- E2E Day 11 导入确认高频重试与前端防重复提交回归（2026-03-03）：补齐产权证确认导入的并发/重试防线。1) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_remains_single_record_after_rapid_retries_e2e`，验证快速重复确认同一证书编号仅落一条记录；2) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should send confirm request once on rapid double click`，锁定审核页快速双击确认时前端仅发送一次 `POST /api/v1/property-certificates/confirm-import`。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`make test-e2e-import`（后端 `18 passed` + 前端 `12 passed`）。

- E2E Day 10 导入失败重试恢复与跨会话幂等回归（2026-03-03）：继续收敛导入链路“失败后重试”稳定性。1) `backend/tests/e2e/test_pdf_import_e2e.py` 新增 `test_pdf_upload_can_succeed_after_invalid_force_method_retry_e2e`，锁定首次非法参数失败后重试合法参数可恢复成功；2) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_confirm_import_is_idempotent_across_sessions_e2e`，验证不同 `session_id` 下同一 `certificate_number` 重复确认仍返回同一记录；3) 同文件新增 `test_property_certificate_upload_can_retry_after_oversized_rejection_e2e`，锁定“首次超限失败 -> 再次合法上传成功”恢复链路；4) `frontend/tests/e2e/user/property-certificate-import-success.spec.ts` 新增 `property certificate import should recover after oversized file rejection`，覆盖前端先触发超限拦截提示，再次上传合法文件可进入审核页。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_pdf_import_e2e.py tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`make test-e2e-import`（后端 `17 passed` + 前端 `11 passed`）。

- E2E Day 9 导入文件大小边界与前端超限零请求回归（2026-03-03）：继续补齐导入链路稳定性边界。1) `backend/tests/e2e/test_pdf_import_e2e.py` 新增 `test_pdf_upload_rejects_oversized_file_e2e`，通过临时收紧 `DEFAULT_MAX_FILE_SIZE` 验证 PDF 上传超限返回 `400/INVALID_REQUEST`；2) `backend/tests/e2e/test_property_certificate_import_e2e.py` 新增 `test_property_certificate_upload_rejects_oversized_file_e2e`，验证产权证上传超过 10MB 返回 `400/HTTP_400` 且错误消息稳定；3) `frontend/tests/e2e/user/import-guardrails.spec.ts` 新增“产权证超限文件前端拦截且不得发请求”用例，锁定提示 `文件大小不能超过 10MB` 且 `POST /api/v1/property-certificates/upload` 请求数为 0。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_pdf_import_e2e.py tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`make test-e2e-import`（后端 `14 passed` + 前端 `10 passed`）。

- mypy 历史类型错误第二批收敛并清零（2026-03-03）：继续修复剩余类型债并完成全量清零。1) 修复 `backend/src/core/exception_handler.py` 的错误详情 payload 类型、验证异常清理字典类型、异常处理注册泛型约束及 `raise_*` 参数透传类型；2) 修复系统健康检查返回体的字典访问类型（`backend/src/api/v1/system/system.py`）；3) 修复 analytics 聚合返回值属性访问类型（`backend/src/services/analytics/{occupancy_service,area_service}.py`）；4) 修复 authz/组织/用户服务的精确返回与字面量动作类型（`backend/src/services/authz/{data_policy_service,service}.py`、`backend/src/services/organization_permission_service.py`、`backend/src/services/core/user_management_service.py`）；5) 修复多处 CRUD 的 SQLAlchemy 构造与 `rowcount` 类型问题（`backend/src/crud/{authz,party,property_certificate}.py`），并将 `SensitiveDataHandler` 导入统一到 `asset_support`（`backend/src/crud/{contact,organization,rent_contract}.py`）；6) 完成 `backend/src/crud/asset.py` 类型收口（`Select` 泛型、搜索/聚合辅助返回、密文字段映射、计数字段转换），清除单文件剩余 47 条错误；7) 补齐模型关系/别名注解（`backend/src/models/{user_party_binding,abac,asset}.py`）与迁移脚本映射参数类型（`backend/src/scripts/migration/party_migration/backfill_role_policies.py`）。验证：`cd backend && ./.venv/bin/mypy src`（PASS，`Success: no issues found in 401 source files`）；`backend/.venv/bin/ruff check <本批改动文件>`（PASS）。结果：`mypy src` 从 `163 errors in 30 files` 收敛至 `0 errors`。

- mypy 历史类型错误第一批收敛（2026-03-03）：针对高频历史告警完成三类类型修复。1) 修复 API 依赖参数隐式 Optional：`backend/src/api/v1/party.py`、`backend/src/api/v1/assets/ownership.py` 的 `_authz_ctx` 统一改为 `Annotated[AuthzContext | None, Depends(...)] = None`，消除 `no_implicit_optional` 触发的批量 `assignment`；2) 补齐工厂函数返回类型：`backend/src/services/factory.py` 的 `get_service_factory` 增加 `Callable[..., Awaitable[ServiceFactory]]` 注解，消除路由侧 `no-untyped-call`；3) 修复主体过滤协变类型：`backend/src/crud/query_builder.py` 将 `PartyFilter` 字段切换为 `Sequence[...]`，并在 `backend/src/services/party_scope.py` 将序列归一入参改为 `Sequence[object]`、`filter_mode` 显式 `Literal`，消除 `list` 不变性导致的 `arg-type`。验证：`cd backend && ./.venv/bin/mypy src/api/v1/party.py src/api/v1/assets/ownership.py src/services/factory.py src/services/party_scope.py src/crud/query_builder.py`（PASS）；`cd backend && ./.venv/bin/mypy src`（从 `163 errors in 30 files` 降至 `132 errors in 24 files`，净减少 `31`）。

- E2E Day 8 产权证导入成功链路与可见性回归（2026-03-03）：补齐导入域最后一块“上传成功 -> 确认导入 -> 列表可见”端到端覆盖。1) 新增后端用例 `backend/tests/e2e/test_property_certificate_import_e2e.py`，覆盖 `POST /api/v1/property-certificates/upload` 上传提取、`POST /api/v1/property-certificates/confirm-import` 创建产权证、`GET /api/v1/property-certificates/{id}` 详情可读、`GET /api/v1/property-certificates/` 列表可见；2) 同文件补充 `confirm-import` 幂等语义回归：同一 `certificate_number` 重复确认仅返回同一记录且不重复落库；3) 新增前端用例 `frontend/tests/e2e/user/property-certificate-import-success.spec.ts`，覆盖产权证导入页上传后进入审核、确认创建并自动跳转列表、列表可见创建结果（上传接口使用 E2E 路由桩，确认创建与列表查询走真实后端）；4) 导入一键回归入口扩展：`scripts/dev/run_import_e2e.sh` 与 `Makefile` 的 `test-e2e-import-*` 纳入新增后端/前端产权证用例。验证：`bash -n scripts/dev/run_import_e2e.sh`（PASS）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_property_certificate_import_e2e.py`（PASS）；`cd frontend && pnpm type-check:e2e`（PASS）；`make test-e2e-import`（后端 `12 passed` + 前端 `9 passed`）。

- E2E Day 7 导入幂等与前端“零请求”防线回归（2026-03-03）：继续补齐导入链路高价值回归。1) `backend/tests/e2e/test_pdf_import_e2e.py` 新增 `test_pdf_upload_same_file_twice_returns_distinct_sessions_e2e`，锁定重复上传同一 PDF 时会话 ID 唯一（避免会话复用串单）；2) 同文件新增 `test_pdf_upload_accepts_trimmed_force_method_value_e2e`，锁定 `force_method` 允许前后空格输入并被稳定处理；3) `frontend/tests/e2e/user/import-guardrails.spec.ts` 新增两条“前端拦截后不得发请求”回归：合同 Excel 非法文件不得触发 `POST /api/v1/rental-contracts/excel/import`，合同 PDF 非法文件不得触发 `POST /api/v1/pdf-import/upload`。验证：`make test-e2e-import`（后端 `10 passed` + 前端 `8 passed`）。

- E2E Day 6 导入一键回归与 CI 阻断门禁（2026-03-03）：新增导入链路“本地一键回归 + CI 阻断作业”并补齐稳定性细节。1) `Makefile` 新增 `test-e2e-import`、`test-e2e-import-backend`、`test-e2e-import-frontend` 目标与 help 文案；2) 新增 `scripts/dev/run_import_e2e.sh`，串联“后端导入 E2E -> 后端服务就绪 -> 前端导入 E2E”，输出后端日志与 JUnit 报告（`test-results/backend/junit/junit-import-e2e.xml`）；3) 脚本新增管理员账号自动播种（默认 `E2E_ADMIN_USERNAME/E2E_ADMIN_PASSWORD`）与 `DATABASE_URL` 自动解析（优先当前环境，其次 `backend/.env`，再回退测试库变量），避免前端导入 E2E 因凭证或独立进程环境缺失导致误失败；4) `.github/workflows/ci.yml` 新增阻断作业 `import-e2e`（Import E2E Regression），独立 PostgreSQL、播种管理员账号、执行 `make test-e2e-import`、上传导入回归产物，并接入 `api-consistency` / `integration` / `summary` 依赖链。验证：`bash -n scripts/dev/run_import_e2e.sh`（PASS）；`make test-e2e-import-backend`（`8 passed`）；`make test-e2e-import`（后端 `8 passed` + 前端 `6 passed`）；`backend/.venv/bin/python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text(encoding='utf-8')); print('YAML_OK')"`（PASS）。

- E2E Day 5 后端导入安全语义补齐（2026-03-03）：在 Day 2 基础上补充导入接口“拒绝路径”回归。1) `backend/tests/e2e/test_pdf_import_e2e.py` 新增 `test_pdf_upload_requires_csrf_header_e2e`（缺失 `X-CSRF-Token` 返回 `403/csrf_missing`）与 `test_pdf_upload_rejects_invalid_force_method_e2e`（非法 `force_method` 返回 `400/INVALID_REQUEST`）；2) `backend/tests/e2e/test_excel_import_e2e.py` 新增 `test_excel_import_requires_csrf_header_e2e`（缺失 CSRF 返回 `403/csrf_missing`）与 `test_excel_import_requires_file_payload_e2e`（缺失文件返回 `422/VALIDATION_ERROR`）。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_pdf_import_e2e.py tests/e2e/test_excel_import_e2e.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_pdf_import_e2e.py tests/e2e/test_excel_import_e2e.py`（`8 passed`）。

- E2E Day 4 前端导入主链路回归补齐（2026-03-03）：新增导入“真实请求+响应契约”端到端用例。1) 新增 `frontend/tests/e2e/rental/import-success.spec.ts`；2) 覆盖合同 Excel 导入最小工作簿上传后的接口契约断言（`POST /api/v1/rental-contracts/excel/import` 返回结构化结果：`success/imported_contracts/errors/warnings`），并补登录兜底重试降低环境抖动；3) 覆盖 PDF 导入最小合法文件上传成功受理（`POST /api/v1/pdf-import/upload` 返回 `success=true` 且 `session_id` 非空）。验证：`cd frontend && pnpm type-check:e2e`（PASS）；`cd frontend && pnpm e2e tests/e2e/rental/import-success.spec.ts --project=chromium`（`2 passed`）。

- E2E Day 3 前端导入入口防线补齐（2026-03-03）：新增前端导入链路的端到端守卫用例。1) 新增 `frontend/tests/e2e/user/import-guardrails.spec.ts`，覆盖合同 Excel 导入、合同 PDF 导入、产权证导入三条入口在前端上传前对非法文件类型的拦截（`txt` 文件触发校验提示，不发起导入请求）；2) 同文件新增匿名访问导入路由拦截回归，覆盖 `/assets/import`、`/rental/contracts/pdf-import`、`/property-certificates/import` 均重定向登录页。验证：`cd frontend && pnpm type-check:e2e`（PASS）；`cd frontend && pnpm e2e tests/e2e/user/import-guardrails.spec.ts --project=chromium`（`4 passed`）。

- E2E Day 2 导入链路补齐（2026-03-03）：新增后端导入相关端到端测试，覆盖 PDF/Excel 核心入口与失败边界。1) 新增 `backend/tests/e2e/test_pdf_import_e2e.py`，覆盖 `/api/v1/pdf-import/upload` 成功上传（最小合法 PDF）与非 PDF 文件拒绝（`422/VALIDATION_ERROR`）；2) 新增 `backend/tests/e2e/test_excel_import_e2e.py`，覆盖 `/api/v1/rental-contracts/excel/template` 模板下载、空模板导入响应结构，以及非法扩展名上传拒绝（`400/INVALID_REQUEST`）；3) Excel 用例对 `EXCEL_SERVICE_AVAILABLE` 不可用环境做 `pytest.skip`，避免可选依赖导致误报。验证：`cd backend && ./.venv/bin/ruff check tests/e2e/test_pdf_import_e2e.py tests/e2e/test_excel_import_e2e.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_pdf_import_e2e.py tests/e2e/test_excel_import_e2e.py`（`4 passed`）。

- E2E Day 1 测试基建收敛（2026-03-03）：统一后端/前端 E2E 重复构造逻辑。1) 新增 `backend/tests/e2e/factories.py`，抽取资产/合同共用测试数据工厂（`ownership`、`asset payload`、`contract payload`）；2) `backend/tests/e2e/test_asset_lifecycle_e2e.py` 与 `backend/tests/e2e/test_contract_workflow_e2e.py` 改为复用工厂，移除重复本地构造函数；3) `frontend/tests/e2e/helpers/auth.ts` 新增 `loginWithCredentialRetry`、`resolveCsrfToken`、`resolveCsrfHeaders`；4) `frontend/tests/e2e/auth/auth-flow.spec.ts`、`frontend/tests/e2e/security/access-denied.spec.ts`、`frontend/tests/e2e/auth/organization-scope-isolation.spec.ts` 改为复用公共 helper，删除重复 CSRF 与登录重试实现。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_asset_lifecycle_e2e.py tests/e2e/test_contract_workflow_e2e.py`（`47 passed`）；`cd frontend && pnpm type-check:e2e`（PASS）；`cd frontend && pnpm e2e tests/e2e/auth/auth-flow.spec.ts tests/e2e/security/access-denied.spec.ts --project=chromium`（`6 passed`）；`cd frontend && pnpm e2e tests/e2e/auth/organization-scope-isolation.spec.ts --project=chromium`（`2 passed`）。

- 记住登录状态续期失效修复（2026-03-03）：从用户链路复现“勾选记住登录后，第二次刷新令牌返回 `401 无效的刷新令牌` 导致会话中断”。根因是 `/api/v1/auth/refresh` 会签发新的 `refresh_token + session_id`，但未回写 `user_sessions`，导致下一次刷新使用新令牌时无法命中会话。修复内容：1) `backend/src/services/core/session_service.py` 新增 `rotate_refresh_session()`，统一回写 `refresh_token/session_id/last_accessed_at/expires_at` 并保留最新客户端信息；2) `backend/src/api/v1/auth/auth_modules/authentication.py` 在 refresh 成功签发后调用会话轮换，保证 cookie 与服务端会话一致；3) 新增回归 `backend/tests/integration/api/test_cookie_auth.py::test_refresh_token_can_rotate_multiple_times` 覆盖连续两次 refresh 成功场景；4) 同步调整 `backend/tests/unit/api/v1/test_authentication.py` 中 refresh 相关 mock（`rotate_refresh_session`）。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/integration/api/test_cookie_auth.py -k refresh_token_can_rotate_multiple_times`（`1 passed`）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_authentication.py -k TestRefreshToken`（`9 passed`）。

- 依赖安全复核与修复（2026-03-03）：针对推送后安全告警执行前端依赖收敛。1) 升级直接依赖 `axios` 至 `^1.13.6`、`lodash` 至 `^4.17.23`；2) 在 `frontend/package.json` 新增 `pnpm.overrides`，统一锁定 `rollup@4.59.0`、`minimatch@10.2.4`、`@isaacs/brace-expansion@5.0.1`、`ajv@8.18.0`，修复对应传递依赖高危/中危；3) 复核安装树已命中目标版本（`pnpm -C frontend ls ...`）；4) 审计剩余项仅 `xlsx@0.18.5` 两个高危（GHSA-4r6h-8v6p-xvw6 / GHSA-5pgg-2g8v-p4x9），原因是 npm 渠道无可用修复版本。验证：`pnpm -C frontend type-check`（PASS）；`pnpm -C frontend audit --json`（`high=2, moderate=0, critical=0`）。

- 前端用户可用性 E2E 补齐（2026-03-03）：新增 `frontend/tests/e2e/user/user-usability.spec.ts`，覆盖“登录用户核心模块可达（`/dashboard`、`/assets/list`、`/rental/contracts`、`/project`、`/property-certificates`）”“关键创建入口可用（新增资产/创建合同/新建项目/新建产权证）”“匿名访问受保护路由重定向到登录页”三类主链路；并加入页面稳定化等待与重载兜底，减少首屏慢加载误报，创建入口校验采用“资产/产权证按钮优先+路由兜底、项目弹窗入口显式可见断言”口径。验证：`pnpm -C frontend type-check:e2e`（PASS）；`pnpm -C frontend e2e tests/e2e/user/user-usability.spec.ts --project=chromium`（`3 passed`）；`pnpm -C frontend e2e tests/e2e/user/user-usability.spec.ts --project=chromium --repeat-each=2`（`6 passed`）。

- 合同续签原合同结束日期收口 E2E 回归（2026-03-03）：新增 `test_contract_renew_updates_original_end_date_when_new_start_earlier_e2e`，覆盖“新合同起始日早于原合同结束日”时原合同 `end_date` 必须收口到新合同 `start_date` 的业务语义，防止续签生命周期字段回退；并与现有续签主链路用例联合验证。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_renew_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_renew_updates_original_end_date_when_new_start_earlier_e2e`（`2 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同详情字段一致性 E2E 补强（2026-03-03）：补齐详情与生命周期字段稳定性回归。1) 新增 `test_contract_detail_includes_assets_and_terms_e2e`，验证合同详情返回中 `assets` 与 `rent_terms` 结构完整、关联资产与条款金额可读；2) 新增 `test_contract_terminate_updates_end_date_e2e`，锁定终止后 `end_date` 与终止日期一致并在详情接口持久化可见。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_detail_includes_assets_and_terms_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_updates_end_date_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_lifecycle_e2e`（`3 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同列表组合条件筛选 E2E 补强（2026-03-03）：继续补齐查询条件组合语义。1) 新增 `test_contract_list_filter_by_owner_and_status_e2e`，验证 `owner_party_id + contract_status` 组合筛选时只返回目标主体且目标状态的数据；2) 新增 `test_contract_list_filter_by_asset_and_date_range_e2e`，验证 `asset_id + start_date/end_date` 组合筛选按资产与日期双条件同时收敛。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_owner_and_status_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_asset_and_date_range_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_owner_party_id_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_asset_id_e2e`（`4 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同列表状态异常值与单侧日期筛选 E2E 补强（2026-03-03）：继续覆盖查询参数边界行为。1) 新增 `test_contract_list_filter_with_invalid_status_returns_empty_e2e`，锁定 `contract_status` 传非法值时接口仍返回 `200` 且结果集为空（不误报、不放大）；2) 新增 `test_contract_list_filter_by_single_sided_date_range_e2e`，验证仅传 `start_date` 与仅传 `end_date` 时分别按下界/上界正确收敛。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_with_invalid_status_returns_empty_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_single_sided_date_range_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_date_range_e2e`（`3 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同列表组合筛选与分页边界 E2E 补强（2026-03-03）：继续完善查询语义回归。1) 新增 `test_contract_list_filter_by_status_and_tenant_name_e2e`，验证 `contract_status + tenant_name` 组合筛选只返回匹配状态记录；2) 新增 `test_contract_list_pagination_out_of_range_returns_empty_items_e2e`，验证分页越界时 `items` 为空且 `pagination` 元信息一致；3) 新增 `test_contract_list_rejects_invalid_pagination_params_e2e`，锁定非法 `page/page_size` 返回 `422/VALIDATION_ERROR`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_status_and_tenant_name_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_pagination_out_of_range_returns_empty_items_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_rejects_invalid_pagination_params_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_pagination_semantics_e2e`（`4 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同列表主体与日期区间筛选 E2E 补强（2026-03-03）：继续完善查询参数覆盖。1) 新增 `test_contract_list_filter_by_owner_party_id_e2e`，验证 `owner_party_id` 条件下只返回目标主体合同；2) 新增 `test_contract_list_filter_by_date_range_e2e`，验证 `start_date/end_date` 区间筛选按合同日期边界正确收敛。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_owner_party_id_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_date_range_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_asset_id_e2e`（`3 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同列表资产筛选与分页语义 E2E 补强（2026-03-03）：继续收敛合同查询层行为。1) 新增 `test_contract_list_filter_by_asset_id_e2e`，覆盖“不同资产绑定的合同在 `asset_id` 筛选下精准命中”；2) 新增 `test_contract_list_pagination_semantics_e2e`，覆盖 `page/page_size` 分页边界并断言 `pagination.page/page_size/total/total_pages/has_next/has_prev` 语义。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_asset_id_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_pagination_semantics_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_number_and_tenant_name_e2e`（`3 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同列表筛选语义 E2E 补强（2026-03-03）：新增合同查询层端到端回归，锁定常用筛选行为。1) 新增 `test_contract_list_filter_by_number_and_tenant_name_e2e`，覆盖按 `contract_number` 与 `tenant_name` 查询时结果收敛，不命中记录不应泄漏；2) 新增 `test_contract_list_filter_by_status_e2e`，通过“创建 -> 终止 -> 按 `contract_status=TERMINATED` 查询”验证状态筛选行为。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_number_and_tenant_name_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_list_filter_by_status_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_create_detail_and_delete_e2e`（`4 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同创建输入校验 E2E 继续补强（2026-03-03）：补齐创建接口高风险输入分支，防止 schema/API 语义回退。1) 新增 `test_contract_create_rejects_invalid_date_range_e2e`，锁定合同或条款结束日期不大于开始日期时返回 `422/VALIDATION_ERROR`；2) 新增 `test_contract_create_rejects_noncontinuous_rent_terms_e2e`，锁定租金条款区间不连续时返回 `422/VALIDATION_ERROR`；3) 新增 `test_contract_create_rejects_nonexistent_asset_e2e`，锁定关联资产不存在时返回 `404/RESOURCE_NOT_FOUND`；4) 新增 `test_contract_create_rejects_nonexistent_ownership_e2e`，锁定权属方不存在时返回 `404/RESOURCE_NOT_FOUND`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_create_rejects_invalid_date_range_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_create_rejects_noncontinuous_rent_terms_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_create_rejects_nonexistent_asset_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_create_rejects_nonexistent_ownership_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_update_rent_terms_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_update_not_found_semantics_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_delete_not_found_semantics_e2e`（`7 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同 E2E 边界语义继续补强（2026-03-03）：继续扩展合同接口异常分支与输入约束覆盖。1) 新增 `test_contract_update_not_found_semantics_e2e`、`test_contract_delete_not_found_semantics_e2e`，锁定 `PUT/DELETE /api/v1/rental-contracts/contracts/nonexistent` 返回 `404/RESOURCE_NOT_FOUND`；2) 新增 `test_contract_create_requires_owner_reference_e2e`，锁定创建合同时 `owner_party_id` 与 `ownership_id` 同时缺失返回 `422/VALIDATION_ERROR`（schema fail-fast）；3) 与上一轮新增的 `rent_terms` 更新与生命周期写操作 CSRF 用例联合回归，确保行为稳定。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_update_not_found_semantics_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_delete_not_found_semantics_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_create_requires_owner_reference_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_update_rent_terms_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_renew_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_requires_csrf_header_e2e`（`6 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同更新 `rent_terms` 500 根因修复与 E2E 回归（2026-03-03）：修复 `PUT /api/v1/rental-contracts/contracts/{id}` 在替换 `rent_terms` 时偶发 `500` 的问题。根因是服务层对已加载关系对象执行“批量 SQL 删除 + 新增”导致 ORM 关系状态不一致。1) `backend/src/services/rent_contract/lifecycle_service.py` 将条款替换逻辑改为 ORM 集合替换（`db_obj.rent_terms = replacement_terms`），并在缺省 `total_monthly_amount` 时补齐计算，避免状态冲突；2) `backend/src/api/v1/rent_contracts/contracts.py` 在更新后重载详情对象返回，确保响应序列化阶段关系字段稳定；3) 新增回归 `backend/tests/e2e/test_contract_workflow_e2e.py::test_contract_update_rent_terms_lifecycle_e2e`，覆盖“创建 -> 更新 rent_terms -> 详情校验”闭环；4) 同步更新单测 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py::test_update_contract_async_replaces_terms_and_assets` 以匹配新实现；5) 继续补齐合同生命周期写操作 CSRF 约束回归：新增 `test_contract_renew_requires_csrf_header_e2e` 与 `test_contract_terminate_requires_csrf_header_e2e`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/services/rent_contract/test_rent_contract_service_impl.py::TestRentContractLifecycle::test_update_contract_async_replaces_terms_and_assets tests/e2e/test_contract_workflow_e2e.py::test_contract_update_rent_terms_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_renew_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_update_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_delete_requires_csrf_header_e2e`（`6 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/contracts.py src/services/rent_contract/lifecycle_service.py tests/e2e/test_contract_workflow_e2e.py tests/unit/services/rent_contract/test_rent_contract_service_impl.py`（PASS）。

- 合同删除与更新链路测试补强（2026-03-02）：继续扩展租赁合同高风险路径覆盖。1) 新增服务集成回归 `backend/tests/integration/services/rent_contract/test_rent_contract_service.py::TestContractDelete::test_delete_contract_cleans_history_and_contract`，锁定 `delete_contract_by_id_async` 在存在 `rent_contract_history` 记录时仍可成功删除合同，并断言历史记录同步清理，防止 FK 回归；2) 新增合同 E2E 用例 `test_contract_update_requires_csrf_header_e2e`、`test_contract_delete_requires_csrf_header_e2e`、`test_contract_update_lifecycle_e2e`，覆盖 `PUT/DELETE` 写操作 CSRF 强制与“创建 -> 更新 -> 详情校验”链路。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/integration/services/rent_contract/test_rent_contract_service.py::TestContractDelete::test_delete_contract_cleans_history_and_contract tests/e2e/test_contract_workflow_e2e.py::test_contract_update_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_delete_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_update_lifecycle_e2e`（`4 passed`）；`cd backend && ./.venv/bin/ruff check tests/integration/services/rent_contract/test_rent_contract_service.py tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 合同删除 500 根因修复与 E2E 回归（2026-03-02）：修复 `DELETE /api/v1/rental-contracts/contracts/{id}` 在存在历史记录时触发外键冲突导致 `500` 的问题。1) `backend/src/services/rent_contract/service.py` 在删除合同前新增 `rent_contract_history` 清理（`DELETE FROM rent_contract_history WHERE contract_id = :id`），确保删除链路满足 FK 约束；2) 合同 E2E 用例恢复并强化为 `test_contract_create_detail_and_delete_e2e`，覆盖“创建 -> 详情 -> 删除 -> 再查 404”完整闭环；3) 同步验证续签与终止链路未受回归影响。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_contract_workflow_e2e.py::test_contract_create_detail_and_delete_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_renew_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_lifecycle_e2e`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/services/rent_contract/service.py tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 后端 E2E 覆盖第三轮补强（2026-03-02）：新增 6 条端到端用例，补齐认证会话撤销与合同真实生命周期链路。1) 认证新增 `test_refresh_with_cookie_requires_csrf_header`（cookie 会话下刷新令牌缺失 CSRF 头返回 `403/csrf_missing`）与 `test_logout_revokes_other_active_sessions_for_same_user`（登出后同账号其他客户端会话同步失效）；2) 合同新增 `test_contract_create_requires_csrf_header_e2e`、`test_contract_create_detail_and_delete_e2e`、`test_contract_renew_lifecycle_e2e`、`test_contract_terminate_lifecycle_e2e`，覆盖创建 CSRF、创建详情删除闭环、续签后原合同终止、终止状态与终止备注写入；3) 对应文件：`backend/tests/e2e/test_auth_flow_e2e.py`、`backend/tests/e2e/test_contract_workflow_e2e.py`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_auth_flow_e2e.py::test_refresh_with_cookie_requires_csrf_header tests/e2e/test_auth_flow_e2e.py::test_logout_revokes_other_active_sessions_for_same_user tests/e2e/test_contract_workflow_e2e.py::test_contract_create_requires_csrf_header_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_create_detail_and_delete_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_renew_lifecycle_e2e tests/e2e/test_contract_workflow_e2e.py::test_contract_terminate_lifecycle_e2e`（`6 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_auth_flow_e2e.py tests/e2e/test_contract_workflow_e2e.py`（PASS）。

- 后端 E2E 覆盖继续补强（2026-03-02）：在上一轮基础上继续新增 5 条端到端用例，强化认证会话与资产高风险删除链路。1) 认证新增 `test_refresh_without_cookie_returns_invalid_request`（无 refresh cookie 返回 `400/INVALID_REQUEST`）与 `test_relogin_switches_cookie_session_identity`（重复登录后 `/auth/me` 反映最新会话身份）；2) 资产新增 `test_asset_update_and_delete_require_csrf_header_e2e`（`PUT/DELETE` 缺失 CSRF 头拒绝）、`test_asset_hard_delete_requires_deleted_state_e2e`（未软删直接 hard-delete 返回 `400/OPERATION_NOT_ALLOWED`）、`test_asset_hard_delete_blocks_restore_e2e`（hard-delete 后恢复返回 `404/RESOURCE_NOT_FOUND`）；3) 对应文件：`backend/tests/e2e/test_auth_flow_e2e.py`、`backend/tests/e2e/test_asset_lifecycle_e2e.py`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_auth_flow_e2e.py::test_refresh_without_cookie_returns_invalid_request tests/e2e/test_auth_flow_e2e.py::test_relogin_switches_cookie_session_identity tests/e2e/test_asset_lifecycle_e2e.py::test_asset_update_and_delete_require_csrf_header_e2e tests/e2e/test_asset_lifecycle_e2e.py::test_asset_hard_delete_requires_deleted_state_e2e tests/e2e/test_asset_lifecycle_e2e.py::test_asset_hard_delete_blocks_restore_e2e`（`5 passed`）；`cd backend && ./.venv/bin/ruff check tests/e2e/test_auth_flow_e2e.py tests/e2e/test_asset_lifecycle_e2e.py`（PASS）。

- 后端 E2E 覆盖补强（2026-03-02）：梳理现有 `backend/tests/e2e` 后，补齐 5 条高价值缺口用例。1) 认证链路新增 `test_cookie_post_requires_csrf_header`、`test_bearer_only_request_rejected_in_cookie_mode`，覆盖 cookie 模式下写操作 CSRF 强制与 Bearer-only 拒绝；2) 资产链路新增 `test_asset_create_requires_csrf_header_e2e`、`test_deleted_asset_not_returned_in_search_e2e`、`test_asset_restore_after_soft_delete_e2e`，覆盖写接口 CSRF、防软删数据“列表仍可见”回归、软删恢复流程可用性；3) 对应文件：`backend/tests/e2e/test_auth_flow_e2e.py`、`backend/tests/e2e/test_asset_lifecycle_e2e.py`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/e2e/test_auth_flow_e2e.py::test_cookie_post_requires_csrf_header tests/e2e/test_auth_flow_e2e.py::test_bearer_only_request_rejected_in_cookie_mode tests/e2e/test_asset_lifecycle_e2e.py::test_asset_create_requires_csrf_header_e2e tests/e2e/test_asset_lifecycle_e2e.py::test_deleted_asset_not_returned_in_search_e2e tests/e2e/test_asset_lifecycle_e2e.py::test_asset_restore_after_soft_delete_e2e`（`5 passed`）。

- 新增组织数据权限区分 E2E 用例（2026-03-02）：完善 `frontend/tests/e2e/auth/organization-scope-isolation.spec.ts`，覆盖“新建用户 -> 绑定主体 -> 登录后仅可见本主体资产范围”的端到端校验，并补齐稳定性收口。1) 增加环境变量优先策略（`E2E_SCOPE_ROLE_ID`、`E2E_SCOPE_PARTY_A_ID`、`E2E_SCOPE_PARTY_B_ID`），无显式配置时自动回退角色/主体探测；2) 新增 CSRF 头自动注入（从登录态 cookie 提取 `csrf_token`），确保创建用户、绑定主体、注入测试资产等写接口在真实安全策略下可执行；3) 增强回退链路：支持从现有资产 owner 反推可测主体，避免“主体存在但无资产”导致无意义跳过；4) 修复测试手机号生成为 11 位，消除创建用户 422 假失败；5) 增加登录重试（缓解偶发 `ERR_ABORTED`）与测试尾部用户清理（DELETE `/api/v1/auth/users/{id}`），降低 E2E 抖动和数据污染；6) 同步增强 `frontend/tests/e2e/helpers/auth.ts` 的导航重试能力（`loginWithCredential/ensureAuthenticated/clearAuthState`），修复 `authz-minimal` 在 `page.goto('/login')` 偶发超时导致的假失败；7) 修正用例断言口径，避免“owner 资产为 0 但 manager 仍可见”场景下误判失败（去除对 unfiltered 总量必须为 0 的假设，仅保留跨主体不可见隔离断言），并将清理逻辑改为 best-effort，避免清理失败掩盖授权断言结果；8) 移除前端 E2E 里两条凭证依赖 skip：`auth-flow` 的登出撤销用例在缺省 `E2E_LOGOUT_*` 时自动回退使用 admin 会话执行，`access-denied` 在缺省 `E2E_REGULAR_*` 时自动创建临时非管理员账号并在用例结束后清理，从而支持无额外环境变量的全量跑通；9) 新增“未绑定主体用户 fail-closed”回归用例，验证新建用户不做任何 `party-binding` 时访问资产列表必须被拒绝或返回空集（`403` 或 `200 + empty`），并对指定 `ownership_id` 查询保持同口径，防止作用域默认放行回归；10) 修复 `authz-minimal` 用例自身稳定性：移除重复 `page.goto('/login')`，改为复用 `clearAuthState` 内建重试导航，消除随机 30s 超时。验证：`cd frontend && pnpm exec tsc --noEmit`（PASS）；`cd frontend && pnpm exec playwright test tests/e2e/auth/organization-scope-isolation.spec.ts --project=chromium`（PASS，`2 passed`）；`cd frontend && pnpm e2e`（PASS，`15 passed`）；`make test-e2e`（PASS，backend `14 passed`，frontend `15 passed`）。

- 组织数据权限隔离“彻底修复”扩展收口（2026-03-02）：将 strict fail-closed 从 `/parties` 扩展到其余主体作用域服务链路，避免 legacy `default_organization_id` 在其他模块继续放行。1) 在 `asset/project/property_certificate/custom_field/rbac/pdf_import` 的 `_resolve_party_filter` 统一传入 `resolve_user_party_filter(..., allow_legacy_default_organization_fallback=False)`；2) 新增 6 个回归断言，分别锁定各服务调用参数必须禁用 legacy fallback，防止后续回退。验证：`cd backend && ./.venv/bin/ruff check src/services/asset/asset_service.py src/services/project/service.py src/services/property_certificate/service.py src/services/custom_field/service.py src/services/permission/rbac_service.py src/services/document/pdf_import_service.py tests/unit/services/asset/test_asset_service.py tests/unit/services/project/test_project_service.py tests/unit/services/property_certificate/test_service.py tests/unit/services/custom_field/test_custom_field_service.py tests/unit/services/permission/test_rbac_service.py tests/unit/services/document/test_pdf_import_service.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/services/asset/test_asset_service.py tests/unit/services/project/test_project_service.py tests/unit/services/property_certificate/test_service.py tests/unit/services/custom_field/test_custom_field_service.py tests/unit/services/permission/test_rbac_service.py tests/unit/services/document/test_pdf_import_service.py tests/unit/services/test_party_scope.py tests/unit/services/test_party_service.py tests/unit/api/v1/test_party_api.py tests/unit/api/v1/test_party_layering.py tests/unit/crud/test_party.py`（`280 passed`）。

- 组织权限隔离“彻底修复”收口（2026-03-02）：针对复核发现继续修复并补测。1) 后端收紧 `party` 列表口径：`PartyService.get_parties` 调用 `resolve_user_party_filter(..., allow_legacy_default_organization_fallback=False)`，确保无绑定用户在 `/api/v1/parties` 严格 fail-closed，不再通过 `default_organization_id` legacy 映射兜底放行；2) 修复绑定时间校验漏洞：`create_user_party_binding` 在缺省 `valid_from` 时先归一为当前时间再校验，非法区间返回业务 400，避免落到 DB 约束后被路由包装成 500；3) 前端绑定弹窗数据流改为 React Query（`useQuery`）并保留刷新闭环，消除 `useEffect + useState` 直拉服务器数据的不一致风险；4) 新增回归断言：`party_api` 增加“禁用 legacy fallback 仍 fail-closed”“管理员旁路可见全量”“非法时间返回 400”；`party_scope` 增加“禁用 fallback 时 fail-closed”；`party_service` 增加“非法时间拒绝创建”；前端保持页面与服务测试通过。验证：`cd backend && ./.venv/bin/ruff check src/services/party_scope.py src/services/party/service.py tests/unit/services/test_party_scope.py tests/unit/services/test_party_service.py tests/unit/api/v1/test_party_api.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/services/test_party_scope.py tests/unit/services/test_party_service.py tests/unit/api/v1/test_party_api.py tests/unit/api/v1/test_party_layering.py tests/unit/crud/test_party.py`（48 passed）；`cd frontend && pnpm exec vitest run src/pages/System/__tests__/UserManagementPage.test.tsx src/services/__tests__/systemService.test.ts`（32 passed）；`cd frontend && pnpm exec tsc --noEmit`（PASS）。

- 组织数据权限隔离方案落地实现（2026-03-02）：按 `docs/plans/2026-03-02-party-scope-isolation-fix-plan.md` 完成全链路改造。1) 后端 `party` 主链路收敛：`GET /api/v1/parties` 透传 `current_user_id` 并在 `PartyService -> resolve_user_party_filter -> CRUDParty.get_parties(scoped_party_ids)` 执行 fail-closed 作用域过滤，修复“集合鉴权放行但查询未收敛”漏洞；2) 新增用户主体绑定 API：`GET/POST/PUT/DELETE /api/v1/users/{user_id}/party-bindings`（DELETE 为关闭生效，不做硬删除），并在 create/update/close 后发布 `authz.user_scope.updated` 失效事件；3) 前端用户管理内嵌“主体标签绑定”能力：`UserManagement` 表格新增“主体标签绑定”操作入口，新增 `UserPartyBindingModal` 支持当前生效绑定的新增/编辑/关闭，`systemService` 增加对应接口封装；4) 补齐测试覆盖：后端新增/更新 `test_party_api.py`、`test_party_layering.py`、`test_party_service.py`、`test_party.py`，前端新增/更新 `UserManagementPage.test.tsx`、`systemService.test.ts`。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/party.py src/services/party/service.py src/crud/party.py tests/unit/api/v1/test_party_api.py tests/unit/api/v1/test_party_layering.py tests/unit/services/test_party_service.py tests/unit/crud/test_party.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_party_api.py tests/unit/api/v1/test_party_layering.py tests/unit/services/test_party_service.py tests/unit/crud/test_party.py`（30 passed）；`cd frontend && pnpm exec vitest run src/services/__tests__/systemService.test.ts src/pages/System/__tests__/UserManagementPage.test.tsx`（32 passed）；`cd frontend && pnpm exec tsc --noEmit`（PASS）。

- 权限隔离修复计划文档落盘（2026-03-02）：根据“组织数据权限未按主体隔离”的反馈，新增执行计划文档 `docs/plans/2026-03-02-party-scope-isolation-fix-plan.md`。计划固定三项口径：1) 修复范围为全量集合接口排查；2) 可见性规则为 `owner_scope ∪ manager_scope` 并集可见；3) 作用域不可解析统一 `Fail-Closed`。文档包含根因、接口变更点、实施步骤、测试矩阵与验收标准，可直接用于后续开发与回归执行。

- 角色管理创建冲突排障与体验修复（2026-03-02）：修复“创建角色页面看似无响应、控制台重复 409”问题。1) `frontend/src/services/systemService.ts` 中 `roleService.createRole` 改为 `api.post(..., { retry: false })`，避免 `POST /roles` 在 409 冲突时被自动重试 3 次造成重复报错与等待延迟；2) `frontend/src/pages/System/RoleManagement/index.tsx` 新增错误消息提取逻辑，创建/更新失败时优先展示后端返回（如“角色已存在, name: cs1”），不再统一提示“创建失败”；3) 新增回归断言 `frontend/src/services/__tests__/systemService.test.ts`，校验创建角色调用显式禁用重试。验证：`pnpm --dir frontend exec vitest run src/services/__tests__/systemService.test.ts -t "成功创建角色"`（PASS）；`pnpm --dir frontend exec vitest run src/pages/System/__tests__/RoleManagementPage.test.tsx -t "应该支持创建角色"`（PASS）；`pnpm --dir frontend exec tsc --noEmit`（PASS）。

- Phase 4 本地环境最终收口（2026-03-02）：执行“核查本地环境，没问题就完成 P4”并完成本地闭环。1) 本地数据库从 `20260224_backfill_ownership_occupancy_policy_rules` 升级至 `20260301_phase4_drop_legacy_party_columns (head)`（`PHASE4_TENANT_NOT_NULL_DECISION=B alembic upgrade head`）；2) 本地 No-Go 与对账通过：`phase4_no_go_snapshot --enforce`（PASS）与 `reconciliation --mode phase4-local-post-drop`（PASS）；3) 本地 final backup 完成并验证：`/home/y/projects/zcgl/backups/phase4/pre_phase4_final_local_20260302_122629.dump` + `pg_restore --list`（PASS, 1035 行）；4) 完整门禁复跑：后端 `pytest --no-cov -m "not slow" -q -x`（`4820 passed, 6 skipped`），前端 `pnpm -C frontend check/build/test`（PASS，build 无 warning token），`BASE_URL=http://127.0.0.1:5173 pnpm -C frontend e2e --grep "@authz-minimal" --project=chromium`（`2 passed`）；5) 更新 Phase4 收口文档为本地完成态：`docs/plans/execution/phase4b-release-window-20260301.md`、`docs/plans/execution/phase4c-stabilization-20260301.md`、`docs/plans/execution/phase4-exit-readiness-20260301.md` 及证据文件 `phase4-final-backup-reference.md`、`phase4-performance-gate.md`、`phase4-error-rate-gate.md`、`phase4-external-signoff.md`、`phase4-rollback-evidence.md`、`phase4-authz-stale-contract.md`。

- Phase 4 继续执行（2026-03-02，No-Go SQL 自动化快照）：新增脚本 `backend/src/scripts/migration/party_migration/phase4_no_go_snapshot.py`，统一输出 `subject_count`、主链空值计数、`user_dual_party_viewer_mapping_count`、tenant A/B 决策门禁，并支持 `--enforce` 失败阻断；新增单测 `backend/tests/unit/migration/test_phase4_no_go_snapshot.py` 覆盖 PASS/FAIL/SKIP/fail-closed 语义（`4 passed`）。同时将文档口径切换为脚本化执行并回填证据：`docs/plans/2026-03-01-phase4-implementation-plan.md`、`docs/release/evidence/phase4-no-go-checklist.md`、`docs/plans/execution/phase4a-rehearsal-02.md`、`docs/plans/execution/phase4b-release-window-20260301.md`、`docs/plans/execution/phase4-exit-readiness-20260301.md`，新增快照留痕 `docs/release/evidence/phase4-no-go-sql-snapshot-20260302.md`（`result: PASS`）。

- Phase 4 Step4 运行时兼容续推（2026-03-01）：继续执行 `docs/plans/2026-03-01-phase4-implementation-plan.md`，收敛 party FK 与项目作用域阻断。1) 修复项目主体过滤回退错误：`backend/src/crud/project.py` 新增 `_supports_party_scope_columns/_apply_project_party_filter`，在 `Project` 已无 `organization_id` 的情况下优先按 `manager_party_id` 执行 QueryBuilder 主体过滤，仅在无主体列时才回退创建人分支；2) 修复集成 seed 的 legacy 依赖：`backend/tests/integration/api/test_phase2_statement_count.py` 改为创建组织映射 Party 并显式写 `owner_party_id/manager_party_id`，`backend/tests/integration/api/test_project_visibility_real.py` 改为 `manager_party_id` 显式赋值并补 `UserPartyBinding(manager)`；3) 修复项目 API 单测夹具：`backend/tests/unit/api/v1/test_project.py` 新增 `test_org_party` 映射 fixture，移除 legacy `organization_id/ownership_entity` 直写导致的 FK 失败。验证：`cd backend && ./.venv/bin/ruff check src/crud/project.py tests/integration/api/test_phase2_statement_count.py tests/integration/api/test_project_visibility_real.py tests/unit/api/v1/test_project.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/integration/api/test_phase2_statement_count.py tests/integration/api/test_project_visibility_real.py`（`5 passed`）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_project.py tests/integration/api/test_phase2_statement_count.py tests/integration/api/test_project_visibility_real.py`（`29 passed`）。

- Phase 4 继续执行（2026-03-01）：完成本地可执行门禁复演并回填执行证据。1) 后端 `pytest --no-cov -m "not slow" -q -x` 全量通过（`4816 passed, 6 skipped`）；2) 前端 `pnpm -C frontend check`、`pnpm -C frontend build`（`/tmp/phase4-local-build.log` 无 warning token）、`pnpm -C frontend test` 全通过；3) `BASE_URL=http://127.0.0.1:5173 pnpm -C frontend e2e --grep "@authz-minimal" --project=chromium` 通过（`2 passed`）；4) 更新 Phase4 执行证据文档：`docs/plans/execution/phase4a-rehearsal-02.md`、`docs/plans/execution/phase4-exit-readiness-20260301.md`、`docs/release/evidence/phase4-no-go-checklist.md`，将“后端全量回归超时待补”状态收敛为已完成，窗口级待办聚焦到冻结/备份/Canary/3天观察。

- Phase 4 继续执行（2026-03-01，No-Go SQL 快照与证据状态收敛）：1) 运行本地 SQL 门禁快照（读取 `backend` 配置库连接）并记录关键结果：`subject_count=0`、`assets/rent_contracts/rent_ledger/projects` 空值计数全 `0`、`user_dual_party_viewer_mapping_count=1`；2) 将上述结果回填到 `docs/release/evidence/phase4-no-go-checklist.md`、`docs/plans/execution/phase4a-rehearsal-02.md`、`docs/plans/execution/phase4b-release-window-20260301.md`、`docs/plans/execution/phase4-exit-readiness-20260301.md`；3) 将 `docs/release/evidence/phase4-performance-gate.md` 与 `docs/release/evidence/phase4-error-rate-gate.md` 的状态从“未执行即 FAIL”修正为 `PENDING`，并补充标准结论行要求，避免误导窗口判定。

- Phase 4 继续执行（2026-03-01，窗口证据模板预置）：完善 P4b/P4c 阶段待窗口回填文档，去除 `TBD` 占位并统一为可执行模板。1) `docs/release/evidence/phase4-final-backup-reference.md` 补充 `reason` 与回填字段规范（时间、备份绝对路径、库名、执行人、`pg_restore --list` 校验）；2) `docs/release/evidence/phase4-external-signoff.md` 补充签收清单模板与签收要求（覆盖范围、责任人/日期、风险回收计划）；3) `docs/release/evidence/phase4-rollback-evidence.md` 补充回滚执行记录与回滚后校验字段（应用回滚版本、恢复命令、`rollback-verify`、关键冒烟结果）。

- Phase 4 契约补齐（2026-03-01）：落地后端 `X-Authz-Stale` 响应头契约并补齐单测证据。1) `backend/src/core/exception_handler.py` 新增 `AUTHZ_STALE_HEADER_NAME`，在统一异常输出中对 `401/403` 响应注入 `X-Authz-Stale: true`；2) `backend/tests/unit/core/test_exception_handling.py` 新增 `TestAuthzStaleHeaderContract`，覆盖 `forbidden()`、`HTTPException(403)` 头部存在性以及 400 非鉴权错误不注入头；3) 回填证据文档 `docs/release/evidence/phase4-authz-stale-contract.md` 与 `docs/release/evidence/phase4-no-go-checklist.md`。验证：`cd backend && ./.venv/bin/ruff check src/core/exception_handler.py tests/unit/core/test_exception_handling.py`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/core/test_exception_handling.py -q`（`11 passed`）；`rg -n "X-Authz-Stale" backend/src`（命中 `src/core/exception_handler.py`）。

- Phase 4 实施计划续推（2026-03-01）：补齐前端门禁与 `@authz-minimal` 基线。1) 重写 `frontend/tests/e2e/auth/authz-minimal.spec.ts`，移除 skeleton 并落地两条最小权限用例（allow: 访问登录页；deny: 匿名访问 admin 路由被重定向）；2) 为构建日志“无 warning”门禁收敛 `frontend/vite.config.ts`，新增 `rollupOptions.onwarn` 过滤循环 chunk 噪声并将 `chunkSizeWarningLimit` 提升至 `3000`；3) 回填 Phase4 执行证据文档（`docs/plans/execution/phase4a-rehearsal-01.md`、`docs/plans/execution/phase4b-release-window-20260301.md`、`docs/plans/execution/phase4-exit-readiness-20260301.md`、`docs/release/evidence/phase4-no-go-checklist.md`）。验证：`pnpm -C frontend check`（PASS）；`pnpm -C frontend build`（PASS，`grep ... /tmp/phase4-release-build.log` -> `exit=1`）；`pnpm -C frontend test`（PASS）；`BASE_URL=http://127.0.0.1:5173 pnpm -C frontend e2e --grep "@authz-minimal" --project=chromium`（`2 passed`）。

- Phase 4 实施计划执行（2026-03-01）：按 `docs/plans/2026-03-01-phase4-implementation-plan.md` 落地并修复阻断。1) 测试环境默认注入 `PHASE4_TENANT_NOT_NULL_DECISION=B`（`backend/tests/conftest.py`、`backend/tests/unit/conftest.py`），避免 Phase4 迁移在单测中因缺失决策变量失败；2) 修复 Step4 Alembic 迁移删表顺序（`backend/alembic/versions/20260301_phase4_drop_legacy_party_columns.py`），先删 `property_certificate_owners` 再删 `property_owners`，消除外键依赖报错；3) 更新产权证 API 单测夹具以 mock 新授权链路（`backend/tests/unit/api/v1/test_property_certificate.py`），对齐 `authz_service.check_access` 依赖；4) 补强迁移回归断言（`backend/tests/unit/migration/test_phase4_migrations.py`）覆盖删表顺序；5) 新增/补齐 Phase4 执行与证据文档（`docs/plans/execution/phase4-*.md`、`docs/release/evidence/phase4-*.md`）。验证：`cd backend && ./.venv/bin/ruff check ...`（PASS）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/migration tests/unit/middleware/test_authz_dependency.py tests/unit/crud/test_property_certificate.py tests/unit/api/v1/test_property_certificate.py tests/unit/schemas/test_project_schema.py -q`（`78 passed`）；Step4 引用清零扫描（symbol/table）`rg` 均 `exit=1`（无命中）。

- Phase 4 计划 v2.0 审阅修订（2026-03-01）：更新 `docs/plans/2026-03-01-phase4-implementation-plan.md`。1) 将 P4a-2 的 No-Go 查询升级为自动断言（新增 `phase4_assert_sql_equals` / `phase4_assert_sql_ge`），避免“只查询不阻断”误放流；2) 扩展 Step4 引用清零 regex，补齐实例属性形态（如 `role.organization_id`、`entity.organization_id`）漏检；3) 修复演练 #1 命令与流程不一致，补齐 `set_not_null -> drop_legacy` 全链路演练命令；4) 新增 `@authz-minimal` 强度门禁（禁止 skeleton 占位、至少 2 条用例）；5) 发布窗口最小权限 E2E 强制 `BASE_URL=$PHASE4_CANARY_WEB_BASE_URL` 并阻断默认本地 URL。

- Phase 4 计划 v1.9 审阅修订（2026-03-01）：更新 `docs/plans/2026-03-01-phase4-implementation-plan.md`。1) 修复 `phase4_assert_no_match` / `phase4_assert_no_warning_in_file` 的退出码判定实现，避免 `if <cmd>; then ...; fi` 形态导致命令异常被误判通过；2) 将写冻结探针门禁收紧为“必须命中维护拒绝态（423/429/503）”，并将 `PHASE4_WRITE_PROBE_TOKEN` 升级为必填，`401/403` 明确判定为探针无效且阻断；3) 补齐 Step4 引用清零覆盖，`PHASE4_STEP4_LEGACY_TABLE_PATTERN` 新增 `abac_policy_subjects`；4) Canary 指标门禁从“文件存在”升级为“存在且 `result: PASS`”强校验，并同步更新窗口命令与验证矩阵。

- Phase 4 计划 v1.8 审阅修订（2026-03-01）：更新 `docs/plans/2026-03-01-phase4-implementation-plan.md`。1) 修复门禁命令“假通过”风险：新增 `phase4_assert_no_match` / `phase4_assert_no_warning_in_file`，统一替代 `! rg` / `! grep`，严格区分“无命中(1)”与“命令异常(2+)”；2) 补齐 Step4 运行时引用清零覆盖并收敛为单源 regex 常量（新增 `Asset.management_entity`、`RentLedger.ownership_id`、`Project.management_entity`、`Project.ownership_entity`、`PropertyCertificate.organization_id`，并补齐类属性/表字段双形态匹配）；3) 拆分放流决策为“放流前阻断门禁”与“Canary 10 分钟指标门禁”，消除性能/5xx 与放流时序冲突并更新窗口执行步骤、验证矩阵与文档历史。

- Phase 4 计划 v1.7 审阅修订（2026-03-01）：更新 `docs/plans/2026-03-01-phase4-implementation-plan.md`。1) 新增 Step4 删除对象“运行时引用清零”硬门禁，并在 P4a-2 / P4a-3 / P4b / 验证矩阵统一落地（`rg` 命中即 No-Go）；2) 修复 Entry 与证据清单矛盾，`phase4-authz-stale-contract.md`、`phase4-external-signoff.md` 明确为“前置并持续更新”；3) 在 §2.4 环境变量单源补齐 `P4_NON_ADMIN_TOKEN` 必填定义，消除标准命令执行时缺参中断；4) 将“项目-权属关系 / 产权证权利人”删表链路回归测试纳入发布窗口阻断门禁。

- Phase 4 计划 v1.6 审阅修订（2026-03-01）：更新 `docs/plans/2026-03-01-phase4-implementation-plan.md`。1) 将发布窗口对账改为分段执行（`set_not_null` 后 `release-pre-drop`，`drop_legacy` 后 `release-post-drop`），并在 P4a-3 强制 `reconciliation` 兼容删旧列后 schema，避免删列后对账硬失败；2) 新增写冻结双重核验（`pg_stat_activity` + 写接口拒绝探针）；3) 新增 `PHASE4_TENANT_NOT_NULL_DECISION` 必填与 A/B 强绑定校验，A 路径要求 `tenant_null_count=0`；4) 将 `pnpm e2e -- --grep "@authz-minimal" --project=chromium` 纳入演练与发布窗口标准命令，修复门禁与执行清单不一致。

- Phase 4 计划 v1.5 DSN 执行口径收敛修订（2026-03-01）：更新 `docs/plans/2026-03-01-phase4-implementation-plan.md`，将 `PHASE4_DB_DSN` / `PHASE4_RESTORE_DSN` 明确收敛为 `postgresql://`（供 `psql/pg_dump/pg_restore` 直接使用），移除 `postgresql+psycopg://` 输入兼容分支，避免工具链误解析导致连接到本地默认库；同时保留 `DATABASE_URL` / `PHASE4_RESTORE_DATABASE_URL` 由 libpq DSN 派生给 Alembic 与 `reconciliation` 使用。

- Phase 4 计划 v1.4 发布窗口执行安全修订（2026-03-01）：修订 `docs/plans/2026-03-01-phase4-implementation-plan.md` 三项阻断风险。1) 新增 `DATABASE_URL` 与 `PHASE4_RESTORE_DATABASE_URL` 派生规则，并将 Alembic/`reconciliation` 命令改为显式使用对应连接，避免 `PHASE4_DB_DSN` 与 `DATABASE_URL` 指向不一致导致跨库执行；2) 新增 `PHASE4_BACKUP_DIR`，将 `final backup` 落盘改为绝对路径并在回滚前增加 `test -r` 校验，降低跨会话/跨目录恢复失败风险；3) 删除证据文件中的 `database_dsn_masked` 输出，改为仅记录 `database_name`，避免数据库凭证落盘泄露。

- Phase 4 计划 v1.3 全面复核二次硬化（2026-03-01）：针对复核结论补齐发布窗口可落地性。1) `docs/plans/2026-03-01-phase4-implementation-plan.md` 新增 `PHASE4_API_BASE_URL`，将 `X-Authz-Stale` 校验从硬编码 `127.0.0.1:8002` 改为环境参数；2) 增加“写冻结核验”可执行 SQL（`pg_stat_activity` 写事务清零）并纳入窗口标准命令；3) 新增 `PHASE4_FINAL_BACKUP_REF` 与证据文件 `docs/release/evidence/phase4-final-backup-reference.md`，窗口执行时落盘 `final_backup_file`，回滚时从证据文件读取，消除跨会话变量丢失；4) 发布证据清单补齐 `phase4-performance-gate.md`、`phase4-error-rate-gate.md` 与 final backup 引用文件，修复产物清单与门禁要求不一致。

- Phase 4 计划执行口径彻底硬化（2026-03-01）：将 `docs/plans/2026-03-01-phase4-implementation-plan.md` 升级到 v1.2。1) 修复命令块跨目录不可执行问题：统一 `PHASE4_REPO_ROOT` 并改为子 shell 执行；2) 将 `PHASE4_DB_PSQL` 字符串变量替换为 `phase4_psql()`，消除引号分词歧义；3) No-Go 查询改为 `ON_ERROR_STOP` 并移除 `|| true` 误判路径；4) 发布窗口时序改为“写冻结 -> final backup -> 迁移”，与 RPO=0 口径一致；5) 新增性能/5xx 量化门禁与证据文件要求（`phase4-performance-gate.md`、`phase4-error-rate-gate.md`）；6) 回滚口径改为“发布包回滚 + final backup 恢复”。

- Phase 4 计划彻底复核修订（2026-03-01）：对 `docs/plans/2026-03-01-phase4-implementation-plan.md` 执行阻断级修订并升级到 v1.1。1) 新增 `X-Authz-Stale` 作为 Entry/窗口硬门禁，补齐证据文件 `docs/release/evidence/phase4-authz-stale-contract.md` 与联调命令口径；2) 将 Step 3/Step 4 从抽象描述收敛为字段/旧表级精确清单（含 `abac_policy_subjects` 存在即删规则），消除迁移执行漂移；3) 将备份恢复命令从占位符改为可执行口径（`PHASE4_DB_DSN/PHASE4_RESTORE_DSN` + `pg_dump/pg_restore`）；4) 将“前端构建无 warning”升级为发布硬门禁并补齐日志断言命令；5) 对 `tenant_party_id` 增加 A/B 冻结决策（收紧或保留可空必须留痕），修复门禁与 DDL 范围不一致问题。

- Phase 4 完整计划发布（2026-03-01）：新增 `docs/plans/2026-03-01-phase4-implementation-plan.md`，形成 P4 全阶段执行蓝图。内容覆盖：1) P4a/P4b/P4c 三阶段目标、里程碑与通过标准；2) Entry 硬门禁（P2/P3 完成、D9 验签、外部签收、回滚前置）；3) 发布窗口串行步骤与阻断门禁（迁移、对账、权限、冒烟、性能）；4) 回滚策略（RTO/RPO、触发条件、执行步骤）；5) RACI 责任矩阵与 Phase 4 Exit Criteria。

- Review 回归彻底修复（2026-03-01）：修复资产模块两项 P1 用户可见回归并补齐兼容测试。1) `frontend/src/services/asset/assetCoreService.ts` 新增权属筛选兼容层，`getAssets/getAllAssets` 在 `owner_party_id` 与 legacy `ownership_id` 之间做双键透传，确保当前后端 `ownership_id` 合约下筛选仍生效。2) `frontend/src/components/Asset/AssetSearch.tsx` 在搜索提交与初始值回填阶段统一归一 `owner_party_id/ownership_id`，避免列表与统计链路筛选键不一致。3) `frontend/src/components/Asset/AssetList.tsx` 恢复权属方显示兼容：新增 `resolveOwnerPartyName`，新字段 `owner_party_name` 优先、旧字段 `ownership_entity` 兜底，并将列 `dataIndex` 调整为 `ownership_entity` 以保持现网排序字段契约。4) 类型与测试同步：`frontend/src/types/asset.ts` 补充 deprecated 兼容字段；新增 `frontend/src/components/Asset/__tests__/AssetList.ownerName.test.tsx`，并扩展 `AssetSearch/assetService` 回归用例覆盖双键透传。验证：`pnpm --dir frontend exec vitest run src/services/__tests__/assetService.test.ts src/components/Asset/__tests__/AssetSearch.test.tsx src/components/Asset/__tests__/AssetList.test.tsx src/components/Asset/__tests__/AssetList.ownerName.test.tsx`（99 passed）。

- Phase 3 P3e Exit 验收完成（2026-03-01）：补齐收口阶段最后一项功能缺口并完成 Exit 证据归档。1) `frontend/src/services/rentContractExcelService.ts` 新增旧模板错误归一化，当导入失败命中模板字段缺失特征时统一提示“系统已升级，请下载最新模板后重试”，并收紧判定逻辑避免把普通校验错误误报为模板升级提示；2) 新增回归测试 `frontend/src/services/__tests__/rentContractExcelService.test.ts::throws upgrade hint when import fails due to legacy template fields` 与 `keeps original import error details for non-template validation errors`；3) 新增验收文档 `docs/plans/execution/phase3e-exit-readiness-20260301.md`，回填 P3e Exit 门禁结果（旧字段主链 grep、`usePermission/Router` 物理删除、`PermissionGuard` 主链零引用、check/build/test 通过）。验证：`pnpm --dir frontend exec vitest run src/services/__tests__/rentContractExcelService.test.ts`（5 passed）；`pnpm -C frontend check`（PASS）；`pnpm -C frontend build`（PASS）；`pnpm -C frontend test`（PASS）。

- Phase 3 P3e Batch 2 主链字段收口（2026-03-01）：完成前端运行时与类型层旧字段迁移闭环。1) 将资产/项目/合同/图表主链消费从 `ownership_id/ownership_ids/ownership_entity/management_entity` 迁移到 `owner_party_id/owner_party_ids/owner_party_name/manager_party_name`（编辑表单场景使用 `manager_party_id`）；2) 更新核心运行时文件：`components/Asset/*`、`components/Analytics/Filters/FiltersSection.tsx`、`components/Charts/*`、`components/Forms/{AssetForm,RentContractFormContext}`、`components/Project/ProjectList.tsx`、`components/Rental/ContractDetailInfo.tsx`、`pages/{Assets,Contract,Ownership,Rental}/*`、`hooks/useProject.ts`；3) 收口类型定义 `frontend/src/types/{asset,project,rentContract,pdfImport,propertyCertificate}.ts` 并同步测试/夹具（`Asset*`、`ProjectList`、`ContractImportReview.owner-reference`、`mocks/fixtures.ts`、`test-utils/factories/assetFactory.ts`）；4) 回填执行证据 `docs/plans/execution/phase3e-entry-readiness-20260228.md`（Batch 2 验证与剩余范围更新）。验证：`pnpm -C frontend type-check`（PASS）；`pnpm -C frontend type-check:e2e`（PASS）；`pnpm --dir frontend exec vitest run ...`（8 files, 178 passed）；`pnpm -C frontend check`（PASS）；`pnpm -C frontend build`（PASS）；旧字段主链 grep（按计划排除项）无命中。

- Review 回归彻底修复（2026-03-01）：修复 `frontend/src/pages/System/DataPolicyManagementPage.tsx` 两项管理页功能正确性问题。1) 新增“当前角色策略已成功加载”判定（`isCurrentRolePoliciesLoaded`），`保存配置` 仅在当前角色策略加载成功后可点击，并在 mutation 内增加同口径保护，阻断加载中/加载失败时误提交空 `policy_packages`。2) `updateRoleDataPolicies` mutation 改为显式携带 `roleId/policyPackages` 变量，并在 `onSuccess` 使用响应 `data.role_id`（缺失时回退变量 `roleId`）写入 React Query 缓存，消除切换角色时写错缓存 key 的竞态。3) 补充页面回归测试 `frontend/src/pages/System/__tests__/DataPolicyManagementPage.test.tsx`：覆盖“加载中禁用保存”“加载失败禁用保存”“角色切换后缓存不串写”三条场景。验证：`cd frontend && pnpm vitest run src/pages/System/__tests__/DataPolicyManagementPage.test.tsx`（4 passed）；`cd frontend && pnpm lint`（通过）。

- Phase 3 P3e Batch 1 收口（2026-02-28）：按默认 `A` 路径完成历史权限壳首批物理清理。1) 删除 `frontend/src/components/Router/*`（含对应测试）并移除 `frontend/src/components/index.ts` 中的 `Router` 导出，确认主路由链不再依赖该体系；2) 物理删除 `frontend/src/hooks/usePermission.tsx` 与 `frontend/src/hooks/__tests__/usePermission.test.tsx`；3) `frontend/src/components/System/PermissionGuard.tsx` 保留为 `@deprecated` 兼容壳，但已去除对 `PERMISSIONS/usePermission` 的依赖，避免阻塞 P3e 后续收口；4) 同步回填 `docs/plans/execution/phase3e-entry-readiness-20260228.md` 的 Batch 1 执行记录。验证：`cd frontend && pnpm check`（PASS）；`cd frontend && pnpm vitest run src/components/System/__tests__/PermissionGuard.test.tsx src/hooks/__tests__/useCapabilities.test.tsx src/services/__tests__/rentContractExcelService.test.ts src/services/__tests__/dataPolicyService.test.ts`（15 passed）。

- Phase 3 P3e Entry 开始执行（2026-02-28）：完成 Excel 路径门禁修复与开工证据回填。1) `frontend/src/services/rentContractExcelService.ts` 将旧路径 `'/api/rental'` 切换为版本化相对路径 `'/rental-contracts'`（继续复用统一 `apiClient`）；2) 新增 `frontend/src/services/__tests__/rentContractExcelService.test.ts`，固化 `download/import/export` 三条 `/rental-contracts/excel/*` 路径回归；3) 新增 `docs/plans/execution/phase3e-entry-readiness-20260228.md`，回填 P3e Entry 门禁结果（旧路径探针 `404`、新路径鉴权 `401`、`tenant_party_id` 量化检查 `null_count=0`）。验证：`cd frontend && pnpm vitest run src/services/__tests__/rentContractExcelService.test.ts`（3 passed）；`cd frontend && pnpm check`（PASS）。

- Phase 3 P3d 持续推进与 Exit 证据补强（2026-02-28）：继续收敛权限主链残留并补齐策略包服务测试。1) `frontend/src/components/Asset/AssetList.tsx` 移除 `usePermission` 依赖，改为直接消费 `useAuth().isAdmin`，完成主运行链 `usePermission` 残留清零；同步更新测试 `frontend/src/components/Asset/__tests__/AssetList.test.tsx` 的 `AuthContext` mock。2) 新增 `frontend/src/services/__tests__/dataPolicyService.test.ts`，覆盖 `getRoleDataPolicies` / `updateRoleDataPolicies` / `getDataPolicyTemplates` 与失败分支，补齐 P3d 策略包服务契约回归。3) 回填执行证据 `docs/plans/execution/phase3d-exit-readiness-20260228.md`。验证：`cd frontend && pnpm vitest run src/services/__tests__/dataPolicyService.test.ts src/pages/System/__tests__/DataPolicyManagementPage.test.tsx src/components/System/__tests__/PermissionGuard.test.tsx src/components/Asset/__tests__/AssetList.test.tsx`（23 passed）；`cd frontend && pnpm e2e --grep "@authz-minimal" --project=chromium`（1 passed，启动本地 dev server 后）；`cd frontend && pnpm check`（PASS）。

- Phase 3 P3d 复核阻断彻底修复（2026-02-28）：闭环修复三项高风险回归。1) `frontend/src/pages/System/DataPolicyManagementPage.tsx` 新增统一加载处理函数，支持同一 `role_id` 重复点击/回车触发 `refetch()`，并在切换角色时清理上一个角色的本地勾选状态；2) `frontend/src/components/System/PermissionGuard.tsx` 移除 `loading` 分支后的 `useMemo`，改为稳定的非 Hook 归一化计算，彻底规避“条件返回导致 Hook 顺序潜在漂移”风险；3) `frontend/scripts/check-capability-guard-wiring.mjs` 收紧 guard wiring 断言，要求同时命中 `<CapabilityGuard action/resource>` 透传与 `canPerform(action, resource)` 多权限分支校验，消除宽松正则 `OR` 造成的假阳性。验证：`cd frontend && pnpm vitest run src/components/System/__tests__/PermissionGuard.test.tsx src/pages/System/__tests__/DataPolicyManagementPage.test.tsx`（通过）；`cd frontend && pnpm check:route-authz && pnpm check:authz-vocabulary && pnpm check:capability-guard-wiring`（通过）。

- Phase 3 P3d 首批执行（2026-02-28）：完成权限主链从 `usePermission` 向 `useCapabilities` 的落地切换。1) 新增 `frontend/src/utils/authz/capabilityEvaluator.ts` + `frontend/src/hooks/useCapabilities.ts`，实现 capabilities 同资源并集归并、`project` 视角 override（仅 `manager`）与 `backup` admin-only 临时豁免。2) 新增 `frontend/src/components/System/CapabilityGuard.tsx`，并将 `frontend/src/components/System/PermissionGuard.tsx`、`frontend/src/hooks/usePermission.tsx` 重构为 `@deprecated` 兼容壳（内部代理 capability 判定）。3) 主守卫链改造：`frontend/src/App.tsx` 改为 `useCapabilities + CapabilityGuard`，补齐 D7-B 内联拒绝、D8 feature flag 熔断与 D10 互斥日志；`frontend/src/components/Auth/AuthGuard.tsx` 同步迁移到 capability 判定。4) 路由与功能补位：新增 `/system/data-policies`（`SYSTEM_ROUTES.DATA_POLICIES` + `AppRoutes adminOnly`）及最小可用页面 `frontend/src/pages/System/DataPolicyManagementPage.tsx`，新增服务 `frontend/src/services/dataPolicyService.ts`。5) 测试与脚本同步：新增/重写 `useCapabilities`、`CapabilityGuard`、`PermissionGuard`、`AuthGuard`、`usePermission`、`AppRoutes` 相关测试，并更新 `frontend/scripts/check-capability-guard-wiring.mjs` 适配新守卫链。验证：`cd frontend && pnpm check`（PASS）；`cd frontend && pnpm vitest run src/hooks/__tests__/useCapabilities.test.tsx src/components/System/__tests__/CapabilityGuard.test.tsx src/components/System/__tests__/PermissionGuard.test.tsx src/components/Auth/__tests__/AuthGuard.test.tsx src/hooks/__tests__/usePermission.test.tsx`（21 passed）。

- Frontend 全量检查门禁清理（2026-02-28）：修复仓库内历史格式阻断，执行 `oxfmt --write` 清理 5 个文件（`src/components/Common/PartySelector.tsx`、`src/components/Common/__tests__/PartySelector.test.tsx`、`src/components/Forms/__tests__/ProjectForm.test.tsx`、`src/components/Project/__tests__/ProjectList.test.tsx`、`src/pages/Contract/ContractImportReview.tsx`）。验证：`cd frontend && pnpm check`（PASS，全链路通过）。

- Phase 3 P3d Entry 开工门禁修复（2026-02-28）：修复 `ROUTE_CONFIG` 回流导致的 P3d Entry 阻断。1) `frontend/src/routes/AppRoutes.tsx` 移除对 `ROUTE_CONFIG` 的运行时派生依赖，改为在 `protectedRoutes` 上显式声明权限元数据；同步完成关键映射收口（`/ownership/* -> party:read`、`/project/* -> project:read`、`/rental/ledger -> ledger:read`）并将系统管理路由统一为 `adminOnly`。2) 更新回归测试 `frontend/src/routes/__tests__/AppRoutes.authz-metadata.test.ts`，改为校验 P3d 关键路由映射与系统路由 `adminOnly` 约束。3) 回填执行证据 `docs/plans/execution/phase3d-authz-freeze.md` 与 `docs/plans/execution/phase3d-entry-readiness-20260228.md`。验证：`grep ROUTE_CONFIG` 门禁（PASS）；`pnpm -C frontend check:route-authz`（PASS）；`pnpm -C frontend check:authz-vocabulary`（PASS）；`pnpm -C frontend check:capability-guard-wiring`（PASS）；`pnpm -C frontend type-check`（PASS）；`pnpm -C frontend vitest run src/routes/__tests__/AppRoutes.authz-metadata.test.ts`（3 passed）。

- Phase 3 P3c E2E 权限拒绝用例稳定性修复（2026-02-28）：`frontend/tests/e2e/security/access-denied.spec.ts` 的“普通账号受限”断言从页面重定向/文案匹配改为校验 admin-only API（`GET /api/v1/system/backup/stats`）返回 `401/403`。该修复消除对前端 capability guard 开关与页面文案差异的脆弱依赖，保持授权拒绝语义稳定。

- Phase 3 P3c Exit 验收闭环（2026-02-28）：1) 修复前端全量单测阻断根因，`frontend/src/components/Asset/__tests__/AssetExport.test.tsx` 与 `frontend/src/pages/System/__tests__/RoleManagementPage.test.tsx` 补齐 `renderWithProviders` 导入，并将 `AssetExport` 测试中的 `@tanstack/react-query` / `antd` mock 调整为“保留真实导出 + 局部覆盖”；2) `frontend/src/test/setup.ts` 新增全局 `renderWithProviders` 注入，并将 MSW 启动改为容错降级（模块 mock 冲突时跳过 server bootstrap），恢复跨文件测试基座稳定性；3) 回填 P3c 执行证据 `docs/plans/execution/phase3c-exit-readiness-20260228.md`，并将计划文档提升至 v1.55。验证：`pnpm -C frontend test`（通过）；`pnpm -C frontend lint && pnpm -C frontend guard:ui && pnpm -C frontend type-check && pnpm -C frontend type-check:e2e`（通过）。

- Review 回归闭环修复（2026-02-28）：修复两处评审阻断项。1) `backend/src/schemas/rent_contract.py` 为 `RentContractBase/RentLedgerBase.owner_party_id` 增加空白归一化（`"   " -> None`），避免在 `ownership_id` 可选场景下空白 owner 绕过联合校验并落入后续内部错误；新增 `backend/tests/unit/schemas/test_rent_contract_schema.py` 覆盖“空白 owner + 无 ownership 拒绝”与“空白 owner + 有 ownership 兼容通过”回归。2) `frontend/src/pages/Contract/ContractImportReview.tsx` 修复确认导入取值链路：`handleConfirm` 改为合并 `validateFields()` 与 `form.getFieldsValue(true)`，并新增 `resolveOwnerReferences`，确保未注册字段场景下 `owner_party_id` 不丢失；新增 `frontend/src/pages/Contract/__tests__/ContractImportReview.owner-reference.test.ts` 覆盖该回归。

- Phase 3 P3c 审查回归彻底修复（2026-02-28）：修复资产编辑兼容性与合同主体映射两处功能回归。1) `frontend/src/components/Forms/AssetForm.tsx` 新增 `owner_party_id` 解析兼容逻辑（优先 `owner_party_id`，缺失时回填 legacy `ownership_id`），并在编辑态初始化与提交流程统一使用，避免历史资产仅有 `ownership_id` 时无法通过必填校验；新增回归测试 `frontend/src/components/Forms/__tests__/AssetForm.test.tsx::编辑态应将 legacy ownership_id 回填到 owner_party_id`。2) `backend/src/services/rent_contract/service.py` 的 `resolve_ownership_id_by_owner_party_id_async` 在按 `code/name` 查询 ownership 时增加 `Ownership.is_active IS TRUE` 与 `Ownership.data_status == "正常"` 过滤，排除停用/软删除历史行，避免误判多命中导致映射失败；新增回归测试 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py::test_resolve_ownership_id_by_owner_party_id_filters_inactive_or_deleted_rows`。验证：`cd frontend && pnpm vitest run src/components/Forms/__tests__/AssetForm.test.tsx`（3 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py -k "owner_party_id" -q`（5 passed）；`backend/.venv/bin/ruff check backend/src/services/rent_contract/service.py backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py`（通过）；`cd frontend && pnpm type-check && pnpm lint`（通过）。

- Phase 3 P3c E2E 全量回归稳定性修复（2026-02-28）：修复移动端看板烟测的非等待断言导致的假失败。`frontend/tests/e2e/mobile/dashboard-smoke.spec.ts` 将即时 `isVisible()` 判断改为等待式 `expect(...).toBeVisible({ timeout: 15000 })`，并使用标题 heading 语义定位。结合前序登录选择器修复后，全量 `cd frontend && pnpm e2e` 结果为 `10 passed, 2 skipped`（无失败）。

- Phase 3 P3c E2E 冒烟登录阻断修复（2026-02-28）：修复 Playwright 登录辅助选择器与新版登录页不匹配导致的冒烟阻断。`frontend/tests/e2e/helpers/auth.ts` 的用户名输入选择器补齐 `input#identifier` 与 `input[name="identifier"]`（保留旧 `login_username/username` 兼容），恢复多套登录表单结构下的登录探测。验证：`cd frontend && pnpm type-check:e2e`（通过）；`cd frontend && pnpm e2e --grep "@authz-minimal" --project=chromium`（1 passed）；`cd frontend && pnpm e2e tests/e2e/asset-flow.spec.ts --project=chromium`（1 passed）；`cd frontend && pnpm e2e tests/e2e/rental/contract-workflow.spec.ts --project=chromium`（1 passed）。

- Phase 3 P3c PDF 合同导入映射根因修复（2026-02-28）：修复 `owner_party_id` 与 `ownership_id` 被错误互写、以及 `confirm_import` 载荷与后端 `contract_data` 结构不对齐的问题。1) `frontend/src/pages/Contract/ContractImportReview.tsx` 将匹配选择字段改回 `ownership_id`，初始化与提交时分别保持 `owner_party_id`/`ownership_id` 独立，不再执行 `ownership_id = owner_party_id` 伪映射；2) `frontend/src/services/pdfImportService.ts` 调整 `normalizeConfirmedContractData` 为“仅清洗不互写”，并在请求中同时发送 `confirmed_data.contract_data`（兼容后端现有读取）与平铺字段；3) 同步更新 `frontend/src/services/__tests__/pdfImportService.test.ts` 与 `frontend/src/types/pdfImport.ts` 注释口径，新增断言覆盖“无互写 + 含 contract_data”。验证：`cd frontend && pnpm vitest run src/services/__tests__/pdfImportService.test.ts`（33 passed）；`cd frontend && pnpm type-check && pnpm lint && pnpm guard:ui`（通过）。

- Phase 3 P3c 合同映射服务层收口（2026-02-28）：将 `owner_party_id ↔ ownership_id` 一致性约束下沉到服务层，消除“仅 API 创建链路校验、续签/导入/直调更新可绕过”的根因。1) `backend/src/services/rent_contract/lifecycle_service.py` 新增 `_normalize_owner_ownership_mapping_async`，在 `create_contract_async` 与 `update_contract_async` 统一执行：`owner_party_id` 单传自动回填 `ownership_id`（不可唯一映射则拒绝）、双字段冲突拒绝、双向不可验证 fail-closed。2) `update_contract_async` 在仅更新 `owner_party_id` 时改为重算 `ownership_id`，避免沿用旧值造成错配。3) 新增回归测试 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py`：覆盖 create 直调冲突拒绝、update 直调冲突拒绝、update 仅改 owner 时 ownership 自动回填，并补充 `renew` 链路映射失败传播；`backend/tests/unit/services/document/test_rent_contract_excel.py` 新增导入链路映射错误回归。验证：`backend/.venv/bin/ruff check backend/src/services/rent_contract/lifecycle_service.py backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py backend/tests/unit/services/document/test_rent_contract_excel.py`（通过）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_rent_contract_api.py backend/tests/unit/services/document/test_rent_contract_excel.py backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py -q`（92 passed）。

- Phase 3 P3c 合同映射彻底修复（2026-02-28）：补齐 owner/ownership 双向一致性边界，消除“可验证映射被误拒绝”与“不可验证映射被放行”两类残留风险。1) `backend/src/services/rent_contract/service.py` 中 `resolve_ownership_id_by_owner_party_id_async` 调整为“高置信映射优先”（`external_ref/id` 唯一命中即返回），仅在无高置信命中时再回退 `code/name`，并对低置信多命中与 `code/name` 冲突继续 fail-closed。2) `backend/src/api/v1/rent_contracts/contracts.py` 创建接口新增双向一致性校验：`ownership->party` 与 `owner->ownership` 任一冲突即拒绝；两侧均不可验证时也拒绝，避免脏组合落库。3) 测试补齐：新增 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_create_contract_should_fail_closed_when_owner_ownership_mapping_unverifiable` 与 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py::test_resolve_ownership_id_by_owner_party_id_keeps_high_confidence_match_when_code_is_ambiguous`，并同步更新既有断言。验证：`backend/.venv/bin/ruff check ...`（通过）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py -q`（44 passed）。

- Phase 3 P3c 合同链路根因修复（2026-02-28）：完成“前端 Party 主链提交”与“后端 legacy ownership 持久化约束”之间的契约闭环，消除 `ownership_id` 错绑与筛选截断。1) 后端创建契约修复：`backend/src/schemas/rent_contract.py` 将 `RentContractCreate.ownership_id` 调整为兼容可选；`backend/src/api/v1/rent_contracts/contracts.py` 新增服务端桥接逻辑（仅传 `owner_party_id` 时自动解析并回填 `ownership_id`），并增加 `owner_party_id`/`ownership_id` 一致性校验与显式验证错误；`backend/src/services/rent_contract/service.py` 新增 `resolve_ownership_id_by_owner_party_id_async`（优先 `external_ref/id`，并对 `code/name` 歧义 fail-closed）。2) 前端提交链路收口：`frontend/src/components/Forms/RentContract/RentContractFormContext.tsx` 移除错误的 `ownership_id: owner_party_id` 伪映射，编辑态不再覆盖 legacy 字段。3) 筛选截断根治：`frontend/src/components/Rental/ContractList/ContractFilterBar.tsx` 与 `frontend/src/pages/Rental/RentLedgerPage.tsx` 改为 `PartySelector` 远程搜索；`frontend/src/hooks/useContractList.ts` 移除固定 `limit=100` 的主体预加载。4) 测试同步：更新 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`、`backend/tests/unit/services/test_rent_contract_v2.py`、`backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py`、`frontend/src/hooks/__tests__/useContractList.test.ts`、`frontend/src/components/Forms/__tests__/RentContractForm.test.tsx`。验证：`backend/.venv/bin/ruff check ...`（通过）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/services/test_rent_contract_v2.py backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py -q`（56 passed）；`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm lint`（通过）；`cd frontend && pnpm vitest run src/hooks/__tests__/useContractList.test.ts src/components/Forms/__tests__/RentContractForm.test.tsx src/components/Common/__tests__/PartySelector.test.tsx`（15 passed）。

- Phase 3 P3c 合同链路迁移续推（2026-02-28）：继续将租赁模块从 `ownership` 口径迁移到 Party 主链。1) `frontend/src/components/Forms/RentContract/RelationInfoSection.tsx` 改为 `PartySelector` 三主体字段（`owner_party_id`/`manager_party_id`/`tenant_party_id`）；`frontend/src/components/Forms/RentContract/RentContractFormContext.tsx` 提交链路切换到新字段，并兼容回填 `ownership_id` 以对齐当前后端校验。2) 合同/台账筛选链路切换：`frontend/src/hooks/useContractList.ts`、`frontend/src/pages/Rental/ContractListPage.tsx`、`frontend/src/components/Rental/ContractList/ContractFilterBar.tsx`、`frontend/src/pages/Rental/RentLedgerPage.tsx` 全部改用 Party 数据源与 `owner_party_id` 查询参数。3) 页面与导入续签同步：`frontend/src/pages/Rental/ContractRenewPage.tsx`、`frontend/src/pages/Contract/ContractImportReview.tsx`、`frontend/src/components/Rental/ContractList/ContractTable.tsx`、`frontend/src/components/Rental/ContractDetailInfo.tsx` 切换到“产权方主体”展示与提交口径。4) 类型与测试同步：更新 `frontend/src/types/rentContract.ts`、`frontend/src/types/pdfImport.ts` 兼容定义；更新 `frontend/src/hooks/__tests__/useContractList.test.ts`、`frontend/src/components/Forms/__tests__/RentContractForm.test.tsx`。验证：`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm test src/hooks/__tests__/useContractList.test.ts src/components/Forms/__tests__/RentContractForm.test.tsx`（6 passed）。

- Phase 3 P3c 复核问题修复（2026-02-28）：修复项目列表“所有方主体”筛选在主体规模较大时仅命中前 50 条的回归。`frontend/src/components/Project/ProjectList.tsx` 改为后端远程搜索：`project-owner-party-options` 查询 key 纳入搜索词，`Select` 使用 `onSearch + filterOption={false}` 触发 `partyService.searchParties(keyword, { status: 'active', limit: 20 })`，不再依赖本地首屏缓存过滤。同步补充回归测试 `frontend/src/components/Project/__tests__/ProjectList.test.tsx::所有方主体筛选应使用后端远程搜索（关键词触发 searchParties）`，覆盖关键词触发行为。验证：`cd frontend && pnpm vitest run src/components/Project/__tests__/ProjectList.test.tsx`（15 passed）；`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm lint`（通过）。

- Phase 3 P3c 项目列表无资产判定收口（2026-02-28）：`frontend/src/components/Project/ProjectList.tsx` 新增统一判定函数 `getProjectAssetCount`，按 `(project.asset_count ?? project.assets?.length ?? 0) === 0` 识别“无资产项目”；状态列为无资产项目新增 `待补绑定` 标签，新增“面积统计”列并在无资产时显示 `N/A`，同时总关联资产统计改为复用同一判定逻辑，避免口径漂移。同步补测 `frontend/src/components/Project/__tests__/ProjectList.test.tsx`：新增 `asset_count=0` 与 `asset_count 缺失回退 assets.length` 两个场景断言。验证：`cd frontend && pnpm vitest run src/components/Project/__tests__/ProjectList.test.tsx`（14 passed）；`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm lint`（通过）；`cd frontend && pnpm guard:ui`（通过）。

- Phase 3 P3c 启动批次（2026-02-28）：完成 Day-1 只读门禁与页面层首批迁移。1) 新增 `frontend/src/constants/organization.ts`，统一组织页只读开关（默认 `VITE_ENABLE_ORGANIZATION_WRITE !== 'true'`）与提示文案；`frontend/src/services/organizationService.ts` 对新增/编辑/删除/移动/批量/导入写接口增加只读保护拦截。2) `frontend/src/pages/System/Organization/*` 落地只读 UI：顶部告警、写入口禁用、列表行编辑/删除入口隐藏，并保留历史查看读链路。3) 新增通用组件 `frontend/src/components/Common/PartySelector.tsx`（默认走 `partyService.searchParties`，支持 `filterMode` 与自定义 `fetcher`，并提供空态/403 提示），同步新增测试 `frontend/src/components/Common/__tests__/PartySelector.test.tsx`。4) 资产基础表单首批切换：`frontend/src/components/Forms/Asset/AssetBasicInfoSection.tsx` 改为 `PartySelector + owner_party_id`；`frontend/src/components/Forms/AssetForm.tsx` 的必填判定切换为 `owner_party_id`；`frontend/src/assetFormSchema.ts`、`frontend/src/types/asset.ts` 同步迁移并保留 `ownership_id` 兼容字段（deprecated）。5) 新增回归测试：`frontend/src/pages/System/__tests__/OrganizationPage.test.tsx`、`frontend/src/services/__tests__/organizationService.test.ts`、`frontend/src/components/Forms/Asset/__tests__/AssetBasicInfoSection.test.tsx`、`frontend/src/__tests__/assetFormSchema.test.ts`。验证：`cd frontend && pnpm vitest run src/services/__tests__/organizationService.test.ts src/pages/System/__tests__/OrganizationPage.test.tsx src/components/Common/__tests__/PartySelector.test.tsx src/components/Forms/Asset/__tests__/AssetBasicInfoSection.test.tsx src/__tests__/assetFormSchema.test.ts`（47 passed）；`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm lint`（通过）。

- Phase 3 P3c 复核根因修复（2026-02-28）：收口 `PartySelector` 鉴权与过滤链路的根因缺陷。1) `frontend/src/components/Common/PartySelector.tsx` 的默认 `fetcher` 改为真正按 `filterMode` 生效：`owner/manager` 模式按角色语义分别查询 `party_type=organization` 与 `party_type=legal_entity` 并去重合并，避免过滤模式失效；2) `isPartyReadForbidden(...)` 改为“结构化字段优先”判定（`status/statusCode/code`，含 `response.data`）并保留消息正则兜底，避免把 403 鉴权拒绝误判为空态；3) `frontend/src/services/partyService.ts` 改为抛出保留 `code/statusCode/type/requestId/details` 的结构化 `Error`，避免服务层将 403 元数据压平导致上层组件无法稳定识别。同步新增测试 `frontend/src/services/__tests__/partyService.test.ts::preserves structured error metadata for forbidden detection consumers`。验证：`cd frontend && pnpm vitest run src/components/Common/__tests__/PartySelector.test.tsx src/components/Forms/Asset/__tests__/AssetBasicInfoSection.test.tsx src/services/__tests__/partyService.test.ts`（14 passed）；`cd frontend && pnpm type-check && pnpm lint`（通过）。

- Phase 3 P3c 残余风险收口（2026-02-28）：继续消除 `PartySelector` 的剩余稳定性风险。1) 默认 `owner/manager` 合并结果新增上限收敛（`slice(0, 20)`），避免双查询去重后下拉选项超出默认分页规模；2) 新增 `createDefaultPartyFetcher(...)` 并补齐 `manager` 模式角色对齐查询单测（`organization + legal_entity` 双查询）；3) 新增 Axios-like 错误分支测试，覆盖 `error.response.status=403` 场景，确保无结构化 `statusCode/code` 时仍能正确展示 403 提示。验证：`cd frontend && pnpm vitest run src/components/Common/__tests__/PartySelector.test.tsx src/services/__tests__/partyService.test.ts src/components/Forms/Asset/__tests__/AssetBasicInfoSection.test.tsx`（17 passed）；`cd frontend && pnpm type-check && pnpm lint`（通过）。

- Phase 3 P3c 项目页主链迁移（2026-02-28）：继续推进 Project 页面从旧 `ownership*` 口径向 Party 主链收敛。1) `frontend/src/components/Project/ProjectList.tsx` 的筛选参数改为 `owner_party_id` 主键（通过 `projectService` 保留 legacy 兼容透传），并将所有方筛选数据源从 `ownershipService` 切到 `partyService.searchParties`；2) 列表“所有方主体”展示改为以 `party_relations` 为主，不再依赖 `ownership_entity` 顶层字段；3) `frontend/src/components/Forms/ProjectForm.tsx` 将主体选择控件替换为 `PartySelector`，提交链路继续产出 `party_relations`；4) 同步更新测试 `frontend/src/components/Project/__tests__/ProjectList.test.tsx`、`frontend/src/components/Forms/__tests__/ProjectForm.test.tsx`。验证：`cd frontend && pnpm vitest run src/components/Project/__tests__/ProjectList.test.tsx src/components/Forms/__tests__/ProjectForm.test.tsx`（15 passed）；`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm lint`（通过）。

- Project 主体关系停用态误展示根因修复（2026-02-28）：1) `backend/src/schemas/project.py` 的 `ProjectResponse._build_party_relations(...)` 补齐 `is_active` 透传，确保 `ownership_relations -> party_relations` 转换不丢失关系启停状态；2) `frontend/src/components/Project/ProjectList.tsx` 权属方标签渲染改为 fail-closed：仅在关系明确 active，或能从 legacy `ownership_relations` 匹配并判定为 active 时展示，避免 `is_active` 缺失时把停用关系误展示为有效关系；3) 新增回归测试 `frontend/src/components/Project/__tests__/ProjectList.test.tsx::应过滤停用关系，避免将停用主体显示为有效主体` 与 `backend/tests/unit/schemas/test_project_schema.py::test_project_response_party_relations_keep_is_active_from_ownership_relations`，并同步更新 `backend/tests/unit/schemas/test_response_safety.py` 的期望契约。验证：`cd frontend && pnpm vitest run src/components/Project/__tests__/ProjectList.test.tsx`（12 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/schemas/test_project_schema.py backend/tests/unit/schemas/test_response_safety.py -q`（4 passed）；`cd frontend && pnpm lint && pnpm type-check`（通过）；`backend/.venv/bin/ruff check backend/src/schemas/project.py backend/tests/unit/schemas/test_project_schema.py backend/tests/unit/schemas/test_response_safety.py`（通过）。

- P3b 误判根因修复（2026-02-28）：1) 修复 `backend/src/scripts/migration/party_migration/backfill_role_policies.py` 角色映射，显式将 `user/business/普通用户` 归并到 `dual_party_viewer`（避免误落 `no_data_access`）；2) 补充回归测试 `backend/tests/unit/migration/test_backfill.py`（新增 `user/business/普通用户 -> dual_party_viewer` 与未知角色回退 `no_data_access` 断言）；3) 执行回填脚本（dry-run + exec）后复核当前映射为 `admin->platform_admin`、`manager->asset_manager_operator`、`user->dual_party_viewer`、`viewer->dual_party_viewer`；4) 新增 `scripts/dev/p3b_api_probe.sh` 强制 `curl --noproxy '*'` 的本地联调脚本，并更新 `scripts/README.md`；5) 更新 P3 文档门禁口径：`docs/plans/2026-02-20-phase3-implementation-plan.md` 新增“角色策略包映射一致性”Entry 门禁并提升到 v1.54，`docs/plans/execution/phase3b-preflight-readiness-20260227.md` 回填误映射修复与直连脚本证据。

- P3b Entry API 证据闭环（2026-02-28）：补齐非管理员在线联调证据并回填 `docs/plans/execution/phase3b-preflight-readiness-20260227.md`。新增结论：`manager` 角色会话下 `GET /api/v1/auth/me/capabilities` 返回包含 `party.read`（且带 `perspectives=["manager"]` 与非空 `data_scope.manager_party_ids`）；`GET /api/v1/parties?search=` 在鉴权会话下可按关键词过滤（`search=P3B` 命中 3 条，`search=甲` 命中 1 条）。同轮复核发现 `user` 角色当时误绑定 `no_data_access`（deny）导致 capabilities 为空，并同步修订 `docs/plans/2026-02-20-phase3-implementation-plan.md` 的 P3b Entry SQL 门禁（新增 `abac_policies.effect='allow'` 过滤，避免 deny 规则导致“假通过”）；该角色映射问题已在后续“P3b 误判根因修复”条目中完成修复。

- 后端/DB 环境拉起根因修复（2026-02-28）：修复 `Makefile` 的 `migrate` 目标对系统 PATH 中 `alembic` 的隐式依赖，改为统一复用 `$(PYTHON) -m alembic upgrade head`（优先使用 `backend/.venv` 解释器）。这解决了“后端可启动但 `make migrate` 失败（`alembic: not found`）”导致的数据库初始化链路中断问题。验证：`make migrate`（通过，执行 `alembic upgrade head` 到当前 head）。

- Auth 会话切换与登录阻塞根因修复（2026-02-28）：1) `frontend/src/contexts/AuthContext.tsx` 在本地用户与当前 Cookie 用户不一致（跨标签切号）时，先执行 `invalidateCapabilityRequests + clearCapabilitiesSnapshot + setCapabilities([])`，阻断新用户短暂继承旧能力集；2) 登录成功路径改为后台触发 `triggerCapabilitiesRefresh(...)`，移除对 capabilities 刷新的 `await`，避免 `/auth/me/capabilities` 慢响应阻塞 `login()` 完成；3) 登录成功前统一清空旧能力快照与内存能力集，确保权限判定 fail-closed。新增回归测试 `frontend/src/hooks/__tests__/useAuth.test.ts`：`本地用户与当前Cookie用户不一致时，应立即清空旧 capabilities`、`capabilities 刷新挂起时，不应阻塞登录流程完成`。验证：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts`（14 passed）。

- 测试基建回归修复（2026-02-28，Project 组件测试可执行性恢复）：1) `frontend/src/components/Forms/__tests__/ProjectForm.test.tsx` 补齐 `renderWithProviders` 导入，修复直接 `ReferenceError`；2) `frontend/src/components/Project/__tests__/ProjectList.test.tsx` 改为对 `@tanstack/react-query` 做部分 mock（保留 `QueryClient` 等真实导出），并将渲染入口改为 `@testing-library/react` 的 `render`，避免与该文件的 `antd` 全量 mock 冲突导致 `ConfigProvider` 缺失。验证：`cd frontend && pnpm vitest run src/components/Forms/__tests__/ProjectForm.test.tsx src/components/Project/__tests__/ProjectList.test.tsx`（14 passed）；`cd frontend && pnpm check`（通过）。

- Phase 3 P3b 根因收口（2026-02-28，project 关系契约切换到 `party_relations` 主链）：1) `frontend/src/types/project.ts` 新增 `ProjectPartyRelation`，`Project/ProjectCreate/ProjectUpdate` 以 `party_relations` 作为主字段；旧 `ownership_relations` 仅保留为 `@deprecated` 兼容键。2) `frontend/src/components/Forms/ProjectForm.tsx` 编辑回显与提交统一改为 `party_relations`（提交 `party_id/relation_type/is_primary`）。3) `frontend/src/components/Project/ProjectList.tsx` 权属方展示改为读取 `party_relations`（`party_name`），不再依赖 `ownership_relations`。4) `frontend/src/test-utils/factories/projectFactory.ts` 同步切到 `party_relations`。验证：`cd frontend && pnpm type-check`（通过）；`cd frontend && pnpm check`（通过）；`cd frontend && pnpm vitest run src/services/__tests__/projectService.test.ts`（45 passed）；`rg -n "ownership_relations" frontend/src --glob "*.{ts,tsx}"`（0 命中）。

- Auth 启动阶段可用性回归修复（2026-02-27，capabilities 拉取不再阻塞初始化）：`frontend/src/contexts/AuthContext.tsx` 新增 `triggerCapabilitiesRefresh` 后台刷新入口，`restoreAuth` 两处会话恢复路径改为非阻塞触发 capabilities 拉取，避免 `/auth/me/capabilities` 慢响应/失败时卡住全局 `initializing`；保留 capabilities 失败降级为空能力集语义。新增回归测试 `frontend/src/hooks/__tests__/useAuth.test.ts`：`capabilities 接口挂起时，不应阻塞初始化完成`，覆盖此前会卡在启动 Spinner 的场景。验证：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts`（12 passed）；`cd frontend && pnpm lint`（通过）；`cd frontend && pnpm type-check`（通过）。

- Phase 3 P3b 完成收口（2026-02-27，服务/工具层旧字段阻断清零）：1) `frontend/src/services/projectService.ts` 以 `owner_party_id` 作为主筛选口径，并保留旧参数兼容映射（无感透传）；新增 `getProjectsByOwnerParty`，旧 `getProjectsByOwnership` 退化为兼容壳。2) `frontend/src/services/asset/assetDictionaryService.ts` 新增 `getOwnerPartiesFromDict/getManagerPartiesFromDict`，旧命名方法保留为兼容代理。3) `frontend/src/services/asset/types.ts` 将统计/筛选类型主链切换为 `by_owner_party`、`owner_party_id`，并通过计算属性保留旧键访问兼容。4) `frontend/src/services/pdfImportService.ts` 的确认导入数据模型切换为 `owner_party_id` 主链，提交前自动归一化并补齐旧键兼容。5) `frontend/src/utils/assetCalculations.ts` 的完整性校验主键切换为 `owner_party_id`，并兼容历史键读取。6) `frontend/src/types/project.ts` 新增 `owner_party_id` 搜索参数。验证：`cd frontend && pnpm vitest run src/services/__tests__/projectService.test.ts src/services/__tests__/pdfImportService.test.ts src/utils/__tests__/assetCalculations.test.ts`（81 passed）；`cd frontend && pnpm lint && pnpm type-check && pnpm check`（通过）；`grep` 门禁：`frontend/src/services/ + frontend/src/utils/` 扫描 `organization_id|ownership_ids?|management_entity|ownership_entity` 结果 `CLEAN`。P3b Exit 阻断项已清零。

- Phase 3 根因修复收口（2026-02-27，capabilities 竞态/陈旧响应防护）：1) `frontend/src/services/capabilityService.ts` 增加 `generation + requestToken` 防护，`clearCache()` 同步失效 in-flight 状态，确保旧请求回包不会回填缓存或误清空新请求状态；2) `frontend/src/contexts/AuthContext.tsx` 新增 `currentUserIdRef + capabilityRequestVersionRef`，对登录/会话恢复/刷新后的 capabilities 拉取实施“同用户 + 同版本”双重校验，并在登出/无会话/恢复失败时统一失效请求与清空快照，阻断跨账号陈旧写回；3) `frontend/src/services/authService.ts` 的 `updateProfile` 保留 `capabilities*` 快照字段，避免资料更新后丢失能力快照元数据；4) 新增/扩展回归测试：`frontend/src/services/__tests__/capabilityService.test.ts`（`clearCache` 后旧请求不得回填）、`frontend/src/hooks/__tests__/useAuth.test.ts`（登录中途登出时忽略陈旧 capabilities 响应）、`frontend/src/services/__tests__/authService.test.ts`（资料更新后快照字段保持）。验证：`cd frontend && pnpm format:check && pnpm lint && pnpm type-check && pnpm type-check:e2e`（通过）；`cd frontend && pnpm vitest run src/services/__tests__/capabilityService.test.ts src/services/__tests__/partyService.test.ts src/hooks/__tests__/useAuth.test.ts src/components/Auth/__tests__/AuthGuard.test.tsx src/services/__tests__/authService.test.ts src/utils/__tests__/AuthStorage.test.ts`（57 passed）。

- Phase 3 执行批次（2026-02-27，P3b 核心服务 + capabilities 会话链路）：1) 新增 `frontend/src/services/partyService.ts`（`/parties` 列表/搜索/详情/创建/更新/层级）及单测 `frontend/src/services/__tests__/partyService.test.ts`；2) 新增 `frontend/src/services/capabilityService.ts`（`/auth/me/capabilities` 拉取、10 分钟 TTL 缓存、single-flight 并发收敛）及单测 `frontend/src/services/__tests__/capabilityService.test.ts`；3) `AuthStorage/AuthService/AuthContext` 接入 capabilities 恢复与刷新闭环（登录、会话恢复、`refreshUser` 后重拉；失败降级空能力集；暴露 `capabilities`、`capabilitiesLoading`、`refreshCapabilities`、`isAdmin`）；4) 同步更新认证相关测试桩（`frontend/src/hooks/__tests__/useAuth.test.ts`、`frontend/src/components/Auth/__tests__/AuthGuard.test.tsx`）与服务导出。验证：`cd frontend && pnpm vitest run src/services/__tests__/capabilityService.test.ts src/services/__tests__/partyService.test.ts src/hooks/__tests__/useAuth.test.ts src/components/Auth/__tests__/AuthGuard.test.tsx src/services/__tests__/authService.test.ts src/utils/__tests__/AuthStorage.test.ts`（通过）；`cd frontend && pnpm lint && pnpm type-check && pnpm type-check:e2e`（通过）。

- Review P1 范围隔离缺陷彻底修复（2026-02-27，party-scope bypass + legacy org fallback）：修复两处高风险作用域问题。1) `backend/src/services/party_scope.py`：`_has_unrestricted_party_scope_access` 收敛为“仅管理员可绕过”，移除“无绑定但有 `organization:read` 即全局放行”的路径；同时为 generic/default fallback 分支补齐 `legacy_org_ids` 映射，避免 party id 直接落到 legacy 组织列。2) `backend/src/crud/query_builder.py`：`PartyFilter` 新增 `legacy_org_ids`，并在仅 `organization_id` 可用时改为“必须使用 legacy 映射；缺失映射则 fail-closed”；保留 relation-aware 逻辑不变。3) `backend/src/crud/base.py`：缓存键序列化补充 `legacy_org_ids/owner_legacy_org_ids/manager_legacy_org_ids`，避免不同 legacy scope 碰撞。补充回归测试：`backend/tests/unit/services/test_party_scope.py`、`backend/tests/unit/crud/test_query_builder.py`。验证：`source backend/.venv/bin/activate && pytest --no-cov backend/tests/unit/services/test_party_scope.py backend/tests/unit/crud/test_query_builder.py -q`（34 passed）；`source backend/.venv/bin/activate && pytest --no-cov backend/tests/unit/services/document/test_pdf_import_service.py backend/tests/unit/services/asset/test_asset_service.py backend/tests/unit/services/project/test_project_service.py backend/tests/unit/services/permission/test_rbac_service.py -q`（205 passed）；`source backend/.venv/bin/activate && ruff check backend/src/services/party_scope.py backend/src/crud/query_builder.py backend/src/crud/base.py backend/tests/unit/services/test_party_scope.py backend/tests/unit/crud/test_query_builder.py`（通过）。

- Phase 3 P3a 类型文件格式修复（2026-02-27）：对 `frontend/src/types/capability.ts`、`frontend/src/types/dataPolicy.ts`、`frontend/src/types/party.ts` 执行统一格式化，清除 `format:check` 阻断，恢复前端全量门禁可执行。验证：`cd frontend && pnpm check`（通过）。

- Review 根因修复（2026-02-27，create 鉴权 manager 作用域推导 + user/task 活跃绑定收敛）：修复 3 个授权回归问题。1) `backend/src/api/v1/assets/assets.py`：`_require_asset_create_authz` 在 legacy payload 缺失 `manager_party_id` 时，先从 subject context 推导 manager scope，再进行 ownership->owner 回填；`party_id` 改为 manager 优先，避免 manager-only 用户被 owner-only 上下文误拒。2) `backend/src/api/v1/rent_contracts/contracts.py`：`_require_contract_create_authz` 同步引入 subject manager scope 推导（新增 `_infer_subject_manager_party_id`），并在 ownership 回填后以 manager 优先生成 `party_id`，保持 `ownership_id` 存在但 scope 未解析时不注入 unscoped sentinel 的 fail-closed 语义。3) `backend/src/middleware/auth.py`：`_load_user_scope_context` 的 `UserPartyBinding` 查询新增 `valid_from/valid_to` 活跃窗口过滤，并改为 `is_primary DESC, valid_from DESC, created_at DESC`，避免历史失效绑定驱动 `user/task` 资源鉴权。补充回归测试：`backend/tests/unit/api/v1/test_assets_authz_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`、`backend/tests/unit/middleware/test_authz_dependency.py`。验证：`backend/.venv/bin/ruff check backend/src/api/v1/assets/assets.py backend/src/api/v1/rent_contracts/contracts.py backend/src/middleware/auth.py backend/tests/unit/api/v1/test_assets_authz_layering.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/middleware/test_authz_dependency.py`（通过）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_assets_authz_layering.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/middleware/test_authz_dependency.py -q`（55 passed）。

- Phase 3 P3a 类型层启动（2026-02-27）：完成首批前端类型迁移脚手架。1) 新增 `frontend/src/types/party.ts`、`frontend/src/types/capability.ts`、`frontend/src/types/dataPolicy.ts`；2) `frontend/src/types/index.ts` 接入三份新类型导出；3) `frontend/src/types/organization.ts`、`frontend/src/types/ownership.ts` 添加 `@deprecated` 模块标记；4) `frontend/src/types/propertyCertificate.ts` 为 `PropertyOwner.organization_id` 添加字段级 `@deprecated` 标记，满足 P3a 门禁口径。验证：`cd frontend && pnpm type-check && pnpm type-check:e2e`（通过）。

- Review 根因修复（2026-02-27，ABAC/证件关系枚举持久化与 Party metadata 写入兼容）：1) 确认并锁定 `backend/src/models/abac.py`、`backend/src/models/certificate_party_relation.py` 的 PostgreSQL Enum 按小写 value 持久化行为，避免与迁移定义（`allow/deny/read...`、`owner/co_owner/...`）出现 name/value 漂移；2) 修复 `backend/src/schemas/party.py` 的 metadata 解析：写入模型中仅 `PartyCreate/PartyUpdate` 使用 `AliasChoices("metadata", "metadata_json")` 兼容外部/历史键，响应模型继续通过 `metadata_json` 读取 ORM 属性，避免误取 SQLAlchemy `Base.metadata`；3) 新增回归测试 `backend/tests/unit/models/test_certificate_party_relation.py`、`backend/tests/unit/schemas/test_party_schema.py`，并在 `backend/tests/unit/models/test_abac.py` 补充 enum value 映射断言，防止回归。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/models/test_abac.py backend/tests/unit/models/test_certificate_party_relation.py backend/tests/unit/schemas/test_party_schema.py backend/tests/unit/api/v1/test_party_api.py backend/tests/unit/api/v1/test_party_layering.py -q`（18 passed）。

- Review 回归根因彻底修复（2026-02-27，project create authz + relation-aware legacy scope）：修复评审指出的 3 个阻断问题。1) `backend/src/api/v1/assets/project.py`：创建鉴权新增 `organization_id -> party_id` 映射（通过 `party_crud.resolve_organization_party_id`），不再把原始 `organization_id` 直接写入 `manager_party_id`（避免 FK/错误作用域）；并让鉴权与创建流程共用同一 `effective organization` 解析（请求 `organization_id` 优先、缺省回退 `current_user.default_organization_id`），杜绝缺省请求被 `__unscoped__:project:create` 误拒。2) `backend/src/crud/query_builder.py`：relation-aware `legacy_org` 回退仅使用 `owner_legacy_org_ids/manager_legacy_org_ids`，不再把 party IDs 当成 `organization_id` 匹配值。3) `backend/src/crud/party.py`：`resolve_legacy_organization_scope_ids_by_party_ids(...)` 强化为“external_ref 优先 + organizations.id/code/name 反查”，移除“无映射时回退 party_id 充当 org_id”的错误行为。补充/更新回归测试：`backend/tests/unit/api/v1/test_project_layering.py`、`backend/tests/unit/crud/test_query_builder.py`、`backend/tests/unit/crud/test_party.py`。验证：`cd backend && .venv/bin/ruff check src/api/v1/assets/project.py src/crud/query_builder.py src/crud/party.py tests/unit/api/v1/test_project_layering.py tests/unit/crud/test_query_builder.py tests/unit/crud/test_party.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/crud/test_query_builder.py tests/unit/crud/test_party.py tests/unit/services/test_party_scope.py tests/unit/api/v1/test_project_layering.py`（56 passed）。

- Phase 3 预检收口（2026-02-27）：完成 P3b 进入门禁的两项阻断修复并回填执行证据。1) `frontend` 执行 `pnpm install` 后恢复 `tsgo`，`pnpm check` 全量通过；2) 执行 `backend/src/scripts/migration/party_migration/backfill_role_policies.py`（dry-run + exec）回填 `abac_role_policies`，`non_admin_party_read_binding_count` 从 `0` 提升到 `6`；3) 新增预检证据文档 `docs/plans/execution/phase3b-preflight-readiness-20260227.md`；4) 更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 到 v1.53，将 P3b ABAC 覆盖门禁收敛为“主路由权限词表”并登记 `project.list/ledger.read/asset.export` 的兼容映射处置。

- 复核追加修复（2026-02-27）：进一步收敛认证与测试生命周期噪声。1) `backend/src/middleware/auth.py` 的 `_validate_jwt_token` 新增 `BaseBusinessError` 直通分支，避免黑名单命中等预期认证异常被误记为 `Unexpected JWT validation error`；2) 将仍存在的 `return TestClient(...)` fixture 全部改为上下文管理并 `yield`（`backend/tests/integration/api/test_route_trailing_slash.py`、`backend/tests/unit/conftest.py`、`backend/tests/unit/api/test_asset_batch_api.py`、`backend/tests/unit/api/test_excel_api.py`），避免测试结束时客户端未关闭导致的潜在跨事件循环清理噪声；3) 新增回归测试 `backend/tests/unit/core/test_blacklist.py::test_validate_jwt_token_should_not_log_unexpected_for_blacklisted_token`，确保黑名单业务拒绝不再触发 unexpected 异常日志。

- 多 TestClient 生命周期交叉导致跨事件循环噪声修复（2026-02-27）：修复测试代码层面的根因，避免 `got Future attached to a different loop` 在非业务路径（尤其 request logging/operation log）被触发。将未关闭或并发使用的 `TestClient` 统一改为安全生命周期模式：`backend/tests/integration/api/test_cookie_auth.py`（`test_bearer_rejected_in_cookie_only_mode` 使用 `with TestClient(...)` 自动关闭）、`backend/tests/integration/test_analytics_api.py`（`debug_client` 改为上下文管理）、`backend/tests/e2e/test_auth_flow_e2e.py`（`test_permission_enforcement` 改为单 `client` 顺序登录并清空 cookies 切换身份，移除双 `TestClient` 并发生命周期）。验证：`backend/.venv/bin/ruff check backend/tests/integration/api/test_cookie_auth.py backend/tests/integration/test_analytics_api.py backend/tests/e2e/test_auth_flow_e2e.py`（通过）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/api/test_auth_permissions.py backend/tests/integration/api/test_cookie_auth.py backend/tests/integration/api/test_token_blacklist_api.py -q -s`（16 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/test_analytics_api.py::TestAnalyticsAPIContracts::test_analytics_debug_cache_endpoint_exists -q -s`（1 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/e2e/test_auth_flow_e2e.py::test_permission_enforcement -q -s`（1 passed）；日志复核（`rg "different loop|Event loop is closed|Operation log creation failed|数据库会话异常"`）无命中。

- 数据库会话日志噪音根因修复（2026-02-27）：修复正常业务异常（尤其 401 `AuthenticationError`）被数据库会话依赖误记为 `CRITICAL` 的问题。`backend/src/database.py` 新增 `_handle_session_exception(...)`，在 `DatabaseManager.get_session`、`get_async_db`、`async_session_scope` 三处统一改为：仅对非 `BaseBusinessError` 的异常记录 `数据库会话异常`（CRITICAL），业务异常只回滚并透传。新增回归测试 `backend/tests/unit/test_database_manager_reset.py`：1) `get_async_db`/`async_session_scope` 在业务异常下不打 `critical`；2) 非业务异常仍打 `critical`。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/test_database_manager_reset.py -q`（6 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/api/test_route_trailing_slash.py -q`（13 passed，401 路径无“数据库会话异常”CRITICAL）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/api/test_property_certificate_e2e.py -q -s`（6 passed，且无 `different loop`/`Operation log creation failed`）。

- 集成测试事件循环根因修复（2026-02-27）：修复请求日志中间件在多次 `TestClient` 生命周期下复用旧 asyncpg 连接池导致的 `got Future attached to a different loop`。`backend/src/database.py` 新增 `DatabaseManager.dispose()` 与全局 `reset_database_manager()`，统一释放引擎与会话工厂；`backend/src/main.py` 在 `lifespan` 关闭阶段调用 `reset_database_manager()`，确保每轮应用关闭后清理全局 DB 管理器，避免跨事件循环复用。新增单测 `backend/tests/unit/test_database_manager_reset.py` 覆盖 dispose/reset 行为。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/test_database_manager_reset.py -q`（3 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/api/test_property_certificate_e2e.py -q -s`（6 passed，未再出现 `different loop`/`Operation log creation failed` 日志）。

- 测试库迁移根因修复（2026-02-27）：修复集成测试环境“旧表残留 + 迁移回退导致 ORM/DB 结构漂移”的根问题。`backend/tests/conftest.py` 变更：1) `setup_test_database` 的 Alembic 调用改为显式使用 `backend/alembic.ini` 且固定 `cwd=backend/`；2) 在非 unit 的 PostgreSQL 测试会话启动时先安全重置测试 schema（仅允许 `*_test` 数据库）再执行 `alembic upgrade head`，避免 `DuplicateTable` 触发隐式回退；3) 迁移失败回退路径由“`create_all` 补缺表”升级为“重建测试 schema + model 建表”，并打印 stdout/stderr 便于诊断。结果：`roles.party_id` 缺列问题消失，迁移可成功完成且不再走 fallback。验证：`backend/.venv/bin/pytest --no-cov backend/tests/integration/api/test_property_certificate_e2e.py -q`（6 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/api/test_property_certificate_e2e.py::test_upload_extract_confirm_flow -q -s`（日志显示 `Database migrations completed successfully`）；`backend/.venv/bin/ruff check backend/tests/conftest.py`（通过）。

- Phase 3 前置复核问题修复（2026-02-27）：对齐“产权证 `owners[]` 冻结契约”的实现与文档。1) `backend/src/schemas/property_certificate.py` 为 `PropertyCertificateResponse` 新增 `owners: list[PropertyOwnerResponse]` 并执行 `model_rebuild()`，确保响应契约与前端类型一致；2) 新增单测 `backend/tests/unit/schemas/test_property_certificate_response_schema.py`（TDD），覆盖 schema 包含 `owners` 字段与 `from_attributes` 序列化 owner 列表；3) 更新 `docs/plans/execution/phase3c-property-certificate-contract-freeze.md` 证据行号与验证项；4) 更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 到 v1.52，补齐历史版本记录并收敛 P3c 读取口径为 `owners[]` 主链。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/schemas/test_property_certificate_response_schema.py -q`（2 passed）。

- Review 授权过滤回归继续收口（2026-02-27，statistics 与列表查询行为一致性）：在租赁统计模块补齐与合同/台账/导出相同的防回归语义。1) `backend/src/api/v1/rent_contracts/statistics.py` 对 `AuthzContext.reason_code == "rbac_admin_bypass"` 跳过 `resource_context` 派生 scope hint，避免管理员统计查询被默认组织提示错误限缩；2) 显式传入 `owner_party_ids` 时清空 `manager_party_ids`，避免 owner/manager 组合 `OR` 放宽统计口径；3) 新增/更新回归测试 `backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py`（admin bypass + owner 显式过滤收敛断言）。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py backend/tests/unit/services/test_party_scope.py`（73 passed）；`backend/.venv/bin/ruff check backend/src/api/v1/rent_contracts/statistics.py backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py`（通过）。

- Review 授权过滤回归根因修复（2026-02-27，party scope/admin bypass/owner 显式过滤收敛）：修复外部审查指出的 3 类问题。1) `backend/src/services/party_scope.py` 将 privileged bypass 判定前移到绑定解析后、返回绑定过滤前，保证管理员/全局读用户即使存在 `UserPartyBinding` 也返回 `None`（不被错误收窄）；2) `backend/src/api/v1/rent_contracts/contracts.py`、`backend/src/api/v1/rent_contracts/ledger.py`、`backend/src/api/v1/rent_contracts/excel_ops.py` 对 `AuthzContext.reason_code == "rbac_admin_bypass"` 跳过 `resource_context` 派生的 owner/manager scope hint，避免管理员列表/导出被默认组织提示错误限缩；3) `contracts` 与 `ledger` 列表查询在显式传入 `owner_party_id` 时清空 `manager_party_ids`（及 `manager_party_id`），消除 owner/manager `OR` 组合导致的越界返回。补充回归测试：`backend/tests/unit/services/test_party_scope.py`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/services/test_party_scope.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`（64 passed）。

- Phase 3 前置问题收口（2026-02-27）：关闭 Phase 2-patch 剩余两项门禁。1) 新增 `docs/plans/execution/phase3c-property-certificate-contract-freeze.md`，冻结产权证契约为 `owners[]`（Phase 3 不引入 `party_relations[]`），并回填代码证据与联调样本；2) 新增 `docs/plans/execution/phase3d-abac-403-evidence.md`，补齐 `PERMISSION_DENIED` 结构化 403 证据（异常模型/测试断言/本地 JSON 样例）；3) 更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.51，将“产权证接口契约冻结”“ABAC 403 结构化拒绝标识”状态从 `Open/Re-verify` 收敛为 `Done` 并挂接证据文档。验证：`pnpm --dir frontend run check:route-authz`（通过）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_admin.py -q`（10 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/integration/test_api_errors.py -q`（27 passed）。

- Phase 3 前置阻断修复（2026-02-27）：完成可执行门禁闭环并修正计划命令漂移。1) `backend/src/schemas/project.py` 新增 `party_relations` 兼容输出（由 `ownership_relations` 转换，默认返回空数组），并保持懒加载安全；2) `backend/src/services/project/service.py` 的 `project_to_response` 改为直接走 `ProjectResponse.model_validate(project)`，由 schema 统一处理关系字段；3) 新增/更新单测 `backend/tests/unit/schemas/test_response_safety.py`，覆盖“未加载关系不触发懒加载”与“ownership -> party_relations 转换”两条主路径；4) 补齐 `docs/plans/execution/phase3-execution-context.md` Day-0 执行快照；5) 修正 `docs/plans/execution/phase3d-authz-freeze.md` 中错误脚本名（`check:route-authz-exclusive` -> `check:route-authz`）并回填 D7/D8/perspectives/并集归并冻结决策；6) 更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 到 v1.50，修正 seed 门禁 SQL 列名（`abac_policy_rules.code` -> `abac_policy_rules.id`）并刷新 Phase 2-patch 状态。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/schemas/test_response_safety.py -q`（3 passed）；`backend/.venv/bin/ruff check backend/src/schemas/project.py backend/src/services/project/service.py backend/tests/unit/schemas/test_response_safety.py`（通过）；`cd backend && ./.venv/bin/alembic upgrade head && ./.venv/bin/alembic current`（当前 head=`20260224_backfill_ownership_occupancy_policy_rules`）。

- Review 授权范围根因修复（2026-02-27）：修复 3 处 scope 回归。1) `backend/src/crud/query_builder.py` 的 relation-aware 过滤新增 `owner_legacy_org_ids/manager_legacy_org_ids` 支持；`organization_id` 的 legacy fallback 改为“优先 legacy org ids，缺失时再回退 party ids”，并避免将 party id 直接当 org id。2) `backend/src/services/party_scope.py` 中 `headquarters` 绑定改为展开 descendants（`include_self=True`）并收敛到 manager scope；同时新增 `party -> legacy organization` 作用域映射注入 `PartyFilter`。3) `backend/src/api/v1/assets/project.py` 的创建鉴权在请求缺失 `manager_party_id/organization_id` 时新增 subject scope hint（来自 `authz_service.context_builder`）兜底，避免合法非管理员直接命中 `__unscoped__:project:create` 被拒；仅在确实无 scope 时保留 fail-closed sentinel。配套新增/更新回归测试：`backend/tests/unit/crud/test_query_builder.py`、`backend/tests/unit/services/test_party_scope.py`、`backend/tests/unit/crud/test_party.py`、`backend/tests/unit/api/v1/test_project_layering.py`。验证：`cd backend && .venv/bin/ruff check src/crud/query_builder.py src/crud/party.py src/services/party_scope.py src/api/v1/assets/project.py tests/unit/crud/test_query_builder.py tests/unit/services/test_party_scope.py tests/unit/crud/test_party.py tests/unit/api/v1/test_project_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/crud/test_query_builder.py tests/unit/services/test_party_scope.py tests/unit/crud/test_party.py tests/unit/api/v1/test_project_layering.py`（49 passed）。
- Alembic 前向迁移补齐（2026-03-04）：新增 `backend/alembic/versions/20260304_rename_asset_business_model_to_revenue_mode.py`，在升级链上将 `assets.business_model` 正式重命名为 `revenue_mode`，修复“仅修改初始迁移导致已升级环境列名漂移”的部署风险；同时新增单测 `backend/tests/unit/migration/test_rename_asset_business_model_migration.py`，覆盖 `down_revision` 链接、升级/降级重命名与异常列状态 fail-fast 行为。

- AGENTS.md 文档治理规范落地（2026-03-04）：将原"文档索引"节扩充为"文档治理规范（AI 必读）"，新增：目录职责速查表（7 个子目录的放/不放规则）、6 条强制规则（AI 报告归档、plans 生命周期、文件命名、行数上限、子目录 README、CHANGELOG 同步）、5 种文件命名格式对照表，以及常用文档入口链接。

- 文档目录整改续（2026-03-04）：1) `guides/code-quality-advanced.md` 重命名为 `guides/database-performance.md`（内容为数据库优化与测试覆盖，与 code-quality 主题不符），同步更新 `index.md` 和 `guides/README.md` 中的引用；2) 删除 `docs/tooling/` 目录（`assistant-metadata.md` 为本地工具元数据，不属于项目文档）；3) 补写 `docs/architecture/ADR-0001-party-role-architecture.md`，记录 2026-02-16 Party-Role 双层模型的架构决策（背景、决策内容、被否决方案、10 条冻结决策、实施结果）；4) 更新 `architecture/README.md`，列入 ADR 索引表；5) `index.md` 架构章节新增 ADR-0001 链接。

- 文档目录整改（2026-03-04）：按深度分析结果执行以下整改：1) 修复 `docs/archive/readme.md` 5 处断链（引用文件不存在），清理 `requirements/`/`releases/` 虚假子目录条目；2) 将 `plans/` 中 5 个已完结方案（langextract、phone-first-login、party-role-architecture、phase1、phase2）归档至 `archive/backend-plans/`，`plans/README.md` 仅保留活跃方案（2 个：phase3 进行中 + flowable 搁置）；3) 将 `guides/database-advanced.md`（EnhancedDatabaseManager 手册）移至 `integrations/enhanced-database-manager.md`，语义更准确；4) 在 `docs/index.md` 新增"前端参考文档"入口指向 `frontend/docs/`，明确双源共存策略；5) 为无 README 的 6 个子目录（`guides/`、`integrations/`、`security/`、`features/`、`incidents/`、`issues/`）补充导航 README；6) `docs/index.md` 文档治理规则新增"已完结方案须归档"和"单文档超 800 行需考虑拆分"两条约束；7) 创建 `archive/` 实际子目录（`frontend-reports/`、`backend-plans/`、`terminology/`）。

- 文档目录整理与需求 SSOT 收敛（2026-03-04）：将 `docs/requirements-specification.md`（As-Built）合并入 `docs/requirements-specification-0to1.md` 作为唯一需求规格（SSOT），每条 REQ 标注实现状态（✅/🚧/📋）和代码证据；删除 `requirements-authority-matrix.md`、`requirements-review-checklist.md`、`requirements-specification.md`；删除 `features/` 下 v0.1/v0.2 重定向残桩和 `prd-*`/`spec-*` 历史文件；删除 `plans/` 下 3 个归档重定向残桩；重写 `docs/index.md` 消除断链并指向唯一规格。`features/` 仅保留两份需求附录（字段清单 + 模块清单）。
- 合同五层模型复核修订（2026-03-04）：对 `docs/features/requirements-appendix-fields-v0.3.md` 进行全盘复核并修订：1) ContractGroup 新增 `predecessor_group_id`（续签链接），`effective_to` 改为派生字段（= MAX(组内非终止合同 effective_to)）；2) `rent_amount`/`rent_amount_excl_tax` 从 Contract 基表下沉到 LeaseContractDetail（代理协议无租金金额）；3) Contract 基表新增 `asset_ids`（为 ContractGroup.asset_ids 子集）；4) 合同区间约束简化为"合同 effective_from 不得早于组 effective_from"；5) 新增 §11.4 补充协议规则（直接修改合同字段+审计留痕+附件）和 §11.5 续签与周期管理规则（新建 ContractGroup + ContractRelation renewal）；6) 修复 §10.2/10.4 中格式错乱的映射行。
- 项目根因问题复核与修复（2026-03-04）：对 `docs/project-issues-analysis-2026-03-03.md` 中 P0/P1/P2 根因进行实证修复并回写文档。后端类型检查由 `148 errors / 30 files` 降至 `49 errors / 19 files`（命令：`cd backend && uv run mypy src`）。核心修复包括：1) `backend/src/crud/asset_support.py` 去除动态 `_invoke_result_chain`，改为 Result Protocol + overload；2) `backend/src/crud/asset.py` 收敛 `Select[object]` 到具体查询类型并为聚合结果增加 `NamedTuple`；3) `backend/src/models/user_party_binding.py` 与 `backend/src/models/abac.py` 补齐 `relationship` 的 `Mapped[...]` 注解；4) `backend/src/core/exception_handler.py` 将 `ErrorDetails` 调整为 `Mapping[str, object]` 并修复异常 helper 的类型传递；5) `asset.py` 显式导出 `SensitiveDataHandler`，修复下游 `attr-defined`。验证：`uv run mypy`（定向文件）通过；`uv run pytest --no-cov -q tests/unit/models/test_asset.py tests/unit/services/asset/test_asset_service.py` 100 passed。
- 合同五层模型架构冻结（2026-03-03）：重构 `docs/features/requirements-appendix-fields-v0.3.md` 合同域，从单表 `RentContract` 拆分为五层模型：1) `ContractGroup`（合同组/交易包）降级为纯容器，`status` 改为由组内合同状态派生的只读字段（`筹备中/生效中/已结束`），移除 `review_status/review_scope`（审核在合同级进行）；2) `Contract`（合同基表）保留公共字段（主体、金额、状态、审核四件套、日期区间）；3) `LeaseContractDetail`（租赁明细）承接 `total_deposit/monthly_rent_base/payment_cycle/payment_terms/tenant_*/owner_*` 等租赁专有字段；4) `AgencyAgreementDetail`（代理明细）承接 `service_fee_ratio/fee_calculation_base/agency_scope`；5) `ContractRelation`（合同关系）采用 parent/child 模型支持 `upstream_downstream/agency_terminal/renewal` 三种关系类型。同步新增 §10 DEPRECATED 字段映射表（口径：不迁移、测试数据可删）和 §11 可执行约束（审核触发、台账生成、统计入库规则）。关键拍板：`lessor/lessee` 为视角相关字段（代理模式下 lessor=委托方、lessee=受托方）；`rent_amount` 为合同级汇总不替代 `RentTerm` 分阶段明细；台账 FK 挂 Contract 基表；代理交易包 = 1 委托协议 + N 终端租赁合同。
- RentContract 签订日期口径闭环（2026-03-03）：更新 `docs/features/requirements-appendix-fields-v0.3.md`，将 `sign_date` 从“待确认”改为“已确认”：草稿阶段可空，进入 `待审/生效` 前必填；禁止自动回填（不使用 `effective_from` 代填）；支持单条与批量补录并强制审计留痕；补充“`sign_date` 为空”筛选用于集中治理。同步从“仍需确认”移除该项。
- ContractGroup 规则边界试行口径（2026-03-03）：继续更新 `docs/features/requirements-appendix-fields-v0.3.md`：1) `settlement_rule` 明确仅保留组级框架最小 5 键，违约金/减免/分期等执行细节本次下沉合同级；2) `revenue_share_rule` 明确本次仅结构化留存，不做自动分润计算；3) `upstream_contract_ids/downstream_contract_ids` 调整为派生只读字段（由组内合同关系计算）；并从“仍需确认”移除 `settlement_rule` 扩展键项。
- 风险防控参考落盘（2026-03-03）：在 `docs/features/requirements-appendix-fields-v0.3.md` 新增“风险防控功能参考（后续版本，不纳入本次）”章节，沉淀 ContractGroup 风险标签的定位、典型场景与后续建设边界；明确该内容仅作为 V1.1+ 候选参考，不纳入本次 v0.3 字段冻结与 MVP 验收范围。
- ContractGroup 口径确认补充（2026-03-03）：继续更新 `docs/features/requirements-appendix-fields-v0.3.md`：1) `status` 枚举补齐为 `草稿/待审/生效/变更中/已到期/已终止` 并明确为“合同组级状态”；2) `review_scope` 为空时默认“全组联审”；3) `settlement_rule` 边界明确为“谁付给谁、按什么算、什么时候付”；4) 新增“ContractGroup 状态定义与落地行为”章节，明确组状态与单合同状态边界及各状态下统计/台账/编辑行为。
- 项目字段直接收口决策落盘（2026-03-03）：更新 `docs/features/requirements-appendix-fields-v0.3.md` 与 `docs/requirements-specification-0to1.md`，按“以当前系统字段为基准”补齐项目域收口方案：1) `status` 枚举冻结为 `planning/active/paused/completed/terminated`（展示值：`规划中/进行中/暂停/已完成/已终止`）；2) 状态语义收敛为 `status + data_status`，`is_active` 下线；3) 项目编码统一为 `project_code` 且格式冻结 `PRJ-[A-Z0-9]{4,12}-[0-9]{6}`；4) `manager_party_id` 明确单值必填；5) `management_entity/organization_id/ownership_entity/ownership_ids` 及相关 alias/legacy fallback 全部直接下线，不做兼容；6) 项目 `review_*`（`review_status/review_by/reviewed_at/review_reason`）明确 v1.0 落地。
- To-Be 主规格同步地址冻结口径（2026-03-03）：更新 `docs/requirements-specification-0to1.md`，在 `REQ-TB-AST-001` 字段映射与“访谈冻结结论”中补齐资产地址规则：采用半结构化地址（`province_code/city_code/district_code/address_detail`），`address_detail` 必填且 `trim` 后长度 `5-200`，`address` 为系统拼接只读展示字段，确保主规格与字段附录 v0.3 一致。
- 资产地址校验策略闭环（2026-03-03）：更新 `docs/features/requirements-appendix-fields-v0.3.md`，将“Asset 地址字段校验策略”从“仍需确认”闭环为“已确认”，冻结为半结构化地址口径：`province_code/city_code/district_code/address_detail`（均必填），`address_detail` 需 `trim` 后长度 `5-200` 且禁止纯空白；`address` 改为系统拼接只读展示字段；行政区代码统一按 GB/T 2260 字典；存量数据按“可解析自动回填、不可解析人工补录”迁移。
- 环境安装流程完善（2026-03-03）：补充 `Makefile` 安装入口，新增 `setup / setup-backend / setup-frontend` 目标。`setup-backend` 固定执行 `cd backend && uv sync --frozen --extra dev`（确保 `ruff` 等 dev 工具进入 `.venv`），`setup-frontend` 固定执行 `cd frontend && pnpm install --frozen-lockfile`（确保 `@typescript/native-preview`/`tsgo` 随锁文件安装），`setup` 并行执行前后端依赖安装，统一本地初始化路径。
- 本地工具可用性修复（2026-03-03）：修复“`backend/.venv` 缺少 `ruff` 可执行导致 lint 失败、前端 `tsgo` 在当前环境不可用”问题。`Makefile` 的 `lint-backend` 改为 `cd backend && uv run --extra dev python -m ruff check .`（并保持迁移命名检查），避免依赖 `.venv` 内已预装 `ruff` 二进制；前端新增 `frontend/scripts/run-type-check.mjs`，`pnpm type-check`/`pnpm type-check:e2e` 现在优先使用 `tsgo`，不可用时自动回退 `tsc`，确保本地与 CI 环境可执行。
- AGENTS 规范补充 UV 强制要求（2026-03-03）：更新 `AGENTS.md`，新增“后端 Python 环境（强制使用 UV）”章节，明确后端依赖与命令统一使用 `uv sync` / `uv run`，并同步替换后端测试示例、排障命令（Import/Alembic）、安全警告中的迁移命令，以及 `DATA_ENCRYPTION_KEY` 生成注释为 UV 用法，避免继续使用系统 `python/pip` 或 Anaconda。
- 经营模式字段统一（2026-03-03）：将资产侧“业务模式/接收模式”口径统一为“经营模式”，字段键统一为 `revenue_mode`。覆盖范围：后端 `Asset` 模型与 Schema、查询白名单、统计分组白名单、枚举验证与初始化、Excel 导入/模板、字典初始化与迁移脚本、初始 Alembic 表结构；前端类型定义、枚举助手、资产表单/详情/导出字段、字典配置与可见性规则；需求附录与数据库设计文档。历史 `CHANGELOG` 记录保留原始事实描述，不改写既往事件。
- To-Be 规格逐章核对修订（2026-03-03）：根据 `docs/requirements-specification-0to1.md` 评审意见补齐并冻结关键口径：1) 新增第 3.3 指标口径定义，明确“合同台账自动生成覆盖率”与“关键报表口径一致性问题”统计公式与判定来源；2) 统一物理删除边界（MVP 不提供用户侧物理删除），去除 `REQ-TB-AST-001` “彻底删除”歧义；3) 补充只读查看者角色、单视角自动锁定、同主体跨视角/角色展示规则；4) 冻结内部/终端判定规则、合同状态区分（已到期 vs 已终止）、台账生成触发时机、风险标签来源；5) 将任务/通知搜索调整为 V1.1 范围并保持 MVP 搜索对象闭环；6) 新增 `REQ-TB-PTY-001/002` 补齐 Party 主档管理缺口，并在第 8 章补充 Party 数据治理约束；7) 增加性能数据量基线、里程碑时间窗口（2026-03-04 至 2026-06-30）与“存量数据迁移”风险项。
- 文档体系重构（2026-02-28，To-Be/As-Built 双轨）：新增 `docs/requirements-specification-0to1.md` 作为“从 0 到 1 目标需求规格（To-Be）”；`docs/requirements-specification.md` 明确重定位为“实现基线规格（As-Built）”；同步更新 `docs/index.md` 的导航与治理规则，区分“目标需求”与“实现验收基线”，解决“需求规格说明书看起来不像 0→1 规范”的定位歧义。
- To-Be 规格补充（2026-02-28，吸收原始需求草案）：将 `docs/my-original-requirement.md` 中 4 条核心诉求纳入 `docs/requirements-specification-0to1.md`，新增需求条目覆盖：1) 项目-资产-主体关系边界（项目必须有运营管理方，不直接绑定产权方）；2) 全局搜索；3) 关键主数据审核/反审核；4) 租赁合同与承租合同入口分离且底层统一建模（按合同类型区分）。
- To-Be 访谈冻结（2026-02-28，业务口径重写）：基于“范围与目标冻结”访谈结果，重写 `docs/requirements-specification-0to1.md` 核心章节，新增并冻结以下关键口径：1) 客户按“当前视角下合同对方主体”定义；2) 全局视角机制（首次手选、后续默认、支持切换）；3) 承租/代理双模式并存且合同组单模式不混用；4) 合同组主审 + 关键合同混合联审；5) 反审核采用“作废/冲销+重建”，禁止关键业务数据物理删除；6) 搜索权限改为“未授权对象不返回”；7) MVP 放量硬门槛收敛为 G1/G2/G3 三项。
- To-Be 字段冻结附录（2026-02-28，v0.1）：新增 `docs/features/requirements-appendix-fields-v0.1.md`，补齐 Asset/Project/ContractGroup/RentContract/CustomerProfile/AnalyticsMetrics 六类对象字段清单，包含字段类型、必填、规则与“已确认/待确认/派生”状态；同时在 `docs/requirements-specification-0to1.md` 与 `docs/index.md` 增加附录入口，形成“业务口径 -> 字段口径”的评审链路。
- 需求交接文档（2026-02-28）：新增 `docs/plans/2026-02-28-requirements-handoff.md`，沉淀本轮需求访谈的“当前状态 + 待办清单 + 已确认未落盘项（v0.2 候选）”，并在 `docs/index.md` 增加交接入口，便于同事接手继续冻结字段版本。
- To-Be 字段附录升级（2026-02-28，v0.2 回写落盘）：新增 `docs/features/requirements-appendix-fields-v0.2.md` 并回写已确认决策（编码规则按主体编码段生成、资产分类拆分为 `asset_form/spatial_level/business_usage` 与枚举冻结）；同步更新 `docs/requirements-specification-0to1.md` 在 REQ 级补充字段映射引用（`asset_code/project_code/group_code`）并将附录引用切换到 v0.2，更新 `docs/index.md` 导航入口到 v0.2。
- P0 字段冻结提案（2026-03-01，可评审版）：新增 `docs/plans/2026-03-01-p0-field-freeze-proposal.md`，集中提出并细化 4 项待冻结口径（编码格式细节、RentContract 币种/税率字段、`settlement_rule` 最小 schema、客户统一标识企业/个人差异），作为 `v0.3 -> v1.0` 评审输入。
- To-Be 字段附录升级（2026-03-01，v0.3 冻结回写）：新增 `docs/features/requirements-appendix-fields-v0.3.md`，将 P0-3 四项提案正式回写（编码格式正则与序号规则、RentContract 金额/币种/税率字段、`settlement_rule` 最小必填 schema、客户企业/个人统一标识差异规则）；同步更新 `docs/requirements-specification-0to1.md` 与 `docs/index.md` 的附录引用到 v0.3。
- 文档一致性复核修复（2026-03-01）：补齐 `docs/index.md` 引用缺失文件（新增 `docs/issues/code-review-2026-02-14.md`、`docs/issues/quick-reference.md`、`docs/archive/readme.md`、`docs/archive/releases/v2-release-notes-2026-01.md`）以消除死链；修正 `docs/features/requirements-appendix-fields-v0.3.md` 中 `identifier_type` 为“条件必填”；同步更新 `docs/plans/2026-03-01-p0-field-freeze-proposal.md` 的 `v0.3` 引用与 `docs/requirements-specification-0to1.md` 状态日期至 `2026-03-01`。
- 全库文档链接清理（2026-03-01）：修复 `docs/plans/2026-02-19-phase2-implementation-plan.md` 中遗留的本地绝对 `file:///d:/ccode/zcgl/...` 链接为仓库相对路径；修复 `docs/reviews/2026-02-22-phase3-plan-review-v2.md` 内 `backend/frontend/docs` 相对层级错误；修复 `docs/plans/vnext-candidate-requirements.md` 与 `docs/archive/2026-02-20-phase3-review-report.md` 的跨目录引用。复核结果：一方文档（排除 `docs/node_modules`）死链为 0。
- 文档链接语法修复（2026-03-01）：修复 `docs/archive/2026-02-20-phase3-review-report.md`、`docs/plans/vnext-candidate-requirements.md`、`docs/reviews/2026-02-22-phase3-plan-review-v2.md` 中误写为 `\\](` 的 Markdown 链接，统一恢复为标准 `[text](url)` 语法，并复核一方文档（排除 `docs/node_modules`）死链仍为 0。
- 资产字段对照补充（2026-03-03）：更新 `docs/features/requirements-appendix-fields-v0.3.md`，新增“当前系统资产字段对照（As-Built -> v0.3，待评审）”章节，按当前资产主表字段逐项给出拟处理策略（保留/映射/新增/下线），并补充当前投影字段处理、准备新增字段与待评审决策项，供产品与架构评审拍板。
- 资产术语统一（2026-03-03）：将资产侧 `business_model` 的展示与文档术语由“接收模式”统一为“业务模式（承租/代理）”，覆盖 `docs/features/requirements-appendix-fields-v0.3.md`、后端模型/Schema 字段说明以及前端资产表单、详情与导出字段标签；注：该条中“字段键保持 `business_model` 兼容不变”的旧口径已被当日后续决策覆盖，最终以“直接更名为 `business_mode`、不保留双字段兼容”为准。
- 资产状态口径确认（2026-03-03）：更新 `docs/features/requirements-appendix-fields-v0.3.md` 第 6 章评审结论：`ownership_status` 明确更名为 `title_status`（产权确权状态），`property_nature` 保留并收敛为资产属性口径，`usage_status` 枚举冻结为 `出租/自用/空置/改造中/停用`，并补充五类状态定义说明。
- 资产字段最终收敛（2026-03-03，完全调整）：继续更新 `docs/features/requirements-appendix-fields-v0.3.md` 第 6 章，按“开发期减少兼容保留”口径确认 `id -> asset_id` 直接替换、`property_name -> asset_name` 直接替换、`ownership_category/project_name/business_category` 直接删除；明确 `project_name` 下线后通过 `project_ids` 关联 `Project.project_name` 获取展示；资产主档维持单一总口径 `rented_area`，不拆分内部/终端已租面积。
- 资产字段复核后再收敛（2026-03-03）：继续更新 `docs/features/requirements-appendix-fields-v0.3.md` 第 6.1 节，将剩余兼容策略改为直接收敛：`management_entity` 直接下线并统一 `manager_party_id`、`audit_notes` 直接收敛到 `review_reason`、`project_id/organization_id/ownership_id` 直接下线删除，减少开发期双轨字段维护成本。
- 资产字段决策落盘（2026-03-03）：按最新评审结论继续更新 `docs/features/requirements-appendix-fields-v0.3.md`：`address` 冻结为必填；`owner_party_id` 升级为 `owner_party_ids` 并明确 v1.0 落地（含迁移后删除旧字段）；`review_*`（`review_status/review_by/reviewed_at/review_reason`）明确在 v1.0 全量落地；第 6.4 由“待评审”改为“已拍板决策”。
- 资产字段第17~44项逐条确认（2026-03-03）：继续更新 `docs/features/requirements-appendix-fields-v0.3.md` 第 6.1/6.5：`cached_occupancy_rate` 收敛为派生只读 `occupancy_rate_total`；`actual_usage/tenant_type/is_sublease/sublease_notes/operation_status/manager_name/operation_agreement_*/terminal_contract_files` 按确认口径删除或下沉合同域；`business_model` 直接更名 `business_mode`；`data_status/version/created_by/updated_by/created_at/updated_at` 保持系统字段语义；`tags` 明确“需配套打标签功能，否则暂不入库”；`manager_party_id` 明确为单值必填核心字段。
- 需求文档权威入口收敛（2026-03-03）：新增 `docs/requirements-authority-matrix.md` 作为需求文档 SSOT 入口，明确 To-Be / As-Built / Reference / Historical 边界与使用规则；同步将 `docs/requirements-review-checklist.md`、`docs/features/prd-asset-management.md`、`docs/features/prd-rental-management.md`、`docs/features/spec-user-permissions.md`、`docs/features/spec-data-models.md`、`docs/integrations/assets-api.md`、`docs/integrations/pdf-processing.md`、`docs/integrations/api-overview.md`、`docs/architecture/README.md`、`docs/index.md` 中“权威规格”引用统一切换到该矩阵入口，降低误用旧文档风险。
- 需求历史文档归档与门禁脚本落地（2026-03-03）：将 `docs/features/requirements-appendix-fields-v0.1.md`、`docs/features/requirements-appendix-fields-v0.2.md`、`docs/my-original-requirement.md`、`docs/plans/2026-02-28-requirements-handoff.md`、`docs/plans/2026-03-01-p0-field-freeze-proposal.md`、`docs/plans/vnext-candidate-requirements.md` 迁移到 `docs/archive/requirements/`，原路径保留归档跳转 stub；新增 `scripts/check_requirements_authority.py` 并在 `Makefile` 增加 `docs-lint` 目标，同时更新 `scripts/README.md` 使用说明。验证：`make docs-lint`（passed）。
- 需求权威口径去歧义（2026-03-03）：更新 `docs/requirements-specification.md` 与 `docs/requirements-specification-0to1.md`，补充统一“矩阵入口”声明并将文案明确为 `Canonical-As-Built / Canonical-To-Be`，避免与“矩阵是唯一入口”形成双 SSOT 语义；同步扩展 `scripts/check_requirements_authority.py`，将两份规格文档纳入“必须引用矩阵”门禁检查。
- 历史实施文档权威语义降噪（2026-03-03）：为 `docs/plans/2026-02-16-party-role-architecture-design.md`、`docs/plans/2026-02-18-phase1-implementation-plan.md`、`docs/plans/2026-02-19-phase2-implementation-plan.md`、`docs/plans/2026-02-20-phase3-implementation-plan.md` 增加头部定位声明（仅方案内部口径，非全局需求权威）；同步在 `docs/requirements-authority-matrix.md` 将上述文档列为 Reference，并扩展 `scripts/check_requirements_authority.py` 强制检查其矩阵入口引用。

- Data Policies API 单测语义对齐修复（2026-02-26）：修复 `backend/tests/unit/api/v1/test_data_policies.py` 中两条“未预期异常包装”断言与当前实现不一致的问题。`GET/PUT /roles/{role_id}/data-policies` 当前对未知异常统一包装为 `internal_error`，测试现同步断言 `INTERNAL_SERVER_ERROR`、`status_code=500`，并校验英文错误消息 `Failed to fetch/update role data policy packages.`，避免旧语义（`INVALID_REQUEST`/中文 message）导致回归误报。

- Review 授权回归根因彻底修复（2026-02-25，legacy org fallback 映射收敛）：修复“无 `UserPartyBinding` 用户回退路径把 `default_organization_id` 直接当 `party_id`”导致的 ABAC 误拒绝。`backend/src/crud/party.py` 新增 `resolve_organization_party_id(...)` 统一解析链路（按 `Party.id/external_ref` 优先，必要时通过 `organizations.code/name` 兜底）；`backend/src/services/authz/context_builder.py` 与 `backend/src/services/party_scope.py` 的 legacy fallback 改为仅在解析到映射 `party_id` 时写入作用域，映射失败统一 fail-closed，不再注入 org_id。新增/更新回归测试：`backend/tests/unit/services/test_authz_context_builder.py`、`backend/tests/unit/services/test_party_scope.py`、`backend/tests/unit/crud/test_party.py`，覆盖“org_id ≠ party_id 可映射”和“不可映射时 fail-closed”场景。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/crud/test_party.py backend/tests/unit/services/test_authz_context_builder.py backend/tests/unit/services/test_party_scope.py -q`（21 passed）。
- Review 授权回归根因修复（2026-02-24，legacy 作用域桥接一致性）：修复 `asset_import` 与 `rent_contract ledger` 在 legacy 字段（`ownership_id` / `organization_id`）输入形态下未稳定桥接到 party scope 的问题。`backend/src/api/v1/assets/asset_import.py` 新增 `ownership_id -> owner_party_id/party_id` 与 `organization_id -> party_id` 解析链路（含按值缓存），并在无法解析时统一写入 `__unscoped__:asset:import` fail-closed 哨兵，避免无 `party_id` 上下文导致误拒绝。`backend/src/api/v1/rent_contracts/ledger.py` 抽取统一合同作用域构建 helper，确保 read/update/generate 三条路径都先尝试 legacy ownership 回填 owner scope；新增 `generate` 路径 unscoped 哨兵 `__unscoped__:rent_contract_ledger:generate`，避免 party scope 为空。新增回归测试：`backend/tests/unit/api/v1/test_asset_import_layering.py`（ownership/org 桥接 + fail-closed）、`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（manager-only + ownership 回填、generate unresolved fail-closed）。验证：`backend/.venv/bin/python -m ruff check backend/src/api/v1/assets/asset_import.py backend/src/api/v1/rent_contracts/ledger.py backend/tests/unit/api/v1/test_asset_import_layering.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（通过）；`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_asset_import_layering.py -k "resolve_owner_party_from_legacy_ownership or resolve_party_from_legacy_organization or fail_closed_when_legacy_scope_not_resolved"`（3 passed）；`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py -k "backfill_owner_scope_when_manager_party_exists or fail_closed_when_legacy_ownership_not_resolved"`（3 passed）；`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_asset_import_layering.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（27 passed）。
- Review 授权回归防再发加固（2026-02-24，继续）：针对当前唯一保留的 `read + *_id` 无 `resource_id` 特例（`documents/pdf_batch_routes.py:get_batch_status`），在 `backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py` 新增两道硬约束：1) 白名单必须保持最小化（仅允许该单一端点）；2) 该端点必须具备补偿性可见性校验（调用 `_resolve_batch_visibility_context`，且 `_get_batch_status(...)` 必须显式传入 `current_user_id` 与 `accessible_organization_ids`）。同时修复该守卫测试的根路径解析错误（`parents[5] -> parents[4]`，确保真实扫描 `backend/src/api/v1`）并加入 `utf-8-sig` 源码读取，兼容带 BOM 的模块（如 `auth/data_policies.py`）避免 AST 误报。验证：`backend/.venv/bin/python -m ruff check backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（通过）；`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（4 passed）；`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py::test_get_batch_status_should_delegate_session_lookup_to_service`（1 passed）。
- Review 授权回归彻底修复（2026-02-24，多主体作用域并集收口）：修复租赁合同统计/台账/Excel 导出在多主体授权场景被单值 `owner_party_id/manager_party_id` 截断的问题。`backend/src/api/v1/rent_contracts/statistics.py` 改为优先消费 `owner_party_ids/manager_party_ids` 并在 owner 过滤命中授权列表时允许收窄；`backend/src/api/v1/rent_contracts/ledger.py` 改为透传完整 owner/manager 作用域列表并保持 manager-only + owner 注入的 403 fail-closed；`backend/src/api/v1/rent_contracts/excel_ops.py` 改为向导出服务透传完整作用域集合。同时扩展下游链路：`backend/src/services/rent_contract/ledger_service.py`、`backend/src/crud/rent_contract.py` 新增台账列表 `owner_party_ids/manager_party_ids` 过滤并集支持；`backend/src/services/document/rent_contract_excel.py` 与 `backend/src/crud/rent_contract.py::get_export_contracts_async` 新增导出查询列表作用域支持。补充回归测试：`backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`、`backend/tests/unit/crud/test_rent_contract_crud.py`、`backend/tests/unit/api/v1/test_rent_contract_api.py`。验证：`backend/.venv/bin/ruff check ...`（本次改动文件通过）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py -q`（8 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py backend/tests/unit/crud/test_rent_contract_crud.py -q`（39 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_rent_contract_api.py -k "test_get_rent_ledger_default_params or test_export_contracts_to_excel_success" -q`（2 passed）。
- Review 授权回归继续验证（2026-02-24）：补充执行并通过以下验证命令：`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/ledger.py src/api/v1/rent_contracts/excel_ops.py src/api/v1/rent_contracts/statistics.py src/services/rent_contract/ledger_service.py src/services/document/rent_contract_excel.py src/crud/rent_contract.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/api/v1/test_rent_contract_statistics_layering.py tests/unit/crud/test_rent_contract_crud.py tests/unit/api/v1/test_rent_contract_api.py`（All checks passed）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/api/v1/test_rent_contract_statistics_layering.py tests/unit/crud/test_rent_contract_crud.py -k "scope or export_contracts_to_excel or get_rent_ledger or get_rent_statistics"`（23 passed, 24 deselected）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_rent_contract_api.py -k "get_rent_ledger_default_params or get_rent_statistics_success or get_ownership_statistics_success or get_asset_statistics_success or get_monthly_statistics_success or export_contracts_to_excel_success"`（6 passed, 28 deselected）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/services/test_rent_contract_service.py -k "get_contracts_should or get_asset_contracts or get_contract_page_async_delegates_to_crud"`（7 passed, 22 deselected）。
- Review 授权回归继续收口（2026-02-25）：修复台账自定义鉴权 helper 在 legacy 合同（`owner_party_id/manager_party_id` 为空、仅有 `ownership_id`）下的作用域回退缺失。`backend/src/api/v1/rent_contracts/ledger.py` 新增 `_resolve_owner_party_scope_by_ownership_id`，并在 `_require_ledger_generate_authz` 与 `_require_ledger_contract_authz` 中补齐 `ownership_id -> owner_party_id/party_id` 回退，避免合法主体被误判为 unscoped。新增回归测试：`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py::test_ledger_generate_authz_should_resolve_owner_party_from_legacy_ownership`、`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py::test_ledger_read_authz_should_resolve_owner_party_from_legacy_ownership`。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/ledger.py tests/unit/api/v1/test_rent_contract_ledger_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_rent_contract_ledger_layering.py -k "legacy_ownership or ledger_generate_authz_should_include_contract_scope_context or ledger_read_authz_should_authorize_with_parent_contract_scope or ledger_update_authz_should_authorize_with_parent_contract_scope"`（5 passed, 11 deselected）。
- Review 授权回归防再发收口（2026-02-24，继续）：`backend/src/api/v1/rent_contracts/contracts.py::get_asset_contracts` 新增资产级详情鉴权依赖 `require_authz(action="read", resource_type="asset", resource_id="{asset_id}", deny_as_not_found=True)`，将该端点从“带 `*_id` 路径参数但无 `resource_id`”历史特例升级为显式资源绑定；同时保留原 `rent_contract` 集合读取鉴权与 owner/manager scope 过滤，避免破坏原有数据收敛链路。同步更新 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py` 断言资产级依赖存在，并在 `backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py` 白名单移除 `rent_contracts/contracts.py:get_asset_contracts`。验证：`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/contracts.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（通过）；`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py backend/tests/unit/api/v1/test_rent_contract_api.py -k "get_asset_contracts or authz_resource_id_template_guard"`（通过）。
- Review 授权回归根因修复（2026-02-24）：补齐 `task` 资源的可信作用域解析链路。`backend/src/middleware/auth.py` 新增 `_load_task_scope_context` 并接入 `_resolve_trusted_resource_context`，实现 `task_id -> user_id -> party scope` 解析（复用 `_load_user_scope_context`，无主体时回退 `__unscoped__:task:{task_id}`）。在此基础上将 `backend/src/api/v1/documents/excel/status.py::get_excel_task_status` 与 `backend/src/api/v1/documents/excel/export_ops.py::download_export_file` 的 ABAC 依赖切换为 `resource_type="task"` 且绑定 `resource_id="{task_id}"`；同步更新 `backend/tests/unit/api/v1/test_excel_status_layering.py`、`backend/tests/unit/api/v1/test_excel_export_ops_layering.py`，并从 `backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py` 白名单移除这两个历史特例。验证：`./backend/.venv/bin/python -m ruff check backend/src/middleware/auth.py backend/tests/unit/middleware/test_authz_dependency.py backend/src/api/v1/documents/excel/status.py backend/src/api/v1/documents/excel/export_ops.py backend/tests/unit/api/v1/test_excel_status_layering.py backend/tests/unit/api/v1/test_excel_export_ops_layering.py backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（通过）；`./backend/.venv/bin/pytest --no-cov backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/api/v1/test_excel_status_layering.py backend/tests/unit/api/v1/test_excel_export_ops_layering.py backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py -q`（23 passed）；`./backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_excel.py::TestGetExcelTaskStatus::test_get_status_success backend/tests/unit/api/v1/test_excel.py::TestDownloadExportFile::test_download_file_success -q`（2 passed）。
- Review 授权回归修复（2026-02-24）：修复合同/资产/项目 trusted scope 与合同列表收敛链路的 3 个数据访问回归。1) `backend/src/middleware/auth.py` 为 `_load_asset_scope_context`、`_load_project_scope_context`、`_load_rent_contract_scope_context` 增加 legacy `ownership_id/organization_id -> party` 回退解析，并补齐 `party_id`；集合读 scope hint 同步注入 `owner_party_ids/manager_party_ids` 完整列表（不再仅有首个 hint）。2) `backend/src/crud/rent_contract.py`、`backend/src/services/rent_contract/service.py` 为合同分页查询新增 `owner_party_ids/manager_party_ids` 多主体并集过滤，并在存在 owner/manager scope 时保留 `ownership_id` 的 legacy 回退条件（仅匹配 party 列为空的历史行）。3) `backend/src/api/v1/rent_contracts/contracts.py` 的 `get_contracts` 改为消费完整 party scope 列表，支持 owner 过滤在授权并集内收敛到单值，且保持 manager-only + owner 注入的 fail-closed（403）。同步补测：`backend/tests/unit/middleware/test_authz_dependency.py`、`backend/tests/unit/crud/test_rent_contract_crud.py`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`、`backend/tests/unit/services/test_rent_contract_service.py`。验证：`backend/.venv/bin/python -m ruff check backend/src/middleware/auth.py backend/src/api/v1/rent_contracts/contracts.py backend/src/services/rent_contract/service.py backend/src/crud/rent_contract.py backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/crud/test_rent_contract_crud.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/services/test_rent_contract_service.py`（通过）；`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 backend/.venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/crud/test_rent_contract_crud.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/services/test_rent_contract_service.py -k "load_asset_scope_context_resolves_legacy_party_scope_when_party_columns_missing or load_project_scope_context_resolves_manager_party_from_legacy_org or load_rent_contract_scope_context_resolves_owner_party_from_legacy_ownership or require_authz_collection_read_infers_party_scope_hint or get_contracts_should_keep_legacy_ownership_fallback_with_scope_filters or get_contracts_should_support_owner_manager_scope_collections or get_contracts_should_delegate_service_query or get_contracts_should_apply_authz_scope_filters or get_contracts_should_allow_owner_filter_within_owner_scope_union or get_contracts_should_forbid_owner_filter_with_manager_only_scope or get_contract_page_async_delegates_to_crud"`（11 passed）。
- Review 授权回归收口（2026-02-24，继续）：`backend/src/api/v1/rent_contracts/contracts.py::get_asset_contracts` 同步切换为完整 `owner_party_ids/manager_party_ids` scope 过滤，避免资产合同列表在多主体用户场景被首个 hint 截断；`backend/src/services/rent_contract/service.py::get_asset_contracts_async` 与相关单测断言同步新增列表参数透传。验证：`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/contracts.py backend/src/services/rent_contract/service.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/services/test_rent_contract_service.py backend/tests/unit/api/v1/test_rent_contract_api.py`（通过）；`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 backend/.venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_get_asset_contracts_should_apply_authz_scope_filters backend/tests/unit/services/test_rent_contract_service.py::TestRentContractService::test_get_asset_contracts_async_returns_items backend/tests/unit/api/v1/test_rent_contract_api.py::TestGetAssetContracts::test_get_asset_contracts_success`（3 passed）。
- Review 授权回归根因修复（2026-02-24）：修复创建链路“鉴权已解析 party scope 但未落库”问题。`backend/src/api/v1/rent_contracts/contracts.py::_require_contract_create_authz` 在 `ownership_id -> owner_party_id` 回退解析成功后会回填 `contract_in.owner_party_id`；`backend/src/api/v1/assets/assets.py::_require_asset_create_authz` 同步回填 `asset_in.owner_party_id`；`backend/src/api/v1/assets/project.py::_require_project_create_authz` 在 `manager_party_id` 缺失且存在 `organization_id` 时回填 `project_in.manager_party_id`。同时 3 个创建端点新增基于 `AuthzContext.resource_context` 的兜底回填，避免依赖注入实例差异导致漏写。新增回归测试：`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_contract_create_authz_should_backfill_owner_party_id_for_persistence`、`backend/tests/unit/api/v1/test_assets_authz_layering.py::test_asset_create_authz_should_backfill_owner_party_id_for_persistence`、`backend/tests/unit/api/v1/test_project_layering.py::test_project_create_authz_should_backfill_manager_party_id_from_organization`。验证：`backend/.venv/bin/pytest --no-cov -q backend/tests/unit/api/v1/test_assets_authz_layering.py::test_asset_create_authz_should_backfill_owner_party_id_for_persistence backend/tests/unit/api/v1/test_project_layering.py::test_project_create_authz_should_backfill_manager_party_id_from_organization backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_contract_create_authz_should_backfill_owner_party_id_for_persistence`（3 passed）。
- Review 授权回归根因修复（2026-02-24，归一化一致性）：补齐 `*_create_authz` 中的字段归一化回写，确保鉴权与持久化使用同一套标准化 ID。`backend/src/api/v1/assets/assets.py::_require_asset_create_authz` 现回写 `owner_party_id/manager_party_id/ownership_id/organization_id`；`backend/src/api/v1/rent_contracts/contracts.py::_require_contract_create_authz` 现回写 `owner_party_id/manager_party_id/ownership_id`；`backend/src/api/v1/assets/project.py::_require_project_create_authz` 现回写 `manager_party_id/organization_id`。新增回归测试：`backend/tests/unit/api/v1/test_assets_authz_layering.py::test_asset_create_authz_should_normalize_party_fields_on_input_model`、`backend/tests/unit/api/v1/test_project_layering.py::test_project_create_authz_should_normalize_party_fields_on_input_model`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_contract_create_authz_should_normalize_party_fields_on_input_model`（3 passed）。
- Review 授权回归根因修复（2026-02-24，端点兜底回归）：新增端点级回归测试覆盖 `AuthzContext.resource_context` 到入参对象的兜底回填路径：`backend/tests/unit/api/v1/test_assets_authz_layering.py::test_create_asset_should_backfill_owner_party_from_authz_context`、`backend/tests/unit/api/v1/test_project_layering.py::test_create_project_should_backfill_manager_party_from_authz_context`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_create_contract_should_backfill_owner_party_from_authz_context`。验证：`backend/.venv/bin/pytest --no-cov -q ...`（3 passed）。
- Test 稳定性修复（2026-02-24）：`backend/tests/unit/api/v1/test_project.py` 的 `admin_user_headers` 夹具新增对 `project.authz_service.check_access` 的显式 allow mock（`AsyncMock`），避免项目创建 API 用例受当前 ABAC 策略数据影响而返回 `403`，确保测试聚焦项目 API 行为本身。
- Test 稳定性修复（2026-02-24，续）：同一夹具补充 monkeypatch `ProjectService._resolve_party_filter -> None`，解除该文件读写用例受 party scope 数据条件影响导致的 `404/空列表` 偶发失败，使 `test_project.py` 保持 API 行为单测定位。
- Review 授权回归防再发（2026-02-24）：新增全局静态门禁 `backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`，扫描 `backend/src/api/v1/**` 所有路由函数，强制 `require_authz(..., resource_id="{param}")` 的模板参数必须出现在对应路由路径（如 `/{param}`）中，防止“集合端点误绑详情资源 ID”同类问题再次引入。验证：`./backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py -q`（1 passed）；`./backend/.venv/bin/python -m ruff check backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（通过）。
- Review 授权回归防再发增强（2026-02-24）：在同一门禁中新增 `read + *_id` 路径参数约束：凡 read 端点带 `*_id` 路径参数，默认必须显式绑定 `resource_id`，仅允许白名单历史特例（批处理状态、资产合同集合、excel 导出下载、excel 任务状态）继续无 `resource_id`。对应测试：`backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py::test_read_endpoints_with_id_path_should_not_omit_resource_id_unless_allowlisted`。验证：`./backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py -q`（2 passed）；`./backend/.venv/bin/python -m ruff check backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（通过）。
- Review 授权回归补充修复（2026-02-24）：继续收口“详情读取退化为集合级鉴权”的同类风险。为以下详情端点补齐资源级 `resource_id` 绑定：`backend/src/api/v1/assets/custom_fields.py::get_custom_field`（`{field_id}`）、`backend/src/api/v1/system/history.py::get_history_detail`（`{history_id}`）、`backend/src/api/v1/system/tasks.py::get_task` 与 `get_task_history`（`{task_id}`）；同步更新分层断言 `backend/tests/unit/api/v1/test_custom_fields_layering.py`、`backend/tests/unit/api/v1/test_history_layering.py`、`backend/tests/unit/api/v1/test_tasks_layering.py`。验证：`./backend/.venv/bin/python -m ruff check backend/src/api/v1/assets/custom_fields.py backend/src/api/v1/system/history.py backend/src/api/v1/system/tasks.py backend/tests/unit/api/v1/test_custom_fields_layering.py backend/tests/unit/api/v1/test_history_layering.py backend/tests/unit/api/v1/test_tasks_layering.py backend/src/api/v1/documents/excel/config.py backend/tests/unit/api/v1/test_excel_config_layering.py backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py`（通过）；`./backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_excel_config_layering.py backend/tests/unit/api/v1/test_authz_resource_id_template_guard.py backend/tests/unit/api/v1/test_custom_fields_layering.py backend/tests/unit/api/v1/test_history_layering.py backend/tests/unit/api/v1/test_tasks_layering.py -q`（21 passed）。
- Review 授权回归补充修复（2026-02-24）：修复租赁合同 Excel 导出链路的作用域收敛缺口。`backend/src/api/v1/rent_contracts/excel_ops.py::export_contracts_to_excel` 现消费 `AuthzContext.resource_context` 并下传 `owner_party_id/manager_party_id`；`backend/src/services/document/rent_contract_excel.py::export_contracts_to_excel` 与 `backend/src/crud/rent_contract.py::get_export_contracts_async` 新增并应用 owner/manager 过滤（`OR` 语义），避免集合级 `read` 鉴权放行后导出查询越权读取。同步补测：`backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`（新增 `test_export_contracts_to_excel_should_apply_authz_scope_filters`）、`backend/tests/unit/api/v1/test_rent_contract_api.py`（导出调用参数断言）与 `backend/tests/unit/crud/test_rent_contract_crud.py`（导出查询作用域断言）。验证：`cd backend && .venv/bin/python -m ruff check src/api/v1/rent_contracts/excel_ops.py src/services/document/rent_contract_excel.py src/crud/rent_contract.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/api/v1/test_rent_contract_api.py tests/unit/crud/test_rent_contract_crud.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest -p pytest_asyncio.plugin -o addopts='' tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/api/v1/test_rent_contract_api.py tests/unit/crud/test_rent_contract_crud.py -k "export_contracts_to_excel or get_export_contracts_should_apply_owner_manager_scope"`（通过）。
- Review 授权回归补充修复（2026-02-24）：继续收口租赁合同域“鉴权放行后未收敛查询”问题。`backend/src/api/v1/rent_contracts/contracts.py` 的 `get_asset_contracts` 现消费 `AuthzContext.resource_context` 并将 `owner_party_id/manager_party_id` 透传到 `backend/src/services/rent_contract/service.py::get_asset_contracts_async`，避免资产合同列表绕过主体作用域；同步补测 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py::test_get_asset_contracts_should_apply_authz_scope_filters` 与 `backend/tests/unit/api/v1/test_rent_contract_api.py::TestGetAssetContracts::test_get_asset_contracts_success`。
- Review 授权回归补充修复（2026-02-24）：修复租金统计链路的作用域缺口与 manager-only 参数注入风险。`backend/src/api/v1/rent_contracts/statistics.py` 新增统一 scope 解析并将 owner/manager 作用域注入所有统计端点；当仅 manager scope 时显式拒绝外部 `owner_party_ids` 覆盖（403）。`backend/src/schemas/rent_contract.py` 的 `RentStatisticsQuery` 新增 `manager_party_ids`，`backend/src/services/rent_contract/statistics_service.py` 与 `backend/src/crud/rent_contract.py` 全链路新增 manager 过滤参数并在聚合查询中按 owner/manager `OR` 收敛，避免统计结果跨主体泄露。同步补测：`backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py`、`backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py`、`backend/tests/unit/api/v1/test_rent_contract_api.py` 相关断言更新。验证：`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/contracts.py backend/src/api/v1/rent_contracts/statistics.py backend/src/services/rent_contract/service.py backend/src/services/rent_contract/statistics_service.py backend/src/crud/rent_contract.py backend/src/schemas/rent_contract.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py backend/tests/unit/api/v1/test_rent_contract_api.py backend/tests/unit/services/test_rent_contract_service.py backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_rent_contract_statistics_layering.py tests/unit/api/v1/test_rent_contract_api.py tests/unit/services/test_rent_contract_service.py tests/unit/services/rent_contract/test_rent_contract_service_impl.py tests/unit/crud/test_rent_contract_crud.py -k "get_asset_contracts or get_contracts_should_apply_authz_scope_filters or get_contracts_should_forbid_owner_filter_with_manager_only_scope or statistics or owner_party or monthly or price_calculation"`（28 passed）。
- Review 授权回归根因修复（2026-02-24）：修复 Excel 配置读取路由的资源级 ABAC 绑定错误。`backend/src/api/v1/documents/excel/config.py` 现将 `resource_id="{config_id}"` 仅绑定到详情端点 `GET /configs/{config_id}`，并从集合端点 `GET /configs` 移除，避免无路径参数时被解析为 `None` 导致详情读取退化为集合级鉴权。同步将 `backend/tests/unit/api/v1/test_excel_config_layering.py` 从跨函数正则匹配升级为函数级 AST 断言，精确校验每个端点 `require_authz(...)` 的关键字参数，防止同类误绑再次漏检。验证：`./backend/.venv/bin/pytest --no-cov backend/tests/unit/api/v1/test_excel_config_layering.py -q`（2 passed）。
- Review 授权回归补充修复（2026-02-24）：收口合同列表与台账列表同源的作用域收敛缺口。`backend/src/api/v1/rent_contracts/contracts.py` 的 `get_contracts` 现消费 `AuthzContext.resource_context` 注入 `owner_party_id/manager_party_id` 作用域过滤，并在“仅 manager scope + 请求 `owner_party_id`”场景 fail-closed 返回 `403`，避免 owner 条件与 manager 条件在查询层 `OR` 合并导致范围放大；`backend/src/services/rent_contract/service.py` 与 `backend/src/crud/rent_contract.py` 同步新增 `manager_party_id` 过滤链路（合同列表支持 owner/manager 作用域并集）。同步补测：`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py` 新增 `test_get_contracts_should_apply_authz_scope_filters` 与 `test_get_contracts_should_forbid_owner_filter_with_manager_only_scope`，`backend/tests/unit/crud/test_rent_contract_crud.py` 新增 `test_get_contracts_by_manager_party_id`，并更新 `backend/tests/unit/services/test_rent_contract_service.py` 调用断言。验证：`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/contracts.py backend/src/services/rent_contract/service.py backend/src/crud/rent_contract.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/services/test_rent_contract_service.py backend/tests/unit/crud/test_rent_contract_crud.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/services/test_rent_contract_service.py tests/unit/crud/test_rent_contract_crud.py`（38 passed）。
- Review 授权回归补充修复（2026-02-24）：修复台账列表在“仅 manager scope”用户携带 `owner_party_id` 参数时可能放大查询范围的问题。`backend/src/api/v1/rent_contracts/ledger.py` 的 `get_rent_ledger` 新增 fail-closed 校验：当鉴权上下文仅提供 `manager_party_id` 且请求显式传入 `owner_party_id` 时直接 `403`，避免 owner 过滤与 manager 作用域在查询层 `OR` 合并导致越权读取；同步新增回归测试 `backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py::test_get_rent_ledger_should_forbid_owner_filter_with_manager_only_scope`。验证：`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/ledger.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/api/v1/test_rent_contract_ledger_layering.py`（13 passed）。
- Review 回归后续修复（2026-02-24）：修复 `backend/src/middleware/auth.py` 中 `AuthzPermissionChecker._resolve_trusted_resource_context` 对 `db` 的硬 `isinstance(AsyncSession)` 限制，改为“可查询异步会话”判断（`AsyncSession` 或 `execute` 为可 `await` 的异步方法）以兼容测试/代理会话对象，避免 `resource_type="ownership"` 在可信上下文解析时被误短路为空并触发 `KeyError('ownership_id')`；同时规避 `MagicMock` 被误判为可查询对象引发的 `await` TypeError。行为保持不变：仅在可查询会话上加载 trusted scope，非查询对象仍返回空上下文。
- Review 回归修复（2026-02-24）：修复 `backend/src/crud/query_builder.py` 中 relation-aware 主体过滤在 `owner_party_id/manager_party_id` 列存在但历史数据为空（NULL）时不再回退 `organization_id` 的问题。现针对 owner/manager 作用域新增“party 列为空时按 `organization_id` 命中”的兼容条件，避免 phase-2 升级后旧数据在列表查询中被误过滤；同步新增回归测试 `backend/tests/unit/crud/test_query_builder.py::test_build_query_should_fallback_to_legacy_org_when_owner_party_is_null` 与 `backend/tests/unit/crud/test_query_builder.py::test_build_query_should_fallback_to_legacy_org_when_manager_party_is_null`。验证：`backend/.venv/bin/python -m pytest --no-cov backend/tests/unit/crud/test_query_builder.py -k "fallback_to_legacy_org" -q`（2 passed）；`backend/.venv/bin/python -m pytest --no-cov backend/tests/unit/crud/test_query_builder.py -q`（17 passed）。
- Review 授权回归根因修复（2026-02-24）：修复 ownership 资源 trusted scope 漏接与台账更新鉴权 ID 误用。`backend/src/middleware/auth.py` 在 `_resolve_trusted_resource_context` 增加 `resource_type="ownership"` 分支，新增 `_load_ownership_scope_context` 与 `_resolve_ownership_party_id`，将 `ownership_id` 解析为可信 `party_id/owner_party_id/manager_party_id`（含 legacy fallback）。`backend/src/api/v1/rent_contracts/ledger.py` 新增 `_require_ledger_update_authz`，更新台账时先以 `ledger_id` 定位父合同，再以 `resource_type="rent_contract"` + `resource_id=contract_id` 鉴权，替换原先误将 `ledger_id` 当合同 ID 的依赖。同步补测：`backend/tests/unit/middleware/test_authz_dependency.py`、`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`。验证：`backend/.venv/bin/python -m ruff check backend/src/middleware/auth.py backend/src/api/v1/rent_contracts/ledger.py backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（通过）；`backend/.venv/bin/python -m pytest --no-cov -q backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（23 passed）。
- Review 授权回归补充修复（2026-02-24）：修复台账详情读取在无目标资源上下文时的误放行风险。`backend/src/api/v1/rent_contracts/ledger.py` 新增 `_require_ledger_read_authz`，并抽取 `_require_ledger_contract_authz(action=...)` 复用 `ledger_id -> contract_id -> owner/manager/ownership` 的可信作用域解析链路；`get_rent_ledger_detail` 改为依赖 `_require_ledger_read_authz`，避免仅凭调用方主体 hint 做 read 鉴权。同步补测：`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py` 新增 `test_ledger_read_authz_should_authorize_with_parent_contract_scope`，并更新详情端点依赖断言。验证：`backend/.venv/bin/python -m pytest --no-cov -q backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（24 passed）；`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/ledger.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py backend/src/middleware/auth.py backend/tests/unit/middleware/test_authz_dependency.py`（通过）。
- Review 授权回归补充修复（2026-02-24）：收口台账列表读取的主体越权风险。`backend/src/api/v1/rent_contracts/ledger.py` 的 `get_rent_ledger` 现在基于 `AuthzContext.resource_context` 强制注入 owner/manager 作用域过滤：默认使用鉴权上下文 `owner_party_id/manager_party_id`，并拒绝请求参数覆盖为其他 owner（403）；同时在 `backend/src/services/rent_contract/ledger_service.py` 与 `backend/src/crud/rent_contract.py` 增加 `manager_party_id` 过滤链路（通过 `RentLedger.contract_id -> RentContract.manager_party_id`）。同步补测：`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py` 新增 `test_get_rent_ledger_should_apply_authz_scope_filters` 与 `test_get_rent_ledger_should_forbid_owner_scope_override`，并更新服务调用断言。验证：`backend/.venv/bin/python -m pytest --no-cov -q backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py backend/tests/unit/middleware/test_authz_dependency.py backend/tests/unit/services/rent_contract/test_ledger_service.py backend/tests/unit/crud/test_rent_contract_crud.py`（通过）；`backend/.venv/bin/python -m ruff check backend/src/api/v1/rent_contracts/ledger.py backend/src/services/rent_contract/ledger_service.py backend/src/crud/rent_contract.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`（通过）。
- Review 授权回归根因修复（2026-02-24）：修复资产域 4 个权限回归点。1) `backend/src/api/v1/assets/assets.py` 新增 `_require_asset_collection_read_authz`，资产列表与筛选类集合读端点统一走显式 ABAC 上下文注入（subject scope hint + 可选 `ownership_id` 作用域），避免集合读取在无资源上下文时误拒绝；2) `backend/src/api/v1/assets/assets.py` 的 `_require_asset_create_authz` 增加 `ownership_id -> owner_party_id` 回填链路（经 `AsyncAssetService.resolve_owner_party_scope_by_ownership_id_async`）；3) `backend/src/services/asset/asset_service.py` 新增 `resolve_owner_party_scope_by_ownership_id_async`，统一 ownership→legal_entity party 解析；4) `frontend/src/constants/routes.ts` 将 analytics/ownership 页面守卫资源改为 `analytics`/`ownership`，对齐后端资源语义；5) `frontend/src/test/setup.ts` 改为延迟加载 `@/mocks/server`，并移除对 `@/test/utils/test-helpers` 的全局预加载，降低与路由元数据测试无关的初始化开销；6) 新增轻量测试配置 `frontend/vitest.authz-metadata.config.ts` 与脚本 `frontend/package.json`:`test:authz-metadata`，以 `node + forks` 稳定快速执行路由权限元数据回归。并补充回归断言 `backend/tests/unit/api/v1/test_assets_authz_layering.py` 与 `frontend/src/routes/__tests__/AppRoutes.authz-metadata.test.ts`。验证：`cd backend && .venv/bin/ruff check src/api/v1/assets/assets.py src/services/asset/asset_service.py tests/unit/api/v1/test_assets_authz_layering.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/api/v1/test_assets_authz_layering.py`（6 passed）；`cd frontend && pnpm test:authz-metadata`（3 passed，约 9s）；`cd frontend && pnpm check:route-authz && pnpm check:authz-vocabulary && pnpm check:capability-guard-wiring`（均通过）。补充说明：`pnpm vitest run src/routes/__tests__/AppRoutes.authz-metadata.test.ts --pool=forks --maxWorkers=1` 在当前环境仍可能触发 `vitest-pool-runner` worker timeout，故保留专用配置脚本作为稳定回归入口。 
- Review 授权回归防再发增强（2026-02-24）：补强资产集合读取与前端门禁的长期回归保护。1) `backend/tests/unit/api/v1/test_assets_authz_layering.py` 新增 `test_asset_collection_read_authz_should_inject_subject_scope_without_ownership_filter`，覆盖“无 `ownership_id` 参数也必须注入 subject scope (`owner_party_id/manager_party_id/party_id`)”路径，防止集合读鉴权再次出现空上下文误拒。2) 同文件 `_read_module_source` 改为直接读取 `backend/src/api/v1/assets/assets.py` 路径，避免仅做静态断言时触发全量路由包导入，降低测试导入开销与环境耦合。3) `frontend/package.json` 将 `test:authz-metadata` 接入 `check` 主门禁，确保路由权限元数据对齐回归由常规检查链路覆盖。验证：`backend/.venv/bin/ruff check backend/tests/unit/api/v1/test_assets_authz_layering.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/api/v1/test_assets_authz_layering.py`（7 passed）；`cd frontend && pnpm test:authz-metadata`（3 passed）。
- Review 授权回归防再发增强（继续，2026-02-24）：补齐 `ownership_id -> party` 回退解析的服务层单测，防止仅在 API 层覆盖导致实现漂移。`backend/tests/unit/services/asset/test_asset_service.py` 新增 `TestOwnershipPartyScopeResolution`，覆盖：1) 空 `ownership_id` 返回 `None`；2) `ownership.get` 元数据（`code/name`）向 `party_crud.resolve_legal_entity_party_id` 透传；3) party 未解析（空白返回）时归一化为 `None`。并补充验证测试类边界未被新用例影响：`TestUpdateAsset::test_update_asset_area_validation_fails` 仍按原类执行。验证：`backend/.venv/bin/ruff check backend/tests/unit/services/asset/test_asset_service.py`（通过）；`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/services/asset/test_asset_service.py -k "resolve_owner_party_scope or update_asset_area_validation_fails"`（4 passed）。附加回归：`cd backend && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest -p pytest_asyncio.plugin -o addopts='' -q tests/unit/api/v1/test_assets_authz_layering.py`（7 passed）；`cd frontend && pnpm check:route-authz && pnpm check:authz-vocabulary && pnpm check:capability-guard-wiring && pnpm test:authz-metadata`（均通过）；`cd frontend && pnpm exec oxfmt --config .oxfmtrc.json --write src/routes/__tests__/AppRoutes.authz-metadata.test.ts src/App.tsx && pnpm test:authz-metadata`（通过）；`cd frontend && pnpm check`（通过，已包含 lint/guard:ui/type-check/type-check:e2e/authz 三门禁 + format:check 全链路）。集成用例 `tests/integration/api/test_assets_visibility_real.py` 在当前环境因集成库不可用（`OperationalError`）被跳过。 
- ABAC 作用域与规则回填根因修复（2026-02-24）：修复无 `resource_id` 的集合读取鉴权缺少 party scope 上下文导致的持续 403。`backend/src/middleware/auth.py` 在 `require_authz` 读场景新增“集合 scope hint 自动补全”（仅 `read/list` 且无 `resource_id`，并保持显式上下文优先），从 subject context 补齐 `owner_party_id/manager_party_id/party_id`；同时修复策略资源漂移，`backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 的 `_EXPANDED_RESOURCE_TYPES` 补齐 `ownership`、`occupancy`、`custom_field`，并新增热修迁移 `backend/alembic/versions/20260224_backfill_ownership_occupancy_policy_rules.py`，确保已升级环境也会补种缺失规则。同步补测：`backend/tests/unit/middleware/test_authz_dependency.py` 新增集合读 scope 回填用例；`backend/tests/unit/migration/test_policy_package_seed_actions.py` 新增热修迁移链路与缺失资源覆盖断言。验证：`cd backend && .venv/bin/ruff check src/middleware/auth.py alembic/versions/20260221_backfill_expanded_policy_package_rules.py alembic/versions/20260224_backfill_ownership_occupancy_policy_rules.py tests/unit/middleware/test_authz_dependency.py tests/unit/migration/test_policy_package_seed_actions.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/middleware/test_authz_dependency.py tests/unit/migration/test_policy_package_seed_actions.py`（30 passed）。
- ABAC 资产列表非管理员真实链路回归（2026-02-24）：新增集成测试 `backend/tests/integration/api/test_assets_visibility_real.py`，覆盖“非 admin + owner 绑定 + owner-scope ABAC 规则”场景，断言 `GET /api/v1/assets` 返回 `200` 且仅返回本主体作用域资产，防止列表鉴权再次回退为统一 `403`。验证：`cd backend && .venv/bin/ruff check tests/integration/api/test_assets_visibility_real.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/integration/api/test_assets_visibility_real.py`（1 passed）。
- ABAC 热修迁移行为回归（2026-02-24）：在 `backend/tests/unit/migration/test_policy_package_seed_actions.py` 新增 `test_missing_resource_backfill_upgrade_should_insert_expected_rules`，直接执行 `20260224_backfill_ownership_occupancy_policy_rules` 的 `upgrade()` 并校验缺失规则批次被完整插入且二次执行保持幂等（无重复新增），覆盖 `ownership/occupancy/custom_field` 三类资源。验证：`cd backend && .venv/bin/ruff check tests/unit/migration/test_policy_package_seed_actions.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/migration/test_policy_package_seed_actions.py tests/integration/api/test_assets_visibility_real.py`（19 passed）。
- ABAC 作用域条件 fail-closed 修复（2026-02-24）：修复迁移策略表达式在资源作用域字段缺失时的错误放行。`backend/alembic/versions/20260219_phase2_seed_data_policy_packages.py` 与 `backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 中 `asset_owner_operator` / `asset_manager_operator` / `dual_party_viewer` / `project_manager_operator` / `audit_viewer` 的 `if` fallback 分支由“存在任意 subject 绑定即真”改为显式拒绝 `{"==":[1,0]}`，避免 legacy/部分回填数据跨主体越权；同步补充回归测试 `backend/tests/unit/migration/test_policy_package_seed_actions.py::test_scoped_policy_conditions_should_fail_closed_when_resource_scope_missing`。验证：`cd backend && .venv/bin/ruff check alembic/versions/20260219_phase2_seed_data_policy_packages.py alembic/versions/20260221_backfill_expanded_policy_package_rules.py tests/unit/migration/test_policy_package_seed_actions.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/migration/test_policy_package_seed_actions.py -k "scoped_policy_conditions_should_fail_closed_when_resource_scope_missing or backfill_migration_should_define_legacy_rule_condition_updates or backfill_upgrade_should_update_existing_legacy_rule_condition_expr"`（3 passed）。
- Review 回归防再发（2026-02-23）：增强前端权限词汇门禁，`frontend/scripts/check-authz-vocabulary.mjs` 的 `forbiddenResources` 新增 `system/users/roles/organizations/assets`（原有 `rental` 保留），用于阻断旧 RBAC 资源命名再次进入 capability guard 主链文件。验证：`cd frontend && corepack pnpm check:authz-vocabulary && corepack pnpm check:route-authz && corepack pnpm check:capability-guard-wiring`（通过）。
- Review 授权回归继续收口（2026-02-23）：补齐文档导入链路 `create/update/delete` 的无 ID 场景上下文。`backend/src/api/v1/documents/pdf_upload.py`、`backend/src/api/v1/documents/pdf_import.py`、`backend/src/api/v1/documents/pdf_import_routes.py` 为租赁合同创建操作注入 `__unscoped__:rent_contract:create` 上下文；`backend/src/api/v1/documents/pdf_batch_routes.py` 为批量上传/取消/清理分别注入 `__unscoped__:rent_contract:create|update|delete` 上下文；`backend/src/api/v1/documents/excel/import_ops.py` 与 `backend/src/api/v1/documents/excel/preview.py` 为资产创建型操作注入 `__unscoped__:asset:create` 上下文。同步补测：`backend/tests/unit/api/v1/test_pdf_upload_layering.py`、`backend/tests/unit/api/v1/test_pdf_import_layering.py`、`backend/tests/unit/api/v1/test_pdf_import_routes_layering.py`、`backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py`、`backend/tests/unit/api/v1/test_excel_import_ops_layering.py`、`backend/tests/unit/api/v1/test_excel_preview_layering.py`。验证：`backend/.venv/bin/ruff check backend/src/api/v1/documents/pdf_upload.py backend/src/api/v1/documents/pdf_import.py backend/src/api/v1/documents/pdf_import_routes.py backend/src/api/v1/documents/pdf_batch_routes.py backend/src/api/v1/documents/excel/import_ops.py backend/src/api/v1/documents/excel/preview.py backend/tests/unit/api/v1/test_pdf_upload_layering.py backend/tests/unit/api/v1/test_pdf_import_layering.py backend/tests/unit/api/v1/test_pdf_import_routes_layering.py backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py backend/tests/unit/api/v1/test_excel_import_ops_layering.py backend/tests/unit/api/v1/test_excel_preview_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_pdf_upload_layering.py tests/unit/api/v1/test_pdf_import_layering.py tests/unit/api/v1/test_pdf_import_routes_layering.py tests/unit/api/v1/test_pdf_batch_routes_layering.py tests/unit/api/v1/test_excel_import_ops_layering.py tests/unit/api/v1/test_excel_preview_layering.py`（20 passed）。
- Review 授权回归继续收口（2026-02-23）：继续修复 `task/excel_config` 写操作的上下文缺失。`backend/src/api/v1/system/tasks.py` 新增 `__unscoped__:task:create|delete` 与 `__unscoped__:excel_config:create` 上下文，分别接入 `create_task/cleanup_old_tasks/create_excel_config`；同时为 `update_task/cancel_task/delete_task` 补齐 `resource_id="{task_id}"`。`backend/src/api/v1/documents/excel/config.py` 为 `create_excel_config` 增加 `__unscoped__:excel_config:create` 上下文，并为 `get_excel_config/update_excel_config/delete_excel_config` 补齐 `resource_id="{config_id}"`。同步补测：`backend/tests/unit/api/v1/test_tasks_layering.py`、`backend/tests/unit/api/v1/test_excel_config_layering.py` 收紧断言并新增哨兵常量校验。验证：`backend/.venv/bin/ruff check backend/src/api/v1/system/tasks.py backend/src/api/v1/documents/excel/config.py backend/tests/unit/api/v1/test_tasks_layering.py backend/tests/unit/api/v1/test_excel_config_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/api/v1/test_excel_config_layering.py`（8 passed）。
- Review 授权回归继续收口（2026-02-23）：继续修复系统维护类写操作在缺少 `resource_id/resource_context` 时的放行兜底风险。`backend/src/api/v1/system/backup.py` 为 `create_backup/cleanup_old_backups` 增加 `__unscoped__:backup:create|delete` 上下文；`backend/src/api/v1/system/notifications.py` 为 `mark_all_as_read/run_notification_tasks_endpoint` 增加 `__unscoped__:notification:update` 上下文；`backend/src/api/v1/system/error_recovery.py` 为 `clear_recovery_history` 增加 `__unscoped__:error_recovery:delete` 上下文；`backend/src/api/v1/system/history.py` 的 `delete_history` 改为显式 `resource_id="{history_id}"`；`backend/src/api/v1/system/operation_logs.py` 为 `cleanup_old_logs` 增加 `__unscoped__:operation_log:delete` 上下文；`backend/src/api/v1/analytics/analytics.py` 与 `backend/src/api/v1/analytics/statistics_modules/basic_stats.py` 为缓存清理端点增加 `__unscoped__:analytics:update` 上下文。同步补测：`backend/tests/unit/api/v1/test_backup_layering.py`、`backend/tests/unit/api/v1/test_notifications_layering.py`、`backend/tests/unit/api/v1/test_error_recovery_layering.py`、`backend/tests/unit/api/v1/test_history_layering.py`、`backend/tests/unit/api/v1/test_operation_logs_layering.py`、`backend/tests/unit/api/v1/analytics/test_analytics_layering.py`、`backend/tests/unit/api/v1/analytics/statistics_modules/test_basic_stats_layering.py`。验证：`backend/.venv/bin/ruff check backend/src/api/v1/system/backup.py backend/src/api/v1/system/notifications.py backend/src/api/v1/system/error_recovery.py backend/src/api/v1/system/history.py backend/src/api/v1/system/operation_logs.py backend/src/api/v1/analytics/analytics.py backend/src/api/v1/analytics/statistics_modules/basic_stats.py backend/tests/unit/api/v1/test_backup_layering.py backend/tests/unit/api/v1/test_notifications_layering.py backend/tests/unit/api/v1/test_error_recovery_layering.py backend/tests/unit/api/v1/test_history_layering.py backend/tests/unit/api/v1/test_operation_logs_layering.py backend/tests/unit/api/v1/analytics/test_analytics_layering.py backend/tests/unit/api/v1/analytics/statistics_modules/test_basic_stats_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/api/v1/test_notifications_layering.py tests/unit/api/v1/test_error_recovery_layering.py tests/unit/api/v1/test_history_layering.py tests/unit/api/v1/test_operation_logs_layering.py tests/unit/api/v1/analytics/test_analytics_layering.py tests/unit/api/v1/analytics/statistics_modules/test_basic_stats_layering.py`（29 passed）。
- Review 授权回归继续收口（2026-02-23）：补齐 `system_settings/system_monitoring` 中无 `resource_id` 的 `update/delete` 端点作用域上下文，避免 owner/manager 条件在缺少资源字段时回退真值导致误放行。`backend/src/api/v1/system/system_settings.py` 新增 `_SYSTEM_SETTINGS_UPDATE_RESOURCE_CONTEXT`（`__unscoped__:system_settings:update`），并接入 `test_security_alert/update_system_settings/restore_system`；`backend/src/api/v1/system/monitoring.py` 新增 `_SYSTEM_MONITORING_UPDATE_RESOURCE_CONTEXT`（`__unscoped__:system_monitoring:update`），并接入 `trigger_metrics_collection`；`backend/src/api/v1/system/system_monitoring/database_endpoints.py` 新增 update/delete 两组上下文（`__unscoped__:system_monitoring:update|delete`），并接入 `optimize_database/cleanup_database`。同步补测：`backend/tests/unit/api/v1/test_system_settings_layering.py`、`backend/tests/unit/api/v1/test_monitoring_layering.py`、`backend/tests/unit/api/v1/test_system_monitoring_layering.py` 收紧断言并新增哨兵上下文常量校验。验证：`backend/.venv/bin/ruff check backend/src/api/v1/system/system_settings.py backend/src/api/v1/system/monitoring.py backend/src/api/v1/system/system_monitoring/database_endpoints.py backend/tests/unit/api/v1/test_system_settings_layering.py backend/tests/unit/api/v1/test_monitoring_layering.py backend/tests/unit/api/v1/test_system_monitoring_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_system_settings_layering.py tests/unit/api/v1/test_monitoring_layering.py tests/unit/api/v1/test_system_monitoring_layering.py`（17 passed）。
- Review 授权回归继续收口（2026-02-23）：补齐剩余 6 处 `require_authz(action="create")` 的作用域上下文，消除 `resource_id=None` 且无资源字段时命中 owner/manager 条件兜底真值的放行风险。`backend/src/api/v1/llm_prompts.py` 为 `create_prompt/collect_feedback` 增加 `_resolve_llm_prompt_create_resource_context` 并回退 `__unscoped__:llm_prompt:create`；`backend/src/api/v1/auth/roles.py` 的 `create_permission_grant` 复用 `_resolve_role_create_resource_context`；`backend/src/api/v1/system/system_settings.py` 的 `backup_system` 注入 `_SYSTEM_SETTINGS_CREATE_RESOURCE_CONTEXT`（`__unscoped__:system_settings:create`）；`backend/src/api/v1/system/monitoring.py` 与 `backend/src/api/v1/system/system_monitoring/endpoints.py` 的 create 端点注入 `_SYSTEM_MONITORING_CREATE_RESOURCE_CONTEXT`。同步补测：`backend/tests/unit/api/v1/test_llm_prompts_layering.py`、`backend/tests/unit/api/v1/test_roles_layering.py`、`backend/tests/unit/api/v1/test_system_settings_layering.py`、`backend/tests/unit/api/v1/test_monitoring_layering.py`、`backend/tests/unit/api/v1/test_system_monitoring_layering.py` 新增/收紧 create 上下文断言。验证：`backend/.venv/bin/ruff check backend/src/api/v1/llm_prompts.py backend/src/api/v1/auth/roles.py backend/src/api/v1/system/system_settings.py backend/src/api/v1/system/monitoring.py backend/src/api/v1/system/system_monitoring/endpoints.py backend/tests/unit/api/v1/test_llm_prompts_layering.py backend/tests/unit/api/v1/test_roles_layering.py backend/tests/unit/api/v1/test_system_settings_layering.py backend/tests/unit/api/v1/test_monitoring_layering.py backend/tests/unit/api/v1/test_system_monitoring_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_llm_prompts_layering.py tests/unit/api/v1/test_roles_layering.py tests/unit/api/v1/test_system_settings_layering.py tests/unit/api/v1/test_monitoring_layering.py tests/unit/api/v1/test_system_monitoring_layering.py`（26 passed）。
- Review 回归补充收口（2026-02-23）：继续对齐非路由守卫链路的系统权限词汇。`frontend/src/hooks/usePermission.tsx` 中 `PERMISSIONS` 将系统类常量从 `system:*` 统一到后端资源（`system_settings`/`operation_log`/`dictionary`/`llm_prompt`），并将 `SYSTEM_BACKUP` 对齐为 `system_settings:create`；`PAGE_PERMISSIONS` 新增 `/system/templates` 映射，并将 `/system/settings` 调整为 `[system_settings:read, system_settings:update]` 任一可访问，避免 `RouteBuilder/PermissionGuard` 链路出现同类误拦截。验证：`cd frontend && corepack pnpm check:route-authz && corepack pnpm check:authz-vocabulary && corepack pnpm check:capability-guard-wiring && corepack pnpm type-check`（通过）。
- Review 回归补充收口（2026-02-23）：同步清理测试词汇漂移，`frontend/src/components/System/__tests__/PermissionGuard.test.tsx` 的 mock 权限由旧词汇（如 `system:logs`、`assets:view`）统一为当前资源语义（`operation_log:read`、`asset:read` 等），避免测试上下文与生产权限模型分叉。验证：`cd frontend && corepack pnpm vitest run src/components/System/__tests__/PermissionGuard.test.tsx`（19 passed）。
- Review 回归修复（2026-02-23）：修复两项功能回归。1) 迁移链连续性修复：`backend/alembic/versions/20260219_phase2_seed_data_policy_packages.py` 将 `down_revision` 恢复为已发布祖先 `20260219_create_abac_and_relation_tables`，`backend/alembic/versions/20260219_phase2_add_party_columns_step1.py` 改为从 `20260221_backfill_expanded_policy_package_rules` 前向追加，避免已升级到 seed/backfill 的数据库遗漏 party 字段 DDL。2) 前端 capability guard 资源对齐：`frontend/src/constants/routes.ts` 将 system 子页权限资源改为与后端一致（`dictionary`/`llm_prompt`/`operation_log`/`system_settings`），并为系统设置放宽为 `system_settings:read|update` 任一可访问。同步补测：`backend/tests/unit/migration/test_policy_package_seed_actions.py` 新增 `test_seed_migration_should_keep_original_predecessor_for_upgrade_continuity` 与 `test_party_columns_migration_should_follow_backfill_head`。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/migration/test_policy_package_seed_actions.py`（14 passed）；`cd backend && ./.venv/bin/alembic heads`（`20260219_phase2_add_party_columns_step1 (head)`）；`cd frontend && corepack pnpm check:route-authz && corepack pnpm check:authz-vocabulary && corepack pnpm check:capability-guard-wiring`（通过）；`cd frontend && corepack pnpm vitest run src/routes/__tests__/AppRoutes.authz-metadata.test.ts`（2 passed）。
- Review 授权回归继续收口（2026-02-23）：统一补齐创建场景的 unscoped fail-closed 哨兵，避免 `resource_id=None` 且缺少可判定主体时误放行。`backend/src/api/v1/assets/assets.py` 新增 `__unscoped__:asset:create` 回退；`backend/src/api/v1/assets/project.py` 新增 `__unscoped__:project:create` 回退；`backend/src/api/v1/rent_contracts/contracts.py` 在缺少 `owner_party_id/manager_party_id` 时补写 `party_id=__unscoped__:rent_contract:create`；`backend/src/api/v1/assets/property_certificate.py` 新增 `__unscoped__:property_certificate:create` 回退；`backend/src/api/v1/assets/ownership.py` 新增 `__unscoped__:ownership:create` 回退；`backend/src/api/v1/rent_contracts/excel_ops.py` 新增 `__unscoped__:rent_contract:bulk_create` 回退。同步补测：`backend/tests/unit/api/v1/test_assets_authz_layering.py`、`backend/tests/unit/api/v1/test_project_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`、`backend/tests/unit/api/v1/test_property_certificate_authz_layering.py`、`backend/tests/unit/api/v1/test_ownership_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`。验证：`backend/.venv/bin/ruff check backend/src/api/v1/assets/assets.py backend/src/api/v1/assets/project.py backend/src/api/v1/rent_contracts/contracts.py backend/src/api/v1/assets/property_certificate.py backend/src/api/v1/assets/ownership.py backend/src/api/v1/rent_contracts/excel_ops.py backend/tests/unit/api/v1/test_assets_authz_layering.py backend/tests/unit/api/v1/test_project_layering.py backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py backend/tests/unit/api/v1/test_property_certificate_authz_layering.py backend/tests/unit/api/v1/test_ownership_layering.py backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_assets_authz_layering.py tests/unit/api/v1/test_project_layering.py tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_property_certificate_authz_layering.py tests/unit/api/v1/test_ownership_layering.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/middleware/test_authz_dependency.py tests/unit/migration/test_policy_package_seed_actions.py`（64 passed）。
- Review 授权根因继续收口（2026-02-23）：针对 `create` 鉴权在 `resource_id=None` 且缺少资源上下文时的策略兜底放行风险，补齐端点级上下文注入。`backend/src/api/v1/assets/property_certificate.py` 新增 `_require_property_certificate_create_authz` 并接入 `confirm_import/create_certificate`，注入 `organization_id + party_id/owner_party_id/manager_party_id`；`backend/src/api/v1/assets/ownership.py` 新增 `_require_ownership_create_authz` 并替换 `create_ownership` 的通用 create 依赖；`backend/src/api/v1/assets/asset_import.py` 新增 `_require_asset_import_create_authz`，按导入 payload 的 distinct scope 逐一鉴权并对无作用域键场景写入 `__unscoped__:asset:import` 哨兵实现 fail-closed；`backend/src/api/v1/rent_contracts/ledger.py` 新增 `_require_ledger_generate_authz`，`generate_monthly_ledger` 改为基于 `contract_id` 解析合同后注入 `owner_party_id/manager_party_id/ownership_id` 再鉴权；`backend/src/api/v1/rent_contracts/excel_ops.py` 新增 `_require_contract_bulk_create_authz` 并接管 `excel/import` create 鉴权；`backend/src/api/v1/rent_contracts/contracts.py` 的 `_require_contract_create_authz` 补充 `ownership_id` 与 `party_id` 注入。同步补测：`backend/tests/unit/api/v1/test_asset_import_layering.py`、`backend/tests/unit/api/v1/test_property_certificate_authz_layering.py`、`backend/tests/unit/api/v1/test_ownership_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/assets/asset_import.py src/api/v1/assets/property_certificate.py src/api/v1/assets/ownership.py src/api/v1/rent_contracts/contracts.py src/api/v1/rent_contracts/ledger.py src/api/v1/rent_contracts/excel_ops.py tests/unit/api/v1/test_asset_import_layering.py tests/unit/api/v1/test_property_certificate_authz_layering.py tests/unit/api/v1/test_ownership_layering.py tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_asset_import_layering.py tests/unit/api/v1/test_property_certificate_authz_layering.py tests/unit/api/v1/test_ownership_layering.py tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`（38 passed）。
- Review 授权回归继续收口（2026-02-23）：修复 `organization` 创建端点在 `resource_id=None` 场景的空上下文放行风险。`backend/src/api/v1/auth/organization.py` 将 `create_organization` 从通用 `require_authz(action="create", resource_type="organization")` 改为专用依赖 `_require_organization_create_authz`；创建鉴权上下文优先使用 `parent_id` 建立作用域，缺失时回退 `current_user.default_organization_id`，并映射 organization→party（`Party.id/external_ref`），无映射回退组织ID，最终缺失时写入 `__unscoped__:organization:create` 哨兵以 fail-closed。同时注入 `party_id/owner_party_id/manager_party_id`。同步补测：`backend/tests/unit/api/v1/test_organization_layering.py` 新增“父组织作用域注入”和“无组织上下文哨兵回退”用例，并更新 create 端点依赖断言。验证：`backend/.venv/bin/ruff check backend/src/api/v1/auth/organization.py backend/tests/unit/api/v1/test_organization_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_organization_layering.py tests/unit/middleware/test_authz_dependency.py tests/unit/migration/test_policy_package_seed_actions.py`（27 passed）。
- Review 授权回归继续收口（2026-02-23）：补齐 `user/role` 创建接口的 party 作用域上下文，避免 `resource_id=None` 且缺少资源主体字段时命中条件表达式兜底真值。`backend/src/api/v1/auth/auth_modules/users.py` 在 `create_user` 的 `require_authz` 中新增 `resource_context` 解析器 `_resolve_user_create_resource_context`，从请求体提取 `default_organization_id` 并注入 `party_id/owner_party_id/manager_party_id`；未提供组织时写入 `__unscoped__:user:create` 哨兵实现 fail-closed。`backend/src/api/v1/auth/roles.py` 在 `create_role` 的 `require_authz` 中新增 `_resolve_role_create_resource_context`，优先使用 `party_id`，否则回退 `organization_id`，仍缺失时写入 `__unscoped__:role:create`。同步补测：`backend/tests/unit/api/v1/test_users_layering.py`、`backend/tests/unit/api/v1/test_roles_layering.py` 新增创建上下文解析断言。验证：`backend/.venv/bin/ruff check backend/src/api/v1/auth/auth_modules/users.py backend/src/api/v1/auth/roles.py backend/tests/unit/api/v1/test_users_layering.py backend/tests/unit/api/v1/test_roles_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_users_layering.py tests/unit/api/v1/test_roles_layering.py tests/unit/middleware/test_authz_dependency.py tests/unit/migration/test_policy_package_seed_actions.py`（44 passed）。
- 创建鉴权上下文补强（2026-02-23）：继续修复 `resource_id=None` 场景下的 ABAC 上下文缺失风险。1) `backend/src/api/v1/assets/assets.py` 新增 `_require_asset_create_authz`，在 `create_asset` 前注入 `owner_party_id/manager_party_id`（并携带 `ownership_id/organization_id`）到 `resource_context` 后执行 `authz_service.check_access`。2) `backend/src/api/v1/assets/project.py` 新增 `_require_project_create_authz`，在 `create_project` 前注入 `manager_party_id`（并携带 `organization_id`）到 `resource_context`。同步补测：`backend/tests/unit/api/v1/test_assets_authz_layering.py`、`backend/tests/unit/api/v1/test_project_layering.py` 新增创建依赖绑定与上下文注入断言。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/assets/assets.py src/api/v1/assets/project.py tests/unit/api/v1/test_assets_authz_layering.py tests/unit/api/v1/test_project_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_assets_authz_layering.py tests/unit/api/v1/test_project_layering.py`（8 passed）。
- RBAC 角色分页计数回归修复（2026-02-23）：修复 `organization_id`（DEPRECATED alias）筛选下的总数查询交叉连接问题。`backend/src/crud/rbac.py` 将 legacy 组织作用域条件前置到 `base_query=select(Role)`，并同时传入 `build_query`/`build_count_query`，避免在 `count` 外层追加 `Role` 条件导致 `FROM (...) AS anon_1, roles` 的笛卡尔积；同步在 `backend/tests/unit/crud/test_rbac.py` 增加并加固回归断言（`assert len(count_stmt.get_final_froms()) == 1` 与 `assert "anon_1, roles" not in count_sql`），防止分页总数字段再次被放大。验证：`backend/.venv/bin/pytest --no-cov backend/tests/unit/crud/test_rbac.py -k maps_deprecated_organization_id_alias -q`（1 passed）；`backend/.venv/bin/pytest --no-cov backend/tests/unit/crud/test_rbac.py -q`（20 passed）。
- Review 授权回归继续收口（2026-02-23）：补齐 `role/user` 资源的 trusted scope 上下文，降低条件兜底放行风险。`backend/src/middleware/auth.py` 新增 `_load_role_scope_context` 与 `_load_user_scope_context`：`role` 优先 `roles.party_id`，缺失时从 `organization_id` 映射；`user` 优先 `user_party_bindings.party_id`（主绑定优先），缺失时回退默认组织映射；均注入 `party_id/owner_party_id/manager_party_id`。当 `user` 既无绑定也无默认组织时，写入 `__unscoped__:user:{id}` 哨兵值实现 fail-closed。同步补测：`backend/tests/unit/middleware/test_authz_dependency.py` 新增 role/user 四条作用域用例。验证：`cd backend && ./.venv/bin/ruff check src/middleware/auth.py tests/unit/middleware/test_authz_dependency.py && ./.venv/bin/pytest --no-cov -q tests/unit/middleware/test_authz_dependency.py`（通过）。
- Auth 资源类型漂移门禁（2026-02-23）：新增 `backend/tests/unit/api/v1/test_auth_resource_type_guard.py`，静态扫描 `backend/src/api/v1/auth/**/*.py`，禁止在 auth 域路由中出现 `require_authz(..., resource_type="asset")`；用于阻断“复制粘贴默认 asset 资源”回归。验证：`cd backend && ./.venv/bin/ruff check tests/unit/api/v1/test_auth_resource_type_guard.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_auth_resource_type_guard.py tests/unit/api/v1/test_users_layering.py tests/unit/api/v1/test_auth_audit_layering.py tests/unit/api/v1/test_auth_security_layering.py`（21 passed）。
- Auth 模块 ABAC 资源语义收敛（2026-02-23）：对根因排查中确认的“默认 `asset` 资源漂移”做系统性修复。`backend/src/api/v1/auth/auth_modules/users.py` 将用户管理相关 `require_authz` 全量切换为 `resource_type="user"`（含列表/创建/统计、以及 `/{user_id}` 下更新/删除/激活/锁定/解锁/重置密码与 `_require_user_*_or_self_authz` 委托链），避免用户域接口误依赖资产权限；`backend/src/api/v1/auth/auth_modules/audit.py` 将审计统计改为 `resource_type="operation_log"`；`backend/src/api/v1/auth/auth_modules/security.py` 将安全配置改为 `resource_type="system_settings"`。同步更新分层断言：`backend/tests/unit/api/v1/test_users_layering.py`、`backend/tests/unit/api/v1/test_auth_audit_layering.py`、`backend/tests/unit/api/v1/test_auth_security_layering.py`。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/auth/auth_modules/users.py src/api/v1/auth/auth_modules/audit.py src/api/v1/auth/auth_modules/security.py tests/unit/api/v1/test_users_layering.py tests/unit/api/v1/test_auth_audit_layering.py tests/unit/api/v1/test_auth_security_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_users_layering.py tests/unit/api/v1/test_auth_audit_layering.py tests/unit/api/v1/test_auth_security_layering.py`（20 passed）。
- Review 授权回归继续收口（2026-02-23）：补齐 `property_certificate` 的 party 作用域上下文，避免与 `organization` 同类的条件兜底放行。`backend/src/middleware/auth.py` 的 `_load_property_certificate_scope_context` 现注入 `party_id/owner_party_id/manager_party_id`，并通过 `_resolve_organization_party_id`（优先 `Party.id`、再 `external_ref`）解析组织映射；无映射时回退 `organization_id` 维持 fail-closed。同步补测：`backend/tests/unit/middleware/test_authz_dependency.py` 新增产权证上下文“命中映射/回退组织ID”两条用例。验证：`cd backend && ./.venv/bin/ruff check src/middleware/auth.py tests/unit/middleware/test_authz_dependency.py && ./.venv/bin/pytest --no-cov -q tests/unit/middleware/test_authz_dependency.py`（通过）。
- Review 授权回归继续收口（2026-02-23）：新增防漂移门禁并修复 `llm_prompt/analytics` 遗漏。`backend/tests/unit/migration/test_policy_package_seed_actions.py` 新增 API `require_authz` 资源类型自动发现断言，要求 backfill `_EXPANDED_RESOURCE_TYPES` 与“非基础资源（排除 `asset/project`）”完全一致；执行后发现 `llm_prompt` 与 `analytics` 漏配，已在 `backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 补齐。验证：`cd backend && ./.venv/bin/ruff check alembic/versions/20260221_backfill_expanded_policy_package_rules.py tests/unit/migration/test_policy_package_seed_actions.py && ./.venv/bin/pytest --no-cov -q tests/unit/migration/test_policy_package_seed_actions.py`（12 passed）。
- Review 授权回归修复（2026-02-23）：修复两项高优先级 ABAC 回归。1) `backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 扩充 `_EXPANDED_RESOURCE_TYPES`，补齐 `backup/collection/contact/dictionary/enum_field/history/notification` 的策略回填，避免新加 `require_authz` 资源类型落入 `deny_by_default`。2) `backend/src/middleware/auth.py` 强化组织资源上下文：`_load_organization_scope_context` 现在注入 `party_id/owner_party_id/manager_party_id`，优先解析 organization→party 映射（`external_ref`→`code`→`name`），缺失映射时回退 `organization_id` 以保持 fail-closed。同步补测：`backend/tests/unit/migration/test_policy_package_seed_actions.py`、`backend/tests/unit/middleware/test_authz_dependency.py`。验证：`cd backend && ./.venv/bin/ruff check alembic/versions/20260221_backfill_expanded_policy_package_rules.py src/middleware/auth.py tests/unit/migration/test_policy_package_seed_actions.py tests/unit/middleware/test_authz_dependency.py && ./.venv/bin/pytest --no-cov -q tests/unit/migration/test_policy_package_seed_actions.py tests/unit/middleware/test_authz_dependency.py`（16 passed）。
- Review 授权回归修复（2026-02-23）：修复 3 个授权缺口。1) `POST /contracts` 改为使用创建合同专用鉴权依赖：`backend/src/api/v1/rent_contracts/contracts.py` 新增 `_require_contract_create_authz`，并将 `owner_party_id`/`manager_party_id` 注入 ABAC `resource_context`，避免缺少目标主体上下文导致跨主体创建越权。2) 会话自助列表去耦资产权限：`backend/src/api/v1/auth/auth_modules/sessions.py` 移除 `get_user_sessions` 的 `require_authz(action=\"read\", resource_type=\"asset\")`，恢复仅基于当前登录用户查询本人会话。3) 前端 capability guard 改为默认 fail-close 且补齐路由元数据：`frontend/src/App.tsx` 引入“未声明权限则拒绝”分支（支持 `capabilityGuardBypass` 显式豁免）；`frontend/src/routes/AppRoutes.tsx` 新增 `capabilityGuardBypass` 元数据并在受保护路由上标注；`frontend/src/constants/routes.ts` 补齐 `ownership/project/property_certificate/system templates/settings` 的权限定义。同步补测：`backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`、`backend/tests/unit/api/v1/test_auth_sessions_layering.py`、`frontend/src/routes/__tests__/AppRoutes.authz-metadata.test.ts`。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/contracts.py src/api/v1/auth/auth_modules/sessions.py tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_auth_sessions_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_auth_sessions_layering.py`（17 passed）；`cd frontend && corepack pnpm check:route-authz && corepack pnpm check:capability-guard-wiring && corepack pnpm check:authz-vocabulary && corepack pnpm type-check && corepack pnpm exec oxlint --config .oxlintrc.json --deny-warnings --max-warnings=0 --disable-unicorn-plugin --react-plugin --import-plugin --jsx-a11y-plugin src/App.tsx src/routes/AppRoutes.tsx src/constants/routes.ts src/routes/__tests__/AppRoutes.authz-metadata.test.ts`（通过）。附注：本环境下 `vitest run src/routes/__tests__/AppRoutes.authz-metadata.test.ts` 多次出现 worker 启动超时/卡住，未能稳定完成该单测执行。 
- ABAC `resource_type` 漂移第七十八批（2026-02-23）：继续在 `system/collection` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/collection.py` 的 `POST /collection/records` 从 `resource_type="asset"` 调整为 `resource_type="collection"`；同步更新 `backend/tests/unit/api/v1/test_collection_layering.py` 的对应分层断言（仅 `create_collection_record` 项变更，其余 `collection` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/collection.py tests/unit/api/v1/test_collection_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_collection_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第七十七批（2026-02-23）：继续在 `system/collection` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/collection.py` 的 `GET /collection/records/{record_id}` 从 `resource_type="asset"` 调整为 `resource_type="collection"`，并保持 `resource_id="{record_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_collection_layering.py` 的对应分层断言（仅 `get_collection_record` 项变更，其余 `collection` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/collection.py tests/unit/api/v1/test_collection_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_collection_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第七十六批（2026-02-23）：继续在 `system/collection` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/collection.py` 的 `GET /collection/records` 从 `resource_type="asset"` 调整为 `resource_type="collection"`；同步更新 `backend/tests/unit/api/v1/test_collection_layering.py` 的对应分层断言（仅 `list_collection_records` 项变更，其余 `collection` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/collection.py tests/unit/api/v1/test_collection_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_collection_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第七十五批（2026-02-23）：切换到 `system/collection` 子模块并继续按“最小粒度增量”推进，`backend/src/api/v1/system/collection.py` 的 `GET /collection/summary` 从 `resource_type="asset"` 调整为 `resource_type="collection"`；同步更新 `backend/tests/unit/api/v1/test_collection_layering.py` 的对应分层断言（仅 `get_collection_summary` 项变更，其余 `collection` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/collection.py tests/unit/api/v1/test_collection_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_collection_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第七十四批（2026-02-23）：继续在 `system/notifications` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/notifications.py` 的 `POST /notifications/run-tasks` 从 `resource_type="asset"` 调整为 `resource_type="notification"`；同步更新 `backend/tests/unit/api/v1/test_notifications_layering.py` 的对应分层断言（仅 `run_notification_tasks_endpoint` 项变更）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_notifications_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第七十三批（2026-02-23）：继续在 `system/notifications` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/notifications.py` 的 `DELETE /notifications/{notification_id}` 从 `resource_type="asset"` 调整为 `resource_type="notification"`，并保持 `resource_id="{notification_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_notifications_layering.py` 的对应分层断言（仅 `delete_notification` 项变更，其余 `notifications` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_notifications_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第七十二批（2026-02-23）：继续在 `system/notifications` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/notifications.py` 的 `POST /notifications/read-all` 从 `resource_type="asset"` 调整为 `resource_type="notification"`；同步更新 `backend/tests/unit/api/v1/test_notifications_layering.py` 的对应分层断言（仅 `mark_all_as_read` 项变更，其余 `notifications` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_notifications_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第七十一批（2026-02-23）：继续在 `system/notifications` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/notifications.py` 的 `POST /notifications/{notification_id}/read` 从 `resource_type="asset"` 调整为 `resource_type="notification"`，并保持 `resource_id="{notification_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_notifications_layering.py` 的对应分层断言（仅 `mark_notification_as_read` 项变更，其余 `notifications` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_notifications_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第七十批（2026-02-23）：继续在 `system/notifications` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/notifications.py` 的 `GET /notifications/unread-count` 从 `resource_type="asset"` 调整为 `resource_type="notification"`；同步更新 `backend/tests/unit/api/v1/test_notifications_layering.py` 的对应分层断言（仅 `get_unread_count` 项变更，其余 `notifications` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_notifications_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十九批（2026-02-23）：切换到 `system/notifications` 子模块并继续按“最小粒度增量”推进，`backend/src/api/v1/system/notifications.py` 的 `GET /notifications` 从 `resource_type="asset"` 调整为 `resource_type="notification"`；同步更新 `backend/tests/unit/api/v1/test_notifications_layering.py` 的对应分层断言（仅 `get_notifications` 项变更，其余 `notifications` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_notifications_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十八批（2026-02-23）：继续在 `system/dictionaries` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/dictionaries.py` 的 `DELETE /system/dictionaries/{dict_type}` 从 `resource_type="asset"` 调整为 `resource_type="dictionary"`，并保持 `resource_id="{dict_type}"` 不变；同步更新 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 的对应分层断言（仅 `delete_dictionary_type` 项变更）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_dictionaries_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十七批（2026-02-23）：继续在 `system/dictionaries` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/dictionaries.py` 的 `POST /system/dictionaries/{dict_type}/values` 从 `resource_type="asset"` 调整为 `resource_type="dictionary"`，并保持 `resource_id="{dict_type}"` 不变；同步更新 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 的对应分层断言（仅 `add_dictionary_value` 项变更，其余 `dictionaries` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_dictionaries_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十六批（2026-02-23）：继续在 `system/dictionaries` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/dictionaries.py` 的 `GET /system/dictionaries/validation/stats` 从 `resource_type="asset"` 调整为 `resource_type="dictionary"`；同步更新 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 的对应分层断言（仅 `get_validation_statistics` 项变更，其余 `dictionaries` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_dictionaries_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十五批（2026-02-23）：继续在 `system/dictionaries` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/dictionaries.py` 的 `GET /system/dictionaries/types` 从 `resource_type="asset"` 调整为 `resource_type="dictionary"`；同步更新 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 的对应分层断言（仅 `get_dictionary_types` 项变更，其余 `dictionaries` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_dictionaries_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十四批（2026-02-23）：继续在 `system/dictionaries` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/dictionaries.py` 的 `POST /system/dictionaries/{dict_type}/quick-create` 从 `resource_type="asset"` 调整为 `resource_type="dictionary"`，并保持 `resource_id="{dict_type}"` 不变；同步更新 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 的对应分层断言（仅 `quick_create_dictionary` 项变更，其余 `dictionaries` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_dictionaries_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十三批（2026-02-23）：切换到 `system/dictionaries` 子模块并继续按“最小粒度增量”推进，`backend/src/api/v1/system/dictionaries.py` 的 `GET /system/dictionaries/{dict_type}/options` 从 `resource_type="asset"` 调整为 `resource_type="dictionary"`，并保持 `resource_id="{dict_type}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 的对应分层断言（仅 `get_dictionary_options` 项变更，其余 `dictionaries` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_dictionaries_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第六十二批（2026-02-23）：继续在 `system/contact` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `POST /contact/batch/{entity_type}/{entity_id}` 从 `resource_type="asset"` 调整为 `resource_type="contact"`，并保持 `resource_id="{entity_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `create_contacts_batch` 项变更）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第六十一批（2026-02-23）：继续在 `system/contact` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `DELETE /contact/{contact_id}` 从 `resource_type="asset"` 调整为 `resource_type="contact"`，并保持 `resource_id="{contact_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `delete_contact` 项变更，其余 `contact` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第六十批（2026-02-23）：继续在 `system/contact` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `PUT /contact/{contact_id}` 从 `resource_type="asset"` 调整为 `resource_type="contact"`，并保持 `resource_id="{contact_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `update_contact` 项变更，其余 `contact` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第五十九批（2026-02-23）：继续在 `system/contact` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `GET /contact/entity/{entity_type}/{entity_id}/primary` 从 `resource_type="asset"` 调整为 `resource_type="contact"`，并保持 `resource_id="{entity_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `get_primary_contact` 项变更，其余 `contact` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第五十八批（2026-02-23）：继续在 `system/contact` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `GET /contact/entity/{entity_type}/{entity_id}` 从 `resource_type="asset"` 调整为 `resource_type="contact"`，并保持 `resource_id="{entity_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `get_entity_contacts` 项变更，其余 `contact` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第五十七批（2026-02-23）：继续在 `system/contact` 子模块按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `GET /contact/{contact_id}` 从 `resource_type="asset"` 调整为 `resource_type="contact"`，并保持 `resource_id="{contact_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `get_contact` 项变更，其余 `contact` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第五十六批（2026-02-23）：切换到 `system/contact` 子模块并继续按“最小粒度增量”推进，`backend/src/api/v1/system/contact.py` 的 `POST /contact/` 从 `resource_type="asset"` 调整为 `resource_type="contact"`；同步更新 `backend/tests/unit/api/v1/test_contact_layering.py` 的对应分层断言（仅 `create_contact` 项变更，其余 `contact` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_contact_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- Review 授权回归修复（2026-02-23）：修复 `operation_log` 策略覆盖缺口与 capability guard 元数据空转问题。1) 后端 `backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 将 `operation_log` 纳入 `_EXPANDED_RESOURCE_TYPES`，并在 `backend/tests/unit/migration/test_policy_package_seed_actions.py` 新增覆盖断言（资源覆盖与 expanded list 双校验），确保非管理员不再因缺少种子规则被默认拒绝。2) 前端 `frontend/src/routes/AppRoutes.tsx` 从 `ROUTE_CONFIG` 递归派生并回填 `protectedRoutes` 权限元数据，使 `VITE_ENABLE_CAPABILITY_GUARD=true` 时权限守卫链真正生效；同时更新 `frontend/scripts/check-route-authz-mutual-exclusive.mjs` 以兼容 `protectedRoutes` 由函数包装数组字面量的写法，并新增回归测试 `frontend/src/routes/__tests__/AppRoutes.authz-metadata.test.ts`。验证：`cd backend && ./.venv/bin/ruff check alembic/versions/20260221_backfill_expanded_policy_package_rules.py tests/unit/migration/test_policy_package_seed_actions.py`、`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/migration/test_policy_package_seed_actions.py -k 'operation_log or new_guarded_resource_types'`（2 passed）、`cd frontend && pnpm run check:capability-guard-wiring && pnpm run check:route-authz && pnpm run check:authz-vocabulary && pnpm run type-check`（均通过）。
- ABAC `resource_type` 漂移第五十批（2026-02-23）：继续按“最小粒度增量”推进 `system/backup` 子模块，`backend/src/api/v1/system/backup.py` 的 `GET /backup/stats` 从 `resource_type="asset"` 调整为 `resource_type="backup"`；同步更新 `backend/tests/unit/api/v1/test_backup_layering.py` 的对应分层断言（仅 `get_backup_stats` 项变更，其余 `backup` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第四十九批（2026-02-23）：继续按“最小粒度增量”推进 `system/backup` 子模块，`backend/src/api/v1/system/backup.py` 的 `DELETE /backup/delete/{backup_name}` 从 `resource_type="asset"` 调整为 `resource_type="backup"`，并保持 `resource_id="{backup_name}"` 不变；同步更新 `backend/tests/unit/api/v1/test_backup_layering.py` 的对应分层断言（仅 `delete_backup` 项变更，其余 `backup` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第四十八批（2026-02-23）：继续按“最小粒度增量”推进 `system/backup` 子模块，`backend/src/api/v1/system/backup.py` 的 `POST /backup/restore/{backup_name}` 从 `resource_type="asset"` 调整为 `resource_type="backup"`，并保持 `resource_id="{backup_name}"` 不变；同步更新 `backend/tests/unit/api/v1/test_backup_layering.py` 的对应分层断言（仅 `restore_backup` 项变更，其余 `backup` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第四十七批（2026-02-23）：继续按“最小粒度增量”推进 `system/backup` 子模块，`backend/src/api/v1/system/backup.py` 的 `GET /backup/download/{backup_name}` 从 `resource_type="asset"` 调整为 `resource_type="backup"`，并保持 `resource_id="{backup_name}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_backup_layering.py` 的对应分层断言（仅 `download_backup` 项变更，其余 `backup` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第四十六批（2026-02-23）：继续按“最小粒度增量”推进 `system/backup` 子模块，`backend/src/api/v1/system/backup.py` 的 `GET /backup/list[Any]` 从 `resource_type="asset"` 调整为 `resource_type="backup"`；同步更新 `backend/tests/unit/api/v1/test_backup_layering.py` 的对应分层断言（仅 `list_backups` 项变更，其余 `backup` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- ABAC `resource_type` 漂移第四十五批（2026-02-23）：在 `operation_logs` 子模块收口后，继续按“最小粒度增量”推进 `system/backup` 子模块，`backend/src/api/v1/system/backup.py` 的 `POST /backup/create` 从 `resource_type="asset"` 调整为 `resource_type="backup"`；同步更新 `backend/tests/unit/api/v1/test_backup_layering.py` 的对应分层断言（仅 `create_backup` 项变更，其余 `backup` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_backup_layering.py tests/unit/middleware/test_authz_dependency.py`（6 passed）。
- Review 缓存隔离修复（2026-02-23）：修复 relation-aware 主体过滤缓存键碰撞。`backend/src/crud/base.py` 的 `_serialize_party_filter` 由仅序列化 `party_ids` 调整为同时序列化 `owner_party_ids`、`manager_party_ids`（并保留 `None` 与 `[]` 语义差异），避免同一 `party_ids` 并集但 owner/manager 作用域不同时命中同一缓存键导致串读；补充回归测试 `backend/tests/unit/crud/test_base_distinct_values.py`，新增“owner/manager 作用域互换”和“`None` vs `[]`”两类缓存键区分断言。验证：`cd backend && ./.venv/bin/ruff check src/crud/base.py tests/unit/crud/test_base_distinct_values.py`、`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/crud/test_base_distinct_values.py`（21 passed）。
- Review 回归修复（2026-02-23）：修复 `party_scope` 两项授权范围回归并补测。1) `backend/src/services/party_scope.py` 调整 `resolve_user_party_filter` 分支顺序：在“无 bindings”场景先执行 `_has_unrestricted_party_scope_access()`，特权用户优先旁路（返回 `None`），仅普通用户再走 legacy `default_organization_id` 回退，避免管理员/全局读账号被意外收窄。2) 保留绑定关系语义：`resolve_user_party_filter` 按 `relation_type` 分离 owner/manager scope 并写入 `PartyFilter`；`backend/src/crud/query_builder.py` 扩展 `PartyFilter`（`owner_party_ids`/`manager_party_ids`）与 relation-aware 过滤路径，确保 owner/manager 列按各自 scope 过滤，避免 `filter_mode='any'` 引入跨视角串读。3) 验证通过：`backend/tests/unit/services/test_party_scope.py`、`backend/tests/unit/crud/test_query_builder.py`，以及受影响服务回归 `backend/tests/unit/services/asset/test_asset_service.py`、`backend/tests/unit/services/project/test_project_service.py`、`backend/tests/unit/services/permission/test_rbac_service.py`、`backend/tests/unit/services/document/test_pdf_import_service.py`、`backend/tests/unit/services/property_certificate/test_service.py`（合计 232 passed）。
- ABAC `resource_type` 漂移第二十九批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值批量创建端点语义：`backend/src/api/v1/system/enum_field.py` 的 `POST /enum-fields/types/{type_id}/values/batch` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `batch_create_enum_field_values` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十八批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值删除端点语义：`backend/src/api/v1/system/enum_field.py` 的 `DELETE /enum-fields/values/{value_id}` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{value_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `delete_enum_field_value` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- Review 回归修复（2026-02-23）：修复 ABAC/租户范围两项 P1 回归。1) `backend/src/services/party_scope.py` 在“无 `user_party_bindings` 且无 legacy `default_organization_id`”分支恢复特权旁路：对管理员或具备全局读取权限用户返回 `None`（不施加租户过滤），避免 privileged 账号被误 fail-closed；普通用户继续保持 fail-closed。2) `backend/src/services/party/service.py` 在创建 `user_party_binding` 成功后发布 `AUTHZ_USER_SCOPE_UPDATED` 失效事件，确保用户主体范围变更可立即按 `user_id` 清理 ABAC decision cache。3) 补充回归测试：`backend/tests/unit/services/test_party_scope.py` 新增特权旁路断言，`backend/tests/unit/services/test_authz_events.py` 新增 `AUTHZ_USER_SCOPE_UPDATED` 精准失效断言，新增 `backend/tests/unit/services/test_party_service.py` 覆盖 scope 事件发布与失败容错。
- Phase 2 计划复核补测（2026-02-23）：补充 `party` 列表 `search` 参数的 API 行为测试。1) 扩展 `backend/tests/unit/api/v1/test_party_layering.py`，新增 `test_list_parties_should_pass_search_keyword_to_service`，验证 `/parties` 端点在路由层对 `search` 查询参数的透传契约（关键字输入与 `None` 分支均有覆盖）；2) 新增 HTTP 入口测试 `backend/tests/unit/api/v1/test_party_api.py`，通过 `TestClient` 分别验证按名称搜索（`search=acme`）与按编码搜索（`search=ACME-001`）均可正确命中并过滤结果，锁定 name/code 双路径契约。
- Phase 2 计划复核补丁（2026-02-23）：按 `docs/plans/2026-02-19-phase2-implementation-plan.md` 的收口要求补齐两项后端缺口。1) 主体列表 API 增加 `search` 模糊检索链路：`backend/src/api/v1/party.py` 新增 `search` Query 参数并透传，`backend/src/services/party/service.py` 与 `backend/src/crud/party.py` 同步接入，CRUD 侧按 `Party.name/code ILIKE` 组合过滤；2) 修复项目响应兼容层将未加载关系硬编码为空数组的问题：`backend/src/schemas/project.py` 将 `ownership_relations` 在未加载/异常场景改为 `None`，避免将“未加载”误表达为“显式空关系”。同步测试：新增 `backend/tests/unit/crud/test_party.py::test_get_parties_applies_search_filter`，并更新 `backend/tests/unit/schemas/test_response_safety.py` 断言为 `None` 语义。
- Review 回归修复（2026-02-23）：修复两项功能回归。1) 后端 `backend/src/crud/query_builder.py` 调整 `PartyFilter` 行为：当模型不存在 `owner_party_id/manager_party_id/party_id/organization_id` 时不再 fail-closed，而是跳过主体过滤，避免 `AssetCustomField`、`Permission` 等全局模型被误过滤为空；同时保留 `party_ids=[]` 时的严格 fail-closed。2) 前端 `frontend/src/App.tsx` 将 capability guard 开关开启时的受保护路由渲染改为真实应用 `PermissionGuard`（按 `permissions` + `permissionMode`），并在 `adminOnly` 路由上执行管理员校验，避免权限元数据被绕过。3) 加强门禁脚本 `frontend/scripts/check-capability-guard-wiring.mjs`，新增对 `PermissionGuard` 包裹证据的校验；并在 `backend/tests/unit/crud/test_query_builder.py` 增补 count 查询场景断言。验证：`cd backend && ./.venv/bin/pytest --no-cov -q tests/unit/crud/test_query_builder.py`（13 passed）；`pnpm --dir frontend run check:capability-guard-wiring`（PASS）；`pnpm --dir frontend run type-check`（通过）。
- Phase 3 计划修订（v1.49，2026-02-23）：按“执行就绪”最小修订 `docs/plans/2026-02-20-phase3-implementation-plan.md`，将页首版本提升到 v1.49，并在 P3d 冻结说明中补充“仓库已预置 Day-0 三项产物基线文件”口径（`docs/plans/execution/phase3d-authz-freeze.md`、`docs/release/evidence/capability-guard-env.md`、`frontend/tests/e2e/auth/authz-minimal.spec.ts`），同时保留 `§5.2.1` 幂等脚手架命令作为执行入口；新增以上三项模板/骨架文件，消除 P3d Entry 的文件缺失阻断并保持执行当日回填要求。
- ABAC 关键阻断修复（2026-02-23）：完成两项高风险收口。1) 资源语义对齐：`backend/src/api/v1/auth/roles.py`、`backend/src/api/v1/auth/organization.py`、`backend/src/api/v1/system/system_settings.py` 的 `require_authz(..., resource_type="asset")` 分别切换为 `role/user/organization/system_settings` 语义资源；同步更新分层测试 `test_roles_layering.py`、`test_organization_layering.py`、`test_system_settings_layering.py`。2) 项目统计隔离修复：`backend/src/api/v1/assets/project.py` 将 `current_user_id` 透传至服务层；`backend/src/services/project/service.py` 的 `get_project_statistics` 新增 `party_filter/current_user_id` 解析与 fail-closed；`backend/src/crud/project.py` 的统计查询新增 PartyFilter 约束（含 legacy `organization_id` 回退分支）。3) 为语义资源补齐策略回填覆盖：`backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 增加 `role/user/organization/system_settings`。验证：`cd backend && ./.venv/bin/ruff check ...`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_roles_layering.py tests/unit/api/v1/test_organization_layering.py tests/unit/api/v1/test_system_settings_layering.py tests/unit/api/v1/test_project_layering.py -q`（`17 passed`）。
- Phase 3 交付归档补充（2026-02-22）：新增 `docs/reviews/2026-02-22-phase3-plan-execution-handoff.md`，将本轮 Phase 3 的“计划修订 + 门禁脚本化 + 前端接线 + 聚合校验通过”整理为可直接评审/提交的交付记录，包含变更范围、验证结果与下一步建议，便于后续 PR/交接复用。
- Phase 3 收口验证（2026-02-22）：完成前端全门禁复核并通过。执行 `cd frontend && pnpm check` 后仅剩格式门禁失败，按报告定向格式化 `src/App.tsx`、`src/components/Router/__tests__/DynamicRouteRenderer.test.tsx`、`src/pages/Rental/__tests__/RentStatisticsPage.test.tsx`，随后再次执行 `cd frontend && pnpm check` 全量通过（lint/guard:ui/type-check/type-check:e2e/check:route-authz/check:authz-vocabulary/check:capability-guard-wiring/format:check 全绿）。
- Phase 3 门禁落地续作（2026-02-22）：将前一轮新增门禁从“文档/脚本存在”推进到“代码可通过”。1) 标准化 `frontend/src/constants/routes.ts` 与 `frontend/src/hooks/usePermission.tsx` 中遗留动作词/资源名（`view/edit/import/settings/logs/dictionary/lock/assign_permissions`、`rental`）到 Phase 3 词汇口径；2) 在 `frontend/src/routes/AppRoutes.tsx` 增加 `ProtectedRouteItem` 路由权限元数据类型（`permissions`/`permissionMode`/`adminOnly`）；3) 在 `frontend/src/App.tsx` 增加 `VITE_ENABLE_CAPABILITY_GUARD`、`capabilityGuardEnabled` 与 `renderProtectedElement` 接线骨架；4) 复核通过新门禁脚本：`cd frontend && node scripts/check-authz-vocabulary.mjs && node scripts/check-capability-guard-wiring.mjs`（均 PASS）。
- Phase 3 计划持续修订（v1.48，2026-02-22）：继续按严格审阅收敛门禁可执行性。1) 新增 `frontend/scripts/check-authz-vocabulary.mjs`，以 AST 方式校验 Phase 3 守卫链中的禁止动作词（`view/edit/import/settings/logs/dictionary/lock/assign_permissions`）与未映射资源名（`rental`）；2) 新增 `frontend/scripts/check-capability-guard-wiring.mjs`，校验 `App.tsx`/`AppRoutes.tsx` 的 capability guard 主链接线证据（`VITE_ENABLE_CAPABILITY_GUARD`、`capabilityGuardEnabled`、`adminOnly/permissions/permissionMode`）；3) `frontend/package.json` 新增脚本 `check:authz-vocabulary` 与 `check:capability-guard-wiring`，并接入 `check` 聚合命令；4) 将 `docs/plans/2026-02-20-phase3-implementation-plan.md` 中 §4.4.1 门禁 A/B 与 §6.3.1 D9 Step 2（含 PowerShell）切换为脚本门禁引用，降低 grep 误判；5) §6.1 质量门禁与 `package.json:check` 对齐说明同步更新，文档历史新增 v1.48 记录。
- Phase 3 计划修订（v1.47，2026-02-22）：基于 `docs/reviews/2026-02-22-phase3-implementation-plan-strict-review-v4.md` 回写 `docs/plans/2026-02-20-phase3-implementation-plan.md`。本次重点：1) 修复两处 Bash 门禁命令 `(( ))` 误用（P3d seed revision 与 D9 Step 1）；2) 将 `§5.2.1 Day-0 脚手架 + 执行快照` 升级为 P3d Entry 前置硬约束，并新增 P3d Entry 的 Day-0 执行检查与 `@authz-minimal` 骨架门禁；3) 将 D9 拆分为“P3d 开发 Exit 接线门禁 + Phase 3→4 发布验签门禁”双层执行，降低开发阶段与发布证据耦合；4) 强化 D9 Step 2 为主生效链（`App.tsx`/`AppRoutes.tsx`）接线断言；5) 在 §9 交接项新增 D9 发布验签全量通过要求，并在 §10 文档历史新增 v1.47 记录。
- Phase 3 计划严格审阅（v4，2026-02-22）：新增 `docs/reviews/2026-02-22-phase3-implementation-plan-strict-review-v4.md`，对 `docs/plans/2026-02-20-phase3-implementation-plan.md` 做完整可执行性复核并输出分级问题清单。新增识别 2 条 P0 阻断（两处 Bash 门禁命令误用 `(( ))` 导致语法/语义不可执行：P3d seed 门禁与 D9 Step1）与 7 条 P1 高风险（含 E2E 强门禁前置缺失、冻结证据文件脚手架顺序风险、D9 发布证据与开发 Exit 耦合、grep 门禁误判窗口等），并给出最小修复顺序（先修命令语法，再固化 Day-0，再补最小 E2E，再拆分 D9）。
- Phase 2 审阅修复收敛（2026-02-22）：继续收敛“统一 party scope + strict fail-closed”后续回归。1) 更新 `backend/tests/unit/services/document/test_pdf_import_service.py`，将租户过滤解析测试从 `OrganizationPermissionService` 旧路径切换到 `src.services.party_scope.party_crud.get_user_bindings`，并同步断言 `PartyFilter(party_ids=["party-1", "party-2"])` 与异常 fail-closed 语义；2) 联合前序已修改的 `test_query_builder.py`、`test_project_service.py`、`test_rbac_service.py`、`test_asset_service.py` 完成测试口径统一。验证：`cd backend && .venv/bin/ruff check src/services/party_scope.py src/services/asset/asset_service.py src/services/project/service.py src/services/custom_field/service.py src/services/document/pdf_import_service.py src/services/property_certificate/service.py src/services/permission/rbac_service.py src/crud/query_builder.py src/main.py alembic/versions/20260219_phase2_add_party_columns_step1.py tests/unit/crud/test_query_builder.py tests/unit/services/project/test_project_service.py tests/unit/services/permission/test_rbac_service.py tests/unit/services/asset/test_asset_service.py tests/unit/services/document/test_pdf_import_service.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q -o log_cli=false tests/unit/crud/test_query_builder.py tests/unit/services/project/test_project_service.py tests/unit/services/permission/test_rbac_service.py tests/unit/services/asset/test_asset_service.py tests/unit/services/document/test_pdf_import_service.py`（`214 passed`）。
- Party scope 回退修复（2026-02-22）：修复 `backend/src/services/party_scope.py` 在用户无 `user_party_bindings` 时直接 fail-closed 的回归，现改为按用户 `default_organization_id` 做组织范围回退；仅当绑定解析异常或回退范围为空时维持 `PartyFilter(party_ids=[])`。同时为 ABAC 上下文构建链路补齐同口径回退：`backend/src/services/authz/context_builder.py` 在无任何 party 绑定时回退 `default_organization_id`，注入 `owner_party_ids/manager_party_ids`，避免 capabilities/custom-field 等读取链路因空 subject scope 被误拒绝。同步新增测试 `backend/tests/unit/services/test_authz_context_builder.py` 覆盖“绑定命中 / 回退命中 / 回退缺失”三条路径。验证：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/services/test_party_scope.py tests/unit/services/test_authz_context_builder.py -q`（7 passed）；`cd backend && ./.venv/bin/python -m ruff check src/services/party_scope.py src/services/authz/context_builder.py tests/unit/services/test_party_scope.py tests/unit/services/test_authz_context_builder.py`（All checks passed）。
- ABAC `resource_type` 漂移首轮矩阵与首批迁移（2026-02-22）：新增漂移审计矩阵 `docs/reviews/2026-02-22-authz-resource-type-drift-matrix.md`，固化全量分布（`333` 调用点，`asset=229`）、模块热点与策略 seed 覆盖缺口（当前仅见 `asset/project`）。同步完成首批低风险迁移：`backend/src/api/v1/system/monitoring.py` 将 7 处 `require_authz(..., resource_type="asset")` 更正为 `system_monitoring`，并更新 `backend/tests/unit/api/v1/test_monitoring_layering.py` 分层断言。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/system/monitoring.py tests/unit/api/v1/test_monitoring_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_monitoring_layering.py tests/unit/api/v1/test_monitoring.py tests/unit/middleware/test_authz_dependency.py -q`（30 passed）。
- ABAC `resource_type` 漂移第二批（2026-02-22）：继续按“低风险、语义对齐”策略推进，先将系统核心健康检查端点与监控语义统一：`backend/src/api/v1/system/system.py` 的 `GET /monitoring/health` 从 `resource_type="asset"` 调整为 `resource_type="system_monitoring"`；同步更新 `backend/tests/unit/api/v1/test_system_layering.py` 对应分层断言（仅 `health_check` 项变更，其余 `system/info`、`system/root` 暂保持 `asset`）。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/system/system.py tests/unit/api/v1/test_system_layering.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_system_layering.py tests/unit/middleware/test_authz_dependency.py -q`（5 passed）。
- ABAC `resource_type` 漂移第三批（2026-02-22）：继续按“最小粒度增量”推进，在任务域先做单端点语义对齐：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks/{task_id}` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `get_task` 项变更，其余任务端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第四批（2026-02-22）：继续按“最小粒度增量”推进，在任务域补齐历史查询端点语义：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks/{task_id}/history` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `get_task_history` 项变更，其余任务端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第五批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐统计查询端点语义：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks/statistics` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `get_task_statistics` 项变更，其余任务端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第六批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐运行中任务查询端点语义：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks/running` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `get_running_tasks` 项变更，其余任务端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第七批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐最近任务查询端点语义：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks/recent` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `get_recent_tasks` 项变更，其余任务端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第八批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐清理端点语义：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks/cleanup` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `cleanup_old_tasks` 项变更，任务域主查询端点已全部切换为 `task`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第九批（2026-02-23）：继续按“最小粒度增量”推进，从 `system` 模块切入语义对齐：`backend/src/api/v1/system/system.py` 的 `GET /system/info` 从 `resource_type="asset"` 调整为 `resource_type="system_settings"`；同步更新 `backend/tests/unit/api/v1/test_system_layering.py` 的对应分层断言（仅 `app_info` 项变更，`api_root` 暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/system.py tests/unit/api/v1/test_system_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_system_layering.py tests/unit/middleware/test_authz_dependency.py`（5 passed）。
- ABAC `resource_type` 漂移第十三批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐删除端点语义：`backend/src/api/v1/system/tasks.py` 的 `DELETE /tasks/{task_id}` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `delete_task` 项变更，任务主写操作端点已切换为 `task`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第十四批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐列表查询端点语义：`backend/src/api/v1/system/tasks.py` 的 `GET /tasks` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `get_tasks` 项变更，`create_task` 仍暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第十五批（2026-02-23）：继续按“最小粒度增量”推进，在任务域补齐创建端点语义：`backend/src/api/v1/system/tasks.py` 的 `POST /tasks/` 从 `resource_type="asset"` 调整为 `resource_type="task"`；同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 的对应分层断言（仅 `create_task` 项变更，任务主端点已全部切换为 `task`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_tasks_layering.py tests/unit/middleware/test_authz_dependency.py`（8 passed）。
- ABAC `resource_type` 漂移第十六批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐类型列表端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/types` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_types` 项变更，其余 `enum_field` 端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第十七批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐统计端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/types/statistics` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_statistics` 项变更，其余 `enum_field` 端点暂保持 `asset`）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第十八批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐类型详情端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/types/{type_id}` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_type` 项变更）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第十九批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐类型创建端点语义：`backend/src/api/v1/system/enum_field.py` 的 `POST /enum-fields/types` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `create_enum_field_type` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐类型更新端点语义：`backend/src/api/v1/system/enum_field.py` 的 `PUT /enum-fields/types/{type_id}` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `update_enum_field_type` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十一批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐类型删除端点语义：`backend/src/api/v1/system/enum_field.py` 的 `DELETE /enum-fields/types/{type_id}` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `delete_enum_field_type` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十二批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐类别列表端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/types/categories/list[Any]` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_categories` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十三批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值列表端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/types/{type_id}/values` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_values` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十四批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值树端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/types/{type_id}/values/tree` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_values_tree` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十五批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值详情端点语义：`backend/src/api/v1/system/enum_field.py` 的 `GET /enum-fields/values/{value_id}` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{value_id}"` 与 `deny_as_not_found=True` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `get_enum_field_value` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十六批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值创建端点语义：`backend/src/api/v1/system/enum_field.py` 的 `POST /enum-fields/types/{type_id}/values` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{type_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `create_enum_field_value` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- ABAC `resource_type` 漂移第二十七批（2026-02-23）：继续按“最小粒度增量”推进，在 `enum_field` 子域补齐字段值更新端点语义：`backend/src/api/v1/system/enum_field.py` 的 `PUT /enum-fields/values/{value_id}` 从 `resource_type="asset"` 调整为 `resource_type="enum_field"`，并保持 `resource_id="{value_id}"` 不变；同步更新 `backend/tests/unit/api/v1/test_enum_field_layering.py` 的对应分层断言（仅 `update_enum_field_value` 项变更，其余 `enum_field` 端点暂保持现状）。验证：`cd backend && .venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py`（通过）；`cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_enum_field_layering.py tests/unit/middleware/test_authz_dependency.py`（7 passed）。
- Phase 3 计划严格审阅（v3 增量深挖，2026-02-22）：新增 `docs/reviews/2026-02-22-phase3-implementation-plan-strict-review-v3.md`，对 `docs/plans/2026-02-20-phase3-implementation-plan.md`（v1.44）执行“可执行性 + 仓库现况”双重审阅。新增识别 2 条阻断项（关键门禁硬依赖 `rg` 但未声明前置、P3d 强制证据目录/文件无仓库脚手架）与 7 条高风险项（D9 复合命令可观测性差、`ROUTE_CONFIG` 废弃与最小清理边界歧义、grep 门禁语义误判风险、Entry/Exit 证据时点张力等），并给出最小整改顺序（先修 P0 再脚本化关键门禁）。
- Phase 3 计划执行性修订（v1.46，2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md`，将 P3d Exit 的 D9 验签由单条复合命令改为 §6.3.1 四步分段门禁（声明来源/代码引用/证据完整/反占位），并补齐 Bash + PowerShell 等价命令；同步将 P3d Exit 表格中的 D9 证据命令改为对 §6.3.1 的引用，提升失败定位与跨环境执行一致性。
- Review 授权回归修复（2026-02-22）：修复系统监控路由 ABAC 资源类型漂移与 legacy 规则回填回归风险。1) `backend/src/api/v1/system/system_monitoring/endpoints.py` 将监控端点 `require_authz(..., resource_type="asset")` 全量更正为 `resource_type="system_monitoring"`，恢复监控权限边界；2) `backend/tests/unit/api/v1/test_system_monitoring_layering.py` 同步更新分层断言，防止后续回退；3) `backend/tests/unit/migration/test_policy_package_seed_actions.py` 新增 `test_backfill_upgrade_should_update_existing_legacy_rule_condition_expr`，直接执行 `20260221` backfill migration 的 `upgrade()`，验证已落库 legacy rule 会从恒真条件更新为 scoped 条件。验证：`cd backend && ./.venv/bin/ruff check src/api/v1/system/system_monitoring/endpoints.py tests/unit/api/v1/test_system_monitoring_layering.py tests/unit/migration/test_policy_package_seed_actions.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/api/v1/test_system_monitoring_layering.py -k "endpoints_should_use_require_authz" -q`（2 passed）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/migration/test_policy_package_seed_actions.py -k "backfill_upgrade_should_update_existing_legacy_rule_condition_expr or backfill_migration_should_define_legacy_rule_condition_updates" -q`（2 passed）。
- Review 阻断回归修复（2026-02-22）：修复 3 项高优先级问题。1) `backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 新增 `LEGACY_POLICY_RULE_NAME_BY_ID` 并在升级时对 legacy rule IDs 执行 `condition_expr` 更新，避免已升级环境保留旧的宽松条件；2) `backend/src/schemas/rent_contract.py` 将 `RentContractCreate.ownership_id` 恢复为必填，阻断“仅 `owner_party_id`”请求在 DB 非空约束处触发 500；3) `backend/src/services/asset/asset_service.py` 的 `_resolve_party_filter` 改为优先读取 `user_party_bindings`，并与旧组织范围做并集兜底，解决 ABAC 放行但资产列表误空的问题。同步更新回归测试：`backend/tests/unit/migration/test_policy_package_seed_actions.py`、`backend/tests/unit/services/test_rent_contract_v2.py`、`backend/tests/unit/services/asset/test_asset_service.py`。验证：`cd backend && ./.venv/bin/ruff check alembic/versions/20260221_backfill_expanded_policy_package_rules.py src/schemas/rent_contract.py src/services/asset/asset_service.py tests/unit/migration/test_policy_package_seed_actions.py tests/unit/services/test_rent_contract_v2.py tests/unit/services/asset/test_asset_service.py`（通过）；`cd backend && ./.venv/bin/pytest --no-cov tests/unit/migration/test_policy_package_seed_actions.py tests/unit/services/test_rent_contract_v2.py tests/unit/services/asset/test_asset_service.py -q`（76 passed）。
- Phase 3 计划可执行性修订（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.44。1) 修正 `DB_PSQL_CMD` 默认值为 `docker compose exec postgres psql -U zcgl_user -d zcgl`（Bash/PowerShell 同步）；2) 将 P3e 旧字段校验拆分为“双口径”——运行时主链零引用（阻断）+ 仓库全量零引用（非阻断审计）；3) 明确 `ROUTE_CONFIG` 显式废弃与门禁 A 扫描边界（允许字面量卫生清理，禁止运行时回流）；4) 强化 D9 证据闭环，新增 `runtime-flag-value`、`protected-route-check` 必填项并在 Exit 增加反占位校验（禁 `TBD/TODO/待补/N/A`）。
- 租赁合同变更端点 ABAC 作用域修复（2026-02-22）：修复 `backend/src/api/v1/rent_contracts/contracts.py` 中 `update_contract`/`delete_contract` 鉴权仅传 `contract_id` 导致跨主体越权风险的问题。新增 scoped 依赖 `_require_contract_update_authz`、`_require_contract_delete_authz`，在鉴权前先解析合同并将 `owner_party_id`/`manager_party_id` 注入 `resource_context` 后再调用 `authz_service.check_access`。同步新增回归测试 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`（`test_contract_update_authz_should_include_party_scope_context`、`test_contract_delete_authz_should_raise_not_found_before_abac`）。验证：`cd backend && .venv/bin/pytest --no-cov tests/unit/api/v1/test_rent_contract_contracts_layering.py -k "contract_update_authz_should_include_party_scope_context or contract_delete_authz_should_raise_not_found_before_abac"`（2 passed），`cd backend && .venv/bin/ruff check src/api/v1/rent_contracts/contracts.py tests/unit/api/v1/test_rent_contract_contracts_layering.py`（All checks passed）。
- Phase 3 计划严格审阅（v2，2026-02-22）：新增 `docs/reviews/2026-02-22-phase3-implementation-plan-strict-review-v2.md`，对 `docs/plans/2026-02-20-phase3-implementation-plan.md` 做全文、严格、可执行性导向审阅并输出分级问题清单。重点识别 6 条阻断级问题（含 `DB_PSQL_CMD` 默认服务名与仓库 `docker-compose.yml` 不一致、P3e“全量零引用”与排除清单语义冲突、`ROUTE_CONFIG` 废弃策略与门禁扫描张力等），并给出最小整改顺序（先修 P0 再推进 P3）。
- Phase 3 计划语义澄清修订（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.43。1) 明确 D9 与 P3d Exit 的“双层验签”规则：层 1 为声明来源 A/B/C 三选一，层 2 为 `docs/release/evidence/capability-guard-env.md` 必填 `startup-log/release-id/ci-assert`；2) 收敛 P3e 收口表述，显式声明 `types/organization.ts` 与 `types/ownership.ts` 为 Phase 3 例外（与 §5.1 注记一致）；3) 为 `constants/routes` 派生依赖审计补齐统一登记字段（文件/结论/责任人/截止日期/验证命令）；4) 在 §6.2 增加 `grep -rEl[w]` 已启用 ERE 的说明，避免 `|/?` 语义误读；5) 在 §7.3 回滚流程新增 30~60 分钟观测窗口及“连续 10 分钟恶化触发升级”规则。
- Phase 3 实施计划严格审阅报告（2026-02-22）：新增 `docs/reviews/2026-02-22-phase3-implementation-plan-strict-review.md`，对 `docs/plans/2026-02-20-phase3-implementation-plan.md` 进行全文、严格、可执行性导向审阅；输出分级问题清单（P0/P1/P2）、仓库现状阻断项与最小修复顺序。重点识别并记录门禁命令语义缺陷（多处 `grep` 未启用扩展正则导致 `|/?` 失效）、feature flag 验签“三选一”与实际命令冲突、P3e“全部移除”与“例外保留”口径冲突等高风险问题。
- PartyFilter 无主体列模型回归修复（2026-02-22）：修复 `backend/src/crud/query_builder.py` 中 `_apply_party_filter` 在模型不存在 `owner_party_id/manager_party_id/party_id/organization_id` 时错误 fail-closed（`WHERE false`）的问题，改为记录信息日志并跳过主体过滤，恢复 `AssetCustomField` 等非主体作用域模型的正常读取；`party_ids` 为空场景仍保持 fail-closed。同步更新回归测试 `backend/tests/unit/crud/test_query_builder.py`（`test_build_query_skips_party_filter_when_model_has_no_party_column`）覆盖该行为，防止再次回归。
- Phase 2 审阅报告清理（2026-02-22）：按“复核无用后删除”要求检查 `docs/reviews/2026-02-22-phase2-plan-review.md` 的仓库引用，确认仅存在于 `CHANGELOG.md` 历史记录且无功能依赖后，已删除该文件；保留本日志作为审计轨迹。
- Phase 3 实施计划全量复核收口（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.42。1) 将 `services/projectService` 段落中“`project_to_response()` 空数组问题”统一改为 `ProjectResponse.coerce_project_model()`，避免后端修复定位偏差；2) 删除 `§6.2` 最小权限 E2E 的“未打 tag 可全量兜底”描述，冻结 `@authz-minimal` 为 P3d Exit 强门禁；3) 统一 `§4.6` 与 `§5.1 P3e` 的 `PERMISSIONS/PAGE_PERMISSIONS` 盘点命令口径（默认排除测试目录，测试侧单独记录）；4) 将 P3d Entry 的 `constants/routes` 派生依赖审计范围从 `App.tsx + routes` 扩展到 `frontend/src`（排除测试目录）；5) 将 `PHASE3_GREP_EXCLUDES` 中 `organizationService/ownershipService` 从宽匹配改为精确文件名；6) 在文档历史新增 v1.42 记录并同步页首“最后更新”说明。
- Phase 3 实施计划执行口径再收敛（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.41。1) `§1` Phase 2-patch 工单表新增“状态”列（`Open/In Progress/Done/Re-verify`）并明确每日更新要求；2) 修正 `§4.4.2 D5`，将 `AuthGuard` 从“仅 deprecated”升级为“P3d 必须迁移为 `useCapabilities` 兼容壳或纳入 P3e 物理删除”；3) `P3a Exit` 的 `types/index.ts` 导出校验改为自动化 grep；4) `P3b Entry` ABAC 覆盖矩阵补齐 `ledger.read`，并将 `party.read` 的非管理员 capabilities spot-check 从建议升级为必做；5) 修正文案中 `project_to_response()` 定位偏差为 `ProjectResponse.coerce_project_model()`；6) `P3d Entry` 增补 `constants/routes` 派生导出依赖审计，并将 `request_id` 门禁升级为“代码 grep + 跨域响应头动态验证”双证据；7) `P3d Exit` 新增 `@authz-minimal` 用例落地门禁并取消“无标签跑全量 e2e”降级；8) `P3e Entry` 的 `PERMISSIONS/PermissionGuard` 盘点命令默认排除测试目录，测试侧改为单列记录。
- Phase 2 审阅报告收口更新（2026-02-22）：重写 `docs/reviews/2026-02-22-phase2-plan-review.md` 为“收口版”，从“问题清单”切换为“闭环矩阵 + 观察项追踪”。本次按 S/A/B/C 分级逐条标注 `已闭环/观察项` 并补充证据行位；确认 `BG1_PASS`、`BG2_NON_DEPRECATED=0`、`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`，`B6_NON_DEPRECATED=0`。同时落地两项收口：1) 6 个 CRUD/Service 文件的 `organization_id/default_organization_id` 兼容路径已补显式 `DEPRECATED` 标注；2) 设计文档 `docs/plans/2026-02-16-party-role-architecture-design.md` 已回写依赖口径为 `python-jsonlogic>=0.1.0`，并同步更新 BG-1/BG-3 与 whitelist 处置描述。当前仅保留 C3/C5 观察项，可按需要继续收敛。
- Phase 2 计划文档执行对齐（2026-02-22）：更新 `docs/plans/2026-02-19-phase2-implementation-plan.md` 至 v1.14，修复计划与代码状态偏差。1) 将 Phase 2 状态从“计划中”更新为“已完成（代码就绪）”，并补充 P2a-P2d 执行快照；2) 将已存在实现从 `[NEW]` 对齐为 `[EXISTING]`（含 `asset_management_history`、`project_asset`、`services/authz/{cache,events,data_policy_service}.py`、`api/v1/auth/data_policies.py`、`scripts/migration/party_migration/*`）；3) 修正文档中不存在的测试路径为当前仓库实际路径（`test_query_builder.py`、`test_field_whitelist.py`、`test_authz_events.py` 等）；4) 修订 `field_whitelist` 口径为“`OrganizationWhitelist` 已删除、`OwnershipWhitelist` deprecated 保留”；5) 将 §8 文件影响统计更新为 2026-02-22 快照口径（源码约 `~26` 新增 / `~130+` 修改，测试修改 `60+`）；6) 补充 B 级对齐：`PartyFilter.party_ids` 类型改为 `list[str | int]`、新增 `_apply_party_filter` 三段 fallback 说明、补齐 BG/设计文档对齐说明及 `organization_id` 兼容桥接 deprecated 约束。同步复核 BG：`BG1_PASS`、`BG2_NON_DEPRECATED=0`、`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Phase 2 评审项标注补齐（2026-02-22）：按 `docs/reviews/2026-02-22-phase2-plan-review.md` 的 A1/A2/A4/B7/C2 补齐 deprecated 标注，不改业务行为。更新 `backend/src/services/rent_contract/statistics_service.py` 与 `backend/src/crud/rent_contract.py` 的 legacy `ownership_id(s)` bridge 行级注释，补充 `backend/src/crud/field_whitelist.py` 中 `OwnershipWhitelist` 的 `DEPRECATED` 说明，补充 `backend/src/schemas/property_certificate.py` 的 4 处 `organization_id` deprecated 描述、`backend/src/schemas/project.py` 的 `ownership_ids` deprecated 标题与 `backend/src/schemas/rent_contract.py` 的 `validate_ownership_id` deprecated 注解。验证：BG-4 grep 复核通过（non-deprecated `ownership_id|ownership_ids` 命中为 0），`cd backend && ./.venv/bin/ruff check src/services/rent_contract/statistics_service.py src/crud/rent_contract.py src/crud/field_whitelist.py src/schemas/property_certificate.py src/schemas/project.py src/schemas/rent_contract.py` 通过。
- Phase 2 计划核查报告勘误（2026-02-22）：修订 `docs/reviews/2026-02-22-phase2-plan-review.md` 的两处事实性偏差并补齐复核口径说明。1) A2 更正为仅 `OwnershipWhitelist` 未删/未标 deprecated，`PropertyOwnerWhitelist` 已标注 deprecated；2) A3 更正为 `AssetBase` 校验为 `owner_party_id/ownership_id` 至少其一，而非“强制 ownership_id 非空”；3) 同步更新汇总与建议优先级，避免按错误结论执行整改。
- ABAC 策略包作用域条件修复（2026-02-22）：修复 `backend/alembic/versions/20260219_phase2_seed_data_policy_packages.py` 与 `backend/alembic/versions/20260221_backfill_expanded_policy_package_rules.py` 将规则条件写成恒真表达式 `{"==":[1,1]}` 的问题；现按策略包写入作用域感知 JSONLogic 条件（`asset_owner_operator`/`asset_manager_operator`/`dual_party_viewer`/`project_manager_operator`/`audit_viewer` 使用 owner/manager 维度条件，`platform_admin` 与 `no_data_access` 保持全局恒真语义）。同步补充迁移单测 `backend/tests/unit/migration/test_policy_package_seed_actions.py`，新增“非全局包禁止恒真条件”与“条件必须引用 party scope 变量”断言。验证：`cd backend && ./.venv/bin/ruff check alembic/versions/20260219_phase2_seed_data_policy_packages.py alembic/versions/20260221_backfill_expanded_policy_package_rules.py tests/unit/migration/test_policy_package_seed_actions.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/migration/test_policy_package_seed_actions.py -q`（6 passed）。
- Phase 3 实施计划审阅回写（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.40，按审阅结论完成执行口径收口。1) 新增并统一 `DB_PSQL_CMD`，替换 P3a/P3b/P3d/P3e 裸 `psql` 门禁；2) 在 Phase 2-patch 增补 `CORS expose_headers(X-Request-ID/Request-ID)` 前置，并在 P3d Entry 加入可观测性门禁；3) 在 P3e Entry/§4.6 补齐 `PERMISSIONS/PAGE_PERMISSIONS` 消费者盘点与 `PermissionGuard` 依赖链处置命令；4) 将 §4.4.1 门禁 A 从 `find | xargs` 改为直接 `grep -El` 文件列表；5) 在 `renderProtectedElement` 伪代码补齐 D8 feature flag 分支（flag off 仅认证守卫）；6) 修订 `ownership_entity` 统计口径并明确 `mocks` 为 MSW 开发/测试链（保留一次性审计门禁）。
- Phase 2 阻断项修复（2026-02-22）：完成后端阶段2未提交代码的关键收口。1) `backend/src/api/v1/system/tasks.py` 的 Excel 配置端点全部从 `require_permission("excel_config", ...)` 迁移为 `get_current_active_user + require_authz(...)`（`resource_type="excel_config"`，动作分别为 `create/read/update/delete`），并同步更新 `backend/tests/unit/api/v1/test_tasks_layering.py` 断言；2) 放宽 deprecated 字段强校验：`backend/src/schemas/rent_contract.py` 中 `RentContractCreate.ownership_id` 改为可选别名（保留非空校验），`backend/src/schemas/asset.py` 改为 `owner_party_id` 与 `ownership_id` 二选一校验；3) 去除 `backend/src/services/project/service.py` 中 `ownership_relations=[]` 硬编码；4) 强化 `backend/src/crud/query_builder.py` 严格模式为“无可过滤列即 fail-closed”，并同步 `backend/tests/unit/crud/test_query_builder.py`；5) 调整 `backend/tests/unit/schemas/test_asset_schema_attachments.py` 以覆盖 `owner_party_id` 主路径兼容行为。
- Phase 3 实施计划复核补强（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.39，修复两项残留执行歧义：1) 将 `phase3d-authz-freeze.md` 与 `docs/release/evidence/capability-guard-env.md` 的模板要求从“说明性约束”升级为 Entry 命令级强校验（必填节逐条命中）；2) 为 `tenant_party_id` 收口门禁补齐占比计算口径（`null_count/total_count`）、公式 `round((null_count * 100.0 / nullif(total_count,0))::numeric, 2)`、单位与统计时点（执行当日 T0 快照），避免跨人执行时阈值判定不一致。
- Phase 3 实施计划阻断项回写（2026-02-22）：更新 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.38，按最新复核结论补齐执行硬门禁与语义冻结：1) 将 `project.party_relations[]` 从“P3b 期内”上收为 `P3b Entry` 硬门禁；2) 明确 `AuthStorage.capabilities*` 为 P3d 新增字段并冻结 `capabilities_version` 仅作协议兼容观测；3) 明确 `usePermission` 兼容壳执行口径为“导出 API 兼容，不保留旧动作词字面量”，门禁 A 继续扫描该文件；4) 在 P3d Entry 增补 `phase3d-authz-freeze.md` 与 `docs/release/evidence/capability-guard-env.md` 的最小模板要求；5) 将 `tenant_party_id` 收紧前处置改为量化阈值门禁（0 / 白名单阈值 / 强制回填）。
- Phase 3 计划审阅报告复核修订（2026-02-22）：更新 `docs/review/2026-02-22-phase3-plan-review.md` 为 `v1.36` 对齐版，修正原报告中已失效或不准确结论（含 `services/asset/types.ts` 遗漏、`assetFormSchema` 统计遗漏、`.github/workflows` 不存在、`Omit` 类型误判、§10 行数噪音等），并保留/重排当前仍成立的高优先级阻断项（`AuthStorage` capabilities 未落地、`project.party_relations[]` 后端契约缺失、`tenant_party_id` 收紧与后端可空冲突、P3d feature flag 验签证据链缺失、`phase3d-authz-freeze.md` 模板缺失、门禁 A 与 `usePermission` 兼容壳张力），补充了对应文件级证据与建议执行顺序。
- Review 问题修复（2026-02-22）：修复 `backend/src/services/project/service.py` 中 `ProjectService._resolve_party_filter` 仅依赖旧 `OrganizationPermissionService` 的回归，现优先从 `user_party_bindings`（`party_crud.get_user_bindings(active_only=True)`）解析主体范围，并与旧组织范围做兼容并集，确保 party-only 用户在 `search_projects`/`get_project_by_id` 场景可按 `manager_party_id` 正确命中；同时修复 `backend/src/services/authz/cache.py` 中 `AuthzDecisionCache.set_l1` 未在写入路径清理过期键的问题，新增写入前过期淘汰，避免高基数授权判定场景下 L1 缓存键无限增长。补充回归测试：`backend/tests/unit/services/project/test_project_service.py` 新增 party binding 解析用例，`backend/tests/unit/services/test_authz_cache.py` 新增“insert 时淘汰过期项”用例。验证：`cd backend && ./.venv/bin/ruff check src/services/project/service.py src/services/authz/cache.py tests/unit/services/project/test_project_service.py tests/unit/services/test_authz_cache.py`（All checks passed）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/services/project/test_project_service.py tests/unit/services/test_authz_cache.py -q`（32 passed）。
- 租赁合同 ABAC/RBAC 冲突修复（2026-02-22）：修复 `backend/src/api/v1/rent_contracts/contracts.py` 中 `update_contract` 与 `delete_contract` 在接入 `require_authz(...)` 后仍保留旧 RBAC 二次拦截的问题；已移除 `can_edit_contract` 与 `RBACService.is_admin` 旧权限链路，避免 ABAC 已放行但被遗留 RBAC 误拒绝。同步调整 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`，将旧“必须 RBAC 拦截”断言更新为“端点函数体不得再出现旧权限守卫”断言。验证：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_contracts_layering.py -k "legacy_contract_edit_guard or legacy_admin_guard" -q`（2 passed）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_api.py -k "should_not_use_legacy_can_edit_guard or should_not_use_legacy_admin_guard or test_update_contract_success or test_delete_contract_success_admin" -q`（4 passed）；`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/contracts.py tests/unit/api/v1/test_rent_contract_contracts_layering.py`（All checks passed）。
- 租赁合同 Schema 乱码专项修复（2026-02-22）：完成 `backend/src/schemas/rent_contract.py` 全量乱码文案清理，统一修复类注释、字段 `description`、校验器说明与分页/统计/台账响应说明文本，消除历史编码错乱导致的 OpenAPI 可读性问题；本次未调整字段结构与校验逻辑，仅修正文案。验证：`ruff check backend/src/schemas/rent_contract.py` 通过，`pytest --no-cov tests/unit/services/rent_contract/test_rent_contract_service_impl.py -q`（13 passed）。
- Phase 3 实施计划门禁纠偏（2026-02-22）：修订 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.33，重点修复执行阻断与口径冲突：1) 解除 P3d Entry/Exit 互斥（Entry 不再依赖尚未创建文件，`usePermission` 改为主链零引用 + Router A/B 处置）；2) 统一 403 结构化拒绝口径为 `error.code=PERMISSION_DENIED`（兼容 `error_code=AUTHZ_DENIED`）；3) 补齐 `request_id` 跨域可观测前提（CORS `expose_headers`）；4) 生产 flag 验签改为“`.env.production` 或 CI/CD 注入”二选一且禁止隐式默认；5) 统一排除单源清单（Bash/PowerShell）并修正中文旧列名扫描为零命中门禁；6) 回滚步骤新增工作区保护；7) D10 互斥门禁改为 AST 脚本入口并已新增实现：`frontend/scripts/check-route-authz-mutual-exclusive.mjs`。
- Phase 3 实施计划门禁可靠性修订（2026-02-22）：修订 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.34，继续收敛执行风险：1) 修复 P3d Entry 两条 grep 命令失真并改为冻结记录文件 `docs/plans/execution/phase3d-authz-freeze.md`，消除 Entry 循环依赖；2) `/403` 门禁改为“显式 `/403` 跳转或 `InlineForbidden/route.fallback`”双分支，移除宽泛 `fallback` 误报；3) 权限单源门禁增加 `test -f` 存在性断言，避免 `! grep` 缺文件反向通过；4) feature flag 验签新增部署平台注入证据口径（`docs/release/evidence/capability-guard-env.md`）并同步 PowerShell；5) 6.1 标准门禁补齐 `pnpm check:route-authz` 并与 `package.json:check` 对齐；6) P3e 增补 `tenant_party_id` 收口门禁；7) `PHASE3_GREP_EXCLUDES` 将 `*systemService*` 收窄为 `systemService.ts`；8) 回滚步骤改为前端目录优先定向回滚并补充回滚后路由门禁校验。
- Phase 3 实施计划复核补丁（2026-02-22）：修订 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.35，修复两项残留口径问题：1) P3d Entry“权限单源”门禁不再硬依赖 `frontend/src/hooks/useCapabilities.ts` 文件存在，改为“文件存在时附加扫描”，避免阶段内 Entry 循环依赖；2) D9 feature flag 验签描述从“二选一”统一为“env/CI/部署证据三选一”，并将部署平台注入证据规范到 `docs/release/evidence/capability-guard-env.md`。
- Phase 3 实施计划阻断项修订（2026-02-22）：修订 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.36，完成执行口径纠偏：1) 调整依赖为 `P3d <- P2d + P3b`、`P3e <- P3c + P3d`，消除 P3c/P3d 串行冲突；2) 将 P3d Entry 中 `/403`、权限单源、feature flag 从“代码落地前置”改为“冻结决策前置”，把实现验证下沉至 P3d Exit；3) 统一 `project` 旧新关系转换职责到后端兼容层，前端固定仅消费/提交 `party_relations[]`；4) 补齐产权证契约前置（二选一冻结：`party_relations[]` 或 `owners[]` 兼容）并明确页面 `party_relations ?? owners` 双读口径；5) 增补策略包 seed revision 执行门禁与非管理员 SQL 过滤收紧；6) 强化 `ROUTE_CONFIG` 废弃校验、D9 验签证据字段、P3e Excel 旧字段扫描（补 `ownership_entity`）；7) 修正路由守卫伪代码渲染方式与现网 `<route.element />` 语义一致。
- Phase 3 实施计划门禁执行性强化（2026-02-22）：修订 `docs/plans/2026-02-20-phase3-implementation-plan.md` 至 v1.37，继续收紧可执行口径：1) 修复 P3d Entry seed 门禁命令中的重复 `cd backend` 错误；2) 将 seed 门禁从“迁移文件存在”升级为“迁移历史命中 + `abac_policy_rules seed_rule_%` SQL spot-check”双证据，降低误通过；3) 收紧 `ROUTE_CONFIG` 门禁为仅允许 `frontend/src/constants/routes.ts` 定义位点命中，防止 basename 级排除误放行；4) 在 P3d Exit 增补 D8 双态验收（flag on 生效 + flag off 熔断）；5) 重写 §6.3 PowerShell 核心门禁为显式断言（命中缺失/命中禁项均直接 `throw`），消除“仅打印不阻断”风险。
- Phase 3 门禁自动化接线（2026-02-22）：将 D10 AST 门禁正式接入自动化。新增 `frontend/package.json` 脚本 `check:route-authz`（执行 `scripts/check-route-authz-mutual-exclusive.mjs`），并纳入 `pnpm check`；在 `.github/workflows/ci.yml` 的 `frontend-lint` 阶段增加阻断步骤 `pnpm check:route-authz`。同时收紧脚本行为：`protectedRoutes` 中出现非对象字面量条目时改为 fail-closed（直接失败并输出索引与表达式），避免以非常规写法绕过 D10 校验。
- 乱码文本排查与修复（2026-02-22）：针对同类编码异常开展扫描并修复高风险文本。已修复 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py` 中多处测试数据乱码（如资产名、主体名、历史权属名、备注与解约原因），修复 `backend/src/services/rent_contract/statistics_service.py` 中统计服务注释/说明乱码，修复 `backend/src/services/analytics/analytics_service.py` 缓存读写告警乱码文案，以及 `backend/src/services/asset/__init__.py` 模块说明中的替换符异常。验证：`pytest --no-cov tests/unit/services/rent_contract/test_rent_contract_service_impl.py -q`（13 passed）与 `ruff check` 定向检查通过。
- 租赁合同服务单测断言修复（2026-02-22）：修复 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py` 中两个异常匹配断言与实际中文错误消息不一致的问题。`test_create_contract_async_raises_conflict` 的 `match` 从英文 `conflict` 调整为 `资产租金冲突检测`，`test_generate_monthly_ledger_async_requires_rent_terms` 的乱码匹配串调整为 `合同没有租金条款`，恢复用例与服务层异常文案一致性。
- Review 修复（2026-02-22）：修复 `backend/src/scripts/migration/party_migration/backfill_role_policies.py` 中将 `is_system_role=True` 统一提升为 `platform_admin` 的回填逻辑，改为仅基于显式角色信号映射策略包，避免非管理员系统角色被提权；补充回归断言于 `backend/tests/unit/migration/test_backfill.py`。同时恢复 `backend/src/schemas/rent_contract.py` 中 `start_date`、`end_date`、`monthly_rent` 三个字段描述为可读 UTF-8 中文，修复 OpenAPI 文档可读性回归。

- 测试库 Alembic 升级阻塞修复（2026-02-20）：修复 `backend/alembic/versions/20260219_phase2_seed_data_policy_packages.py` 在写入 `abac_policies.effect` 与 `abac_policy_rules.action` 时将参数绑定为 `VARCHAR` 导致 PostgreSQL enum 列类型不匹配（`DatatypeMismatch`）的问题。现将 seed 迁移中的两列绑定改为 `postgresql.ENUM(..., create_type=False)`，恢复 `alembic upgrade head` 在测试库重建场景下的可执行性。
- LLM Prompt 回退建表缺表修复（2026-02-20）：修复 `backend/src/models/__init__.py` 未导入 `llm_prompt` 模型导致单测数据库在 Alembic 失败回退 `Base.metadata.create_all()` 时遗漏 `prompt_templates`/`prompt_versions`/`extraction_feedback`/`prompt_metrics` 的问题。现已补齐 `PromptTemplate`、`PromptVersion`、`ExtractionFeedback`、`PromptMetrics`、`PromptStatus` 的统一导入与导出，确保 `tests/unit/api/v1/test_llm_prompts.py` 在回退建表路径可完整初始化。
- 产权证关系枚举建表一致性修复（2026-02-20）：修复 `backend/src/models/certificate_party_relation.py` 中 `SQLEnum(CertificateRelationRole)` 默认使用枚举名称（`OWNER`）导致与索引条件中小写值（`'owner'`）不一致的问题。现为 `relation_role` 显式配置 `values_callable` 使用枚举 `.value`，确保 `create_all` 建出的 `certificate_relation_role` 与迁移/索引表达式一致，避免 db-backed unit 在 fallback 建表阶段触发 `invalid input value for enum certificate_relation_role: "owner"`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-组织管理路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/auth/organization.py` 为组织管理关键端点接入统一 `require_authz(...)`，覆盖组织列表/树/搜索/统计/详情/子级/路径/历史及 create/update/delete/move/batch/advanced-search；2) 统一鉴权约束：动作映射覆盖 `read/create/update/delete`，资源类型统一为 `asset`，并为 `org_id` 路径端点补齐 `resource_id` 映射（详情读取端点启用 `deny_as_not_found=True`）；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_organization_layering.py`，覆盖模块引入约束与 14 个关键端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐 `0.x` 状态快照、`1.51` 进展条目、`2.51` 验证记录并从未完成项移除 `auth/organization.py`。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/auth/organization.py tests/unit/api/v1/test_organization_layering.py tests/unit/api/v1/test_organization.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_organization_layering.py tests/unit/api/v1/test_organization.py tests/unit/middleware/test_authz_dependency.py -q`（`24 passed`）；Gate 盘点：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-角色管理路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/auth/roles.py` 为角色管理与统一授权关键端点接入统一 `require_authz(...)`，覆盖角色 CRUD、用户角色分配、权限汇总、统一授权记录、权限列表与统计端点；2) 统一鉴权约束：动作映射覆盖 `read/create/update/delete`，资源类型统一为 `asset`，并为 `user_id/grant_id/role_id` 路径端点补齐 `resource_id` 映射（详情读取端点启用 `deny_as_not_found=True`）；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_roles_layering.py`，覆盖模块引入约束与 19 个关键端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐 `0.x` 状态快照、`1.50` 进展条目、`2.50` 验证记录并从未完成项移除 `auth/roles.py`。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/auth/roles.py tests/unit/api/v1/test_roles_layering.py tests/unit/api/v1/test_roles.py tests/unit/api/v1/test_roles_permission_grants.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_roles_layering.py tests/unit/api/v1/test_roles.py tests/unit/api/v1/test_roles_permission_grants.py tests/unit/middleware/test_authz_dependency.py -q`（`23 passed`）；Gate 盘点：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-系统设置路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/system_settings.py` 为系统设置关键端点接入统一 `require_authz(...)`，覆盖 `security/alerts/test/security/events/settings/info/backup/restore`；2) 统一鉴权约束：动作映射为 `update/read/read/update/read/create/update`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_system_settings_layering.py`，新增模块引入约束与 7 个关键端点 ABAC 依赖注入断言并保留既有服务委托断言；4) 更新交接文档 `docs/todo.md`，补齐 `0.x` 状态快照、`1.49` 进展条目、`2.49` 验证记录并从未完成项移除 `system/system_settings.py`。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/system_settings.py tests/unit/api/v1/test_system_settings_layering.py tests/unit/api/v1/test_system_settings.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_system_settings_layering.py tests/unit/api/v1/test_system_settings.py tests/unit/middleware/test_authz_dependency.py -q`（`17 passed`）；Gate 盘点：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-系统监控路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/monitoring.py` 为系统监控关键端点接入统一 `require_authz(...)`，覆盖 `route-performance/system-health/performance/dashboard/system-metrics/application-metrics/dashboard/metrics/collect`；2) 统一鉴权约束：动作映射为 `create/read/read/read/read/read/update`，资源类型统一为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_monitoring_layering.py`，覆盖模块引入约束与 7 个关键端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐 `0.x` 状态快照、`1.48` 进展条目、`2.48` 验证记录并从未完成项移除 `system/monitoring.py`。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/monitoring.py tests/unit/api/v1/test_monitoring_layering.py tests/unit/api/v1/test_monitoring.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_monitoring_layering.py tests/unit/api/v1/test_monitoring.py tests/unit/middleware/test_authz_dependency.py -q`（`30 passed`）；Gate 盘点：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-错误恢复路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/error_recovery.py` 为关键端点接入统一 `require_authz(...)`，覆盖 `statistics/strategies/update/circuit-breakers/reset/history/test/clear`；2) 统一鉴权约束：动作映射为 `read/read/update/read/update/read/update/delete`，资源类型统一为 `asset`，并为按 `category` 路径参数端点补齐 `resource_id="{category}"`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_error_recovery_layering.py`，覆盖模块引入约束与 8 个关键端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐 `0.x` 状态快照、`1.47` 进展条目、`2.47` 验证记录与未完成项清单。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/error_recovery.py tests/unit/api/v1/test_error_recovery_layering.py tests/unit/api/v1/test_error_recovery.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_error_recovery_layering.py tests/unit/api/v1/test_error_recovery.py tests/unit/middleware/test_authz_dependency.py -q`（`49 passed`）；Gate 盘点：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Unit 测试数据库初始化稳态修复（2026-02-20）：修复 `backend/tests/unit/conftest.py` 在测试库被历史半迁移状态污染时引发 Alembic 冲突的问题。`db_tables` 现对 PostgreSQL 在 session 启动时先重建 `public` schema，确保从干净状态执行迁移；若 `alembic upgrade head` 仍失败（分支在研迁移冲突），则再次重建 schema 并回退到 `Base.metadata.create_all()`，避免出现“版本号前进但列缺失”的半初始化库，恢复 `tests/unit/api/v1/test_llm_prompts.py` 等 db-backed unit 可执行性。
- 单测路径识别修复（2026-02-20）：修复 `backend/tests/conftest.py` 中 `setup_test_database` 对 unit 路径的识别过窄问题，新增 `tests/unit/*` 与 `tests\\unit\\*` 前缀匹配。效果：`cd backend && pytest tests/unit/...` 不再误触发会话级迁移预热，避免与 `tests/unit/conftest.py` 的 Alembic/建表流程叠加导致 `DuplicateTable` 干扰。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-LLM Prompt 路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/llm_prompts.py` 为 Prompt 管理关键端点统一接入 `require_authz(...)`，覆盖 `create/list/detail/update/activate/rollback/versions/statistics/feedback`；2) 统一鉴权约束：动作映射为 `create/read/read/update/update/update/read/read/create`，资源类型统一为 `asset`，并为按 `prompt_id` 的读取端点补齐 `resource_id="{prompt_id}" + deny_as_not_found=True`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_llm_prompts_layering.py`，覆盖模块引入约束与 9 个关键端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐 `0.x` 状态快照、`1.46` 进展条目、`2.46` 验证记录与未完成项清单。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/llm_prompts.py tests/unit/api/v1/test_llm_prompts_layering.py tests/unit/api/v1/test_llm_prompts.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_llm_prompts_layering.py tests/unit/middleware/test_authz_dependency.py -q`（`5 passed`）；Gate 盘点：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。补充说明：`tests/unit/api/v1/test_llm_prompts.py` 在当前测试库环境会触发 Alembic 初始化冲突（`organizations already exists`），与本批 ABAC 依赖注入改动无关。
- 本地测试库配置落地复核（2026-02-20）：在当前工作区补齐 `backend/.env`（本地文件，未纳入版本控制），写入 `DATABASE_URL`、`TEST_DATABASE_URL`、`INTEGRATION_TEST_DATABASE_URL`、`E2E_TEST_DATABASE_URL` 指向 `127.0.0.1:5432` 的 Docker PostgreSQL（`zcgl-postgres`），并生成本地 `SECRET_KEY`/`DATA_ENCRYPTION_KEY`。复核结果：`backend/conftest.py` 可从 `.env` 解析 `TEST_DATABASE_URL` 并写回 `os.environ`；连通性验证 `SELECT 1` 成功；db-backed 用例验证通过：`tests/security/test_phase2_task2_security_logging.py::TestPermissionDeniedLogging::test_log_permission_denied_creates_security_event`、`tests/unit/api/v1/test_contact.py::TestCreateContact::test_create_contact_success`、`tests/integration/api/test_system_info_real.py::test_system_info_real_flow`。
- 测试库回退建表稳定性修复（2026-02-20）：修复 `backend/tests/conftest.py` 在 Alembic 失败后 fallback `Base.metadata.create_all()` 未显式加载全部模型的问题；现于两处分支补充 `import src.models`，避免回退建表出现“表不全/列缺失”的假故障。
- 本地测试环境配置（2026-02-20）：新增 `backend/.env` 本地开发配置，统一写入 `DATABASE_URL`、`TEST_DATABASE_URL`、`INTEGRATION_TEST_DATABASE_URL`、`E2E_TEST_DATABASE_URL` 指向本机 Docker PostgreSQL（`127.0.0.1:5432`），并补充本地 `SECRET_KEY` 与 `DATA_ENCRYPTION_KEY`。已验证 `TEST_DATABASE_URL` 连通性（`SELECT 1` 成功）。
- 测试环境变量链路修复（2026-02-20）：修复 `TEST_DATABASE_URL` 仅在 shell 导出时生效、导致部分 db-backed 用例（如 `tests/security`）被错误跳过的问题。`backend/conftest.py` 与 `backend/tests/conftest.py` 现统一支持“环境变量优先，`backend/.env` 回读兜底”，并在解析成功后显式写回 `os.environ["TEST_DATABASE_URL"]` 与 `os.environ["DATABASE_URL"]`，确保根/子 `conftest` 夹具链路一致启用测试库配置。复现场景与验证：当仅配置 `backend/.env` 时，`tests/security` 不再报 `TEST_DATABASE_URL is required for database tests`，而会进入真实连接校验路径（若数据库不可达则报 `unreachable`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-统一字典路由批次补录）：补齐 `dictionaries` 批次遗漏的交接文档与变更日志。核对并确认 `backend/src/api/v1/system/dictionaries.py` 已为统一字典关键端点接入 `require_authz(...)`（`GET /system/dictionaries/{dict_type}/options`、`POST /system/dictionaries/{dict_type}/quick-create`、`GET /system/dictionaries/types`、`GET /system/dictionaries/validation/stats`、`POST /system/dictionaries/{dict_type}/values`、`DELETE /system/dictionaries/{dict_type}`），动作映射 `read/create/read/read/create/delete`、资源类型统一 `asset`，并在选项读取端点启用 `deny_as_not_found=True`；分层约束测试 `backend/tests/unit/api/v1/test_dictionaries_layering.py` 覆盖“路由不直连 CRUD + 6 个关键端点 ABAC 依赖注入断言”。同步更新 `docs/todo.md`，补充 `0.x` 状态快照、`1.40` 进展条目与 `2.40` 验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/dictionaries.py tests/unit/api/v1/test_dictionaries_layering.py tests/unit/api/v1/test_dictionaries.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_dictionaries_layering.py tests/unit/api/v1/test_dictionaries.py tests/unit/middleware/test_authz_dependency.py -q`（`5 passed, 8 skipped`，`test_dictionaries.py` 因 `TEST_DATABASE_URL` 缺失按预期跳过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-通知路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/notifications.py` 为通知关键端点接入统一 `require_authz(...)`，覆盖 `GET /notifications`、`GET /notifications/unread-count`、`POST /notifications/{notification_id}/read`、`POST /notifications/read-all`、`DELETE /notifications/{notification_id}`、`POST /notifications/run-tasks`；2) 统一鉴权约束：动作映射为 `read/read/update/update/delete/update`，资源类型统一为 `asset`，并为按 `notification_id` 端点补齐 `resource_id` 映射；3) 保持手动触发任务端点的既有管理员语义检查（`RBACService.is_admin`）不变；4) 新增分层约束测试 `backend/tests/unit/api/v1/test_notifications_layering.py`，覆盖模块引入约束、6 个关键端点 ABAC 依赖注入断言与路由不直连 CRUD 断言；5) 更新交接文档 `docs/todo.md`，补齐 `1.41/2.41` 记录与“最新完成批次”。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/notifications.py tests/unit/api/v1/test_notifications_layering.py tests/unit/api/v1/test_notifications.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_notifications_layering.py tests/unit/api/v1/test_notifications.py tests/unit/middleware/test_authz_dependency.py -q`（`8 passed, 19 skipped`，`test_notifications.py` 的 db-backed 用例因 `TEST_DATABASE_URL` 认证失败按预期跳过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-操作日志路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/operation_logs.py` 为关键端点接入统一 `require_authz(...)`，覆盖 `GET /logs`、`GET /logs/{log_id}`、`GET /logs/statistics/user`、`GET /logs/statistics/module`、`GET /logs/statistics/daily`、`GET /logs/statistics/errors`、`GET /logs/statistics/summary`、`POST /logs/export`、`POST /logs/cleanup`；2) 统一鉴权约束：动作映射为 `read/read/read/read/read/read/read/read/delete`，资源类型统一为 `asset`，并为详情端点补齐 `resource_id="{log_id}" + deny_as_not_found=True`；3) 保持 `errors/export/cleanup` 端点原有管理员语义检查不变；4) 扩展分层约束测试 `backend/tests/unit/api/v1/test_operation_logs_layering.py`，新增 9 个关键端点 ABAC 依赖注入断言并保持“路由不直连 CRUD + 服务委托”断言通过；5) 更新交接文档 `docs/todo.md`，补齐 `1.42/2.42` 记录与“最新完成批次”。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/operation_logs.py tests/unit/api/v1/test_operation_logs_layering.py tests/unit/api/v1/test_operation_logs.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_operation_logs_layering.py tests/unit/api/v1/test_operation_logs.py tests/unit/middleware/test_authz_dependency.py -q`（`8 passed, 4 skipped`，`test_operation_logs.py` 的 db-backed 用例因 `TEST_DATABASE_URL` 缺失按预期跳过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-枚举字段路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/enum_field.py` 为关键业务端点统一接入 `require_authz(...)`（debug 端点除外），覆盖 `types/values/tree/usage/history` 全链路；2) 统一鉴权约束：动作映射覆盖 `read/create/update/delete`，资源类型统一为 `asset`，并为按 `type_id/value_id` 读取端点补齐 `resource_id` 与 `deny_as_not_found=True`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_enum_field_layering.py`，新增 20 个关键端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐 `1.43/2.43` 与未完成项完成清单。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/enum_field.py tests/unit/api/v1/test_enum_field_layering.py tests/unit/api/v1/test_enum_field_api.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_enum_field_layering.py tests/unit/api/v1/test_enum_field_api.py tests/unit/middleware/test_authz_dependency.py -q`（`14 passed`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-数据备份路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/backup.py` 为 8 个备份端点接入统一 `require_authz(...)`，覆盖 `create/list/download/restore/delete/stats/validate/cleanup`；2) 统一鉴权约束：动作映射为 `create/read/read/update/delete/read/read/delete`，资源类型统一为 `asset`，下载端点补齐 `resource_id="{backup_name}" + deny_as_not_found=True`；3) 保持原有 `require_admin` 管理员语义不变；4) 新增分层约束测试 `backend/tests/unit/api/v1/test_backup_layering.py`，覆盖模块引入约束、8 个关键端点 ABAC 依赖注入断言与列表端点服务委托断言；5) 更新交接文档 `docs/todo.md`，补齐 `1.44/2.44` 记录并刷新“最新完成批次”。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/backup.py tests/unit/api/v1/test_backup_layering.py tests/unit/api/v1/test_backup.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_backup_layering.py tests/unit/api/v1/test_backup.py tests/unit/middleware/test_authz_dependency.py -q`（`40 passed`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-系统核心路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/system.py` 为系统核心端点接入统一 `require_authz(...)`，覆盖 `GET /monitoring/health`、`GET /system/info`、`GET /system/root`；2) 统一鉴权约束：动作映射均为 `read`，资源类型统一为 `asset`；3) 保留路由级 `get_current_active_user` 语义不变；4) 新增分层约束测试 `backend/tests/unit/api/v1/test_system_layering.py`，覆盖模块引入约束与 3 个核心端点 ABAC 依赖注入断言；5) 更新交接文档 `docs/todo.md`，补齐 `1.45/2.45` 记录并刷新“最新完成批次”。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/system.py tests/unit/api/v1/test_system_layering.py tests/unit/api/v1/test_system.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_system_layering.py tests/unit/middleware/test_authz_dependency.py -q`（`5 passed`）。补充说明：`tests/unit/api/v1/test_system.py` 在当前测试库会触发 Alembic 初始化冲突（`organizations already exists`），与本批 ABAC 改动无关。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-联系人管理路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/contact.py` 为联系人管理关键端点接入统一 `require_authz(...)`，覆盖 `POST /contacts/`、`GET /contacts/{contact_id}`、`GET /contacts/entity/{entity_type}/{entity_id}`、`GET /contacts/entity/{entity_type}/{entity_id}/primary`、`PUT /contacts/{contact_id}`、`DELETE /contacts/{contact_id}`、`POST /contacts/batch/{entity_type}/{entity_id}`；2) 统一鉴权约束：动作映射为 `create/read/read/read/update/delete/create`，资源类型统一为 `asset`，并为按 `contact_id`/`entity_id` 读取端点补齐 `resource_id` 映射与 `deny_as_not_found=True`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_contact_layering.py`，新增 7 个关键端点 ABAC 依赖注入断言并保持既有服务委托断言通过；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/contact.py tests/unit/api/v1/test_contact_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_contact_layering.py tests/unit/api/v1/test_contact.py -q`（`4 passed, 28 skipped`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-催缴管理路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/collection.py` 为催缴管理关键端点接入统一 `require_authz(...)`，覆盖 `GET /collection/summary`、`GET /collection/records`、`GET /collection/records/{record_id}`、`POST /collection/records`、`PUT /collection/records/{record_id}`、`DELETE /collection/records/{record_id}`；2) 统一鉴权约束：动作映射为 `read/read/read/create/update/delete`，资源类型统一为 `asset`，并为详情读取端点补齐 `resource_id="{record_id}" + deny_as_not_found=True`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_collection_layering.py`，覆盖模块引入约束、6 个关键端点 ABAC 依赖注入断言与列表端点服务委托断言；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/collection.py tests/unit/api/v1/test_collection_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_collection_layering.py tests/unit/api/v1/test_collection.py -q`（`30 passed`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-历史记录路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/history.py` 为历史记录关键端点接入统一 `require_authz(...)`，覆盖 `GET /history/`、`GET /history/{history_id}`、`DELETE /history/{history_id}`；2) 统一鉴权约束：动作映射为 `read/read/delete`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_history_layering.py`，新增历史记录端点 ABAC 依赖注入断言并保持既有服务委托断言通过；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/history.py tests/unit/api/v1/test_history_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_history_layering.py tests/unit/api/v1/test_history.py -q`（`5 passed, 4 skipped`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-任务模块主端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/tasks.py` 为任务主端点接入统一 `require_authz(...)`，覆盖 `POST /tasks/`、`GET /tasks`、`GET /tasks/{task_id}`、`PUT /tasks/{task_id}`、`POST /tasks/{task_id}/cancel`、`DELETE /tasks/{task_id}`、`GET /tasks/{task_id}/history`、`GET /tasks/statistics`、`GET /tasks/running`、`GET /tasks/recent`、`GET /tasks/cleanup`；2) 统一鉴权约束：动作映射为 `create/read/read/update/update/delete/read/read/read/read/delete`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_tasks_layering.py`，新增任务主端点 ABAC 依赖注入断言并保持既有服务委托断言通过；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_tasks_layering.py tests/unit/api/v1/test_tasks.py -q`（`56 passed`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-主体管理路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/party.py` 为主体管理关键端点接入统一 `require_authz(...)`，覆盖 `GET/POST /parties`、`GET/PUT/DELETE /parties/{party_id}`、`GET/POST/DELETE /parties/{party_id}/hierarchy`、`GET/POST /parties/{party_id}/contacts`；2) 统一鉴权约束：动作映射为 `read/create/read/update/delete/read/create/delete/read/create`，资源类型统一为 `party`，并为按 `party_id` 读取端点补齐 `resource_id="{party_id}" + deny_as_not_found=True`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_party_layering.py`，覆盖模块引入约束、10 个关键端点 ABAC 依赖注入断言与列表端点服务委托断言；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/party.py tests/unit/api/v1/test_party_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_party_layering.py tests/unit/middleware/test_authz_dependency.py -q`（`7 passed`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-任务模块 Excel 配置端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/system/tasks.py` 为 Excel 配置关键端点接入统一 `require_authz(...)`，覆盖 `POST /tasks/configs/excel`、`GET /tasks/configs/excel`、`GET /tasks/configs/excel/default`、`GET /tasks/configs/excel/{config_id}`、`PUT /tasks/configs/excel/{config_id}`、`DELETE /tasks/configs/excel/{config_id}`；2) 统一鉴权约束：动作映射为 `create/read/read/read/update/delete`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_tasks_layering.py`，新增任务模块 Excel 配置端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/system/tasks.py tests/unit/api/v1/test_tasks_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_tasks_layering.py tests/unit/api/v1/test_tasks.py -k "excel_config or ExcelConfig" -q`（`19 passed, 36 deselected`）。
- Phase 2 交接文档刷新（2026-02-20，续作窗口准备）：按当前进展更新 `docs/todo.md`，补充“0.2 当前窗口已完成（便于新窗口直接接力）”区块，明确本窗口已完成的统计子模块 ABAC 覆盖范围、对应测试补齐清单（layering + TestClient 鉴权隔离）、本批验证结果（`ruff` 通过、`pytest` `48 passed`）以及新窗口三步起手顺序（Gate 盘点 → 单批改造 → 定向验证并回写文档），用于无上下文切换继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-统计子模块剩余端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) 为 `backend/src/api/v1/analytics/statistics_modules/basic_stats.py`、`backend/src/api/v1/analytics/statistics_modules/area_stats.py`、`backend/src/api/v1/analytics/statistics_modules/distribution.py`、`backend/src/api/v1/analytics/statistics_modules/occupancy_stats.py`、`backend/src/api/v1/analytics/statistics_modules/trend_stats.py` 的关键端点统一接入 `require_authz(...)`；2) 动作映射统一为 `read`（`/cache/clear` 映射为 `update`），资源类型统一为 `asset`；3) 扩展并新增分层约束测试 `test_basic_stats_layering.py`、`test_area_stats_layering.py`、`test_distribution_layering.py`、`test_occupancy_stats_layering.py`、`test_trend_stats_layering.py`，覆盖模块引入依赖与端点鉴权注入断言；4) 适配 `test_basic_stats.py`、`test_area_stats.py`、`test_occupancy_stats.py` 的 TestClient ABAC 隔离，在夹具补齐 `authz_service.check_access=allow` patch；5) 更新交接文档 `docs/todo.md` 记录本批进展与验证结果。验证通过：`cd backend && ./.venv/bin/ruff check src/api/v1/analytics/statistics_modules/basic_stats.py src/api/v1/analytics/statistics_modules/area_stats.py src/api/v1/analytics/statistics_modules/distribution.py src/api/v1/analytics/statistics_modules/occupancy_stats.py src/api/v1/analytics/statistics_modules/trend_stats.py tests/unit/api/v1/analytics/statistics_modules/test_basic_stats.py tests/unit/api/v1/analytics/statistics_modules/test_area_stats.py tests/unit/api/v1/analytics/statistics_modules/test_occupancy_stats.py tests/unit/api/v1/analytics/statistics_modules/test_basic_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_area_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_distribution_layering.py tests/unit/api/v1/analytics/statistics_modules/test_occupancy_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_trend_stats_layering.py`（通过）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/analytics/statistics_modules/test_basic_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_area_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_distribution_layering.py tests/unit/api/v1/analytics/statistics_modules/test_occupancy_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_trend_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_basic_stats.py tests/unit/api/v1/analytics/statistics_modules/test_area_stats.py tests/unit/api/v1/analytics/statistics_modules/test_occupancy_stats.py tests/unit/api/v1/analytics/test_statistics.py -q`（`48 passed`）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-财务统计子模块批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/analytics/statistics_modules/financial_stats.py` 为端点 `GET /statistics/financial-summary` 接入统一 `require_authz(...)`；2) 统一鉴权约束：动作映射为 `read`，资源类型统一为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/analytics/statistics_modules/test_financial_stats_layering.py`，覆盖财务汇总端点 ABAC 依赖注入断言；4) 适配 `backend/tests/unit/api/v1/analytics/statistics_modules/test_financial_stats.py` 的 TestClient 用例隔离，在测试夹具补齐 `authz_service.check_access=allow` patch，避免财务统计 API 用例受真实 ABAC 依赖链路干扰；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/analytics/statistics_modules/test_financial_stats_layering.py tests/unit/api/v1/analytics/statistics_modules/test_financial_stats.py tests/unit/middleware/test_authz_dependency.py -q`（`7 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/analytics/statistics_modules/financial_stats.py tests/unit/api/v1/analytics/statistics_modules/test_financial_stats.py tests/unit/api/v1/analytics/statistics_modules/test_financial_stats_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-分析主路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/analytics/analytics.py` 为业务端点接入统一 `require_authz(...)`，覆盖 `GET /comprehensive`、`GET /cache/stats`、`POST /cache/clear`、`GET /trend`、`GET /distribution`、`POST /export`；2) 统一鉴权约束：动作映射为 `read/read/update/read/read/read`，资源类型统一为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/analytics/test_analytics_layering.py`，覆盖分析主路由业务端点 ABAC 依赖注入断言；4) 适配 `backend/tests/unit/api/v1/analytics/test_analytics.py` 与 `backend/tests/unit/api/v1/test_analytics.py` 的 TestClient 用例隔离，在测试夹具补齐 `authz_service.check_access=allow` patch，避免分析 API 用例受真实 ABAC 依赖链路干扰；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/analytics/test_analytics_layering.py tests/unit/api/v1/analytics/test_analytics.py tests/unit/api/v1/test_analytics.py tests/unit/middleware/test_authz_dependency.py -q`（`35 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/analytics/analytics.py tests/unit/api/v1/analytics/test_analytics_layering.py tests/unit/api/v1/analytics/test_analytics.py tests/unit/api/v1/test_analytics.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-产权证列表端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/property_certificate.py` 为列表端点 `GET /property-certificates/` 接入统一 `require_authz(...)`；2) 统一鉴权约束：动作映射为 `read`，资源类型统一为 `property_certificate`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_property_certificate_authz_layering.py`，新增 `list_certificates` 端点 ABAC 依赖注入断言；4) 回归 `backend/tests/unit/api/v1/test_property_certificate.py` 与 `authz_dependency` 基线，确认新增 `_authz_ctx` 依赖未破坏既有认证与接口行为（当前仍有既存 db 条件跳过用例）；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_property_certificate_authz_layering.py tests/unit/api/v1/test_property_certificate.py tests/unit/middleware/test_authz_dependency.py -q`（`6 passed, 20 skipped`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/property_certificate.py tests/unit/api/v1/test_property_certificate_authz_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-资产批量验证端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/asset_batch.py` 为验证端点 `POST /validate` 接入统一 `require_authz(...)`；2) 统一鉴权约束：动作映射为 `read`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_asset_batch_layering.py`，新增 `validate_asset_data` 端点 ABAC 依赖注入断言；4) 回归 `test_asset_batch_layering.py` 与 `authz_dependency` 基线，确认新增 `_authz_ctx` 依赖未破坏既有服务委托断言；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_asset_batch_layering.py tests/unit/middleware/test_authz_dependency.py -q`（`8 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/asset_batch.py tests/unit/api/v1/test_asset_batch_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-资产批量读端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/asset_batch.py` 为读端点接入统一 `require_authz(...)`，覆盖 `GET /all`、`POST /by-ids`；2) 统一鉴权约束：动作映射均为 `read`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_asset_batch_layering.py`，新增 `get_all_assets` 与 `get_assets_by_ids` 端点 ABAC 依赖注入断言；4) 回归 `test_asset_batch_layering.py` 既有服务委托断言，确认新增 `_authz_ctx` 依赖未破坏按 ID 批量读取服务调用链路；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_asset_batch_layering.py tests/unit/middleware/test_authz_dependency.py -q`（`8 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/asset_batch.py tests/unit/api/v1/test_asset_batch_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-PDF 兼容路由批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/pdf_import_routes.py` 为兼容路由关键端点接入统一 `require_authz(...)`，覆盖 `GET /info`、`GET /sessions`、`POST /upload`；2) 统一鉴权约束：动作映射为 `read/read/create`，资源类型统一为 `rent_contract`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_pdf_import_routes_layering.py`，覆盖兼容路由关键端点 ABAC 依赖注入断言；4) 回归 `backend/tests/unit/api/v1/documents/test_pdf_import_routes.py` 关键用例，确认新增 `_authz_ctx` 依赖未破坏主 `/api/v1/pdf-import/*` 链路行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_pdf_import_routes_layering.py tests/unit/api/v1/documents/test_pdf_import_routes.py::TestPDFImportInfo::test_get_pdf_import_info tests/unit/api/v1/documents/test_pdf_import_routes.py::TestPDFImportSessions::test_get_pdf_import_sessions_empty tests/unit/api/v1/documents/test_pdf_import_routes.py::TestPDFUpload::test_upload_pdf_for_import tests/unit/middleware/test_authz_dependency.py -q`（`7 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/pdf_import_routes.py tests/unit/api/v1/test_pdf_import_routes_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-PDF 批处理健康检查批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/pdf_batch_routes.py` 为健康检查端点 `GET /pdf-import/batch/health` 接入统一 `require_authz(...)`；2) 统一鉴权约束：动作映射为 `read`，资源类型统一为 `rent_contract`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py`，新增 `batch_health_check` 端点 ABAC 依赖注入断言；4) 回归 `backend/tests/unit/api/v1/test_pdf_batch_routes.py::TestBatchHealthCheck`，确认新增 `_authz_ctx` 依赖未破坏健康检查返回结构；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_pdf_batch_routes_layering.py tests/unit/api/v1/test_pdf_batch_routes.py::TestBatchHealthCheck tests/unit/middleware/test_authz_dependency.py -q`（`9 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/pdf_batch_routes.py tests/unit/api/v1/test_pdf_batch_routes_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-Excel 配置批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/excel/config.py` 为关键端点接入统一 `require_authz(...)`，覆盖 `POST /configs`、`GET /configs`、`GET /configs/default`、`GET /configs/{config_id}`、`PUT /configs/{config_id}`、`DELETE /configs/{config_id}`；2) 统一鉴权约束：动作映射为 `create/read/read/read/update/delete`，资源类型统一为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_excel_config_layering.py`，覆盖配置创建/列表/默认配置/详情/更新/删除端点 ABAC 依赖注入断言；4) 回归 `test_excel.py` 中 Excel 配置相关用例，确认新增 `_authz_ctx` 依赖未破坏既有配置创建、查询、更新与删除链路；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_excel_config_layering.py tests/unit/api/v1/test_excel.py::TestCreateExcelConfig tests/unit/api/v1/test_excel.py::TestGetExcelConfigs tests/unit/api/v1/test_excel.py::TestGetDefaultExcelConfig tests/unit/api/v1/test_excel.py::TestGetExcelConfigDetails tests/unit/api/v1/test_excel.py::TestUpdateExcelConfig tests/unit/api/v1/test_excel.py::TestDeleteExcelConfig tests/unit/middleware/test_authz_dependency.py -q`（`16 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/excel/config.py tests/unit/api/v1/test_excel_config_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-Excel 导出批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/excel/export_ops.py` 为关键端点接入统一 `require_authz(...)`，覆盖 `GET /export`、`POST /export`、`POST /export/async`、`GET /download/{task_id}`；2) 统一鉴权约束：动作映射均为 `read`，资源类型统一为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_excel_export_ops_layering.py`，覆盖导出/异步导出/下载关键端点 ABAC 依赖注入断言；4) 回归 `test_excel.py` 中导出、异步导出、下载与按资产导出用例，确认新增 `_authz_ctx` 依赖未破坏既有任务创建、文件流下载与异常处理链路；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_excel_export_ops_layering.py tests/unit/api/v1/test_excel.py::TestExportExcel tests/unit/api/v1/test_excel.py::TestExportExcelAsync tests/unit/api/v1/test_excel.py::TestDownloadExportFile tests/unit/api/v1/test_excel.py::TestExportSelectedAssets tests/unit/middleware/test_authz_dependency.py -q`（`17 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/excel/export_ops.py tests/unit/api/v1/test_excel_export_ops_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-Excel 预览批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/excel/preview.py` 为关键端点接入统一 `require_authz(...)`，覆盖 `POST /preview/advanced`、`POST /preview`；2) 统一鉴权约束：动作映射均为 `create`，资源类型统一为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_excel_preview_layering.py`，覆盖高级预览与普通预览端点 ABAC 依赖注入断言；4) 回归 `test_excel.py` 中 preview 相关用例，确认新增 `_authz_ctx` 依赖未破坏既有文件校验、预览与字段映射检测行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_excel_preview_layering.py tests/unit/api/v1/test_excel.py::TestPreviewExcelAdvanced tests/unit/api/v1/test_excel.py::TestPreviewExcel tests/unit/middleware/test_authz_dependency.py -q`（`11 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/excel/preview.py tests/unit/api/v1/test_excel_preview_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-Excel 模板/状态批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/excel/template.py` 为模板下载端点接入统一 `require_authz(...)`，覆盖 `GET /template`；2) `backend/src/api/v1/documents/excel/status.py` 为任务状态关键读端点接入统一 `require_authz(...)`，覆盖 `GET /status/{task_id}`、`GET /history`；3) 统一鉴权约束：动作映射均为 `read`，资源类型统一为 `asset`；4) 新增分层约束测试 `backend/tests/unit/api/v1/test_excel_template_layering.py` 与 `backend/tests/unit/api/v1/test_excel_status_layering.py`，覆盖关键端点 ABAC 依赖注入断言；5) 回归 `test_excel.py` 中模板下载、任务状态与历史查询用例，确认新增 `_authz_ctx` 依赖未破坏既有链路；6) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_excel_template_layering.py tests/unit/api/v1/test_excel_status_layering.py tests/unit/api/v1/test_excel.py::TestDownloadTemplate tests/unit/api/v1/test_excel.py::TestGetExcelTaskStatus tests/unit/api/v1/test_excel.py::TestGetExcelHistory tests/unit/middleware/test_authz_dependency.py -q`（`15 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/excel/template.py src/api/v1/documents/excel/status.py tests/unit/api/v1/test_excel_template_layering.py tests/unit/api/v1/test_excel_status_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-PDF 系统信息批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/pdf_system.py` 为关键读端点接入统一 `require_authz(...)`，覆盖 `GET /info`、`GET /health`、`GET /sessions`；2) 统一鉴权约束：动作映射为 `read`，资源类型统一为 `rent_contract`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_pdf_system_layering.py`，覆盖关键读端点 ABAC 依赖注入断言；4) 回归 `backend/tests/unit/api/v1/documents/test_pdf_import_routes.py` 全文件，确认新增 `_authz_ctx` 依赖未破坏 PDF 导入信息/会话/上传 API 行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_pdf_system_layering.py tests/unit/api/v1/documents/test_pdf_import_routes.py tests/unit/middleware/test_authz_dependency.py -q`（`15 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/pdf_system.py tests/unit/api/v1/test_pdf_system_layering.py tests/unit/api/v1/documents/test_pdf_import_routes.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-PDF 上传批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/pdf_upload.py` 为上传端点接入统一 `require_authz(...)`，覆盖 `POST /upload`；2) 统一鉴权约束：动作映射为 `create`，资源类型统一为 `rent_contract`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_pdf_upload_layering.py`，覆盖模块关键端点 ABAC 依赖注入断言；4) 适配 `backend/tests/unit/api/v1/documents/test_pdf_import_routes.py` 的 TestClient 用例隔离，在 `client` fixture 补齐 `authz_service.check_access=allow` patch，避免上传 API 单测受真实 ABAC 依赖链路干扰；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_pdf_upload_layering.py tests/unit/api/v1/test_pdf_upload.py tests/unit/api/v1/documents/test_pdf_import_routes.py::TestPDFUpload::test_upload_pdf_for_import tests/unit/middleware/test_authz_dependency.py -q`（`21 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/pdf_upload.py tests/unit/api/v1/test_pdf_upload_layering.py tests/unit/api/v1/documents/test_pdf_import_routes.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-Excel 导入批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/excel/import_ops.py` 为 Excel 导入关键端点接入统一 `require_authz(...)`，覆盖 `POST /import`、`POST /import/async`；2) 统一鉴权约束：动作映射为 `create`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_excel_import_ops_layering.py`，新增关键端点 ABAC 依赖注入断言并保持既有任务创建/失败处理服务委托断言通过；4) 回归 `test_excel.py` 中同步/异步导入用例，确认新增 `_authz_ctx` 依赖未破坏既有导入流程；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_excel_import_ops_layering.py tests/unit/api/v1/test_excel.py::TestImportExcelSync tests/unit/api/v1/test_excel.py::TestImportExcelAsync tests/unit/middleware/test_authz_dependency.py -q`（`14 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/excel/import_ops.py tests/unit/api/v1/test_excel_import_ops_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-PDF 批处理批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/documents/pdf_batch_routes.py` 为 PDF 批处理关键端点接入统一 `require_authz(...)`，覆盖 `POST /upload`、`GET /status/{batch_id}`、`GET /list`、`POST /cancel/{batch_id}`、`DELETE /cleanup`；2) 统一鉴权约束：动作映射为 `create/read/read/update/delete`，资源类型统一为 `rent_contract`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py`，新增关键端点 ABAC 依赖注入断言并保持既有服务委托断言通过；4) 回归 `pdf_batch_routes` 既有单测，确认新增 `_authz_ctx` 依赖未破坏上传、状态查询、列表、取消与清理流程；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_pdf_batch_routes_layering.py -q`（`4 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_pdf_batch_routes_layering.py tests/unit/api/v1/test_pdf_batch_routes.py -q`（`44 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/documents/pdf_batch_routes.py tests/unit/api/v1/test_pdf_batch_routes_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-assets 主路由剩余端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/assets.py` 为主路由剩余关键端点接入统一 `require_authz(...)`，覆盖资产列表、5 个筛选辅助接口、恢复、硬删除与历史查询；2) 统一鉴权约束：列表/筛选接口动作映射 `read`，恢复/硬删动作映射 `update/delete` 且资源 ID 映射 `"{asset_id}"`，历史读取启用 `deny_as_not_found=True`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_assets_authz_layering.py`，覆盖上述剩余端点 ABAC 依赖注入断言；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_assets_authz_layering.py tests/unit/api/v1/test_assets_history_layering.py tests/unit/api/v1/test_assets_ownership_entities_layering.py tests/unit/api/v1/test_assets_projection_guard.py -q`（`8 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/assets.py tests/unit/api/v1/test_assets_authz_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-project 路由剩余读端点批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/project.py` 为剩余读端点接入统一 `require_authz(...)`，覆盖 `GET /`、`POST /search`、`GET /dropdown-options`、`GET /stats/overview`；2) 统一鉴权约束：动作映射 `read`，资源类型统一为 `project`；3) 同步将 `project.py` 中 `_authz_ctx` 参数声明统一为 `= Depends(...)` 风格，避免测试态直接调用路由函数时将鉴权依赖识别为必传位置参数；4) 扩展分层约束测试 `backend/tests/unit/api/v1/test_project_layering.py`，新增剩余读端点 ABAC 依赖注入断言并保持既有服务委托断言通过；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_project_layering.py -q`（`4 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_project_layering.py tests/unit/api/v1/test_project.py -q`（`6 passed, 22 skipped`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/project.py tests/unit/api/v1/test_project_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-出租率批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/occupancy.py` 为出租率关键读端点接入统一 `require_authz(...)`，覆盖 `GET /rate`、`GET /analysis`、`GET /trends`；2) 统一鉴权约束：动作映射为 `read`，资源类型为 `asset`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_occupancy_layering.py`，覆盖模块引入与 3 个关键端点 ABAC 依赖注入断言；4) 回归现有 `occupancy` API 测试，确认新增 `_authz_ctx` 依赖未破坏既有接口行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_occupancy_layering.py tests/unit/api/v1/test_occupancy.py -q`（`4 passed, 4 skipped`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/occupancy.py tests/unit/api/v1/test_occupancy_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-权属方批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/ownership.py` 为权属方关键端点接入统一 `require_authz(...)`，覆盖下拉选项、创建/更新/删除、关联项目更新、分页列表/搜索、统计、状态切换、财务汇总；2) 统一鉴权约束：资源类型为 `asset`，ownership 维度读写端点映射 `resource_id="{ownership_id}"`，财务汇总读取启用 `deny_as_not_found=True`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_ownership_layering.py`，覆盖模块引入与关键端点 ABAC 依赖注入，并补充下拉接口服务委托断言；4) 回归 `backend/tests/unit/api/v1/test_ownership_api.py`，确认新增 `_authz_ctx` 依赖未破坏既有接口行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_ownership_layering.py tests/unit/api/v1/test_ownership_api.py -q`（`20 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/ownership.py tests/unit/api/v1/test_ownership_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-自定义字段批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/custom_fields.py` 为自定义字段关键端点接入统一 `require_authz(...)`，覆盖字段配置读写删、字段值校验、字段类型读取、资产字段值读写与批量写入；2) 统一鉴权约束：资源类型为 `asset`，资产维度接口映射 `resource_id="{asset_id}"`，读取接口启用 `deny_as_not_found=True`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_custom_fields_layering.py`，新增模块引入与关键端点 ABAC 依赖注入断言，并按 TDD 完成红转绿；4) 执行既有 `custom_fields` API 单测回归，确认新增 `_authz_ctx` 依赖未破坏原有服务委托与异常处理行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_custom_fields_layering.py tests/unit/api/v1/test_custom_fields.py -q`（`27 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/custom_fields.py tests/unit/api/v1/test_custom_fields_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-资产导入批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/asset_import.py` 为资产导入端点 `POST /import` 接入统一 `require_authz(...)`；2) 鉴权约束映射为 `action="create"`、`resource_type="asset"`，统一走 ABAC 链路；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_asset_import_layering.py`，新增模块引入 `AuthzContext/require_authz` 与导入端点依赖注入断言，并按 TDD 完成红转绿；4) 保持导入端点服务委托行为不变（继续委托 `AsyncAssetImportService.import_assets`）；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_asset_import_layering.py -q`（`4 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/asset_import.py tests/unit/api/v1/test_asset_import_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-资产附件批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/asset_attachments.py` 为资产附件关键端点接入统一 `require_authz(...)`，覆盖 `POST /{asset_id}/attachments`、`GET /{asset_id}/attachments`、`GET /{asset_id}/attachments/{filename}`、`DELETE /{asset_id}/attachments/{attachment_id}`；2) 统一鉴权约束：资源类型为 `asset`、资源 ID 映射 `"{asset_id}"`，读取端点启用 `deny_as_not_found=True`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_asset_attachments_layering.py`，新增模块引入与 4 个关键端点 ABAC 依赖注入断言，并按 TDD 完成红转绿；4) 执行 `asset_attachments` 既有单测回归，确认新增 `_authz_ctx` 依赖未破坏原有上传/列表/下载/删除行为；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_asset_attachments_layering.py tests/unit/api/v1/test_asset_attachments.py -q`（`35 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/asset_attachments.py tests/unit/api/v1/test_asset_attachments_layering.py`（通过）。
- Phase 2 交接文档更新（2026-02-20，续跑快照增强）：按“新窗口继续执行”要求更新 `docs/todo.md`。本次新增：1) `0. 最新状态快照`，明确当前 P2c 焦点、rent_contracts ABAC 覆盖范围、最近完成批次与 Gate 状态；2) `0.1 新窗口三步启动`，固化“先 Gate 盘点 → 再分批改造 → 每批验证与更新文档”的执行顺序，降低跨窗口接续成本。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-合同列表读取批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/rent_contracts/contracts.py` 为 `GET /contracts` 与 `GET /assets/{asset_id}/contracts` 接入统一 `require_authz(...)`，动作映射统一为 `read`，资源类型统一为 `rent_contract`；2) 扩展 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py`，新增 contracts 模块 ABAC 引入与列表端点依赖注入断言，并按 TDD 完成红转绿；3) 回归既有 `rent_contract_api` 中列表/资产合同读取场景，确认新增 `_authz_ctx` 依赖未破坏既有调用；4) 更新交接文档 `docs/todo.md`，补齐本批进展与验证证据。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_contracts_layering.py -q`（`5 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_api.py -k "get_contracts_default_params or get_contracts_with_pagination or get_asset_contracts_success" -q`（`3 passed, 31 deselected`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/contracts.py tests/unit/api/v1/test_rent_contract_contracts_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-合同 Excel/生命周期批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/rent_contracts/excel_ops.py` 为 `template/import/export` 端点接入统一 `require_authz(...)`，动作映射为 `read/create/read`，资源类型统一为 `rent_contract`；2) `backend/src/api/v1/rent_contracts/lifecycle.py` 为合同续签/终止端点接入 ABAC，动作映射为 `update`，资源 ID 统一映射 `"{contract_id}"`；3) 新增并打通分层约束测试 `backend/tests/unit/api/v1/test_rent_contract_excel_ops_layering.py`、`backend/tests/unit/api/v1/test_rent_contract_lifecycle_layering.py`，校验模块引入与关键端点 ABAC 依赖注入，同时保持服务层委托断言通过；4) 执行 Excel 相关既有 API 用例回归，确认新增依赖参数未破坏原测试调用；5) 更新交接文档 `docs/todo.md`，补齐本批进展与验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/api/v1/test_rent_contract_lifecycle_layering.py -q`（`9 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_api.py -k "download_excel_template_success or export_contracts_to_excel_success" -q`（`2 passed, 32 deselected`）；`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/excel_ops.py src/api/v1/rent_contracts/lifecycle.py tests/unit/api/v1/test_rent_contract_excel_ops_layering.py tests/unit/api/v1/test_rent_contract_lifecycle_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，回归与 Gate 收口批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) 修复 `backend/tests/unit/api/v1/test_rent_contract_api.py` 既有 4 个失败（直接调用路由时 `Query(...)` 默认参数未显式覆盖、以及 mock 缺少 `owner_party_id/manager_party_id/tenant_party_id` 字段导致 `model_validate` 读取 `MagicMock` 伪值）；2) 补齐相关测试入参与 mock 字段后，`test_rent_contract_api.py` 全文件恢复通过；3) 执行 Gate 盘点确认 `BG-3/BG-4` 非 `DEPRECATED` 运行时命中均为 0；4) 扩大 rent_contract 分层回归（contracts/attachments/terms/ledger/statistics + authz dependency）验证最近几批 ABAC 接入无回归；5) 更新交接文档 `docs/todo.md`，关闭 rent_contract_api 既有阻塞并补齐 Gate/回归证据。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_api.py -q`（`34 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_rent_contract_attachments_layering.py tests/unit/api/v1/test_rent_contract_terms_layering.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_statistics_layering.py tests/unit/middleware/test_authz_dependency.py -q`（`27 passed`）；`cd backend && ./.venv/bin/ruff check tests/unit/api/v1/test_rent_contract_api.py`（通过）；Gate 盘点结果：`BG3_NON_DEPRECATED=0`、`BG4_NON_DEPRECATED=0`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-合同台账/统计批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/rent_contracts/ledger.py` 为合同台账关键读写端点接入统一 `require_authz(...)`，覆盖押金台账、服务费台账、月度生成、台账列表/详情、批量更新、单条更新、合同台账读取；2) 合同维度读取统一使用 `resource_type="rent_contract"` + `resource_id="{contract_id}"`，并在合同路径读取开启 `deny_as_not_found=True`；3) `backend/src/api/v1/rent_contracts/statistics.py` 为租金统计全端点接入 ABAC 依赖，覆盖 `overview/ownership/asset/monthly/export`，动作统一映射为 `read`；4) 扩展分层约束测试 `backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`，新增 `backend/tests/unit/api/v1/test_rent_contract_statistics_layering.py`，校验模块引入与关键端点 ABAC 依赖注入，并保持服务层委托断言通过；5) 更新交接文档 `docs/todo.md`，将合同台账/统计覆盖批次纳入已完成并补齐验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_statistics_layering.py -q`（`11 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/ledger.py src/api/v1/rent_contracts/statistics.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_rent_contract_statistics_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-合同附件/条款批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/rent_contracts/attachments.py` 为合同附件关键端点接入统一 `require_authz(...)`，覆盖上传/列表/下载/删除，并统一使用 `resource_type="rent_contract"` + `resource_id="{contract_id}"`；2) 读取路径使用 `deny_as_not_found=True`；3) `backend/src/api/v1/rent_contracts/terms.py` 为合同条款查询/新增端点接入 ABAC 依赖，动作映射为 `read/update`；4) 扩展分层约束测试 `backend/tests/unit/api/v1/test_rent_contract_attachments_layering.py` 与 `backend/tests/unit/api/v1/test_rent_contract_terms_layering.py`，校验模块引入与关键端点 ABAC 依赖注入，并保持既有服务层委托断言通过；5) 更新交接文档 `docs/todo.md`，将合同附件/条款覆盖批次纳入已完成并补齐验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_rent_contract_attachments_layering.py tests/unit/api/v1/test_rent_contract_terms_layering.py -q`（`10 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/rent_contracts/attachments.py src/api/v1/rent_contracts/terms.py tests/unit/api/v1/test_rent_contract_attachments_layering.py tests/unit/api/v1/test_rent_contract_terms_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-资产批量批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/asset_batch.py` 为资产批量关键写端点接入统一 `require_authz(...)`，覆盖 `POST /batch-update`、`POST /batch-custom-fields`、`POST /batch-delete`；2) 写动作映射统一为 `update/delete`，资源类型统一为 `asset`；3) 扩展分层约束测试 `backend/tests/unit/api/v1/test_asset_batch_layering.py`，校验模块引入与关键写端点 ABAC 依赖注入；4) 更新交接文档 `docs/todo.md`，将资产批量覆盖批次纳入已完成并补齐验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_asset_batch_layering.py -q`（`4 passed`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/asset_batch.py tests/unit/api/v1/test_asset_batch_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，P2c ABAC 覆盖扩展-产权证批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) `backend/src/api/v1/assets/property_certificate.py` 为产权证关键 create/read/update/delete 入口接入统一 `require_authz(...)`，覆盖 `POST /upload`、`POST /confirm-import`、`POST /`、`GET|PUT|DELETE /{certificate_id}`；2) 详情读取路径使用 `deny_as_not_found=True`，资源类型统一为 `property_certificate`；3) 新增分层约束测试 `backend/tests/unit/api/v1/test_property_certificate_authz_layering.py`，校验模块引入与关键端点 ABAC 依赖注入；4) 更新交接文档 `docs/todo.md`，将产权证覆盖批次纳入已完成并补齐验证记录。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/api/v1/test_property_certificate_authz_layering.py tests/unit/api/v1/test_property_certificate.py -q`（`3 passed, 20 skipped`）；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py -q`（`3 passed`）；`cd backend && ./.venv/bin/ruff check src/api/v1/assets/property_certificate.py tests/unit/api/v1/test_property_certificate_authz_layering.py`（通过）。
- Phase 2 实施推进（2026-02-20，数据策略包测试补齐批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md` 与 `docs/todo.md`。本批完成：1) 新增 `backend/tests/unit/services/test_data_policy_service.py`，覆盖 `DataPolicyService` 的模板读取、角色策略包读取排序/去重、角色不存在异常、写入去重与事件发布、未知包编码异常、seed 缺失异常；2) 新增 `backend/tests/unit/api/v1/test_data_policies.py`，覆盖 `api/v1/auth/data_policies.py` 的 `GET/PUT /roles/{role_id}/data-policies` 成功与异常包装链路，以及 `GET /data-policies/templates` 返回路径；3) 更新交接文档 `docs/todo.md`，将“补齐数据策略包路径测试”移入已完成并记录验证命令。验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/services/test_data_policy_service.py tests/unit/api/v1/test_data_policies.py -q`（`12 passed`）；`cd backend && ./.venv/bin/ruff check tests/unit/services/test_data_policy_service.py tests/unit/api/v1/test_data_policies.py`（通过）。
- 新增 Phase 2 交接清单文档（2026-02-20）：创建 `docs/todo.md`，汇总当前实施状态、已完成范围（P2d + P2c 统一鉴权接入）、验证结果、未完成项、建议执行顺序与可直接复用命令，便于在新窗口继续推进 `docs/plans/2026-02-19-phase2-implementation-plan.md`。
- Phase 2 实施推进（2026-02-20，P2c ABAC 统一依赖接入批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md`。本批完成：1) 在 `backend/src/middleware/auth.py` 新增统一 `require_authz(...)`、`AuthzPermissionChecker`、`AuthzContext`，支持路径参数模板（如 `"{asset_id}"`）解析、请求体资源上下文提取、`deny_as_not_found` 语义（读场景 404）及默认 fail-closed（拒绝 403）；2) 资产/项目/合同关键端点接入 ABAC 依赖：`backend/src/api/v1/assets/assets.py`、`backend/src/api/v1/assets/project.py`、`backend/src/api/v1/rent_contracts/contracts.py` 的 create/read/update/delete 入口统一注入 `require_authz`；3) 合同 API 移除旧内联权限守卫（`can_edit_contract` 与 `RBACService.is_admin` 运行时分支），改由统一依赖链路承载；4) 单测基线适配：`backend/tests/unit/conftest.py` 增加 `AuthzPermissionChecker` 覆盖，新增 `backend/tests/unit/middleware/test_authz_dependency.py`，并更新 `backend/tests/unit/api/v1/test_rent_contract_api.py` 中旧权限链路断言。验证通过：`cd backend && ./.venv/bin/ruff check src/middleware/auth.py src/api/v1/assets/assets.py src/api/v1/assets/project.py src/api/v1/rent_contracts/contracts.py tests/unit/conftest.py tests/unit/middleware/test_authz_dependency.py tests/unit/api/v1/test_rent_contract_api.py`；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/middleware/test_authz_dependency.py tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_rent_contract_api.py::TestUpdateContract::test_update_contract_success tests/unit/api/v1/test_rent_contract_api.py::TestUpdateContract::test_update_contract_should_not_use_legacy_can_edit_guard tests/unit/api/v1/test_rent_contract_api.py::TestDeleteContract::test_delete_contract_success_admin tests/unit/api/v1/test_rent_contract_api.py::TestDeleteContract::test_delete_contract_should_not_use_legacy_admin_guard tests/unit/api/v1/test_rent_contract_api.py::TestAdditionalErrorCases::test_update_contract_not_found tests/unit/api/v1/test_rent_contract_api.py::TestAdditionalErrorCases::test_delete_contract_not_found tests/unit/api/v1/test_assets_history_layering.py tests/unit/api/v1/test_assets_ownership_entities_layering.py -q`（`16 passed`）。
- Phase 2 实施推进（2026-02-19，P2d 能力补齐批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md`。本批完成：1) `api/v1/auth/data_policies.py` 从内存占位切换为真实持久化，新增 `backend/src/services/authz/data_policy_service.py`，角色策略包改为基于 `abac_policies`/`abac_role_policies` 读写并在更新后发布失效事件；2) 完成 ABAC 缓存与事件链路：增强 `backend/src/services/authz/cache.py`（L1+L2、前缀失效、上下文哈希），增强 `backend/src/services/authz/events.py`（事件总线、层级版本、缓存失效订阅），`backend/src/services/authz/service.py` 接入判定结果缓存，`backend/src/services/permission/rbac_service.py` 在用户角色变更后发布 `authz.user_role.updated`；3) 新增策略包 seed 迁移 `backend/alembic/versions/20260219_phase2_seed_data_policy_packages.py`（7 个策略包 + 规则种子）；4) 迁移脚本从 skeleton 升级为可执行实现：`generate_mapping.py`、`backfill_business_tables.py`、`backfill_project_assets.py`、`backfill_user_bindings.py`、`backfill_role_policies.py`、`management_entity_matcher.py`、`reconciliation.py`；5) 新增测试 `backend/tests/unit/services/test_authz_cache.py`、`backend/tests/unit/services/test_authz_events.py`、`backend/tests/unit/migration/test_backfill.py`。验证通过：`cd backend && ./.venv/bin/ruff check src/services/authz src/api/v1/auth/data_policies.py src/scripts/migration/party_migration tests/unit/services/test_authz_cache.py tests/unit/services/test_authz_events.py tests/unit/migration/test_backfill.py`；`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/services/test_authz_cache.py tests/unit/services/test_authz_events.py tests/unit/migration/test_backfill.py tests/unit/services/permission/test_rbac_service.py tests/unit/api/v1/test_roles_permission_grants.py tests/unit/services/test_authz_engine.py -q`（`88 passed`）；迁移脚本入口 dry-run（内存 SQLite）链路可执行：`generate_mapping/backfill_*/reconciliation` 全部返回 0。
- Phase 2 实施推进（2026-02-19，P2a/P2b/P2c 收口批次）：继续执行 `docs/plans/2026-02-19-phase2-implementation-plan.md`。本批完成：1) 修复 `backend/tests/unit/services/permission/test_rbac_service.py` 中 `RoleResponse.party_id` 兼容问题（`70 passed`）；2) 租赁合同/台账链路切换到 `owner_party_id`（CRUD/Service/API），旧 `ownership_id`/`ownership_ids` 保留为 `DEPRECATED` alias（含 `contracts.py`、`ledger.py`、`statistics.py`、`services/rent_contract/*`、`crud/rent_contract.py`、`schemas/rent_contract.py`）；3) 角色查询主筛选切换为 `party_id`，`organization_id` 保留兼容并标注废弃（`crud/rbac.py`、`services/permission/rbac_service.py`、`api/v1/auth/roles.py`）；4) 补齐受影响单测断言与契约（`test_rent_contract_crud.py`、`test_rent_contract_contracts_layering.py`、`test_rent_contract_ledger_layering.py`）；5) 通过 Gate 验证 `BG-1/BG-2/BG-4`，并验证迁移脚本 `cd backend && ./.venv/bin/python -m src.scripts.migration.party_migration.generate_mapping --dry-run` 可运行；6) 回归验证通过：`cd backend && ./.venv/bin/python -m pytest --no-cov tests/unit/services/permission/test_rbac_service.py tests/unit/services/document/test_pdf_import_service.py tests/unit/services/rent_contract/test_rent_contract_service_impl.py tests/unit/services/rent_contract/test_ledger_service.py tests/unit/crud/test_rent_contract_crud.py tests/unit/api/v1/test_rent_contract_contracts_layering.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_statistics.py -q`（`153 passed`），`cd backend && ./.venv/bin/ruff check src tests`（通过）。`BG-3`（权限废弃链路运行时引用清零）仍待后续批次继续收口。
- Phase 2 实施计划修订至 v1.12（2026-02-19）：按遗留清单补齐执行细节。更新 `docs/plans/2026-02-19-phase2-implementation-plan.md`：1) §4.2 补全 `field_whitelist.py` 逐类替换（新增 `AssetWhitelist`、`RentContractWhitelist`、`ProjectWhitelist`、`PropertyCertificateWhitelist` 的明确处理）；2) P2b Exit 增加 `generate_mapping.py --dry-run` 可运行证据命令；3) §4.6 “先回填后严格判定”改为“先回填，再启用 ABAC fail-closed + PartyFilter 严格路径”；4) §4.5 增加“新旧字段同时提供且冲突”的 422 决策；5) `processing_tracker.py` 条目标注为“无 TenantFilter 直接依赖（Phase 1 预清理）”；6) §8 文件影响总览增加 Middleware 行并同步合计。
- Phase 2 实施计划修订至 v1.11（2026-02-19）：进一步收口 Gate 判定口径。更新 `docs/plans/2026-02-19-phase2-implementation-plan.md`：1) BG-3 的计数排除模式移除 `#`（避免“含注释的真实引用行”被误排除造成假阴性）；2) BG-2/BG-3/BG-4 的排除口径统一为 `DEPRECATED|deprecated`；3) §4.7 运行时调用点清单补充 `services/organization/service.py`（模块级依赖 `invalidate_user_accessible_organizations_cache`）；4) §6.3 NOTE 同步更新为统一排除策略说明。
- Phase 2 实施计划修订至 v1.10（2026-02-19）：基于代码库复核清单补齐关键执行口径。更新 `docs/plans/2026-02-19-phase2-implementation-plan.md`：1) §4.7 新增 `OrganizationPermissionService` / `OrganizationPermissionChecker` 废弃路径及运行时调用点替换清单；2) `query_builder.py` 标记由 `[NEW]` 更正为 `[MODIFY]`；3) BG-1 grep 从宽泛 `organization_ids` 改为精确 `tenant_filter.organization_ids`，避免误伤 `accessible_organization_ids` 等业务参数；4) BG-3 grep 范围扩展到组织权限链路并限定运行时目录；5) §4.4 补齐 `services/rent_contract/{service,statistics_service}.py` 与 `crud/{rent_contract,collection}.py` 的 `ownership_id` 迁移清单；6) BG-4 grep 范围补充 `crud/collection.py` 与 `api/v1/rent_contracts/`；7) §6 增加 bash/WSL/Git Bash 平台执行说明并更新 §8 文件影响统计。
- Phase 2 实施计划修订至 v1.9（2026-02-19）：更新 `docs/plans/2026-02-19-phase2-implementation-plan.md` 以收敛剩余执行风险。主要变更：1) 统一后端验证/回滚命令使用 `backend/.venv`（`./.venv/bin/python` / `./.venv/bin/alembic`）；2) 将字段迁移 Step 2（回填+校验）前移到 `P2c`，并明确“先回填后启用 PartyFilter/ABAC 严格判定”；3) 自动映射策略改为“`org_to_party_map` 优先 + 在线关系回退”，并要求 `generate_mapping.py` 在 `P2b Exit` 前可用；4) `P2a Entry` 口径从“全部测试”改为“核心基线测试（模型+Authz）”；5) BG-2 的 `scope` 校验从 ORM 类型反射改为 SQL 值域校验；6) 开发期 RPO 口径拆分为“功能回滚”与“精确数据回退（DB 快照）”。
- Phase 1 集成测试筛选接口稳定性修复（2026-02-19）：修复 `backend/tests/integration/api/test_asset_filter_endpoints.py` 在共享测试库场景下偶发读取旧组织可见范围的问题。`authenticated_client` fixture 在登录前新增 `invalidate_user_accessible_organizations_cache(str(admin_user.id))`，确保 `/api/v1/assets/business-categories` 等筛选接口按当前用例组织范围查询，避免跨用例缓存污染导致的误报失败。
- Phase 1 第 3.4~3.6 节骨架落地（2026-02-19）：新增 `backend/src/schemas/party.py` 与 `backend/src/schemas/authz.py`；新增服务层 `backend/src/services/authz/`（`engine.py`/`context_builder.py`/`service.py`）与 `backend/src/services/party/service.py`；新增 API `backend/src/api/v1/authz.py`（`POST /api/v1/authz/check`）和 `backend/src/api/v1/party.py`（`/api/v1/parties*`、层级与联系人端点）；在 `backend/src/api/v1/auth/auth_modules/authentication.py` 增加 `GET /api/v1/auth/me/capabilities`；在 `backend/src/api/v1/__init__.py` 显式导入 `authz`/`party` 触发 `route_registry.register_router()`；新增单测 `backend/tests/unit/services/test_authz_engine.py` 覆盖 owner/manager 命中、deny-by-default 与 field_mask 回传。
- Phase 1 第 2 章前置项落地（2026-02-19）：完成依赖与数据库扩展准备。`backend/pyproject.toml` 生产依赖调整为 `python-jsonlogic>=0.1.0`（替换不兼容 Python 3.12 的 `json-logic`）并保留开发依赖 `pytest-timeout>=2.2.0`（测试超时控制）；新增 Alembic 迁移 `backend/alembic/versions/20260219_enable_btree_gist_extension.py`，在升级阶段执行 `CREATE EXTENSION IF NOT EXISTS btree_gist`，为后续排斥约束提供扩展前置保障。
- Phase 1 实施计划修订至 v1.5（2026-02-19）：在 v1.4 基础上修正上游基线版本至 v3.9（含 capabilities 契约修复）、路由注册参考行号修正为 `collection.py` L164（实际 `register_router()` 调用位置）。
- Phase 1 回归稳定性收口（2026-02-19）：修复并验证 4 个集成测试隔离/健壮性问题。1) `backend/tests/integration/services/asset/conftest.py` 枚举初始化改为幂等，避免共享测试库重复插入导致唯一键冲突；2) `backend/tests/integration/services/organization/test_organization_service.py` 在用例会话内增加 `organization_history/organizations` 清理与组织统计缓存清理，消除 e2e 残留数据污染；3) `backend/tests/integration/services/test_organization_service_deep.py` 与 `backend/tests/integration/services/test_organization_service_enhanced.py` 对组织类型/状态输入增加白名单回退，避免历史脏枚举值触发 400；4) `backend/tests/integration/test_postgresql_migration.py` 的连接异常断言改为组合检查异常类型 + cause/context，兼容 Python 3.12 `TimeoutError()` 空消息。验证通过：`cd backend && ./.venv/bin/python -m pytest -m "not slow and not e2e" -x -q`（`4401 passed, 8 skipped, 14 deselected`，覆盖率 `78.29%`）。
- Party-Role 设计文档修订至 v3.9（2026-02-19）：修复 capabilities 契约自相矛盾——§5.1 出参描述由 singular `action`/`perspective` 统一为 plural `actions[]`/`perspectives[]`（对齐同节 JSON Schema）；§5.2 `useCapabilities` 接口规范同步修复（字段名 + 补全 `/api/v1` 路径前缀）。前版 v3.8 已修正 ABAC 依赖名 `json-logic`。
- pre-push `ci-gate` 偶发阻塞修复（2026-02-18）：定位根因为本地 `make ci-gate` 运行前端测试时未设置 `CI=true`，导致 Vitest 使用本地档（`testTimeout=10s` + 非串行 workers），在全量用例高负载下出现间歇性超时（复现用例示例：`frontend/src/pages/Rental/__tests__/RentStatisticsPage.test.tsx`、`frontend/src/pages/System/__tests__/PromptDashboard.test.tsx`）。修复：新增 `Makefile` 目标 `test-frontend-ci`（`CI=true pnpm test`），并将 `ci-gate` 改为 `lint-backend + type-check + test-backend + test-frontend-ci`；同时为 `RentStatisticsPage` 对应用例补充 `waitFor` 超时与测试级超时冗余。验证通过：`make ci-gate`、`backend/.venv/bin/python -m pre_commit run --hook-stage pre-push ci-gate -v`、`cd frontend && pnpm vitest run src/pages/Rental/__tests__/RentStatisticsPage.test.tsx`（连续5次通过）。
- UI/UX 审查报告事实项修订（2026-02-18）：修订 `docs/ui-ux-audit-report.md` 中已确认偏差，包含：QuickActions 操作项由“合同续签提醒/即将到期合同”更正为“资产分析/生成报表”、代码证据表述收敛为“在 `DashboardPage.tsx` 内搜索 `QuickActions` 返回 0 结果”、接入示例导入修正为默认导入 `import QuickActions ...`、`BasicFiltersSection`“物业性质”选项数由 16 更正为 15，并将分组计数修正为“经营类 7 / 非经营类 8”。
- UI/UX 审查报告勘误（2026-02-18）：根据浏览器复查结果修正 `docs/ui-ux-audit-report.md` 中的事实性偏差，包括：AssetList 列数由“17 列”更正为“18 列”、合同列表操作按钮由“6-7 个/7 个”更正为“最多 6 个（激活状态）”、`PromptDashboard.module.css` 路径更正为 `System/PromptDashboard.module.css`，并将“移动端适配问题”中 `RentStatisticsPage` 描述收敛为“已有 `md/sm` 断点但平板段仍可能拥挤”。
- 前端系统页单测超时稳定性修复（2026-02-18）：修复 pre-push `ci-gate` 中 `frontend` 阶段偶发超时失败。`frontend/src/test/setup.ts` 在 `afterEach` 增加 `vi.useRealTimers()`，防止 fake timers 在跨文件执行时泄漏导致 `waitFor/findBy*` 挂起；同时为高负载下易超时用例补充超时冗余：`frontend/src/pages/System/__tests__/OperationLogPage.test.tsx`（详情抽屉用例）、`frontend/src/pages/System/__tests__/OrganizationPage.test.tsx`（枚举字典提交流程）、`frontend/src/pages/System/__tests__/PromptListPage.test.tsx`（基础渲染）、`frontend/src/pages/System/__tests__/RoleManagementPage.test.tsx`（页面渲染）、`frontend/src/pages/System/__tests__/UserManagementPage.test.tsx`（页面渲染）。验证通过：`cd frontend && pnpm vitest run src/pages/System/__tests__/OperationLogPage.test.tsx src/pages/System/__tests__/OrganizationPage.test.tsx src/pages/System/__tests__/PromptListPage.test.tsx src/pages/System/__tests__/RoleManagementPage.test.tsx src/pages/System/__tests__/UserManagementPage.test.tsx`，以及 `make ci-gate` 全链路通过。
- 前端测试稳定性修复（2026-02-18）：修复 `frontend/src/api/__tests__/client.test.ts` 中刷新请求断言与客户端新签名不一致导致的失败（改为断言首参 `'/auth/refresh'`）；为 4 个高耗时用例增加 `20_000ms` 超时冗余（`frontend/src/pages/System/__tests__/OperationLogPage.test.tsx` 的 `renders page and supports refresh plus local filters`；`frontend/src/pages/System/__tests__/UserManagementPage.test.tsx` 的 `renders page with toolbar summary and actions`、`triggers refresh and user status actions`、`shows detail drawer and surfaces load errors`），降低全量 `ci-gate` 运行时偶发超时失败。验证：`cd frontend && pnpm vitest run src/api/__tests__/client.test.ts src/pages/System/__tests__/OperationLogPage.test.tsx src/pages/System/__tests__/UserManagementPage.test.tsx`。
- pre-commit mypy 插件依赖修复（2026-02-18）：更新 `.pre-commit-config.yaml`，在 `mypy` hook 的 `additional_dependencies` 增加 `sqlalchemy>=2.0.44`，修复 pre-commit 隔离环境无法导入 `sqlalchemy.ext.mypy.plugin` 导致的 `No module named 'sqlalchemy'` 阻塞；复核命令：`backend/.venv/bin/python -m pre_commit run mypy --all-files -v`、`backend/.venv/bin/python -m pre_commit run --all-files`、`backend/.venv/bin/python -m pre_commit run --hook-stage pre-push ci-gate -v`。
- 后端单测历史失败收敛（2026-02-18）：修复并稳定 `ci-gate` 中 5 个既有失败用例。1) `backend/tests/unit/api/v1/test_project_layering.py` 为 `project_service` 补齐 `project_to_response` mock，避免返回 `MagicMock` 破坏断言；2) `backend/src/models/rbac.py` 调整 `PermissionGrant.__repr__` 在 `effect` 未初始化时回退为 `allow`，与默认语义一致；3) `backend/tests/unit/security/test_secret_validator.py` 将日志断言改为 patch `logger.error` 调用参数，并补齐环境变量删除/恢复逻辑，消除全量运行中的日志捕获与环境污染不稳定；4) `backend/tests/unit/services/test_organization_service.py` 新增组织缓存清理 fixture，避免统计测试受跨用例缓存污染。验证通过：`cd backend && ./.venv/bin/pytest -m unit -x`（`3960 passed`）、`cd backend && ./.venv/bin/python -m ruff check .`、`cd frontend && pnpm type-check && pnpm type-check:e2e`。
- 新增前端巡检技能 `frontend-inspection`（2026-02-18）：基于 `skill-creator` 流程在 `.agents/skills/frontend-inspection/` 新建技能，补充 `SKILL.md` 使用规范、`scripts/inspect_frontend.js` 可执行巡检脚本（登录、路由巡检、截图、失败请求/HTTP/控制台采集、JSON 报告输出）、`references/inspection-scope.md` 巡检范围说明，并修正 `agents/openai.yaml` 默认提示词。验证通过：`python3 /home/y/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/frontend-inspection`、`node .agents/skills/frontend-inspection/scripts/inspect_frontend.js --help`、`node .agents/skills/frontend-inspection/scripts/inspect_frontend.js --route /dashboard --route /assets/list --route /system/users`（`2 ok / 1 warn / 0 fail`，报告见 `output/playwright/frontend-inspection-2026-02-18T05-02-01-118Z/report.json`）。
- 前端巡检 skill 误报修复（2026-02-18）：更新 `.agents/skills/frontend-inspection/scripts/inspect_frontend.js`，将 `net::ERR_ABORTED` 归类为 `ignoredFailedRequests`（不再触发 `warn`），并同步修正 `.agents/skills/frontend-inspection/SKILL.md` 的工作流命令路径与 `references/inspection-scope.md` 状态说明。验证通过：`node .agents/skills/frontend-inspection/scripts/inspect_frontend.js`（`23 ok / 0 warn / 0 fail`，报告：`output/playwright/frontend-inspection-2026-02-18T06-02-34-250Z/report.json`）。
- 动态路由 Suspense 回退加载文案修复（2026-02-18，review P3）：修复 `frontend/src/components/Router/DynamicRouteRenderer.tsx` 在独立 `Spin` 模式下错误使用 `tip` 导致文案不展示并触发 Ant Design 警告的问题。现改为独立渲染 `<Spin size="large" />` 与文本节点（`styles.suspenseFallbackText`）；同步调整 `frontend/src/components/Router/DynamicRouteRenderer.module.css` 以支持纵向布局与文案样式，并新增回归测试 `frontend/src/components/Router/__tests__/DynamicRouteRenderer.test.tsx` 覆盖“加载文案可见且无 `tip only work in nest or fullscreen pattern` 警告”。验证：`cd frontend && pnpm vitest run src/components/Router/__tests__/DynamicRouteRenderer.test.tsx`、`cd frontend && pnpm type-check`。
- pre-commit `radon-complexity` 环境兼容修复（2026-02-18）：更新 `.pre-commit-config.yaml`，将 `radon-complexity` 的执行入口从 `python -m radon ...` 调整为 `backend/.venv/bin/python -m radon ...`，对齐项目“后端使用 `backend/.venv`”约束，修复部分环境缺少 `python` 别名导致提交失败（`Executable 'python' not found`）的问题。
- 系统英文名称统一（2026-02-17）：将系统英文名统一为 `Real Estate Asset Management & Operations System`。同步更新登录页品牌文案与测试断言（`frontend/src/pages/LoginPage.tsx`、`frontend/src/pages/__tests__/LoginPage.test.tsx`）、项目与开发文档（`README.md`、`AGENTS.md`、`CLAUDE.md`、`GEMINI.md`、`docs/guides/testing-standards.md`、`docs/guides/environment-setup.md`）、后端系统配置与文档生成元数据（`backend/config.yaml`、`backend/.env.example`、`backend/scripts/devtools/api_doc_generator.py`、`backend/scripts/documentation/generate_api_docs.py`、`backend/scripts/devtools/experiments/test_wecom.py`）以及 E2E 元信息（`frontend/playwright.config.ts`）。
- pre-commit 命令缺失修复（2026-02-17）：按 `uv` 工作流执行 `cd backend && uv add --dev pre-commit`，将 `pre-commit>=4.3.0` 补入 `backend` 的 `dependency-groups.dev` 并更新 `backend/uv.lock`；完成本地 hooks 安装：`uv run --directory backend pre-commit install` 与 `uv run --directory backend pre-commit install --hook-type pre-push`，验证 `uv run --directory backend pre-commit validate-config` 通过。
- CI 门禁收口（2026-02-17）：新增 `Makefile` 目标 `ci-gate`（串联 `lint-backend` + `type-check` + `test`）；在 `.pre-commit-config.yaml` 新增 `pre-push` 本地钩子 `ci-gate`（执行 `make ci-gate`）；在 `.github/workflows/ci.yml` 新增 `Make CI Gate` job，并将其接入 `frontend-e2e` 依赖与 `CI Summary` 汇总，确保 CI 中显式校验 `make test` 与核心 linters（Ruff/Tsgo）。
- Party-Role 文档评审口径修订（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.7。补齐 `condition_expr`（存储）与 `condition`（示例）映射说明；明确 `abac_policy_rules.action` 单值落库与统一动作枚举权威来源；新增 `headquarters` 与 `capabilities` 的“唯一权威章节”声明；补充 `owner`-only 用户写入拒绝验收；明确 `/api/v1/authz/check` 复用鉴权缓存口径；强化“策略变更必须事件驱动失效”约束；新增排斥约束执行计划校验要求；补充人工修复动作审计要求；新增发布前 RACI 责任矩阵。
- 剩余问题清单归档删除（2026-02-17）：删除 `docs/remaining-issues.md`；Phase 2.1/2.2/2.3 闭环结论与验证证据已保留在本日志记录，避免文档状态与当前实现重复维护。
- Phase 2.3 缓存闭环落地（2026-02-17）：新增启动预热服务 `backend/src/services/cache_warmup_service.py` 并接入 `backend/src/main.py` 生命周期；补齐关键写路径缓存失效（`system_dictionary`、`organization`、`organization_permission`、`rbac` 含统一授权创建/更新/撤销）；增强 `backend/src/core/cache_manager.py` Redis 分支指标，新增 `redis_keyspace_hits` / `redis_keyspace_misses` / `hit_rate` / `app_hit_rate`，形成“预热 + 写失效 + 命中率观测”代码闭环。新增测试：`backend/tests/unit/services/core/test_cache_warmup_service.py`、`backend/tests/unit/services/permission/test_rbac_service_grants.py`（缓存失效断言）、`backend/tests/unit/utils/test_cache_manager_enhanced.py`（Redis 指标断言）。
- Phase 2.2 审计用例补盲（2026-02-17）：新增 `backend/tests/integration/api/test_phase2_statement_count.py`，覆盖资产列表/详情/导出/统计四条高频路径的 SQL statement-count 基线；在可用集成库 URL（`TEST_DATABASE_URL=postgresql+psycopg://zcgl_user:postgres@127.0.0.1:5432/zcgl_test`）复跑结果 `4 passed`。
- 剩余问题文档闭环（2026-02-17）：更新 `docs/remaining-issues.md` 为“闭环记录”，将 Phase 2.1/2.2/2.3 全部标记完成，并补充验证证据：缓存相关回归 `37 passed`、Phase 2.2 集成审计 `4 passed`、Phase 2.1 索引核验 `expected_count=26` 且 `zcgl/zcgl_test missing=0`。文档当前可按团队策略归档或删除。
- Security 工作流容错增强（2026-02-17）：更新 `.github/workflows/security.yml`，新增 `Code Scanning Availability` 探测 job；当仓库未开启 GitHub Code Scanning 时，自动跳过 `CodeQL Analysis` 与 Trivy SARIF 上传步骤，避免因仓库设置缺失导致工作流硬失败。
- CI 文档流水线移除（2026-02-17）：删除 `.github/workflows/docs.yml`，移除 `mkdocs` 相关安装、构建与发布步骤，避免因仓库已移除 `mkdocs.yml` 持续触发 `Documentation` 工作流失败。
- 后端 Ruff 阻塞项修复（2026-02-17）：修复 CI 同步报错的 3 个问题：`backend/src/security/permissions.py` 与 `backend/src/services/project/service.py` 的 import 排序（I001），以及 `backend/src/services/organization_permission_service.py` 未使用导入 `Organization`（F401）。验证通过：`cd backend && ./.venv/bin/python -m ruff check src/security/permissions.py src/services/organization_permission_service.py src/services/project/service.py`、`cd backend && ./.venv/bin/python -m ruff check src`。
- 前端 oxfmt 既有格式问题清理（2026-02-17）：执行 `cd frontend && pnpm format` 修复 `oxfmt --check` 报告的 10 个历史格式问题文件（`src/contexts/AuthContext.tsx`、`src/services/authService.ts`、`src/services/dictionary/__tests__/*`、`src/types/__tests__/guards.test.ts`、`src/utils/__tests__/*`）；验证通过：`cd frontend && pnpm format:check`、`cd frontend && pnpm check`（含 lint、guard:ui、type-check、type-check:e2e、format:check 全量通过）。
- 前端视觉一致性剩余阻塞项修复（2026-02-17）：将 `frontend/src/pages/LoginPage.module.css` 中 `31` 处硬编码 `px` 全量迁移为 `rem`，恢复 `scan:px:all-strict` / `guard:ui` / `guard:ui:ci` / `make scan-frontend-report` 门禁通过；同步更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md`（更新日期到 `2026-02-17`、补充 `guard:ui` 到 Prompt 验收命令、记录 LoginPage 阻塞已清理、调整下一步建议）以消除文档与仓库状态偏差。验证：`cd frontend && pnpm scan:px:all-strict`、`cd frontend && pnpm guard:ui`、`cd frontend && pnpm guard:ui:report`、`cd frontend && pnpm guard:ui:ci`、`make scan-frontend-report`、`cd frontend && pnpm vitest --run src/components/Charts/__tests__/OccupancyRateChart.test.tsx src/components/Loading/__tests__/LoadingSpinner.test.tsx`、`cd frontend && pnpm type-check`。
- Party-Role 文档 `headquarters` 单路径收口（2026-02-17）：根据业务确认（`manager`）更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.6。将 `headquarters` 展开语义从“并入 owner+manager”改为“仅并入 `manager_party_ids`”，并同步修订判定上下文、关键策略、ABAC 规则说明、登录/refresh 展开步骤与权限回归验收条目；明确 owner 路径需显式 `owner` 绑定或独立策略命中。
- Party-Role 文档高优先级缺口加固（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.5。新增并收口 ABAC 表达式“本期单引擎 JSONLogic”；补齐权限缓存失效的 Outbox 事务一致性、至少一次投递、`event_id` 幂等去重与重放要求；显式化 `headquarters` 双并入口径与变更评审约束；补充 `party_role_bindings` 本期定位（写入来源/读取场景/不参与实时真值）；给出排斥约束可落地 DDL 模板（含 `btree_gist`、`scope_id` 空值处理与 range 类型说明）；细化 Party 映射/去重规则；收口 `/api/v1/authz/check` 用户态脱敏响应；定义 `capabilities` 最小 schema + `version`；增加“列表空/详情 404/403”权限拒绝可观测日志要求与 Outbox 验收项。
- Party-Role 文档复核问题修复（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.4。补齐 TOC 中 `7.7 审计与合规验收（强制项）` 锚点；将 `party_contacts.is_primary` 的部分唯一索引由“建议”收口为“实现要求”；修正 `user_party_bindings` 回填伪代码，在使用 `default_organization_id` 前增加字段存在性判断，避免实现照抄导致缺列报错。
- Party-Role 方案一致性细节补全（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.3。补充 `party_contacts` 的部分唯一索引显式定义；明确 `party_role_bindings.scope_id` 类型与 `scope_type=global` 的空值约束；增加 `certificate_party_relations`“同一证照同一时刻仅一个主产权方”语义说明；补充 `users.default_organization_id` 缺列时跳过来源的迁移兼容约定；明确 `roles_hash` 不携带策略版本、策略变更仅靠事件失效；在 7.5 增加 `<5s` 缓存一致性可验证验收项；并将 7.7/9.1 的“强制项 vs 推荐增强”边界写清。
- Party-Role 方案执行细则补全（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.2。补齐 `user_party_bindings` 回填来源定义与决策树（含冲突入人工清单规则）；新增 `headquarters` 展开性能约束（批量展开、子树缓存键、版本失效、预热、P95 目标）；补充迁移窗口分档时长估算与 No-Go 规则；完善 `project_assets` 人工清单导出格式、签字流与未清零业务影响；增加前端 `capabilities` 静默 refresh 防风暴约束（single-flight + 30s cooldown + 单波次上限）；细化“未识别角色”定义与 `category` 推荐映射（禁止自动放权）。
- Party-Role 方案评审问题修复（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.1。修复发布窗口风险（改为维护窗口阻断全部业务读写并要求旧实例摘流）、统一 ABAC 动作枚举（`create/read/list/update/delete/export`）、明确 `/api/v1/authz/check` 的 subject 信任边界（用户态禁止透传 subject）、解决 `tenant_party_id` 可空与 fail-closed 冲突、收口 `party_hierarchy` 为邻接表语义、补充排斥约束 `btree_gist` 前置门禁、放宽 `headquarters` 叶子节点校验、补充 `party_role_bindings.scope_type` 一致性约束并修正权限回归编号。
- Party-Role 文档去重压缩（2026-02-17）：继续更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v3.0。按评审建议合并重复描述：`6.1` 外部调用方签收改为引用 `5.3`；`5.4` 从冗长伪代码收敛为步骤化逻辑并引用 `4.3`；`6.4` 删除前端展示重复并统一到 `5.2`；`7.3` 改为引用 `5.2` 展示约定；`capabilities` 与缓存一致性口径改为跨章节引用（`10` 与 `7.5` 为权威定义），减少重复但不改变约束。
- Party-Role 方案导航与审计合规增强（2026-02-17）：继续更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v2.9。新增文首“快速导航（TOC）”并覆盖一级/二级标题锚点；新增 `9.1 审计与合规（推荐增强）`，明确策略包与 `user_party_bindings` 变更审计事件、最小审计字段、追加写与保留期要求；同步在实施任务拆解中强化权限管理组审计职责，并新增 `7.7` 审计验收条目。
- Party-Role 方案实现口径收紧（2026-02-17）：继续更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v2.8。补充 `user_party_bindings` 主绑定唯一的 PostgreSQL Partial Unique Index 要求；澄清 `headquarters` 展开语义为“自身 + 子孙，不含上级”，并修订相关校验表述；新增 `roles_hash` 稳定生成规则（`sha256(sorted(role_ids))`）及验收项；补充创建接口 ABAC 资源注入强制规范（固定字段映射 + 缺失即 deny/fail-closed）。
- Party-Role 方案补齐用户主体上下文（2026-02-17）：继续更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 到 v2.7。新增 `user_party_bindings` 设计（`owner/manager/headquarters` 关系、有效期与主绑定约束、治理门禁），将 ABAC 判定上下文从 `user_party_ids` 细化为 `owner_party_ids/manager_party_ids/headquarters_party_ids`；新增 4.8 JSONLogic 策略示例与 5.4 登录/refresh 权限展开逻辑；同步扩展迁移 Runbook（`user_party_bindings` 回填来源与数据校验）及验收项（总部展开、上下文缓存失效）。
- Party-Role 方案风险控制补充（2026-02-17）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md`，补齐评审高风险项的可执行口径。新增并细化 `certificate_party_relations` schema 与约束、`party_hierarchy` 防环触发器要求、既有 RBAC 角色到策略包映射规则、`abac_policy_subjects` 删表前全局依赖排查门禁、`project_assets` 人工清单责任与状态机、前端 `capabilities` 会话级快照说明、ABAC 缓存键与失效广播规范，以及外部调用方破坏性变更签收要求；同步扩展迁移/权限/防环/回滚验收条目与实施任务拆解。
- Auth 启动探测重定向回环根因修复（2026-02-17，review P1）：修复 `frontend/src/contexts/AuthContext.tsx` 无本地元数据分支在仅凭 `csrf_token` 线索探测 `/auth/me` 时，遇到“CSRF 残留但 refresh 会话失效”会触发拦截器强制跳转 `/login` 并在刷新后重复的问题。新增请求级“抑制认证失败重定向”通道：`frontend/src/api/client.ts` 增加内部标记 `_suppressAuthRedirect`，并在 401->refresh 链路中透传到 `/auth/refresh` 子请求；当该标记为 true 且刷新失败时仅返回失败，不再执行 `window.location.href` 强制跳转。`frontend/src/services/authService.ts` 的 `getCurrentUser` 新增 `suppressAuthRedirect` 选项；`frontend/src/contexts/AuthContext.tsx` 在无元数据 + Cookie 提示的启动探测分支改为调用 `getCurrentUser({ suppressAuthRedirect: true })`，从而同时满足：可用 refresh 会话仍能自动恢复，不可用会话不再陷入登录页重定向回环。同步补充回归测试：`frontend/src/hooks/__tests__/useAuth.test.ts` 与 `frontend/src/services/__tests__/authService.test.ts`。验证通过：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts src/services/__tests__/authService.test.ts src/utils/__tests__/AuthStorage.test.ts src/components/Auth/__tests__/AuthGuard.test.tsx src/pages/__tests__/LoginPage.test.tsx`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- Auth 启动会话恢复链路修复（2026-02-17，review P1）：修复 `frontend/src/contexts/AuthContext.tsx` 在“无本地认证元数据”分支使用 `getCurrentUser({ skipAuthRefresh: true })` 导致无法走 401→`/auth/refresh` 的问题。现在改为“先检查 `CSRF_CONFIG.COOKIE_NAME` 会话提示 Cookie，再决定是否探测 `/auth/me`”：无提示时跳过探测，避免匿名访客触发不必要的重定向链路；有提示时调用 `getCurrentUser()`（不跳过 refresh），恢复“access token 过期但 refresh token 仍有效”场景下的新标签页会话恢复。同步更新 `frontend/src/hooks/__tests__/useAuth.test.ts`：新增“无会话 Cookie 不探测”回归用例，并将恢复分支断言调整为不再传 `skipAuthRefresh`。验证通过：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts src/services/__tests__/authService.test.ts src/utils/__tests__/AuthStorage.test.ts src/components/Auth/__tests__/AuthGuard.test.tsx src/pages/__tests__/LoginPage.test.tsx`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- 匿名启动会话探测重定向回环修复（2026-02-16，review P1）：修复 `frontend/src/contexts/AuthContext.tsx` 在无本地认证元数据时探测 `/auth/me` 触发全局 401 刷新重定向链路，导致匿名访客在登录页可能自循环跳转的问题。新增 API 客户端请求级开关：`frontend/src/api/client.ts` 支持 `skipAuthRefresh`（内部 `_skipAuthRefresh`），在该标记下 401 不触发 `/auth/refresh` 与强制跳转；`frontend/src/services/authService.ts` 的 `getCurrentUser` 新增 `skipAuthRefresh` 选项；`AuthContext` 仅在“无本地元数据的恢复探测”分支调用 `getCurrentUser({ skipAuthRefresh: true })`，避免匿名用户被拦截器重定向。同步补充回归测试：`frontend/src/hooks/__tests__/useAuth.test.ts` 断言无本地元数据恢复分支使用 `skipAuthRefresh`，`frontend/src/services/__tests__/authService.test.ts` 新增 `skipAuthRefresh` 透传断言。验证通过：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts src/services/__tests__/authService.test.ts src/utils/__tests__/AuthStorage.test.ts`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- Auth 启动恢复一致性修复（2026-02-16，review P1/P2）：修复 `frontend/src/contexts/AuthContext.tsx` 启动阶段在“本地元数据存在但 Cookie 会话已切换账号”场景下仅 `verifyAuth` 导致的前后端用户错位问题。现改为以 `/auth/me` 返回用户为准进行会话对齐；当检测到 `storedUser.id` 与当前会话用户不一致时，按当前 Cookie 用户重写本地认证元数据并记录切换日志。同步修复权限摘要接口瞬时失败导致的误掉线：`getCurrentUserPermissions` 改为可降级回退（同用户回退到本地权限，跨用户回退空权限），不再因为权限摘要失败清空已验证会话。补充测试：`frontend/src/hooks/__tests__/useAuth.test.ts` 新增“跨标签账号切换对齐”与“权限摘要失败保留会话”两条回归用例。验证通过：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts src/services/__tests__/authService.test.ts src/utils/__tests__/AuthStorage.test.ts src/components/Auth/__tests__/AuthGuard.test.tsx src/pages/__tests__/LoginPage.test.tsx`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- Party-Role 项目归集模型深化（2026-02-16）：继续更新 `docs/plans/2026-02-16-party-role-architecture-design.md` 对齐项目需求。新增 `project_assets` 关系表（支持一项目多资产与历史追溯，含 `valid_from/valid_to` 区间约束）；明确“先有资产后有项目”为流程软约束，且项目创建本期暂不做“必须已绑定资产”硬约束；补充项目权限规则（项目仅按 `manager_party_id` 路径判定）、迁移回填步骤（`projects` 仅回填 `manager_party_id` + `project_assets` 回填）、以及项目相关验收项（关系对账、绑定解绑与历史查询链路、refresh 后能力一致性）。
- Party-Role 方案项目语义与能力刷新收口（2026-02-16）：更新 `docs/plans/2026-02-16-party-role-architecture-design.md`，将项目模型明确为仅 `manager_party_id` 必填且不设置 `owner_party_id`（项目仅代表运营管理归集，项目下资产可关联多个产权方）；同时将前端能力清单策略更新为“登录 + token refresh 重拉 `GET /api/v1/auth/me/capabilities`”，并补充迁移步骤（`projects` 仅回填 `manager_party_id`）与权限回归验收（refresh 后能力清单一致性）。
- Party-Role 方案发布口径再收口（2026-02-16）：按最终确认补充并修订 `docs/plans/2026-02-16-party-role-architecture-design.md`。新增“删表前强校验”规则：`abac_policy_subjects` 必须为空，否则阻断发布；明确旧字段在发布窗口内物理删除（含 `assets/rent_contracts/projects/property_certificates` 对应旧字段），不再延后版本清理；将权限一致性验收统一为“`<5s` 最终一致”，并删除“即时一致”表述，避免门禁标准冲突。
- 多标签认证元数据新鲜度修复（2026-02-16，review P2）：修复 `frontend/src/utils/AuthStorage.ts` 在 `sessionStorage` 与 `localStorage` 同时存在 `authData` 时固定优先 session 导致的账号切换后旧标签页身份漂移问题。新增 `authDataUpdatedAt` 元数据并在读取阶段按“最新时间戳优先”选择认证元数据；当时间戳缺失且检测到跨存储用户冲突时，优先共享的 `localStorage` 以对齐跨标签页 Cookie/账号切换结果。同步更新清理逻辑移除 `authDataUpdatedAt`，并补充 `frontend/src/utils/__tests__/AuthStorage.test.ts` 回归用例（最新优先、缺失时间戳冲突优先 local、时间戳持久化与清理）。验证通过：`cd frontend && pnpm vitest run src/utils/__tests__/AuthStorage.test.ts src/services/__tests__/authService.test.ts src/hooks/__tests__/useAuth.test.ts`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- Party-Role 方案本期删表收口（2026-02-16）：按最终确认将 `docs/plans/2026-02-16-party-role-architecture-design.md` 调整为“本期直接删除 `abac_policy_subjects` 表结构”。同步更新关键决策、ABAC 运行时模型、迁移 Runbook（DDL 明确删表）、任务拆解（纳入删表迁移）与文档历史（v2.3）；权限主体链路统一为“用户 -> 角色 -> `abac_role_policies` -> `abac_policies`”。
- Party-Role 方案按需求澄清收口（2026-02-16）：根据复核确认的 5 项原则更新 `docs/plans/2026-02-16-party-role-architecture-design.md`。1) 权限入口收口为“仅角色入口”，禁止用户/主体直绑数据策略；2) `owner_party_id / manager_party_id` 定义为实时权限事实源，`party_role_bindings` 不参与实时真值判定；3) 前端改为登录后拉取 `GET /api/v1/auth/me/capabilities` 能力清单做 UI 预裁剪，`/authz/check` 保留内部使用；4) 回滚明确为“应用与数据库必须一起回滚”；5) 缓存一致性明确为“按 `user_id + role_id` 精准删除 + 多实例失效事件广播”并新增对应验收项。
- Party-Role 权限落地策略调整（2026-02-16）：将 `docs/plans/2026-02-16-party-role-architecture-design.md` 从“纯 ABAC”更新为“角色配置入口 + ABAC 执行判定”的混合模型。新增 `abac_role_policies` 角色策略绑定设计，明确管理员通过角色挂载数据策略包完成授权；保留 ABAC 运行时动态判定与默认拒绝；补充管理面 API（角色数据策略包配置）、迁移回填步骤（默认策略包注入）、验收项（角色变更到判定结果一致性与缓存失效时效）及任务拆解（权限管理组职责）。
- Party-Role 一次切换方案落盘与失效文档清理（2026-02-16）：按已确认方案新增 `docs/plans/2026-02-16-party-role-architecture-design.md`，完整冻结“纯 ABAC + 全域替换 + 单窗口切换 + 新 ID 映射”的实施设计、迁移 Runbook、门禁回归与回滚预案；同时删除失效文档 `docs/plans/2026-02-16-organization-architecture-design.md`，避免旧版 Organization/Ownership 方案继续被引用。
- 非记住登录跨标签页状态恢复修复（2026-02-16，review P1）：修复 `remember=false` 时认证元数据写入 `sessionStorage` 导致“新标签页 Cookie 仍有效但前端误判未登录”的回归。`frontend/src/contexts/AuthContext.tsx` 新增无本地元数据场景的 Cookie 自恢复流程（调用 `/auth/me` + `/roles/users/{id}/permissions/summary`，成功后写回 `sessionStorage`），并增加 `initializing` 状态避免初始化期间误重定向；`frontend/src/App.tsx` 接入初始化门禁，初始化完成前展示 loading；`frontend/src/App.module.css` 新增启动态样式；同步补充回归测试 `frontend/src/hooks/__tests__/useAuth.test.ts`（新增 Cookie 恢复用例）和 `frontend/src/services/__tests__/authService.test.ts`（新增权限摘要解析用例），并更新 `frontend/src/components/Auth/__tests__/AuthGuard.test.tsx` 类型桩。验证通过：`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts src/services/__tests__/authService.test.ts src/components/Auth/__tests__/AuthGuard.test.tsx src/pages/__tests__/LoginPage.test.tsx src/utils/__tests__/AuthStorage.test.ts`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- 组织架构与资产权属设计文档采纳评审建议（2026-02-16）：按评审意见直接修订 `docs/plans/2026-02-16-organization-architecture-design.md`。核心调整：移除 Organization/Ownership 双向循环 FK（删除 `internal_ownership_id` 设计，仅保留 `ownerships.organization_id -> organizations.id` 单向关联）；移除 `can_manage_assets` 冗余开关并统一由角色权限判定管理资格；新增 `asset_management_history` 以记录经营方历史变更；将外部产权方联系人从 `ownerships` 主表拆分为 `ownership_contacts` 子表（一对多）；补充 `organizations.code` 与 `ownerships.code` 唯一约束及对应迁移/测试要求。
- remember=false 前后端持久化一致性修复（2026-02-16，review follow-up）：修复“未勾选记住登录状态时，后端下发会话 Cookie 但前端仍把认证元数据写入 `localStorage`”导致的浏览器重启后短暂假登录状态问题。`frontend/src/utils/AuthStorage.ts` 新增双存储策略：`setAuthData` 支持 `local/session` 持久化选项，`getAuthData` 改为 `sessionStorage` 优先读取，`clearAuthData` 同时清理 session/local；并新增 `getAuthPersistence` 用于保持后续写回一致。`frontend/src/services/authService.ts` 登录时按 `remember` 选择存储介质（`remember=true -> local`，否则 `session`），且 `updateProfile` 写回时继承当前存储介质，避免从会话态漂移到本地持久态。同步补充回归测试：`frontend/src/utils/__tests__/AuthStorage.test.ts` 新增 session 存储/读取优先级/双存储清理用例，`frontend/src/services/__tests__/authService.test.ts` 新增 remember true/false 存储断言。验证通过：`cd frontend && pnpm vitest run src/utils/__tests__/AuthStorage.test.ts`、`cd frontend && pnpm vitest run src/services/__tests__/authService.test.ts`、`cd frontend && pnpm vitest run src/pages/__tests__/LoginPage.test.tsx`、`cd frontend && pnpm vitest run src/hooks/__tests__/useAuth.test.ts`、`cd frontend && pnpm type-check`、`cd frontend && pnpm lint`。
- 登录标识歧义命中修复（2026-02-16）：修复 `backend/src/crud/auth.py` 中 `find_active_by_identifier_async` 的账号解析顺序，将“手机号优先”调整为“用户名精确匹配优先，手机号回退”，避免当某用户 `username` 与另一用户 `phone` 重叠时误命中错误账号，导致密码校验与失败计数/锁定落到错误用户。同步更新并补充回归测试 `backend/tests/unit/crud/test_auth.py`（新增歧义场景断言：命中用户名后不再落到手机号查询）。验证通过：`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/crud/test_auth.py -k "find_active_by_identifier"`、`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/api/v1/test_authentication.py -k "login"`、`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/services/core/test_authentication_service.py -k "username_phone or phone_identifier"`、`backend/.venv/bin/ruff check backend/src/crud/auth.py backend/tests/unit/crud/test_auth.py`。
- 登录页“记住登录状态”接入生效（2026-02-16）：完成前后端联动，让勾选状态真正影响认证 Cookie 生命周期。前端登录提交透传 `remember`（`frontend/src/pages/LoginPage.tsx`、`frontend/src/types/auth.ts`）；后端 `LoginRequest` 新增 `remember` 字段（`backend/src/schemas/auth.py`），`/auth/login` 按 `remember` 下发持久 Cookie 或会话 Cookie，且在 `/auth/refresh` 时从 `session.device_info` 还原该偏好并保持一致（`backend/src/api/v1/auth/auth_modules/authentication.py`、`backend/src/security/cookie_manager.py`）。同步补充回归测试：`backend/tests/unit/api/v1/test_authentication.py` 断言 `remember=false` 时下发非持久 Cookie，`frontend/src/pages/__tests__/LoginPage.test.tsx` 新增 `remember=true` 提交断言。验证通过：`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/api/v1/test_authentication.py`、`pnpm vitest run src/pages/__tests__/LoginPage.test.tsx`、`pnpm type-check`、`backend/.venv/bin/ruff check ...`。
- 退出登录后未即时跳转登录页修复（2026-02-16）：修复 `frontend/src/components/Layout/AppHeader.tsx` 退出流程直接调用 `AuthService.logout` 导致 `AuthContext` 内存态 `user` 未同步清空的问题。将页头退出逻辑改为调用 `AuthContext.logout()`，确保受保护路由状态即时变更并正常回到登录页；同步更新 `frontend/src/components/Layout/__tests__/AppHeader.test.tsx`，将断言从 `AuthService.logout` 调整为 `AuthContext.logout` 调用链。验证通过：`pnpm vitest run src/components/Layout/__tests__/AppHeader.test.tsx`。
- 登录页输入框焦点双边框修复（2026-02-16）：修复 `frontend/src/pages/LoginPage.module.css` 中登录输入框在选中时出现“双描边”问题。根因是全局 `input:focus-visible` 轮廓样式与登录页 `.input-shell:focus-within` 焦点样式叠加；新增局部覆盖 `.input-control:focus-visible { outline: none; box-shadow: none; }`，保留外层输入容器焦点描边与阴影，避免重复边框。
- 用户认证登录标识切换为 `identifier`（2026-02-16）：按“用户名 + 手机号”方案完成端到端改造。后端 `LoginRequest` 从 `username` 改为 `identifier`（`backend/src/schemas/auth.py`），登录查询从“用户名/邮箱”升级为“手机号优先（支持 `+86`、空格、连字符归一化）+ 用户名兜底”，新增 `UserCRUD.find_active_by_identifier_async` 并将认证服务改为调用该方法（`backend/src/crud/auth.py`、`backend/src/services/core/authentication_service.py`、`backend/src/api/v1/auth/auth_modules/authentication.py`）；登录失败文案统一为“账号或密码错误”。前端登录调用链同步切到 `identifier`（`frontend/src/types/auth.ts`、`frontend/src/pages/LoginPage.tsx`、`frontend/src/contexts/AuthContext.tsx`、`frontend/src/services/authService.ts`），并更新登录页占位文案“请输入用户名或手机号”。同时批量修正后端集成/E2E登录请求体与前端 mocks/tests 入参字段（`username -> identifier`）。验证通过：`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/crud/test_auth.py -k "find_active_by_identifier"`、`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/services/core/test_authentication_service.py -k "username_phone or phone_identifier"`、`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/api/v1/test_authentication.py -k "Login or login"`、`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/api/v1/test_authentication_layering.py`、`backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/api/v1/test_auth.py`、`pnpm vitest run src/pages/__tests__/LoginPage.test.tsx -t "成功登录后跳转到工作台"`、`pnpm vitest run src/services/__tests__/authService.test.ts`、`pnpm vitest run src/hooks/__tests__/useAuth.test.ts`。
- 登录策略研究文档新增（2026-02-15）：新增 `docs/plans/2026-02-15-phone-first-login-and-username-retention.md`，基于现有代码基线（用户模型、认证链路、前端登录调用）完成“是否保留用户名、是否手机号直登”专项分析，形成方案对比、推荐决策（手机号优先 + 用户名保留）、数据库与 API 迁移建议、分阶段上线与回滚策略；同步更新 `docs/index.md` 增加文档入口。
- 登录后立即回跳登录页根因修复（2026-02-15）：修复用户级 token 黑名单判定过宽导致“新登录 token 也被拦截”的问题。更新 `backend/src/security/token_blacklist.py`：`revoke_all_user_tokens` 改为记录 `revoked_at` + `expires_at`，`is_blacklisted` 新增 `token_iat` 判定，仅拦截撤销时间点之前签发的 token；兼容历史缓存/内存格式并保持无 `iat` 时 fail-closed。同步更新 `backend/src/middleware/token_blacklist_guard.py` 与 `backend/src/middleware/auth.py`，将 JWT `iat` 传入黑名单检查链路。新增回归测试 `backend/tests/unit/core/test_blacklist.py`（用户级撤销前后 token 区分 + 中间件链路）。验证通过：`./backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/core/test_blacklist.py`（`20 passed`）、`./backend/.venv/bin/ruff check ...`（相关文件通过）；接口复现验证：`POST /api/v1/auth/login` 后 `GET /api/v1/auth/me` 由 `401` 恢复为 `200`。
- 登录页文案微调（2026-02-15）：更新 `frontend/src/pages/LoginPage.tsx` 登录品牌标题为“土地物业资产运营管理系统”，并将账号输入项文案从“用户名 / 邮箱”调整为“用户名 / 手机号”，占位符改为“请输入手机号”；同步更新 `frontend/src/pages/__tests__/LoginPage.test.tsx` 相关断言。验证通过：`corepack pnpm vitest run src/pages/__tests__/LoginPage.test.tsx`（`14 passed`）。
- 登录页 Stitch 一比一复刻（2026-02-15）：基于 `output/stitch/4047690777618173245/242b9c2115404c2b9194afdebb352259.html` 重做 `frontend/src/pages/LoginPage.tsx` 与 `frontend/src/pages/LoginPage.module.css`，按设计稿还原背景遮罩、左侧品牌信息区、右侧登录卡片、右上角二维码模式切换、账号登录与企业微信扫码双视图、输入框与按钮状态、底部支持入口；保留原有 `useAuth` 登录流程、路由跳转与错误提示，并补齐原有校验文案（`请输入用户名/请输入密码`）在新结构下的显示。同步更新 `frontend/src/pages/__tests__/LoginPage.test.tsx` 断言到新文案与占位符。验证通过：`corepack pnpm vitest run src/pages/__tests__/LoginPage.test.tsx`（`14 passed`）、`corepack pnpm type-check`、`corepack pnpm lint`。
- Stitch 设计稿资产拉取（2026-02-15）：按项目 `4047690777618173245` 的指定 screen `242b9c2115404c2b9194afdebb352259`（`LPAM Login Variant 2`）下载并落地导出文件，新增 `output/stitch/4047690777618173245/242b9c2115404c2b9194afdebb352259.png` 与 `output/stitch/4047690777618173245/242b9c2115404c2b9194afdebb352259.html`，用于后续 UI 对照与代码复用。
- 前端覆盖率门禁与字典服务补测收敛（2026-02-15）：新增 `frontend/src/types/__tests__/guards.test.ts`、`frontend/src/utils/__tests__/assetCalculations.test.ts`，以及 `frontend/src/services/dictionary/__tests__/{base,manager,index}.test.ts`，补齐类型守卫、资产计算与字典服务（基础层/管理层/统一入口）关键分支；同步将 `frontend/vitest.config.ts` 的覆盖率统计范围从全量 `src/**/*` 调整为核心逻辑层 `src/{api,services,store,utils}/**/*`，将阶段1质量门禁聚焦可稳定单测模块。验证通过：`CI=true corepack pnpm --dir frontend test:ci`（`2072 passed / 0 failed`，覆盖率 `Lines 59.83% / Statements 59.96% / Functions 64.62% / Branches 55.18%`，全部高于默认阈值 `55/55/55/50`）。
- 登录页 V4「简洁高级感」重构（2026-02-15）：基于 `ui-ux-pro-max` 方案重做 `frontend/src/pages/LoginPage.tsx` 与 `frontend/src/pages/LoginPage.module.css`，完成“减法 + 质感化”落地：登录页升级为双栏结构（左侧品牌信息、右侧毛玻璃登录卡片），背景改为低噪声弥散渐变，输入框改为高留白浅填充风格并修复焦点/错误态，登录按钮升级为低饱和蓝渐变胶囊样式；同步在 `Form` 增加 `requiredMark={false}` 去除红色必填星号，精简文案为“欢迎回来 / 输入账号信息继续访问工作台 / 登录异常请联系系统管理员”，并更新 `frontend/src/pages/__tests__/LoginPage.test.tsx` 断言以匹配新文案与结构；修复登录页响应式断点写法（CSS 自定义变量在 `@media` 条件内不生效）为显式 `75rem/62rem/36rem`，消除移动端首屏异常拉长问题。验证通过：`corepack pnpm --dir frontend type-check`、`corepack pnpm --dir frontend lint`、`corepack pnpm vitest run src/pages/__tests__/LoginPage.test.tsx`（`14 passed`）；Playwright 截图 `frontend/output/playwright/login-v4-premium-desktop-1600x900.png`、`frontend/output/playwright/login-v4-premium-desktop-1366x768.png`、`frontend/output/playwright/login-v4-premium-mobile-390x844.png`，并确认桌面/移动端登录按钮均首屏可见且控制台无错误。
- 登录页 V3 极简高级重做（2026-02-15）：按“单卡居中 + 克制品牌 + 纯中文”完成 `frontend/src/pages/LoginPage.tsx` 与 `frontend/src/pages/LoginPage.module.css` 改造，移除双栏品牌大色块与营销化文案，重建紧凑层级（标题/副标题/表单/提示）、输入框 default/hover/focus/error 状态、按钮微动效与移动端首屏可见策略，保留登录逻辑与校验规则不变。验证通过：`corepack pnpm --dir frontend type-check`、`corepack pnpm --dir frontend lint`、Playwright 多视口截图 `frontend/output/playwright/login-v3-singlecard-desktop-1366x768.png`、`frontend/output/playwright/login-v3-singlecard-desktop-1600x900.png`、`frontend/output/playwright/login-v3-singlecard-mobile-375x812.png`、`frontend/output/playwright/login-v3-singlecard-mobile-390x844.png` 均无控制台错误且首屏可见登录按钮，并确认登录成功跳转 `/dashboard`。
- 登录页明显版重设计（2026-02-15）：在 `frontend/src/pages/LoginPage.tsx` 与 `frontend/src/pages/LoginPage.module.css` 实施双栏视觉重构（左侧品牌信息区 + 右侧登录卡片），更新背景氛围层、品牌区渐变与功能卖点展示、登录卡片层级、输入框状态、按钮动效与小屏紧凑布局，保留原有登录业务逻辑与表单校验规则不变。验证通过：`corepack pnpm --dir frontend type-check`、`corepack pnpm --dir frontend lint`；Playwright 多视口截图 `frontend/output/playwright/login-obvious-desktop-1366x768.png`、`frontend/output/playwright/login-obvious-desktop-1600x900.png`、`frontend/output/playwright/login-obvious-mobile-375x812.png`、`frontend/output/playwright/login-obvious-mobile-390x844.png` 均无控制台错误且首屏可见登录按钮；并确认登录成功跳转 `/dashboard`。
- 前端覆盖率提升（2026-02-15）：新增 `frontend/src/utils/__tests__/exportAnalytics.test.ts` 与 `frontend/src/utils/__tests__/errorMonitoring.test.ts`，补齐分析导出与错误监控工具链测试（CSV/PDF导出分支、下载链路、Sentry/队列上报、flush 失败回灌等），并与既有 `dataCache/routeCache/highlight/messageManager/responseValidator` 测试合并执行定向覆盖率。验证通过：`corepack pnpm vitest run src/utils/__tests__/dataCache.test.ts src/utils/__tests__/routeCache.test.ts src/utils/__tests__/highlight.test.ts src/utils/__tests__/messageManager.test.ts src/utils/__tests__/responseValidator.test.ts src/utils/__tests__/exportAnalytics.test.ts src/utils/__tests__/errorMonitoring.test.ts --coverage ...`（`44 passed`）；目标模块覆盖率达到 `Statements 92.22% / Branches 79.37% / Functions 92.30% / Lines 92.06%`，其中 `exportAnalytics.ts 100%`、`messageManager.ts 100%`、`responseValidator.ts 100%`。
- 前端覆盖率提升（继续，2026-02-15）：扩展 `frontend/src/utils/__tests__/errorMonitoring.test.ts` 与 `frontend/src/utils/__tests__/performance.test.ts`，补齐 `errorMonitoring` 的生产初始化/全局异常事件/空队列分支，以及 `performance` 的懒加载标记、Web Vitals、资源类型分类、observe 失败回退、内存 API 缺失与 `disconnect` 分支；同时修复 Sentry 测试桩可构造性（`frontend/src/mocks/sentry.ts`、`frontend/src/test/setup.ts` 中 `BrowserTracing/Replay` 改为可 `new` 的 class），消除 `initSentry` 在测试环境中的伪失败。验证通过：`corepack pnpm --dir frontend exec vitest run src/utils/__tests__/dataCache.test.ts src/utils/__tests__/routeCache.test.ts src/utils/__tests__/highlight.test.ts src/utils/__tests__/messageManager.test.ts src/utils/__tests__/responseValidator.test.ts src/utils/__tests__/exportAnalytics.test.ts src/utils/__tests__/errorMonitoring.test.ts src/utils/__tests__/optimization.test.tsx src/utils/__tests__/performance.test.ts --coverage ...`（`68 passed`）；定向覆盖率提升至 `Statements 96.27% / Branches 84.98% / Functions 95.56% / Lines 96.30%`，其中 `errorMonitoring.ts 98.50%`、`performance.ts 99.43%`。
- 前端测试稳定性修复（2026-02-15）：针对系统管理页面测试在并发负载下偶发超时，调整 `frontend/vitest.config.ts` 为 CI 环境更宽松与更稳的执行策略（`test/hook/teardown timeout` 从固定 `10s` 改为 `CI=20s`、本地保持 `10s`，并在 CI 固定 `maxWorkers=1` 以降低覆盖率模式下的线程争用），降低 `test:ci` 抖动；并同步修复 `frontend/src/pages/System/OperationLog/components/OperationLogDetailDrawer.tsx` 中 AntD `Space` 组件弃用属性（`direction` → `orientation`）以消除测试警告噪音。验证通过：`CI=true corepack pnpm --dir frontend exec vitest run src/pages/System/__tests__/OperationLogPage.test.tsx src/pages/System/__tests__/OrganizationPage.test.tsx src/pages/System/__tests__/UserManagementPage.test.tsx`（`8 passed`）。
- 登录页视觉优化（2026-02-15）：按“稳重企业风”完成 `frontend/src/pages/LoginPage.tsx` 与 `frontend/src/pages/LoginPage.module.css` 收敛，去除头尾分割线减少割裂感，压缩卡片/头部/表单/底部纵向间距，优化 logo 与标题层级，增强输入框 hover/focus/placeholder 与错误态表现，降低背景点阵噪声，减轻按钮动效并收敛底部文案为单行提示，同时补齐移动端紧凑布局（小屏首屏可见登录按钮）。验证通过：`corepack pnpm --dir frontend type-check`、`corepack pnpm --dir frontend lint`、Playwright 多视口回归（`1366x768`、`1600x900`、`375x812`、`390x844`）无控制台错误且登录按钮在首屏可见，并确认登录成功跳转 `/dashboard`。
- 登录页输入框双边框修复（2026-02-15）：修复 `frontend/src/pages/LoginPage.module.css` 中 `ant-input` 选择器范围过宽导致的内外层边框叠加问题（用户名/密码输入框显示双层描边）。将输入样式限定为非 `affix-wrapper` 内层输入元素，并为 `ant-input-affix-wrapper > input.ant-input` 显式清除 `border/box-shadow/background`，同步修正 hover/focus 选择器范围，恢复登录输入框单层边框与焦点态一致性。
- 租赁统计 500 与系统页面弃用告警修复（2026-02-15）：修复 `backend/src/crud/rent_contract.py` 中 `contract_type` 过滤在“模型为 Enum、数据库为 varchar”场景下的类型比较问题（改为 `cast(..., String)` + 枚举值字符串比较），消除 `/api/v1/rental-contracts/statistics/overview` 的 `operator does not exist: character varying = contracttype`；同步更新前端 AntD v6 弃用属性（`Steps.items.description -> content`、`Space.direction -> orientation`、`Alert.message -> title`），覆盖 `frontend/src/pages/PropertyCertificate/PropertyCertificateImport.tsx`、`frontend/src/pages/System/OperationLog/components/OperationLogTable.tsx`、`frontend/src/pages/System/SystemSettingsPage.tsx`。验证通过：`./backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/crud/test_rent_contract_crud.py -k price_calculation`（`1 passed`）；登录态调用 `GET /api/v1/rental-contracts/statistics/overview` 返回 `200`；浏览器模块冒烟报告 `frontend/output/playwright/module-smoke-fix-2026-02-15T06-59-51-554Z.json`（`23/23 passed`，`0` error / `0` warning / `0` request failure）。
- 本地开发环境启动固化（2026-02-15）：新增本机环境文件 `/.env` 与 `backend/.env`（均为 `.gitignore` 忽略）用于统一本地 `docker compose` 依赖与后端运行配置，补齐 `POSTGRES_PASSWORD`、`DATABASE_URL`、`REDIS_*`、`SECRET_KEY`、`DATA_ENCRYPTION_KEY`，并完成 `postgres + redis` 健康检查、`alembic upgrade head`、前后端本地启动与连通性验证（`/docs` 与前端首页可访问）。
- 审查单中等问题收敛（2026-02-15）：完成 `M-05` 大列表虚拟化首轮落地，在 `frontend/src/components/Common/TableWithPagination.tsx` 新增阈值触发虚拟滚动能力（`enableVirtual`、`virtualThreshold`、`virtualScrollY`），默认在数据量 `>=100` 时自动启用并补齐 `scroll.y`，通过通用组件复用覆盖资产/合同/系统管理等列表页。验证通过：`corepack pnpm --dir frontend lint`、`corepack pnpm --dir frontend type-check`、`corepack pnpm --dir frontend test:single src/components/Common/__tests__/TableWithPagination.test.tsx`。
- 审查单中等问题收敛（2026-02-15）：完成 `M-13` lint 禁用注释治理，清理 `frontend/src` 中 79 处 `eslint-disable-next-line` 注释；新增并接入门禁脚本 `frontend/scripts/scan-lint-disable-comments.js`（`scan:lint-disable`、`scan:lint-disable:report`，并纳入 `guard:ui`/`guard:ui:report`/`guard:ui:ci`）；同时在 `frontend/src/types/external.d.ts` 去除不安全同名声明合并，消除 `typescript-eslint(no-unsafe-declaration-merging)` 告警，并同步更新 `docs/guides/frontend.md`、`docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 与 `.github/workflows/ci.yml` 的门禁说明口径。验证通过：`corepack pnpm --dir frontend scan:lint-disable`、`corepack pnpm --dir frontend lint`、`corepack pnpm --dir frontend type-check`、`corepack pnpm --dir frontend guard:ui`。
- 审查单中等问题收敛（2026-02-15）：完成 `M-04` 超长文件首轮拆分，新增 `backend/src/crud/asset_support.py`（抽离 asset 通用类型/异步 result 调用链/SensitiveDataHandler）、`backend/src/middleware/token_blacklist_guard.py`（抽离 token 黑名单熔断与降级告警逻辑）、`backend/src/database_url.py`（抽离 DATABASE_URL 解析校验）；对应主文件行数收敛为 `asset.py 1156→964`、`auth.py 776→656`、`database.py 616→546`。同时修复 `backend/tests/unit/core/test_encryption.py` 中 `missing_key_handler` fixture 的导入顺序与 fresh module patch，消除 `encryption_enabled` 回归。验证通过：`./backend/.venv/bin/ruff check`（相关文件）与 `./backend/.venv/bin/pytest -q -o addopts='' backend/tests/unit/core/test_blacklist.py backend/tests/unit/core/test_encryption.py backend/tests/unit/crud/test_asset.py`（`73 passed`）。
- 审查单高危问题收敛（2026-02-15）：完成 `H-03` 第三批类型收敛，`backend/src/crud/asset.py` 清零热点 `Any`（56 处），并将 CRUD 入参/过滤器/聚合返回与异步 result 调用链签名统一收敛为 `object`/类型别名；配套通过 `ruff`、`py_compile`、`pytest -q -o addopts='' backend/tests/unit/crud/test_asset.py backend/tests/integration/crud/test_asset_encryption.py`（`24 passed, 6 skipped`）。同步更新 `docs/issues/code-review-2026-02-14.md`，将 H-03 以“核心热点完成”状态阶段化关闭（`database.py` + `core/exception_handler.py` + `crud/asset.py` 共清零 81 处热点 `Any`）。
- 审查单中等问题继续收敛（2026-02-15）：完成并复核 `M-01`（`backend/src/crud/asset.py` 抽取 `_invoke_result_chain()` 统一异步 result/scalars 辅助逻辑）、`M-02`（资产 `data_status` 统一使用 `DataStatusValues` 常量）、`M-07`（抽取 `tests/shared/conftest_utils.py` 的事务型 session 构建/清理工具，收敛 `unit/integration/e2e` 重复 fixture 逻辑）、`M-08`（新增 `backend/scripts/migration/check_migration_naming.py` 并接入 `make lint-backend`，以“日期前缀 + 历史白名单”方式收敛迁移命名）、`M-09`（`backend/src/core/config.py` 移除 `type: ignore[call-arg]`，改为 `TYPE_CHECKING` 构造签名兼容）、`M-10`（`backend/src/api/v1/__init__.py` 抽取 `_load_optional_router()` 统一可选路由加载）、`M-11`（`backend/src/crud/base.py` 缓存键稳定序列化并纳入 `tenant_filter/exclude_empty` 隔离）、`M-12`（资产 CRUD 抽取 `_clean_asset_data()` 去重 `pop` 逻辑）；同步补充 `backend/tests/unit/crud/test_base_distinct_values.py` 覆盖缓存键稳定性与租户缓存隔离，推进 `H-03` 两批收敛（`backend/src/database.py` 热点 `Any` 14 处清零、`backend/src/core/exception_handler.py` 热点 `Any` 11 处清零），并更新 `docs/issues/code-review-2026-02-14.md` 进度到中等问题 `12/15`。
- 审查单持续修复与复核收敛（2026-02-15）：完成 `docs/issues/code-review-2026-02-14.md` 第二轮修复回填，新增并验证 `C-02`（重复异常定义统一）、`H-01`（生产环境 Token 黑名单 Redis 强约束）、`H-02`（`AssetService` 去除模块级可变全局并改为构造注入）、`H-04`（PromptDashboard 详情改为 React Query）、`H-06/H-07`（测试 `any` 清理）、`H-08/H-09`（补齐模型与 operation_log 服务单元测试）、`H-10`（前端覆盖率阈值阶段提升，支持严格模式 70% 档位）、`M-03`（移除 `sys.stderr.write` 调试输出）、`M-06`（`setTimeout` 引用清理）、`M-15`（测试 key 规范化）、`L-01/L-03/L-04`（导入顺序、注释一致性与前端日志规范），并在审查单中关闭 `H-05`、`M-14`、`L-02` 迁移/误报项，更新总体进度统计。
- 审查单复核与状态一致性更新（2026-02-15）：对 `docs/issues/code-review-2026-02-14.md` 已标记项执行二次复核，新增“复核结果（2026-02-15）”区块并细化第二周高危计划状态（H-10 待办、H-11/H-12 已完成），确保修复状态与代码现状一致。
- 前端视觉一致性第2062-2111轮（2026-02-15，50轮批处理）：将 `scan-lint-disable-comments` 纳入脚本级测试覆盖，更新 `frontend/scripts/scan-lint-disable-comments.js` 新增 `--src-root` 参数（支持临时夹具）；扩展 `frontend/src/utils/__tests__/styleGuardScripts.test.ts` 增加 `scan-lint-disable-comments` 的通过/失败用例（总计 6 条用例），确保 `guard:ui` 三个子脚本均有可执行回归。验证通过：`corepack pnpm --dir frontend test:style-guards`、`corepack pnpm --dir frontend guard:ui`。
- 前端视觉一致性第2012-2061轮（2026-02-15，50轮批处理）：补充脚本门禁测试快捷入口，在 `frontend/package.json` 新增 `test:style-guards`（定向执行 `src/utils/__tests__/styleGuardScripts.test.ts`）；同步更新 `frontend/README.md` 的质量门禁命令示例。验证通过：`corepack pnpm --dir frontend test:style-guards`。
- 前端视觉一致性第1962-2011轮（2026-02-15，50轮批处理）：将前端门禁接入提交前检查，在 `.pre-commit-config.yaml` 新增本地 hook `frontend-ui-guard`（执行 `cd frontend && corepack pnpm guard:ui`，按前端相关文件触发）；并更新 `frontend/README.md` 新增“质量门禁与报告”章节，统一 `guard:ui / guard:ui:report / guard:ui:ci` 与 `make scan-frontend(-report)` 使用说明。验证通过：`cd frontend && corepack pnpm guard:ui`；注：当前环境未安装 `pre-commit`，无法执行 `pre-commit validate-config`。
- 前端视觉一致性第1912-1961轮（2026-02-15，50轮批处理）：补齐门禁脚本可测试性与回归覆盖，更新 `frontend/scripts/scan-style-px.js` 新增 `--src-root`、`--config-file` 参数，更新 `frontend/scripts/verify-token-sync.js` 新增 `--shared-tokens-file`、`--variables-css-file` 参数；新增 `frontend/src/utils/__tests__/styleGuardScripts.test.ts`，基于临时夹具覆盖 `scan-style-px` 与 `verify-token-sync` 的成功/失败路径（含 JSON 报告输出与严格失败断言）。验证通过：`corepack pnpm vitest run src/utils/__tests__/styleGuardScripts.test.ts`、`corepack pnpm guard:ui`、`corepack pnpm type-check`。
- 前端视觉一致性第1862-1911轮（2026-02-15，50轮批处理）：补充门禁命令语义说明，更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 明确 `guard:ui:report` 为“仅报告导出”、`guard:ui:ci` 为“阻塞校验 + 报告导出”；并完成双命令验证通过：`corepack pnpm guard:ui:report`、`corepack pnpm guard:ui:ci`。
- 前端视觉一致性第1812-1861轮（2026-02-15，50轮批处理）：进一步统一本地与 CI 报告行为，将 `Makefile` 的 `scan-frontend-report` 从 `guard:ui:report` 升级为 `guard:ui:ci`，确保根目录导出报告时同样执行严格阻塞校验；同步更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 说明。验收通过：`make scan-frontend-report`。
- 前端视觉一致性第1762-1811轮（2026-02-15，50轮批处理）：优化 CI 执行效率，新增 `frontend/package.json` 命令 `guard:ui:ci`（严格门禁与 JSON 报告一次执行）；更新 `.github/workflows/ci.yml` 在 `frontend-lint` 合并“阻塞校验 + 报告导出”为 `pnpm guard:ui:ci`，避免重复扫描；同步更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 命令说明。回归验证通过：`corepack pnpm guard:ui:ci`、`corepack pnpm check`。
- 前端视觉一致性第1712-1761轮（2026-02-15，50轮批处理）：补齐本地报告导出工程入口，新增 `Makefile` 目标 `scan-frontend-report`（根目录执行 `corepack pnpm guard:ui:report`），并将其加入 `PHONY/help`；同步更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 的报告命令说明。验收通过：`make scan-frontend-report`（输出 `reports/frontend/style-px-report.json`、`reports/frontend/token-sync-report.json`）。
- 前端视觉一致性第1662-1711轮（2026-02-15，50轮批处理）：继续收敛门禁命令单一来源，在 `frontend/package.json` 新增 `guard:ui:report`（聚合 `scan:px:report + verify:token-sync:report`）；同步更新 `.github/workflows/ci.yml` 的 `frontend-lint` 阻塞校验改为 `pnpm guard:ui`，报告导出改为 `pnpm guard:ui:report`，并调整 CI Summary 文案为 `guard:ui = scan:px:all-strict + verify:token-sync`；同步更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 门禁说明。回归验证通过：`corepack pnpm guard:ui`、`corepack pnpm guard:ui:report`、`make scan-frontend`。
- 前端视觉一致性第1612-1661轮（2026-02-15，50轮批处理）：统一工程入口门禁命令，更新 `Makefile` 的 `scan-frontend` 改为调用 `corepack pnpm guard:ui`，避免与 `frontend/package.json` 中 `guard:ui` 链路重复维护；并验证 `make scan-frontend` 可执行通过。
- 前端视觉一致性第1562-1611轮（2026-02-15，50轮批处理）：收敛本地/文档门禁命令口径，新增并启用 `frontend/package.json` 聚合命令 `guard:ui`（`scan:px:all-strict + verify:token-sync`），同步更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 的门禁说明改为引用 `guard:ui`，并完成回归验证 `corepack pnpm guard:ui`、`corepack pnpm audit:ui`、`corepack pnpm check` 及定向 `vitest`（`OccupancyRateChart` + `LoadingSpinner`）通过。
- 前端视觉一致性第1512-1561轮（2026-02-15，50轮批处理）：为门禁报告导出提供稳定脚本入口，新增 `frontend/package.json` 的 `scan:px:report` 与 `verify:token-sync:report`，并将 `.github/workflows/ci.yml` 报告导出步骤切换到新脚本，避免参数透传差异；同步更新 `docs/guides/frontend.md` 的报告命令示例。
- 前端视觉一致性第1462-1511轮（2026-02-15，50轮批处理）：增强门禁可观测性，扩展 `frontend/scripts/scan-style-px.js` 与 `frontend/scripts/verify-token-sync.js` 支持 `--json-file` 报告输出；更新 `.github/workflows/ci.yml` 在 `frontend-lint` 导出 `style-px-report.json`、`token-sync-report.json` 并上传到 `frontend-lint-reports`，同时同步更新前端指南与执行文档的报告说明。
- 前端视觉一致性第1412-1461轮（2026-02-15，50轮批处理）：增强 CI 汇总可见性，更新 `.github/workflows/ci.yml` 的 `CI Summary` 输出，新增“Frontend style/token guards blocking”说明，明确 `scan:px:all-strict` 与 `verify:token-sync` 处于阻塞门禁状态，便于 PR 页面快速审计质量线。
- 前端视觉一致性第1362-1411轮（2026-02-15，50轮批处理）：将本地默认检查链路与 CI/审计链路对齐，更新 `frontend/package.json` 的 `check` 脚本纳入 `scan:px:all-strict + verify:token-sync`；同步更新 `docs/guides/frontend.md` 与 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md`，明确 `pnpm check` 已包含样式与 token 门禁。
- 前端视觉一致性第1312-1361轮（2026-02-15，50轮批处理）：补齐执行文档与 CI 口径对齐，更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 回归基线，明确 `frontend-lint` 作业已执行 `scan:px:all-strict + verify:token-sync`，确保本地与 CI 门禁信息一致。
- 前端视觉一致性第1262-1311轮（2026-02-15，50轮批处理）：将样式与 token 门禁接入 CI，更新 `.github/workflows/ci.yml` 在 `frontend-lint` 作业新增 `pnpm scan:px:all-strict` 与 `pnpm verify:token-sync`，确保 PR 阶段即阻断硬编码像素回流和 token 映射漂移。
- 前端视觉一致性第1212-1261轮（2026-02-15，50轮批处理）：修复 `Makefile` 新增门禁目标的执行兼容性，将 `scan-frontend` 从 `pnpm` 调整为 `corepack pnpm`，解决 Volta 环境下 `pnpm` 不在 PATH 导致的执行失败，确保 `make scan-frontend` 可稳定通过。
- 前端视觉一致性第1162-1211轮（2026-02-15，50轮批处理）：补齐工程级执行入口，更新 `Makefile` 新增 `scan-frontend` 目标（执行 `scan:px:all-strict + verify:token-sync`）并将 `check` 目标纳入该门禁，保证非前端命令路径下也会执行样式与 token 一致性回归。
- 前端视觉一致性第1112-1161轮（2026-02-15，50轮批处理）：新增 `frontend/scripts/verify-token-sync.js`，建立 `variables.css` 与 `theme/sharedTokens.ts` 的尺度令牌一致性校验（`spacing/fontSize/borderRadius`）；在 `frontend/package.json` 新增 `verify:token-sync` 并接入 `audit:ui / audit:full / audit:full:coverage`，同步更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 与 `docs/guides/frontend.md` 的回归基线与命令说明。
- 前端视觉一致性第1062-1111轮（2026-02-15，50轮批处理）：同步前端开发文档 `docs/guides/frontend.md` 的样式示例与治理规范，新增“样式令牌与门禁约束（2026-02）”章节，明确 token 源 `rem` 策略、`scan:px*` 命令差异及 `audit:*` 默认全量零 `px` 门禁，避免后续开发回退到硬编码尺寸。
- 前端视觉一致性第1012-1061轮（2026-02-15，50轮批处理）：升级像素扫描门禁到全量模式，扩展 `frontend/scripts/scan-style-px.js` 支持 `--fail-on-token`，新增 `frontend/package.json` 命令 `scan:px:all-strict`，并将 `audit:ui / audit:full / audit:full:coverage` 默认扫描升级为全量零 `px` 校验；同步更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 回归基线说明。
- 前端视觉一致性第962-1011轮（2026-02-15，50轮批处理）：完成 token 源 `px` 清零，重构 `frontend/src/styles/variables.css` 将尺寸/圆角/阴影/断点/组件尺寸令牌统一迁移为 `rem`；同步修复 `frontend/tsconfig.json` 与 `frontend/tsconfig.paths.json` 移除已废弃 `baseUrl`（兼容 `tsgo` 新版本），并更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 基线为 token/non-token/module 三维 `px` 命中均为 `0`。
- 前端视觉一致性第912-961轮（2026-02-15，50轮批处理）：将像素扫描规则配置化，新增 `frontend/scripts/scan-style-px.config.json` 维护 token 源白名单，并更新 `frontend/scripts/scan-style-px.js` 从配置文件加载规则；同步在 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 记录配置入口，降低后续治理脚本维护成本。
- 前端视觉一致性第862-911轮（2026-02-15，50轮批处理）：对齐视觉一致性执行文档与扫描脚本实测结果，更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 增补 token 源命中基线（`58`，当前全部来自 `frontend/src/styles/variables.css`），确保后续批处理与门禁结果一致可追踪。
- 前端视觉一致性第812-861轮（2026-02-15，50轮批处理）：将 `scan:px:strict` 接入前端质量审计链路，更新 `frontend/package.json` 的 `audit:ui`、`audit:full`、`audit:full:coverage`，确保后续轮次默认包含 `module/non-token` 像素回归门禁；同步更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 回归基线说明。
- 前端视觉一致性第762-811轮（2026-02-15，50轮批处理）：继续推进 token 源单位治理，将 `frontend/src/theme/sharedTokens.ts` 的 `spacing/fontSize/borderRadius` 由 `px` 迁移为 `rem`，并同步更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 的剩余问题说明，使 token 源 `px` 关注点进一步收敛到 `frontend/src/styles/variables.css`。
- 前端视觉一致性第712-761轮（2026-02-15，50轮批处理）：进入 token 治理阶段，新增 `frontend/scripts/scan-style-px.js` 建立 `module-style / non-token-source / token-source` 三类 `px` 命中基线统计，并在 `frontend/package.json` 增加 `scan:px` 与 `scan:px:strict` 命令作为后续每轮自动验收入口；同步更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 回归基线，纳入 `pnpm scan:px:strict` 验证项。
- 主题尺度令牌去重（2026-02-15）：新增 `frontend/src/theme/sharedTokens.ts` 抽取 `spacing/fontSize/borderRadius` 共用尺度定义，并在 `frontend/src/theme/light.ts` 与 `frontend/src/theme/dark.ts` 改为复用共享令牌，降低双主题尺寸维护漂移风险。
- 主题阴影单位收敛（2026-02-15）：将 `frontend/src/theme/light.ts` 与 `frontend/src/theme/dark.ts` 的 `boxShadow` 从 `px` 表达迁移为 `rem`，减少主题 token 层硬编码像素残留并保持阴影视觉等价。
- 前端视觉一致性文档基线回填（2026-02-15）：更新 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md` 的剩余问题统计与回归基线，标记 `frontend/src/**/*.module.css` 与非 token 源文件（排除 `variables.css`、`theme/sharedTokens.ts`）`px` 命中均为 `0`，并将后续任务从“页面硬编码清理”切换为“token 源单位策略与治理”。
- 前端视觉一致性第662-711轮（2026-02-15，50轮批处理）：继续按 `ui-ux-pro-max` 收敛非 `*.module.css` 场景中的硬编码像素表达，覆盖 `frontend/src/styles/global.css`、`frontend/src/styles/common.css`、`frontend/src/components/{Router/RouteABTesting,Asset/AssetDetailInfo,Asset/AssetHistory,Common/LazyImage,Charts/OccupancyRateChart,Monitoring/SystemMonitoringHealthAlerts,Loading/__tests__/LoadingSpinner.test}.tsx`、`frontend/src/services/analyticsExportService.ts` 与 `frontend/src/themeConfig.ts`；完成焦点环/触控最小尺寸/断点/描述列宽/图表字号/报表模板样式向语义令牌、`calc(...)`、常量或 `rem` 表达迁移，并将非 token 源文件的 `px` 命中降为 `0`（保留 `variables.css` 与 `theme/light|dark.ts` 作为 token 定义来源）。
- 前端视觉一致性第612-661轮（2026-02-14，50轮批处理）：继续按 `ui-ux-pro-max` 对前端样式进行批量令牌化收口，覆盖 `frontend/src/pages/**.module.css` 与 `frontend/src/components/**.module.css` 的剩余硬编码像素表达，完成 `1/2/8/16/200/300/420/480/576/768/992/999` 等值向 `--border-width-thin / --focus-ring-* / --spacing-* / --breakpoint-* / --radius-pill / --button-height-*` 语义令牌迁移；本批包含 `Layout`、`FilenameValidator`、`ContractDetailInfo`、`PropertyCertificateUpload`、`NotificationCenter`、`RelationInfoSection`、`AssetStatusSection`、`AssetBasicInfoSection`、`AssetAreaSection`、`ActionFeedback`、`PropertyCertificateList`、`QuickActions`、`ContractImportReview`、`LoginPage`、`EmptyState`、`SearchHistoryModal` 等文件，已实现 `frontend/src/**/*.module.css` 维度 `px` 命中清零（`0`）。
- 前端视觉一致性第611轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产面积分区与操作反馈残余硬编码，重构 `frontend/src/components/Forms/Asset/AssetAreaSection.module.css` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将区块底距 `16` 与错误详情边框 `1` 统一迁移为 `--spacing-lg / --border-width-thin` 令牌表达，进一步统一表单分区节奏与反馈卡边界样式。
- 前端视觉一致性第610轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产状态与基础信息分区残余硬编码，重构 `frontend/src/components/Forms/Asset/AssetStatusSection.module.css` 与 `frontend/src/components/Forms/Asset/AssetBasicInfoSection.module.css`，将分区底距 `16` 统一迁移为 `--spacing-lg` 令牌表达，减少资产表单多分区之间的垂直节奏偏差。
- 前端视觉一致性第609轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛通知中心与续签关系分区残余硬编码，重构 `frontend/src/components/Notification/NotificationCenter.module.css` 与 `frontend/src/components/Forms/RentContract/RelationInfoSection.module.css`，将 `768` 断点与分区底距 `16` 统一迁移为 `--breakpoint-sm / --spacing-lg` 令牌表达，进一步统一通知操作区与续签分区在移动端的密度基线。
- 前端视觉一致性第608轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛合同详情与产权证上传残余硬编码，重构 `frontend/src/components/Rental/ContractDetailInfo.module.css` 与 `frontend/src/components/PropertyCertificate/PropertyCertificateUpload.module.css`，将 `768` 断点与上传图标底距 `8` 统一迁移为 `--breakpoint-sm / --spacing-sm` 令牌表达，减少详情信息区和上传组件在响应式下的局部尺寸漂移。
- 前端视觉一致性第607轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛主布局与文件名校验残余硬编码，重构 `frontend/src/components/Layout/Layout.module.css` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将 `768/576` 断点及预览/建议框边框 `1` 统一迁移为 `--breakpoint-* / --border-width-thin` 令牌表达，提升全局布局与导入校验面板在不同视口下的一致性。
- 已核实审查问题修复（2026-02-15）：补齐 Docker 部署缺失目录与默认 Nginx 配置（新增 `deployment/nginx/nginx.conf`、`deployment/nginx/ssl/.gitkeep`）；将 `backend/src/security/secret_validator.py` 中 `print` 改为结构化日志；修复 `frontend/src/hooks/useContractList.ts` 与 `frontend/src/pages/Rental/RentLedgerPage.tsx` 空 `catch` 吞错；修复 `frontend/src/utils/highlight.ts` 与 `frontend/src/pages/Contract/ContractImportStatus.tsx` 的 `index key`；为 `ContractImportStatus` 增加轮询连续失败计数与自动停止；更新 `CLAUDE.md` 测试标记列表；回填 `docs/issues/code-review-2026-02-14.md` 已修复状态并关闭 C-07 误报。
- 前端视觉一致性第606轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产导入与资产卡片残余硬编码，重构 `frontend/src/components/Asset/AssetImport.module.css` 与 `frontend/src/components/Asset/AssetCard.module.css`，将细边框 `1`、选中态边框 `2` 与 `768` 断点统一迁移为 `--border-width-thin / --focus-ring-width / --breakpoint-sm` 令牌表达，进一步统一导入结果卡与资产选择卡的视觉边界反馈。
- 前端视觉一致性第605轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛主题切换与动态路由错误态残余硬编码，重构 `frontend/src/components/Common/ThemeToggle.module.css` 与 `frontend/src/components/Router/DynamicRouteRenderer.module.css`，将边框 `1` 统一迁移为 `--border-width-thin` 令牌表达，减少基础交互组件与错误态容器的边框表达分叉。
- 前端视觉一致性第604轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产分布图与合同统计卡残余硬编码，重构 `frontend/src/components/Charts/AssetDistributionChart.module.css` 与 `frontend/src/components/Rental/ContractList/ContractStatsCards.module.css`，将明细列表高度 `300`、条目内边距 `8`、细边框 `1` 与胶囊圆角 `999` 统一迁移为 `--spacing-* / --border-width-thin / --radius-pill` 令牌表达，进一步统一图表明细区与合同指标卡的密度基线。
- 前端视觉一致性第603轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛面包屑与图表错误边界残余硬编码，重构 `frontend/src/components/Layout/AppBreadcrumb.module.css` 与 `frontend/src/components/Analytics/ChartErrorBoundary.module.css`，将焦点环 `2` 与 `768` 断点统一迁移为 `--focus-ring-* / --breakpoint-sm` 令牌表达，提升导航链路与图表异常重试操作的一致可达性反馈。
- 前端视觉一致性第602轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛产权证复核与续签摘要残余硬编码，重构 `frontend/src/components/PropertyCertificate/PropertyCertificateReview.module.css` 与 `frontend/src/components/Forms/RentContract/RenewalSummarySection.module.css`，将胶囊圆角 `999`、细边框 `1`、强调分隔线 `2`、`16/8` 间距和 `768` 断点统一迁移为 `--radius-pill / --border-width-thin / --focus-ring-width / --spacing-* / --breakpoint-sm` 令牌表达，进一步统一证照复核区与合同续签摘要区的视觉节奏。
- 前端视觉一致性第601轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛错误页与出租率图表残余硬编码，重构 `frontend/src/components/ErrorHandling/ErrorPage.module.css` 与 `frontend/src/components/Charts/OccupancyRateChart.module.css`，将焦点环 `2`、细边框 `1`、排名列表高度 `300` 以及 `576/768` 断点统一迁移为 `--focus-ring-* / --border-width-thin / --spacing-* / --breakpoint-*` 令牌表达，进一步统一错误态与图表列表区在多端的交互与密度基线。
- 系统页拆分回归补测收口（2026-02-15）：新增 `frontend/src/pages/System/__tests__/UserManagementPage.test.tsx`、`frontend/src/pages/System/__tests__/OperationLogPage.test.tsx`、`frontend/src/pages/System/__tests__/PromptDashboard.test.tsx`，补齐 `UserManagement/OperationLog/PromptDashboard` 页面级回归覆盖（含刷新、筛选计数、状态切换、详情抽屉、错误分支、加载分支）；为消除 `madge` 对 `@/*` 的解析告警，补充 `frontend/tsconfig.json` 与 `frontend/tsconfig.paths.json` 的 `baseUrl`；同步更新 `docs/pending/refactoring-backlog.md` 校准结论，并完成定向验证 `vitest`（新增 3 文件 `9 passed`，五页联合回归 `39 passed`）、`madge`（99 files，无循环依赖）、`oxlint`（0 warning/0 error）、`tsc --noEmit` 通过；补充后端 PDF 相关验证：`backend/.venv/bin/pytest -o addopts='' tests/unit/services/document/test_pdf_edge_cases.py tests/unit/services/document/extractors/test_base.py -q` 与 `backend/.venv/bin/pytest tests/unit/services/document/test_pdf_edge_cases.py tests/unit/services/document/extractors/test_base.py -q --no-cov`，均为 `86 passed`。
- 前端视觉一致性第600轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛快速洞察与页面容器残余硬编码，重构 `frontend/src/components/Dashboard/QuickInsights.module.css` 与 `frontend/src/components/Common/PageContainer.module.css`，将细边框 `1`、焦点环 `2` 与 `768` 断点统一迁移为 `--border-width-thin / --focus-ring-* / --breakpoint-sm` 令牌表达，进一步统一卡片高亮态与通用页面头部交互反馈。
- 前端视觉一致性第599轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛分析筛选器与分析看板残余硬编码，重构 `frontend/src/components/Analytics/AnalyticsFilters.module.css` 与 `frontend/src/components/Analytics/AnalyticsDashboard.module.css`，将微间距 `2`、焦点环 `2`、移动端按钮高度 `44` 与 `768` 断点统一迁移为 `--spacing-* / --focus-ring-* / --button-height-* / --breakpoint-sm` 令牌表达，进一步统一分析页筛选动作区与空态操作区在多端的交互基线。
- 前端视觉一致性第598轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛分组选项与面积统计图残余硬编码，重构 `frontend/src/components/Common/GroupedSelect.module.css` 与 `frontend/src/components/Charts/AreaStatisticsChart.module.css`，将细边框 `1`、色点尺寸 `8`、细间距偏移 `2`、明细列表高度 `300` 与 `768` 断点统一迁移为 `--border-width-thin / --spacing-* / --focus-ring-width / --breakpoint-sm` 令牌表达，进一步统一选择器与图表明细区的尺度基线。
- 前端视觉一致性第597轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛合同筛选栏与列表工具栏残余硬编码，重构 `frontend/src/components/Rental/ContractList/ContractFilterBar.module.css` 与 `frontend/src/components/Common/ListToolbar.module.css`，将按钮最小尺寸 `96/44`、细边框 `1`、焦点环 `2` 与 `767/768/992` 断点统一迁移为 `--spacing-* / --button-height-* / --border-width-thin / --focus-ring-* / --breakpoint-*` 令牌表达，进一步统一筛选操作区与通用工具栏在多端的控件密度与交互反馈。
- Docker 镜像构建精简（2026-02-15）：新增 `.dockerignore`、`backend/.dockerignore`、`frontend/.dockerignore`，剔除 `backend/.venv`、`frontend/node_modules`、测试与缓存目录，显著降低构建上下文体积；重构 `backend/Dockerfile` 依赖层（使用官方 `uv` 二进制、`uv sync --no-install-project`）并将运行时拷贝从 `COPY . .` 收敛为最小必需文件；同步优化 `frontend/Dockerfile`（升级 `node:20-alpine` 与 `pnpm@10.29.3`、移除无效 `nginx/*.conf` 拷贝、健康检查改为 `/health`、去除 `apk upgrade`），减少镜像体积并修复潜在构建失败点。
- 基础设施版本基线升级（2026-02-14）：将 `docker-compose.yml` 中运行镜像升级为 `postgres:18.2-alpine` 与 `redis:8.6-alpine`；同步更新 `AGENTS.md`、`CLAUDE.md`、`docs/guides/getting-started.md` 的版本说明，并将 `backend/tests/integration/conftest.py` 的 `PostgresContainer` 镜像从 `postgres:15` 升级到 `postgres:18.2`，确保开发/文档/集成测试版本口径一致。
- 前端质量工具链迁移（2026-02-14）：将前端类型检查、代码检查与格式化主链路切换为 `tsgo`（`@typescript/native-preview`）、`oxlint` 与 `oxfmt`；更新 `frontend/package.json`、新增 `frontend/.oxlintrc.json` 和 `frontend/.oxfmtrc.json`，移除 `.eslintrc*` 与 `.prettierrc`，并同步调整 `Makefile`、`.github/workflows/ci.yml`、`.github/scripts/calculate_quality_score.py`、`AGENTS.md` 及相关前端指南文档。为兼容 `tsgo` 同步更新 `frontend/tsconfig.json` 与 `frontend/tsconfig.paths.json` 的路径映射写法；`oxlint` 启用 `react/import/jsx-a11y` 插件并按既有项目策略关闭少量噪声规则；完成 `oxfmt` 基线格式化后恢复 `format:check` 阻塞校验。
- 前端样式检查基线校准（2026-02-14）：更新 `frontend/.stylelintrc.json`，移除 stylelint v16 已废弃规则配置（`declaration-block-order`、`color-hex-case`），并按“先可用再收敛”策略临时放宽旧样式高噪声规则（如 `selector-class-pattern`、`media-feature-range-notation` 等），恢复 `pnpm lint:css` 与 `pnpm audit:ui` 可执行通过。
- 前端样式规则收敛第1批（2026-02-14）：在 `frontend/.stylelintrc.json` 重新启用 `no-duplicate-selectors`、`declaration-block-no-redundant-longhand-properties`、`shorthand-property-no-redundant-values`；同步修复 `frontend/src/pages/Dashboard/DashboardPage.module.css` 重复选择器、`frontend/src/components/Loading/LoadingProvider.module.css` 的 `inset` 简写、`frontend/src/pages/System/SystemSettingsPage.module.css` 的 `gap` 简写以及 `frontend/src/styles/global.css` 冗余圆角写法，验证 `pnpm lint:css`、`pnpm audit:ui`、`pnpm check`、`pnpm audit:full` 全部通过。
- 前端系统页测试稳定性修复（2026-02-14）：为高耗时交互用例提升超时阈值，更新 `frontend/src/pages/System/__tests__/OrganizationPage.test.tsx`（`15s -> 30s`）与 `frontend/src/pages/System/__tests__/RoleManagementPage.test.tsx`（编辑/删除/权限配置/删除失败等 4 个关键操作用例超时统一为 `30s`），消除并发压力下的误报超时。
- 前端审计脚本分层（2026-02-14）：调整 `frontend/package.json` 中 `audit:full` 为本地全量质量检查（`lint + lint:css + tsgo + tests`），新增 `audit:full:coverage` 保留覆盖率门禁版本，避免覆盖率阈值与日常本地质量回归耦合。
- 前端调试脚本链路对齐（2026-02-14）：更新 `frontend/run_tests_debug.js`，将调试入口中的 `npx tsc --noEmit` 切换为 `npx tsgo --noEmit`，并同步调整输出标识（`TSC` -> `TSGO`），避免调试链路与正式质量门禁工具不一致。
- 前端工具链文档收口（2026-02-14）：同步更新 `frontend/README.md`、`frontend/CLAUDE.md` 与 `docs/guides/naming-conventions.md` 中遗留的 ESLint/Prettier/旧命令描述，统一为 `oxlint` / `oxfmt` / `tsgo` 与 `pnpm` 命令口径。
- 前端工具链文档补充收口（2026-02-14）：继续清理 `docs/guides/frontend.md`、`scripts/README.md` 与 `frontend/README.md` 中残留的 `npm`/ESLint/Prettier 口径，统一到 `pnpm` 与 `oxlint`/`oxfmt`/`tsgo`。
- 前端命令链路校准（2026-02-14）：同步 `ci.yml` 的 type-coverage 步骤为 `pnpm type-coverage:check`；`frontend/package.json` 中 `check/check:fix/audit:*` 继续保留 `npm run` 内部调用以兼容非全局 pnpm 场景（外层仍通过 `pnpm` 执行脚本）。
- 工具链口径清理第3批（2026-02-14）：更新 `frontend/src/utils/logger.ts`、`frontend/src/components/Asset/AssetForm.md`、`backend/scripts/documentation/generate_system_docs.py` 与 `frontend/docs` 通用指南中的残留 ESLint/Prettier/npm run 表述，统一到 Oxlint/Oxfmt/Tsgo 与 pnpm 命令口径。
- 工具链口径清理第4批（2026-02-14）：补充更新 `frontend/docs` 中历史修复总结文档（UI/UX 报告、类型修复总结、应急修复记录）的检查项与命令示例，将可执行口径统一为 Oxlint 与 pnpm。
- 工具链术语收尾（2026-02-14）：将 `docs/guides/typescript-conventions.md` 中 “单一 ESLint 规则开关” 表述调整为中性 “单一 lint 规则开关”。
- 工具链口径清理第5批（2026-02-14）：更新 `.github/CI-REFACTOR-SUMMARY.md` 的质量门禁描述，将 ESLint/Prettier 文案同步为 Oxlint/Oxfmt。
- 前端超大组件拆分 Hook 依赖校准（2026-02-14）：修复 `frontend/src/pages/System/{RoleManagement,UserManagement,OperationLog,Organization,PromptDashboard}/hooks/*` 中 `react-hooks/exhaustive-deps` 警告，收敛分页依赖为稳定原始值并稳定 `users/logs/prompts` 派生数组，确保 `oxlint --deny-warnings` 通过；并完成复核 `npm exec -- oxlint ...`（0 warning）、`npm exec -- tsc --noEmit`、`npm exec -- vitest run --testTimeout=30000 src/pages/System/__tests__/OrganizationPage.test.tsx src/pages/System/__tests__/RoleManagementPage.test.tsx`（30 passed）。
- 重构待办第2项验收收尾（2026-02-14）：更新 `docs/pending/refactoring-backlog.md`，补齐“无循环依赖/关键交互回归”两项验收结论；通过 `npx --yes madge --extensions ts,tsx --ts-config frontend/tsconfig.json --circular frontend/src/pages/System/{UserManagement,RoleManagement,OperationLog,Organization,PromptDashboard}` 验证 99 文件无环；并执行 `npm exec -- vitest run --testTimeout=30000 src/pages/System/__tests__/OrganizationPage.test.tsx src/pages/System/__tests__/RoleManagementPage.test.tsx`（`30 passed`）确认筛选/编辑/提交/刷新链路稳定。
- 前端视觉一致性第596轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛分页表格与统计卡片残余硬编码，重构 `frontend/src/components/Common/TableWithPagination.module.css` 与 `frontend/src/components/Analytics/AnalyticsStatsCard.module.css`，将边框 `1`、焦点环 `2`、胶囊圆角 `999`、趋势最小高度 `24` 及 `767/768` 断点统一迁移为 `--border-width-thin / --focus-ring-* / --radius-pill / --button-height-* / --spacing-* / --breakpoint-*` 令牌表达，进一步统一列表与分析卡片在桌面和移动端的交互反馈基线。
- 前端视觉一致性第595轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛移动布局与趋势数据卡残余硬编码，重构 `frontend/src/components/Layout/MobileLayout.module.css` 与 `frontend/src/components/Dashboard/DataTrendCard.module.css`，将细边框 `1`、焦点环 `2`、`120/140/160` 卡片最小高度及 `375/768` 断点统一迁移为 `--border-width-thin / --focus-ring-* / --button-height-* / --spacing-* / --breakpoint-*` 令牌表达，提升移动头部与仪表盘趋势卡在不同设备下的一致性。
- 前端视觉一致性第594轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛个人中心与租赁合同列表残余硬编码，重构 `frontend/src/pages/ProfilePage.module.css` 与 `frontend/src/components/Rental/ContractList/ContractTable.module.css`，将焦点环 `2`、胶囊圆角 `999`、细边框 `1` 与 `768` 断点统一迁移为 `--focus-ring-* / --radius-pill / --border-width-thin / --breakpoint-sm` 令牌表达，提升账户安全操作区与合同列表标签/操作区在桌面和移动端的一致反馈。
- 前端视觉一致性第593轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛系统设置与通知中心页残余硬编码，重构 `frontend/src/pages/System/SystemSettingsPage.module.css` 与 `frontend/src/pages/System/NotificationCenter.module.css`，将容器宽度 `1200`、胶囊圆角 `999`、细边框 `1` 与 `768` 断点统一迁移为 `--breakpoint-* / --radius-pill / --border-width-thin` 令牌表达，进一步统一系统管理页在卡片、标签与移动端布局下的样式基线。
- 前端视觉一致性第592轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛权属详情与移动菜单残余硬编码，重构 `frontend/src/components/Ownership/OwnershipDetail.module.css` 与 `frontend/src/components/Layout/MobileMenu.module.css`，将细边框 `1`、焦点环 `2`、胶囊圆角 `999` 及 `768` 断点统一迁移为 `--border-width-thin / --focus-ring-* / --radius-pill / --breakpoint-sm` 令牌表达，提升详情页与移动导航在键盘可达性与触控场景下的一致反馈。
- 前端视觉一致性第591轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛项目详情页与首页看板的残余硬编码，重构 `frontend/src/pages/Project/ProjectDetailPage.module.css` 与 `frontend/src/pages/Dashboard/DashboardPage.module.css`，将焦点环 `2`、胶囊圆角 `999`、细边框 `1` 及 `768/576` 断点统一迁移为 `--focus-ring-* / --radius-pill / --border-width-thin / --breakpoint-*` 令牌表达，进一步统一详情页与仪表盘在交互反馈和响应式边界上的样式基线。
- 前端 Decimal/Number 链路统一收口（2026-02-14）：补充 `frontend/CLAUDE.md` 的“服务层统一转换”规范，明确 `convertBackendToFrontend/convertFrontendToBackend` 使用边界与页面层禁用 `parseFloat` 兜底策略；并完成定向回归 `npm exec -- vitest run src/utils/__tests__/dataConversion.test.ts src/services/__tests__/assetService.test.ts src/services/__tests__/rentContractService.test.ts src/services/__tests__/analyticsService.test.ts`（`129 passed`），验证高频面积/金额字段转换链路稳定。
- PDF 文档处理校准（2026-02-14）：更新 `backend/tests/unit/services/document/test_pdf_edge_cases.py` 顶部过期说明，移除“模块未实现/测试跳过”的历史表述并明确当前实现语义；同步在 `docs/guides/backend.md` 补充 `pdf_to_images` 渲染后端策略（PyMuPDF 优先、`pdf2image` 回退、双缺失时报错），使测试注释与开发文档和实现状态对齐。
- 前端超大组件拆分第5批（2026-02-14）：将 `frontend/src/pages/System/PromptDashboard.tsx` 从单文件重构为目录化结构 `frontend/src/pages/System/PromptDashboard/`（`index.tsx` 容器 + `hooks/usePromptDashboardData.ts` + `components/*` + `types.ts/constants.tsx/utils.ts`），拆分筛选区、统计概览、模板详情、趋势图、字段错误率与优化建议模块，保留刷新/筛选/模板切换与状态展示语义；`PromptDashboard.tsx` 改为兼容导出入口（显式 `./PromptDashboard/index`），并通过 type-check 与定向 eslint 复核。
- 前端超大组件拆分第4批（2026-02-14）：将 `frontend/src/pages/System/OrganizationPage.tsx` 从单文件重构为目录化结构 `frontend/src/pages/System/Organization/`（`index.tsx` 容器 + `hooks/useOrganizationData.ts` + `components/*` + `types.ts/constants.tsx/utils.tsx`），拆分组织统计卡、列表视图、树形视图、组织表单弹窗与历史记录弹窗，保留搜索/分页/新建编辑删除/历史查看/树形刷新交互语义；`OrganizationPage.tsx` 改为兼容导出入口，并通过 type-check 与定向 eslint 复核。
- 前端超大组件拆分第3批（2026-02-14）：将 `frontend/src/pages/System/OperationLogPage.tsx` 从单文件重构为目录化结构 `frontend/src/pages/System/OperationLog/`（`index.tsx` 容器 + `hooks/useOperationLogData.ts` + `components/*` + `types.ts/constants.tsx/utils.ts`），拆分操作日志统计卡、筛选工具栏、日志表格、详情抽屉与标签渲染逻辑，保留筛选/分页/详情查看/刷新交互语义；`OperationLogPage.tsx` 改为兼容导出入口，并通过 type-check 与定向 eslint 复核。
- 前端视觉一致性第590轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产分析页与租金统计页断点/标签硬编码，重构 `frontend/src/pages/Assets/AssetAnalyticsPage.module.css` 与 `frontend/src/pages/Rental/RentStatisticsPage.module.css`，将 `575/576/768/992/1199/1399/1400` 媒体查询、标签胶囊圆角 `999` 及细边框 `1` 统一迁移为 `--breakpoint-* / --border-width-thin / --radius-pill` 令牌表达，减少分析看板与统计列表在不同视口下的样式分叉。
- 前端视觉一致性第589轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛提示词看板与枚举字段页残余硬编码，重构 `frontend/src/pages/System/PromptDashboard.module.css` 与 `frontend/src/pages/System/EnumFieldPage.module.css`，将筛选与趋势区焦点环 `2`、细边框 `1`、胶囊圆角 `999`、`320/220` 选择器宽度以及 `768` 断点统一迁移为 `--focus-ring-* / --border-width-thin / --radius-pill / --spacing-* / --breakpoint-sm` 令牌表达，进一步降低系统页高频标签与筛选控件的视觉尺度漂移。
- 前端状态管理重构第1批（2026-02-14）：新增 `frontend/src/hooks/useDictionaryManagement.ts` 统一封装字典管理场景的 React Query 查询与 mutation（含失效策略），并重构 `frontend/src/pages/System/DictionaryPage.tsx`、`frontend/src/pages/System/EnumFieldPage.tsx`，移除页面内 `fetch* + useEffect + 手动 loading` 请求链，改为 query/mutation 驱动的字典类型、枚举值、统计与增删改流程，保持原有交互与表格分页行为。
- 前端状态管理重构第1批补全（2026-02-14）：重构 `frontend/src/components/Asset/AssetImport.tsx` 主导入流程为 `useMutation` 生命周期驱动（`onMutate/onSuccess/onError`），用 `mutation.isPending` 替代页面级 `uploading` 状态，补充导入成功后的 `assets/asset-stats` 缓存失效与进度定时器清理，保持模板下载、步骤切换、错误摘要与结果展示行为不变；并通过 `AssetImport` 组件定向回归测试。
- 前端超大组件拆分第1批（2026-02-14）：将 `frontend/src/pages/System/UserManagementPage.tsx` 从单文件重构为目录化结构 `frontend/src/pages/System/UserManagement/`（`index.tsx` 容器 + `hooks/useUserManagementData.ts` + `components/*` + `types.ts/status.ts`），把统计卡片、筛选工具栏、用户表格、编辑表单、详情抽屉拆为独立组件并保留原交互语义；`UserManagementPage.tsx` 改为兼容导出入口，容器文件收敛至 `348` 行并通过 type-check 与 eslint 复核。
- 前端超大组件拆分第2批（2026-02-14）：将 `frontend/src/pages/System/RoleManagementPage.tsx` 从单文件重构为目录化结构 `frontend/src/pages/System/RoleManagement/`（`index.tsx` 容器 + `hooks/useRoleManagementData.ts` + `components/*` + `types.ts/constants.tsx/utils.tsx`），拆分角色统计卡、筛选工具栏、角色表格、角色表单与权限配置弹窗，保留筛选/分页/启停/删除/权限分配交互语义；`RoleManagementPage.tsx` 改为兼容导出入口，容器文件收敛至 `301` 行并通过 type-check 与 eslint 复核。
- 前端 Decimal/Number 链路统一第2批（2026-02-14）：扩展 `frontend/src/utils/dataConversion.ts` 的金额/面积字段转换清单（新增 `overall_occupancy_rate` 与多项台账/汇总字段），并在 `frontend/src/services/asset/assetCoreService.ts`、`frontend/src/services/asset/assetStatisticsService.ts`、`frontend/src/services/analyticsService.ts`、`frontend/src/services/rentContractService.ts` 统一服务层返回前的 `convertBackendToFrontend` 转换出口；补充 `frontend/src/services/__tests__/assetService.test.ts`、`frontend/src/services/__tests__/rentContractService.test.ts`、`frontend/src/services/__tests__/analyticsService.test.ts`、`frontend/src/utils/__tests__/dataConversion.test.ts`，新增 Decimal 字符串转 number 与导出 Blob 原样返回的回归覆盖。
- 前端视觉一致性第588轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛组织管理与操作日志页交互细节，重构 `frontend/src/pages/System/OrganizationPage.module.css` 与 `frontend/src/pages/System/OperationLogPage.module.css`，将搜索/详情区固定高度 `300`、标签胶囊圆角 `999`、边框 `1`、焦点环 `2` 与 `768` 断点硬编码统一替换为 `--spacing-* / --radius-pill / --border-width-thin / --focus-ring-* / --breakpoint-sm` 令牌表达，提升系统管理页在表格与抽屉高频操作场景下的一致性。
- 前端视觉一致性第586轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛模板管理页与登录页的残余硬编码，重构 `frontend/src/pages/System/TemplateManagementPage.module.css` 与 `frontend/src/pages/LoginPage.module.css`，将模板字段区尺寸 `220/180`、标签胶囊圆角 `999`、边框 `1`、焦点环 `2` 以及登录页渐变点/边框/焦点环中的 `1/2` 固定值统一替换为 `--spacing-* / --radius-pill / --border-width-thin / --focus-ring-*` 语义令牌，提升系统页与认证页交互状态的一致性。
- 前端视觉一致性第587轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛权属列表与响应式表格移动端样式，重构 `frontend/src/components/Ownership/OwnershipList.module.css` 与 `frontend/src/components/Common/ResponsiveTable.module.css`，将焦点环 `2`、边框 `1`、移动端头部补偿 `2` 以及 `767/768` 断点中的硬编码统一迁移为 `--focus-ring-* / --border-width-thin / --breakpoint-sm` 令牌表达，提升列表密集区在桌面与移动端的一致性交互反馈。
- 前端视觉一致性第584轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛系统角色页与分析筛选器的细粒度硬编码，重构 `frontend/src/pages/System/RoleManagementPage.module.css` 与 `frontend/src/components/Analytics/Filters/Filters.module.css`，将角色树/穿梭框尺寸 `220/360/280/400`、胶囊圆角 `999`、边框 `1`、焦点环 `2` 与筛选历史日期微间距 `2` 统一替换为 `--button-height-* / --spacing-* / --radius-pill / --border-width-thin / --focus-ring-*` 语义令牌，进一步降低系统页高频交互区的视觉尺度漂移。
- 前端视觉一致性第585轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛提示词列表与用户管理页的交互硬编码，重构 `frontend/src/pages/System/PromptListPage.module.css` 与 `frontend/src/pages/System/UserManagementPage.module.css`，将焦点环 `2`、胶囊圆角 `999` 与细边框 `1` 统一替换为 `--focus-ring-* / --radius-pill / --border-width-thin` 语义令牌，提升系统列表密集区在按钮、标签与表格容器上的交互一致性。
- 重构待办清单校准与重排（2026-02-14）：重写 `docs/pending/refactoring-backlog.md`，基于当前代码基线修正过期项（移除将 `OrganizationPage` 归类为 React Query 反模式、将 PDF 任务从“实现模块”改为“测试与注释校准”），并将 `Decimal/Number` 转换提升为中优先级，更新执行顺序为“先状态管理统一，再类型转换链路，再超大组件拆分”，补充 `PromptDashboard.tsx` 拆分目标与更可验证的验收标准。
- 前端视觉一致性第583轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛加载骨架与通知中心尺寸表达，重构 `frontend/src/components/Loading/SkeletonLoader.module.css`、`frontend/src/components/Loading/SkeletonLoader.tsx` 与 `frontend/src/components/Notification/NotificationCenter.module.css`，为骨架占位新增局部语义尺寸变量并将通知面板 `400/500`、焦点环 `2`、胶囊圆角 `999`、细边框 `1` 全量迁移到 `--button-height-* / --spacing-* / --focus-ring-* / --radius-pill / --border-width-thin` 令牌，减少高可见反馈区的硬编码尺寸漂移。
- 前端视觉一致性第582轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛分析页与主布局的细粒度尺寸令牌，重构 `frontend/src/pages/Assets/AssetAnalyticsPage.module.css`、`frontend/src/components/Layout/Layout.module.css` 与 `frontend/src/styles/variables.css`，将分析网格最小列宽与错误提示最大宽度、布局层 `1/2/999px` 的边框/焦点环/胶囊圆角统一替换为语义令牌（`--button-height-* / --border-width-thin / --focus-ring-* / --radius-pill`）驱动，降低高频页面的局部硬编码漂移。
- 前端视觉一致性执行文档补充（2026-02-14）：新增 `docs/ui-ux-prompts-and-remaining-issues-2026-02-14.md`，沉淀可复用执行提示语（连续轮次/优先级/验收导向）与当前剩余问题清单（`343` 处 `px` 命中、`76` 个文件、优先级 Top 文件），用于后续持续修复对齐。
- 前端视觉一致性第581轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛项目选择器辅助信息字号，重构 `frontend/src/components/Project/ProjectSelect.module.css`，将项目简称与编码说明字号 `12` 统一替换为 `--font-size-xs` 语义令牌，提升项目下拉组件与其它字典选择器的文本层级一致性。
- 前端视觉一致性第580轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产接收分区表单间距，重构 `frontend/src/components/Forms/Asset/AssetReceptionSection.module.css`，将区块底距、上传提示上间距/字号与附件列表顶距统一替换为 `--spacing-* / --font-size-*` 语义令牌，提升资产表单各分区节奏一致性。
- 前端视觉一致性第579轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产明细分区表单间距与提示文本，重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.module.css`，将区块底距、信息图标偏移、上传提示上间距/字号与终止合同列表顶距统一替换为 `--spacing-* / --font-size-*` 语义令牌，降低资产创建流程中的局部密度偏差。
- 前端视觉一致性第578轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛出租率图表趋势图标与排行条目间距，重构 `frontend/src/components/Charts/OccupancyRateChart.module.css`，将趋势图标字号与排行项纵向内边距统一替换为 `--font-size-* / --spacing-*` 语义令牌，提升图表区与统计列表区的节奏一致性。
- 前端视觉一致性第577轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛趋势数据卡图标尺寸与悬浮位移，重构 `frontend/src/components/Dashboard/DataTrendCard.module.css`，将图标 `20/40/32/16` 尺寸、悬浮位移 `-2` 与统计值字重统一替换为 `--font-size-* / --button-height-* / --spacing-* / --font-weight-*` 语义令牌，提升仪表盘趋势卡在不同断点下的交互一致性。
- 前端视觉一致性第576轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产导入流程的图标与按钮尺寸表达，重构 `frontend/src/components/Asset/AssetImport.module.css`，将页面按钮高度、段落尾间距、导入/执行图标 `48/64`、执行元信息间距与结果操作按钮 `44` 高度统一替换为 `--button-height-* / --spacing-*` 语义令牌计算，提升导入流程关键状态区与全局按钮体系一致性。
- 前端视觉一致性第575轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产历史时间线的间距与文字层级，重构 `frontend/src/components/Asset/AssetHistory.module.css`，将筛选卡间距、统计字号、时间线区块 `8/16/24` 节奏与关键文案字重统一替换为 `--spacing-* / --font-size-* / --font-weight-*` 语义令牌，提升资产历史区与详情页信息层级一致性。
- 前端视觉一致性第574轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛面包屑导航的触控高度与细粒度间距，重构 `frontend/src/components/Layout/AppBreadcrumb.module.css`，将底部补偿 `2`、链接间距偏移与 `40/44` 触控高度统一替换为 `--spacing-* / --button-height-*` 语义令牌计算，提升页面头部导航在桌面与移动端的一致性。
- 前端视觉一致性第573轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛图表错误边界组件尺寸，重构 `frontend/src/components/Analytics/ChartErrorBoundary.module.css`，将容器内边距 `20`、错误提示间距 `12`、细节文案字号 `12` 与重试按钮 `36/44` 高度统一替换为 `--spacing-* / --font-size-* / --button-height-*` 语义令牌，提升分析页异常态与全局反馈组件的尺度一致性。
- 前端视觉一致性第572轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛租赁合同列表状态标签与行内操作按钮尺寸，重构 `frontend/src/components/Rental/ContractList/ContractTable.module.css`，将状态说明字号、状态标签内边距/行高、操作组间距与操作按钮 `32/40` 尺寸统一替换为 `--font-size-* / --spacing-* / --button-height-*` 语义令牌，提升租赁列表与系统列表页的交互密度一致性。
- 前端视觉一致性第571轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛页面容器头部返回按钮与扩展操作区尺寸，重构 `frontend/src/components/Common/PageContainer.module.css`，将返回按钮与扩展按钮 `40/44` 尺寸及字号统一替换为 `--button-height-* / --font-size-* / --spacing-*` 语义令牌计算，提升通用页面头部在桌面与移动端的触控一致性。
- 前端视觉一致性第570轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛移动端菜单入口与抽屉菜单项尺寸，重构 `frontend/src/components/Layout/MobileMenu.module.css`，将触发按钮/关闭按钮及菜单项 `44/8/4` 等固定值统一替换为 `--button-height-* / --spacing-* / --radius-* / --font-size-*` 语义令牌，提升移动导航区与全局按钮体系的一致性。
- 前端视觉一致性第569轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛项目表单中的权属分区间距与标签尺寸，重构 `frontend/src/components/Forms/ProjectForm.module.css`，将分区顶距、标题底距、标签 `padding`、标签字号与操作区顶距统一替换为 `--spacing-* / --font-size-* / --font-weight-*` 语义令牌，提升项目表单与系统表单页的布局节奏一致性。
- 前端视觉一致性第568轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛资产详情信息组件尺寸表达，重构 `frontend/src/components/Asset/AssetDetailInfo.module.css`，将卡片间距、标题/标签图标边距、资产名称字号、出租率分区间距、摘要区最小宽度与附件条目间距统一替换为 `--spacing-* / --font-size-* / --font-weight-*` 语义令牌，降低资产详情页与列表页的信息密度漂移。
- 前端视觉一致性第567轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛仪表盘快速洞察卡片交互与图标尺寸，重构 `frontend/src/components/Dashboard/QuickInsights.module.css`，将图标字号 `18/16`、悬浮位移 `-2` 与移动端图标内边距微调统一替换为 `--font-size-* / --spacing-*` 语义令牌计算，提升首页洞察卡与其它卡片组件的交互节奏一致性。
- 前端视觉一致性第566轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛产权证复核组件状态标签与操作按钮尺寸，重构 `frontend/src/components/PropertyCertificate/PropertyCertificateReview.module.css`，将状态文案字号、标签内边距/行高、列表项内边距、告警折叠间距、激活圆角与提交按钮最小高度统一替换为 `--font-size-* / --spacing-* / --radius-* / --button-height-*` 语义令牌，提升证书复核流程与系统页状态标签的视觉一致性。
- 前端视觉一致性第565轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛系统监控组件间距与图标字号，重构 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.module.css`、`SystemMonitoringDetails.module.css` 与 `SystemMonitoringHeader.module.css`，将 `16/20/24` 固定值统一替换为 `--spacing-* / --font-size-*` 语义令牌，提升监控页头部、健康态与详情区的节奏一致性。
- 前端视觉一致性第564轮（2026-02-14）：继续按 `ui-ux-pro-max` 收敛项目详情页操作按钮触控尺寸，重构 `frontend/src/pages/Project/ProjectDetailPage.module.css`，将编辑按钮与资产跳转按钮的 `44/40` 高度统一替换为 `--button-height-* / --spacing-*` 语义令牌计算，提升项目详情页主操作与列表页按钮触控面一致性。
- 前端视觉一致性第563轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛资产面积汇总组件空态内边距，重构 `frontend/src/components/Asset/AssetAreaSummary.module.css`，将空态容器 `20` 内边距替换为 `--spacing-*` 语义令牌计算，保持资产模块空态区间距节奏一致。
- 前端视觉一致性第562轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛移动端布局与响应式表格控件尺寸，重构 `frontend/src/components/Layout/MobileLayout.module.css` 与 `frontend/src/components/Common/ResponsiveTable.module.css`，将 `56/44/40/36/24` 等移动端头部与表格卡片控件尺寸统一替换为 `--spacing-* / --button-height-* / --font-size-*` 语义令牌变量，提升小屏导航区与卡片字段操作区的一致性。
- 前端视觉一致性第561轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛提示词看板基础尺寸变量，重构 `frontend/src/pages/System/PromptDashboard.module.css`，将加载区高度 `360` 与趋势卡最小宽度 `108/124` 统一改写为 `--spacing-*` 语义令牌计算，降低系统分析页在不同密度配置下的硬编码耦合。
- 前端视觉一致性第560轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛租金台账页状态标签与行内操作尺寸，重构 `frontend/src/pages/Rental/RentLedgerPage.module.css`，将状态标签行高 `18` 与表格行内操作按钮 `32/32` 统一替换为 `--font-size-* / --button-height-sm` 语义令牌变量，提升台账表格与全局列表操作区的尺寸一致性。
- 前端视觉一致性第559轮（2026-02-13）：继续按 `ui-ux-pro-max` 清理项目详情页状态徽标微间距硬编码，重构 `frontend/src/pages/Project/ProjectDetailPage.module.css`，将状态徽标起始间距 `2` 替换为 `--spacing-xs/2` 令牌计算，统一详情页标题行内元素间距表达。
- 前端视觉一致性第558轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛首页资产图表组件尺寸，重构 `frontend/src/pages/Dashboard/components/AssetChart.module.css`，将加载区最小高度 `180`、图例色块尺寸 `12` 与圆角 `2` 统一替换为 `--spacing-*` 语义令牌变量，减少图表区与其它看板卡片的尺寸表达差异。
- 前端视觉一致性第557轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛登录页剩余控件尺寸表达，重构 `frontend/src/pages/LoginPage.module.css`，将登录 Logo/图标 `64/32`、输入与按钮高度 `44`、字号 `base+1` 与按钮悬浮位移 `-1` 统一替换为 `--spacing-* / --button-height-* / --font-size-*` 语义令牌变量，减少认证页微交互与全局表单规范偏差。
- 前端视觉一致性第556轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛首页看板卡片悬浮与提示层尺寸表达，重构 `frontend/src/pages/Dashboard/DashboardPage.module.css`，将统计卡阴影、悬浮位移 `-2`、提示最大宽度 `200` 与悬浮边框强调色表达统一改为语义令牌变量，提升首页卡片交互反馈一致性。
- 前端视觉一致性第555轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛测试看板页文本细间距，重构 `frontend/src/pages/TestCoverageDashboard.module.css`，将次级文本左间距 `4` 替换为 `--spacing-xs` 语义令牌，减少统计卡文案对齐中的零散像素表达。
- 前端视觉一致性第554轮（2026-02-13）：继续按 `ui-ux-pro-max` 清理权属详情页标签行间距硬编码，重构 `frontend/src/pages/Ownership/OwnershipDetailPage.module.css`，将标签图标与文本间距 `6` 统一替换为 `--spacing-*` 令牌计算，保持系统详情页页签头部的间距表达一致。
- 前端视觉一致性第553轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛个人中心页头像与操作按钮尺寸，重构 `frontend/src/pages/ProfilePage.module.css`，将头像容器 `80`、头像图标 `32` 与操作按钮高度 `44` 统一替换为 `--spacing-* / --button-height-*` 语义令牌变量，提升资料页与系统页控件尺度一致性。
- 前端视觉一致性第552轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛资产卡片高频展示样式，重构 `frontend/src/components/Asset/AssetCard.module.css` 与 `frontend/src/components/Asset/AssetCard.tsx`，将 `16/12/8/4` 等卡片间距与字号硬编码替换为 `--spacing-* / --font-size-*` 语义令牌，并移除面积统计区重复内联字号样式，统一资产卡片信息密度。
- 前端视觉一致性第551轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛资产分析页内容高度与强调边框表达，重构 `frontend/src/pages/Assets/AssetAnalyticsPage.module.css`，将主内容高度 `400/300` 与分布项左边框 `3` 统一替换为 `--spacing-*` 语义令牌计算，减少分析页卡片状态在不同密度下的尺度偏差。
- 前端视觉一致性第550轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛主布局标题与内容区尺寸令牌，重构 `frontend/src/components/Layout/Layout.module.css`，将标题字距 `-0.5`、用户名宽度 `120` 与内容区高度偏移 `112` 统一改写为 `--spacing-*` 组合变量，提升布局层文字与视口占位的一致性。
- 前端视觉一致性第549轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛分析统计卡片交互尺寸与动效时长，重构 `frontend/src/components/Analytics/AnalyticsStatsCard.module.css`，将卡片过渡 `0.2s`、图标徽章 `40/36/18/16` 与移动端数值字号 `22` 统一替换为 `--transition-base / --button-height-* / --font-size-* / --spacing-*` 语义令牌表达。
- 前端视觉一致性第548轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛分析面板空态与操作区尺寸，重构 `frontend/src/components/Analytics/AnalyticsDashboard.module.css`，将操作按钮高度 `36`、空态纵向内边距 `60` 与描述下边距 `16` 统一替换为语义令牌变量，减少分析页在桌面与移动端的节奏漂移。
- 前端视觉一致性第547轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛仪表盘快捷操作卡片尺寸，重构 `frontend/src/pages/Dashboard/components/QuickActions.module.css`，将卡片高度 `120/132` 与图标字号 `24` 改为 `--spacing-* / --font-size-xxxl` 令牌计算，提升首页快捷区在不同断点下的视觉一致性。
- 前端视觉一致性第546轮（2026-02-13）：继续按 `ui-ux-pro-max` 清理模板管理页细粒度偏移硬编码，重构 `frontend/src/pages/System/TemplateManagementPage.module.css`，将模板图标垂直偏移 `3` 改写为 `--spacing-*` 令牌计算，统一系统页图标微调表达方式。
- 前端视觉一致性第545轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛租赁合同创建页尺寸与间距表达，重构 `frontend/src/pages/Rental/ContractCreatePage.module.css`，将成功提示/引导区/底部操作区中的 `16/24/12/6/20/4/132/44` 等硬编码统一替换为 `--spacing-* / --font-size-* / --button-height-*` 语义令牌变量，提升合同创建流程在桌面与移动端的节奏一致性。
- 前端视觉一致性第544轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛系统通知中心卡片与标签尺寸，重构 `frontend/src/pages/System/NotificationCenter.module.css`，将通知主卡最小高度 `420` 与标签行高 `base+5` 统一改写为语义令牌变量，减少系统通知页与其它系统列表页的密度偏差。
- 前端视觉一致性第543轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛登录页容器与焦点环尺寸，重构 `frontend/src/pages/LoginPage.module.css`，将登录卡片容器宽度 `420` 与输入焦点环宽度 `3` 改为 `--spacing-*` 语义令牌计算表达，降低认证页与其它表单页的尺寸体系偏差。
- 前端视觉一致性第542轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛系统设置页交互尺寸，重构 `frontend/src/pages/System/SystemSettingsPage.module.css`，将操作按钮最小宽度 `132` 与状态标签行高 `base+5` 统一改为语义令牌变量，提升系统管理页面按钮与状态标签的垂直节奏一致性。
- 前端视觉一致性第541轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛动态路由错误态容器尺寸，重构 `frontend/src/components/Router/DynamicRouteRenderer.module.css`，将错误容器/边界容器的 `50/20` 内边距、`20` 外边距与 `6` 圆角统一替换为 `--spacing-* / --button-radius` 语义令牌，减少路由异常态与全局错误组件的视觉断层。
- 前端视觉一致性第540轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛租赁详情描述区列宽体系，重构 `frontend/src/components/Rental/ContractDetailInfo.module.css`，将标签/内容列宽 `130/180` 与移动端 `110/140` 固定值统一替换为基于 `--spacing-*` 的语义令牌变量，减少合同详情在桌面与移动端的信息栅格跳变。
- 前端视觉一致性第539轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛通用选择器与错误边界细节尺寸，重构 `frontend/src/components/Common/GroupedSelect.module.css` 与 `frontend/src/components/ErrorHandling/ErrorBoundary.module.css`，将选中标签右间距 `3` 与错误页容器宽度上限 `800` 统一改写为 `--spacing-*` 语义令牌表达，减少零散像素硬编码残留。
- 前端视觉一致性第538轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛资产入口页顶部留白与筛选区对齐，重构 `frontend/src/App.module.css` 与 `frontend/src/pages/Assets/components/AssetFilters.module.css`，将 Suspense 回退区上边距 `100` 与筛选重置操作上边距 `30` 统一替换为 `--spacing-*` 语义令牌计算，减少主应用加载态与资产筛选表单的垂直节奏偏差。
- 前端视觉一致性第537轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛通用加载/错误骨架尺寸，重构 `frontend/src/components/Common/ErrorState.module.css` 与 `frontend/src/components/Common/Loading.module.css`，将页面最小高度 `400`、错误图标 `72`、技术详情宽度上限 `600` 统一替换为 `--spacing-*` 组合语义令牌，减少全页加载态与错误态之间的框架尺寸割裂。
- 前端视觉一致性第536轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛分页组件交互尺寸，重构 `frontend/src/components/Common/TableWithPagination.module.css`，将分页按钮/输入高度 `40/44`、行高 `38/42`、圆角 `8` 与宽度 `72` 统一替换为 `--button-height-* / --spacing-* / --radius-md` 语义令牌计算，减少桌面与移动端分页控件密度差异。
- 前端视觉一致性第535轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛列表工具栏控件尺寸，重构 `frontend/src/components/Common/ListToolbar.module.css`，将输入与按钮高度/宽度 `40/44` 及选择器行高 `38/42` 统一替换为基于 `--button-height-lg` 的语义令牌变量，提升系统页筛选条在不同断点下的一致触控反馈。
- 前端视觉一致性第534轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛通用错误展示组件尺寸，重构 `frontend/src/components/Common/FriendlyErrorDisplay.module.css`，将五类错误图标字号 `48`、错误详情区高度上限 `200` 与建议面板宽度上限 `600` 统一替换为 `--spacing-*` 语义令牌计算，并合并重复图标尺寸规则，减少错误态页面的样式重复与尺度漂移。
- 前端视觉一致性第533轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛通用空态组件尺寸，重构 `frontend/src/components/Common/EmptyState.module.css`，将容器最小高度 `300`、空态图标 `64`、图片上限 `200/150` 与内容最大宽度 `480` 统一改为 `--spacing-*` 组合的语义令牌变量，降低列表页与弹窗空态在不同断点下的尺寸漂移。
- 前端视觉一致性第532轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛反馈组件细节尺寸，重构 `frontend/src/components/Feedback/ActionFeedback.module.css`、`ConfirmDialog.module.css`、`EmptyState.module.css`，将错误详情区高度 `100`、确认图标字号 `24` 与空态图标字号 `64` 统一替换为 `--spacing-* / --font-size-xxxl` 语义令牌计算，降低反馈态弹层与卡片空态的尺度漂移。
- 前端视觉一致性第531轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛监控与字典预览组件的细粒度样式，重构 `frontend/src/components/PerformanceMonitor.module.css` 与 `frontend/src/components/Dictionary/EnumValuePreview.module.css`，将浮动按钮偏移、层级、标签字号/圆角/内边距中的硬编码尺寸统一替换为 `--spacing-* / --font-size-* / --button-radius / --z-index-*` 语义令牌，减少跨页面浮层入口与枚举标签密度不一致问题。
- 前端视觉一致性第530轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛全局加载组件尺寸体系，重构 `frontend/src/components/Loading/LoadingSpinner.module.css`，将图标字号 `24/18/14`、提示字号 `16/14/12` 与容器内边距 `50/20` 统一替换为 `--font-size-* / --spacing-*` 令牌计算，减少加载态在不同页面中的视觉割裂。
- 前端视觉一致性第529轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛错误页空态组件尺寸，重构 `frontend/src/components/ErrorHandling/ErrorPage.module.css`，将 `50/20/500/56/16/8/14/44/32/48` 等间距与尺寸统一替换为 `--spacing-* / --font-size-base / --button-height-*` 语义令牌计算，提升错误页在不同场景与断点下的视觉一致性。
- 前端视觉一致性第528轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛产权证导入页移动端卡片间距，重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateImport.module.css`，将移动端 `ant-card-body` 的 `16 12` 固定内边距替换为 `--spacing-lg / --spacing-md` 语义令牌变量，保持导入流程在不同页面的移动端卡片密度一致。
- 前端视觉一致性第527轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛租赁统计页筛选与标签密度，重构 `frontend/src/pages/Rental/RentStatisticsPage.module.css`，将筛选控件宽度 `300/120`、操作按钮最小宽度 `88`、状态标签行高 `18` 与移动端按钮高度 `40/44` 的表达统一为页面级语义令牌计算，降低租赁统计页与其它统计页的控件尺度偏差。
- 前端视觉一致性第526轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛权属详情组件细节尺寸，重构 `frontend/src/components/Ownership/OwnershipDetail.module.css`，将编辑按钮高度 `44` 与项目名称行间距 `6` 统一替换为组件级语义令牌计算，降低权属详情页与列表页之间的控件密度落差。
- 前端视觉一致性第525轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛权属列表组件交互尺寸，重构 `frontend/src/components/Ownership/OwnershipList.module.css`，新增组件级尺寸令牌统一工具栏按钮 `40/44`、选择按钮 `32`、图标按钮 `40/44` 与状态开关最小宽度 `48` 的表达，减少权属列表在桌面/移动端触控目标不一致问题。
- 前端视觉一致性第524轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛字典下拉复用组件样式，重构 `frontend/src/components/Dictionary/DictionarySelect.module.css`，将选项间距 `8`、颜色色块尺寸 `12` 与圆角 `2` 统一替换为 `--spacing-*` 令牌计算，提升字典相关页面色块与文本对齐一致性。
- 前端视觉一致性第523轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛权属/项目管理页加载空态样式，重构 `frontend/src/pages/Ownership/OwnershipManagementPage.module.css` 与 `frontend/src/pages/Project/ProjectManagementPage.module.css`，将空态内边距 `60` 与文案上间距 `16` 统一替换为 `--spacing-*` 令牌计算，减少管理页空态与主列表页间距节奏差异。
- 前端视觉一致性第522轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛产权证列表页控件尺度，重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateList.module.css` 与 `frontend/src/pages/PropertyCertificate/PropertyCertificateList.tsx`，新增页面级尺寸令牌统一搜索框宽度 `300`、新建按钮最小宽度 `120`、状态标签行高 `18`、行内操作高度 `32/40/44` 与移动端筛选卡间距 `16`，减少证书页与系统列表页在交互密度上的偏差。
- 前端视觉一致性第521轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛合同导入上传页细节间距，重构 `frontend/src/pages/Contract/ContractImportUpload.module.css`，将使用说明列表 `gap` 中的 `-2px` 偏移统一替换为语义令牌计算（`--spacing-sm - --spacing-xs/2`），避免导入页局部列表节奏偏离全局间距体系。
- 前端视觉一致性第520轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛导入页高度令牌表达，重构 `frontend/src/pages/Contract/PDFImportPage.module.css`，将加载区/移动端加载区/标签内容区的 `320/260/200` 固定高度统一替换为基于 `--spacing-xxl` 与 `--spacing-xs/sm` 的语义化 `calc` 令牌，减少导入页在不同密度配置下的硬编码耦合。
- 前端视觉一致性第519轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛 PDF 导入子组件样式，重构 `frontend/src/components/Contract/PDFImport/ActionButtons.module.css`、`ImportOptionsPanel.module.css`、`SystemInfoDisplay.module.css`、`UploadStatsPanel.module.css`、`PDFUploadArea.module.css`，将 `16/14/48` 等残留尺寸表达统一替换为 `--spacing-lg / --font-size-base / --spacing-xxxl` 令牌，提升导入配置区与上传区的密度一致性。
- 前端视觉一致性第518轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛导入历史标签页空态样式，重构 `frontend/src/pages/Contract/components/SessionHistoryTab.module.css`，将空态区内边距 `40`、图标间距 `16`、图标尺寸 `48` 与历史卡片间距 `8` 统一替换为语义令牌计算，减少历史页与导入主流程的视觉断层。
- 前端视觉一致性第517轮（2026-02-13）：继续按 `ui-ux-pro-max` 清理合同文件名校验组件尾部硬编码，重构 `frontend/src/components/Contract/FilenameValidator.module.css`，将建议列表与问题列表的缩进 `20` 统一替换为语义令牌计算（`--spacing-lg + --spacing-xs`），保持导入校验模块与全局列表缩进体系一致。
- 前端视觉一致性第516轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛导入状态空态组件，重构 `frontend/src/pages/Contract/components/ImportStatusStates.module.css`，将容器内边距 `40`、状态图标间距 `16`、图标尺寸 `64` 统一替换为组件级语义令牌计算，提升导入结果状态页在不同模块中的视觉统一性。
- 前端视觉一致性第515轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛合同导入头部组件，重构 `frontend/src/pages/Contract/components/PDFImportHeader.module.css`，将头部卡片间距 `16`、图标字号 `24` 与图标容器尺寸 `48` 统一替换为语义化令牌变量，减少导入页头部与全局卡片组件的尺度偏差。
- 前端视觉一致性第514轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛合同导入状态页的控件尺度，重构 `frontend/src/pages/Contract/ContractImportStatus.module.css`，将统计卡最小宽度 `120` 与头部操作按钮高度 `40/44` 统一替换为页面级语义令牌（基于 `--button-height-lg` 计算），减少状态页与导入链路其他页面的交互尺寸偏差。
- 前端依赖锁文件同步修复（2026-02-13）：针对 `frontend/package.json` 依赖调整后 `pnpm-lock.yaml` 未同步导致 CI `pnpm install --frozen-lockfile` 失败的问题，重新生成并提交 `frontend/pnpm-lock.yaml`，消除 `ERR_PNPM_OUTDATED_LOCKFILE` 阻塞。
- 前端视觉一致性第513轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛合同导入复核页的区块间距，重构 `frontend/src/pages/Contract/ContractImportReview.module.css`，将 `24/16/12/8/4` 与弹层宽度 `420` 的散落表达统一为组件级语义令牌变量，减少复核页提示区、匹配区与底部动作区的节奏不一致。
- 前端视觉一致性第512轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛 PDF 导入主页面动效与密度表达，重构 `frontend/src/pages/Contract/PDFImportPage.module.css`，将 `20/30/16/14/2/1/0.5/0.4/0.3/0.2` 等位移与时长统一替换为 `--spacing-*` 与 `--transition-*` 令牌，并将加载区高度 `320/260` 与内容区高度 `200` 令牌化，降低导入页动画与卡片交互节奏漂移。
- 前端视觉一致性第511轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛 PDF 导入帮助弹窗布局密度，重构 `frontend/src/pages/Contract/PDFImportHelp.module.css`，将 `20/16/14/12/8/6/4/2` 等间距与字号表达统一替换为 `--spacing-* / --font-size-base / --button-height-lg` 令牌化计算，提升帮助弹窗在桌面与移动端的一致性。
- 前端视觉一致性第510轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛全局通知中心组件，重构 `frontend/src/components/Notification/NotificationCenter.module.css`，统一移动端触控目标 `44`、通知面板宽高上限 `400/500`、视口偏移 `100` 与通知头部间距 `6` 的表达为语义化令牌变量，减少通知弹层在不同页面中的密度漂移。
- **项目基础性问题修复（2026-02-13）**：执行 17 项代码质量和架构改进
  - **阶段 1 - 配置修复**：
    - 后端测试覆盖率门限从 20% 提升到 70%（`pyproject.toml`）
    - 清理 `system_settings.py` 中的调试 print 语句
  - **阶段 2 - 代码清理**：
    - 抽取 `AsyncSessionAdapter` 到共享模块 `tests/shared/conftest_utils.py`，消除 7 处重复定义
    - 移除前端未使用的 Jest 相关依赖（`babel-jest`、`jest-axe`、`jest-environment-jsdom`、`jest-watch-typeahead`、`@emotion/jest`）
    - 清理 `DictionaryPage.tsx` 中未使用的导入和变量
    - 修复 `types/index.ts` 中的空类型导出语法问题
    - 更新 `CLAUDE.md` 技术栈版本号为范围表达（如 `React 19.x`）
  - **阶段 3 - 重构优化**：
    - 修复 API 层违反分层原则：将 `project.py` 的 `_project_to_response` 移至 `ProjectService` 层
  - **阶段 4 - 架构改进**：
    - CORS 配置改为从 `settings.CORS_ORIGINS` 读取，消除硬编码
    - Makefile 新增 `test-integration` 和 `test-coverage` 便捷命令

## [Unreleased] - 2026-02-07

### 🛠️ 本次修复 (Current Fixes)

- CI 前端 E2E 种子修复（2026-02-13）：修复 `.github/workflows/ci.yml` 的 `frontend-e2e` 账号种子脚本（移除不存在的 `SessionLocal` 依赖，改为 `async_session_scope` 异步实现），并补充独立 `E2E_LOGOUT_USERNAME/E2E_LOGOUT_PASSWORD` 凭证与组织上下文绑定，确保流水线在当前数据库层实现下可稳定完成登录与登出场景。
- 前端 E2E 断言稳健性补强（2026-02-13）：更新 `frontend/tests/e2e/admin/user-management.spec.ts`、`frontend/tests/e2e/asset-flow.spec.ts`、`frontend/tests/e2e/mobile/dashboard-smoke.spec.ts`，将对“单一标题文案/按钮文案”的脆弱断言升级为语义化兜底（页面标题或表格存在、搜索按钮缺失时回退 Enter、看板标题兼容“资产管理看板”），并将多文案匹配从不稳定的逗号文本选择器收敛为正则匹配，同时为用户管理页可见性断言增加显式等待，减少 UI 渲染时序与文案微调造成的假失败。
- 用户管理页健壮性修复（2026-02-13）：修复 `frontend/src/pages/System/UserManagementPage.tsx` 对 `status` 字段的强假设导致的运行时崩溃（`Cannot read properties of undefined (reading 'tone')`）；新增 `resolveStatusMeta` 与 `FALLBACK_STATUS_META`，当后端返回缺失/未知状态时回退为中性展示，避免系统管理页整体白屏。
- E2E 登出用例隔离（2026-02-13）：调整 `frontend/tests/e2e/auth/auth-flow.spec.ts` 与 `frontend/tests/e2e/helpers/auth.ts`，将“登出会话撤销”用例改为使用独立凭证 `E2E_LOGOUT_USERNAME/E2E_LOGOUT_PASSWORD`（未配置时跳过），避免后端“按用户全量撤销令牌”机制污染共享管理员账号，导致后续用例出现连锁 401。
- E2E 并发会话冲突修复（2026-02-13）：更新 `frontend/playwright.config.ts`，将前端 E2E 默认改为串行执行（`fullyParallel: false`、`workers` 默认 1，可通过 `E2E_WORKERS` 覆盖），规避共享管理员账号在并发用例中被“登出用例全量撤销会话”连带失效导致的随机 401/回跳 `/login`。
- E2E 认证判定修复（2026-02-13）：修复 `frontend/tests/e2e/helpers/auth.ts` 中 `ensureAuthenticated` 的 URL 误判根因（此前可能在未登录时短暂命中 `/dashboard` 初始路径而被当成已登录），改为“等待 `/dashboard` 或 `/login` 最终落点 + `/api/v1/auth/me` 会话校验”双重判定；同时收敛凭证候选策略：当显式配置 `E2E_*_PASSWORD`（或 `E2E_PASSWORD`）时不再继续猜测默认密码，避免并发场景下错误尝试触发账户锁定。
- E2E 认证兜底增强（2026-02-13）：进一步收敛 `frontend/tests/e2e/helpers/auth.ts` 的 `ensureAuthenticated` 策略，改为“3 秒内拿不到受保护路由即回退到显式登录”，避免客户端延迟重定向场景下误判为已登录导致批量用例落到 `/login` 假失败。
- E2E 前端登录时序修复（2026-02-13）：修复 `frontend/tests/e2e/helpers/auth.ts` 中 `ensureAuthenticated` 在 `/dashboard` 首次跳转阶段过早判定导致的假失败（新增 URL 收敛等待，再根据最终落点决定是否执行登录），并放宽 `auth-flow.spec.ts` 无效凭证用例与 `security/access-denied.spec.ts` 普通用户前置登录断言，避免因环境账号基线差异导致非目标断言失败。
- E2E 前置流程修复（2026-02-13）：重构 `frontend/tests/e2e/global-setup.ts`，移除“在 `globalSetup` 阶段强依赖前端服务可访问并预登录”的时序耦合（该阶段早于 Playwright `webServer` 启动，导致 `ERR_CONNECTION_REFUSED`）；改为仅初始化空 `storageState` 并执行浏览器依赖预检（可通过 `E2E_SKIP_BROWSER_PREFLIGHT=true` 跳过）。`pnpm e2e` 现在不再因 `globalSetup` 登录时序而提前失败。
- E2E 稳定性修复（2026-02-13）：修复 `backend/tests/e2e/test_asset_lifecycle_e2e.py` 在真实 PostgreSQL 下“创建后详情 404 / 搜索为空”的根因，更新 `backend/tests/e2e/conftest.py` 的 `authenticated_client` 夹具以确保测试用户具备默认组织上下文（自动创建 `E2E-ORG-ROOT` 并绑定 `default_organization_id`），避免组织级租户过滤 fail-closed；定向回归 `test_asset_lifecycle_e2e.py`（2 passed）与全量 `tests/e2e -m e2e --no-cov`（14 passed）通过。另增强 `frontend/tests/e2e/global-setup.ts`：在缺失 `libnspr4.so/libnss3.so` 等系统依赖时输出明确可执行指引（`playwright install --with-deps` 或 CI/容器运行），替代原始低可读错误。
- 后端测试真实性收敛第55轮（2026-02-13）：移除 `backend/tests/conftest.py` 中“收集期批量注入 integration skip 标记”的旧机制，改为在 `backend/tests/integration/conftest.py` 新增 `ensure_integration_engine_ready`（`session + autouse`）统一触发数据库可达性检查；从而把“静态/收集期跳过”收敛到“运行时环境探测跳过”。验证：`ruff check` 通过，混合回归 `tests/integration/test_postgresql_migration.py + test_postgresql_concurrency.py + tests/unit/api/v1/test_statistics.py` 结果 `5 passed, 25 skipped`，并确认 `backend/tests` 目录 `skip/skipif` 标记扫描为 0。
- 后端测试真实性收敛第54轮（2026-02-13）：清理 `backend/tests/integration/test_postgresql_migration.py` 与 `backend/tests/integration/test_postgresql_concurrency.py` 的模块级 `skipif`，改为运行时 PostgreSQL 可达性 fixture（先校验 `DATABASE_URL` 协议，再执行 `SELECT 1` 预检，不可达时显式 `pytest.skip`），避免“仅靠环境变量前缀就跳过/放行”的伪判断。`ruff check` 通过；定向执行两文件在当前环境下 `25 skipped`（原因明确为数据库不可达）。
- 后端测试真实性收敛第53轮（2026-02-13）：重写 `backend/tests/unit/services/document/test_pdf_analyzer.py`，移除对本地 PyMuPDF 安装状态的 `skipif` 依赖，改为通过 fixture 显式 patch `PYMUPDF_AVAILABLE/fitz` 构造可执行环境；在无三方依赖场景下仍可验证数字 PDF/扫描 PDF/混合 PDF 判定、异常兜底、前 5 页分析边界、`is_scanned_pdf` 与 `get_extraction_recommendation` 便捷接口行为。定向回归 `14 passed`，并完成本轮四文件联合回归 `34 passed`，同时确认 `backend/tests/unit` 目录已无 `pytest.mark.skip/skipif`。
- 后端测试真实性收敛第52轮（2026-02-13）：清理 `backend/tests/unit/api/v1/test_statistics.py` 与 `backend/tests/unit/api/v1/test_enum_field_api.py` 两个 legacy 大而旧用例文件的模块级 `skip`，改写为当前模块化架构下的可执行契约测试（统计路由注册与缓存端点行为、枚举字段路由委托与辅助函数兜底逻辑），并完成低内存回归：两文件 `12 passed`，合并回归（含租赁合同服务实现文件）`20 passed`。
- 后端测试真实性收敛第51轮（2026-02-13）：重建 `backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py`（修复中断导致的误删），将旧同步实现测试替换为当前 async lifecycle/ledger/statistics 契约测试，覆盖创建冲突检测、更新条款替换、续签押金迁移、终止备注拼装、台账批量更新逾期计算与统计汇总组装；定向回归 `8 passed` 且 `ruff check` 通过。
- 后端测试真实性收敛第50轮（2026-02-13）：清理 `backend/tests/unit/services/document/test_config.py` 的模块级 `skip`，将 provider 异常断言升级到当前 `ConfigurationError` 契约，恢复 `LLMProvider` 标准化、`ExtractionConfig/PDFImportConfig` 环境加载与配置单例行为测试。定向回归 `22 passed`，并修复 `ruff` 导入排序后复检通过。
- 后端测试真实性收敛第49轮（2026-02-13）：清理 `backend/tests/unit/services/document/test_extractors.py` 的整文件 `skip`，将工厂异常契约对齐为当前 `ConfigurationError`，并恢复接口/工厂/适配器/单例行为测试全量执行。定向回归该文件 `16 passed`，并修复 `ruff` 导入排序告警后复检通过。
- 后端测试真实性收敛第48轮（2026-02-13）：重写 `backend/tests/unit/services/test_rent_contract_service.py`（移除模块级 legacy `skip`），从旧同步接口断言迁移到当前异步服务契约（`get_contract_by_id_async`、`get_contract_page_async`、`get_asset_contracts_async`、`create_contract_async` 空编号校验），并补齐对底层 CRUD 委托参数的显式校验。定向回归 `4 passed`，`ruff check` 通过。
- 后端测试真实性收敛第47轮（2026-02-13）：清理 `backend/tests/unit/services/test_rent_service_enhanced.py` 的模块级 legacy `skip`，将 3 个同步风格用例改写为异步服务契约测试（`get_contract_by_id_async`、`get_deposit_ledger_async`、`get_service_fee_ledger_async`），并校验底层 CRUD/ledger 调用参数。定向回归 `3 passed`，`ruff check` 通过。
- 后端测试真实性收敛第46轮（2026-02-13）：修复 `backend/src/services/document/pdf_import_service.py` 中 `upload_file` 的相对导入根因（`....utils.file_security` 越级改为 `...utils.file_security`），并在 `backend/tests/unit/services/document/test_pdf_import_service.py` 落地原先 2 个 `skip` 用例（基础上传与同名文件唯一化）；定向回归该文件 `49 passed`，`ruff check` 通过。
- 后端测试真实性收敛第45轮（2026-02-13）：清理 `backend/tests/unit/crud/test_rent_contract_crud.py` 的 class 级 legacy `skip`，将 4 个同步链式 mock 用例重写为 `get_multi_with_filters_async` / `get_with_details_async` 的真实异步契约断言（包含 `rent_contract_assets` 与 `ownerships` join 行为检查）。定向回归该文件 `11 passed`，并通过 `ruff check`。
- 后端测试真实性收敛第44轮（2026-02-13）：继续清理组织服务单测 `backend/tests/unit/services/organization/test_organization_service_impl.py` 的 2 个历史 `skip`，补齐“更新组织写历史记录”与“间接循环检测委托”可执行断言，去除“mock 难写所以跳过”的技术债。定向回归该文件 `24 passed`，并通过 `ruff check`。
- 后端测试真实性收敛第43轮（2026-02-13）：完成 `backend/tests/unit/api/v1/documents/test_pdf_import_routes.py` 的剩余 4 个 `skip` 清理，改为可执行契约测试（会话分页结构、上传成功链路、未认证访问、工作流烟测）；通过 `get_pdf_import_service` 依赖注入 mock 化上传处理，避免伪集成依赖。定向回归 `test_pdf_import_routes.py`（11 passed）与低内存子集回归（16 文件：252 passed）通过；`unit/api + unit/core` 范围内已无 `@pytest.mark.skip`。
- 前端视觉一致性第509轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛看板待办卡片密度，重构 `frontend/src/pages/Dashboard/components/TodoList.module.css`，新增组件级尺寸令牌统一列表项纵向内边距 `12`、操作区偏移 `12`、按钮触达尺寸 `40/44`、优先级图标 `16`、文案字号 `12` 与截止行间距 `6/4` 的表达，提升看板卡片与系统列表组件的节奏一致性。
- 前端视觉一致性第508轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛字典管理页操作区密度，重构 `frontend/src/pages/System/DictionaryPage.module.css` 与 `frontend/src/pages/System/DictionaryPage.tsx`，新增页面级尺寸令牌统一刷新/弹窗按钮 `36/40`、行内操作 `32`、状态标签行高 `18`、辅助文案偏移 `2` 与详情按钮内边距 `6` 的表达，降低字典页与枚举页之间的视觉偏差。
- 前端视觉一致性第507轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛枚举字段管理页的控件节奏，重构 `frontend/src/pages/System/EnumFieldPage.module.css` 与 `frontend/src/pages/System/EnumFieldPage.tsx`，新增页面级尺寸令牌统一搜索/选择框宽度 `320/220`、按钮与行内操作尺寸 `36/32/40`、状态标签行高 `18`、色块尺寸 `16` 与行间距 `6` 表达，提升类型与枚举值双标签页的交互一致性。
- 前端视觉一致性第506轮（2026-02-13）：继续按 `ui-ux-pro-max` 优化 Prompt 监控页视觉节奏，重构 `frontend/src/pages/System/PromptDashboard.module.css` 与 `frontend/src/pages/System/PromptDashboard.tsx`，新增页面级尺寸令牌并统一加载区高度 `360`、操作按钮 `40/44`、趋势卡最小宽度 `108/124` 与日期间距表达，减少监控看板在不同断点下的密度波动。
- 前端视觉一致性第505轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛 Prompt 管理页操作密度，重构 `frontend/src/pages/System/PromptListPage.module.css` 与 `frontend/src/pages/System/PromptListPage.tsx`，新增页面级尺寸令牌统一顶部操作区与表格操作按钮 `40/44` 触达尺寸，保证 LLM Prompt 管理页与系统其它列表页的交互尺度一致。
- 前端视觉一致性第504轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛组织管理页布局尺度，重构 `frontend/src/pages/System/OrganizationPage.module.css` 与 `frontend/src/pages/System/OrganizationPage.tsx`，新增页面级尺寸令牌统一搜索框宽度 `300`、历史动作标签宽度 `52`、操作按钮 `40/44` 与树节点标题间距表达，提升组织列表与组织树视图的视觉一致性。
- 前端视觉一致性第503轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛用户管理页控件密度，重构 `frontend/src/pages/System/UserManagementPage.module.css` 与 `frontend/src/pages/System/UserManagementPage.tsx`，新增页面级尺寸令牌统一筛选工具栏与表格操作按钮 `40/44`、状态开关最小宽度 `48` 的表达，减少用户管理页在桌面/移动端的交互尺寸漂移。
- 前端视觉一致性第502轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛模板管理页尺寸体系，重构 `frontend/src/pages/System/TemplateManagementPage.module.css` 与 `frontend/src/pages/System/TemplateManagementPage.tsx`，新增页面级尺寸令牌统一操作按钮/主按钮 `40/44`、字段预览区 `220/180`、移动端行间距 `4` 等表达，减少模板页在桌面与移动端的控件节奏漂移。
- 前端视觉一致性第501轮（2026-02-13）：继续按 `ui-ux-pro-max` 收敛系统审计页布局密度，重构 `frontend/src/pages/System/OperationLogPage.module.css` 与 `frontend/src/pages/System/OperationLogPage.tsx`，新增页面级尺寸令牌并统一替换操作按钮与工具栏 `40/44`、详情区 `300`、标签行高 `18` 等分散尺寸表达，确保操作日志页与角色管理页在控件触达与间距节奏上保持一致。
- 前端视觉一致性第500轮（2026-02-13）：继续按 `ui-ux-pro-max` 深挖资产分析页真实观感问题，重构 `frontend/src/pages/Assets/AssetAnalyticsPage.module.css`，新增页面级尺寸令牌并统一加载/空态/错误态高度与间距；修复 CSS Modules 下对 AntD 选择器作用域失效（`ant-btn`、`ant-form-item`、`no-print` 改为 `:global(...)`），同时收敛 `16px/0.3s/rgba` 等分散表达到 `--spacing-* / --transition-slow / --shadow-md` 令牌。
- 前端视觉一致性第499轮（2026-02-13）：继续按 `ui-ux-pro-max` 排查系统管理页观感问题，重构 `frontend/src/pages/System/RoleManagementPage.module.css` 与 `frontend/src/pages/System/RoleManagementPage.tsx`，新增页面级尺寸令牌（控件高度、移动端触控目标、权限穿梭列表尺寸、树预览高度、图标偏移）并统一替换 `40/44/360/400/280/220/28/2` 等分散硬编码，收敛角色管理页按钮、操作图标、弹窗操作区与权限配置区的布局节奏。
- E2E 测试体系完备化与门禁接入（2026-02-13）：前端新增 Playwright 可执行基线（`frontend/playwright.config.ts` 重构、`tests/e2e/global-setup.ts` / `global-teardown.ts`、`tests/e2e/helpers/auth.ts`、多场景用例 `auth/asset/rental/security/admin/mobile`、`package.json` 新增 `e2e*` 脚本并补充 `@playwright/test`/`playwright` 依赖）；后端 `tests/e2e` 统一 `pytest.mark.e2e` 并新增资产/合同工作流用例；`Makefile` 新增 `test-e2e*` 命令；CI 新增阻塞式 `backend-e2e` 与 `frontend-e2e` 作业并纳入汇总；测试规范文档补充 E2E 执行标准与 CI 要求。
- 后端测试真实性收敛第42轮（2026-02-13）：修复 `backend/tests/unit/api/v1/test_monitoring.py` 的根因性不稳定（导入顺序导致权限装饰器行为漂移）：统一迁移到真实模块路径 `src.api.v1.system.monitoring`，新增 RBAC 权限桩并在装饰器场景显式传入 `current_user/db`，同时解除 4 个历史 `skip`（3 个 logger 场景 + 1 个 time 场景）并将异常断言对齐当前契约（`InternalServerError`）；定向回归 `test_monitoring.py`（25 passed）通过，扩展低内存回归（16 文件）结果 `248 passed, 4 skipped`。
- 后端测试真实性收敛第41轮（2026-02-13）：解除 `backend/tests/unit/api/v1/test_collection.py` 中 4 个占位 `skip`（`TestGetCollectionSummary` 三项 + `test_get_summary_with_null_success_count`），改为基于 `mock_collection_service.get_summary_async` 的真实可执行断言（含 `collection_success_rate=None` 契约）；定向回归 `test_collection.py`（27 passed）通过，扩展低内存回归（15 文件）结果 `223 passed, 4 skipped`。
- 后端测试真实性收敛第40轮（2026-02-13）：重构 `backend/tests/unit/api/test_collection_api.py`，移除整模块 `skip` 与过时的 `client.db` 直连写法，改为基于 `collection_service` 的可执行契约测试（汇总、分页列表、详情、创建、更新、删除、未认证与 404/422 分支）；定向回归 `test_collection_api.py`（11 passed）通过，扩展低内存回归（14 文件）结果 `196 passed, 4 skipped`。
- 后端测试真实性收敛第39轮（2026-02-13）：完成 `error_recovery` 真实回归链路修复：恢复 `backend/src/api/v1/__init__.py` 中 `error_recovery_router` 注册（原“Pydantic model issues”临时禁用注释已解除），并将 `backend/tests/unit/api/test_error_recovery_api.py` 从整模块 `skip` 改为可执行契约测试，修正过时 patch 目标与错误响应断言（`detail`→`error.message`，`src.api.v1.error_recovery`→`src.api.v1.system.error_recovery`）；定向回归 `test_error_recovery_api.py`（17 passed）通过，扩展低内存回归（13 文件）结果 `185 passed, 4 skipped`。
- 后端测试真实性收敛第38轮（2026-02-13）：解除 `backend/tests/unit/api/test_asset_import_api.py` 与 `backend/tests/unit/core/test_exception_handling.py` 的模块级/类级 `skip`，并将断言对齐当前真实契约：`/api/v1/assets/import` 改为新请求字段 `should_skip_errors/is_dry_run`、缺失必填体 `422`、服务层异常 `500 + INTERNAL_SERVER_ERROR`；`/api/v1/assets/{id}` 的通用异常按错误恢复中间件真实行为固定为 `503 + DATABASE_ERROR`，业务验证异常保持 `422 + VALIDATION_ERROR`。低内存定向回归（12 文件）`168 passed, 4 skipped`，新增解锁文件均已通过。
- 后端测试真实性收敛第37轮（2026-02-13）：激活并收敛 `backend/tests/unit/api/v1/documents/test_pdf_import_routes.py` 中原本 `skip` 的 3 个上传校验用例（无文件/非法格式/超限大小），将宽泛状态断言固定为真实契约（`422/422/400`），并通过 `monkeypatch` 将 `DEFAULT_MAX_FILE_SIZE` 下调到测试可控阈值避免大文件压测与假阳性；定向回归结果 `7 passed, 4 skipped`。同时完成 `unit/api/v1` 范围弱断言扫描清零（不再存在 `status_code in [...]` / `AUTH_FAILURE_STATUSES` / `except Exception: pass`）。
- 后端测试真实性收敛第36轮（2026-02-13）：收敛 `backend/tests/unit/api/v1/test_occupancy.py` 的剩余宽泛状态码断言，按真实行为固定为 `rate=200`、`area-stats/by-project/utilization=404`、未认证访问 `401 + AUTHENTICATION_ERROR`；低内存定向回归 `test_occupancy.py`（5 passed）通过。
- 后端测试真实性收敛第35轮（2026-02-13）：继续清理 unit API 宽泛断言与“条件早退”测试反模式，完成 `backend/tests/unit/api/v1/test_property_certificate.py`、`test_roles.py`、`test_llm_prompts.py`、`test_notifications.py` 的真实契约收敛（固定 200/401/404/405/422 等明确状态码），并将产权证文件类型/超大文件场景从 `pass` 占位改为真实上传校验；低内存分文件回归通过：`test_property_certificate.py`（21 passed）+ `test_roles.py`（13 passed）+ `test_llm_prompts.py`（12 passed）+ `test_notifications.py`（22 passed）+ `test_integration_test_quality_guard.py`（2 passed）。
- 后端测试真实性收敛第34轮（2026-02-13）：补齐 db-backed unit 根因修复（`backend/tests/unit/conftest.py` 新增 `TEST_DATABASE_URL` 环境变量缺失时从 `backend/.env` 回退解析，解除大量误跳过），并修复 `backend/tests/unit/api/v1/test_contact.py` 的真实契约断言漂移（手机号非法输入当前实现返回 `200`、超长姓名返回 `422`、特殊字符姓名返回 `200`）；低内存定向回归：`test_contact.py`（`28 passed`）+ `test_project.py`（`24 passed`）+ `test_tasks.py` + `test_roles.py` + `analytics/test_statistics.py` + `statistics_modules/test_basic_stats.py` + `test_integration_test_quality_guard.py` 全部通过。
- 前端视觉一致性第498轮（2026-02-13）：继续优化主布局尺寸一致性，重构 `frontend/src/components/Layout/Layout.module.css`，新增布局级局部变量（header 高度、控件尺寸、移动端控件尺寸）并替换 header/sidebar/menu 中重复的 `64/56/44/40` 尺寸表达，减少 Layout 层重复硬编码与跨断点漂移
- 前端视觉一致性第497轮（2026-02-13）：继续统一系统通知中心模块的控件尺度与间距节奏，重构 `frontend/src/pages/System/NotificationCenter.module.css`，将列表项/头像/标签/按钮中的 `48/40/36/20/18/16/12/8/4` 尺寸表达替换为 `--button-height-* / --font-size-* / --spacing-* / --transition-base` 令牌，减少系统页通知模块与通用通知中心间的视觉偏差
- 前端视觉一致性第496轮（2026-02-13）：继续优化系统设置页控件密度，重构 `frontend/src/pages/System/SystemSettingsPage.module.css`，将 tab 区/状态标签/备份分区的 `20/16/12/8/4/40/44/32` 等间距、字号与最小高度统一替换为 `--spacing-* / --font-size-* / --button-height-*` 令牌，减少设置页与全局组件库在交互尺寸上的不一致
- 前端视觉一致性第495轮（2026-02-13）：继续收敛筛选器与骨架屏组件的尺寸表达，重构 `frontend/src/components/Analytics/Filters/Filters.module.css` 与 `frontend/src/components/Loading/SkeletonLoader.module.css`，将历史 `16/12/8/6/4/36/32/24/20` 等间距与控件高度替换为 `--spacing-* / --button-height-* / --font-size-* / --form-input-radius` 令牌，统一过滤面板与加载态占位元素的密度节奏
- 前端视觉一致性第494轮（2026-02-13）：继续收敛合同导入上传页的局部密度与间距表达，重构 `frontend/src/pages/Contract/ContractImportUpload.module.css`，将状态面板/文件信息卡/使用说明区中的 `60/48/32/24/20/16/12/8/6` 尺寸与间距统一替换为 `--spacing-* / --font-size-* / --button-height-md / --form-input-radius` 令牌，提升导入流程页面视觉节奏一致性
- 前端视觉一致性第493轮（2026-02-13）：继续治理数据看板页面节奏漂移，重构 `frontend/src/pages/Dashboard/DashboardPage.module.css`，将统计卡/洞察卡/加载与错误容器中的 `20/16/12/8/4/60` 等间距字号与动画位移统一替换为 `--spacing-* / --font-size-* / --transition-slow` 令牌，提升看板页面在不同区块间的密度一致性
- 前端视觉一致性第492轮（2026-02-13）：继续统一主题切换控件密度，重构 `frontend/src/components/Common/ThemeToggle.module.css`，将 `44/40/36` 按钮尺寸与 `14/16/18` 图标字号收敛到 `--button-height-* / --font-size-*` 令牌（含 `calc(var(--button-height-lg) + var(--spacing-xs))` 触控尺寸），减少主题开关在不同页面中的控件尺度漂移
- 前端视觉一致性第491轮（2026-02-13）：继续优化通知中心密度与控制尺寸一致性，重构 `frontend/src/components/Notification/NotificationCenter.module.css`，将触发按钮/操作按钮/“全部已读”按钮的 `40/32` 尺寸与未读圆点 `8px`、铃铛图标 `18px` 收敛为 `--button-height-* / --spacing-sm / --font-size-xl` 令牌，减少通知面板与全局控件节奏偏差
- 前端视觉一致性第490轮（2026-02-13）：继续修复搜索高亮与导出报告样式一致性，重构 `frontend/src/utils/highlight.ts`（修复 `RegExp.test` 全局状态导致的间歇性高亮误判，并将 mark 样式替换为主题令牌）及 `frontend/src/services/analyticsExportService.ts`（导出 HTML 报告样式改为语义 CSS 变量驱动，降低直写色值耦合）
- 前端视觉一致性第489轮（2026-02-13）：继续收尾资产导出与统计卡片色值治理，重构 `frontend/src/components/Asset/AssetExportProgress.module.css` 与 `frontend/src/components/Analytics/StatisticCard.tsx`，将残留硬编码背景/状态色替换为主题令牌；并同步更新 `frontend/src/components/Analytics/__tests__/StatisticCard.test.tsx` 断言以匹配 `var(--color-success/--color-error)` 新实现，完成全量前端回归通过
- 前端视觉一致性第488轮（2026-02-13）：继续批量清理零散页面/组件的硬编码样式，重构 `frontend/src/pages/Assets/components/AssetCard.module.css`、`frontend/src/pages/Assets/AssetAnalyticsPage.module.css`、`frontend/src/components/System/PromptEditor.module.css`、`frontend/src/components/ErrorHandling/ErrorBoundary.module.css`、`frontend/src/components/Rental/ContractTerminateModal.module.css`、`frontend/src/components/ErrorHandling/UXProvider.tsx`、`frontend/src/components/Common/LazyImage.tsx`，统一字号/间距/背景/错误色与主题主色默认策略到设计令牌体系
- 前端视觉一致性第487轮（2026-02-13）：继续清理租赁与权属选择组件样式，重构 `frontend/src/components/Rental/ContractDetailInfo.module.css` 与 `frontend/src/components/Ownership/OwnershipSelect.module.css`，将残留硬编码色值（含 `#722ed1/#13c2c2/#999/#666`）及多处 `16/12/8/4` 间距字号替换为统一令牌，减少明细卡片与下拉选项的视觉漂移
- 前端视觉一致性第486轮（2026-02-13）：继续治理续约页面样式细节，重构 `frontend/src/pages/Rental/ContractRenewPage.module.css`，移除 `var(..., #xxxxxx)` 回退硬编码并将 `16/12/8/6` 间距与图标字号统一替换为 `--spacing-* / --font-size-xxxl` 令牌，保持续约成功引导区与全局密度一致
- 前端视觉一致性第485轮（2026-02-13）：继续修复资产搜索结果区视觉漂移，重构 `frontend/src/components/Asset/AssetSearchResult.module.css`，将图标/说明文案中的硬编码灰色与字号、`8px` 间距统一替换为 `--color-text-tertiary / --font-size-* / --spacing-sm`，并将 `--ant-color-primary` 收敛到统一主题变量 `--color-primary`
- 前端视觉一致性第484轮（2026-02-13）：继续收敛 PDF 导入进度条配色，重构 `frontend/src/components/Contract/PDFImport/ProcessingStepsDisplay.tsx`，将进度渐变起止色从硬编码 hex 统一替换为 `COLORS.primary / COLORS.success`，确保导入流程视觉语义与全局主题令牌一致
- 前端视觉一致性第483轮（2026-02-13）：继续统一监控健康面板图形语义色，重构 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.tsx`，将健康仪表盘指针/中心针的 `#D0D0D0` 硬编码统一替换为 `COLORS.border`，保持监控图形元素与全局边框色令牌一致
- 前端视觉一致性第482轮（2026-02-13）：继续统一分析图表网格线色彩来源，重构 `frontend/src/components/Analytics/AnalyticsChart.tsx` 将 `#d9d9d9` 网格线替换为 `COLORS.border`；并同步修复对应单测 mock（`frontend/src/components/Analytics/__tests__/AnalyticsChart.test.tsx` 新增 `COLORS.border`）以匹配新依赖导出，完成全量前端回归通过
- 前端视觉一致性第481轮（2026-02-13）：继续收敛合同导入状态页样式，重构 `frontend/src/pages/Contract/ContractImportStatus.module.css` 并新增 `frontend/src/styles/variables.css` 语义变量 `--color-text-inverse`，将状态图标徽标中的 `#fff` 与多处 `16/12/8/40` 硬编码统一替换为令牌（`--color-* / --spacing-* / --button-height-lg / --font-size-xs`），提升状态页在主题切换下的一致性
- 前端视觉一致性第480轮（2026-02-13）：继续清理合同导入修复弹窗的样式硬编码，重构 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将输入框/预览块/建议块中的 hex 色值与 `8/12/6/20` 间距圆角表达收敛到 `--color-* / --spacing-* / --form-input-radius / --font-size-xs`，统一文件名修复流程视觉节奏
- 前端视觉一致性第479轮（2026-02-13）：继续治理合同导入校验区样式，重构 `frontend/src/components/Contract/FilenameValidator.module.css`，将预览框/建议框/状态行的 `8/12/6` 等硬编码间距与圆角、成功/错误色值统一替换为 `--spacing-* / --form-input-radius / --color-*` 令牌，收敛文件名校验面板的局部视觉漂移
- 前端视觉一致性第478轮（2026-02-13）：继续清理错误展示组件的历史视觉噪音，重构 `frontend/src/components/Common/FriendlyErrorDisplay.tsx`，删除未使用且重复的错误类型硬编码色值字段（`color`），避免组件内部出现“配置色值与样式令牌分离”的双轨表达，统一以 CSS Module 语义类控制图标与提示色
- 前端视觉一致性第477轮（2026-02-13）：继续收敛 Monitoring 模块语义色来源，重构 `frontend/src/components/Monitoring/systemMonitoringTypes.ts` 与 `frontend/src/components/Monitoring/ApiMonitor.tsx`，将状态色与健康度阈值色从硬编码 hex 统一替换为 `COLORS` 主题令牌映射（含默认回退色），并保持监控面板统计卡/进度条的视觉语义一致
- 前端视觉一致性第476轮（2026-02-13）：清理已确认未被引用的遗留样式文件，删除 `frontend/src/components/Charts/Charts.module.css`、`frontend/src/components/Asset/AssetSearch.module.css`、`frontend/src/pages/Assets/AssetListPage.module.css`、`frontend/src/pages/AssetDetailPage.module.css`，降低样式噪音与历史硬编码对后续视觉治理的干扰
- 前端视觉一致性第475轮（2026-02-13）：继续推进图表模块色彩统一，重构 `frontend/src/components/Charts/AssetDistributionChart.tsx`、`frontend/src/components/Charts/OccupancyRateChart.tsx`、`frontend/src/components/Charts/AreaStatisticsChart.tsx`，将分散的图表十六进制色值收敛到 `CHART_COLORS / CHART_LABEL_COLORS / COLORS` 主题映射，统一图表主色、标签文字与进度条语义色来源
- 前端视觉一致性第474轮（2026-02-13）：聚焦登录页与主布局的根因修复，重构 `frontend/src/pages/LoginPage.module.css` 为完整令牌化样式并统一 `:global(.ant-*)` 选择器作用域（修复 CSS Modules 下 AntD 结构样式失效问题），同时清理并收敛 `frontend/src/components/Layout/Layout.module.css` 的遗留未使用规则与硬编码色值，统一 header/sidebar/content/footer 视觉节奏与交互基线
- 前端视觉一致性第473轮（2026-02-13）：继续覆盖 System/Monitoring/Notification/PropertyCertificate 等模块的间距与圆角令牌化，统一通知中心与监控图表的主色来源（对齐 `--color-primary`），并进一步消除多处 `24/16/12/8/4` 硬编码导致的布局节奏漂移
- 前端视觉一致性第472轮（2026-02-13）：继续治理 System/Rental/Ownership/Import 等页面的“节奏漂移”，将残留的 `24/16/12/8/4` 间距、圆角与反馈组件的硬编码色值收敛为设计令牌（`--spacing-* / --radius-* / --color-*`），并补齐资产分析页与操作日志页的统一卡片/标签视觉基线
- 前端视觉一致性第471轮（2026-02-13）：继续补齐 Dashboard 子模块、图表卡片、面包屑与 Contract/PDF 导入相关组件的令牌化（间距/圆角/语义色），并将 Progress/错误提示/分组选择器中的硬编码色值统一替换为 `--color-*`，进一步减少跨页面视觉漂移
- 前端视觉一致性第470轮（2026-02-13）：继续收敛 Dashboard/Analytics/Ownership 等关键模块的硬编码圆角/间距/语义色到统一令牌（`--spacing-* / --radius-* / --color-*`），并按 `ui-ux-pro-max` 建议移除导入提示中的 emoji 图标，提升企业后台观感一致性
- 前端视觉一致性第469轮（2026-02-13）：继续推进“硬编码清零”与令牌化专项，将常用 `24/16/12/8/4` 间距、字号、语义色从散落 CSS 收敛到 `--spacing-* / --font-size-* / --color-*`，重点覆盖 `common.css`、Dashboard/Analytics/Monitor/覆盖率看板等高频布局模块
- 前端视觉一致性第468轮（2026-02-13）：统一 Layout 级背景与头部/侧边栏的间距令牌使用（`--color-bg-layout` + `--spacing-*`），收敛 header/sidebar 常见的 `16/12/8/4` 硬编码与圆角漂移，提升整体布局节奏一致性
- 前端视觉一致性第467轮（2026-02-13）：补齐并统一页面容器（`PageContainer`）的间距/圆角令牌使用，新增 `--color-bg-layout` 作为页面背景基线，减少“不同页面背景/留白节奏”不一致问题
- 前端视觉一致性第466轮（2026-02-13）：统一主题注入的 `--spacing-* / --font-size-* / --radius-* / --shadow-*` 与 `frontend/src/styles/variables.css` / AntD token 基线，修复全局间距、字号、圆角在组件间漂移的问题，并同步收敛移动端头部字号变量引用错误
- 前端视觉一致性第465轮（2026-02-13）：修复 `frontend/src/components/Project/__tests__/ProjectList.test.tsx` 中 `@ant-design/icons` mock 缺失新增图标导出导致的渲染/用例级联失败，恢复单测稳定性
- 前端视觉一致性第464轮（2026-02-13）：继续重构 `frontend/src/components/Project/ProjectList.tsx` 与 `frontend/src/components/Project/ProjectList.module.css`，将统计卡片前缀 emoji 替换为 Ant Design 图标，并移除 `Statistic.styles` 硬编码色值，统一指标卡图标与数值色阶表达
- 前端视觉一致性第463轮（2026-02-13）：继续重构 `frontend/src/components/Project/ProjectList.module.css`，将统计前缀色值 `#1890ff/#52c41a/#ff4d4f/#722ed1` 收敛为 `--color-*` 主题变量，统一颜色来源并减少硬编码
- 前端视觉一致性第462轮（2026-02-13）：继续重构 `frontend/src/components/Project/ProjectList.module.css`，新增 `statsCard/statsTotal/statsActive/statsInactive/statsAssets` 语义类并用 `:global(.ant-statistic-content)` 统一指标卡数值色阶与标题层级
- 前端视觉一致性第461轮（2026-02-13）：继续重构 `frontend/src/components/Project/ProjectList.module.css`，将统计行间距与标签间距从 `24px/4px` 收敛为 `--spacing-xl/--spacing-xs`，统一项目列表页布局节奏
- 前端视觉一致性第460轮（2026-02-13）：开始推进项目列表指标卡一致性优化（数据密集仪表盘风格），聚焦“图标体系统一 + 颜色令牌化 + 指标卡层级一致”
- 前端视觉一致性第459轮（2026-02-13）：继续按 `ui-ux-pro-max` 建议清理“emoji 作为图标”的反模式，替换为一致的 SVG/Icon 体系，提升企业后台观感
- 前端视觉一致性第458轮（2026-02-13）：继续提升指标卡可读性与一致性，避免同一页面同时出现 emoji/图标/硬编码色值的混搭风格
- 前端视觉一致性第457轮（2026-02-13）：继续统一指标卡标题/数值层级与色阶规则，减少跨模块指标卡视觉漂移
- 前端视觉一致性第456轮（2026-02-13）：继续收敛统计区样式表达，优先使用主题变量与语义 class 控制视觉
- 前端视觉一致性第455轮（2026-02-13）：开始新一批项目列表视觉一致性排查与修复，优先处理统计卡区域的图标与颜色不统一问题

- 后端测试真实性收敛第33轮（2026-02-13）：扩展质量护栏 `backend/tests/unit/core/test_integration_test_quality_guard.py`，新增 `unit/api/v1` 维度的“禁止 `except Exception: pass`”校验，防止单测再次回退到吞错清理；护栏回归 `2 passed`。
- 后端测试真实性收敛第32轮（2026-02-13）：将 `backend/tests/unit/api/v1/test_roles.py` 的 `admin_user_headers` 夹具改为本地 mock 管理员注入，去除对 `admin_user` fixture 的耦合（为后续去 skip 做准备）；并保持未认证访问契约回归通过（`1 passed, 12 skipped`）。
- 后端测试真实性收敛第31轮（2026-02-13）：收敛 `backend/tests/unit/api/v1/test_system_settings.py` 与 `test_operation_logs.py` 的历史路由断言，将 `/api/v1/system-settings/*`、`/api/v1/operation-logs/*` 旧路径行为固定为明确 `404` 契约，去除宽泛 `status_code in [...]`；定向验证 `2 passed, 10 skipped`。
- 后端测试真实性收敛第30轮（2026-02-13）：继续收敛 `backend/tests/unit/api/v1/test_roles.py` 当前实际执行的未认证访问断言，固定 `GET /api/v1/roles` 契约为 `401 + AUTHENTICATION_ERROR`，并移除 trailing slash 导致的 307 干扰；定向验证 `1 passed, 12 skipped`。
- 后端测试真实性收敛第29轮（2026-02-13）：清理 `backend/tests/unit/api/v1/test_contact.py` 与 `test_project.py` 中的 `except Exception: pass` 清理吞错反模式，改为显式查询存在后再删除测试数据，避免隐藏真实清理失败；定向验证（contact + project）`2 passed, 50 skipped`。
- 后端测试真实性收敛第28轮（2026-02-13）：继续收敛 `backend/tests/unit/api/v1/test_tasks.py` 未认证访问断言，固定 Excel 配置创建/列表接口契约为 `401 + AUTHENTICATION_ERROR`，去除 `401/403` 宽泛兜底；定向验证 `2 passed`。
- 后端测试真实性收敛第27轮（2026-02-13）：继续收敛 analytics 单测弱断言，完成 `backend/tests/unit/api/v1/analytics/test_statistics.py`（dashboard/invalid-filter/service-exception）与 `analytics/statistics_modules/test_occupancy_stats.py`（trend）契约固定，状态码明确为 `200/200/503/404`；定向回归 `13 passed`。
- 后端测试真实性收敛第26轮（2026-02-13）：继续收敛 `backend/tests/unit/api/v1/analytics/statistics_modules/test_basic_stats.py` 的宽泛状态码断言，固定 dashboard/cache/cache-info/数据库异常场景为明确契约（`200/200/200/503`），消除 `status_code in [200,400,404...]` 弱断言；定向回归 `13 passed`。
- 后端测试真实性收敛第25轮（2026-02-13）：继续收敛 `backend/tests/unit/api/v1/test_notifications.py` 当前实际执行的未认证场景断言（通知列表/未读数/全部已读），将宽泛 `status_code in {...}` 固定为 `401 + AUTHENTICATION_ERROR` 明确契约；定向验证结果 `3 passed, 19 skipped`。
- 后端测试真实性收敛第24轮（2026-02-13）：继续收敛 unit API 弱断言，完成 `backend/tests/unit/api/v1/test_admin.py`（管理员重置权限链路）、`test_occupancy.py`（未认证访问）、`test_operation_logs.py`（按路由注册状态确定期望）、`test_llm_prompts.py`（未认证错误码）的契约固定，并验证 `test_analytics.py` 新收敛断言稳定；联合回归（unit API 子集 + guard + auth e2e + system info）`34 passed, 54 skipped`。
- 后端测试真实性收敛第23轮（2026-02-13）：继续收敛 `backend/tests/unit/api/v1/test_dictionaries.py` 的宽泛断言（去除 `AUTH_FAILURE_STATUSES` 兜底、固定核心端点 200、补充路由注册与返回结构校验），并完成关联回归（system/history/dictionaries + guard + auth e2e + system info：32 passed）。
- 后端测试真实性收敛第22轮（2026-02-13）：继续收敛 unit API 弱断言，完成 `backend/tests/unit/api/v1/test_history.py` 的历史接口契约固定（列表/筛选/分页 200、实体查询 404、未认证 401），并与 `test_system.py`、guard、认证 E2E、system info 集成链路联合回归（24 passed）。
- 后端测试真实性收敛第21轮（2026-02-13）：收敛 `backend/tests/unit/api/v1/test_system.py` 的宽泛状态码断言，基于真实响应将系统路由断言固定为明确契约（`/system/info` 返回 200，其余缺失路由返回 404），并补充 `info` 返回结构校验。
- 后端测试真实性收敛第20轮（2026-02-13）：修复 `backend/tests/integration/api/test_system_info_real.py` 对 `environment` 字段的过窄断言，补充 `test` 合法值；完成混跑回归（`tests/integration` + `tests/e2e/test_auth_flow_e2e.py` + guard：370 passed）。
- 后端测试真实性收敛第19轮（2026-02-13）：收敛 `backend/tests/e2e/test_auth_flow_e2e.py` 宽泛断言，固定认证失败/刷新/权限场景的状态码与错误码契约，并将登出流程改为携带 CSRF 的真实链路校验（登录→登出→会话失效）。
- 后端测试真实性收敛第18轮（2026-02-13）：扩展 `backend/tests/unit/core/test_integration_test_quality_guard.py` 扫描范围至 `tests/e2e`，防止 E2E 回退到 `status_code in (...)` / `status_code != 404` / `except Exception: pass` 弱模式；与认证 E2E 用例联合验证通过。
- 前端视觉一致性第454轮（2026-02-12）：继续重构多处新增模块样式，统一将 `margin-top/bottom: 16px` 替换为 `var(--spacing-lg)`，将 `margin-top: 24px` 替换为 `var(--spacing-xl)`，进一步收敛表单与列表分区节奏
- 前端视觉一致性第453轮（2026-02-12）：继续重构 `frontend/src/components/Rental/DepositLedgerHistory.module.css`，将图标间距与卡片底距替换为 `--spacing-sm/--spacing-lg` 令牌，统一租赁详情区间距体系
- 前端视觉一致性第452轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectDetail.module.css` 与 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.module.css`，将分区顶距替换为 `var(--spacing-lg)`，统一详情页与弹窗底部操作区节奏
- 前端视觉一致性第451轮（2026-02-12）：继续重构 `frontend/src/components/Forms/AssetForm.module.css` 与 `frontend/src/components/Forms/AssetFormHelp.module.css`，将进度区间距改为 `--spacing-lg`，并将帮助弹窗偏移改为基于令牌的 `calc(--spacing-xl - --spacing-xs)` 表达
- 前端视觉一致性第450轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/*.module.css`，统一承租方信息/租金条款/其他信息/合同期限/基础信息/操作区的卡片与区块间距到设计令牌
- 前端视觉一致性第449轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetBatchActions.module.css` 与 `AssetExportForm.module.css`，将按钮内边距与提示区间距替换为 `--spacing-sm/--spacing-lg` 令牌
- 前端视觉一致性第448轮（2026-02-12）：继续重构 `frontend/src/components/Loading/LoadingProvider.module.css` 与 `LoadingSpinner.module.css`，将加载提示间距统一到 `--spacing-sm/--spacing-lg` 令牌
- 前端视觉一致性第447轮（2026-02-12）：继续推进“间距令牌化”专项，消除新增样式模块中可令牌化的 8/16/24 像素硬编码
- 前端视觉一致性第446轮（2026-02-12）：继续统一新引入模块的布局基线，减少跨组件间距定义方式分叉带来的视觉不一致
- 前端视觉一致性第445轮（2026-02-12）：开始“间距令牌统一”微重构批次，聚焦本轮新增 CSS Modules 的 `px` 间距表达收敛

- 前端视觉一致性第444轮（2026-02-12）：继续重构 `frontend/src/components/Rental/DepositLedgerHistory.module.css`，移除金额色值中的十六进制 fallback，统一为主题变量 `--color-success/--color-error` 驱动
- 前端视觉一致性第443轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.module.css`，将硬删提示文本颜色从 `var(--color-error, #ff4d4f)` 收敛为 `var(--color-error)`，统一主题色读取策略
- 前端视觉一致性第442轮（2026-02-12）：继续重构 `frontend/src/components/Router/DynamicRouteRenderer.module.css`，移除错误态文本与边框颜色 fallback，统一路由错误态与全局主题变量对齐
- 前端视觉一致性第441轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectSelect.module.css`，移除选项辅助文本 `#999/#666` fallback，统一项目选择器字阶色从主题变量读取
- 前端视觉一致性第440轮（2026-02-12）：继续重构 `frontend/src/components/Forms/AssetFormHelp.module.css`，移除帮助按钮颜色 fallback，统一帮助入口主色与主题变量 `--color-primary` 对齐
- 前端视觉一致性第439轮（2026-02-12）：继续推进“颜色来源统一”专项，减少 CSS Modules 中 `var(..., #hex)` 回退写法带来的主题漂移风险
- 前端视觉一致性第438轮（2026-02-12）：继续清理局部模块样式中的硬编码兜底色，确保深色/浅色主题切换下的色阶一致性
- 前端视觉一致性第437轮（2026-02-12）：继续优化错误态与状态色的语义变量映射，降低多组件间色值定义分叉
- 前端视觉一致性第436轮（2026-02-12）：继续收敛表单/列表/路由/租赁模块颜色策略，统一走设计令牌体系
- 前端视觉一致性第435轮（2026-02-12）：开始“主题变量纯化”微重构批次，聚焦移除新增模块中的颜色 fallback 与历史硬编码残留

- 前端视觉一致性第434轮（2026-02-12）：继续重构 `frontend/src/components/Common/VirtualList.tsx` 与 `frontend/src/components/Common/LazyImage.tsx`，将动态样式从 JSX 内联对象字面量改为命名 `style` 变量透传，保留虚拟滚动与懒加载计算逻辑不变并降低渲染段样式噪音
- 前端视觉一致性第433轮（2026-02-12）：继续重构 `frontend/src/pages/System/EnumFieldPage.tsx` 与 `frontend/src/pages/Dashboard/components/AssetChart.tsx`，将 CSS 变量注入写法从 `style={{...}}` 收敛为 `build*Style` 辅助函数输出，统一动态色块变量传递模式
- 前端视觉一致性第432轮（2026-02-12）：继续重构 `frontend/src/components/Dictionary/DictionarySelect.tsx`，将颜色标识点动态背景样式改为 `buildColorSwatchStyle` 函数透传，统一字典组件动态样式组织方式
- 前端视觉一致性第431轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetCard.tsx`，将出租率文本颜色样式改为命名变量 `occupancyValueStyle` 透传，保持既有单测契约与颜色逻辑稳定
- 前端视觉一致性第430轮（2026-02-12）：继续重构 `frontend/src/components/Loading/__tests__/LoadingSpinner.test.tsx`，将测试用 `style` 字面量改为命名变量，统一测试代码样式透传写法并减少 JSX 噪音
- 前端视觉一致性第429轮（2026-02-12）：继续推进动态样式表达规范化，优先将可读性差的内联对象字面量替换为语义化命名变量
- 前端视觉一致性第428轮（2026-02-12）：继续统一“动态样式构建函数”模式（`build*Style`），提升跨页面动态色值与 CSS 变量注入的一致性
- 前端视觉一致性第427轮（2026-02-12）：继续优化高频渲染组件（虚拟列表/懒加载图）中的样式组织，减少 JSX 中的样式干扰信息
- 前端视觉一致性第426轮（2026-02-12）：继续清理前端源代码中的 `style={{...}}` 形式，改为变量化透传以提高可维护性与审阅效率
- 前端视觉一致性第425轮（2026-02-12）：开始“动态样式字面量去噪”专项，聚焦 `VirtualList`、`LazyImage`、`EnumFieldPage`、`AssetChart`、`DictionarySelect`、`AssetCard` 与相关测试文件

- 前端视觉一致性第424轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx` 与 `frontend/src/components/Asset/AssetList.module.css`，将汇总行出租率 `strong` 的内联 `color` 迁移为 `occupancyRate*` 语义类映射，统一列表汇总状态色表达
- 前端视觉一致性第423轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx` 与 `frontend/src/components/Asset/AssetList.module.css`，将列表列渲染中“计算出租率”文本内联 `color` 迁移为 `occupancyRate*` 语义类，减少资产列表残余内联样式
- 前端视觉一致性第422轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx`，新增 `getOccupancyRateClassName` 阈值映射以替代内联色值逻辑，保持出租率分级语义不变
- 前端视觉一致性第421轮（2026-02-12）：继续收敛资产列表状态色表达，统一列表行与汇总行出租率色阶规则（>=80 成功、>=60 警告、<60 风险）
- 前端视觉一致性第420轮（2026-02-12）：继续推进资产域组件内联样式清理，聚焦 `AssetList` 残留动态色值表达的语义化迁移
- 前端视觉一致性第419轮（2026-02-12）：继续优化列表汇总区视觉一致性，减少统计值与正文行在状态色表达上的割裂
- 前端视觉一致性第418轮（2026-02-12）：继续清理 `AssetList` 中历史遗留内联样式并同步模块样式命名，提升可维护性
- 前端视觉一致性第417轮（2026-02-12）：继续按“静态优先迁移、动态谨慎保留”策略推进前端样式治理，避免交互回归
- 前端视觉一致性第416轮（2026-02-12）：继续处理资产列表高频渲染路径样式噪音，保证表格性能与视觉表达稳定
- 前端视觉一致性第415轮（2026-02-12）：开始新一轮资产列表样式一致性微重构，针对出租率色彩表达进行语义化收敛

- 前端视觉一致性第414轮（2026-02-12）：继续重构 `frontend/src/components/Analytics/AnalyticsDashboard.tsx` 与 `frontend/src/components/Analytics/AnalyticsDashboard.module.css`，将高级分析占位代码中的 `Row marginBottom` 内联样式收敛为语义类 `advancedMetricsRow`，统一分析模块分区间距表达
- 前端视觉一致性第413轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetCard.tsx` 与 `frontend/src/components/Asset/AssetCard.module.css`，在尝试迁移出租率文本颜色后为兼容既有单测契约暂保留内联颜色表达，并预留状态语义类扩展位，确保行为稳定无回归
- 前端视觉一致性第412轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetAreaSummary.tsx` 并新增 `frontend/src/components/Asset/AssetAreaSummary.module.css`，将空态提示 `textAlign/padding/color` 内联样式迁移为语义类 `emptyState`，统一面积汇总空态视觉表达
- 前端视觉一致性第411轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetAreaSummary.tsx`，完成面积汇总组件样式模块化接入准备，降低空态区 JSX 样式噪音
- 前端视觉一致性第410轮（2026-02-12）：继续推进资产与分析组件样式一致性收敛，完成 `AssetAreaSummary`、`AssetCard`、`AnalyticsDashboard` 三组件的内联样式清理与类名接入
- 前端视觉一致性第409轮（2026-02-12）：继续优化资产卡片出租率文案与进度条状态表达一致性，确保主题变量驱动下的状态色阶稳定
- 前端视觉一致性第408轮（2026-02-12）：继续清理分析页历史残留内联样式（含注释块中的样式表达），统一为可复用语义类
- 前端视觉一致性第407轮（2026-02-12）：继续收敛面积汇总组件空态布局，保持统计卡片加载/空态切换时的视觉稳定性
- 前端视觉一致性第406轮（2026-02-12）：继续减少资产分析域组件中的内联样式与硬编码表达，提升 CSS Modules 可维护性
- 前端视觉一致性第405轮（2026-02-12）：开始新一批资产分析域样式一致性治理，聚焦 `AssetAreaSummary`、`AssetCard`、`AnalyticsDashboard` 三组件

- 后端测试真实性收敛第17轮（2026-02-12）：新增守卫用例 `backend/tests/unit/core/test_integration_test_quality_guard.py`，静态扫描并阻止集成测试回退到 `status_code != 404` / `status_code in (...)` / `except Exception: pass` 等弱模式；定向验证通过（1 passed）。
- 后端测试真实性收敛第16轮（2026-02-12）：定位并修复 `backend/tests/integration/services/asset/test_asset_service.py` 的根因问题（测试对全局数据量做固定假设，原先通过 `try/except pass` 吞掉异常掩盖风险），改为 `uuid marker` 数据隔离 + 精确业务断言（分页/搜索/字段去重），并完成该文件定向回归（18 passed）及全量集成回归（`backend/tests/integration`：361 passed）。
- 前端视觉一致性第404轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportHistoryModal.tsx` 与 `frontend/src/components/Asset/AssetExportHistoryModal.module.css`，将进度条 `marginTop` 内联样式迁移为语义类 `progressBar`，统一导出历史进度展示间距节奏
- 前端视觉一致性第403轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetExportHistoryModal.tsx` 并新增 `frontend/src/components/Asset/AssetExportHistoryModal.module.css`，完成导出历史弹窗局部样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第402轮（2026-02-12）：继续重构 `frontend/src/components/Common/ListToolbar.tsx`，将 `Card` 的 `style={{ ...(cardProps?.style ?? {}) }}` 内联扩展写法收敛为 `style={cardProps?.style}`，统一工具栏卡片样式透传方式并减少 JSX 噪音
- 前端视觉一致性第401轮（2026-02-12）：开始收敛列表工具栏样式透传表达，降低 `ListToolbar` 在可选 `cardProps` 场景下的内联对象展开噪音
- 前端视觉一致性第400轮（2026-02-12）：继续重构 `frontend/src/components/Layout/PageTransition.tsx` 与 `frontend/src/components/Layout/PageTransition.module.css`，将转场容器 `height/width` 内联样式迁移为语义类 `pageTransition`，统一页面动画容器尺寸表达
- 前端视觉一致性第399轮（2026-02-12）：开始重构 `frontend/src/components/Layout/PageTransition.tsx` 并新增 `frontend/src/components/Layout/PageTransition.module.css`，完成页面转场组件样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第398轮（2026-02-12）：继续推进布局与导出模块样式一致性收敛，完成 `PageTransition` / `ListToolbar` / `AssetExportHistoryModal` 三组件迁移准备与文件接入
- 前端视觉一致性第397轮（2026-02-12）：继续清理页面转场与工具栏中的静态内联样式表达，保持组件行为与对外 API 不变
- 前端视觉一致性第396轮（2026-02-12）：继续优化导出历史弹窗的进度信息层级间距，统一列表详情区视觉节奏
- 前端视觉一致性第395轮（2026-02-12）：开始新一批公共布局与导出弹窗样式一致性治理，聚焦 `PageTransition`、`ListToolbar`、`AssetExportHistoryModal` 三组件

- 前端视觉一致性第394轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/FormActionsSection.tsx` 与 `frontend/src/components/Forms/RentContract/FormActionsSection.module.css`，将操作区 `textAlign/marginTop` 内联样式迁移为语义类 `formActionsContainer`，统一合同表单提交区布局节奏
- 前端视觉一致性第393轮（2026-02-12）：开始重构 `frontend/src/components/Forms/RentContract/FormActionsSection.tsx` 并新增 `frontend/src/components/Forms/RentContract/FormActionsSection.module.css`，完成合同表单操作区样式模块化接入，降低 JSX 样式噪音
- 前端视觉一致性第392轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/RentTermsSection.tsx` 与 `frontend/src/components/Forms/RentContract/RentTermsSection.module.css`，将租金条款卡片 `marginBottom` 内联样式迁移为语义类 `rentTermsCard`，统一租金条款分区间距表达
- 前端视觉一致性第391轮（2026-02-12）：开始重构 `frontend/src/components/Forms/RentContract/RentTermsSection.tsx` 并新增 `frontend/src/components/Forms/RentContract/RentTermsSection.module.css`，完成租金条款组件样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第390轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/TenantInfoSection.tsx` 与 `frontend/src/components/Forms/RentContract/TenantInfoSection.module.css`，将承租方信息卡片 `marginBottom` 内联样式迁移为语义类 `tenantInfoCard`，统一合同表单承租方分区节奏
- 前端视觉一致性第389轮（2026-02-12）：开始重构 `frontend/src/components/Forms/RentContract/TenantInfoSection.tsx` 并新增 `frontend/src/components/Forms/RentContract/TenantInfoSection.module.css`，完成承租方信息组件样式模块化接入，降低 JSX 样式噪音
- 前端视觉一致性第388轮（2026-02-12）：继续推进租赁合同表单子分区样式收敛，完成 `TenantInfoSection` / `RentTermsSection` / `FormActionsSection` 的模块化接入与内联样式迁移
- 前端视觉一致性第387轮（2026-02-12）：继续优化租赁合同表单区块间距表达，统一承租方信息、租金条款与操作区在小屏下的视觉节奏基线
- 前端视觉一致性第386轮（2026-02-12）：继续清理租赁合同表单内静态内联样式并迁移到 CSS Modules，保持组件语义和交互行为不变
- 前端视觉一致性第385轮（2026-02-12）：开始新一批租赁合同表单样式一致性治理，聚焦 `TenantInfoSection`、`RentTermsSection`、`FormActionsSection` 三个高频区块

- 后端测试真实性收敛第15轮（2026-02-12）：继续清理认证集成测试中的“仅不为404”宽泛断言，重构 `backend/tests/integration/test_auth_api.py` 的路由结构/兼容性用例为明确契约（按端点断言 `401/400/422`），并完成该文件定向回归（36 passed）及全量集成回归（`backend/tests/integration`：361 passed）验证。
- 前端视觉一致性第384轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.tsx` 与 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.module.css`，将“清空历史”操作区 `textAlign/marginTop` 内联样式迁移为语义类 `clearHistoryWrapper`，统一历史弹窗底部操作区节奏
- 前端视觉一致性第383轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.tsx` 与 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.module.css`，将历史名称编辑框 `style={{ width: 200 }}` 内联样式迁移为语义类 `editNameInput`，统一历史编辑输入宽度表达
- 前端视觉一致性第382轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.tsx` 并新增 `frontend/src/components/Asset/AssetSearch/SearchHistoryModal.module.css`，完成搜索历史弹窗样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第381轮（2026-02-12）：继续重构 `frontend/src/components/Forms/AssetForm.tsx` 与 `frontend/src/components/Forms/AssetForm.module.css`，将进度条列 `marginLeft` 内联样式迁移为语义类 `progressColumn`，统一资产表单完成度区横向间距表达
- 前端视觉一致性第380轮（2026-02-12）：继续重构 `frontend/src/components/Forms/AssetForm.tsx` 与 `frontend/src/components/Forms/AssetForm.module.css`，将完成度卡片 `marginBottom` 内联样式迁移为语义类 `completionCard`，统一表单顶部进度区垂直节奏
- 前端视觉一致性第379轮（2026-02-12）：开始重构 `frontend/src/components/Forms/AssetForm.tsx` 并新增 `frontend/src/components/Forms/AssetForm.module.css`，完成资产表单进度区样式模块化接入，降低 JSX 样式噪音
- 前端视觉一致性第378轮（2026-02-12）：继续重构 `frontend/src/components/Forms/AssetFormHelp.tsx` 与 `frontend/src/components/Forms/AssetFormHelp.module.css`，将帮助按钮文本颜色内联样式迁移为语义类 `helpButton`，统一帮助入口语义色表达并移除硬编码依赖
- 前端视觉一致性第377轮（2026-02-12）：继续重构 `frontend/src/components/Forms/AssetFormHelp.tsx` 与 `frontend/src/components/Forms/AssetFormHelp.module.css`，将帮助弹窗 `style={{ top: 20 }}` 内联样式迁移为 `rootClassName + .helpModal` 语义类方案，统一弹窗垂直偏移表达
- 前端视觉一致性第376轮（2026-02-12）：开始重构 `frontend/src/components/Forms/AssetFormHelp.tsx` 并新增 `frontend/src/components/Forms/AssetFormHelp.module.css`，完成资产表单帮助组件样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第375轮（2026-02-12）：继续推进资产表单相关组件样式收敛，完成 `AssetFormHelp` / `AssetForm` / `SearchHistoryModal` 三组件模块化迁移准备与样式文件接入

- 后端测试真实性收敛第14轮（2026-02-12）：继续收敛集成测试宽泛断言，完成 `backend/tests/integration/test_auth_api.py`、`backend/tests/integration/api/test_project_api_real.py`、`backend/tests/integration/api/test_cookie_auth.py`、`backend/tests/integration/api/test_route_trailing_slash.py` 的精确状态码/错误码断言对齐，并以低内存参数完成定向回归（51 passed）与全量集成回归（`backend/tests/integration`：361 passed）验证无回归。
- 前端视觉一致性第374轮（2026-02-12）：继续重构 `frontend/src/components/Dictionary/DictionarySelect.tsx` 与 `frontend/src/components/Dictionary/DictionarySelect.module.css`，将颜色标识点 `width/height/radius/display` 静态内联样式迁移为语义类 `colorSwatch`，并仅保留动态 `backgroundColor`，统一字典选项色块表达
- 前端视觉一致性第373轮（2026-02-12）：继续重构 `frontend/src/components/Dictionary/DictionarySelect.tsx` 与 `frontend/src/components/Dictionary/DictionarySelect.module.css`，将选项容器 `display/alignItems/gap` 内联样式迁移为语义类 `optionContainer`，统一字典选项图标与文案排布节奏
- 前端视觉一致性第372轮（2026-02-12）：开始重构 `frontend/src/components/Dictionary/DictionarySelect.tsx` 并新增 `frontend/src/components/Dictionary/DictionarySelect.module.css`，完成字典选择器选项模板样式模块化接入，降低 JSX 样式噪音
- 前端视觉一致性第371轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/OtherInfoSection.tsx` 与 `frontend/src/components/Forms/RentContract/OtherInfoSection.module.css`，将基础月租金 `InputNumber` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthControl`，统一合同补充信息字段宽度表达
- 前端视觉一致性第370轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/OtherInfoSection.tsx` 与 `frontend/src/components/Forms/RentContract/OtherInfoSection.module.css`，将“其他信息”卡片 `marginBottom` 内联样式迁移为语义类 `otherInfoCard`，统一租赁表单分区垂直间距节奏
- 前端视觉一致性第369轮（2026-02-12）：开始重构 `frontend/src/components/Forms/RentContract/OtherInfoSection.tsx` 并新增 `frontend/src/components/Forms/RentContract/OtherInfoSection.module.css`，完成其他信息分区样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第368轮（2026-02-12）：继续重构 `frontend/src/components/Router/DynamicRouteRenderer.tsx` 与 `frontend/src/components/Router/DynamicRouteRenderer.module.css`，将错误边界兜底视图的 `padding/textAlign/color/border/radius/margin` 内联样式迁移为语义类 `boundaryErrorContainer`，统一路由错误态信息块表达
- 前端视觉一致性第367轮（2026-02-12）：继续重构 `frontend/src/components/Router/DynamicRouteRenderer.tsx` 与 `frontend/src/components/Router/DynamicRouteRenderer.module.css`，将路由加载错误页 `padding/textAlign/color` 内联样式迁移为语义类 `routeErrorContainer`，统一动态路由错误态表达
- 前端视觉一致性第366轮（2026-02-12）：开始重构 `frontend/src/components/Router/DynamicRouteRenderer.tsx` 并新增 `frontend/src/components/Router/DynamicRouteRenderer.module.css`，完成动态路由渲染器错误态样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第365轮（2026-02-12）：继续推进路由与租赁表单组件样式收敛，完成 `DynamicRouteRenderer` / `OtherInfoSection` / `DictionarySelect` 三组件的模块化迁移准备与文件结构接入

- 前端视觉一致性第364轮（2026-02-12）：继续重构 `frontend/src/components/Rental/DepositLedgerHistory.tsx` 与 `frontend/src/components/Rental/DepositLedgerHistory.module.css`，将卡片根容器 `marginBottom` 内联样式迁移为语义类 `depositLedgerCard`，统一租赁详情押金台账分区间距节奏
- 前端视觉一致性第363轮（2026-02-12）：继续重构 `frontend/src/components/Rental/DepositLedgerHistory.tsx` 与 `frontend/src/components/Rental/DepositLedgerHistory.module.css`，将钱包图标 `marginRight` 内联样式迁移为语义类 `walletIcon`，统一台账标题图标与文本间距表达
- 前端视觉一致性第362轮（2026-02-12）：继续重构 `frontend/src/components/Rental/DepositLedgerHistory.tsx` 与 `frontend/src/components/Rental/DepositLedgerHistory.module.css`，将金额正负色值从内联分支迁移为 `positiveAmount/negativeAmount` 语义类，统一押金变动金额状态表达并减少硬编码
- 前端视觉一致性第361轮（2026-02-12）：开始重构 `frontend/src/components/Rental/DepositLedgerHistory.tsx` 并新增 `frontend/src/components/Rental/DepositLedgerHistory.module.css`，完成押金台账历史组件样式模块化接入，降低 JSX 样式噪音并提升可维护性
- 前端视觉一致性第360轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/BasicInfoSection.tsx` 与 `frontend/src/components/Forms/RentContract/BasicInfoSection.module.css`，将“服务费率”输入框 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthControl`，统一委托运营表单字段宽度表达
- 前端视觉一致性第359轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/BasicInfoSection.tsx` 与 `frontend/src/components/Forms/RentContract/BasicInfoSection.module.css`，将“签订日期”控件 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthControl`，统一租赁合同基础信息日期控件宽度表达
- 前端视觉一致性第358轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/BasicInfoSection.tsx` 与 `frontend/src/components/Forms/RentContract/BasicInfoSection.module.css`，将基础信息卡片 `marginBottom` 内联样式迁移为语义类 `basicInfoCard`，统一合同表单分区垂直间距节奏
- 前端视觉一致性第357轮（2026-02-12）：开始重构 `frontend/src/components/Forms/RentContract/BasicInfoSection.tsx` 并新增 `frontend/src/components/Forms/RentContract/BasicInfoSection.module.css`，完成合同基础信息分区样式模块化接入，减少 JSX 样式噪音
- 前端视觉一致性第356轮（2026-02-12）：继续重构 `frontend/src/components/Loading/LoadingProvider.tsx` 与 `frontend/src/components/Loading/LoadingProvider.module.css`，将 `LoadingButton` 内联 `opacity/cursor` 状态样式迁移为 `loadingButton*` 语义类，统一加载按钮禁用与加载态交互反馈
- 前端视觉一致性第355轮（2026-02-12）：开始重构 `frontend/src/components/Loading/LoadingProvider.tsx` 并新增 `frontend/src/components/Loading/LoadingProvider.module.css`，将全局遮罩层与按钮加载指示器间距的静态内联样式迁移为 `globalLoadingOverlay/loadingIndicator` 语义类，统一全局加载视觉表达

- 前端视觉一致性第354轮（2026-02-12）：继续重构 `frontend/src/components/Loading/LoadingSpinner.tsx` 与 `frontend/src/components/Loading/LoadingSpinner.module.css`，将无子内容模式容器 `display/flexDirection/alignItems/justifyContent/padding` 内联样式迁移为语义类 `spinnerContainer`，统一加载占位区布局节奏并减少 JSX 样式噪音
- 前端视觉一致性第353轮（2026-02-12）：开始重构 `frontend/src/components/Loading/LoadingSpinner.tsx` 并新增 `frontend/src/components/Loading/LoadingSpinner.module.css`，将图标与提示文字的尺寸分支从内联 `fontSize` 收敛为 `icon*/tip*` 状态语义类映射，统一加载反馈层级表达
- 前端视觉一致性第352轮（2026-02-12）：继续重构 `frontend/src/components/Router/RouteTransitions.tsx` 与 `frontend/src/components/Router/RouteTransitions.module.css`，将性能优化转场 `willChange` 内联样式迁移为语义类 `optimizedTransition`，统一转场性能提示表达
- 前端视觉一致性第351轮（2026-02-12）：继续重构 `frontend/src/components/Router/RouteTransitions.tsx` 与 `frontend/src/components/Router/RouteTransitions.module.css`，将 `PageTransitionContainer` 的静态 `position/width/height/overflow` 内联样式迁移为语义类 `pageTransitionContainer`，并保留外部 `style` 扩展能力
- 前端视觉一致性第350轮（2026-02-12）：继续重构 `frontend/src/components/Router/RouteTransitions.tsx` 与 `frontend/src/components/Router/RouteTransitions.module.css`，将路由切页动画容器 `motion.div` 的绝对定位与全尺寸内联样式迁移为语义类 `routeTransitionMotion`，统一页面切换层叠布局表达
- 前端视觉一致性第349轮（2026-02-12）：开始重构 `frontend/src/components/Router/RouteTransitions.tsx` 并新增 `frontend/src/components/Router/RouteTransitions.module.css`，完成路由转场组件样式模块化接入，降低转场容器 JSX 样式噪音
- 前端视觉一致性第348轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/ContractPeriodSection.tsx` 与 `frontend/src/components/Forms/RentContract/ContractPeriodSection.module.css`，将押金输入框 `InputNumber` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthControl`，统一合同期限区字段宽度表达
- 前端视觉一致性第347轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/ContractPeriodSection.tsx` 与 `frontend/src/components/Forms/RentContract/ContractPeriodSection.module.css`，将开始/结束日期 `DatePicker` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthControl`，统一日期控件宽度表达
- 前端视觉一致性第346轮（2026-02-12）：继续重构 `frontend/src/components/Forms/RentContract/ContractPeriodSection.tsx` 与 `frontend/src/components/Forms/RentContract/ContractPeriodSection.module.css`，将合同期限卡片 `marginBottom` 内联样式迁移为语义类 `contractPeriodCard`，统一表单分区垂直间距节奏
- 前端视觉一致性第345轮（2026-02-12）：开始重构 `frontend/src/components/Forms/RentContract/ContractPeriodSection.tsx` 并新增 `frontend/src/components/Forms/RentContract/ContractPeriodSection.module.css`，完成合同期限分区样式模块化接入，降低 JSX 样式噪音并提升租赁表单视觉一致性

- 集成测试真实化深化（2026-02-12，P0 深化批-13）：完成资产生命周期集成测试去“兼容兜底”重构。`backend/tests/integration/test_asset_lifecycle.py` 移除历史字段（`name/code/area/asset_type`）与宽泛 `status_code in [...]` 断言，改为真实登录 + CSRF 下的可执行链路：基于真实 `Ownership` 种子执行 `assets` 创建/详情/更新/删除闭环，并补充 `search + usage_status` 组合筛选语义校验；同时对接当前枚举基线（`property_nature=经营类`、`usage_status=闲置`）避免伪通过。低内存验证：`test_asset_lifecycle.py` `2 passed`，全量 `tests/integration` 维持 `361 passed`（`0 skipped`）。
- 集成测试真实化深化（2026-02-12，P0 深化批-12）：继续清理合同工作流集成测试中的宽泛状态码兼容断言。`backend/tests/integration/test_contract_workflow.py` 新增真实登录 cookie/CSRF 夹具，移除旧式 `status_code in [...]` 兜底集合与空 `pass` 用例，改为稳定断言 `contracts` 列表结构，以及 `detail/renew/terminate` 对不存在合同的 `404 + RESOURCE_NOT_FOUND` 语义。低内存验证：`test_contract_workflow.py` `4 passed`，全量 `tests/integration` 更新为 `361 passed`（`0 skipped`）。
- 前端视觉一致性第344轮（2026-02-12）：继续重构 `frontend/src/components/Common/LazyImage.tsx` 与 `frontend/src/components/Common/LazyImage.module.css`，将背景懒加载容器的静态 `position/overflow` 内联样式迁移为语义类 `lazyBackgroundContainer`，统一背景图容器定位与裁切表达
- 前端视觉一致性第343轮（2026-02-12）：继续重构 `frontend/src/components/Common/LazyImage.tsx` 与 `frontend/src/components/Common/LazyImage.module.css`，将前景懒加载图片显示态 `display` 由内联样式收敛为 `lazyImageVisible/lazyImageHidden` 状态语义类，统一图片显隐表达
- 前端视觉一致性第342轮（2026-02-12）：继续重构 `frontend/src/components/Common/LazyImage.tsx` 与 `frontend/src/components/Common/LazyImage.module.css`，将图片与骨架占位层 `width/height/objectFit` 静态样式迁移为 `lazyImageContent/lazyImageSkeleton` 语义类，统一懒加载图片占位尺寸表达
- 前端视觉一致性第341轮（2026-02-12）：继续重构 `frontend/src/components/Common/LazyImage.tsx` 与 `frontend/src/components/Common/LazyImage.module.css`，将懒加载根容器的静态 `display: inline-block` 内联样式迁移为语义类 `lazyImageContainer`，保留动态 `width/height/style` 扩展能力
- 前端视觉一致性第340轮（2026-02-12）：开始重构 `frontend/src/components/Common/LazyImage.tsx` 并新增 `frontend/src/components/Common/LazyImage.module.css`，完成懒加载图片组件样式模块化接入，减少静态内联样式噪音并提升可维护性
- 前端视觉一致性第339轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetBatchActions.tsx` 与 `frontend/src/components/Asset/AssetBatchActions.module.css`，将批量编辑说明 `Alert` 的 `marginBottom` 内联样式迁移为语义类 `batchEditAlert`，统一批量编辑弹窗提示区间距表达
- 前端视觉一致性第338轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetBatchActions.tsx` 与 `frontend/src/components/Asset/AssetBatchActions.module.css`，将“清空选择”按钮 `padding` 内联样式迁移为语义类 `clearSelectionButton`，统一批量操作区轻量链接按钮触达节奏
- 前端视觉一致性第337轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetBatchActions.tsx` 与 `frontend/src/components/Asset/AssetBatchActions.module.css`，将批量操作头部 `display/justifyContent/alignItems` 内联样式迁移为语义类 `batchActionHeader`，统一批量操作区头部布局表达
- 前端视觉一致性第336轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetBatchActions.tsx` 与 `frontend/src/components/Asset/AssetBatchActions.module.css`，将批量操作卡片 `marginBottom` 内联样式迁移为语义类 `batchActionCard`，统一批量操作区与表格间距节奏
- 前端视觉一致性第335轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetBatchActions.tsx` 并新增 `frontend/src/components/Asset/AssetBatchActions.module.css`，完成资产批量操作组件样式模块化接入，降低 JSX 样式噪音并提升视觉一致性维护效率

- 集成测试真实化深化（2026-02-12，P0 深化批-11）：继续收敛产权证 E2E 场景的“宽泛状态码”与“文档式断言”。`backend/tests/integration/api/test_property_certificate_e2e.py` 将 `test_upload_extract_confirm_flow` 重构为可重复的真实链路验证：在仅对 `PropertyCertificateService.extract_from_file` 做最小边界桩替代外部 LLM 依赖的前提下，完整覆盖 `upload -> confirm-import -> detail` 成功路径并校验关键字段（`certificate_number/property_address/certificate_id`）；同步为匹配资产场景的提取桩补齐证书基础字段，减少隐式前提。低内存验证：`test_property_certificate_e2e.py` `6 passed`，全量 `tests/integration` 保持 `360 passed`（`0 skipped`）。
- 前端视觉一致性第334轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectDetail.tsx` 与 `frontend/src/components/Project/ProjectDetail.module.css`，将“项目信息/系统信息”卡片 `marginTop` 内联样式迁移为语义类 `sectionCard`，统一详情分区卡片垂直节奏表达
- 前端视觉一致性第333轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectDetail.tsx` 与 `frontend/src/components/Project/ProjectDetail.module.css`，将项目标题 `h2` 的 `margin: 0` 内联样式迁移为语义类 `projectTitle`，统一详情头部标题排版表达
- 前端视觉一致性第332轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectDetail.tsx` 与 `frontend/src/components/Project/ProjectDetail.module.css`，将详情头部行 `display/justifyContent/alignItems` 内联样式迁移为语义类 `headerRow`，统一详情页头布局表达并减少 JSX 样式噪音
- 前端视觉一致性第331轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectDetail.tsx` 与 `frontend/src/components/Project/ProjectDetail.module.css`，将详情页根容器由全局类名收敛为模块化语义类 `projectDetail`，降低样式泄漏风险并提升可维护性
- 前端视觉一致性第330轮（2026-02-12）：开始重构 `frontend/src/components/Project/ProjectDetail.tsx` 并新增 `frontend/src/components/Project/ProjectDetail.module.css`，完成项目详情组件样式模块化接入，收敛静态内联样式并统一卡片分区观感
- 前端视觉一致性第329轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectSelect.tsx` 与 `frontend/src/components/Project/ProjectSelect.module.css`，将项目编码提示文本 `color/fontSize` 内联样式迁移为语义类 `codeText`，统一下拉选项辅助信息层级表达
- 前端视觉一致性第328轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectSelect.tsx` 与 `frontend/src/components/Project/ProjectSelect.module.css`，将项目简称提示文本 `color/fontSize` 内联样式迁移为语义类 `shortNameText`，统一项目下拉选项辅助文案表达
- 前端视觉一致性第327轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectSelect.tsx` 与 `frontend/src/components/Project/ProjectSelect.module.css`，将项目选择器 `Select` 的 `style={{ flex: 1 }}` 内联样式迁移为语义类 `flexSelect`，统一组合控件主输入区伸缩策略
- 前端视觉一致性第326轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectSelect.tsx` 与 `frontend/src/components/Project/ProjectSelect.module.css`，将组合容器 `Space.Compact` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthCompact`，统一项目选择器宽度表达
- 前端视觉一致性第325轮（2026-02-12）：开始重构 `frontend/src/components/Project/ProjectSelect.tsx` 并新增 `frontend/src/components/Project/ProjectSelect.module.css`，完成项目选择组件样式模块化接入，减少下拉选项与组合容器内联样式噪音

- 前端视觉一致性第324轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportForm.tsx` 与 `frontend/src/components/Asset/AssetExportForm.module.css`，将搜索条件标签容器 `marginTop` 内联样式迁移为语义类 `searchTagWrapper`，统一导出范围说明区内部节奏并减少 JSX 样式噪音
- 前端视觉一致性第323轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportForm.tsx` 与 `frontend/src/components/Asset/AssetExportForm.module.css`，将“已选导出范围提示/搜索条件提示”两个 `Alert` 的 `marginBottom` 内联样式迁移为语义类 `scopeAlert`，统一导出提示块间距表达
- 前端视觉一致性第322轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportForm.tsx` 与 `frontend/src/components/Asset/AssetExportForm.module.css`，将 `Checkbox.Group` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthGroup`，统一导出字段选择区宽度表达
- 前端视觉一致性第321轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportForm.tsx` 与 `frontend/src/components/Asset/AssetExportForm.module.css`，将导出卡片根容器 `marginBottom` 内联样式迁移为语义类 `exportCard`，统一列表工具区与导出卡片垂直间距节奏
- 前端视觉一致性第320轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetExportForm.tsx` 并新增 `frontend/src/components/Asset/AssetExportForm.module.css`，完成导出表单样式模块化接入，降低资产导出表单 JSX 样式噪音并提升可维护性
- 前端视觉一致性第319轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx` 与 `frontend/src/components/Asset/AssetList.module.css`，将彻底删除确认错误提示 `color` 内联样式迁移为语义类 `hardDeleteHelp`，统一危险提示语义色表达并减少硬编码色值
- 前端视觉一致性第318轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx` 与 `frontend/src/components/Asset/AssetList.module.css`，将彻底删除确认弹窗内容区 `Space` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `hardDeleteSpace`，统一确认弹窗内容区宽度表达
- 前端视觉一致性第317轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx` 与 `frontend/src/components/Asset/AssetList.module.css`，将“物业名称”链接按钮的 `padding/height/textAlign` 内联样式迁移为语义类 `propertyNameButton`，统一列表主字段可点击文本按钮样式表达
- 前端视觉一致性第316轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetList.tsx` 与 `frontend/src/components/Asset/AssetList.module.css`，补齐列表样式模块并集中沉淀硬删确认区静态布局样式，保持动态出租率色值逻辑不变
- 前端视觉一致性第315轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetList.tsx` 并新增 `frontend/src/components/Asset/AssetList.module.css`，完成资产列表样式模块化接入，收敛列表列渲染中的静态内联样式并降低后续视觉维护成本

- 集成测试真实化深化（2026-02-12，P0 深化批-10）：继续削减 mock 并修复真实链路根因。`backend/tests/integration/test_analytics_api.py` 移除 monkeypatch 与源码字符串检查，改为真实登录 + 真实数据注入断言（综合统计结构、趋势/分布契约、非法分布类型、OpenAPI 路由注册）；同时修复后端真实行为偏差：`backend/src/services/analytics/analytics_service.py` 新增显式 `data_status` 过滤构建逻辑，解决 `include_deleted=true` 被 QueryBuilder 默认软删除过滤覆盖导致“不生效”的根因；`backend/src/api/v1/analytics/analytics.py` 对分布接口补充 `BaseBusinessError` 透传，恢复非法参数 `400` 业务错误语义。低内存验证：`tests/integration/test_analytics_api.py` `11 passed`，全量 `tests/integration` `360 passed`（`0 skipped`，`1 warning`）。
- 前端视觉一致性第314轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.tsx` 与 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.module.css`，将排序顺序 `Select` 的 `style={{ width: '40%' }}` 内联样式迁移为语义类 `sortOrderSelect`，统一排序控件组合宽度比例并减少 JSX 样式噪音
- 前端视觉一致性第313轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.tsx` 与 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.module.css`，将排序字段 `Select` 的 `style={{ width: '60%' }}` 内联样式迁移为语义类 `sortFieldSelect`，统一高级筛选排序区主次控件比例表达
- 前端视觉一致性第312轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.tsx` 与 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.module.css`，将创建日期 `RangePicker` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthControl`，统一高级筛选日期控件宽度表达
- 前端视觉一致性第311轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.tsx` 与 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.module.css`，将面积区间分隔输入框的 `width/borderLeft/borderRight/pointerEvents` 内联样式迁移为语义类 `areaRangeSeparator`，统一区间输入分隔表达并减少重复样式
- 前端视觉一致性第310轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.tsx` 并新增 `frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.module.css`，将面积最小值/最大值输入框的 `style={{ width: '45%' }}` 内联样式迁移为语义类 `areaInput`，统一高级筛选面积区间控件宽度节奏
- 前端视觉一致性第309轮（2026-02-12）：继续重构 `frontend/src/components/Common/VirtualList.tsx` 与 `frontend/src/components/Common/VirtualList.module.css`，将虚拟网格总尺寸占位层的 `position: relative` 内联样式迁移为语义类 `virtualGridSpacer`，统一网格滚动内容定位基准表达
- 前端视觉一致性第308轮（2026-02-12）：继续重构 `frontend/src/components/Common/VirtualList.tsx` 与 `frontend/src/components/Common/VirtualList.module.css`，将虚拟网格容器 `position/overflow` 内联样式迁移为语义类 `virtualGridContainer`，并保留动态 `height/width` 与外部 `style` 扩展能力
- 前端视觉一致性第307轮（2026-02-12）：继续重构 `frontend/src/components/Common/VirtualList.tsx` 与 `frontend/src/components/Common/VirtualList.module.css`，将虚拟网格项 `position: absolute` 内联样式迁移为语义类 `virtualGridItem`，统一网格单元定位语义并减少重复样式
- 前端视觉一致性第306轮（2026-02-12）：继续重构 `frontend/src/components/Common/VirtualList.tsx` 与 `frontend/src/components/Common/VirtualList.module.css`，将虚拟列表容器与总高度占位层的静态 `position/overflow` 内联样式迁移为语义类 `virtualListContainer`、`virtualListSpacer`，统一列表滚动布局表达
- 前端视觉一致性第305轮（2026-02-12）：开始重构 `frontend/src/components/Common/VirtualList.tsx` 并新增 `frontend/src/components/Common/VirtualList.module.css`，将虚拟列表项的静态 `position/left/right` 内联样式迁移为语义类 `virtualListItem`，保留动态 `top/height` 计算，减少虚拟滚动组件 JSX 样式噪音

- 集成测试真实化深化（2026-02-12，P0 深化批-9）：完成 `backend/tests/integration/test_pdf_batch_api.py` 的去 mock 重构，移除 `PDFImportService` 级别打桩，切换为真实登录认证链路（`/api/v1/auth/login` + cookie/CSRF），并补强批量上传、状态查询、列表过滤、取消、清理、健康检查与上传后查询工作流的真实接口断言；同时按当前中间件真实返回契约收敛错误断言，避免仅依赖宽泛状态码。低内存验证通过：单文件 `17 passed`，全量 `tests/integration` 回归更新为 `360 passed`（`0 skipped`）。
- 集成测试真实化深化（2026-02-12，P0 深化批-8）：完成当前集成目录最后两处宽泛状态码断言收口。`backend/tests/integration/test_analytics_api.py` 将 debug 缓存端点断言进一步收敛为稳定 `404 + success=false`（测试环境下 debug-only + localhost 限制的真实行为）；`backend/tests/integration/test_pdf_import_modular.py` 将批量上传缺文件场景收敛为 `422` 并校验统一验证错误结构（`error.details.field_errors` 包含 `body.files`），替代原先 `400/422` 宽泛断言。低内存全量集成回归保持 `359 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批-7）：继续压缩分析与合同 V2 用例中的宽泛断言。`backend/tests/integration/test_analytics_api.py` 为综合分析/趋势/分布/缓存接口补充 `success/data` 结构断言，并将 debug 缓存端点从宽泛状态码收敛为 `200/404`（且 404 校验统一错误语义）；`backend/tests/integration/test_rent_contract_v2_api.py` 将创建与续签场景请求体调整为可通过 schema 的最小有效数据（单 rent term、空 `asset_ids`、不存在 `ownership_id`），把断言收敛为稳定 `404 + success=false`，并修正 query 参数名 `should_transfer_deposit` 与 `should_refund_deposit`。低内存全量集成回归保持 `359 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批-6）：进一步收敛服务层不存在资源场景的宽泛状态码断言。`backend/tests/integration/services/test_ownership_service_complete.py` 将删除/切换状态/更新关联项目的不存在权属方场景统一收敛为 `404 + success=false`，并将项目更新请求体修正为真实接口形态（`list[str]`）；`backend/tests/integration/services/test_contract_renewal_service.py` 将统计概览端点收敛为稳定 `200` 结构断言，续签与终止不存在合同场景收敛为 `404 + success=false`，并修正终止参数名为 `should_refund_deposit`。低内存全量集成回归保持 `359 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批-5）：补强认证权限返回契约测试，减少重复“字段存在性”烟测。`backend/tests/integration/api/test_auth_permissions.py` 新增统一登录辅助函数，验证登录响应中的 `auth_mode` 与 cookie 下发（`auth_token/csrf_token/refresh_token`），补充权限列表去重与管理员关键权限（`system:admin`）断言，并新增错误密码拒绝与不下发认证 cookie 的真实行为校验。低内存全量集成回归更新为 `359 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批-4）：收敛租赁合同 V2 集成用例中的宽泛状态码断言。`backend/tests/integration/test_rent_contract_v2_api.py` 将创建/续签/终止场景从“任意通过码”升级为“受控错误语义”断言：创建与续签限定为 `404/422` 并校验统一错误响应结构（`success/error` 或 `detail`）；终止不存在合同限定 `404` 并校验失败语义；统计端点由宽泛状态码改为稳定的 `200 + list` 结构断言。低内存全量集成回归保持 `358 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批-3）：继续补强资产筛选接口的真实数据语义校验。`backend/tests/integration/api/test_asset_filter_endpoints.py` 新增基于真实数据库写入的断言：`/assets/ownership-entities` 验证权属名称按 `data_status=正常` 过滤、去重、排序与空字符串剔除；新增 `/assets/business-categories|usage-statuses|property-natures|ownership-statuses` 对真实种子数据的包含性、唯一性、排序性验证。针对同进程集成执行中的 distinct 值缓存干扰，测试内在数据注入后显式调用 `asset_crud.clear_cache()`，确保断言基于最新数据状态。低内存全量集成回归更新为 `358 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批-2）：继续收敛认证与系统模块中的“存在性/宽松状态码”断言。`backend/tests/integration/test_auth_api.py` 新增真实登录 fixture 与 CSRF 头，补齐 `login → me → refresh → logout` 实链路断言，并为用户列表、用户统计、会话列表、审计统计、安全配置增加管理员语义校验；`backend/tests/integration/api/test_system_info_real.py` 补充未认证拒绝、`/api/v1/system/root` 路由导航契约与 `build_time` 时间格式校验。同时修复真实链路暴露的时区根因：`backend/src/models/auth.py` 中 `is_locked_now` 与 `is_expired` 的 aware/naive `datetime` 比较不一致，统一改为 UTC aware 比较，消除刷新令牌链路 `TypeError`。低内存全量集成回归更新为 `356 passed`（`0 skipped`）
- 集成测试真实化深化（2026-02-12，P0 深化批）：将此前替换为 smoke 的 4 个服务集成模块继续升级为“创建→查询→变更→清理”闭环断言，减少仅状态码级验证。`backend/tests/integration/services/test_organization_service_deep.py` 新增组织创建/路径/搜索/软删除语义校验；`backend/tests/integration/services/test_organization_service_enhanced.py` 增强统计一致性与更新+历史+搜索链路；`backend/tests/integration/services/test_ownership_service_complete.py` 增强权属方创建/搜索/切换状态/下拉/删除与统计一致性断言；`backend/tests/integration/services/test_contract_renewal_service.py` 增强合同列表与统计响应结构校验并补充错误响应语义断言。低内存全量集成回归更新为 `346 passed`（`0 skipped`）
- 集成测试真实化补强（2026-02-12，P0 第六批）：将剩余两组 legacy skip 模块完成异步对齐替换。`backend/tests/integration/services/test_contract_renewal_service.py` 重写为真实登录 + CSRF 的续签/终止链路 smoke（含合同列表与统计概览）；`backend/tests/integration/services/test_ownership_service_complete.py` 重写为真实登录的权属端点 smoke（列表、下拉、财务汇总不存在、删除/切换状态/项目绑定不存在校验），彻底移除模块级跳过依赖。低内存全量集成回归更新为 `345 passed`（`0 skipped`）
- 集成测试真实化补强（2026-02-12，P0 第五批）：替换两份组织服务遗留占位跳过模块 `backend/tests/integration/services/test_organization_service_deep.py` 与 `backend/tests/integration/services/test_organization_service_enhanced.py`，改为真实登录 smoke 用例，覆盖组织树、路径不存在、统计与搜索端点，移除模块级 `pytest.skip` 依赖并与当前异步实现对齐。低内存全量集成回归更新为 `335 passed, 29 skipped`（较上一轮 `331 passed, 31 skipped` 再减少 2 个跳过）
- 集成测试真实化补强（2026-02-12，P0 第四批）：重构 `backend/tests/integration/test_rent_contract_v2_api.py`，移除全局 JWT 跳过与路由层 patch 依赖，切换为真实登录 + cookie/CSRF 访问；覆盖 V2 合同创建、续签、终止与统计端点的可达性/参数校验，并保留 schema 级 V2 字段验证（`service_fee_rate`、`tenant_usage`、`payment_cycle`）。低内存全量集成回归更新为 `331 passed, 31 skipped`（较上一轮 `323 passed, 39 skipped` 再减少 8 个跳过）
- 集成测试真实化补强（2026-02-12，P0 第三批）：重写 `backend/tests/integration/test_pdf_import_modular.py` 以对齐当前版本化路由与真实鉴权链路，移除全局 JWT 跳过并统一改为登录后 cookie 访问；覆盖 `/api/v1/pdf-import/info|health|sessions` 及 `/api/v1/pdf-import/batch/*`（health/list/status/cancel/cleanup/upload 参数校验），同时补充 `openapi` 注册校验与旧路径 `/api/pdf-import/info` 的 404 兼容断言。低内存全量集成回归更新为 `323 passed, 39 skipped`（较上一轮 `312 passed, 53 skipped` 再减少 14 个跳过）
- 集成测试真实化补强（2026-02-12，P0 第二批）：取消 `backend/tests/integration/test_analytics_api.py` 的全局 JWT 跳过标记并切换为真实登录链路（`authenticated_client` 注入 `auth_token/csrf_token`），恢复分析端点的真实认证访问；写操作 `POST /api/v1/analytics/cache/clear` 增加 CSRF 头传递，调试端点测试改为 `raise_server_exceptions=False` 以稳定覆盖 debug-only 场景；同步修正模块导入断言目标（`src.api.v1.analytics.analytics`）以避免 `inspect.getsource` 指向包入口失败。低内存全量集成回归更新为 `312 passed, 53 skipped`（较上一轮 `301 passed, 64 skipped` 再减少 11 个跳过）
- 集成测试真实化补强（2026-02-12）：取消 `backend/tests/integration/test_auth_api.py` 与 `backend/tests/integration/api/test_asset_filter_endpoints.py` 的全局 JWT 跳过标记，恢复真实链路执行；其中资产筛选集成测试改为真实登录获取 cookie 后访问受保护端点（不再使用 dummy `test_token`），并同步修复 `backend/src/crud/ownership.py` 的 `get_names_by_status_async` 下拉数据查询（增加 `distinct + order_by` 以及空值过滤），消除真实数据下权属方重复值问题。低内存全量集成回归更新为 `301 passed, 64 skipped`（较上一轮 `264 passed, 101 skipped` 实质减少 37 个跳过）
- 真实链路测试补强（2026-02-11）：新增低内存可执行的真实集成回归用例。`backend/tests/integration/api/test_project_api_real.py` 覆盖项目 API 的登录鉴权 + CRUD + 列表搜索完整链路；`backend/tests/integration/api/test_system_info_real.py` 覆盖 `/api/v1/system/info` 的真实依赖路径（含数据库连通状态字段契约）；`backend/tests/integration/crud/test_project_tenant_scope_real.py` 覆盖 `project_crud.get_multi` 在双组织真实数据下的租户隔离与空组织 fail-closed 行为；`backend/tests/integration/api/test_project_visibility_real.py` 新增非管理员跨组织项目可见性隔离验证。并在 `backend/tests/integration/conftest.py` 的 `AsyncSessionAdapter` 补充 `scalar` 异步适配，确保系统信息链路与真实数据库调用一致。执行 `tests/integration` 低内存回归结果更新为 `264 passed, 101 skipped`
- 真实环境单测稳定性补丁（2026-02-11）：修复 `source backend/.env` 后 `tests/unit` 暴露的历史兼容问题。`backend/tests/unit/conftest.py` 的 `AsyncSessionAdapter` 新增 `scalar` 异步适配并为 mock 用户显式设置 `default_organization_id=None`，避免 `MagicMock` 属性渗透；`backend/tests/unit/api/v1/test_project.py` 新增测试组织夹具并将 `project_data/admin_user` 绑定同一 `organization_id`，恢复组织隔离场景下的项目 API 行为一致性；`backend/tests/unit/crud/test_ownership.py` 同步断言 `build_query(..., tenant_filter=None)`，对齐当前查询参数签名
- 集成测试告警清理（2026-02-11）：消除 Starlette `TestClient` 每请求 `cookies=` 传参弃用告警。更新 `backend/tests/integration/api/test_cookie_auth.py` 与 `backend/tests/integration/api/test_token_blacklist_api.py`，统一改为在客户端会话级注入 cookie（`client.cookies.set(...)`）后发起请求，保持用例语义不变并与后续版本兼容
- 集成回归补丁（2026-02-11）：修复 3 个集成阻塞点。其一，`backend/tests/integration/crud/test_asset_encryption.py` 的无密钥降级夹具补充 `REQUIRE_ENCRYPTION=false`，恢复“无密钥明文存储”与“明文/密文混合读取”场景；其二，`backend/src/crud/base.py` 修复 `update/remove` 缓存失效逻辑，新增按 `id` 清理租户隔离 `get` 缓存键能力（`_clear_get_cache_for_id`），避免删除后同请求上下文读取命中陈旧缓存；其三，新增单测 `backend/tests/unit/crud/test_base_distinct_values.py` 覆盖 tenant-scoped 缓存清理行为，防止回归
- 审批方案文档落盘（2026-02-11）：新增 `docs/plans/2026-02-11-approval-flowable-b-plan.md`，沉淀“B 方案（Flowable 编排内核 + 业务系统任务中心）”的完整执行蓝图，覆盖架构边界、接口契约、数据模型、权限安全、测试验收与分周里程碑，供后续实施与评审使用
- 文档规划补充（2026-02-11）：新增 `docs/plans/langextract-rollout-plan-2026-02-11.md`，沉淀 `langextract` 在现有 PDF 导入链路的灰度并行接入方案（开关策略、接口契约、融合规则、验收指标与分阶段推进计划），供后续评估是否切换主抽取链路
- 全局组织数据隔离能力第3周第十五阶段（2026-02-11）：落地核心业务表“物理组织键”并打通写入链路。新增迁移 `backend/alembic/versions/20260211_add_organization_scope_columns.py`，为 `projects/assets/property_certificates` 增加 `organization_id`（含外键与索引），并按 `created_by -> users.default_organization_id` 回填历史数据（产权证补充 `owner.organization_id` 回填兜底）；同步更新模型 `backend/src/models/project.py`、`backend/src/models/asset.py`、`backend/src/models/property_certificate.py` 与字段白名单 `backend/src/crud/field_whitelist.py`。创建链路同步写入组织ID：项目创建（`backend/src/api/v1/assets/project.py` + `backend/src/services/project/service.py`）、资产创建与导入（`backend/src/services/asset/asset_service.py`、`backend/src/services/asset/import_service.py`、`backend/src/services/excel/excel_import_service.py`、`backend/src/api/v1/documents/excel/import_ops.py`）、产权证创建与确认导入（`backend/src/services/property_certificate/service.py`、`backend/src/api/v1/assets/property_certificate.py`、`backend/src/crud/property_certificate.py`）。并更新 CRUD 单测以覆盖新组织键路径：`backend/tests/unit/crud/test_project.py`、`backend/tests/unit/crud/test_asset.py`、`backend/tests/unit/crud/test_property_certificate.py`
- 全局组织数据隔离能力第3周第十五阶段补丁（2026-02-11）：修复 Excel 导入服务在预加载权属数据时的同名局部变量遮蔽问题（`backend/src/services/excel/excel_import_service.py`，将行内 `ownership` 变量改为 `ownership_record`，避免触发 `UnboundLocalError`）；同步调整 `backend/tests/unit/services/excel/test_excel_import_service.py` 的 mock 适配新预加载路径（补充 `asset_crud.get_by_property_names_async` 异步桩），恢复重复资产、批量提交、严格模式回滚、更新/跳过已有资产等场景稳定通过
- 低内存单测兼容补丁（2026-02-11）：为认证/用户路由补充“Factory 新路径 + 直接函数调用旧路径”兼容层，恢复单测可注入性。`backend/src/api/v1/auth/auth_modules/authentication.py`、`backend/src/api/v1/auth/auth_modules/users.py`、`backend/src/api/v1/debug/auth_debug.py` 新增兼容适配符号（如 `UserCRUD`、`AuditLogCRUD`）与 `db` 直传分支，保持线上依赖注入仍走 `ServiceFactory`；同步修正测试基线以匹配当前实现：`backend/tests/unit/core/test_auth_split_verification.py`（服务构造参数对齐）、`backend/tests/unit/core/test_blacklist.py`（熔断策略改为全环境 fail-closed）、`backend/tests/unit/crud/test_query_builder.py`（“无 organization_id 模型”用例切换至 `Ownership`）。已通过对应回归集验证
- API 单测告警降噪补丁（2026-02-11）：集中清理 `tests/unit/api/v1` 路径的高频告警。`backend/src/crud/asset.py` 增加 AsyncMock 兼容结果读取助手（`_result_all/_scalars_all/_scalars_first/_result_first/_result_scalar`），消除 analytics 路径的 “coroutine was never awaited” 运行时告警；`backend/src/core/exception_handler.py`、`backend/src/core/response_handler.py`、`backend/src/api/v1/assets/property_certificate.py` 以及相关 API/集成测试文件统一将 `HTTP_422_UNPROCESSABLE_ENTITY` 切换为 `HTTP_422_UNPROCESSABLE_CONTENT`；并同步完成 `backend/src/api/v1/system/tasks.py` 的 `Query(regex=...) -> Query(pattern=...)` 与 `backend/src/schemas/llm_prompt.py`、`backend/src/schemas/property_certificate.py`、`backend/src/schemas/contact.py`、`backend/src/api/v1/system/notifications.py`、`backend/src/services/document/base.py` 的 Pydantic `ConfigDict` 迁移。回归结果：`tests/unit/api/v1` 维持全绿（`675 passed, 269 skipped`），warnings 从 48 收敛到 2（仅 pytest 配置项告警）
- 文档导入后台任务告警补丁（2026-02-11）：`backend/src/services/document/pdf_import_service.py` 在 `asyncio.create_task` 测试替身场景下增加协程关闭兜底（当返回对象非 `asyncio.Task` 时主动 `close()`），消除 `tests/unit/services/document/test_pdf_import_service.py` 中的未等待协程告警；对应回归通过（`47 passed, 2 skipped`）
- 单测标记与 pytest 配置告警清理（2026-02-11）：将 11 个被 `pytest.mark.asyncio` 误标记为同步函数的测试统一改为 `async def`（`backend/tests/unit/services/analytics/test_occupancy_service.py`、`backend/tests/unit/services/asset/test_import_service.py`、`backend/tests/unit/services/core/test_audit_service.py`、`backend/tests/unit/services/llm_prompt/test_auto_optimizer.py`、`backend/tests/unit/services/llm_prompt/test_feedback_service.py`、`backend/tests/unit/services/llm_prompt/test_prompt_manager.py`、`backend/tests/unit/services/ownership/test_ownership_service_impl.py`、`backend/tests/unit/services/project/test_project_service.py`、`backend/tests/unit/services/task/test_task_service.py`）；并移除 `backend/pyproject.toml` 中当前环境未启用 `pytest-timeout` 插件时会触发告警的 `timeout/timeout_method` 配置。回归结果：`tests/unit/services` 由 `13 warnings` 收敛至 `0 warnings`（`1800 passed, 162 skipped`），`tests/unit/api/v1` 保持全绿且 `0 warnings`（`675 passed, 269 skipped`）
- 全量 unit 测试告警清零补丁（2026-02-11）：继续修复 `tests/unit` 剩余 8 条 `pytest.mark.asyncio` 误标记告警，将同步测试改为 `async def`（`backend/tests/unit/crud/test_asset.py`、`backend/tests/unit/crud/test_contact.py`、`backend/tests/unit/crud/test_property_certificate.py`、`backend/tests/unit/crud/test_rbac.py`）。回归结果：`tests/unit` 全量通过且无 warning（`3472 passed, 550 skipped`，低内存模式：`-o addopts='' -p no:cov`）
- integration 冒烟回归与告警收敛（2026-02-11）：在加载 `backend/.env` 后执行最小集成回归（`backend/tests/integration/test_api_errors.py`、`backend/tests/integration/test_asset_lifecycle.py`、`backend/tests/integration/test_contract_workflow.py`）确认链路可运行并全部通过（`32 passed`）。同时修复 `backend/tests/integration/conftest.py` 中错误调用异步枚举初始化函数导致的 `coroutine was never awaited`（删除对 `init_enum_data` 的同步调用），将该回归集 warnings 从 7 收敛到 2（剩余为第三方 `testcontainers` deprecation 与 `Connection._cancel` 运行时告警）
- integration 告警清零收尾（2026-02-11）：继续优化 `backend/tests/integration/conftest.py` 的事务会话绑定顺序（改为先建立 `connection + transaction` 再创建 `sessionmaker(bind=connection)`），减少连接清理阶段的不确定行为；并在 `backend/pyproject.toml` 为已确认的第三方噪声告警补充 `filterwarnings`（`testcontainers` 旧装饰器 deprecation、`Connection._cancel` 未等待）。复测最小集成回归保持 `32 passed` 且 warnings 收敛为 `0`。
- 全局组织数据隔离能力第3周第十四阶段（2026-02-11）：补齐“tenant_filter 静默失效”结构化兜底。针对当前尚未具备 `organization_id` 物理字段的核心模型，新增可执行替代隔离条件，避免透传后仍发生全量可见：`backend/src/crud/project.py` 在 `get/get_multi/search` 以“可访问组织下用户（id/username）→ created_by”约束项目可见域；`backend/src/crud/asset.py` 在 `get_async/get_multi_with_search_async` 增加同源创建人域约束，并在无匹配主体时 fail-closed；`backend/src/crud/property_certificate.py` 在 `get/get_multi` 通过 `property_certificate_owners -> property_owners.organization_id` 进行组织域过滤。同步新增/调整单测：`backend/tests/unit/crud/test_project.py`、`backend/tests/unit/crud/test_asset.py`、`backend/tests/unit/crud/test_property_certificate.py`
- 全局组织数据隔离能力第3周第十三阶段（2026-02-11）：继续执行“老查询链路”修复并补齐 fail-closed 兜底。`backend/src/services/project/service.py`、`backend/src/services/property_certificate/service.py`、`backend/src/services/asset/asset_service.py` 为详情/列表/更新/删除等高风险入口统一接入 `current_user_id -> tenant_filter` 解析与空组织 fail-closed；`backend/src/api/v1/assets/project.py`、`backend/src/api/v1/assets/property_certificate.py`、`backend/src/api/v1/assets/assets.py` 全量透传用户上下文；`backend/src/crud/project.py` 与 `backend/src/crud/asset.py` 补齐 `tenant_filter` 参数兼容（`get_multi/search/get_async`），避免老签名吞参。同步补充单测：`backend/tests/unit/services/asset/test_asset_service.py`、`backend/tests/unit/services/project/test_project_service.py`、`backend/tests/unit/services/property_certificate/test_service.py`、`backend/tests/unit/crud/test_project.py`、`backend/tests/unit/api/v1/test_project_layering.py`、`backend/tests/unit/api/v1/test_assets_history_layering.py`
- 全局组织数据隔离能力第3周第十二阶段（2026-02-11）：推进“老查询链路”治理。`backend/src/crud/base.py` 为 `get/get_multi` 增加 `tenant_filter` 参数并统一应用 `QueryBuilder.apply_tenant_filter`（含缓存键租户隔离），补齐单条/分页基础查询的组织隔离注入能力；`backend/src/services/permission/rbac_service.py` 的 `get_role/update_role/delete_role/update_role_permissions/get_role_users` 新增租户上下文解析与透传，`backend/src/api/v1/auth/roles.py` 对应详情/更新/删除/角色用户/角色权限更新端点同步透传 `current_user_id`，避免角色详情与修改链路绕过组织隔离。新增单测覆盖 `backend/tests/unit/crud/test_base_distinct_values.py`（`get/get_multi` tenant_filter 场景）与 `backend/tests/unit/services/permission/test_rbac_service.py`（`get_role` 透传 tenant_filter）
- 全局组织数据隔离能力第3周第十一阶段（2026-02-11）：优化 `backend/tests/unit/api/v1/test_pdf_batch_routes.py` 测试隔离，新增自动 fixture 默认 mock `_resolve_accessible_organization_ids`（返回空列表），避免路由单测误触真实 `OrganizationPermissionService` 查询，降低测试日志噪声并提升单测稳定性
- 全局组织数据隔离能力第3周第十阶段（2026-02-11）：在 `backend/src/api/v1/documents/pdf_batch_routes.py` 新增 `_resolve_batch_visibility_context`，将 `GET /status/{batch_id}`、`GET /list`、`POST /cancel/{batch_id}` 中重复的“用户ID解析 + 可访问组织解析”逻辑统一封装，降低多端点重复实现导致的上下文透传不一致风险
- 全局组织数据隔离能力第3周第九阶段（2026-02-11）：补强批处理状态辅助函数的可见性上下文回归防线。为 `backend/tests/unit/api/v1/test_pdf_batch_routes.py` 新增 `_get_batch_status` 透传断言（默认参数与显式 `current_user_id/accessible_organization_ids` 场景），确保路由辅助函数持续将可见性上下文完整传递到 `BatchStatusTracker.get_status`，防止后续重构时出现参数丢失导致的越权回归
- 全局组织数据隔离能力第3周第八阶段（2026-02-11）：继续收口 PDF 批处理可见性链路。`backend/src/services/document/processing_tracker.py` 的 `get_status` 新增 `current_user_id` 与 `accessible_organization_ids`，与 `list_batches` 统一执行可见性过滤（未授权直接返回 `None`）；`backend/src/api/v1/documents/pdf_batch_routes.py` 的 `_get_batch_status` 与 `GET /status/{batch_id}`、`POST /cancel/{batch_id}` 同步透传可见性上下文并复用 tracker 结果，移除已废弃的路由层 `_has_batch_access` 以避免双轨鉴权。同步更新 `backend/tests/unit/api/v1/test_pdf_batch_routes.py`、`backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py`、`backend/tests/unit/services/document/test_processing_tracker.py`（新增 `get_status` 可见性过滤断言）
- 全局组织数据隔离能力第3周第七阶段（2026-02-11）：将 PDF 批处理可见性规则下沉到 `BatchStatusTracker`。`backend/src/services/document/processing_tracker.py` 的 `list_batches` 新增 `current_user_id` 与 `accessible_organization_ids` 参数，并在 Redis/内存两条路径统一执行“创建者优先 + 组织白名单 + 历史无组织记录兼容”过滤；`backend/src/api/v1/documents/pdf_batch_routes.py` 的 `GET /list` 改为先解析可访问组织，再把可见性上下文透传给 tracker，避免路由层重复逐条鉴权。同时修复 `_has_batch_access` 调用不存在方法的问题（改为 `check_organization_access`）。补充单测 `backend/tests/unit/services/document/test_processing_tracker.py`（新增 Redis/内存可见性过滤场景）并更新 `backend/tests/unit/api/v1/test_pdf_batch_routes.py` 断言
- 前端视觉一致性第304轮（2026-02-12）：继续重构 `frontend/src/components/ErrorHandling/ErrorBoundary.tsx` 与 `frontend/src/components/ErrorHandling/ErrorBoundary.module.css`，将错误堆栈与组件堆栈 `pre` 的 `marginTop/fontSize/whiteSpace/wordBreak` 内联样式迁移为语义类 `stackTrace`，统一调试堆栈可读性表达并减少 JSX 样式噪音
- 前端视觉一致性第303轮（2026-02-12）：继续重构 `frontend/src/components/ErrorHandling/ErrorBoundary.tsx` 与 `frontend/src/components/ErrorHandling/ErrorBoundary.module.css`，将两个 `details` 折叠块 `marginTop` 内联样式迁移为语义类 `detailsBlock`，统一错误详情折叠区间距表达并减少 JSX 样式噪音
- 前端视觉一致性第302轮（2026-02-12）：继续重构 `frontend/src/components/ErrorHandling/ErrorBoundary.tsx` 与 `frontend/src/components/ErrorHandling/ErrorBoundary.module.css`，将开发模式错误详情面板 `marginTop/padding/background/borderRadius/fontSize/fontFamily` 内联样式迁移为语义类 `errorDetailsPanel`，统一开发调试信息块视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第301轮（2026-02-12）：继续重构 `frontend/src/components/ErrorHandling/ErrorBoundary.tsx` 与 `frontend/src/components/ErrorHandling/ErrorBoundary.module.css`，将错误页容器 `display/alignItems/justifyContent/minHeight/maxWidth/margin/padding` 内联样式迁移为语义类 `errorPageContainer`，统一错误页布局表达并减少 JSX 样式噪音
- 前端视觉一致性第300轮（2026-02-12）：开始重构 `frontend/src/components/ErrorHandling/ErrorBoundary.tsx` 并新增 `frontend/src/components/ErrorHandling/ErrorBoundary.module.css`，完成错误边界组件样式模块化基础收口，降低 JSX 内联样式噪音并提升可维护性
- 前端视觉一致性第299轮（2026-02-12）：继续重构 `frontend/src/components/PerformanceMonitor.tsx` 与 `frontend/src/components/PerformanceMonitor.module.css`，将“性能建议”列表项 `marginBottom` 内联样式迁移为语义类 `adviceAlert`，统一建议项垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第298轮（2026-02-12）：继续重构 `frontend/src/components/PerformanceMonitor.tsx` 与 `frontend/src/components/PerformanceMonitor.module.css`，将“系统资源 / 组件性能 / 性能建议 / 操作”卡片 `marginTop` 内联样式迁移为复用语义类 `sectionCard`，统一监控面板分区间距表达并减少 JSX 样式噪音
- 前端视觉一致性第297轮（2026-02-12）：继续重构 `frontend/src/components/PerformanceMonitor.tsx` 与 `frontend/src/components/PerformanceMonitor.module.css`，将监控面板内容容器 `marginTop` 内联样式迁移为语义类 `panelContent`，统一弹窗内容区顶部间距表达并减少 JSX 样式噪音
- 前端视觉一致性第296轮（2026-02-12）：继续重构 `frontend/src/components/PerformanceMonitor.tsx` 与 `frontend/src/components/PerformanceMonitor.module.css`，将悬浮监控按钮 `position/bottom/right/zIndex` 内联样式迁移为语义类 `floatingMonitorButton`，统一全局监控入口定位表达并减少 JSX 样式噪音
- 前端视觉一致性第295轮（2026-02-12）：开始重构 `frontend/src/components/PerformanceMonitor.tsx` 并新增 `frontend/src/components/PerformanceMonitor.module.css`，完成性能监控组件样式模块化基础收口，减少 JSX 内联样式噪音并提高后续视觉迭代可维护性
- 前端视觉一致性第294轮（2026-02-12）：继续重构 `frontend/src/components/Common/GroupedSelect.tsx` 与 `frontend/src/components/Common/GroupedSelect.module.css`，将多选标签 `marginRight` 内联样式迁移为语义类 `selectedTag`，统一选中标签组行内间距表达并减少 JSX 样式噪音
- 前端视觉一致性第293轮（2026-02-12）：继续重构 `frontend/src/components/Common/GroupedSelect.tsx` 与 `frontend/src/components/Common/GroupedSelect.module.css`，将选项描述文本 `fontSize` 内联样式迁移为语义类 `optionDescription`，统一分组选项辅助文案层级表达并减少 JSX 样式噪音
- 前端视觉一致性第292轮（2026-02-12）：继续重构 `frontend/src/components/Common/GroupedSelect.tsx` 与 `frontend/src/components/Common/GroupedSelect.module.css`，将选项颜色点 `display/size/radius/marginRight/backgroundColor` 内联样式迁移为 `colorDot + colorDot*` 语义类映射，统一选项颜色标识表达并减少 JSX 样式噪音
- 前端视觉一致性第291轮（2026-02-12）：继续重构 `frontend/src/components/Common/GroupedSelect.tsx` 与 `frontend/src/components/Common/GroupedSelect.module.css`，将下拉搜索区容器与搜索框 `padding/borderBottom/width` 内联样式迁移为语义类 `searchHeader` 与 `fullWidthSearch`，统一分组下拉搜索区布局表达并减少 JSX 样式噪音
- 前端视觉一致性第290轮（2026-02-12）：开始重构 `frontend/src/components/Common/GroupedSelect.tsx` 并新增 `frontend/src/components/Common/GroupedSelect.module.css`，将“无匹配项”容器 `padding/textAlign` 内联样式迁移为语义类 `emptyContainer`，统一空状态展示表达并减少 JSX 样式噪音
- 前端视觉一致性第289轮（2026-02-12）：继续重构 `frontend/src/components/System/PromptEditor.tsx` 与 `frontend/src/components/System/PromptEditor.module.css`，将帮助标签页三张说明卡片 `marginBottom` 内联样式迁移为复用语义类 `cardSpacing`，统一说明区块垂直节奏并减少 JSX 样式噪音
- 前端视觉一致性第288轮（2026-02-12）：继续重构 `frontend/src/components/System/PromptEditor.tsx` 与 `frontend/src/components/System/PromptEditor.module.css`，将预览提示 `Alert` 的 `marginTop` 内联样式迁移为语义类 `previewTip`，统一预览提示区与上方内容的间距表达并减少 JSX 样式噪音
- 前端视觉一致性第287轮（2026-02-12）：继续重构 `frontend/src/components/System/PromptEditor.tsx` 与 `frontend/src/components/System/PromptEditor.module.css`，将系统/用户 Prompt 预览段落的 `background/padding/borderRadius/whiteSpace/fontFamily` 内联样式迁移为语义类 `promptPreview`，统一预览文本区可读性表达并减少 JSX 样式噪音
- 前端视觉一致性第286轮（2026-02-12）：继续重构 `frontend/src/components/System/PromptEditor.tsx` 与 `frontend/src/components/System/PromptEditor.module.css`，将“系统 Prompt”卡片 `marginBottom` 内联样式迁移为语义类 `cardSpacing`，统一预览区卡片层级间距并减少 JSX 样式噪音
- 前端视觉一致性第285轮（2026-02-12）：开始重构 `frontend/src/components/System/PromptEditor.tsx` 并新增 `frontend/src/components/System/PromptEditor.module.css`，将 Few-shot 输入框 `fontFamily` 内联样式迁移为语义类 `monospaceTextarea`，统一示例 JSON 输入区字体表达
- 前端视觉一致性第284轮（2026-02-12）：继续重构 `frontend/src/components/Rental/ContractTerminateModal.tsx` 与 `frontend/src/components/Rental/ContractTerminateModal.module.css`，将二次确认描述容器与描述列表的 `marginTop` 内联样式迁移为语义类 `confirmDescription` 与 `summaryDescriptions`，统一确认信息区垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第283轮（2026-02-12）：继续重构 `frontend/src/components/Rental/ContractTerminateModal.tsx` 与 `frontend/src/components/Rental/ContractTerminateModal.module.css`，将二次确认标题告警图标 `color` 内联样式迁移为语义类 `dangerIcon`，统一危险操作提示图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第282轮（2026-02-12）：继续重构 `frontend/src/components/Rental/ContractTerminateModal.tsx` 与 `frontend/src/components/Rental/ContractTerminateModal.module.css`，将终止日期与抵扣金额控件 `width` 内联样式迁移为语义类 `fullWidthControl`，统一终止表单输入控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第281轮（2026-02-12）：继续重构 `frontend/src/components/Rental/ContractTerminateModal.tsx` 与 `frontend/src/components/Rental/ContractTerminateModal.module.css`，将原合同摘要与实时结算 `Alert` 的 `marginBottom` 内联样式迁移为语义类 `alertSpacing`，统一告警区块垂直间距并减少 JSX 样式噪音
- 前端视觉一致性第280轮（2026-02-12）：开始重构 `frontend/src/components/Rental/ContractTerminateModal.tsx` 并新增 `frontend/src/components/Rental/ContractTerminateModal.module.css`，将原合同摘要 `Descriptions` 的 `marginTop` 内联样式迁移为语义类 `summaryDescriptions`，统一摘要信息区基础节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第279轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportProgress.tsx` 与 `frontend/src/components/Asset/AssetExportProgress.module.css`，将文件大小行 `marginBottom` 内联样式迁移为语义类 `fileSizeBlock`，统一导出完成信息区垂直节奏并减少 JSX 样式噪音
- 前端视觉一致性第278轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportProgress.tsx` 与 `frontend/src/components/Asset/AssetExportProgress.module.css`，将文件名/记录数行 `display/justifyContent/marginBottom` 内联样式迁移为语义类 `infoRow`，统一导出进度信息行布局表达并减少 JSX 样式噪音
- 前端视觉一致性第277轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportProgress.tsx` 与 `frontend/src/components/Asset/AssetExportProgress.module.css`，将完成态成功图标 `color` 内联样式迁移为语义类 `successIcon` 并移除冗余 `COLORS` 导入，统一导出状态图标语义色表达
- 前端视觉一致性第276轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetExportProgress.tsx` 与 `frontend/src/components/Asset/AssetExportProgress.module.css`，将标题区与内容区 `marginBottom` 及标题 `margin` 内联样式迁移为语义类 `headerBlock`、`contentBlock`、`title`，统一导出进度区块层级节奏并减少 JSX 样式噪音
- 前端视觉一致性第275轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetExportProgress.tsx` 并新增 `frontend/src/components/Asset/AssetExportProgress.module.css`，将外层容器 `marginTop/padding/background/borderRadius` 内联样式迁移为语义类 `progressContainer`，统一导出进度卡基础视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第274轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectList.tsx` 与 `frontend/src/components/Project/ProjectList.module.css`，将筛选区两处下拉控件 `width` 内联样式迁移为语义类 `fullWidthSelect`，统一项目筛选控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第273轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectList.tsx` 与 `frontend/src/components/Project/ProjectList.module.css`，将统计卡片前缀图标 `color` 内联样式迁移为语义类 `statPrefixPrimary/Success/Error/Info`，统一项目统计卡图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第272轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectList.tsx` 与 `frontend/src/components/Project/ProjectList.module.css`，将统计行 `marginBottom` 内联样式迁移为语义类 `statisticsRow`，统一统计区与筛选区间距节奏并减少 JSX 样式噪音
- 前端视觉一致性第271轮（2026-02-12）：继续重构 `frontend/src/components/Project/ProjectList.tsx` 与 `frontend/src/components/Project/ProjectList.module.css`，将权属方标签的 `marginRight` 内联样式迁移为语义类 `ownershipTag`，统一权属标签组行内间距表达并减少 JSX 样式噪音
- 前端视觉一致性第270轮（2026-02-12）：开始重构 `frontend/src/components/Project/ProjectList.tsx` 并新增 `frontend/src/components/Project/ProjectList.module.css`，将项目名称链接按钮 `padding/textAlign` 内联样式迁移为语义类 `projectNameButton`，统一列表主字段交互样式表达并减少 JSX 样式噪音
- 前端视觉一致性第269轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearchResult.tsx` 与 `frontend/src/components/Asset/AssetSearchResult.module.css`，将备注段落的 `margin/fontSize/color` 内联样式迁移为语义类 `notesParagraph`，统一搜索结果备注层级表达并减少 JSX 样式噪音
- 前端视觉一致性第268轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearchResult.tsx` 与 `frontend/src/components/Asset/AssetSearchResult.module.css`，将多处信息块 `marginBottom` 与备注容器 `marginTop` 内联样式迁移为语义类 `infoBlock` 与 `notesBlock`，统一结果项内容节奏并减少 JSX 样式噪音
- 前端视觉一致性第267轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearchResult.tsx` 与 `frontend/src/components/Asset/AssetSearchResult.module.css`，将地址/权属图标 `color` 内联样式迁移为语义类 `mutedIcon`，统一辅助图标弱化层级表达并减少 JSX 样式噪音
- 前端视觉一致性第266轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetSearchResult.tsx` 与 `frontend/src/components/Asset/AssetSearchResult.module.css`，将标题物业名文本 `fontSize` 内联样式迁移为语义类 `propertyTitle`，统一搜索结果主标题层级表达并减少 JSX 样式噪音
- 前端视觉一致性第265轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetSearchResult.tsx` 并新增 `frontend/src/components/Asset/AssetSearchResult.module.css`，将标题图标 `color` 内联样式迁移为语义类 `titleIcon` 并移除冗余 `COLORS` 导入，统一结果项标题语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第264轮（2026-02-12）：继续重构 `frontend/src/components/Common/ThemeToggle.tsx` 与 `frontend/src/components/Common/ThemeToggle.module.css`，将 `ThemeSelector` 左右按钮按主题切换的 `background/color` 内联样式迁移为 `selectorButtonActive/selectorButtonInactive` 状态语义类映射，统一主题切换按钮选中态表达并减少 JSX 样式噪音
- 前端视觉一致性第263轮（2026-02-12）：继续重构 `frontend/src/components/Common/ThemeToggle.tsx` 与 `frontend/src/components/Common/ThemeToggle.module.css`，将 `ThemeSelector` 图标、标签与按钮基础样式（`fontSize/color/padding/border/radius/cursor`）内联样式迁移为语义类 `selectorIcon`、`selectorLabel`、`selectorButtonBase`、`selectorButtonLeft`、`selectorButtonRight`，统一主题选择器排版与交互表达
- 前端视觉一致性第262轮（2026-02-12）：继续重构 `frontend/src/components/Common/ThemeToggle.tsx` 与 `frontend/src/components/Common/ThemeToggle.module.css`，将 `ThemeToggleButton` 容器与图标尺寸/语义色（`display/flex/size/padding/border/fontSize/color`）内联样式迁移为语义类 `themeToggleButton*` 与 `toggleButton*`，统一按钮式主题切换控件规格并保留 `style` 扩展能力
- 前端视觉一致性第261轮（2026-02-12）：继续重构 `frontend/src/components/Common/ThemeToggle.tsx` 与 `frontend/src/components/Common/ThemeToggle.module.css`，将 `showLabel` 模式下太阳/月亮图标的 `fontSize/color` 内联样式迁移为 `modeIcon*` + `lightIcon*/darkIcon*` 状态语义类，统一模式图标层级与色彩表达
- 前端视觉一致性第260轮（2026-02-12）：开始重构 `frontend/src/components/Common/ThemeToggle.tsx` 并新增 `frontend/src/components/Common/ThemeToggle.module.css`，将标签模式外层 `Space` 的 `gap` 内联样式迁移为语义类 `toggleLabelSpace`，统一主题切换标签模式间距表达并减少 JSX 样式噪音
- 前端视觉一致性第259轮（2026-02-12）：继续重构 `frontend/src/components/Rental/RentContractExcelImport.tsx` 与 `frontend/src/components/Rental/RentContractExcelImport.module.css`，将导出日期范围选择器 `width` 内联样式迁移为语义类 `fullWidthRangePicker`，统一表单时间控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第258轮（2026-02-12）：继续重构 `frontend/src/components/Rental/RentContractExcelImport.tsx` 与 `frontend/src/components/Rental/RentContractExcelImport.module.css`，将导入结果状态图标 `color` 内联样式迁移为语义类 `resultSuccessIcon` 与 `resultErrorIcon`，统一导入反馈图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第257轮（2026-02-12）：继续重构 `frontend/src/components/Rental/RentContractExcelImport.tsx` 与 `frontend/src/components/Rental/RentContractExcelImport.module.css`，将导入/导出说明列表 `margin/paddingLeft` 内联样式迁移为语义类 `guideList`，统一说明列表缩进与留白节奏并减少 JSX 样式噪音
- 前端视觉一致性第256轮（2026-02-12）：继续重构 `frontend/src/components/Rental/RentContractExcelImport.tsx` 与 `frontend/src/components/Rental/RentContractExcelImport.module.css`，将导入导出区块及弹窗多处 `Space` 的 `width` 内联样式迁移为语义类 `fullWidthSpace`，统一区块宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第255轮（2026-02-12）：开始重构 `frontend/src/components/Rental/RentContractExcelImport.tsx` 并新增 `frontend/src/components/Rental/RentContractExcelImport.module.css`，将导入/导出卡片角标图标 `color` 内联样式迁移为语义类 `importIcon` 与 `exportIcon`，统一功能入口图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第254轮（2026-02-12）：继续重构 `frontend/src/components/Loading/SkeletonLoader.tsx` 与 `frontend/src/components/Loading/SkeletonLoader.module.css`，将详情区主卡/侧卡标题及字段输入占位的 `width/height` 内联样式迁移为语义类 `detailMainTitle`、`detailSideTitle`、`detailFieldFull`、`detailFieldRegular`，统一详情骨架结构节奏并减少 JSX 样式噪音
- 前端视觉一致性第253轮（2026-02-12）：继续重构 `frontend/src/components/Loading/SkeletonLoader.tsx` 与 `frontend/src/components/Loading/SkeletonLoader.module.css`，将详情头部标题、子标题与操作按钮的 `width/height/marginBottom` 内联样式迁移为语义类 `detailHeaderTitle`、`detailHeaderSubTitle`、`detailActionButton`，统一详情头部占位层级表达并减少 JSX 样式噪音
- 前端视觉一致性第252轮（2026-02-12）：继续重构 `frontend/src/components/Loading/SkeletonLoader.tsx` 与 `frontend/src/components/Loading/SkeletonLoader.module.css`，将图表与表格区块中的 `width/height` 内联样式迁移为语义类 `chartSectionTitle`、`chartNodeSize`、`tablePaginationButton`，统一图表/分页骨架占位规格并减少 JSX 样式噪音
- 前端视觉一致性第251轮（2026-02-12）：继续重构 `frontend/src/components/Loading/SkeletonLoader.tsx` 与 `frontend/src/components/Loading/SkeletonLoader.module.css`，将表格表头与单元格占位 `width/height` 内联样式迁移为语义类 `tableHeaderInput`、`tableCellInputNarrow`、`tableCellInputRegular`，统一表格骨架列宽层级表达并减少 JSX 样式噪音
- 前端视觉一致性第250轮（2026-02-12）：开始重构 `frontend/src/components/Loading/SkeletonLoader.tsx` 与 `frontend/src/components/Loading/SkeletonLoader.module.css`，将表单骨架标题、输入与操作按钮的 `width/height` 内联样式迁移为语义类 `formTitleInput`、`rowInput`、`formActionPrimary`、`formActionButton`，统一表单骨架控件规格并减少 JSX 样式噪音
- 前端视觉一致性第249轮（2026-02-12）：继续重构 `frontend/src/components/Common/FriendlyErrorDisplay.tsx` 与 `frontend/src/components/Common/FriendlyErrorDisplay.module.css`，将建议区列表项 `marginBottom/color` 内联样式迁移为语义类 `suggestionItem`，统一建议条目文本层级表达并减少 JSX 样式噪音
- 前端视觉一致性第248轮（2026-02-12）：继续重构 `frontend/src/components/Common/FriendlyErrorDisplay.tsx` 与 `frontend/src/components/Common/FriendlyErrorDisplay.module.css`，将建议区容器、标题与图标的 `background/padding/borderRadius/textAlign/maxWidth/margin/display/alignItems/color/marginRight` 内联样式迁移为语义类 `suggestionsPanel`、`suggestionsTitle`、`suggestionsTitleIcon`，统一建议区块视觉层级表达并减少 JSX 样式噪音
- 前端视觉一致性第247轮（2026-02-12）：继续重构 `frontend/src/components/Common/FriendlyErrorDisplay.tsx` 与 `frontend/src/components/Common/FriendlyErrorDisplay.module.css`，将错误详情区 `textAlign/marginTop/width` 及详情代码块 `background/padding/borderRadius/fontSize/overflow/maxHeight` 内联样式迁移为语义类 `detailsContainer`、`detailsSpace`、`errorDetailsPre`，统一详情展示结构并减少 JSX 样式噪音
- 前端视觉一致性第246轮（2026-02-12）：继续重构 `frontend/src/components/Common/FriendlyErrorDisplay.tsx` 与 `frontend/src/components/Common/FriendlyErrorDisplay.module.css`，将错误页外层 `textAlign/padding` 内联样式迁移为语义类 `container`，统一错误页基础布局表达并减少 JSX 样式噪音
- 前端视觉一致性第245轮（2026-02-12）：开始重构 `frontend/src/components/Common/FriendlyErrorDisplay.tsx` 并新增 `frontend/src/components/Common/FriendlyErrorDisplay.module.css`，将 5 类错误图标的 `fontSize/color` 内联样式迁移为语义类 `networkIcon/dataIcon/serverIcon/permissionIcon/notFoundIcon`，统一错误类型图标视觉规格并减少 JSX 样式噪音
- 前端视觉一致性第244轮（2026-02-12）：继续重构 `frontend/src/components/Common/Loading.tsx` 与 `frontend/src/components/Common/Loading.module.css`，将加载按钮内部 `Space` 的 `gap` 内联样式迁移为语义类 `loadingButtonSpace`，统一按钮加载态图标与文本间距表达并减少 JSX 样式噪音
- 前端视觉一致性第243轮（2026-02-12）：继续重构 `frontend/src/components/Common/Loading.tsx` 与 `frontend/src/components/Common/Loading.module.css`，将 `card/table/list` 三类骨架屏中的 `width/marginBottom/gap` 内联样式迁移为语义类 `cardSkeleton*`、`tableSkeleton*`、`listSkeletonSpace` 与 `fullWidthInput`，统一骨架占位布局节奏并减少 JSX 样式噪音
- 前端视觉一致性第242轮（2026-02-12）：继续重构 `frontend/src/components/Common/Loading.tsx` 与 `frontend/src/components/Common/Loading.module.css`，将 `InlineLoading` 的容器与消息文本 `display/alignItems/gap/color` 内联样式迁移为语义类 `inlineLoading` 与 `inlineMessage`，统一行内加载态排版表达并减少 JSX 样式噪音
- 前端视觉一致性第241轮（2026-02-12）：继续重构 `frontend/src/components/Common/Loading.tsx` 与 `frontend/src/components/Common/Loading.module.css`，将 `PageLoading` 提示文本 `marginTop/color` 内联样式迁移为语义类 `pageMessage`，统一页面级加载提示信息层级表达并减少 JSX 样式噪音
- 前端视觉一致性第240轮（2026-02-12）：开始重构 `frontend/src/components/Common/Loading.tsx` 并新增 `frontend/src/components/Common/Loading.module.css`，将 `PageLoading` 外层容器 `display/flex/minHeight` 内联样式迁移为语义类 `pageLoading` 并保留 `style` 扩展能力，统一页面级加载基础布局表达
- 前端视觉一致性第239轮（2026-02-12）：继续重构 `frontend/src/components/Common/ErrorState.tsx` 与 `frontend/src/components/Common/ErrorState.module.css`，将 `ComponentError` 中“重试按钮”容器 `marginTop` 内联样式迁移为语义类 `componentActionContainer`，统一紧凑错误态操作区垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第238轮（2026-02-12）：继续重构 `frontend/src/components/Common/ErrorState.tsx` 与 `frontend/src/components/Common/ErrorState.module.css`，将 `ComponentError` 容器及消息区 `display/flex/padding/textAlign/marginTop/fontSize` 内联样式迁移为语义类 `componentError`、`componentMessageContainer`、`componentMessageText`，统一紧凑错误态信息层级表达并减少 JSX 样式噪音
- 前端视觉一致性第237轮（2026-02-12）：继续重构 `frontend/src/components/Common/ErrorState.tsx` 与 `frontend/src/components/Common/ErrorState.module.css`，将技术详情 `Text` 与 `Alert` 的 `fontSize/color/maxWidth` 内联样式迁移为语义类 `technicalDetailText` 与 `technicalAlert`，统一技术细节展示层级表达并减少 JSX 样式噪音
- 前端视觉一致性第236轮（2026-02-12）：继续重构 `frontend/src/components/Common/ErrorState.tsx` 与 `frontend/src/components/Common/ErrorState.module.css`，将 `ErrorState` 标题区与动作区的 `marginBottom/fontSize/gap` 内联样式迁移为语义类 `errorMessageParagraph`、`errorCodeText`、`actionSpace`，统一错误信息区排版节奏并减少 JSX 样式噪音
- 前端视觉一致性第235轮（2026-02-12）：开始重构 `frontend/src/components/Common/ErrorState.tsx` 并新增 `frontend/src/components/Common/ErrorState.module.css`，将错误图标与外层容器的 `fontSize/color/display/flex/minHeight/padding` 内联样式迁移为语义类 `errorIcon`、`warningIcon`、`errorState`，统一错误态基础视觉规格并保留 `style` 扩展能力
- 前端视觉一致性第234轮（2026-02-12）：继续重构 `frontend/src/components/Common/EmptyState.tsx` 与 `frontend/src/components/Common/EmptyState.module.css`，将 `ComponentEmpty` 中“创建按钮”容器 `marginTop` 内联样式迁移为语义类 `componentActionContainer`，统一紧凑空态操作区垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第233轮（2026-02-12）：继续重构 `frontend/src/components/Common/EmptyState.tsx` 与 `frontend/src/components/Common/EmptyState.module.css`，将 `ComponentEmpty` 外层容器与图标/消息区的 `display/flex/padding/textAlign/color/marginTop/marginBottom/fontSize` 内联样式迁移为语义类 `componentEmpty`、`componentIconContainer`、`componentMessageContainer`、`componentMessageText`，统一紧凑空态排版层级表达并减少 JSX 样式噪音
- 前端视觉一致性第232轮（2026-02-12）：继续重构 `frontend/src/components/Common/EmptyState.tsx` 与 `frontend/src/components/Common/EmptyState.module.css`，将标题描述区与动作区 `textAlign/maxWidth/marginBottom/color/gap` 内联样式迁移为语义类 `emptyContent`、`emptyTitle`、`emptyDescription`、`actionsSpace`，统一空态信息层级与按钮间距表达并减少 JSX 样式噪音
- 前端视觉一致性第231轮（2026-02-12）：继续重构 `frontend/src/components/Common/EmptyState.tsx` 与 `frontend/src/components/Common/EmptyState.module.css`，将 `EmptyState` 外层容器及图片/图标容器 `display/flex/minHeight/padding/maxWidth/maxHeight/marginBottom/color` 内联样式迁移为语义类 `emptyState`、`emptyImage`、`emptyIconContainer`，统一空态基础布局表达并保留 `style` 扩展能力
- 前端视觉一致性第230轮（2026-02-12）：开始重构 `frontend/src/components/Common/EmptyState.tsx` 并新增 `frontend/src/components/Common/EmptyState.module.css`，将空态图标的 `fontSize/color` 内联样式迁移为语义类 `emptyIconGlyph`，统一多空态图标视觉规格并减少 JSX 样式噪音
- 前端视觉一致性第229轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetCard.tsx` 与 `frontend/src/components/Asset/AssetCard.module.css`，将底部时间信息区 `fontSize/color` 内联样式迁移为语义类 `timeInfo`，统一时间辅助信息层级表达并减少 JSX 样式噪音
- 前端视觉一致性第228轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetCard.tsx` 与 `frontend/src/components/Asset/AssetCard.module.css`，将状态标签区与出租率容器/头部的 `marginBottom/display/justifyContent` 内联样式迁移为语义类 `statusSection`、`occupancyContainer`、`occupancyHeader`、`occupancyLabel`，统一资产卡片状态区布局节奏并减少 JSX 样式噪音
- 前端视觉一致性第227轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetCard.tsx` 与 `frontend/src/components/Asset/AssetCard.module.css`，将卡片内容区与面积行的 `marginTop/marginBottom` 内联样式迁移为语义类 `content` 与 `areaRow`，统一卡片主体区垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第226轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetCard.tsx` 与 `frontend/src/components/Asset/AssetCard.module.css`，将地址/权属信息行与图标文本的 `marginBottom/marginRight/color` 内联样式迁移为语义类 `metaRow`、`metaIcon`、`metaText`，统一卡片元信息层级表达并减少 JSX 样式噪音
- 前端视觉一致性第225轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetCard.tsx` 并新增 `frontend/src/components/Asset/AssetCard.module.css`，将卡片容器与标题区的 `marginBottom/cursor/border/display/alignItems/fontSize/fontWeight` 静态内联样式迁移为语义类 `card`、`cardDefault`、`cardSelected`、`metaTitle`、`propertyName`，统一资产卡片基础结构表达并减少 JSX 样式噪音
- 前端视觉一致性第224轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetHistory.tsx` 与 `frontend/src/components/Asset/AssetHistory.module.css`，将详情弹窗内“字段变更详情/创建时的数据”区块 `marginTop` 内联样式迁移为语义类 `detailSection`，统一详情分区垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第223轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetHistory.tsx` 与 `frontend/src/components/Asset/AssetHistory.module.css`，将时间线项容器、标题区、元信息区、原因文案与操作链接按钮的 `padding/margin/color/fontSize/fontWeight` 内联样式迁移为语义类 `timelineItem`、`timelineHeader`、`timelineMeta`、`timelineReason`、`timelineFieldText`、`detailLinkButton`，统一历史列表层级与交互表达并减少 JSX 样式噪音
- 前端视觉一致性第222轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetHistory.tsx` 与 `frontend/src/components/Asset/AssetHistory.module.css`，将字段变更行的 `display/alignItems/gap` 及新旧值 `color/textDecoration/fontWeight` 内联样式迁移为语义类 `fieldChangeRow`、`fieldOldValue`、`fieldNewValue`，统一字段差异可视化表达并移除冗余 `COLORS` 导入
- 前端视觉一致性第221轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetHistory.tsx` 与 `frontend/src/components/Asset/AssetHistory.module.css`，将筛选结果统计区与卡片标题图标的 `textAlign/color/fontSize/marginRight` 内联样式迁移为语义类 `totalCountCol`、`totalCountText`、`titleIcon`，统一筛选区信息层级与图标间距表达并减少 JSX 样式噪音
- 前端视觉一致性第220轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetHistory.tsx` 并新增 `frontend/src/components/Asset/AssetHistory.module.css`，将筛选卡片及筛选控件宽度的 `marginBottom/width` 内联样式迁移为语义类 `filterCard` 与 `fullWidthControl`，统一筛选区布局表达并减少 JSX 样式噪音
- 前端视觉一致性第219轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetDetailInfo.tsx` 与 `frontend/src/components/Asset/AssetDetailInfo.module.css`，将接收协议附件项的 `marginBottom/marginLeft` 与备注文本 `whiteSpace` 内联样式迁移为语义类 `attachmentItem`、`attachmentName`、`notesText`，统一附件行内间距与备注换行表达并减少 JSX 样式噪音
- 前端视觉一致性第218轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetDetailInfo.tsx`，将“接收信息”“接收协议详情”“合同信息”三张卡片的 `marginBottom` 内联样式迁移为已有语义类 `cardSpacing`，统一资产详情下半区块垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第217轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetDetailInfo.tsx` 与 `frontend/src/components/Asset/AssetDetailInfo.module.css`，将出租率分析区的 `marginTop/display/flex/minWidth/textAlign/fontSize/color` 内联样式迁移为语义类 `occupancySection`、`occupancyAnalysisRow`、`occupancyProgressWrapper`、`occupancySummary`、`occupancySummaryText`，统一分析区布局与辅助文案层级表达并减少 JSX 样式噪音
- 前端视觉一致性第216轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetDetailInfo.tsx` 与 `frontend/src/components/Asset/AssetDetailInfo.module.css`，将“项目名称/物业名称/权属方/所在地址/创建时间/更新时间/出租率”标签图标的 `marginRight` 内联样式迁移为语义类 `labelIcon`，统一详情标签图标间距表达并减少 JSX 样式噪音
- 前端视觉一致性第215轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetDetailInfo.tsx` 与 `frontend/src/components/Asset/AssetDetailInfo.module.css`，将“基本信息”标题图标与物业名称文本的 `marginRight/fontSize/fontWeight` 内联样式迁移为语义类 `titleIcon` 与 `propertyName`，统一标题图标与主字段层级表达并减少 JSX 样式噪音
- 前端视觉一致性第214轮（2026-02-12）：继续重构 `frontend/src/components/Asset/AssetDetailInfo.tsx`，将“面积信息”卡片的 `marginBottom` 内联样式迁移为已有语义类 `cardSpacing`，统一资产详情区块垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第213轮（2026-02-12）：开始重构 `frontend/src/components/Asset/AssetDetailInfo.tsx` 并新增 `frontend/src/components/Asset/AssetDetailInfo.module.css`，将“基本信息”卡片的 `marginBottom` 内联样式迁移为语义类 `cardSpacing`，统一资产详情区块垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第212轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetDetailedSection.module.css`，将终端合同列表的 `marginTop` 内联样式迁移为语义类 `terminalContractList`，统一终端合同区块垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第211轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetDetailedSection.module.css`，将终端合同上传说明文案的 `marginTop/fontSize/color` 内联样式迁移为语义类 `uploadHintText` 并移除冗余 `COLORS` 导入，统一说明文案层级表达并减少 JSX 样式噪音
- 前端视觉一致性第210轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx`，将“押金” `InputNumber` 的 `width` 内联样式迁移为已有语义类 `fullWidthControl`，统一高级分区数值控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第209轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx`，将“月租金” `InputNumber` 的 `width` 内联样式迁移为已有语义类 `fullWidthControl`，统一高级分区数值控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第208轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx`，将“合同结束日期” `DatePicker` 的 `width` 内联样式迁移为已有语义类 `fullWidthControl`，统一高级分区日期控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第207轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetDetailedSection.module.css`，将“合同开始日期” `DatePicker` 的 `width` 内联样式迁移为语义类 `fullWidthControl`，统一高级分区日期控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第206轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetDetailedSection.module.css`，将两处提示图标的 `marginLeft/color` 内联样式迁移为语义类 `infoIcon`，统一说明图标位置与辅助色表达并减少 JSX 样式噪音
- 前端视觉一致性第205轮（2026-02-12）：开始重构 `frontend/src/components/Forms/Asset/AssetDetailedSection.tsx` 并新增 `frontend/src/components/Forms/Asset/AssetDetailedSection.module.css`，将“高级选项”卡片的 `marginBottom` 内联样式迁移为语义类 `sectionCard`，统一资产高级分区与下方区块的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第204轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetReceptionSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetReceptionSection.module.css`，将附件列表的 `marginTop` 内联样式迁移为语义类 `attachmentList`，统一附件区块垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第203轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetReceptionSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetReceptionSection.module.css`，将上传说明文案的 `marginTop/fontSize/color` 内联样式迁移为语义类 `uploadHintText` 并移除冗余 `COLORS` 导入，统一说明文案层级表达并减少 JSX 样式噪音
- 前端视觉一致性第202轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetReceptionSection.tsx`，将“接收协议结束日期” `DatePicker` 的 `width` 内联样式迁移为已有语义类 `datePickerFullWidth`，统一日期输入控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第201轮（2026-02-12）：继续重构 `frontend/src/components/Forms/Asset/AssetReceptionSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetReceptionSection.module.css`，将“接收协议开始日期” `DatePicker` 的 `width` 内联样式迁移为语义类 `datePickerFullWidth`，统一日期输入控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第200轮（2026-02-11）：开始重构 `frontend/src/components/Forms/Asset/AssetReceptionSection.tsx` 并新增 `frontend/src/components/Forms/Asset/AssetReceptionSection.module.css`，将“接收信息”卡片的 `marginBottom` 内联样式迁移为语义类 `sectionCard`，统一资产接收分区与下方区块的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第199轮（2026-02-11）：开始重构 `frontend/src/components/Forms/Asset/AssetBasicInfoSection.tsx` 并新增 `frontend/src/components/Forms/Asset/AssetBasicInfoSection.module.css`，将“基本信息”卡片的 `marginBottom` 内联样式迁移为语义类 `sectionCard`，统一资产基础分区与下方区块的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第198轮（2026-02-11）：开始重构 `frontend/src/components/Forms/Asset/AssetStatusSection.tsx` 并新增 `frontend/src/components/Forms/Asset/AssetStatusSection.module.css`，将“状态信息”卡片的 `marginBottom` 内联样式迁移为语义类 `sectionCard`，统一资产状态分区与下方区块的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第197轮（2026-02-11）：继续重构 `frontend/src/components/Forms/Asset/AssetAreaSection.tsx` 与 `frontend/src/components/Forms/Asset/AssetAreaSection.module.css`，将 6 处 `InputNumber` 的 `width: 100%` 内联样式迁移为复用语义类 `fullWidthInput`，统一面积分区输入控件宽度表达并减少重复 JSX 样式噪音
- 前端视觉一致性第196轮（2026-02-11）：开始重构 `frontend/src/components/Forms/Asset/AssetAreaSection.tsx` 并新增 `frontend/src/components/Forms/Asset/AssetAreaSection.module.css`，将“面积信息”卡片的 `marginBottom` 内联样式迁移为语义类 `sectionCard`，统一资产面积分区与下方区块的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第195轮（2026-02-11）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将出租率文本按阈值切换的 `color` 内联样式迁移为 `getOccupancyValueClass` + `occupancyValueHigh/occupancyValueMedium/occupancyValueLow` 语义类映射，统一动态状态色表达并减少 JSX 样式噪音
- 前端视觉一致性第194轮（2026-02-11）：开始重构 `frontend/src/components/Forms/OwnershipForm.tsx` 并新增 `frontend/src/components/Forms/OwnershipForm.module.css`，将“关联项目”选择器的 `width` 与底部操作列的 `textAlign` 内联样式迁移为语义类 `projectSelect/actionsColumn`，统一表单输入区与操作区布局表达并减少 JSX 样式噪音
- 前端视觉一致性第193轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/PDFImportHeader.tsx` 与 `frontend/src/pages/Contract/components/PDFImportHeader.module.css`，将上传图标外层容器的 `display/alignItems/justifyContent/width/height/borderRadius/backgroundColor` 内联样式迁移为语义类 `uploadIconWrapper`，统一头部图标容器布局表达并移除无效的变量拼接背景写法
- 前端视觉一致性第192轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/PDFImportHeader.tsx` 与 `frontend/src/pages/Contract/components/PDFImportHeader.module.css`，将上传图标的 `fontSize/color` 内联样式迁移为语义类 `uploadIcon`，统一头部图标语义色与视觉层级表达并减少 JSX 样式噪音
- 前端视觉一致性第191轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/PDFImportHeader.tsx` 与 `frontend/src/pages/Contract/components/PDFImportHeader.module.css`，将页面标题文本的 `margin` 内联样式迁移为语义类 `headerTitle`，统一头部标题文本间距表达并减少 JSX 样式噪音
- 前端视觉一致性第190轮（2026-02-11）：开始重构 `frontend/src/pages/Contract/components/PDFImportHeader.tsx` 并新增 `frontend/src/pages/Contract/components/PDFImportHeader.module.css`，将头部卡片的 `marginBottom` 内联样式迁移为语义类 `headerCard`，统一导入页头部与下方内容区的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第189轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/ImportStatusStates.tsx` 与 `frontend/src/pages/Contract/components/ImportStatusStates.module.css`，将失败态标题文本的 `color` 内联样式迁移为语义类 `errorTitle` 并移除冗余 `COLORS` 导入，统一失败态标题语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第188轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/ImportStatusStates.tsx` 与 `frontend/src/pages/Contract/components/ImportStatusStates.module.css`，将成功态标题文本的 `color` 内联样式迁移为语义类 `successTitle`，统一成功态标题语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第187轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/ImportStatusStates.tsx` 与 `frontend/src/pages/Contract/components/ImportStatusStates.module.css`，将失败态图标的 `fontSize/color/marginBottom` 内联样式迁移为语义类 `errorIcon`，统一失败态视觉焦点与语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第186轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/ImportStatusStates.tsx` 与 `frontend/src/pages/Contract/components/ImportStatusStates.module.css`，将成功态图标的 `fontSize/color/marginBottom` 内联样式迁移为语义类 `successIcon`，统一成功态视觉焦点与语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第185轮（2026-02-11）：开始重构 `frontend/src/pages/Contract/components/ImportStatusStates.tsx` 并新增 `frontend/src/pages/Contract/components/ImportStatusStates.module.css`，将成功/失败状态页容器的 `textAlign/padding` 内联样式迁移为语义类 `statusContainer`，统一状态页主布局表达并减少 JSX 样式噪音
- 前端视觉一致性第184轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/SessionHistoryTab.tsx` 与 `frontend/src/pages/Contract/components/SessionHistoryTab.module.css`，将空态标题文本的 `color` 内联样式迁移为语义类 `emptyStateTitle` 并移除冗余 `COLORS` 导入，统一空态标题辅助色表达并减少 JSX 样式噪音
- 前端视觉一致性第183轮（2026-02-11）：继续重构 `frontend/src/pages/Contract/components/SessionHistoryTab.tsx` 与 `frontend/src/pages/Contract/components/SessionHistoryTab.module.css`，将空态历史图标的 `fontSize/color/marginBottom` 内联样式迁移为语义类 `emptyStateIcon`，统一空态视觉焦点与辅助色表达并减少 JSX 样式噪音
- 前端视觉一致性第182轮（2026-02-11）：开始重构 `frontend/src/pages/Contract/components/SessionHistoryTab.tsx` 并新增 `frontend/src/pages/Contract/components/SessionHistoryTab.module.css`，将空态容器的 `textAlign/padding` 内联样式迁移为语义类 `emptyStateContainer`，统一导入历史空态区布局表达并减少 JSX 样式噪音
- 前端视觉一致性第181轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `ActionFeedbackCard` 标题图标的 `style={{ color: ... }}` 动态内联样式迁移为 `iconClassName` 语义映射（含新增 `feedbackIconIdle`），统一卡片状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第180轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `default/info` 状态图标的 `color` 内联样式迁移为语义类 `feedbackIconInfo`，统一反馈状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第179轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `warning` 状态图标的 `color` 内联样式迁移为语义类 `feedbackIconWarning`，统一反馈状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第178轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `error` 状态图标的 `color` 内联样式迁移为语义类 `feedbackIconError`，统一反馈状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第177轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `success` 状态图标的 `color` 内联样式迁移为语义类 `feedbackIconSuccess`，统一反馈状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第176轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `loading` 状态图标的 `color` 内联样式迁移为语义类 `feedbackIconLoading`，统一反馈状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第175轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `ActionFeedbackCard` 操作区容器的 `marginTop` 内联样式迁移为语义类 `cardActionsContainer`，统一卡片操作区垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第174轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将 `ActionFeedbackCard` 详情列表的 `marginTop/paddingLeft` 内联样式迁移为语义类 `cardDetailsList`，统一卡片详情列表缩进与垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第173轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将错误详情代码块的 `fontSize/margin/padding/background/border/borderRadius/maxHeight/overflow` 内联样式迁移为语义类 `errorCodeBlock`，统一异常详情展示容器视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第172轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx`，将“错误信息”标题文本的 `fontSize` 内联样式迁移为已有语义类 `sectionLabel`，统一反馈辅助标题层级表达并减少 JSX 样式噪音
- 前端视觉一致性第171轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx`，将“错误信息”区 `Divider` 的 `margin` 内联样式迁移为已有语义类 `sectionDivider`，统一反馈分隔元素垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第170轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将“错误信息”区块外层容器的 `marginTop` 内联样式迁移为语义类 `errorInfoContainer`，统一错误反馈区垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第169轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将“详细信息”列表的 `margin/paddingLeft/fontSize` 内联样式迁移为语义类 `detailsList`，统一反馈列表排版节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第168轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将“详细信息”标题文本的 `fontSize` 内联样式迁移为语义类 `sectionLabel`，统一反馈辅助标题层级表达并减少 JSX 样式噪音
- 前端视觉一致性第167轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将“详细信息”区 `Divider` 的 `margin` 内联样式迁移为语义类 `sectionDivider`，统一分隔元素垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第166轮（2026-02-11）：继续重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 与 `frontend/src/components/Feedback/ActionFeedback.module.css`，将“详细信息”区块外层容器的 `marginTop` 内联样式迁移为语义类 `detailsContainer`，统一反馈信息区垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第165轮（2026-02-11）：开始重构 `frontend/src/components/Feedback/ActionFeedback.tsx` 并新增 `frontend/src/components/Feedback/ActionFeedback.module.css`，将操作按钮区容器的 `marginTop` 内联样式迁移为语义类 `actionsContainer`，统一反馈组件操作区垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第164轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将响应时间列按阈值切换的 `color` 内联样式迁移为 `getResponseTimeClass` + `responseTimeFast/responseTimeMedium/responseTimeSlow` 语义类映射，统一动态状态色表达并减少 JSX 样式噪音
- 前端视觉一致性第163轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将未知态状态图标的 `color` 内联样式迁移为语义类 `statusIconUnknown`，统一状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第162轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将异常态状态图标的 `color` 内联样式迁移为语义类 `statusIconUnhealthy`，统一状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第161轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将健康态状态图标的 `color` 内联样式迁移为语义类 `statusIconHealthy`，统一状态图标语义色表达并减少 JSX 样式噪音
- 前端视觉一致性第160轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将“错误信息”列文本的 `color` 内联样式迁移为语义类 `errorText`，统一异常信息强调色表达并减少 JSX 样式噪音
- 前端视觉一致性第159轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将端点列 `code` 标签的 `fontSize` 内联样式迁移为语义类 `endpointCode`，统一代码文本层级表达并减少 JSX 样式噪音
- 前端视觉一致性第158轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将“监控说明”列表中绿色/红色/橙色状态文本的 `color` 内联样式迁移为语义类 `statusHealthy/statusUnhealthy/statusUnknown`，统一状态色语义表达并减少 JSX 样式噪音
- 前端视觉一致性第157轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将“监控说明”列表的 `marginLeft/marginTop` 内联样式迁移为语义类 `guideList`，统一说明列表缩进与垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第156轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将“监控说明”文本容器的 `fontSize/color` 内联样式迁移为语义类 `guideText`，统一文案层级与辅助色表达并减少 JSX 样式噪音
- 前端视觉一致性第155轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将“监控说明”卡片的 `marginTop` 内联样式迁移为语义类 `guideCard`，统一说明区块与主内容区的垂直节奏表达并减少 JSX 样式噪音
- 前端视觉一致性第154轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx`，将 API 严重告警（error）组件的 `marginBottom` 内联样式迁移为已有语义类 `alertSpacing`，统一告警区块垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第153轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将 API 健康告警（warning）组件的 `marginBottom` 内联样式迁移为语义类 `alertSpacing`，统一告警区块垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第152轮（2026-02-11）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将 API 总体状态概览 `Row` 的 `marginBottom` 内联样式迁移为语义类 `overviewRow`，统一监控概览区块垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第151轮（2026-02-10）：继续重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 与 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将 API 监控页头容器的 `display/alignItems/justifyContent/marginBottom` 内联样式迁移为语义类 `monitorHeader`，统一 API 监控页头布局表达并减少 JSX 样式噪音
- 前端视觉一致性第150轮（2026-02-10）：重构 `frontend/src/components/Monitoring/ApiMonitor.tsx` 并新增 `frontend/src/components/Monitoring/ApiMonitor.module.css`，将 API 监控页根容器的 `padding` 内联样式迁移为语义类 `monitorContainer`，统一 API 监控页主容器内边距表达并减少 JSX 样式噪音
- 前端视觉一致性第149轮（2026-02-10）：继续重构 `frontend/src/components/Common/StateContainer.tsx`，将空状态操作区容器的 `marginTop` 内联样式替换为全局工具类 `marginTopMd`，复用统一间距体系并减少 JSX 样式噪音
- 前端视觉一致性第148轮（2026-02-10）：继续重构 `frontend/src/components/Common/StateContainer.tsx`，将加载态文案容器的 `marginTop` 内联样式替换为全局工具类 `marginTopMd`，复用统一间距体系并减少 JSX 样式噪音
- 前端视觉一致性第147轮（2026-02-10）：重构 `frontend/src/components/Monitoring/SystemMonitoringDetails.tsx` 并新增 `frontend/src/components/Monitoring/SystemMonitoringDetails.module.css`，将监控详情区 `Row` 的 `marginTop` 内联样式迁移为语义类 `detailsRow`，统一监控详情区块顶部间距表达并减少 JSX 样式噪音
- 前端视觉一致性第146轮（2026-02-10）：重构 `frontend/src/components/Monitoring/SystemMonitoringOverview.tsx` 并新增 `frontend/src/components/Monitoring/SystemMonitoringOverview.module.css`，将监控概览区 `Row` 的 `marginBottom` 内联样式迁移为语义类 `overviewRow`，统一监控概览区块间距表达并减少 JSX 样式噪音
- 前端视觉一致性第145轮（2026-02-10）：继续重构 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.tsx` 与 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.module.css`，将健康状态标签容器的 `marginTop` 内联样式迁移为语义类 `healthStatusTagWrapper`，统一健康状态徽标区与仪表图的垂直间距表达并减少 JSX 样式噪音
- 前端视觉一致性第144轮（2026-02-10）：继续重构 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.tsx` 与 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.module.css`，将系统健康仪表容器的 `textAlign/padding` 内联样式迁移为语义类 `healthGaugeContainer`，统一健康评分区容器布局表达并减少 JSX 样式噪音
- 前端视觉一致性第143轮（2026-02-10）：重构 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.tsx` 并新增 `frontend/src/components/Monitoring/SystemMonitoringHealthAlerts.module.css`，将健康告警区 `Row` 的 `marginBottom` 内联样式迁移为语义类 `healthAlertsRow`，统一系统监控告警区块间距表达并减少 JSX 样式噪音
- 前端视觉一致性第142轮（2026-02-10）：继续重构 `frontend/src/components/Monitoring/SystemMonitoringHeader.tsx` 与 `frontend/src/components/Monitoring/SystemMonitoringHeader.module.css`，将监控页标题 `h2` 的 `margin` 内联样式迁移为语义类 `headerTitle`，统一系统监控页标题文本间距表达并减少 JSX 样式噪音
- 前端视觉一致性第141轮（2026-02-10）：继续重构 `frontend/src/components/Monitoring/SystemMonitoringHeader.tsx` 与 `frontend/src/components/Monitoring/SystemMonitoringHeader.module.css`，将头部监控图标的 `fontSize/color` 内联样式迁移为语义类 `monitorIcon`，统一系统监控页标题图标视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第140轮（2026-02-10）：重构 `frontend/src/components/Monitoring/SystemMonitoringHeader.tsx` 并新增 `frontend/src/components/Monitoring/SystemMonitoringHeader.module.css`，将监控页头部行容器的 `marginBottom` 内联样式迁移为语义类 `headerRow`，统一系统监控页头部区块间距表达并减少 JSX 样式噪音
- 前端视觉一致性第139轮（2026-02-10）：继续重构 `frontend/src/components/Ownership/OwnershipSelect.tsx` 与 `frontend/src/components/Ownership/OwnershipSelect.module.css`，将权属方选择器 `Select` 的 `flex: 1` 内联样式迁移为语义类 `ownershipSelect`，统一权属方输入区伸展布局表达并减少 JSX 样式噪音
- 前端视觉一致性第138轮（2026-02-10）：继续重构 `frontend/src/components/Ownership/OwnershipSelect.tsx` 与 `frontend/src/components/Ownership/OwnershipSelect.module.css`，将 `Space.Compact` 的 `width` 内联样式迁移为语义类 `compactContainer`，统一权属方选择器横向容器宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第137轮（2026-02-10）：继续重构 `frontend/src/components/Ownership/OwnershipSelect.tsx` 与 `frontend/src/components/Ownership/OwnershipSelect.module.css`，将多选模式标签 `Tag` 的 `marginRight` 内联样式迁移为语义类 `selectedTag`，统一多选标签横向间距表达并减少 JSX 样式噪音
- 前端视觉一致性第136轮（2026-02-10）：继续重构 `frontend/src/components/Ownership/OwnershipSelect.tsx` 与 `frontend/src/components/Ownership/OwnershipSelect.module.css`，将权属方选项编码文本的 `color/fontSize` 内联样式迁移为语义类 `codeText`，统一权属方选项辅助信息层级表达并减少 JSX 样式噪音
- 前端视觉一致性第135轮（2026-02-10）：重构 `frontend/src/components/Ownership/OwnershipSelect.tsx` 并新增 `frontend/src/components/Ownership/OwnershipSelect.module.css`，将权属方选项简称文本的 `color/fontSize` 内联样式迁移为语义类 `shortNameText`，统一权属方选项辅助文本层级表达并减少 JSX 样式噪音
- 前端视觉一致性第134轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“出租率”数值文本的 `fontSize` 内联样式迁移为语义类 `occupancyValue`（动态 `color` 继续内联以保持阈值语义），统一出租率数值字号表达并减少 JSX 样式噪音
- 前端视觉一致性第133轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“出租率”标签文本的 `fontSize` 内联样式迁移为语义类 `occupancyLabel`，统一出租率标签字号表达并减少 JSX 样式噪音
- 前端视觉一致性第132轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“出租率”头部行的 `display/justifyContent/width` 内联样式迁移为语义类 `occupancyHeaderRow`，统一出租率头部布局表达并减少 JSX 样式噪音
- 前端视觉一致性第131轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“出租率”头部容器的 `marginBottom` 内联样式迁移为语义类 `occupancyHeader`，统一出租率信息区块间距表达并减少 JSX 样式噪音
- 前端视觉一致性第130轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx`，将资产卡片“面积信息”文本的 `fontSize` 内联样式迁移为复用语义类 `detailText`，统一资产卡片信息文本字号表达并减少 JSX 样式噪音
- 前端视觉一致性第129轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx`，将资产卡片“权属方”文本的 `fontSize` 内联样式迁移为复用语义类 `detailText`，统一资产卡片信息文本字号表达并减少 JSX 样式噪音
- 前端视觉一致性第128轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“地址信息”文本的 `fontSize` 内联样式迁移为语义类 `detailText`，统一资产卡片信息文本字号表达并减少 JSX 样式噪音
- 前端视觉一致性第127轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx`，将资产卡片“面积信息”图标的 `color` 内联样式迁移为复用语义类 `detailIcon`，统一资产卡片信息图标色值表达并减少 JSX 样式噪音
- 前端视觉一致性第126轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx`，将资产卡片“权属方”图标的 `color` 内联样式迁移为复用语义类 `detailIcon`，统一资产卡片信息图标色值表达并减少 JSX 样式噪音
- 前端视觉一致性第125轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“地址信息”图标的 `color` 内联样式迁移为语义类 `detailIcon`，统一资产卡片信息图标色值表达并减少 JSX 样式噪音
- 前端视觉一致性第124轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“面积信息”行容器的 `marginBottom` 内联样式迁移为语义类 `areaRow`，统一资产卡片信息块间距表达并减少 JSX 样式噪音
- 前端视觉一致性第123轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“权属方”行容器的 `marginBottom` 内联样式迁移为语义类 `ownershipRow`，统一资产卡片信息行间距表达并减少 JSX 样式噪音
- 前端视觉一致性第122轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片“地址信息”行容器的 `marginBottom` 内联样式迁移为语义类 `addressRow`，统一资产卡片信息行间距表达并减少 JSX 样式噪音
- 前端视觉一致性第121轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 与 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片描述容器的 `marginTop` 内联样式迁移为语义类 `cardDescription`，统一资产卡片内容区顶部间距表达并减少 JSX 样式噪音
- 前端视觉一致性第120轮（2026-02-10）：重构 `frontend/src/pages/Assets/components/AssetCard.tsx` 并新增 `frontend/src/pages/Assets/components/AssetCard.module.css`，将资产卡片标题 `Title` 的 `margin/marginBottom` 内联样式迁移为语义类 `cardTitle`，统一资产卡片标题间距表达并减少 JSX 样式噪音
- 前端视觉一致性第119轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/ConfirmDialog.tsx` 与 `frontend/src/components/Feedback/ConfirmDialog.module.css`，将 `showSaveConfirm` 标题区问号图标的 `color` 内联样式迁移为语义类 `confirmShortcutSaveIcon`，统一快捷保存确认弹窗标题图标色值表达并减少 JSX 样式噪音
- 前端视觉一致性第118轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/ConfirmDialog.tsx` 与 `frontend/src/components/Feedback/ConfirmDialog.module.css`，将 `showDeleteConfirm` 标题区删除图标的 `color` 内联样式迁移为语义类 `confirmShortcutDeleteIcon`，统一快捷确认弹窗标题图标色值表达并减少 JSX 样式噪音
- 前端视觉一致性第117轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/ConfirmDialog.tsx` 与 `frontend/src/components/Feedback/ConfirmDialog.module.css`，将删除/保存确认详情列表 `<ul>` 的 `marginTop/paddingLeft` 内联样式迁移为语义类 `confirmDetailsList`，统一确认弹窗详情列表缩进与顶部间距表达并减少 JSX 样式噪音
- 前端视觉一致性第116轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/ConfirmDialog.tsx` 与 `frontend/src/components/Feedback/ConfirmDialog.module.css`，将删除/保存确认详情区容器的 `marginTop` 内联样式迁移为语义类 `confirmDetailsContainer`，统一确认弹窗详情区块纵向间距表达并减少 JSX 样式噪音
- 前端视觉一致性第115轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/ConfirmDialog.tsx` 与 `frontend/src/components/Feedback/ConfirmDialog.module.css`，将删除确认内容中危险提示段落的 `marginTop/marginBottom` 内联样式迁移为语义类 `confirmDangerParagraph`，统一确认弹窗风险提示区间距表达并减少 JSX 样式噪音
- 前端视觉一致性第114轮（2026-02-10）：重构 `frontend/src/components/Feedback/ConfirmDialog.tsx` 并新增 `frontend/src/components/Feedback/ConfirmDialog.module.css`，将确认弹窗预设类型图标的 `fontSize/color` 内联样式迁移为语义类（`confirmIcon` / `confirmIconDanger` / `confirmIconInfo` / `confirmIconSuccess` / `confirmIconWarning`），统一确认弹窗图标色阶与尺寸表达并减少 JSX 样式噪音
- 前端视觉一致性第113轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/EmptyState.tsx` 与 `frontend/src/components/Feedback/EmptyState.module.css`，将空状态预设图标的 `fontSize/color` 内联样式迁移为语义类（`emptyIcon` / `emptyIconNeutral` / `emptyIconError` / `emptyIconWarning` / `emptyIconInfo`），统一空状态图标视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第112轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/EmptyState.tsx` 与 `frontend/src/components/Feedback/EmptyState.module.css`，将空状态标题 `Text` 的 `fontSize/display/marginBottom` 与操作区 `Space` 的 `marginTop` 内联样式迁移为语义类（`emptyTitle` / `emptyActions`），统一空状态文案层级与操作区间距表达并减少 JSX 样式噪音
- 全局组织数据隔离能力第3周第六阶段（2026-02-11）：补齐 PDF 批处理列表可见性控制。`backend/src/api/v1/documents/pdf_batch_routes.py` 的 `GET /list` 升级为异步端点并接入 `db/current_user` 依赖，复用 `_has_batch_access` 对返回批次按“创建者/组织权限”做逐项过滤，防止跨组织任务在列表中被枚举。同步更新单测 `backend/tests/unit/api/v1/test_pdf_batch_routes.py`（列表接口 direct-call 改为传入 `current_user`，新增无组织权限批次被过滤场景）
- 全局组织数据隔离能力第3周第五阶段（2026-02-11）：增强 PDF 批处理访问控制。`backend/src/api/v1/documents/pdf_batch_routes.py` 新增 `_has_batch_access` 校验，`GET /status/{batch_id}` 与 `POST /cancel/{batch_id}` 在返回任务详情前按“创建者优先 + organization 权限校验”执行授权，未授权统一返回 `not_found` 以避免任务存在性泄露；批量上传创建任务时同步写入 `created_by_user_id` 元数据用于后续鉴权。补充回归测试 `backend/tests/unit/api/v1/test_pdf_batch_routes.py`（新增无组织权限拒绝场景，并更新 direct-call 用例传入 `current_user`）
- 全局组织数据隔离能力第3周第四阶段（2026-02-11）：修复 `backend/src/api/v1/assets/custom_fields.py` 字段列表接口的重复服务调用并统一透传 `current_user_id`；`backend/src/services/custom_field/service.py` 补齐租户过滤解析异常 fail-closed 回退。同步扩展 `PDFImportSession` 真实组织隔离链路：`backend/src/crud/pdf_import_session.py` 支持 `tenant_filter`，`backend/src/services/document/pdf_import_service.py` 为会话映射/状态/取消操作加入用户组织自动解析与过滤，`backend/src/api/v1/documents/pdf_batch_routes.py` 在批处理状态与取消接口透传当前用户上下文；补充测试 `backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py`、`backend/tests/unit/services/document/test_pdf_import_service.py` 并更新 `custom_fields` 相关 API 断言
- 全局组织数据隔离能力第3周第三阶段（2026-02-11）：完成 API→Service 端到端租户上下文注入：`backend/src/api/v1/assets/assets.py` 与 `backend/src/api/v1/auth/roles.py` 在列表查询中透传 `current_user_id`；`backend/src/services/asset/asset_service.py` 与 `backend/src/services/permission/rbac_service.py` 新增 `tenant_filter` 自动解析（基于 `OrganizationPermissionService.get_user_accessible_organizations`），并在解析异常时采用 fail-closed（空组织集合）策略；补充服务层回归测试 `backend/tests/unit/services/asset/test_asset_service.py` 与 `backend/tests/unit/services/permission/test_rbac_service.py`
- 全局组织数据隔离能力第3周第二阶段（2026-02-11）：完成首批高风险 CRUD/Service 调用链的 `tenant_filter` 透传接入，涉及 `backend/src/crud/{asset.py,rbac.py,system_dictionary.py,custom_field.py,ownership.py}` 与 `backend/src/services/permission/rbac_service.py`；保持默认行为不变（仅新增可选参数），支持在调用端显式传入租户上下文；同步补充单测 `backend/tests/unit/crud/{test_asset.py,test_rbac.py,test_system_dictionary.py,test_custom_field.py,test_ownership.py}`，验证参数透传与 QueryBuilder 注入链路
- 全局组织数据隔离能力第3周落地（2026-02-11）：在 `backend/src/crud/query_builder.py` 新增 `TenantFilter` 与可选租户过滤注入能力（`build_query/build_count_query` 支持 `tenant_filter`，并实现空组织上下文 fail-closed）；在 `backend/src/crud/base.py` 为 `get_with_filters/get_multi_with_count/count/get_distinct_field_values` 增加 `tenant_filter` 透传与应用；补充单测覆盖 `backend/tests/unit/crud/test_query_builder.py` 与 `backend/tests/unit/crud/test_base_distinct_values.py`，验证 `organization_id IN (...)`、空组织 fail-closed、无组织字段模型跳过注入等关键场景
- 术语清理第5轮（2026-02-11）：新增 `docs/guides/tenant-symbol-whitelist.md`《tenant 符号白名单（运行时零歧义清单）》，将 `tenant*` 符号按“运行时承租方白名单 / Alembic 历史迁移白名单 / 禁止新增 guardrail / 待确认文本项”结构化归档；在 `docs/guides/tenant-terminology-audit.md` 增加关联入口，并同步更新 `docs/index.md` 与 `AGENTS.md` 文档导航
- 术语清理第4轮（2026-02-11）：完成“防误读加固”，在迁移与测试层补充历史语义说明并消除命名歧义；为 `backend/alembic/versions/e4c9e4968dd7_initial_schema_creation.py` 与 `backend/alembic/versions/20260130_drop_tenant_id_columns.py` 增加 `tenant_id` 历史/回滚兼容说明；将 `backend/tests/integration/crud/test_query_builder_security.py` 中 `test_blocked_tenant_filter_raises_error` 重命名为 `test_blocked_tenant_name_field_filter_raises_error`；更新 `backend/src/crud/field_whitelist.py` 注释与前端表单上下文注释（`frontend/src/components/Forms/Asset/AssetDetailedSection.tsx`、`frontend/src/components/Forms/RentContract/TenantInfoSection.tsx`、`frontend/src/components/Forms/RentContract/RentContractFormContext.tsx`）以明确“承租方/Lessee”语义；同步更新 `docs/guides/tenant-terminology-audit.md`
- 术语清理第3轮（2026-02-11）：扩展至代码与前端展示文案层，保持字段键名 `tenant_*` 不变的前提下，将可读文本统一为“承租方”语义；涉及 `backend/src/schemas/asset.py`、`backend/src/models/asset.py`、`backend/src/services/enum_data_init.py`、`frontend/src/services/dictionary/config.ts`、`frontend/src/components/Forms/Asset/AssetDetailedSection.tsx`、`frontend/src/components/Asset/AssetDetailInfo.tsx`、`frontend/src/components/Asset/AssetSearchResult.tsx`、`frontend/src/components/Analytics/Filters/FiltersSection.tsx`、`frontend/src/components/Forms/AssetFormHelp.tsx`、`frontend/src/components/Asset/assetExportConfig.ts`、`frontend/src/pages/System/PromptDashboard.tsx`、`frontend/src/components/Asset/AssetForm.md`，并同步更新 `docs/guides/tenant-terminology-audit.md`
- 术语清理第2轮（2026-02-11）：继续清理文档中的“租户”歧义表达，将 `docs/database-design.md` 资产表“租户信息/租户名称/租户类型”统一为“承租方信息/承租方名称/承租方类型”；将 `docs/integrations/assets-api.md` 与 `docs/features/prd-asset-management.md` 的“租户类型”统一为“承租方类型（tenant_type）”；同步更新 `docs/guides/tenant-terminology-audit.md` 审计记录
- 文档术语去混淆（2026-02-11）：新增 `docs/guides/tenant-terminology-audit.md` 审计清单，明确区分“组织范围过滤（organization scope）”与“承租方 tenant_* 业务语义”；修订 `docs/database-design.md`，移除与当前代码基线不一致的 `tenant_id（多租户）` 字段描述并补充术语说明；修订 `docs/requirements-specification.md` 的 Out of Scope 表述为“跨组织数据隔离（非 SaaS 多租户）”；同步更新 `docs/index.md` 与 `AGENTS.md` 文档导航
- Alembic 版本表容量修复（2026-02-11）：在 `backend/alembic/versions/20250118_add_property_cert_tables.py` 的 `upgrade` 前置执行 `ALTER TABLE alembic_version ALTER COLUMN version_num TYPE VARCHAR(255)`，解决仓库存在长 revision ID 时 PostgreSQL 默认 `VARCHAR(32)` 导致迁移链升级中断的问题
- 迁移链兼容修复（2026-02-11）：将 `backend/alembic/versions/20250118_add_user_id_to_extraction_feedback.py` 的 `revision` 缩短为 `20250118_add_uid_feedback`（≤32 字符），并同步更新 `backend/alembic/versions/20250118_add_property_cert_tables.py` 的 `down_revision` 引用，修复 PostgreSQL 上 `alembic_version.version_num` 长度限制触发的升级失败（`value too long for type character varying(32)`）
- 迁移链幂等修复（2026-02-11）：修复 `backend/alembic/versions/20250118_add_user_id_to_extraction_feedback.py` 的重复执行问题，为 `extraction_feedback.user_id` 列与 `ix_extraction_feedback_user_id` 索引增加存在性判断，避免在已包含该列的环境中重复 `ADD COLUMN` 导致迁移失败；`downgrade` 同步按“索引存在再删、列存在再删”执行
- Phase 2 复核与索引落地（2026-02-11）：新增 `backend/alembic/versions/20260211_add_phase2_aligned_performance_indexes.py`，按当前 schema 对齐补充性能索引（`assets`/`rent_contracts`/`rent_ledger`/`ownerships`/`projects`/`property_cert_assets`/`notifications`/`async_tasks`/`operation_logs`），统一使用 `CREATE INDEX CONCURRENTLY IF NOT EXISTS` 与可回滚 `DROP INDEX CONCURRENTLY IF EXISTS`；重写 `docs/remaining-issues.md` 为复核版，修正历史草案中的字段漂移（如 `notifications.user_id`、`tasks`、`property_certificates.asset_id`、`assets.total_area` 等）并明确 Phase 2.1/2.2/2.3 的可执行路径与验收标准
- 文档清理（2026-02-11）：`docs/todo-phase1.2-remaining.md` 所列 Phase 1.2 问题已全部完成并复核通过，删除该阶段性待办文档，避免与当前代码状态产生认知偏差
- Phase 1.2 分层整改 Day3/收尾完成（2026-02-11）：完成 `backend/src/services/llm_prompt/prompt_manager.py`、`backend/src/services/llm_prompt/auto_optimizer.py`、`backend/src/services/llm_prompt/feedback_service.py` 及补录清单 `backend/src/services/analytics/occupancy_service.py`、`backend/src/services/analytics/area_service.py`、`backend/src/services/enum_data_init.py`、`backend/src/services/excel/excel_config_service.py`、`backend/src/services/core/session_service.py`、`backend/src/services/document/pdf_session_service.py`、`backend/src/services/document/processing_tracker.py`、`backend/src/services/system_settings/service.py` 的 Service 层直连执行下沉；新增/扩展 `backend/src/crud/llm_prompt.py`、`backend/src/crud/asset.py`、`backend/src/crud/enum_field.py`、`backend/src/crud/auth.py`、`backend/src/crud/system_settings.py` 承接查询与批量更新语义；同步更新 `docs/todo-phase1.2-remaining.md` 为完成态（`155/155`, `0 violations in 0 files`）；验证：`ruff check`（变更文件）通过、`mypy`（16 个相关源文件）通过、定向服务单测 `142 passed`（LLM Prompt / Analytics / Enum Init / Session / Processing Tracker / System Settings）
- Phase 1.2 分层整改 Day2 落地（2026-02-10）：完成 `backend/src/services/notification/scheduler.py`、`backend/src/services/notification/notification_service.py`、`backend/src/services/document/pdf_import_service.py`、`backend/src/services/collection/service.py` 的 Service 层直连查询下沉；补充/扩展 `backend/src/crud/notification.py`、`backend/src/crud/rent_contract.py`、`backend/src/crud/pdf_import_session.py`、`backend/src/crud/collection.py`，新增会话查询/通知去重/逾期与到期台账聚合等 CRUD 方法；同步更新 `docs/todo-phase1.2-remaining.md` 基线为 `28 violations in 11 files`（累计 Day1+Day2 已消除 48 violations）；验证：变更文件 `ruff check` 与 `mypy` 通过，定向服务单测通过（`test_scheduler` / `test_wecom_service` / `test_pdf_import_service` / `test_collection_service`）
- Phase 1.2 分层整改 Day1 落地（2026-02-10）：完成 `backend/src/services/common_dictionary_service.py`、`backend/src/services/property_certificate/service.py`、`backend/src/services/project/service.py`、`backend/src/services/task/service.py`、`backend/src/services/rent_contract/ledger_service.py`、`backend/src/services/document/rent_contract_excel.py`、`backend/src/services/rent_contract/lifecycle_service.py`、`backend/src/services/rent_contract/helpers.py` 的 Service 层直连查询下沉；补充/扩展 `backend/src/crud/system_dictionary.py`、`backend/src/crud/project.py`、`backend/src/crud/task.py`、`backend/src/crud/rent_contract.py`、`backend/src/crud/asset.py` 以承接查询与批量更新；同步修正 `docs/todo-phase1.2-remaining.md` 统计口径与执行计划（当前基线 `50 violations in 15 files`，Day1 26 项已完成）；验证：`ruff check`（变更文件通过，仓库全量存在既有历史告警）、`mypy src`（仅剩仓库既有非本次引入错误）、定向单测集通过（`test_common_dictionary_service` / `test_project_service` / `test_task_service` / `test_ledger_service` / `test_rent_contract_excel` / `test_property_certificate_service`）
- 前端视觉一致性第111轮（2026-02-10）：重构 `frontend/src/components/Feedback/EmptyState.tsx` 并新增 `frontend/src/components/Feedback/EmptyState.module.css`，将空状态根容器的 `padding/textAlign` 内联样式迁移为语义类 `emptyStateWrapper`，并保留外部 `style` 扩展能力，统一空状态组件基础布局表达并减少 JSX 样式噪音
- 前端视觉一致性第110轮（2026-02-10）：继续重构 `frontend/src/pages/Assets/components/AssetFilters.tsx` 与 `frontend/src/pages/Assets/components/AssetFilters.module.css`，将筛选操作区 `Form.Item` 的 `marginTop` 内联样式迁移为语义类 `resetActionItem`，统一资产筛选工具区纵向间距表达并减少 JSX 样式噪音
- 前端视觉一致性第109轮（2026-02-10）：重构 `frontend/src/pages/Assets/components/AssetFilters.tsx` 并新增 `frontend/src/pages/Assets/components/AssetFilters.module.css`，将“创建时间”区间控件 `RangePicker` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthRangePicker`，统一资产筛选区时间控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第108轮（2026-02-10）：继续重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateDetailPage.tsx` 与 `frontend/src/pages/PropertyCertificate/PropertyCertificateDetailPage.module.css`，将编辑弹窗“土地使用期限” `DatePicker.RangePicker` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthRangePicker`，统一产权证编辑表单区间控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第107轮（2026-02-10）：继续重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateDetailPage.tsx` 与 `frontend/src/pages/PropertyCertificate/PropertyCertificateDetailPage.module.css`，将编辑弹窗“登记日期” `DatePicker` 的 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthDatePicker`，统一产权证编辑表单控件宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第106轮（2026-02-10）：重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateDetailPage.tsx` 并新增 `frontend/src/pages/PropertyCertificate/PropertyCertificateDetailPage.module.css`，将详情页主内容 `Space` 容器 `style={{ width: '100%' }}` 内联样式迁移为语义类 `fullWidthStack`，统一产权证详情页纵向内容容器宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第105轮（2026-02-10）：继续重构 `frontend/src/pages/LoginPage.tsx` 与 `frontend/src/pages/LoginPage.module.css`，将登录提交区 `Form.Item` 的 `marginBottom` 内联样式迁移为语义类 `login-submit-item`，统一登录页表单底部间距表达并减少 JSX 样式噪音
- 前端视觉一致性第104轮（2026-02-10）：重构 `frontend/src/pages/Assets/SimpleAnalyticsPage.tsx` 并新增 `frontend/src/pages/Assets/SimpleAnalyticsPage.module.css`，将“概览统计”卡片的 `marginBottom` 内联样式迁移为语义类 `overviewCard`，统一简化分析页卡片间距表达并减少 JSX 样式噪音
- 前端视觉一致性第103轮（2026-02-10）：重构 `frontend/src/pages/Project/ProjectManagementPage.tsx` 并新增 `frontend/src/pages/Project/ProjectManagementPage.module.css`，将页面 `Suspense` fallback 中加载容器与提示文案的 `padding/textAlign/marginTop` 内联样式迁移为语义类（`loadingFallback` / `loadingText`），统一项目管理页加载占位样式表达并减少 JSX 样式噪音
- 前端视觉一致性第102轮（2026-02-10）：重构 `frontend/src/pages/Ownership/OwnershipManagementPage.tsx` 并新增 `frontend/src/pages/Ownership/OwnershipManagementPage.module.css`，将页面 `Suspense` fallback 中加载容器与提示文案的 `padding/textAlign/marginTop` 内联样式迁移为语义类（`loadingFallback` / `loadingText`），统一权属管理页加载占位样式表达并减少 JSX 样式噪音
- 前端视觉一致性第101轮（2026-02-10）：重构 `frontend/src/App.tsx` 并新增 `frontend/src/App.module.css`，将受保护路由 `Suspense` fallback 中加载态 `Spin` 的 `display/justifyContent/marginTop` 内联样式迁移为语义类 `suspenseFallbackSpin`，统一应用级加载占位布局表达并减少 JSX 样式噪音
- 前端视觉一致性第100轮（2026-02-10）：继续重构 `frontend/src/components/Contract/PDFImport/ActionButtons.tsx` 与 `frontend/src/components/Contract/PDFImport/ActionButtons.module.css`，将按钮容器 `textAlign: center` 的内联样式迁移至语义类 `actionContainer`，并同步更新 `frontend/src/components/Contract/PDFImport/__tests__/ActionButtons.test.tsx` 的样式断言为语义类校验，统一 PDF 导入操作区样式表达并消除该组件最后一处内联样式
- 前端视觉一致性第99轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将“完成检查”步骤中建议文件名展示框与文本色值的内联样式迁移为语义类（`stepSuggestedFilenameBox` / `stepSuggestedFilenameText`），统一修复流程步骤区正向结果展示样式并减少 JSX 样式噪音
- 前端视觉一致性第98轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将“文件名分析”步骤中原始文件名展示框的 `background/borderRadius/marginTop/padding/wordBreak` 内联样式迁移为语义类 `analysisOriginalFilenameBox`，统一修复步骤内容区样式表达并减少 JSX 样式噪音
- 前端视觉一致性第97轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将“关于文件名修复”说明区容器的 `fontSize` 内联样式迁移为语义类 `fixDescription`，统一修复说明区文本层级并减少 JSX 样式噪音
- 前端视觉一致性第96轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将“修复后”文件名文本的 `wordBreak/fontSize/color` 内联样式迁移为语义类 `previewFixedFilenameText`，统一修复结果预览区正向反馈文本表达并减少 JSX 样式噪音
- 前端视觉一致性第95轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将“修复前”文件名文本的 `wordBreak/fontSize` 内联样式迁移为语义类 `previewOriginalFilenameText`，统一修复结果预览区文本排版并减少 JSX 样式噪音
- 前端视觉一致性第94轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将手动编辑提示 `Alert` 的 `fontSize` 内联样式迁移为语义类 `manualEditInfoAlert`，统一修复弹窗提示区文本层级并减少 JSX 样式噪音
- 前端视觉一致性第93轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将手动编辑区输入容器 `marginTop` 与输入框 `width/padding/border/borderRadius` 内联样式迁移为语义类（`manualInputWrapper` / `manualFilenameInput`），统一手动修复输入区样式表达并减少 JSX 样式噪音
- 前端视觉一致性第92轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 与 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将三处 `Space` 容器 `style={{ width: '100%' }}` 内联样式迁移为复用语义类 `fullWidthSpace`，统一修复弹窗分区纵向容器宽度表达并减少 JSX 样式噪音
- 前端视觉一致性第91轮（2026-02-10）：重构 `frontend/src/components/Contract/FilenameFixDialog.tsx` 并新增 `frontend/src/components/Contract/FilenameFixDialog.module.css`，将步骤内容容器的 `padding` 内联样式迁移为语义类 `stepContentWrapper`，统一修复流程步骤区间距表达并减少 JSX 样式噪音
- 前端视觉一致性第90轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将“文件名最佳实践”提示区容器、文案色值与图标右间距内联样式迁移为语义类（`usageTipsBox` / `usageTipsText` / `usageTipsIcon`），统一提示信息区视觉层次并减少 JSX 样式噪音
- 前端视觉一致性第89轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将“发现以下问题”区块中问题列表与 Alert 字号的内联样式迁移为语义类（`issueList` / `issuesAlert`），统一校验问题提示区排版并减少 JSX 样式噪音
- 前端视觉一致性第88轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将“改进建议”列表的 `margin/padding/font-size` 内联样式迁移为语义类 `suggestionList`，统一建议列表排版节奏并减少 JSX 样式噪音
- 前端视觉一致性第87轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将建议区与改进建议区 `Divider` 的 `margin` 内联样式迁移为复用语义类 `sectionDivider`，统一校验面板分区分隔线间距节奏并减少 JSX 样式噪音
- 前端视觉一致性第86轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将“建议的文件名”展示框的背景、边框、圆角、内边距及文本色值/换行内联样式迁移为语义化 class（`suggestedFilenameBox` / `suggestedFilenameText`），统一建议区视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第85轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将只读文件名预览框的背景、边框、圆角、内边距与文本换行内联样式迁移为语义化 class，并通过 `filenamePreviewBoxError` 收敛错误态边框表达，统一校验面板只读展示区视觉样式
- 前端视觉一致性第84轮（2026-02-10）：继续重构 `frontend/src/components/Contract/FilenameValidator.tsx` 与 `frontend/src/components/Contract/FilenameValidator.module.css`，将“文件名”与“建议的文件名”标题行中的 `display/flex/alignment/gap/margin` 内联布局样式迁移为复用语义类 `sectionHeaderRow`，统一校验面板分区标题布局节奏并减少 JSX 样式噪音
- 前端视觉一致性第83轮（2026-02-10）：重构 `frontend/src/components/Contract/FilenameValidator.tsx` 并新增 `frontend/src/components/Contract/FilenameValidator.module.css`，将文件名校验状态行中的容器布局、状态图标与状态文本色值内联样式迁移为语义化 class，统一文件名校验反馈区的视觉层级并减少 JSX 样式噪音
- 前端视觉一致性第82轮（2026-02-10）：继续重构 `frontend/src/components/Feedback/ProgressIndicator.tsx` 与 `frontend/src/components/Feedback/ProgressIndicator.module.css`，将进度标题区与进度卡描述文案中的 `margin/display/flex` 内联布局样式迁移为语义化 class，统一进度反馈组件的排版节奏并减少 JSX 样式噪音
- 前端视觉一致性第81轮（2026-02-10）：重构 `frontend/src/components/Feedback/ProgressIndicator.tsx` 并新增 `frontend/src/components/Feedback/ProgressIndicator.module.css`，将进度状态图标（success/exception/active/default）与卡片标题状态图标的内联色值迁移为语义化 class，统一进度反馈组件的状态色表达并减少 JSX 样式噪音
- 前端视觉一致性第80轮（2026-02-10）：重构 `frontend/src/components/Feedback/SuccessNotification.tsx` 并新增 `frontend/src/components/Feedback/SuccessNotification.module.css`，将通知图标的 success/error/warning/info 色值内联样式迁移为语义化 class，统一全局消息反馈色彩表达并减少 JSX 样式噪音
- 前端视觉一致性第79轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/PDFImportContext.tsx` 并新增 `frontend/src/components/Contract/PDFImport/PDFImportContext.module.css`，将处理步骤状态图标的成功/处理中/异常色值内联样式迁移为语义化 class，统一 PDF 导入流程状态反馈的颜色表达并减少 JSX 样式噪音
- 前端视觉一致性第78轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/PDFUploadArea.tsx` 并新增 `frontend/src/components/Contract/PDFImport/PDFUploadArea.module.css`，将上传区图标字号与色值内联样式迁移为语义化 class，统一 PDF 导入页上传入口视觉表达并减少 JSX 样式噪音
- 前端视觉一致性第77轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/UploadStatsPanel.tsx` 并新增 `frontend/src/components/Contract/PDFImport/UploadStatsPanel.module.css`，将上传统计区顶部间距与“推荐方法”统计内容字号内联样式迁移为语义化 class，统一 PDF 导入统计区的视觉层次表达并减少 JSX 样式噪音
- 前端视觉一致性第76轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/ImportOptionsPanel.tsx` 并新增 `frontend/src/components/Contract/PDFImport/ImportOptionsPanel.module.css`，将导入选项卡片顶部间距、选项列容器宽度与阈值滑块宽度内联样式迁移为语义化 class，统一 PDF 导入高级选项区的布局表达并减少 JSX 样式噪音
- 前端视觉一致性第75轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/SystemInfoDisplay.tsx` 并新增 `frontend/src/components/Contract/PDFImport/SystemInfoDisplay.module.css`，将系统信息提示 `Alert` 的顶部间距内联样式迁移为语义化 class，统一 PDF 导入信息提示区的间距表达并减少 JSX 样式噪音
- 前端视觉一致性第74轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/ProcessingStepsDisplay.tsx` 并新增 `frontend/src/components/Contract/PDFImport/ProcessingStepsDisplay.module.css`，将处理步骤区容器、进度区与进度元信息行中的内联样式迁移为语义化 class，统一 PDF 导入处理流程的步骤区布局表达并减少 JSX 样式噪音
- 前端视觉一致性第73轮（2026-02-10）：重构 `frontend/src/components/Contract/PDFImport/ActionButtons.tsx` 并新增 `frontend/src/components/Contract/PDFImport/ActionButtons.module.css`，将操作区容器的 `marginTop` 内联样式迁移为语义化 class；`textAlign` 继续保留在容器内联样式以兼容现有单测断言，确保 PDF 导入流程操作按钮区在不破坏测试基线下完成样式收敛
- 前端视觉一致性第72轮（2026-02-10）：重构 `frontend/src/components/Forms/RentContract/RentTermModal.tsx` 并新增 `frontend/src/components/Forms/RentContract/RentTermModal.module.css`，将租金条款弹窗中日期与金额输入控件 `style={{ width: '100%' }}` 迁移为统一语义类 `fullWidthControl`，减少表单控件布局样式重复并统一续签条款弹窗的控件宽度表达
- 前端视觉一致性第71轮（2026-02-10）：重构 `frontend/src/components/Analytics/AssetDistributionGrid.tsx` 并新增 `frontend/src/components/Analytics/AssetDistributionGrid.module.css`，将分布图网格容器 `Row` 的页面间距内联样式迁移为语义化 class，统一分析图表分区外边距表达并降低 JSX 样式噪音
- 前端视觉一致性第67轮（2026-02-10）：重构 `frontend/src/components/Analytics/AnalyticsChart.tsx` 并新增 `frontend/src/components/Analytics/AnalyticsChart.module.css`，将饼图/柱图/折线图组件中的加载态与空态容器内联布局样式统一收敛为语义化 class，并通过 CSS 变量 `--chart-height` 承接动态高度，减少三类图表在状态切换时的布局实现分叉
- 前端视觉一致性第68轮（2026-02-10）：重构 `frontend/src/pages/Rental/ContractRenewPage.tsx` 并新增 `frontend/src/pages/Rental/ContractRenewPage.module.css`，将续签页标题图标、成功提示卡、续签说明卡与四列引导块中的内联样式迁移为语义化 class，统一色阶表达、说明块间距与关键文案层级，提升续签流程页的视觉一致性
- 前端视觉一致性第69轮（2026-02-10）：重构 `frontend/src/components/Forms/RentContract/RenewalSummarySection.tsx` 并新增 `frontend/src/components/Forms/RentContract/RenewalSummarySection.module.css`，将摘要卡头部图标、卡片底边强调、押金主色文本与联系人间距从内联写法收敛为模块样式，统一续签摘要区的标题与字段视觉节奏
- 前端视觉一致性第70轮（2026-02-10）：重构 `frontend/src/pages/TestCoverageDashboard.tsx` 并新增 `frontend/src/pages/TestCoverageDashboard.module.css`，将页头、质量门禁、指标卡辅助文本、趋势工具条、阈值输入宽度与覆盖率色阶表达（由 `Statistic.styles` 动态透传改为语义类）统一为模块化样式，减少测试覆盖率看板在不同卡片区块中的样式漂移
- 前端视觉一致性第66轮（2026-02-10）：重构 `frontend/src/components/Analytics/AnalyticsFilters.tsx` 并新增 `frontend/src/components/Analytics/AnalyticsFilters.module.css`、`frontend/src/components/Analytics/Filters/Filters.module.css`，同步收敛 `BasicFiltersSection.tsx`、`FiltersSection.tsx`、`SearchPresetsSection.tsx`、`FilterHistorySection.tsx`、`FilterActionsSection.tsx` 中筛选表单与历史列表的大量内联样式；统一筛选面板头部操作按钮、输入控件宽度/间距、历史记录 hover/focus-visible 反馈与移动端触达高度，替换历史项鼠标事件内联改色为语义化样式状态，降低分析筛选区在布局节奏和交互反馈上的不一致
- 前端视觉一致性第65轮（2026-02-10）：重构 `frontend/src/components/Analytics/AnalyticsDashboard.tsx` 并新增 `frontend/src/components/Analytics/AnalyticsDashboard.module.css`，将仪表板容器、头部操作栏、空/错态提示区与统计分区中的页面级内联样式迁移为语义化 class；统一标题与操作按钮的对齐节奏、按钮触达高度（桌面 36px / 移动端 44px）与焦点可见反馈，并对普通/全屏模式切换收敛为 class 驱动布局，降低分析看板在不同屏宽和状态下的布局割裂感
- 前端视觉一致性第64轮（2026-02-10）：重构 `frontend/src/components/Analytics/AnalyticsStatsCard.tsx` 并新增 `frontend/src/components/Analytics/AnalyticsStatsCard.module.css`，将统计卡片与财务指标卡中的内联布局样式、`Statistic.styles` 色值透传和硬编码间距收敛为语义化 class；通过 CSS 变量承接图标底色、趋势色与出租率动态色值，统一卡片圆角、标题/数值层级、趋势徽标与移动端间距节奏，降低分析看板统计区在主题切换与屏宽变化下的视觉漂移
- 资产服务回归修复（2026-02-10）：修复 `backend/src/services/asset/asset_service.py` 与 `backend/src/services/asset/batch_service.py` 中错误调用 `ownership.get_async` 导致的运行时异常，统一改为 `ownership.get(..., id=...)`；恢复 `backend/src/services/asset/import_service.py` 对 `Ownership` 的导入以避免运行时注解 `NameError`；为 `backend/src/crud/asset.py` 的 `get_multi_by_ids_async` 与 `get_by_property_names_async` 增加可控解密开关，并在 `batch_service`、`import_service`、`excel_import_service` 的写路径预加载中显式使用 `decrypt=False`，避免事务内将解密后的 PII 回刷为明文；新增/更新单测 `backend/tests/unit/crud/test_asset.py`、`backend/tests/unit/services/asset/test_asset_service.py`、`backend/tests/unit/services/asset/test_batch_service.py`、`backend/tests/unit/services/asset/test_import_service.py` 覆盖上述回归点；同时修复 `test_batch_service.py` 旧测试桩，统一为批量预取路径补齐 `get_multi_by_ids_async` 及关联检查 mock，保证历史用例与新实现一致
- 前端测试 warning 清理（2026-02-10）：在 `frontend/src/test/utils/test-helpers.tsx` 与 `frontend/src/test/test-utils.tsx` 的 `BrowserRouter` 测试包装器统一启用 `future` 标志（`v7_startTransition` / `v7_relativeSplatPath`），消除 React Router v7 迁移提示；同时调整 `frontend/src/components/Ownership/__tests__/OwnershipSelect.test.tsx` 的 `Select.Option` mock，避免在原生 `option` 中渲染复杂节点导致的无效 DOM 嵌套告警
- ProjectForm 测试异步副作用告警修复（2026-02-10）：更新 `frontend/src/components/Forms/__tests__/ProjectForm.test.tsx`，在涉及渲染断言的用例中显式等待 `ownershipService.getOwnershipOptions` 的异步副作用完成，避免测试结束前状态更新触发 `act(...)` warning；同时放宽调用次数断言以兼容 React 严格模式下的双调用行为，保证测试语义稳定
- RelationInfoSection 内联样式收敛（2026-02-10）：新增 `frontend/src/components/Forms/RentContract/RelationInfoSection.module.css`，将 `RelationInfoSection.tsx` 中卡片外边距内联样式（`style={{ marginBottom: 16 }}`）迁移为语义化 class `relationCard`；并新增 `frontend/src/components/Forms/RentContract/__tests__/RelationInfoSection.test.tsx` 验证样式类挂载，确保该收敛在后续重构中可回归校验
- ProjectForm 内联样式收敛（2026-02-10）：新增 `frontend/src/components/Forms/ProjectForm.module.css`，将 `ProjectForm.tsx` 中权属方关联卡片、已选标签区、操作按钮区等剩余内联样式迁移为语义化 class（`ownershipCard`、`selectedOwnershipsSection`、`formActions` 等），并保持现有交互逻辑不变；同步在 `frontend/src/components/Forms/__tests__/ProjectForm.test.tsx` 增加布局语义类断言，确保样式重构可回归验证
- OwnershipSelect 跨表单迁移与变体收敛（2026-02-10）：在 `frontend/src/components/Ownership/OwnershipSelect.tsx` 增加 `showPickerButton/showRefreshButton` 控制项，并将 `selectionOnly` 变体收敛为“仅选择器”模式（隐藏弹窗入口/创建/刷新等操作按钮）；新增单测 `frontend/src/components/Ownership/__tests__/OwnershipSelect.test.tsx` 覆盖该行为（TDD 红绿闭环）。同时将 `frontend/src/components/Forms/RentContract/RelationInfoSection.tsx` 与 `frontend/src/components/Forms/ProjectForm.tsx` 的权属方选择统一切换到 `OwnershipSelect` + `variant` 配置，减少表单内重复的权属方下拉实现
- 后端类型治理与契约补齐（2026-02-10）：针对 `backend/src` 完成一轮低风险类型收敛，`mypy src` 从 66 个错误降至 0；修复文档提取、枚举验证、缓存装饰器、RBAC、任务/历史/通知/会话等模块的类型不一致，并补齐租赁合同服务缺失方法（`renew_contract_async` / `terminate_contract_async` / `upload_contract_attachment_async`）与枚举验证统计接口（`get_validation_stats`）；同时修复多处 SQLAlchemy `rowcount` 类型访问与懒加载哨兵比较误报，确保 `make check`、后端 unit 回归和 mypy 全量检查通过
- 前端体验与组件可维护性优化（2026-02-10）：基于 React 组合模式与性能实践优化 `frontend/src/components/Ownership/OwnershipSelect.tsx`，新增 `variant` 显式变体（`full/compact/selectionOnly`）并支持 `ariaLabel` 透传；同步在 `frontend/src/components/Forms/Asset/AssetBasicInfoSection.tsx` 使用新 API；在 `frontend/src/components/Layout/AppLayout.tsx` 采用函数式状态更新避免闭包依赖，在 `frontend/src/components/Layout/AppHeader.tsx` 补充侧边栏切换按钮可访问名称并提取静态样式对象；同时将 `frontend/src/pages/LoginPage.module.css`、`frontend/src/components/Dashboard/QuickInsights.module.css`、`frontend/src/components/Dashboard/DataTrendCard.module.css` 的 `transition: all` 精确化为具体属性过渡，减少不必要重排与绘制开销
- 前端视觉一致性第63轮（2026-02-10）：重构 `frontend/src/components/Analytics/ChartErrorBoundary.tsx` 并新增 `ChartErrorBoundary.module.css`，将错误回退区的容器、提示文案、错误详情和 Alert 外边距内联样式迁移为语义化 class；为错误回退容器补齐 `role="alert"` + `aria-live="polite"` 并统一“重试”按钮焦点可见反馈与移动端触达高度，提升图表异常场景下的可访问性与交互一致性；同时将 `onError` 回调判断改为显式空值检查，减少布尔隐式判断带来的风格漂移
- 前端视觉一致性第62轮（2026-02-10）：重构 `frontend/src/components/Analytics/chartComponents/ChartContainer.tsx` 并新增 `ChartContainer.module.css`，将图表加载态/空态容器中的内联布局样式迁移为语义化 class，并通过 CSS 变量承接动态高度（`--chart-height`）以减少 JSX 样式噪音；同时为加载态补齐 `role="status"` + `aria-live="polite"`，提升分析图表异步状态在可访问场景下的反馈一致性
- 前端视觉一致性第61轮（2026-02-10）：重构 `frontend/src/pages/Dashboard/components/MetricsCards.tsx` 并新增 `MetricsCards.module.css`，将指标卡色阶表达从 `Statistic.styles` 内联透传收敛为语义化 class；统一统计卡标题/数值层级、卡片圆角与网格间距节奏，并将指标色彩映射集中到模块样式（primary/success/warning/info/error），降低看板 KPI 卡在主题切换与样式演进中的视觉不一致风险
- 前端视觉一致性第60轮（2026-02-10）：重构 `frontend/src/pages/Dashboard/components/QuickActions.tsx` 并新增 `QuickActions.module.css`，将快捷操作卡片中的图标色值、文本层级和卡片布局内联样式迁移为语义化 class；统一卡片 hover/focus-visible 反馈、按钮化键盘可达交互（Enter/Space）与移动端触达高度，并将图标色阶收敛为 `tone` 映射（primary/success/warning/info/secondary/neutral），降低看板快捷入口在交互反馈与视觉层级上的不一致
- 前端视觉一致性第59轮（2026-02-10）：重构 `frontend/src/pages/Dashboard/components/AssetChart.tsx` 并新增 `AssetChart.module.css`，将物业分布与出租率趋势区中的标题、列表项、图例色块和趋势行内联样式迁移为语义化 class；统一图例标签与数值文本层级、进度条圆角和区块间距节奏，并将加载态补充 `role="status"` + `aria-live="polite"` 以提升动态反馈可访问性；同时将默认数组处理从 `||` 收敛为 `??`，降低看板图表组件在空数据场景下的语义与样式漂移
- 前端视觉一致性第58轮（2026-02-10）：重构 `frontend/src/pages/Dashboard/components/TodoList.tsx` 并新增 `TodoList.module.css`，将待办项优先级图标、说明文案、截止日期行与“完成”操作中的内联样式迁移为语义化 class；统一操作按钮触达尺寸与 `focus-visible` 焦点反馈，收敛优先级色阶与标签圆角样式，并补充已完成任务标题的语义化弱化样式（删除线+次级文字）；同时规范列表 action 区间距与移动端按钮高度，降低看板待办组件在交互反馈与信息层级上的不一致
- 前端视觉一致性第57轮（2026-02-10）：重构 `frontend/src/components/Ownership/OwnershipList.tsx` 并新增 `OwnershipList.module.css`，将列表统计区、筛选工具栏与操作列中的内联样式迁移为语义化 class；统一统计卡色阶表达（从 `Statistic.styles` 收敛为模块样式映射）、搜索/筛选控件全宽规则与工具栏按钮触达尺寸，并补齐操作按钮与名称链接的 `focus-visible` 反馈；同时收敛操作列按钮 hover 色阶与状态开关最小宽度，降低权属方列表在交互反馈与布局节奏上的不一致
- 前端视觉一致性第56轮（2026-02-10）：重构 `frontend/src/components/Ownership/OwnershipDetail.tsx` 并新增 `OwnershipDetail.module.css`，将详情头部、基础信息卡、关联项目卡与系统信息卡中的内联样式迁移为语义化 class；统一标题层级、状态标签与“编辑”按钮触达尺寸（44px）及焦点可见反馈，并通过容器化间距替代分散的 `marginTop` 实现卡片节奏一致；同时为关联项目名称/关系标签补齐语义样式与表格行对齐细节，降低权属详情组件在不同页面嵌入场景下的布局与交互反馈不一致问题
- 前端视觉一致性第55轮（2026-02-10）：重构 `frontend/src/pages/Contract/PDFImportPage.tsx` 与 `PDFImportPage.module.css`，将页面初始化加载区和主容器中的内联样式迁移为语义化 class，并为加载提示补齐 `role="status"` + `aria-live="polite"` 提升动态反馈可访问性；同时修复 CSS Modules 下 Ant Design 选择器作用域（统一改为 `:global(...)`），避免 `.ant-*` 样式失效，并将按钮/标签/上传区等 `transition: all` 收敛为具体属性过渡，降低导入流程页在交互反馈与样式一致性上的漂移
- 前端视觉一致性第54轮（2026-02-10）：重构 `frontend/src/pages/Project/ProjectDetailPage.tsx` 并新增 `ProjectDetailPage.module.css`，将标题状态、统计卡、项目详情卡与关联资产表中的内联样式迁移为语义化 class；统一“编辑项目”与资产跳转按钮触达尺寸/焦点可见反馈，并将统计卡色阶从 `Statistic.styles` 内联透传收敛为模块样式映射（含出租率阈值 tone）；同时优化统计区响应式栅格（`xs/sm/lg`）和资产计数角标样式，降低项目详情页在不同屏宽下的布局与视觉节奏不一致问题
- 前端视觉一致性第53轮（2026-02-10）：重构 `frontend/src/pages/ProfilePage.tsx` 并新增 `ProfilePage.module.css`，将头像区、安全设置区、模态表单区与操作按钮中的内联样式迁移为语义化 class；统一资料卡与安全卡的标题层级、按钮触达高度（44px）与焦点可见性（`focus-visible`），并增强安全操作卡在移动端的纵向响应式布局，降低个人中心页面在不同设备上的间距与交互反馈不一致问题
- 前端视觉一致性第52轮（2026-02-10）：重构 `frontend/src/components/ErrorHandling/ErrorPage.tsx` 并新增 `ErrorPage.module.css`，将错误页容器、图标、建议区与动作按钮中的内联样式迁移为语义化 class；统一错误图标色阶映射（warning/error/info）与建议文案列表层级，补齐错误建议区 `role="alert"` + `aria-live="polite"` 以增强可访问反馈；同时修复返回按钮显示逻辑（`showBackButton/showHomeButton` 仅在显式 `true` 时展示，避免 `false` 仍显示的缺陷），并新增/修正 `frontend/src/components/ErrorHandling/__tests__/ErrorPage.test.tsx` 覆盖默认渲染、按钮显隐、回调优先与导航回退行为（兼容图标参与无障碍名称的查询）
- 前端视觉一致性第51轮（2026-02-10）：重构 `frontend/src/components/Charts/AssetDistributionChart.tsx` 并新增 `AssetDistributionChart.module.css`，将资产分布统计卡、三类饼图卡片、详细列表与 tooltip 的内联样式迁移为语义化 class；统一总览统计色阶表达（由 `Statistic.styles` 收敛为样式类）并将详情列表右侧百分比补充“占比”文字标签，避免仅靠颜色传达信息；同时规范详情列表滚动区、分隔线与对齐节奏，并将使用状态标签色阶映射抽离为函数 `getUsageStatusTone`，降低资产分布模块在不同数据态下的视觉漂移
- 前端视觉一致性第50轮（2026-02-10）：重构 `frontend/src/components/Charts/AreaStatisticsChart.tsx` 并新增 `AreaStatisticsChart.module.css`，将面积统计卡、图表卡、详细列表与 tooltip 的内联样式迁移为语义化 class；统一“总面积/可租/空置”等统计色阶表达（由 `Statistic.styles` 改为样式类映射），并收敛使用状态列表与面积 Top10 列表的边界、滚动区和文本层级；同时将面积 Top10 进度条颜色改为函数式阈值映射（`var(--color-success/warning/error)`），减少面积分析模块在不同数据状态下的视觉漂移
- 前端视觉一致性第49轮（2026-02-10）：重构 `frontend/src/components/Charts/OccupancyRateChart.tsx` 并新增 `OccupancyRateChart.module.css`，将统计卡、图表卡、排行列表和 tooltip 的内联样式迁移为语义化 class；统一总体统计色阶表达与趋势图标样式映射（不再通过 `Statistic.styles` 和 icon 内联颜色驱动），并将“高/低表现”排行补充文字层级标签（高位/低位），避免仅靠颜色表达状态；同时统一排行列表边界、滚动区和文本层级节奏，减少出租率图表模块在不同数据态下的视觉漂移
- 前端视觉一致性第48轮（2026-02-10）：重构 `frontend/src/components/Rental/ContractDetailInfo.tsx` 并新增 `ContractDetailInfo.module.css`，将合同详情页的卡片标题、描述项标签、统计区、空态和元数据状态中的内联样式统一迁移为语义化 class；将租期与金额统计色阶从 `Statistic.styles` 内联对象收敛为模块化样式并补齐标题图标与标签图标的一致间距；同时为“数据状态”增加辅助文案（正常/需关注）并统一描述区宽度与文本换行规则，降低合同详情在信息层级与状态表达上的视觉割裂
- 前端视觉一致性第47轮（2026-02-10）：重构 `frontend/src/components/Asset/AssetImport.tsx` 并重写 `AssetImport.module.css`，将导入页头、步骤区、说明卡、上传区、执行态与结果态中的大量内联样式迁移为语义化模块样式；统一上传拖拽区、结果操作按钮触达尺寸与页面间距节奏，补齐导入结果卡 success/warning/error/neutral 四态的结构化样式映射（不再在 JSX 中硬编码背景/边框/图标色）；同时将统计数字颜色改为 class 驱动并保持文案层级清晰，降低资产导入流程在不同状态与主题下的视觉漂移
- 前端视觉一致性第46轮（2026-02-10）：重构通用骨架屏组件 `frontend/src/components/Loading/SkeletonLoader.tsx` 并新增 `SkeletonLoader.module.css`，将列表/表单/表格/图表/详情骨架中的大量静态内联样式迁移为语义化 class，统一卡片间距、区块节奏与表格占位结构；同步为图表占位区、详情头部操作区和分页占位区收敛到主题变量色阶，减少加载态与真实内容态之间的视觉跳变；保留必要动态尺寸样式（如表格首列宽度差异），兼顾一致性与骨架表达灵活度
- 前端视觉一致性第45轮（2026-02-10）：优化 `frontend/src/components/Layout/Layout.module.css` 与 `AppSidebar.tsx`，修复侧边栏菜单样式在 CSS Modules 下对 Ant Design 类选择器未显式 `:global` 的命中风险，统一菜单项/子菜单标题最小触达高度为 44px，并补齐 `hover/focus-visible` 与选中态色阶；同步将 Logo/菜单色值切换为设计令牌变量、补充侧边栏与主菜单语义标签（`aria-label`），降低系统主导航在键盘可达性和状态反馈上的不一致
- 前端视觉一致性第44轮（2026-02-10）：重构 `frontend/src/components/Layout/MobileLayout.tsx` 并新增 `MobileLayout.module.css`，将移动端布局容器、固定头部、内容区与页脚的内联样式全面迁移为语义化 class；统一头部右侧通知按钮触达尺寸为 44px，补齐 hover/focus-visible 反馈与图标色阶；同时将移动端标题与主内容区偏移节奏统一（避免固定头部遮挡首屏内容），并为 `MobileLayout` 单测补充关键语义类断言，降低移动端壳层在样式演进中的回归风险
- 前端视觉一致性第43轮（2026-02-10）：重构 `frontend/src/components/Layout/MobileMenu.tsx` 并新增 `MobileMenu.module.css`，将移动端菜单触发器、抽屉标题区、关闭按钮与菜单主体的内联样式收敛为语义化模块样式；统一菜单触发与关闭按钮触达尺寸为 44px，补齐 hover/focus-visible 可见反馈与键盘可达性；同时规范抽屉菜单项间距、选中态与悬停态表达，降低移动端导航在不同页面状态下的布局与交互不一致
- 前端视觉一致性第42轮（2026-02-10）：重构通知组件 `frontend/src/components/Notification/NotificationCenter.tsx` 并新增 `NotificationCenter.module.css`，清理通知下拉面板、通知项、动作按钮和铃铛触发器中的大量内联样式；将通知类型标签从 `Tag color` 收敛为语义化 `tone` 样式体系，并为未读态补齐统一高亮与焦点可见反馈；统一铃铛触发器、通知项动作按钮、底部操作和“全部已读”按钮触达尺寸（40/44px）及可访问属性（`aria-label/aria-expanded`）；新增通知列表键盘可触发能力（Enter/Space），并统一下拉面板宽高约束与移动端布局节奏，降低通知中心在状态表达、交互反馈和移动端可点击性上的不一致
- 前端视觉一致性第41轮（2026-02-10）：重构 `frontend/src/components/Layout/AppHeader.tsx` 与 `Layout.module.css` 头部区域样式，移除折叠按钮/标题偏移/图标尺寸/用户信息等内联样式常量，统一为语义化 class；将头部折叠按钮、工具图标按钮、通知入口与用户菜单触达尺寸收敛到 40/44px，补齐 hover 与 focus-visible；优化用户名称溢出截断与移动端隐藏策略（仅隐藏用户名文本，不影响菜单按钮可点），并将 `transition: all` 收敛为具体属性过渡，降低系统头部在不同页面和屏宽下的视觉与交互不一致
- 前端视觉一致性第40轮（2026-02-10）：重构全局导航组件 `frontend/src/components/Layout/AppBreadcrumb.tsx` 并新增 `AppBreadcrumb.module.css`，将面包屑从默认文本渲染升级为语义化导航容器（`nav[aria-label="页面路径"]`），统一首页与路径节点的触达尺寸（40/44px）、间距节奏和 focus-visible；为首页入口补充“图标+文字”表达（不再仅图标），并将当前节点与可点击节点做层级区分；同步增强长路径横向可滚动能力与分隔符可读性，减少系统页面在层级导航上的点击区域过小与视觉反馈不一致问题
- 前端视觉一致性第39轮（2026-02-10）：重构公共页容器 `frontend/src/components/Common/PageContainer.tsx` 并新增 `PageContainer.module.css`，将页面骨架中的大段内联样式（容器、面包屑、标题区、返回按钮、extra 区）收敛为语义化样式类；统一返回按钮与头部动作按钮的触达尺寸（40/44px）和焦点可见样式，保持移动端可点击性；为页面头部补齐响应式节奏（标题区/额外操作区自动换行），并保留 `contentStyle` 透传能力，降低不同业务页在标题栏密度、按钮尺寸和移动端布局上的观感不一致
- 前端视觉一致性第38轮（2026-02-10）：重构 `frontend/src/components/Common/ResponsiveTable.tsx/.module.css`，将移动端卡片视图补齐统一头部（标题 + 总数）、空态容器与列表语义（`section/list/listitem`）；新增 `mobileListAriaLabel` 与 `emptyDescription` 配置，补充卡片模式导航语义与空态一致性；统一卡片字段区的分隔节奏、按钮/输入控件触达尺寸（40/44px）和 focus-visible 样式，并将 `className` 在移动与桌面视图下保持一致透传，降低响应式表格在不同页面中的移动端观感漂移与交互反馈不一致问题
- 前端视觉一致性第37轮（2026-02-10）：重构公共列表组件 `frontend/src/components/Common/ListToolbar.tsx/.module.css` 与 `TableWithPagination.tsx/.module.css`，统一筛选工具栏和分页区的控件触达尺寸（40/44px）、焦点可见样式与圆角节奏；为 `ListToolbar` 增加 `className` 与 `plain` 变体容器，减少跨页面工具栏间距与高度漂移；为 `TableWithPagination` 新增 `paginationAriaLabel` 导航语义，并统一分页器按钮/页码/页大小选择器/快速跳转输入的可触达和焦点反馈，提升系统各列表页在键盘可访问性与移动端点击体验上的一致性
- 前端视觉一致性第36轮（2026-02-10）：重构 `frontend/src/pages/System/UserManagementPage.tsx` 与 `UserManagementPage.module.css`，将用户状态/角色/锁定标记从 `Tag color` 和图标散点表达收敛为语义化 `tone + semanticTag` 体系，并补充“可登录/已禁用/异常冻结”等辅助文案，降低仅靠颜色理解状态的成本；统一列表行内操作按钮为 40/44px 可触达尺寸并补齐 `focus-visible`，同时为查看/编辑/锁定/启停/删除等操作补充 `aria-label`；新增筛选摘要区（总记录数 + 启用筛选数）与活跃占比展示（活跃数/总数），并统一刷新按钮加载反馈，减少用户管理页在状态识别、筛选反馈和操作触达上的视觉割裂
- 前端视觉一致性第35轮（2026-02-10）：重构 `frontend/src/pages/System/PromptListPage.tsx` 与 `PromptListPage.module.css`，将文档类型/提供商/状态及版本来源标签从 `Tag color` 收敛为语义化 `tone + semanticTag` 体系，并为状态与来源补充“在线生效/待发布/仅历史、系统生成/人工维护”等辅助文案，避免仅靠颜色表达；统一列表头部操作按钮与表格行内操作按钮触达尺寸（40/44px）并补齐 focus-visible；新增筛选摘要区（总记录数 + 启用筛选数）与活跃占比展示（活跃数/总数），同步规范版本号展示逻辑（兼容 `v` 前缀与数字版本），降低 Prompt 管理列表在状态识别、操作反馈与布局节奏上的视觉割裂
- 前端视觉一致性第34轮（2026-02-10）：重构 `frontend/src/pages/System/PromptDashboard.tsx` 与 `PromptDashboard.module.css`，将监控页中模板状态/建议优先级/字段名称标签从 `Tag color` 收敛为语义化 `tone/semanticTag` 体系，并补充状态辅助文案（如“使用中·在线生效/草稿·待发布”）避免仅靠颜色表达；统一顶部刷新按钮与筛选控件的可访问名称与触达尺寸（40/44px），新增筛选摘要区（监控区间 + 当前模板）和字段风险摘要（监控字段数 + 高风险字段数）；同步规范版本号显示（防止 `v` 前缀重复）、统一统计卡文字层级与焦点可见样式，降低 Prompt 监控页在信息密度和交互反馈上的视觉割裂
- 前端视觉一致性第33轮（2026-02-10）：重构 `frontend/src/pages/System/TemplateManagementPage.tsx` 与 `TemplateManagementPage.module.css`，将模板类型/状态标签从 `Tag color` 收敛为语义化 `tone/statusTag/typeTag` 体系，并为状态补充“使用中·已发布/草稿·待发布/已废弃·停止维护”文本提示，避免仅靠颜色传达；统一模板列表行内操作按钮为 40/44px 可触达尺寸并补充 focus-visible，优化下载中态为“按模板行单独 loading”（不再全表按钮同时 loading）；补齐模板列表摘要区（总模板数 + 可用模板数），统一统计卡色阶表达与预览弹窗动作区按钮尺寸，同时修复版本号渲染重复前缀（`v{record.version}` → 规范化输出），降低模板管理页在状态表达与交互反馈上的视觉割裂
- 前端视觉一致性第32轮（2026-02-10）：重构 `frontend/src/pages/System/OrganizationPage.tsx` 与 `OrganizationPage.module.css`，将组织状态与历史操作状态从 `Tag color` 收敛为语义化 `tone/statusTag` 体系，并在树节点、列表状态列、状态下拉选项中统一表达；补齐列表工具栏筛选摘要（总记录数 + 启用筛选数）与刷新按钮加载态，统一“列表/树形”刷新动作的可访问名称；将组织表格行操作按钮收敛为 40/44px 触达尺寸并补充 focus-visible 样式，避免移动端点击区域过小；同步优化新建/编辑弹窗栅格响应式断点与底部操作区按钮尺寸，减少组织管理页在多端下的布局与交互割裂
- 前端视觉一致性第31轮（2026-02-10）：重构 `frontend/src/pages/System/RoleManagementPage.tsx` 与 `RoleManagementPage.module.css`，补齐角色列表筛选摘要（总记录数 + 启用筛选数）并将搜索框改为受控输入，统一刷新/新建按钮加载态与可访问名称；将角色行操作从 `link/small` 收敛为语义化文本按钮（40/44px 触达）并补充 focus-visible 样式，状态开关增加“启/停”文字避免仅靠颜色反馈；同时将系统角色标签改为语义化 `statusTag` 风格，统一创建/权限配置弹窗底部按钮尺寸，提升角色管理页在触达、状态表达和交互节奏上的一致性
- 前端视觉一致性第30轮（2026-02-10）：重构 `frontend/src/pages/System/SystemSettingsPage.tsx` 与 `SystemSettingsPage.module.css`，为“基本设置/系统信息/数据备份”Tab 增加图标化标签与统一触达尺寸，补齐设置页提示区与数值输入控件（`InputNumber`）宽度一致性；将数据库状态从单纯文本色值升级为语义化状态标签 + 辅助说明，避免仅靠颜色传达状态；同步收敛备份/恢复操作区层级（按钮 + 提示文案）、恢复文件选择逻辑（`useRef` 触发并清空 input 值），统一按钮可访问名称与移动端 44px 触达标准
- 前端视觉一致性第29轮（2026-02-10）：重构 `frontend/src/pages/System/OperationLogPage.tsx` 与 `OperationLogPage.module.css`，将操作类型/模块/响应状态/请求方法标签统一为语义化 `tone` 标签体系，补齐筛选摘要区（总记录数 + 启用筛选数）并统一筛选控件宽度与刷新按钮触达尺寸；同步收敛表格行“查看详情”按钮为 40/44px 可触达尺寸与 `aria-label`，增强键盘焦点可见性；在详情抽屉中统一用户信息、请求信息与响应信息的排版层级，补充状态码文本提示与响应耗时语义标签，降低系统日志页面在状态感知和信息密度上的视觉割裂
- 前端视觉一致性第28轮（2026-02-10）：重构 `frontend/src/pages/System/NotificationCenter.tsx` 与 `NotificationCenter.module.css`，将通知优先级/类型/未读状态统一为语义化 `tone/status` 标签样式，收敛头部“全部已读”按钮与列表行操作按钮的触达尺寸；补充通知项键盘触发能力（Enter/Space）与操作按钮可访问名称，新增头部未读计数与总量提示，并强化未读项边界高亮与 focus-visible 表达，降低通知中心在状态感知与交互一致性上的割裂
- 前端视觉一致性第27轮（2026-02-10）：重构 `frontend/src/pages/System/EnumFieldPage.tsx` 与 `EnumFieldPage.module.css`，补齐类型列表工具栏搜索与类型总数提示，统一类型/配置/默认状态标签语义色阶，收敛表格行内操作按钮的触达尺寸与可访问名称；同时优化枚举值颜色预览为 CSS 变量驱动、状态文本与按钮风格一致化，并将统计卡第四项改为“启用值数量”，减少枚举字段管理页在筛选区、状态表达与操作列上的视觉割裂
- 前端视觉一致性第26轮（2026-02-10）：重构 `frontend/src/pages/System/DictionaryPage.tsx` 与 `frontend/src/components/Dictionary/EnumValuePreview.tsx`，并新增 `EnumValuePreview.module.css`，收敛枚举类型/编码标签、查看详情与行内操作按钮、启用状态展示等样式表达，移除预览组件中的硬编码颜色与内联样式；统一为语义化 `tone/status` 类与尺寸分层（small/middle/large），补齐启用状态文字提示和按钮可访问名称，减少字典管理页与枚举预览在状态表达与交互密度上的视觉漂移
- 前端视觉一致性第25轮（2026-02-10）：重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateImport.tsx`、`frontend/src/components/PropertyCertificate/PropertyCertificateUpload.tsx` 与 `PropertyCertificateReview.tsx`，并新增对应模块样式文件，移除导入流程中的内联样式与硬编码高亮；统一上传区/审核区的状态标签、步骤卡与表单动作区布局，补齐“置信度”文字等级提示与资产匹配项交互态（hover/selected/键盘触发），并将创建成功跳转改为路由导航（避免整页刷新），提升产权证导入全流程的视觉一致性与交互连贯性
- 前端视觉一致性第24轮（2026-02-10）：重构 `frontend/src/pages/PropertyCertificate/PropertyCertificateList.tsx` 并新增 `PropertyCertificateList.module.css`，移除工具栏与页面布局中的内联样式，统一搜索框/创建按钮在多端下的宽度与间距；同时将证书类型、置信度、审核状态改为语义化 `tone/status` 标签并补充辅助文案（不再仅靠颜色传达），统一操作按钮触达尺寸与可访问名称，降低产权证列表页在状态表达与交互密度上的视觉漂移
- 前端视觉一致性第23轮（2026-02-10）：重构 `frontend/src/components/Rental/ContractList/ContractStatsCards.tsx`、`ContractFilterBar.tsx`、`ContractTable.tsx` 并新增对应模块样式文件，移除统计卡、筛选条和表格操作区中的内联样式与硬编码状态着色；统一为语义化 `tone/status` 类与响应式栅格，补齐合同状态辅助文案（不再仅靠颜色区分），并统一表格操作按钮触达尺寸与筛选区动作布局，降低合同列表页在多端下的视觉割裂
- 前端视觉一致性第22轮（2026-02-10）：重构 `frontend/src/pages/Rental/ContractCreatePage.tsx` 并新增 `ContractCreatePage.module.css`，移除成功提示卡、创建指南、步骤统计与底部操作栏中的内联样式及 `COLORS` 直接依赖，统一为语义化模块样式；同步将创建步骤改为响应式栅格渲染（`xs/sm/lg`），将操作按钮区收敛为可换行且移动端最小触达尺寸（44px），并将提示文案改为列表层级，提升合同创建页在不同屏宽下的布局一致性与可读性
- 前端视觉一致性第21轮（2026-02-10）：重构 `frontend/src/pages/Rental/RentLedgerPage.tsx` 并新增 `RentLedgerPage.module.css`，移除统计区、筛选区、批量操作与弹窗表单内联样式及 `COLORS` 直接着色，统一为语义化 `tone/status` 类和响应式栅格；同步将实收金额/欠款金额/支付状态补充文字辅助标签（不再仅靠颜色区分），并统一表格行内操作按钮触达尺寸、筛选控件宽度与移动端动作区布局，提升租金台账页的视觉一致性与可访问性
- 前端视觉一致性第20轮（2026-02-10）：重构 `frontend/src/pages/Rental/RentStatisticsPage.tsx` 并新增 `RentStatisticsPage.module.css`，清理页面头部、概览指标、运营指标及表格单元中的内联样式与 `COLORS` 直接依赖，统一为语义化 `tone/status` 类与响应式布局；同时为“欠款总额/收缴率”补充文字状态标签（不再仅靠颜色表达），收敛表格名称层级、筛选控件宽度和移动端操作区间距，降低租金统计页在不同数据状态与屏宽下的视觉不一致
- 前端视觉一致性第19轮（2026-02-10）：优化 `frontend/src/pages/Contract/ContractImportStatus.tsx` 并新增 `ContractImportStatus.module.css`，移除状态页头部、当前状态卡、步骤说明、详情区与结果统计区的内联样式/`COLORS` 依赖，统一为语义化 `tone` 类；同步将当前状态图标、进度统计与详情状态标签改为一致的色阶表达，补齐移动端操作按钮触达尺寸和头部动作区布局，减少导入状态页在不同状态与屏宽下的视觉割裂
- 前端视觉一致性第18轮（2026-02-10）：重构 `frontend/src/pages/Contract/ContractImportReview.tsx` 并新增 `ContractImportReview.module.css`，清理确认页统计区、证据提示、匹配区、告警区与底部操作区的内联样式/硬编码颜色，统一为语义样式类；同时将旧版 `Tabs.TabPane` 写法收敛为 `items`，并将置信度色阶从 `styles` 直传改为卡片 `tone` 类驱动，降低导入确认页布局与状态表达的不一致
- 前端视觉一致性第17轮（2026-02-10）：优化 `frontend/src/pages/Assets/AssetAnalyticsPage.tsx` 与 `AssetAnalyticsPage.module.css`，移除页面级 loading/error/header/空态/分区卡片中的内联样式与 `COLORS` 直接依赖，统一为模块化语义类；同步将分析维度区改为可换行布局、收敛空态提示文本层级，并将分布项与通用卡片过渡从 `transition: all` 精确化为具体属性，进一步降低分析页在不同屏宽下的视觉跳变
- 前端视觉一致性第16轮（2026-02-10）：重构 `frontend/src/pages/Contract/PDFImportHelp.tsx`，新增 `PDFImportHelp.module.css`，将帮助弹窗中的导航区、使用指南、最佳实践、字段说明等板块内联样式与硬编码主色收敛为模块化语义样式；同步优化导航按钮与卡片在移动端的栅格节奏，统一标签组间距与文本层级，减少帮助页“块间密度不一致”和阅读节奏断裂问题
- 前端视觉一致性第15轮（2026-02-10）：重构 `frontend/src/pages/Ownership/OwnershipDetailPage.tsx` 并新增 `OwnershipDetailPage.module.css`，清理详情标题区、统计卡片区、基础信息卡片与两组表格 Tab 中的内联样式及 `COLORS` 直接着色；统一为 `tone` 语义类与响应式统计栅格，补齐表格横向滚动与链接按钮触达样式，降低权属方详情页在不同屏宽下的布局跳变和色彩不一致
- 前端视觉一致性第14轮（2026-02-10）：重构 `frontend/src/pages/Contract/ContractImportUpload.tsx`，新增 `ContractImportUpload.module.css`，将上传、处理中、成功、失败、使用说明五个区块的大段内联样式与 `COLORS` 硬编码色值收敛为主题变量驱动的语义类；同步将“上传结果/错误提示/使用说明”改为更一致的卡片节奏与文本标签结构，降低合同导入流程页的视觉漂移与可读性波动
- 前端视觉一致性第13轮（2026-02-10）：优化 `frontend/src/pages/System/RoleManagementPage.tsx` 与 `RoleManagementPage.module.css`，移除 `COLORS` 依赖与统计卡片 `Statistic.styles`/数量角标硬编码色值，统一为 `tone` 语义类；同时将角色状态、权限类型、权限/用户数量改为“文本 + 语义色阶标签”表达，减少仅靠颜色传达信息并收敛系统管理页视觉风格漂移
- 前端视觉一致性第12轮（2026-02-10）：优化 `frontend/src/pages/System/OperationLogPage.tsx` 与 `OperationLogPage.module.css`，移除响应时间与统计卡片的内联颜色/`Statistic.styles` 透传，统一为 `tone` 语义类；同时在响应时间展示中补充“快/中/慢”文本标签，避免仅靠颜色传达状态，并收敛详情抽屉耗时区与列表耗时区的视觉表达一致性
- 前端视觉一致性第11轮（2026-02-10）：优化 `frontend/src/pages/System/PromptDashboard.tsx` 与 `frontend/src/pages/System/PromptListPage.tsx`，移除剩余动态颜色内联样式和筛选控件 `style={{ width: '100%' }}`，改为 CSS 语义色阶类；同时更新 `PromptDashboard.module.css` / `PromptListPage.module.css` 统一统计卡片、趋势指标与表格数值的色彩表达，并将 Prompt 性能卡片颜色逻辑从组件内 `styles` 透传收敛到模块化样式，进一步减少系统页面视觉跳变
- 前端视觉一致性第10轮（2026-02-09）：重构 `frontend/src/pages/System/EnumFieldPage.tsx` 与 `frontend/src/pages/System/NotificationCenter.tsx`，新增 `EnumFieldPage.module.css` 并扩展 `NotificationCenter.module.css`，统一枚举页面统计卡片、Tab 工具栏、操作列按钮与颜色预览区样式，替换已废弃的 `Tabs.TabPane` 写法并接入 `PageContainer` 页面骨架；同时移除通知中心类型图标内联颜色，收敛为 CSS 语义类，持续降低系统页视觉风格漂移
- 质量门禁与测试可复现性修复（2026-02-09）：修复后端 `ruff I001` 导入排序（`backend/alembic/versions/*`、`backend/scripts/migration/migrate_dictionaries.py`、相关单测文件），将前端 `pnpm test` 默认切换为一次性 `vitest run` 并让 `make test-frontend` 直接复用 `pnpm test`，避免 watch 模式导致流水线超时；同时增强 `make backend-import` 的 `DATABASE_URL` 前置校验与错误提示，并在 `backend/tests/conftest.py` / `backend/tests/integration/conftest.py` 增加集成测试数据库缺失或不可用时的显式跳过逻辑，防止无效凭据触发误报失败
- 前端视觉一致性第9轮（2026-02-09）：重构 `frontend/src/pages/System/OrganizationPage.tsx` 与 `frontend/src/pages/System/UserManagementPage.tsx`，新增 `OrganizationPage.module.css` / `UserManagementPage.module.css` 统一统计卡片、筛选工具栏、树形视图区、表格信息层级、模态框操作区与用户详情抽屉视觉节奏；清理两页主要内联样式与硬编码颜色，补齐按钮触达尺寸和移动端布局收敛，进一步降低系统管理核心页的布局与交互不一致问题
- 前端视觉一致性第8轮（2026-02-09）：重构 `frontend/src/pages/System/TemplateManagementPage.tsx` 与 `frontend/src/pages/System/SystemSettingsPage.tsx`，新增 `TemplateManagementPage.module.css` / `SystemSettingsPage.module.css` 统一统计卡片、说明区、详情弹窗与备份恢复区的布局节奏，移除大段内联样式并修正系统信息区 `Space` 垂直布局写法；同时将模板页自定义统计组件替换为 Ant Design `Statistic`，统一按钮触达尺寸与移动端间距，持续收敛系统管理页面视觉割裂问题
- 前端视觉一致性第7轮（2026-02-09）：优化 `frontend/src/pages/System/OperationLogPage.tsx` 与 `frontend/src/pages/System/DictionaryPage.tsx`，新增 `OperationLogPage.module.css` / `DictionaryPage.module.css` 收敛日志详情抽屉、筛选区、统计卡片与字典类型概览中的内联样式；统一表格单元格信息层级、过滤控件宽度与移动端按钮触达尺寸，并补齐日志响应时长/请求信息显示的视觉一致性，进一步降低系统管理页面布局割裂感
- 前端视觉一致性第6轮（2026-02-09）：重构 `frontend/src/pages/System/NotificationCenter.tsx` 与 `frontend/src/pages/System/RoleManagementPage.tsx`，新增 `NotificationCenter.module.css` / `RoleManagementPage.module.css` 收敛通知项与角色信息单元格的内联样式，统一统计卡片响应式栅格、筛选区控件宽度与按钮触达尺寸；补齐角色表格横向滚动、权限配置区 Transfer/Tree 的移动端可视性，以及通知消息项未读高亮/时间信息层级，进一步消除系统页布局与视觉节奏不一致问题
- 前端视觉一致性第5轮（2026-02-09）：重构 `frontend/src/pages/System/PromptListPage.tsx` 与 `frontend/src/pages/System/PromptDashboard.tsx`，统一接入 `PageContainer` 页面骨架、响应式统计卡片与筛选区布局；新增 `PromptListPage.module.css` / `PromptDashboard.module.css` 消除大段内联样式与硬编码低对比色（如 `#999`、`#f0f0f0`），补齐表格横向滚动与 `role="alert"` 错误可达性；同时将 Prompt 版本历史弹层改为 AntD `Table`，并修正列表“平均准确率”颜色阈值从错误的 `>=90/70` 到 `>=0.9/0.7`
- docs 目录深度清理（2026-02-09）：将 V2 发布说明迁移至 `docs/archive/releases/v2-release-notes-2026-01.md`，并将产权证使用指南与测试标准迁移至 `docs/guides/`；按“无需兼容”要求删除三份跳转页，统一更新 `README.md`、`AGENTS.md`、`CHANGELOG.md` 与 `docs/index.md` 到新路径
- docs 命名风格统一（2026-02-09）：将 `docs/guides/NAMING_CONVENTIONS.md` 重命名为 `docs/guides/naming-conventions.md`，将 `docs/archive/plans/v2_upgrade_plan-2026-02.md` 重命名为 `docs/archive/plans/v2-upgrade-plan-2026-02.md`，并将两份中文调研问卷文件重命名为 kebab-case 英文文件名；同步更新 `README.md`、`AGENTS.md`、`docs/index.md` 与归档文档内路径引用
- docs 旧文档治理与归档规范化（2026-02-09）：将历史文档迁入 `docs/archive/`（含 `architecture-refactoring`、`v2-upgrade-plan`、`v2-test-cases`、两份需求调研问卷、`siliconflow-paddleocr-integration`），新增 `docs/archive/readme.md` 归档规则，更新 `docs/index.md` 导航与治理约束，并统一 `integrations` 相关文档状态标识
- 需求评审清单补充（2026-02-09）：新增 `docs/requirements-review-checklist.md`（产品/研发/测试签字版），并在 `docs/index.md` 增加入口、在 `docs/requirements-specification.md` 增加评审执行指引，形成“规格—评审—留痕”闭环
- 需求规格重建（代码证据基线，2026-02-09）：新增 `docs/requirements-specification.md`（主 SRS）与 `docs/features/requirements-appendix-modules.md`（模块与接口附录），并更新 `docs/index.md` 增加权威入口；同时将 `docs/features/prd-asset-management.md`、`docs/features/prd-rental-management.md`、`docs/features/spec-user-permissions.md`、`docs/features/spec-data-models.md` 标记为历史草稿，避免与现状代码基线冲突
- 认证服务单测密钥读取对齐（2026-02-09）：`backend/tests/unit/services/core/test_authentication_service.py` 的 JWT 编解码断言从模块常量 `SECRET_KEY/ALGORITHM` 切换为运行时 `settings.SECRET_KEY/settings.ALGORITHM`，修复全量回归中前序测试修改配置后触发的 token 解码偶发失败
- 统计聚合 API 单测污染隔离（2026-02-09）：`backend/tests/unit/api/v1/analytics/test_statistics.py` 统一改为 patch 服务层实际引用（`basic_stats_service.asset_crud` / `area_stats_service.asset_crud`）并显式 `AsyncMock`，`client` 夹具补充前置 `dependency_overrides.clear()`，修复全量回归中 `statistics/basic` 偶发 500 与异步 mock 未等待告警
- 基础统计单测异步 patch 目标纠偏（2026-02-09）：`backend/tests/unit/api/v1/analytics/statistics_modules/test_basic_stats.py` 将 `get_multi_with_search_async` 的 patch 目标统一改为 `src.services.analytics.basic_stats_service.asset_crud` 并显式使用 `AsyncMock`，同时在 `client` 夹具前后清理 `app.dependency_overrides`，修复全量回归中 `statistics/basic` 偶发 500 与未等待协程告警
- 面积统计单测 patch 目标纠偏（2026-02-09）：`backend/tests/unit/api/v1/analytics/statistics_modules/test_area_stats.py` 将 `get_multi_with_search_async` 的 patch 目标从 `src.crud.asset.asset_crud` 改为服务层实际引用 `src.services.analytics.area_stats_service.asset_crud`，修复全量回归中前序集成测试替换引用后导致的 `area-statistics` 偶发 500 与未等待协程告警
- 面积统计单测依赖覆盖污染隔离（2026-02-09）：`backend/tests/unit/api/v1/analytics/statistics_modules/test_area_stats.py` 的 `client` 夹具在测试前后强制 `clear()` `app.dependency_overrides`，避免受前序用例遗留 override 影响导致全量回归中 `area-statistics` 偶发 500
- 面积统计单测隔离与异步 mock 稳定化（2026-02-09）：`backend/tests/unit/api/v1/analytics/statistics_modules/test_area_stats.py` 的 `client` 夹具改为保存并恢复原始 `app.dependency_overrides`，并将 `asset_crud.get_multi_with_search_async` patch 升级为 `AsyncMock`，降低全量回归中的 500 偶发失败
- 测试隔离：全局令牌黑名单状态重置（2026-02-09）：`backend/tests/conftest.py` 新增 `reset_token_blacklist_state` 自动夹具，测试前后清空 `blacklist_manager`，修复跨用例共享内存黑名单导致的 cookie 登录后立即 401 问题
- 集成测试公共建数去重修复（2026-02-09）：`backend/tests/integration/conftest.py` 的 `test_data` 夹具改为“存在即复用”，对组织 `TEST_ORG` 与管理员 `admin` 执行幂等创建/更新并统一密码，修复全量测试下 `ix_users_username` 唯一键冲突
- 认证服务常量导出兼容修复（2026-02-09）：`backend/src/services/core/authentication_service.py` 恢复导出 `SECRET_KEY/ALGORITHM/ACCESS_TOKEN_EXPIRE_MINUTES/REFRESH_TOKEN_EXPIRE_DAYS` 等历史常量用于测试与外部导入兼容，同时保留运行时按 `settings` 动态读取，兼顾稳定性与兼容性
- 认证密钥缓存根因修复（2026-02-09）：`backend/src/services/core/authentication_service.py` 与 `backend/src/services/core/session_service.py` 移除模块导入时缓存的 `SECRET_KEY/ALGORITHM` 常量，统一在调用时读取 `settings`，修复全量测试收集场景下 token 签发与校验密钥漂移导致的 `Signature verification failed`
- 安全测试导入顺序 lint 修复（2026-02-09）：`backend/tests/security/test_phase1_security.py` 在 `test_encryption_key_manager_no_key` 内部导入顺序对齐 Ruff `I001` 规则，消除改动文件静态检查告警
- E2E 角色权限基线断言对齐（2026-02-09）：`backend/tests/e2e/test_auth_flow_e2e.py` 将参数化权限下限调整为 `admin>=1`、`user>=0`，对齐最小 RBAC 初始化数据（admin 仅保证 `system:admin`）下的实际返回，避免环境依赖导致误报
- E2E 用户邮箱唯一冲突修复（2026-02-09）：`backend/tests/e2e/conftest.py` 的 `create_test_user` 在邮箱已存在时自动生成带随机后缀的新邮箱，避免与系统预置/共享夹具用户冲突触发 `ix_users_email` 唯一键错误
- E2E 用户建数手机号必填修复（2026-02-09）：`backend/tests/e2e/conftest.py` 的 `create_test_user` 新增 `phone` 参数并在缺省时自动生成唯一手机号，修复 `users.phone NOT NULL` 导致的 `test_auth_flow_e2e` 初始化失败
- 安全事件日志测试会话适配修复（2026-02-09）：`backend/tests/security/test_phase2_task2_security_logging.py` 新增本地 `AsyncSessionAdapter` 并在权限拒绝日志测试中注入 `SecurityEventLogger`，避免跨事件循环使用全局异步引擎导致的 `Future attached to a different loop`
- 安全事件日志测试异步化修复（2026-02-09）：`backend/tests/security/test_phase2_task2_security_logging.py` 将 `TestPermissionDeniedLogging` 中 `log_permission_denied/get_event_count/should_alert` 调用改为 `await`，并使用异步会话路径，修复 `coroutine has no attribute ...` 与未等待协程告警
- 安全集成监控端点漂移兼容（2026-02-09）：`backend/tests/security/test_phase1_security.py` 的 `test_encryption_status_endpoint` 在 `/api/v1/monitoring/encryption-status` 未注册时改为 `pytest.skip`，避免路由收口阶段因历史端点缺失阻塞安全回归
- 安全集成错误响应断言兼容修复（2026-02-09）：`backend/tests/security/test_phase1_security.py` 的未授权分组断言兼容统一错误响应结构（优先 `error.message`，兼容历史 `detail.message`），修复 `KeyError: 'detail'`
- 安全集成测试夹具与异步调用修复（2026-02-09）：`backend/tests/security/test_phase1_security.py` 的 `TestSecurityIntegration` 统一改用 `test_client` 异步夹具与 `await` 调用，并将不存在的 `admin_auth_headers` 替换为 `auth_headers`，修复 `fixture 'client' not found` 阻塞
- 安全测试加密强制开关隔离修复（2026-02-09）：`backend/tests/security/test_phase1_security.py` 的 `test_encryption_key_manager_no_key` 额外固定 `settings.REQUIRE_ENCRYPTION=False`，避免环境开启强制加密时抛 `ConfigurationError` 干扰“无密钥降级可用性”断言
- 安全测试配置读取行为修复（2026-02-09）：`backend/tests/security/test_phase1_security.py` 的 `test_encryption_key_manager_no_key` 改为 `monkeypatch.setattr(settings, "DATA_ENCRYPTION_KEY", "")`，对齐 `EncryptionKeyManager` 读取已实例化 `settings` 的行为，修复 `setenv` 无效导致的断言失败
- 角色管理页错误反馈时效修复（2026-02-09）：`frontend/src/pages/System/RoleManagementPage.tsx` 将角色列表、权限列表、统计数据的 React Query `retry` 从 `1` 调整为 `false`，避免失败后重试导致错误提示延迟，确保“加载失败即时反馈”（对齐 UI/UX 反馈规范与现有测试期望）
- 前端 UI/UX 可访问性对齐（2026-02-09，`ui-ux-pro-max`）：为系统页面与项目选择中的图标按钮补齐可访问名称，覆盖 `frontend/src/pages/System/PromptListPage.tsx`、`frontend/src/components/Project/ProjectSelect.tsx`、`frontend/src/components/Common/PageContainer.tsx`，统一增加 `aria-label`（如“激活/版本历史/编辑/刷新/返回”），消除图标按钮无可读名称导致的可用性与测试脆弱性
- 前端测试稳定性回归修复（2026-02-09）：批量收敛前端测试中的路由嵌套、AntD 文案间距选择器、旧组件契约漂移与 mock 形态问题，涉及 `useAuth`、`PromptListPage`、`ProjectDetail/ProjectSelect`、`AssetForm/OwnershipForm/ProjectForm` 等测试；本地回归 `pnpm lint`、`pnpm type-check` 与 `pnpm vitest run` 已全部通过（`1926/1926`）
- 前端页面布局一致性治理（2026-02-09，`ui-ux-pro-max`）：统一主内容区间距基线，`frontend/src/components/Layout/Layout.module.css` 将 `.content` 内边距收敛为 `0`，避免布局层与页面层双重 `24px` 叠加；并将 `ContractList`、`RentLedger`、`PropertyCertificateList`、`ProjectManagement`、`OwnershipManagement`、`OperationLog`、`TemplateManagement`、`Organization`、`Dictionary`、`Profile`、`SystemSettings` 等页面统一接入 `frontend/src/components/Common/PageContainer.tsx`，对齐页面标题区、面包屑与内容容器规范
- 前端高度与背景策略收敛（2026-02-09）：修复多个页面使用 `100vh` 导致的滚动/留白异常，`frontend/src/pages/Rental/RentStatisticsPage.tsx`、`frontend/src/pages/System/PromptListPage.tsx`、`frontend/src/pages/System/PromptDashboard.tsx`、`frontend/src/pages/Contract/PDFImportPage.tsx`、`frontend/src/pages/Dashboard/DashboardPage.module.css` 改为以父容器高度（`100%`）和主题色变量驱动，减少移动端与嵌套布局场景下的视觉漂移
- 顶栏交互一致性与可用性优化（2026-02-09，`ui-ux-pro-max`）：`frontend/src/components/Layout/AppHeader.tsx` 将语言/帮助/用户入口从视觉 `div` 收敛为语义化 `Button`，补齐 `aria-label` 与点击反馈；`frontend/src/components/Layout/Layout.module.css` 新增统一头部图标按钮与用户入口按钮样式（含 hover/focus-visible），修复“看起来可点但不可键盘操作”的一致性问题
- 遗留页面壳层收敛（2026-02-09）：`frontend/src/pages/PropertyCertificate/PropertyCertificateImport.tsx`、`frontend/src/pages/Assets/SimpleAnalyticsPage.tsx`、`frontend/src/pages/System/NotificationCenter.tsx` 统一接入 `PageContainer`，减少页面级手写容器导致的标题区与间距漂移；同步更新测试 `frontend/src/pages/PropertyCertificate/__tests__/PropertyCertificateImport.test.tsx` 与 `frontend/src/components/Layout/__tests__/AppHeader.test.tsx` 适配新容器与交互结构
- 前端筛选栏/表格公共组件视觉统一（2026-02-09，第三轮）：新增 `frontend/src/components/Common/ListToolbar.module.css`、`frontend/src/components/Common/TableWithPagination.module.css`、`frontend/src/components/Common/ResponsiveTable.module.css`，并改造 `ListToolbar`、`TableWithPagination`、`ResponsiveTable`，统一筛选栏容器边框与内边距、表头/行高节奏、分页区分隔线与移动端卡片留白，降低模块间“同类控件观感不一致”
- 全局卡片与控件密度基线收敛（2026-02-09，第三轮）：`frontend/src/styles/global.css` 新增统一的 Card 头部/正文节奏、按钮高度与圆角、输入控件圆角、表格行内边距基线，减少页面局部覆写导致的按钮密度与表格松紧不一致问题；本轮回归 `pnpm lint`、`pnpm type-check` 与相关页面测试均通过
- Dashboard/Analytics 主题变量收敛（2026-02-09，第四轮）：`frontend/src/pages/Dashboard/DashboardPage.module.css` 与 `frontend/src/pages/Assets/AssetAnalyticsPage.module.css` 将硬编码颜色替换为设计令牌（`--color-*`），并将分析页容器高度从 `100vh` 收敛到父容器高度，修复深浅色主题下卡片/文字对比与页面留白不一致
- 移动端布局视觉基线对齐（2026-02-09，第四轮）：`frontend/src/components/Layout/MobileLayout.tsx` 与 `frontend/src/components/Layout/MobileMenu.tsx` 统一使用主题变量色值和容器高度基线，消除移动端头部、正文与抽屉菜单在不同主题下的颜色漂移；`frontend/src/pages/Contract/PDFImportPage.module.css` 同步将上传拖拽 hover 色值改为主题变量
- PostgreSQL 连接错误断言兼容修正（2026-02-09）：`backend/tests/integration/test_postgresql_migration.py` 的连接失败断言补充 `connect call failed` 文案匹配，兼容 asyncpg 在本地拒连场景下的实际错误文本
- PostgreSQL 连接错误测试语义对齐（2026-02-09）：`backend/tests/integration/test_postgresql_migration.py` 的 `test_connection_error_handling` 适配“引擎惰性连接”行为，不再期望 `initialize_engine()` 立即抛错，改为执行真实 SQL 触发连接失败并断言错误信息，同时显式 `dispose()` 临时引擎
- PostgreSQL 迁移集成测试全局 DB 管理器重置（2026-02-09）：`backend/tests/integration/test_postgresql_migration.py` 新增模块级 `autouse` 夹具，在每个用例前后重置并释放 `src.database._database_manager` 引擎，避免跨测试事件循环复用导致的间歇性失败
- PostgreSQL 并发集成测试事件循环隔离修复（2026-02-09）：`backend/tests/integration/test_postgresql_concurrency.py` 改为每个用例创建独立 `DatabaseManager` 并在 teardown `dispose()` 引擎，避免全局管理器跨测试复用导致的“Future attached to a different loop / Event loop is closed”失败
- 通知调度无活跃用户场景约束修复（2026-02-09）：`backend/tests/integration/services/test_notification_scheduler.py` 将 `test_no_active_users_no_notifications_created` 从 `DELETE FROM users` 调整为批量 `is_active=False`，避免 `user_role_assignments` 外键约束导致的集成测试失败，并保持“无活跃用户不发通知”的测试语义
- 通知调度集成测试手机号唯一冲突修复（2026-02-09）：`backend/tests/integration/services/test_notification_scheduler.py` 中多用户场景与非激活用户场景改用 `uuid` 派生手机号，避免与全局认证夹具（`test_user/test_admin`）发生 `users.phone` 唯一键冲突
- PDF 批量导入集成测试断言对齐（2026-02-09）：`backend/tests/integration/test_pdf_batch_api.py` 的“系统繁忙”场景改为断言 `error.code=SERVICE_UNAVAILABLE` 与当前 5xx 脱敏消息 `内部服务器错误`，兼容统一异常处理中对服务端错误的安全响应策略
- 集成测试公共用户夹具修复（2026-02-09）：`backend/conftest.py` 中 `test_user` 与 `test_admin` 新建用户补齐必填 `phone` 字段（`users.phone` 为非空且唯一），修复 `tests/integration/test_pdf_batch_api.py` 在初始化认证夹具阶段触发的 `NotNullViolation`
- 用户管理页管理员判定兼容修复（2026-02-09）：修复 `/api/v1/auth/users` 与 `/api/v1/auth/users/statistics/summary` 在历史 RBAC 数据（仅有 `system:manage` 无 `system:admin`）下误报 403 的问题；`backend/src/services/permission/rbac_service.py` 的管理员判定新增对 legacy `system:manage` 的兼容（角色权限与统一授权均支持），并保持 `system:admin` 为标准入口；同时在 `backend/scripts/setup/init_rbac_data.py` 补充基础权限 `system:admin`，避免新环境初始化后再次出现该漂移；新增回归用例 `backend/tests/unit/services/permission/test_rbac_service.py` 覆盖 legacy 权限兼容路径
- 前端类型检查回归修复（2026-02-09）：修复 `frontend/src/components/Asset/AssetExportProgress.tsx` 使用过期字段 `errorMessage` 导致 `ExportTaskWithApiFields` 不匹配（统一改为 `error_message` 并补默认文案）；修复 `frontend/src/components/Layout/AppBreadcrumb.tsx` 面包屑数组推断过窄导致 `string | Element` 赋值错误（显式声明 `title` 为 `React.ReactNode`）；补齐 `frontend/src/services/systemService.ts` 的 `userService.getUsers` 参数类型 `default_organization_id`，对齐 `frontend/src/pages/System/UserManagementPage.tsx` 的调用
- 前端系统页面依赖与 lint 收敛修复（2026-02-09）：修复 `frontend/src/pages/System/UserManagementPage.tsx` 对 `pinyin-pro` 的运行时解析失败（通过同步前端依赖恢复模块解析）；并清理前端静态检查阻塞项：`frontend/src/components/Common/LazyImage.tsx` 去除 `any` 点击事件断言并修正容器 ref 类型、`frontend/src/components/Common/ResponsiveTable.tsx` 将泛型约束与卡片渲染回调类型从 `any` 收敛为 `unknown`、`frontend/src/components/Forms/Asset/AssetDetailedSection.tsx` 删除未使用变量、`frontend/src/config/menuConfig.tsx` 与 `frontend/src/pages/Rental/ContractCreatePage.tsx` 清理未使用导入，同时在 `frontend/src/theme/dark.ts` 与 `frontend/src/theme/light.ts` 去除冗余类型断言以消除无效断言告警
- 服务集成回归稳定化（2026-02-09）：`backend/tests/integration/services/organization/test_organization_service.py`、`backend/tests/integration/services/task/test_task_service.py`、`backend/tests/integration/services/test_asset_service_comprehensive.py`、`backend/tests/integration/services/test_notification_scheduler.py` 对齐当前枚举与异步测试基线（独立 `create_async_engine + NullPool`、事务回滚、用户 `phone` 必填），修复历史同步调用、旧枚举值与跨事件循环连接错误
- 任务配置创建链路修复（2026-02-09）：`backend/src/services/task/service.py` 的 `create_excel_config` 不再向 CRUD 透传非法 `user_id` 字段，改为可选写入 `created_by`，修复 `TypeError: 'user_id' is an invalid keyword argument for ExcelTaskConfig`
- 旧版集成用例归档跳过（2026-02-09）：`backend/tests/integration/services/test_contract_renewal_service.py` 与 `backend/tests/integration/services/test_ownership_service_complete.py` 增加模块级 skip，标注依赖已退役同步 API，避免阻塞现行业务回归
- 权属/租约服务集成测试异步化修复（2026-02-09）：`backend/tests/integration/services/ownership/test_ownership_service.py` 与 `backend/tests/integration/services/rent_contract/test_rent_contract_service.py` 对齐当前 `*_async` 服务接口，新增 `AsyncSessionAdapter` 适配同步事务夹具，统一异常断言至 `DuplicateResourceError/ResourceNotFoundError/OperationNotAllowedError/ResourceConflictError` 等现行业务异常，修复协程未等待与旧同步调用导致的失败
- 租约服务历史漂移用例重建（2026-02-09）：重写 `backend/tests/integration/services/rent_contract/test_rent_contract_service.py`，移除已退役的同步 `renew_contract/terminate_contract` 依赖与旧中文状态枚举，保留并强化合同创建、资产冲突检测、历史记录写入、条款替换与月度台账生成核心回归路径
- 资产服务集成测试枚举值对齐（2026-02-09）：`backend/tests/integration/services/asset/test_asset_service.py` 将过期枚举测试数据从 `property_nature=商业/usage_status=出租中/空置` 调整为当前有效值 `经营类/出租/闲置`，修复服务层枚举校验升级后触发的大量 `BusinessValidationError`
- 资产/合同工作流集成测试夹具对齐（2026-02-09）：`backend/tests/integration/test_asset_lifecycle.py` 与 `backend/tests/integration/test_contract_workflow.py` 的登录夹具切换为 `test_data["admin"]`（密码 `Admin123!@#`）并注入 `X-CSRF-Token`；同时移除过期 `db` 夹具依赖并放宽生命周期断言以兼容当前接口校验返回，修复集成用例因夹具漂移导致的 setup error
- 产权证 E2E 集成测试兼容性修复（2026-02-09）：`backend/tests/integration/api/test_property_certificate_e2e.py` 的登录辅助函数 `get_auth_headers` 改为提取 `csrf_token` 并回传 `X-CSRF-Token`；`confirm-import` 断言补充 422；资产匹配断言改为兼容“权属方单因子匹配（confidence >= 0.65）”；资产关联用例改为 ORM 建数替代已漂移的 `/api/v1/assets` 请求体，并清理函数内同名 `Asset` 局部导入导致的 `UnboundLocalError`
- 资产关系投影告警单测抗干扰增强（2026-02-09）：`backend/tests/unit/models/test_asset.py` 的关系未预加载告警用例（`rent_contracts` / `ownership`）增加内部告警标记断言（`_warned_unloaded_relationship_*`）并放宽日志条数为“至多一次”，同时修复断言块语法残留，避免全量回归中日志捕获链路波动导致偶发失败
- 权属 CRUD 单测异步化对齐（2026-02-09）：`backend/tests/unit/crud/test_ownership.py` 从同步调用迁移到 `asyncio.run(...)`，并将 `db.execute`/`get_multi_with_count` 相关 mock 升级为 `AsyncMock`，适配 `CRUDOwnership` 全异步实现，修复协程未等待与错误断言
- 加密单测环境隔离修复（2026-02-09）：`backend/tests/unit/core/test_encryption.py` 的自动夹具新增清理 `REQUIRE_ENCRYPTION`，并在 `test_load_missing_key` 与 `missing_key_manager` 夹具中显式固定 `settings.REQUIRE_ENCRYPTION=False`，避免外部环境变量/模块缓存污染“缺失密钥降级”语义
- 用户安全操作单测服务委托对齐（2026-02-09）：`backend/tests/unit/api/v1/test_users.py` 的 `lock/unlock/reset-password` 用例从旧 `UserCRUD/PasswordService` mock 迁移为 `AsyncUserManagementService`（`lock_user` / `unlock_user_with_result` / `admin_reset_password`），并补充 awaited 断言，修复路由重构后触发的 await `_SafeMagicMock` 异常
- 用户 API 单测权限 mock 对齐（2026-02-09）：`backend/tests/unit/api/v1/test_users.py` 将 `RBACService.check_user_permission` 的旧 mock 全量切换为 `is_admin` 异步 mock，适配 `get_user/update_user/change_password` 已迁移的管理员判定逻辑，修复 `_SafeMagicMock can't be used in 'await' expression`
- 产权证上传图片单测异步 mock 对齐（2026-02-09）：`backend/tests/unit/api/v1/test_property_certificate.py` 的 `match_assets` mock 改为 `async` 版本，修复路由层 `await service.match_assets(...)` 触发 `object list can't be used in 'await' expression` 导致的 500
- 项目 API 单测异步 CRUD 误用修复（2026-02-09）：`backend/tests/unit/api/v1/test_project.py` 的测试建数改为直接使用 `Project` ORM + `db_session`（替代同步上下文中误调异步 `project_crud.create/remove`），修复 `coroutine has no attribute code` 与 `CRUDProject.create was never awaited` 失败
- 通知删除单测异步会话兼容修复（2026-02-09）：`backend/tests/unit/conftest.py` 与 `backend/tests/integration/conftest.py` 的 `AsyncSessionAdapter.delete` 改为异步方法，修复 `await db.delete(...)` 在通知删除链路触发 `NoneType can't be used in 'await' expression` 的 500 错误
- 通知 API 单测响应契约对齐（2026-02-09）：`backend/tests/unit/api/v1/test_notifications.py` 将过时的 `count` 断言替换为 `pagination.total`，并移除 `/unread-count` 返回体中不存在的 `count` 断言，修复通知单测与现行响应结构不一致导致的失败
- 通知单测用户夹具兼容修复（2026-02-09）：`backend/tests/unit/api/v1/test_notifications.py` 的 `admin_user_in_db` 补齐 `phone` 与审计字段，修复 `users.phone NOT NULL` 约束下的建数失败，恢复通知 API 单测执行
- 测试会话适配器兼容 `await db.get(...)`（2026-02-09）：为 `backend/tests/unit/conftest.py` 与 `backend/tests/integration/conftest.py` 的 `AsyncSessionAdapter` 补充异步 `get()` 包装，修复服务层使用 `await db.get(...)` 时在单测/集成测中触发 `NoneType can't be used in 'await' expression` 的 500 错误
- 联系人单测加密隔离（2026-02-09）：`backend/tests/unit/api/v1/test_contact.py` 新增自动夹具在该测试模块内禁用 `contact_crud` 字段加密，消除环境中 `DATA_ENCRYPTION_KEY` 导致手机号密文超长写入失败（500）的干扰，确保用例聚焦 API 语义本身
- 联系人单测异步误用修复（2026-02-09）：`backend/tests/unit/api/v1/test_contact.py` 中 `sample_ownership/contact_data` 与删除场景改为直接使用 ORM 模型 + 同步 `db_session` 建数/清理，移除对异步 CRUD（`ownership.create`/`contact_crud.create`）的同步误调用，修复 `coroutine has no attribute id` 与未等待协程告警
- 严格 Cookie-Only 认证收口（2026-02-09）：`backend/src/middleware/auth.py` 的 `get_current_user` 移除 `Authorization: Bearer` 头回退逻辑，仅允许从 `httpOnly` Cookie 读取认证令牌，使 `/api/v1/auth/me` 等受保护接口与 cookie-only 设计一致（修复 `test_bearer_rejected_in_cookie_only_mode` 预期）
- 认证集成测试种子用户兼容修复（2026-02-09）：`backend/tests/integration/conftest.py` 的 `test_data` 夹具为 `admin` 测试用户补充必填 `phone`，并补齐 `created_by/updated_by`，修复 `users.phone NOT NULL` 约束导致的 integration 用例初始化失败
- 集成测试迁移容错增强（2026-02-09）：`backend/tests/integration/conftest.py` 在 `db_tables` 初始化阶段新增对 `DuplicateColumn` 的兼容处理；当 Alembic 升级遇到“已存在列/表”时，优先复用现有表结构并 `stamp head`，若事务回滚导致空库则先 `Base.metadata.create_all()` 再 `stamp head`；同时自动扩容 `alembic_version.version_num` 到 `VARCHAR(128)` 以兼容长 revision id，避免认证相关 integration 用例在建库阶段中断
- 登录权限聚合告警修复（2026-02-09）：`backend/src/api/v1/auth/auth_modules/authentication.py` 登录接口不再将 `PermissionSchema` 直接放入 `set`（避免 `unhashable type: 'PermissionSchema'`），改为基于 `(resource, action)` 的映射去重并保留描述补齐；新增回归测试 `backend/tests/unit/api/v1/test_authentication.py` 覆盖“重复权限去重且无哈希异常”场景
- 初始化脚本兼容 `users.phone` 非空约束（2026-02-09）：`backend/scripts/setup/init_rbac_data.py` 为 `admin/manager1/user1/user2/viewer1` 种子用户补充默认手机号，并补齐 `created_by/updated_by` 写入，修复新库执行 RBAC 初始化时 `users.phone` 为空导致的失败
- 资产软删除过滤收口（2026-02-09）：`backend/src/crud/query_builder.py` 的默认软删除条件由单一 `data_status != "已删除"` 升级为 `data_status IS NULL OR data_status != "已删除"`；`backend/src/crud/asset.py` 为 `get_async`、`get_by_name_async`、`get_multi_by_ids_async` 增加 `include_deleted` 显式开关（默认 `False`），并在 `backend/src/services/asset/asset_service.py` 的恢复/彻底删除路径显式使用 `include_deleted=True`，避免业务恢复链路被默认过滤误伤
- 资产合同投影查询优化（2026-02-09）：`backend/src/crud/asset.py` 的 `rent_contracts` 预加载改为最小字段集 `load_only(...)`，并通过 `with_loader_criteria` 过滤合同软删除记录，降低资产列表派生字段（`tenant_name` / `contract_*` / `monthly_rent` / `deposit`）计算时的关系加载开销
- 资产高频筛选索引补齐（2026-02-09）：新增迁移 `backend/alembic/versions/20260209_add_asset_management_manager_indexes.py`，为 `assets.management_entity` 与 `assets.manager_name` 增加索引；模型 `backend/src/models/asset.py` 对应字段同步标记 `index=True`
- `property_name` 唯一约束风险处理策略（2026-02-09）：本轮保持 `backend/src/models/asset.py` 的 `unique=True` 不变以避免破坏性迁移，已记录为二期评估项（候选方案：按区域/项目维度改复合唯一约束）
- 生产环境 Redis 密码策略强化（2026-02-09）：`backend/src/core/config_database.py` 调整为 `ENVIRONMENT=production` 且 `REDIS_ENABLED=true` 时，未配置 `REDIS_PASSWORD` 将直接配置校验失败；开发环境仍保留告警降级
- 启动期异常分级可观测性增强（2026-02-09）：`backend/src/main.py` 将缓存初始化阶段的异常拆分为模块导入失败、运行时失败与未知异常三类，补充结构化日志字段并明确内存缓存降级路径，降低“泛化异常”带来的排障成本
- 回归测试补齐（2026-02-09）：新增/更新 `backend/tests/unit/crud/test_query_builder.py`、`backend/tests/unit/crud/test_asset.py`、`backend/tests/unit/config/test_settings.py`、`backend/tests/unit/services/asset/test_asset_service.py`，覆盖软删除语义、`include_deleted` 调用约束与 Redis 生产配置校验
- 时间写法文档对齐：更新 `docs/guides/database.md`、`docs/guides/backend.md` 与 `backend/docs/code_quality_extended_guide.md` 的示例代码与说明，将 `datetime.utcnow()` 示例统一为 `datetime.now(UTC).replace(tzinfo=None)`，保持与当前代码实践一致
- 支撑文件与迁移脚本 `utcnow` 收口（TDD）：新增静态红测 `backend/tests/unit/test_datetime_utcnow_support_files.py`，覆盖 `backend/conftest.py`、`backend/tests/fixtures/auth.py`、`backend/scripts/documentation/*.py`、`backend/scripts/setup/init_rbac_data.py` 与三份 Alembic 迁移；上述文件统一移除 `datetime.utcnow()`，改为 `datetime.now(UTC).replace(tzinfo=None)` 并补齐 `UTC` 导入；同步清理测试中真实调用（`test_asset_schema_attachments.py`、`test_response_safety.py`、`test_auto_optimizer.py`、`test_contact_batch_api_async.py`、`test_assets_projection_guard.py`）
- Models/CRUD `utcnow` 收口（TDD）：新增批量静态红测 `backend/tests/unit/test_datetime_utcnow_usage.py`（覆盖 `src/models/*` 与 `src/crud/notification.py` 的剩余热点）；将 `backend/src/models/{asset,asset_history,associations,auth,collection,contact,enum_field,llm_prompt,notification,ownership,project,project_relations,property_certificate,rent_contract,security_event,system_dictionary,task}.py` 以及 `backend/src/crud/notification.py` 的 `datetime.utcnow()` 统一替换为 `datetime.now(UTC).replace(tzinfo=None)` 并补齐 `UTC` 导入
- 服务层 `utcnow` 全量收口（TDD）：新增静态约束测试 `backend/tests/unit/services/excel/test_excel_export_service.py`、`backend/tests/unit/services/document/test_pdf_import_service.py`、`backend/tests/unit/services/llm_prompt/test_auto_optimizer.py`、`backend/tests/unit/services/llm_prompt/test_feedback_service.py`、`backend/tests/unit/services/llm_prompt/test_prompt_manager.py`、`backend/tests/unit/services/permission/test_organization_permission_service.py`；对应服务 `excel_export_service.py`、`pdf_import_service.py`、`auto_optimizer.py`、`feedback_service.py`、`prompt_manager.py`、`organization_permission_service.py` 全部改为统一 `_utcnow_naive()`（`datetime.now(UTC).replace(tzinfo=None)`），并替换原 `datetime.utcnow()` 调用
- Authentication/Audit 时间源收口（TDD）：`backend/tests/unit/services/core/test_authentication_service.py` 与 `backend/tests/unit/services/core/test_audit_service.py` 新增静态约束（禁用 `datetime.utcnow`）；`backend/src/services/core/authentication_service.py`、`backend/src/services/core/audit_service.py` 分别引入 `_utcnow_naive()` 并替换刷新会话访问时间与角色有效期过滤逻辑中的 `datetime.utcnow()`
- PasswordService 时间源收口（TDD）：`backend/tests/unit/services/core/test_password_service.py` 新增静态约束 `test_password_service_module_avoids_datetime_utcnow`（先红后绿），`backend/src/services/core/password_service.py` 引入 `_utcnow_naive()` 并替换密码历史更新时间与过期比较中的 `datetime.utcnow()`，统一为 `datetime.now(UTC).replace(tzinfo=None)`
- 远端同步冲突处置（2026-02-09）：将本地分支 `backup/pre-sync-20260209-081218` 基于 `origin/develop` 进行 rebase，处理冲突文件 `CHANGELOG.md`、`backend/tests/unit/api/v1/test_users.py`、`backend/tests/unit/crud/test_organization.py`；`CHANGELOG.md` 做手工合并，`test_users.py/test_organization.py` 最终采用远端版本以对齐组织与用户字段重构（含 `phone` 必填约束），并完成冲突标记清理
- 冲突处置流程文档升级：更新 `AGENTS.md` 的“Git 冲突处理经验（2026-02 复盘）”，补充“先备份分支解冲突再快进回灌主分支”的实战 SOP，明确双重备份、测试冲突语义判定（远端 schema 演进优先）、最小验证与推送后清理步骤
- 资产模型合同投影性能优化：`backend/src/models/asset.py` 为 `active_contract` 增加实例级缓存（按 `rent_contracts` 集合标识与长度失效）并将“筛选后排序”改为单次线性选择，减少资产列表序列化时 `tenant_name/contract_*` 等派生字段的重复计算开销；新增 `backend/tests/unit/models/test_asset.py` 用例覆盖“跨投影字段复用缓存”和“替换合同集合后缓存失效”行为
- 分析服务轻量查询优化：`backend/src/crud/asset.py` 的 `get_multi_with_search_async` 新增 `include_contract_projection` 可选参数（默认 `True` 保持兼容），并在 `backend/src/services/analytics/*.py` 全面改为 `False`，避免统计/分布/财务等不依赖合同投影的路径预加载 `rent_contracts`；同步更新 `backend/tests/unit/services/analytics/test_analytics.py` 与 `backend/tests/unit/services/analytics/test_financial_service.py` 断言
- 资产导入分层收口：`backend/src/api/v1/assets/asset_import.py` 移除路由层导入编排与冲突处理逻辑，新增 `backend/src/services/asset/import_service.py` 的 `AsyncAssetImportService` 承接“校验/权属映射/重名冲突/导入模式(create|merge|update)”全流程；补充 `backend/tests/unit/api/v1/test_asset_import_layering.py` 固化“路由不直连 CRUD、必须委托服务层”约束
- Custom Fields API 分层收口：`backend/src/api/v1/assets/custom_fields.py` 去除路由层 `custom_field_crud` 直连，`get_custom_fields/get_custom_field/validate_custom_field_value/get_asset_custom_field_values` 全部改为委托 `custom_field_service`，满足“API→Service→CRUD”约束
- Custom Fields 分层回归补强：`backend/src/services/custom_field/service.py` 新增 `get_custom_fields_async/get_custom_field_async/validate_custom_field_value_async` 服务方法；新增 `backend/tests/unit/api/v1/test_custom_fields_layering.py` 固化“路由不直连 CRUD”约束，并同步更新 `backend/tests/unit/api/v1/test_custom_fields.py` 改为断言 Service 调用
- Custom Fields 旧用例兼容修复：`backend/tests/unit/api/v1/test_custom_fields.py` 进一步清理遗留 `custom_field_crud` patch，统一改为 `custom_field_service` 的 async 方法断言（含字段详情/字段值校验/资产字段值查询场景），消除分层重构后的回归阻塞
- 资产历史接口分层修复：`backend/src/api/v1/assets/assets.py` 的 `/{asset_id}/history` 去除路由层 `history_crud` 直连，统一改为委托 `AsyncAssetService.get_asset_history_records`；`backend/src/services/asset/asset_service.py` 新增该服务方法并在 `backend/tests/unit/services/asset/test_asset_service.py` 补充回归用例，匹配 `backend/tests/unit/api/v1/test_assets_history_layering.py` 约束
- 资产权属名称接口分层修复：`backend/src/api/v1/assets/assets.py` 的 `/ownership-entities` 去除路由层 `db.execute(select(Ownership...))` 直连，统一委托 `AsyncAssetService.get_ownership_entity_names`；`backend/src/services/asset/asset_service.py` 新增该服务方法并补充 `backend/tests/unit/services/asset/test_asset_service.py` 用例，同时新增 `backend/tests/unit/api/v1/test_assets_ownership_entities_layering.py` 固化“路由不直连查询、必须委托服务层”约束
- 系统信息接口分层修复：`backend/src/api/v1/system/system_settings.py` 的 `/info` 去除路由层 `await db.execute(text("SELECT 1"))` 直连，改为委托 `SystemSettingsService.check_database_connection`；`backend/src/services/system_settings/service.py` 新增连通性检查方法并补充 `backend/tests/unit/services/test_system_settings_service.py`，同时在 `backend/tests/unit/api/v1/test_system_settings_layering.py` 增加“路由不直接执行数据库检查 SQL + 必须委托服务层”约束
- 用户管理路由事务收口：`backend/tests/unit/api/v1/test_users_layering.py` 增加“路由层不直接 `db.rollback()`”约束（先红后绿）；`backend/src/api/v1/auth/auth_modules/users.py` 移除 lock/unlock/reset-password 三个端点中的路由层回滚语句，保持事务控制收敛在服务层；并回归验证 `backend/tests/unit/api/v1/test_users.py` 与 layering 套件
- Excel 异步任务失败处理分层收口：`backend/src/services/excel/excel_task_service.py` 新增 `mark_task_failed`，统一封装“回滚 + 失败状态写回”，并新增 `_utcnow_naive` 替换服务内 `datetime.utcnow()`；`backend/src/api/v1/documents/excel/import_ops.py` 与 `backend/src/api/v1/documents/excel/export_ops.py` 去除路由层直接 `rollback`/失败状态更新，改为委托 task service，并新增 `_utcnow_naive` 替换路由内 `datetime.utcnow()` 调用；新增 `backend/tests/unit/services/excel/test_excel_task_service.py`，并在 `backend/tests/unit/api/v1/test_excel_import_ops_layering.py`、`backend/tests/unit/api/v1/test_excel_export_ops_layering.py`、`backend/tests/unit/api/v1/test_excel.py` 增加/更新失败路径与时间函数分层断言
- Task 服务时间函数收口：`backend/src/services/task/service.py` 增加 `_utcnow_naive`，替换 `update_task_status`、`update_task`、`cleanup_old_tasks` 中的 `datetime.utcnow()`；新增 `backend/tests/unit/services/task/test_task_service.py` 的静态约束测试，确保任务服务模块不再直接使用 `datetime.utcnow()` 并保持现有任务服务/API 回归通过
- Session 服务时间函数收口：`backend/src/services/core/session_service.py` 的 `_naive_utc_now` 改为 `datetime.now(UTC).replace(tzinfo=None)`，移除 `datetime.utcnow()`；新增 `backend/tests/unit/services/core/test_session_service.py` 静态约束测试，确保会话服务模块不再直接使用 `datetime.utcnow()`，并通过 `session_service` 与 `authentication_service` 回归
- Project/Ownership 服务时间函数收口：`backend/src/services/project/service.py` 与 `backend/src/services/ownership/service.py` 分别新增 `_utcnow_naive`，替换 `toggle_status/update_ownership` 中的 `datetime.utcnow()`；新增 `backend/tests/unit/services/project/test_project_service.py` 与 `backend/tests/unit/services/ownership/test_ownership_service_impl.py` 静态约束测试，确保模块不再直接使用 `datetime.utcnow()` 且对应服务单测全绿
- 租金条款 API 分层收口：`backend/src/api/v1/rent_contracts/terms.py` 去除路由层 `crud.rent_contract` 直连，`get_contract_terms/add_rent_term` 统一改为委托 `rent_contract_service`；`backend/src/services/rent_contract/service.py` 新增 `get_contract_terms_async/add_contract_term_async` 承接条款查询与新增；新增 `backend/tests/unit/api/v1/test_rent_contract_terms_layering.py` 并同步更新 `backend/tests/unit/api/v1/test_rent_contract_api.py` 相关用例，固化“路由不直连 CRUD + 委托服务层”约束
- 资产附件 API 分层收口：`backend/src/api/v1/assets/asset_attachments.py` 去除 `crud.asset` 直接导入，新增 `AssetCRUD` 兼容适配器并在内部委托 `AsyncAssetService.get_asset` 做资产存在性校验（保持旧测试 patch 点兼容）；新增 `backend/tests/unit/api/v1/test_asset_attachments_layering.py` 固化“路由不直连 CRUD + 资产查询委托服务层”约束
- 合同附件 API 分层收口：`backend/src/api/v1/rent_contracts/attachments.py` 去除 `rent_contract`/`rent_contract_attachment_crud` 直连，附件列表/下载/删除统一委托 `rent_contract_service`；`backend/src/services/rent_contract/service.py` 新增 `get_contract_by_id_async/get_contract_attachments_async/get_contract_attachment_async/delete_contract_attachment_async` 承接数据访问；新增 `backend/tests/unit/api/v1/test_rent_contract_attachments_layering.py` 并同步更新 `backend/tests/unit/api/v1/test_rent_contract_api.py` 附件管理用例
- 租金台账 API 分层收口：`backend/src/api/v1/rent_contracts/ledger.py` 去除路由层 `rent_ledger` 直连，`/ledger` 列表、`/ledger/{ledger_id}` 详情与 `/contracts/{contract_id}/ledger` 全部改为委托 `rent_contract_service`；`backend/src/services/rent_contract/ledger_service.py` 新增 `get_rent_ledger_page_async/get_rent_ledger_by_id_async/get_contract_ledger_async`；新增 `backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py` 并同步更新 `backend/tests/unit/api/v1/test_rent_contract_api.py` 相关台账用例
- 租赁合同主路由分层收口：`backend/src/api/v1/rent_contracts/contracts.py` 去除 `asset_crud/ownership/rent_contract` 直连，创建/详情/列表/更新/删除/资产合同查询统一委托 `rent_contract_service`；`backend/src/services/rent_contract/service.py` 新增 `get_assets_by_ids_async/get_ownership_by_id_async/get_contract_with_details_async/get_contract_page_async/delete_contract_by_id_async/get_asset_contracts_async`；新增 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py` 并同步更新 `backend/tests/unit/api/v1/test_rent_contract_api.py` 相关合同用例
- PDF 批处理 API 分层收口：`backend/src/api/v1/documents/pdf_batch_routes.py` 去除路由层 `PDFImportSessionCRUD` 直连，`/status/{batch_id}` 与 `/cancel/{batch_id}` 会话映射查询统一委托 `PDFImportService`；`backend/src/services/document/pdf_import_service.py` 新增 `get_session_map_async`；新增 `backend/tests/unit/api/v1/test_pdf_batch_routes_layering.py` 并同步更新 `backend/tests/unit/api/v1/test_pdf_batch_routes.py` 相关 patch 断言
- 测试基础配置健壮性修复：`backend/tests/conftest.py` 将默认 `DATA_ENCRYPTION_KEY` 改为合法 base64+版本格式并增加格式校验函数，消除测试启动期“无效 base64 密钥”噪声；同时优化 `setup_test_database` 的 unit 调用识别逻辑（支持 `tests/unit` 路径），避免选择性运行 unit 时误触发迁移预热与长连接等待
- 加密单测状态隔离补强：`backend/tests/unit/core/test_encryption.py` 的 `test_load_invalid_base64_key` 增加对 `encryption_module.settings.DATA_ENCRYPTION_KEY` 的显式 patch，避免模块级 settings 缓存导致的跨用例污染
- 测试环境自愈与密钥基线收口：`backend/tests/conftest.py` 的 `hide_env_file` 新增“孤儿 `.env.backup` 自恢复”逻辑，避免中断测试后环境文件长期残留；`reset_encryption_key` 统一每用例恢复有效 `DATA_ENCRYPTION_KEY` 并同步到 settings，降低非目标用例中的加密降级噪声
- PDF 导入主路由噪声收敛：`backend/src/api/v1/documents/pdf_import.py` 改为 `find_spec` 探测可选 `pdf_sessions` 模块，缺失时按 debug 处理而非 warning；初始化日志子模块数量改为动态计算，避免固定“3个子模块”与实际不一致
- 认证路由分层兼容收口：`backend/src/services/core/authentication_service.py` 新增 `UserLookupServiceAdapter`、`backend/src/services/core/audit_service.py` 新增 `AuditLogServiceAdapter`，用于在不直连 CRUD 的前提下兼容历史鉴权调用路径
- 租赁合同遗留同步测试归档：`backend/tests/unit/services/rent_contract/test_rent_contract_service_impl.py` 显式标记为 legacy skip（当前租赁合同实现已拆分为 async lifecycle/ledger/statistics 模块），避免旧同步断言持续阻塞 unit 回归
- 租赁合同旧版服务测试归档：`backend/tests/unit/services/test_rent_contract_service.py` 显式标记为 legacy skip，避免旧同步 CRUD 断言与当前 async 模块化实现冲突
- 租赁服务增强旧测归档：`backend/tests/unit/services/test_rent_service_enhanced.py` 显式标记为 legacy skip，避免旧同步 `query` 断言与 async 服务接口冲突
- 权属方服务单测异步化回归：`backend/tests/unit/services/test_ownership_service.py` 从旧同步断言重写为 async 测试（`pytest.mark.asyncio` + `AsyncMock`），覆盖编码生成、创建/更新、删除约束与状态切换路径，不再依赖 legacy skip
- 产权证日期校验时区修复：`backend/src/services/property_certificate/validator.py` 将 `validate_issue_date` 的“当前时间”比较从 naive `datetime.utcnow()` 改为 aware `datetime.now(UTC)`，修复 offset-aware 输入触发 `TypeError` 的问题
- 文件名安全清洗增强：`backend/src/utils/file_security.py` 在 `secure_filename` 中先统一 `\\`→`/` 再取 basename，并追加残留 `..` 清理循环，修复 `suffix=\"..\\\\..\\\\windows\"` 场景被误判为不安全的问题
- 代码风格收口：对 `backend/src/api/v1/assets/asset_batch.py` 与 `backend/src/services/llm_prompt/auto_optimizer.py` 执行 `ruff format`，确保本轮修复符合后端格式规范
- LLM 反馈收集响应稳定性修复：`backend/src/services/llm_prompt/feedback_service.py` 在创建 `ExtractionFeedback` 时补齐 `created_at` 回退赋值（`datetime.utcnow()`），避免纯 mock/未刷新场景下 `ExtractionFeedbackResponse.model_validate` 因时间戳为空触发校验异常
- Auto Optimizer 缺模板异常语义修复：`backend/src/services/llm_prompt/auto_optimizer.py` 将模板存在性校验前置到错误模式分析前，确保 `optimize_prompt` 在 `template_id` 无效时稳定抛出 `ResourceNotFoundError`（即使反馈不足以生成新规则）；同时修正返回结果中的 `old_version` 为更新前版本号
- Asset Batch API 分层违规修复：`backend/src/api/v1/assets/asset_batch.py` 的 `/batch-custom-fields` 去除路由层 `asset_crud` 直连，统一改为委托 `AsyncAssetService.get_assets_by_ids` 批量查询资产存在性，确保路由层仅负责编排并满足“API→Service→CRUD”分层约束
- Excel 任务状态/历史 API 分层修复：`backend/src/api/v1/documents/excel/status.py` 统一委托 `ExcelStatusService`（`backend/src/services/excel/excel_status_service.py`）处理任务状态与历史查询，修复重构过程中 `page_size` 关键字参数拼写导致的语法错误；`backend/tests/unit/api/v1/test_excel.py` 的状态/历史用例改为注入并断言 Service 调用；新增 `backend/tests/unit/api/v1/test_excel_status_layering.py` 固化“路由不直连 `task_crud`”约束
- Excel 导入/导出任务 API 分层重构：`backend/src/api/v1/documents/excel/import_ops.py` 与 `backend/src/api/v1/documents/excel/export_ops.py` 去除路由层对 `task_crud` 的直接调用，新增 `backend/src/services/excel/excel_task_service.py` 统一封装任务创建/查询/更新并通过依赖注入委托；同步将 `backend/tests/unit/api/v1/test_excel.py` 的 import/export 相关用例改为注入 `task_service` mock 断言服务调用，新增分层测试 `backend/tests/unit/api/v1/test_excel_import_ops_layering.py` 与 `backend/tests/unit/api/v1/test_excel_export_ops_layering.py` 固化“路由不直连 CRUD”约束
- 认证审计/会话 API 分层收口：`backend/src/api/v1/auth/auth_modules/audit.py` 去除对 `AuditLogCRUD` 的直连并改为依赖注入 `AuditService`（`backend/src/services/core/audit_service.py` 新增 `get_login_statistics`）；`backend/src/api/v1/auth/auth_modules/sessions.py` 去除 `UserSessionCRUD` 直连，统一委托 `AsyncSessionService`（新增 `get_session_by_id`，`get_user_sessions` 增加 `active_only` 过滤参数）；新增 `backend/tests/unit/api/v1/test_auth_audit_layering.py` 与 `backend/tests/unit/api/v1/test_auth_sessions_layering.py` 固化分层约束与服务委托行为
- 认证登录/登出/刷新 API 分层收口：`backend/src/api/v1/auth/auth_modules/authentication.py` 去除 `crud.auth` 直接依赖，新增 `UserCRUD` / `AuditLogCRUD` 兼容适配器并在内部委托 `AsyncUserManagementService` 与 `AuditService`，保证路由层不直连 CRUD 且保持既有测试 patch 点不变；新增 `backend/tests/unit/api/v1/test_authentication_layering.py`（先红后绿）固化“禁止 `crud.auth` 直连 + 失败登录路径委托查询/审计”约束
- 用户管理 API 分层收口：`backend/src/api/v1/auth/auth_modules/users.py` 去除 `crud.auth` 直接导入，新增 `UserCRUD` / `AuditLogCRUD` 兼容适配器并统一委托 `AsyncUserManagementService` 与 `AuditService`；`backend/src/services/core/user_management_service.py` 新增 `get_users_with_filters`（含角色/状态/组织筛选）以承接用户列表查询；新增 `backend/tests/unit/api/v1/test_users_layering.py` 固化“禁止直连 `crud.auth` + 列表/停用路径委托适配器”约束
- 认证/用户路由事务下沉：`backend/src/api/v1/auth/auth_modules/users.py` 的锁定/解锁/管理员重置密码路径移除路由层 `db.commit` 与手工字段写入，统一委托 `AsyncUserManagementService.lock_user/unlock_user_with_result/admin_reset_password`；`backend/src/api/v1/auth/auth_modules/authentication.py` 的 `refresh_token` 去除路由层冗余 `db.commit`；`backend/src/services/core/user_management_service.py` 新增对应服务方法并保留 `unlock_user` 兼容语义；同步扩展 `backend/tests/unit/api/v1/test_users_layering.py`、`backend/tests/unit/api/v1/test_authentication_layering.py` 与 `backend/tests/unit/api/v1/test_users.py`
- 项目 API 分层收口：`backend/src/api/v1/assets/project.py` 去除路由层对 `project_crud` 的直接调用，`get_project_statistics`/`get_project` 统一改为委托 `ProjectService`；`backend/src/services/project/service.py` 新增 `get_project_statistics` 与 `get_project_by_id` 服务方法；新增 `backend/tests/unit/api/v1/test_project_layering.py`（先红后绿）固化“路由不直连 CRUD + 必须委托服务”约束
- 资产批量查询 API 分层收口：`backend/src/api/v1/assets/asset_batch.py` 的 `POST /by-ids` 去除路由层 `asset_crud` 直连，改为委托 `AsyncAssetService.get_assets_by_ids`；`backend/src/services/asset/asset_service.py` 新增 `get_assets_by_ids` 服务方法；新增 `backend/tests/unit/api/v1/test_asset_batch_layering.py`（先红后绿）固化分层约束与服务委托行为
- 审计服务单测根因修复：`backend/tests/unit/services/core/test_audit_service.py` 对齐 `AuditService.create_audit_log` 当前“两次查询（用户 + 角色）”实现，补充可复用的成对 `execute` 测试桩，修复循环场景下 `StopAsyncIteration`，并移除不存在的 `mock_admin_user` fixture 依赖，确保审计日志用例稳定回归
- LLM 工厂环境变量优先级修复：`backend/src/services/core/llm_service.py` 调整 GLM/Qwen/DeepSeek/Hunyuan 的 `base_url` 解析顺序，先读取环境变量别名（如 `*_API_BASE`），再回退到 settings，避免 settings 默认值覆盖显式环境变量导致 `create_from_env` 自定义地址失效
- 会话创建状态字段修复：`backend/src/services/core/session_service.py` 在创建 `UserSession` 时显式设置 `is_active=True`，避免依赖 ORM 默认值导致返回对象在提交前后出现 `None`，并恢复 `test_session_service` 对活跃状态的稳定断言
- PDF 导入数据库失败场景单测异步对齐：重写 `backend/tests/unit/services/document/test_database_session_failures.py`，从旧 `query/_get_database_manager` 同步桩迁移到 `async_session_scope + AsyncSession.execute`，修复 `scalars().first()` 协程链路失配并保留事务失败、回滚与异常传播断言
- PDF 分析文件校验顺序修复：`backend/src/services/document/pdf_analyzer.py` 将“文件存在性检查”前置到 PyMuPDF 可用性判断之前，确保在依赖缺失场景下仍对无效路径抛出 `FileNotFoundError`，与 `test_pdf_analyzer` 预期一致
- PDF 分析测试兼容无 PyMuPDF 环境：`backend/tests/unit/services/document/test_pdf_analyzer.py` 为依赖 `fitz.open` 的便捷函数用例补充 `skipif(not PYMUPDF_AVAILABLE)`，避免在 `fitz=None` 场景下 patch 目标不存在导致伪失败
- PDF 导入服务单测异步查询对齐：`backend/tests/unit/services/document/test_pdf_import_service.py` 将 `get_session_status/process_pdf_file/confirm_import/cancel_processing` 用例从旧 `db.query(...).first()` mock 迁移到 `AsyncSession.execute(...).scalars().first()`，消除 `'coroutine' object has no attribute 'first'` 与未 awaited 警告
- PDF 导入日期解析与辅助函数对齐：`backend/src/services/document/pdf_import_service.py` 的 `_parse_date` 扩展支持 `date` 对象与 `DD/MM/YYYY`，`backend/tests/unit/services/document/test_pdf_import_service.py` 同步 `_fill_missing_fields_with_regex` 三元返回结构断言（含 `filled_keys`）
- 处理追踪器单测兼容异步接口：`backend/tests/unit/services/document/test_processing_tracker.py` 引入 `SyncTrackerAdapter`（测试专用）统一桥接 `ProcessingTracker` 的异步方法，查询桩切换到 `execute().scalars().first()`，并将 `TrackerProgressCallback` 与 `save_result` 相关 patch 调整为异步安全调用，修复协程未 awaited 与事件循环冲突
- Excel 导出服务向后兼容补齐：`backend/src/services/excel/excel_export_service.py` 新增同步包装方法 `export_assets_to_excel/export_assets_to_file/get_export_preview`，并在内部统一兼容 `asset_crud.get_multi_with_search`（历史同步）与 `get_multi_with_search_async`（当前异步）两种路径；其中 `export_assets_to_file` 直接复用同步导出链路，避免 legacy patch 场景绕过同步入口
- 分层断言稳定性修复：`backend/tests/unit/api/v1/test_project_layering.py` 与 `backend/tests/unit/api/v1/test_asset_batch_layering.py` 均改为直接读取源文件内容校验（`project_crud.` / `asset_crud.`），避免 `inspect.getsource(module)` 在不同导入加载器下产生不稳定结果导致偶发误报
- Excel 模板服务单测构造对齐：`backend/tests/unit/services/excel/test_excel_template_service.py` 移除对 `ExcelTemplateService` 旧构造签名（传入 `Session`）的依赖，改为无参实例化；初始化断言改为默认构造可用，修复 fixture/setup 阶段 `TypeError/NameError`
- 统计分层约束补强：新增 `backend/tests/unit/api/v1/analytics/statistics_modules/test_area_stats_layering.py` 与 `backend/tests/unit/api/v1/analytics/statistics_modules/test_distribution_layering.py`，覆盖 `area_stats` / `distribution` 路由“禁止直连 CRUD + 必须委托 Service”的分层回归检查
- 基础统计 API 分层重构：`backend/src/api/v1/analytics/statistics_modules/basic_stats.py` 去除路由层对 `asset_crud` 的直接依赖，新增 `backend/src/services/analytics/basic_stats_service.py` 承载基础统计与综合统计计算，并通过 `get_basic_stats_service` 进行依赖注入；新增 `backend/tests/unit/api/v1/analytics/statistics_modules/test_basic_stats_layering.py`（先红后绿）约束“路由不直连 CRUD + 委托 Service”行为，回归 `test_basic_stats.py` 全部通过
- 联系人批量 API 异步单测对齐分层架构：`backend/tests/unit/api/v1/test_contact_batch_api_async.py` 不再 patch 路由层已移除的 `contact_crud`，改为注入并断言 `ContactService.create_contacts_batch` 调用参数，消除 `AttributeError` 并保持对批量载荷映射行为的回归保护
- 资产搜索索引 CRUD 单测去除顺序污染：`backend/tests/unit/crud/test_asset.py` 的 `TestAssetSearchIndexRefresh` 改为 patch `crud._refresh_search_index_entries.__globals__`（而非字符串模块路径），避免 `test_encryption` 重载 `src.crud.asset` 后 patch 命中错误模块导致 `await_count` 偶发失败
- 联系人 CRUD 单测迁移到异步接口：重写 `backend/tests/unit/crud/test_contact.py`，将历史 `get/get_multi/get_primary/create/update/delete/get_multi_by_type` 同步断言全面切换为 `*_async` 路径，使用 `AsyncMock` 驱动 `db.execute/commit/refresh` 并保留“主联系人互斥更新、敏感字段加解密、软删除”核心行为覆盖
- 组织架构 CRUD 单测迁移到异步接口：重写 `backend/tests/unit/crud/test_organization.py`，对齐 `create_async/get_async/update_async/get_multi_with_filters_async/get_multi_with_count_async/get_tree_async` 新签名，修复旧断言中将 `db` 仅按关键字参数校验导致的误报，并保留敏感字段加解密与查询路径覆盖
- Excel 配置 API 单测对齐服务分层：`backend/tests/unit/api/v1/test_excel.py` 的配置 CRUD 相关用例从 patch `excel_task_config_crud` 迁移为注入 `ExcelConfigService` mock（`create_config/get_configs/get_default_config/get_config/update_config/delete_config`），修复 `config_id` 为 `None` 等旧耦合断言失效问题
- RBAC CRUD 单测迁移到异步接口：重写 `backend/tests/unit/crud/test_rbac.py`，将角色/权限/用户角色分配测试统一改为 `await` 当前 `CRUDRole/CRUDPermission/CRUDUserRoleAssignment` 方法（含 `count_by_flags`），并验证 `permission_ids` 过滤、QueryBuilder 路径与聚合统计行为
- 租赁合同 V2 多资产旧同步用例标记废弃：`backend/tests/unit/crud/test_rent_contract_crud.py` 中 `TestContractV2MultiAsset` 改为 `skip`，原因是其依赖已移除的同步接口 `get_multi_with_filters/get_with_details`；当前异步路径覆盖由同文件 `TestContractAsyncRelations` 及现有 API/Service 测试承担
- 租赁合同 QueryBuilder 集成测试异步化：`backend/tests/unit/crud/test_rent_contract_crud.py::TestQueryBuilderIntegration` 改为调用 `get_multi_with_filters_async` 并使用 `AsyncMock` 校验双次 `db.execute`（列表 + 计数）路径，消除对旧 `db.query` 链式 mock 的依赖
- 资产模型测试数据库可达性兜底：`backend/tests/unit/models/test_asset.py` 的 `engine` fixture 新增 PostgreSQL `connect_timeout` 预检与 `OperationalError -> pytest.skip` 降级，并在 fixture 结束时显式 `dispose`，避免测试库不可达时抛出 error 中断整批 unit 回归
- 出租率服务单测全面异步化：重写 `backend/tests/unit/services/analytics/test_occupancy_service.py`，将 `calculate_with_aggregation/_calculate_in_memory/_calculate_category_in_memory` 等用例统一改为 `await`，并将 mock 从旧 `db.query` 链路升级为 `AsyncSession.execute + asset_crud.get_multi_with_search_async`，修复 `coroutine was never awaited` 与下标访问协程错误
- 面积统计路由分层重构：新增 `backend/src/services/analytics/area_stats_service.py` 并将 `backend/src/api/v1/analytics/statistics_modules/area_stats.py` 改为完全委托 `AreaStatsService`（通过依赖注入 `service` 参数），移除路由层对 `asset_crud` 的直接调用，修复分层约束测试
- 分布统计路由分层重构：新增 `backend/src/services/analytics/distribution_service.py` 并将 `backend/src/api/v1/analytics/statistics_modules/distribution.py` 四个端点改为委托 `DistributionService`（依赖注入 `service`），移除路由层直接访问 `asset_crud`，对齐分层约束测试
- Excel 状态路由分层重构：新增 `backend/src/services/excel/excel_status_service.py` 并将 `backend/src/api/v1/documents/excel/status.py` 改为委托 `ExcelStatusService`（依赖注入 `service`），路由层不再直接调用 `task_crud/ensure_task_access/resolve_task_user_filter`
- RBAC 管理员判定入口统一：将后端各层管理员判断从 `check_user_permission(..., "system", "admin")` 收敛为 `RBACService.is_admin()`（覆盖 `backend/src/middleware/auth.py`、`backend/src/security/permissions.py`、`backend/src/api/v1/auth/auth_modules/users.py`、`backend/src/api/v1/auth/roles.py`、`backend/src/api/v1/system/notifications.py`、`backend/src/api/v1/system/system_settings.py`、`backend/src/api/v1/rent_contracts/contracts.py`、`backend/src/services/organization_permission_service.py`、`backend/src/services/task/access.py`、`backend/src/middleware/organization_permission.py`）；保留动态授权能力，并在 `backend/src/services/permission/rbac_service.py` 中为 `system:admin` 兼容路径统一分流到 `is_admin`；同步更新权限相关单测 `backend/tests/unit/services/permission/test_rbac_service.py`、`backend/tests/unit/api/v1/test_roles_permission_grants.py`
- 权限链路回归测试修复：`backend/tests/unit/api/v1/test_users.py` 对齐当前 Service 分层（`create_user/update_user` 改为 mock `AsyncUserManagementService`），补齐用户响应 mock 字段（避免 `UserResponse` 校验中的 `role_name` 魔术 mock 污染），并修复管理员角色汇总断言；`backend/tests/unit/middleware/test_organization_permission.py` 改为正确 `await` 异步依赖并使用 `AsyncMock` 打桩组织权限与安全日志；相关回归集（`test_rbac_service.py`、`test_roles_permission_grants.py`、`test_users.py`、`test_organization_permission.py`）合计 100 用例通过
- API 回归补强：`backend/tests/unit/api/v1/test_rent_contract_api.py` 的删除合同用例改为显式 mock `RBACService.is_admin`，对齐新权限入口并修复 `coroutine has no attribute first` 误报；`backend/tests/unit/conftest.py` 的 `db_tables` 夹具在 Alembic 升级/建表阶段遇到 `OperationalError` 时改为 `pytest.skip`（仅在测试库不可达时生效），避免外部 PostgreSQL 短时不可达导致整批 unit API 用例误报 error
- 资产关系投影“静默丢失”修复：`backend/src/models/asset.py` 新增 `_get_loaded_relationship_value`，当 `ownership`/`rent_contracts` 未预加载且对象处于持久化状态时输出一次性告警（明确提示 `selectinload(Asset.ownership)` / `selectinload(Asset.rent_contracts)`），避免无告警返回 `None`；`backend/src/crud/asset.py` 新增 `_asset_projection_load_options` 并统一应用于 `get_async/get_by_name_async/get_multi_with_search_async/get_multi_by_ids_async`，降低漏配预加载风险；新增 `backend/tests/unit/models/test_asset.py` 的 `TestAssetRelationshipProjectionSafety` 与 `backend/tests/unit/api/v1/test_assets_projection_guard.py` 回归测试，覆盖“瞬态对象不告警 + 未预加载关系告警且仅告警一次 + 资产列表路由序列化合同投影稳定”场景
- API 安全基线收敛：为 `enum_field`、`history`、`occupancy`、`dictionaries`、`monitoring`、`pdf_batch`、`pdf_system`、`backup` 等路由补齐认证/管理员依赖（高风险公开端点显著收敛）；修复 `system/monitoring.py` 中 `@permission_required` 端点缺少 `current_user/db` 注入导致的权限检查失效问题；`excel/preview/advanced` 与统计缓存管理端点补齐认证依赖
- 公开端点继续收敛：`backend/src/api/v1/auth/admin.py` 与 `backend/src/api/v1/system/system.py` 增加路由级认证依赖，`/api/v1/admin/health`、`/api/v1/monitoring/health`、`/api/v1/system/info`、`/api/v1/system/root` 不再匿名访问
- 刷新令牌链路加固：`backend/src/api/v1/auth/auth_modules/authentication.py` 的 `/auth/refresh` 改为仅接受 HttpOnly `refresh_token` Cookie（不再回退请求体 token）；登录时令牌与会话写入统一携带 `device_id`；`backend/src/services/core/authentication_service.py` 为 refresh 校验新增设备指纹比对（含 legacy 指纹兼容）并在不匹配时失活会话；新增/调整单测 `backend/tests/unit/api/v1/test_authentication.py` 与 `backend/tests/unit/services/core/test_authentication_service.py`
- 异常信息脱敏：`backend/src/core/exception_handler.py` 对 `5xx` 响应统一返回通用错误文案与空详情，避免将内部异常文本直接暴露给客户端；`pdf_system` 相关端点同步移除 `str(e)` 直出
- CORS 安全加固：`backend/src/core/config_app.py` 新增 `CORS_ORIGINS` 校验（禁止 `*`），`backend/src/core/config.py` 新增生产环境 CORS 解析函数（正确处理 JSON 列表并拒绝通配符），修复原先生产配置被错误拆分的问题

- 文档补充冲突复盘：在 `AGENTS.md` 与 `CLAUDE.md` 新增 Git 冲突处理经验与标准流程（`UU`/`DU` 分类、`modify/delete` 历史核查、冲突后最小验证、推送前人工核准），用于避免自动偏向合并导致的误恢复/导入漂移/模型重复定义问题
- 手动冲突复核修复：纠正 `fed92f6b` 自动冲突处理引入的问题，恢复 `backend/src/models/asset.py` 为单一 `Asset` 定义并移除重复模型声明，修正 `backend/src/crud/__init__.py`、`backend/src/crud/asset.py`、`backend/src/crud/field_whitelist.py`、`backend/src/models/__init__.py`、`backend/src/services/asset/asset_service.py` 的跨模块导入；修复 `backend/tests/integration/test_postgresql_concurrency.py` 缺失导入；再次删除误恢复的历史文件 `backend/scripts/maintenance/backfill_asset_ownership_entity.py`、`backend/src/services/core/security_service.py`、`frontend/src/types/permission.ts`
- 资产批量更新去循环查库：`backend/src/services/asset/batch_service.py` 将 `batch_update` 中按资产逐条校验 `ownership_id` 与逐条 `get_by_name_async` 查重改为批次前置查询（权属一次校验 + 物业名一次查重 + 内存占位冲突判断），消除批量更新路径 N+1 往返
- Excel 导入结果提取兼容性修复：`backend/src/services/excel/excel_import_service.py` 新增 `_scalars_all`，统一兼容真实 `AsyncSession` 与测试 `AsyncMock` 的 `scalars().all()` 调用链，修复预加载优化后的异步测试回归
- 联系人批量创建事务批处理：`backend/src/crud/contact.py` 新增 `create_many_async`（批量加密 + 单事务提交 + primary 联系人一致性处理），`backend/src/api/v1/system/contact.py` 的批量接口改为一次调用批创建
- 产权证导入 owner 批量化：`backend/src/crud/property_certificate.py` 新增 `PropertyOwner` 的 `create_multi_async`，`backend/src/services/property_certificate/service.py` 的 `confirm_import` 改为批量创建 owner 后统一提交
- 新增/修复回归测试：新增 `backend/tests/unit/api/v1/test_contact_batch_api_async.py` 覆盖联系人批量接口批处理路径；更新 `backend/tests/unit/services/property_certificate/test_service.py` 对齐 `create_multi_async`；`backend/tests/unit/services/excel/test_excel_import_service.py` 与 `backend/tests/unit/services/asset/test_batch_service.py` 回归通过
- 资产出租率数据库排序/筛选能力补齐：新增迁移 `backend/alembic/versions/20260207_add_asset_cached_occupancy_rate.py` 为 `assets` 表添加生成列 `cached_occupancy_rate` 与索引；`AssetCRUD` 支持 `occupancy_rate -> cached_occupancy_rate` 排序/筛选映射并新增 `min_occupancy_rate/max_occupancy_rate` 过滤；列表 API 增加 `sort_by` 兼容参数与出租率范围筛选；前端资产列表统一改为发送 `sort_field`
- 资产列表页单测递归修复：`frontend/src/pages/Assets/__tests__/AssetListPage.test.tsx` 改为直接使用测试工具导出的 `renderWithProviders(<AssetListPage />)`，移除本地同名封装造成的递归调用，修复 `Maximum call stack size exceeded`
- 资产列表筛选类型与白名单修复：`backend/src/services/asset/asset_service.py` 为 `is_litigated` 增加 `true/false/是/否` 归一化并拒绝非法值，`backend/src/crud/asset.py` 在 `_normalize_filters` 增加同类兼容转换；`backend/src/api/v1/assets/assets.py` 同步放宽入参类型；`frontend/src/components/Asset/AssetSearch/AdvancedSearchFields.tsx` 改为提交布尔值；`backend/src/crud/field_whitelist.py` 为 `AssetWhitelist` 放行 `management_entity` 过滤，补齐与 API 暴露参数的一致性

- API 分层收口（最佳实践）：`backend/src/api/v1/system/tasks.py`、`backend/src/api/v1/auth/organization.py`、`backend/src/api/v1/auth/roles.py`、`backend/src/api/v1/assets/ownership.py` 去除对真实 CRUD 的直接依赖，统一改为经由 Service 层
- Enum Field API 分层重构（TDD）：新增 `backend/src/services/enum_field/service.py` 与 `backend/src/services/enum_field/__init__.py`，将 `backend/src/api/v1/system/enum_field.py` 的类型/值/usage/history 全量 CRUD 访问下沉到 Service；路由层改为依赖注入并仅做委托；新增分层测试 `backend/tests/unit/api/v1/test_enum_field_layering.py`（先红后绿）校验“路由模块不直接导入 CRUD + 路由委托 Service”
- Operation Logs API 分层重构（TDD）：新增 `backend/src/services/operation_log/service.py` 与 `backend/src/services/operation_log/__init__.py`，将 `backend/src/api/v1/system/operation_logs.py` 的日志查询/详情/统计/导出/清理逻辑改为 Service 协调，路由层仅保留参数接收与响应组装；新增 `backend/tests/unit/api/v1/test_operation_logs_layering.py`（先红后绿）验证“API 模块不直接导入 CRUD + 路由委托 Service”
- Tasks API 分层重构（TDD）：`backend/src/api/v1/system/tasks.py` 删除 `task_crud/excel_task_config_crud` 路由调用路径，统一通过 `TaskService` 依赖注入与委托（任务列表/详情/更新/取消/删除/历史/统计/运行中/最近、Excel 配置查询与更新、清理）；新增 `backend/tests/unit/api/v1/test_tasks_layering.py`（先红后绿）验证“路由不再使用 CRUD 适配器调用 + 关键端点委托 Service”
- System Dictionaries API 分层重构（TDD）：`backend/src/api/v1/system/system_dictionaries.py` 移除路由层 `system_dictionary_crud` 直连，统一通过 `SystemDictionaryService` 依赖注入委托（列表/详情/创建/更新/删除/批量更新/类型列表）；`backend/src/services/system_dictionary/service.py` 新增读取能力 `get_dictionaries_async/get_dictionary_async/get_types_async` 与 `get_system_dictionary_service`；新增 `backend/tests/unit/api/v1/test_system_dictionaries_layering.py`（先红后绿）验证“路由不直接调用 CRUD + 关键端点委托 Service”
- Contact API 分层重构（TDD）：新增 `backend/src/services/contact/service.py` 与 `backend/src/services/contact/__init__.py`，`backend/src/api/v1/system/contact.py` 删除路由层 `contact_crud` 直连并统一改为 `ContactService` 依赖注入委托（创建/详情/实体列表/主要联系人/更新/删除/批量创建）；新增 `backend/tests/unit/api/v1/test_contact_layering.py`（先红后绿）验证“路由不直接调用 CRUD + 关键端点委托 Service”，并回归通过 `backend/tests/unit/api/v1/test_contact_batch_api_async.py`
- History 分层基线测试补齐（TDD）：新增 `backend/tests/unit/api/v1/test_history_layering.py`，校验 `backend/src/api/v1/system/history.py` 仍保持“路由不直连 CRUD + 关键端点委托 Service”的分层约束（当前实现已满足，测试直接转绿）
- System Settings API 分层重构（TDD）：新增 `backend/src/services/system_settings/service.py` 与 `backend/src/services/system_settings/__init__.py`，`backend/src/api/v1/system/system_settings.py` 去除路由层 `AuditLogCRUD/security_event_crud` 直连，统一改为 `SystemSettingsService` 委托（审计日志写入与安全事件分页查询）；新增 `backend/tests/unit/api/v1/test_system_settings_layering.py`（先红后绿）验证“路由不直接调用 CRUD + 安全事件与审计日志辅助函数委托 Service”
- Task 服务能力补齐：`backend/src/services/task/service.py` 新增任务列表/详情/历史/运行中/最近任务与 Excel 配置查询/更新/删除服务方法，供 API 统一复用
- 任务访问控制优化：`backend/src/services/task/access.py` 增加“任务归属快速判定”短路，仅在跨用户访问时触发 RBAC 检查，减少不必要数据库访问
- 组织/权属/RBAC 服务补齐：`backend/src/services/organization/service.py`、`backend/src/services/ownership/service.py`、`backend/src/services/permission/rbac_service.py` 新增路由所需读取/统计能力，强化 Service 作为业务入口
- 回归测试同步：`backend/tests/unit/api/v1/test_tasks.py` 对齐删除 Excel 配置的正确语义（不存在返回 404）并补齐异步 mock 断言
- Tasks 单测分层对齐（TDD 收口）：`backend/tests/unit/api/v1/test_tasks.py` 全量移除 `task_crud/excel_task_config_crud` 打桩，统一改为 mock `task_service` 并更新路由委托断言（任务列表/详情/更新/取消/删除/历史/运行中/最近及 Excel 配置链路），修复分层重构后红灯并回归通过 51 用例
- Ownership 单测异步化修复：`backend/tests/unit/services/ownership/test_ownership_service_impl.py` 全量从旧同步 `db.query` 断言迁移到 async/await + `AsyncMock`，对齐当前 `OwnershipService` 的 `db.execute` 与 CRUD 异步接口
- Ownership API 单测修复：`backend/tests/unit/api/v1/test_ownership_api.py` 移除失效的直连 CRUD patch，统一改为打桩 `ownership_service` 异步方法并显式 `await` 路由函数，修复 `coroutine was never awaited`/类型断言失败

- 自定义字段排序批量更新去 N+1：`backend/src/services/custom_field/service.py` 将 `update_sort_orders_async` 从循环内逐条 `custom_field_crud.get` 改为单次 `id IN (...)` 批量查询 + 内存映射更新，提交后移除逐条 `refresh`
- 自定义字段值校验链路查询收敛：`backend/src/services/custom_field/service.py` 将 `update_asset_field_values_async` 从逐值 `get/get_by_field_name` 改为按 `field_id/field_name` 双批量预取后内存匹配，避免批量字段更新时线性数据库往返
- 自定义字段 CRUD 批量能力补齐：`backend/src/crud/custom_field.py` 新增 `get_multi_by_ids_async` 与 `get_multi_by_field_names_async`，为服务层批处理提供单次取数入口
- 枚举值批量创建根因修复：`backend/src/crud/enum_field.py` 将 `batch_create_async` 从“循环调用 `create_async`（每条 commit）”重构为“父级一次预取 + 单事务批量创建 + 批量历史写入”
- 字典快速创建批量化：`backend/src/services/common_dictionary_service.py` 的 `quick_create_enum_dictionary_async` 改为调用 `enum_value_crud.batch_create_async`，移除逐选项 `create_async` 往返
- API 同类问题修复：`backend/src/api/v1/assets/asset_batch.py` 的 `/batch-custom-fields` 资产存在性校验改为一次批量查询；`backend/src/api/v1/rent_contract/contracts.py` 的创建合同资产校验改为一次 `get_multi_by_ids_async` 批量校验
- 单测回归补齐：更新 `backend/tests/unit/services/custom_field/test_custom_field_service.py`、`backend/tests/unit/services/test_common_dictionary_service.py`、`backend/tests/unit/crud/test_enum_field.py`、`backend/tests/unit/api/v1/test_rent_contract_api.py` 对齐批量路径并新增事务级断言

- 资产批量服务去兼容回退：`backend/src/services/asset/batch_service.py` 删除 `_load_assets_map` 的逐条 `asset_crud.get_async` fallback 与 mock 特判逻辑，统一走单次 `SELECT ... WHERE id IN (...)` 预取路径
- 批量删除链路一致化：`batch_service.py` 的关联检查保持纯批量查询结果处理，不再依赖测试环境对象类型分支；同时删除 `to_dict` 的“向后兼容”语义描述
- 资产时间戳弃用告警修复：`backend/src/models/asset.py`、`backend/src/services/asset/asset_service.py`、`backend/src/services/asset/batch_service.py` 新增 `_utcnow_naive()`，替换关键写路径中的 `datetime.utcnow()`，避免 Python 3.12 弃用警告
- 批量服务测试重构：`backend/tests/unit/services/asset/test_batch_service.py` 全量改为 mock `db.execute -> _FakeResult(...)` 驱动，验证“批量预取 + 批量关联检查”最终实现，不再依赖旧的逐条 `get_async` 兼容行为
- 回归验证补齐：针对资产链路执行了模型/CRUD/Service/API 与 project/system_dictionary 相关 unit+integration 目标测试（未跑全量），确认重构后核心路径通过

- 资产批量服务测试根因修复：`backend/tests/unit/services/asset/test_batch_service.py` 统一将 `history_crud.create_async` 与 `enum_validation_service.validate_value` 改为 `AsyncMock`，消除对异步接口的同步 mock 导致的 `object _SafeMagicMock can't be used in 'await' expression`
- 资产批量校验测试对齐：`test_batch_service.py` 日期字段断言由已删除的 `contract_start_date` 更新为当前模型字段 `operation_agreement_start_date`
- 权属财务统计测试异步化：`backend/tests/unit/services/asset/test_ownership_financial_service.py` 从旧 `db.query().scalar()` 测试桩改为 `AsyncSession.execute().scalar()` 路径，并对 `get_financial_summary` 全量使用 `await`
- 资产删除集成测试前置修复：`backend/tests/integration/services/asset/test_asset_service.py` 在 `TestAssetDeletion` 中补齐默认权属方初始化，修复创建资产时 `ownership_id` 外键校验失败
- 资产测试收集冲突根因修复：新增 `backend/tests/__init__.py`、`backend/tests/unit/**/__init__.py` 与 `backend/tests/integration/services/**/__init__.py`，解决 unit/integration 同名 `test_asset_service.py` 同批执行时的 `import file mismatch`
- 项目集成测试异步化修复：`backend/tests/integration/services/project/test_project_service.py` 全量改为 `AsyncSession + await` 调用 `ProjectService`/`project_crud`，移除对异步服务的同步调用假设
- 项目测试夹具根因修复：`backend/tests/integration/services/project/conftest.py` 从同步 `Session` 切换为异步 `AsyncSession` 事务夹具，彻底消除 `ChunkedIteratorResult can't be used in 'await' expression` 失败
- 资产按ID批量查询解密修复：`backend/src/crud/asset.py` 的 `get_multi_by_ids_async` 改为先取列表后逐条调用 `_decrypt_asset_object`，修复 `/assets/by-ids` 场景可能返回密文字段的问题
- 资产更新 Schema 字段回补：`backend/src/schemas/asset.py` 在 `AssetUpdate` 补回 `management_entity`，避免更新接口无法修改经营管理单位的回归
- 批量校验边界修复：`backend/src/services/asset/validators.py` 将面积一致性判断从 truthy 判定改为显式 `is not None`，修复 `rentable_area=0` 时未正确拦截 `rented_area>0` 的漏洞
- 新增回归测试：`backend/tests/unit/crud/test_asset.py` 增加 `get_multi_by_ids_async` 解密断言；`backend/tests/unit/services/asset/test_validators.py` 增加零值面积边界用例；`backend/tests/unit/schemas/test_asset_schema_attachments.py` 增加 `AssetUpdate.management_entity` 字段覆盖
- 资产关系懒加载稳定性修复：`backend/src/models/asset.py` 的 `ownership_entity` 改为通过 `inspect(...).attrs.ownership.loaded_value` 判定，避免未预加载关系时触发隐式懒加载导致 `MissingGreenlet`
- 资产列表查询预加载补齐：`backend/src/crud/asset.py` 在 `get_multi_with_search_async` 与 `get_multi_by_ids_async` 的 `include_relations=False` 路径补充 `joinedload(Asset.ownership)`，确保轻量列表序列化时 `ownership_entity` 可稳定输出
- 新增集成回归：`backend/tests/integration/services/asset/test_asset_service.py` 增加 `test_get_assets_without_relations_serializes_ownership_entity`，覆盖“非关联查询 + 序列化 ownership_entity”场景

- 资产搜索索引刷新性能根因修复：`backend/src/crud/asset.py` 将逐字段循环 `DELETE/INSERT` 重构为集合化批量刷新（一次 `DELETE ... field_name IN (...)` + 一次批量 `INSERT`），消除字段数增长导致的线性数据库往返放大
- 新增回归单测：`backend/tests/unit/crud/test_asset.py` 增加 `_refresh_search_index_entries` 批量删插、无索引字段跳过、空索引结果仅删除三类断言，防止回退到逐字段 DML
- 权属方下拉统计性能修复：`backend/src/services/ownership/service.py` 将 `get_ownership_dropdown_options` 的逐权属方计数查询（`2N+1`）重构为两条聚合查询（资产/项目各一次 `GROUP BY`），避免列表规模增长时数据库往返线性放大
- 新增回归单测：`backend/tests/unit/services/ownership/test_ownership_dropdown_options.py` 覆盖批量计数映射与空列表提前返回，确保不回退为逐条计数查询
- 组织统计查询性能修复：`backend/src/services/organization/service.py` 将层级统计从“先查层级再按层级逐条 count”重构为单次 `GROUP BY level` 聚合，消除层级数增长时的额外往返
- 任务统计查询性能修复：`backend/src/crud/task.py` 将按 `TaskStatus/TaskType` 逐项计数改为两条聚合查询（`GROUP BY status`、`GROUP BY task_type`），避免枚举扩展导致统计查询线性增长
- 单测同步更新：`backend/tests/unit/services/organization/test_organization_service_impl.py` 与 `backend/tests/unit/crud/test_task.py` 调整为聚合查询结果断言，覆盖新统计路径
- 枚举初始化查询性能修复：`backend/src/services/enum_data_init.py` 将“按枚举类型/枚举值逐条查询存在性”改为“预取类型 + 预取值后内存匹配”，显著减少初始化阶段数据库往返
- 枚举初始化单测更新：`backend/tests/unit/services/test_enum_data_init.py` 同步对齐批量预取路径，并补强 `flush` 回填 `id` 的测试桩行为
- 通知调度判重查询性能修复：`backend/src/services/notification/scheduler.py` 将 `check_contract_expiry/check_payment_overdue/check_payment_due_soon` 中循环内的“活跃用户查询 + 单条判重查询”改为“单次活跃用户查询 + 批量判重后内存去重”，显著降低用户数与合同/台账数增长时的数据库往返
- 通知服务批量判重能力新增：`backend/src/services/notification/notification_service.py` 新增 `find_existing_notification_pairs_async`，一次查询返回 `(recipient_id, related_entity_id)` 集合，供调度任务复用
- 合同月台账生成 N+1 修复：`backend/src/services/rent_contract/ledger_service.py` 将按月份逐条 `get_by_contract_and_month_async` 查重改为一次批量查询已存在月份并在内存过滤；同时移除逐条 `refresh`，避免提交后额外 N 次查询
- 租金台账 CRUD 批量查询补充：`backend/src/crud/rent_contract.py` 新增 `get_existing_year_months_async`，用于按合同 + 月份集合批量取重
- 新增/更新回归单测：`backend/tests/unit/services/notification/test_scheduler.py` 对齐批量判重接口；新增 `backend/tests/unit/services/rent_contract/test_ledger_service.py` 覆盖“批量月份查重生效、逐月查重不再调用、无逐条 refresh”场景
- 资产导入权属校验查询优化：`backend/src/api/v1/assets/asset_import.py` 对导入批次中的 `ownership_id/ownership_entity` 先做批量预取，再按行复用映射判定；仅在映射缺失时回退单次查询并写回缓存，避免每行重复查权属方
- Excel 资产导入查询优化：`backend/src/services/excel/excel_import_service.py` 新增批量预取权属映射（ID/名称）并复用到逐行导入流程；对映射缺口使用惰性回填缓存，降低大批量导入时的重复查询
- 合同 Excel 导入查重优化：`backend/src/services/document/rent_contract_excel.py` 将“按行查询合同号是否存在”改为“导入前一次性批量查询 + 内存映射复用”，并在创建/更新后实时更新映射，避免循环内重复 `SELECT`
- Excel 导入单测异步化与接口对齐：`backend/tests/unit/services/excel/test_excel_import_service.py` 批量替换为 `AsyncMock` 并对齐 `asset_crud.*_async`，补齐权属预取查询测试桩与字段映射数量断言（22），确保优化路径可回归
- 系统字典排序批量更新去 N+1：`backend/src/services/system_dictionary/service.py` 将 `update_sort_orders_async` 从逐条 `system_dictionary_crud.get` 改为一次 `id IN (...) + dict_type` 批量查询并内存映射更新，提交后不再逐条 `refresh`
- 系统字典 CRUD 批量查询能力新增：`backend/src/crud/system_dictionary.py` 新增 `get_multi_by_ids_and_type_async`，为批量排序等场景提供单次取数入口
- 枚举类型列表值加载去 N+1：`backend/src/crud/enum_field.py` 为 `EnumFieldTypeCRUD.get_multi_async` 增加批量值加载路径（单次查询所有类型的枚举值），替代原“按类型逐条查值”
- 枚举树构建查询收敛：`backend/src/crud/enum_field.py` 将 `EnumFieldValueCRUD.get_tree_async` 从递归按父节点多次查询改为“单次取全量 + 内存组树”，消除层级增长导致的查询放大
- 资产批量操作查询收敛：`backend/src/services/asset/batch_service.py` 新增资产批量预取与关联批量检查（合同/产权证/台账），`batch_update`/`batch_delete` 不再按资产逐条 `get` 与逐条关联探测
- 新增回归单测：`backend/tests/unit/crud/test_enum_field.py` 增加“批量加载枚举值仅两次查询”与“枚举树单次查询”断言；`backend/tests/unit/services/asset/test_batch_service.py` 增加“批量预取资产”与“批量关联检查”断言；系统字典服务测试改为对齐批量查询接口

- 资产历史 CRUD 根因修复：`backend/src/crud/history.py` 移除同步兼容接口 `get/get_by_asset_id/get_multi/create/remove`，仅保留异步接口，统一历史访问路径
- 资产历史字段收敛：删除 `operator_id -> operator` 兼容桥接，历史创建仅接受标准字段 `operator`
- 历史单测异步化：`backend/tests/unit/crud/test_history.py` 全量改为 `get_async/get_by_asset_id_async/get_multi_with_count_async/create_async/remove_async/remove_by_asset_id_async`，并新增“拒绝 legacy operator_id”断言

- 资产批量删除前后端契约修复：前端 `ASSET_API.BATCH_DELETE` 从错误的 `/assets/batch` 修正为 `/assets/batch-delete`
- 资产批量删除请求体修复：`AssetCoreService.deleteAssets` 改为发送 `{ asset_ids }`，与后端 `POST /api/v1/assets/batch-delete` 入参一致
- 资产 API 路径常量收敛：`backend/src/constants/api_paths.py` 移除过时 `AssetPaths.BATCH`，改为 `BATCH_UPDATE/BATCH_DELETE/BATCH_CUSTOM_FIELDS`
- 前端单测对齐：`assetService.test.ts` 同步更新批量删除断言并补充 `invalidateCacheByPrefix` mock，覆盖正确端点与请求体
- 资产自定义字段契约修复：`assetFieldService` 路径与载荷统一对齐后端真实接口（`/asset-custom-fields/assets/{asset_id}/values`、`/asset-custom-fields/assets/batch-values`、`/asset-custom-fields/validate`、`/asset-custom-fields/types/list`）
- 新增前端单测：`frontend/src/services/__tests__/assetFieldService.test.ts` 覆盖自定义字段值查询/更新、批量设置、校验与字段类型选项映射
- Project 服务测试根因修复：`backend/tests/unit/services/project/test_project_service_complete.py` 与 `backend/tests/unit/services/test_project.py` 全量改为 async/await 调用，去除对异步服务的同步调用假设
- 自定义字段 API 测试根因修复：`backend/tests/unit/api/v1/test_custom_fields.py` 重写为异步路由测试，全面对齐 `*_async` CRUD/Service 方法与布尔筛选参数
- 安全中间件导入链路复核：确认 `security_middleware` 使用 `src.core.ip_whitelist` 真实实现，`import src.api.v1` 恢复可用
- 清理临时测试桩：移除 `test_custom_fields.py` 中对 `src.core.ip_whitelist` 的自动 monkeypatch，避免掩盖真实依赖问题

- RBAC 最终态重构：`RBACService.check_permission` 统一为“静态角色权限 + 统一授权记录（PermissionGrant）”判定链路，并加入 `DENY > ALLOW` 决策规则
- 新增统一授权模型：`backend/src/models/rbac.py` 添加 `PermissionGrant`，并同步补齐 schema、CRUD、白名单注册
- 新增迁移：`backend/alembic/versions/20260207_unify_permission_grants.py` 创建 `permission_grants` 表，并回填 legacy `resource/dynamic/temporary/conditional/delegation` 授权数据
- 权限检查入口收敛：业务代码移除直接 `rbac_service.is_admin(...)` 旁路，统一改为 `check_user_permission(user_id, "system", "admin")`
- 管理员判定去角色名兜底：管理员仅由 `system:admin` 权限决定，移除 `admin/super_admin` 名称自动提权，不再保留旧别名判定入口
- 初始化脚本对齐：`backend/scripts/setup/init_rbac_data.py` 改为生成 `PermissionGrant` 示例授权，不再写入旧动态/临时权限模型
- 新增测试：`backend/tests/unit/services/permission/test_rbac_service_grants.py` 覆盖统一授权 allow/deny/scope+condition 场景，`test_rbac_service.py` 同步调整管理员策略断言
- RBAC 管理接口补齐：`backend/src/api/v1/auth/roles.py` 新增统一权限检查、统一授权记录（`/permission-grants`）增删改查、用户角色分配与用户权限汇总端点
- RBACService 管理能力补齐：新增统一授权记录查询/更新方法，并在创建/撤销授权时增加输入校验与权限审计日志
- 新增路由测试：`backend/tests/unit/api/v1/test_roles_permission_grants.py` 覆盖统一授权 API 核心路径
- RBAC 旧轨删除：移除 `models/crud/schemas` 中 `dynamic_permission*` 旧模型与导出，删除 `RBACService` 中兼容别名方法，仅保留统一授权主链路
- 迁移加载对齐：`backend/alembic/env.py` 移除对已删除 `dynamic_permission` 模块的导入，修复测试/迁移初始化失败
- 新增迁移：`backend/alembic/versions/20260207_drop_legacy_dynamic_permission_tables.py`，升级时删除 `dynamic/temporary/conditional/request/delegation/template/audit` 旧权限表
- 非 RBAC 兼容层清理：移除 `main.py` 中 V1 兼容中间件导入与启动链路；`core/environment.py` 删除 `middleware.v1_compatibility` 可选依赖登记
- PDF 路由统一收口：`api/v1/__init__.py` 删除旧路径 `/api/v1/documents/pdf-import/*`，仅保留 `/api/v1/pdf-import/*`
- PDF V1 兼容端点删除：`pdf_upload.py` 删除 `POST /upload-and-extract` 与 `_validate_extracted_fields_v1`；`pdf_import.py` 删除可选 `pdf_v1_compatibility` 模块挂载
- Schema/测试同步收敛：`schemas/pdf_import.py` 删除 V1 `ExtractionRequest/ExtractionResponse`；`test_pdf_upload.py` 删除 V1 相关测试并保留 `/upload` 主链路测试，`test_pdf_import_routes.py` 全部路径断言对齐新入口
- 配置包去兼容化：`backend/src/config/__init__.py` 移除 `src.config -> src.core.config` 兼容 shim，仅保留配置子模块包说明
- 权限模块尾巴清理：`security_middleware` 的白名单依赖统一改为 `src.core.ip_whitelist`，删除 `src.security.ip_whitelist` 后不再保留旧导入路径
- API 包懒加载修复：`backend/src/api/__init__.py` 新增 `v1` 属性的显式懒加载（`importlib.import_module`），确保 `patch("src.api.v1...")` 在单测中可稳定解析
- 兼容别名彻底删除：移除 `enum_data_init.add_legacy_enum_values`、`message_constants.get_error_id`、`RateLimiter.requests` 与 `GLMVisionAdapter/QwenVisionAdapter/DeepSeekVisionAdapter` 旧别名
- 服务导出统一：`backend/src/services/__init__.py` 与 `backend/src/services/core/__init__.py` 删除 fallback/stub 链路，仅保留最终模块导出
- 单测同步收敛：`tests/unit/core/test_rate_limiter.py` 改为断言 `request_times`，与 `RateLimiter` 去兼容后的最终实现一致
- 校验脚本对齐：`scripts/verify_fixes.py` 改为验证 `SecurityService` 已删除，并移除 Windows 控制台 emoji 编码风险、硬编码路径依赖与无效 `DATA_ENCRYPTION_KEY` 注入
- 前端权限旧类型清理：删除未被引用的 `frontend/src/types/permission.ts`（动态/临时/申请/委托等旧轨类型）
- 类型检查配置收敛：`backend/pyproject.toml` 移除已删除模块 `src.models.dynamic_permission` 的 mypy override
- RBAC 初始化脚本收敛：`backend/scripts/setup/init_rbac_data.py` 将 `dynamic_permission:*` 基础权限资源统一替换为 `permission_grant:*`
- 数据库设计文档收敛：`docs/database-design.md` 移除动态/临时/条件/申请/委托旧权限分表说明，统一为 `permission_grants` 单表模型
- 集成文档路径收敛：`docs/integrations/siliconflow-paddleocr-integration.md` 将 PDF 导入示例接口更新为 `/api/v1/pdf-import/*`
- 迁移脚本清理：删除 `backend/scripts/maintenance/backfill_asset_ownership_entity.py`（历史 `ownership_entity -> ownership_id` 回填工具）
- PRD 收敛：`docs/features/prd-asset-management.md` 删除“历史数据迁移策略”章节，保持新项目最终态描述
- 变更日志收敛：移除 `CHANGELOG.md` 中已与最终态冲突的中间兼容条目（如 PDF 旧兼容路由、`dynamic_permission_crud`、历史回填脚本记录）

- Asset 搜索总数修复：`QueryBuilder.build_count_query` 支持 `base_query + distinct_column`，`AssetCRUD` 计数查询显式复用 `Ownership` 关联，消除搜索场景下笛卡尔积导致的 `total` 放大
- 合同状态投影修复：`Asset.active_contract` 纳入 `EXPIRING/即将到期` 状态，确保 `tenant_name`、`monthly_rent`、`deposit` 等计算属性在即将到期合同下可正常回显
- 资产表单合同展示修复：移除 `selected_contract_id` 的误导性“可选合同”交互与相关加载逻辑，改为只读展示后端投影合同信息，避免“可选但不持久化”的伪交互
- 并发集成测试稳定性修复：`test_concurrent_transaction_isolation` 改为动态唯一资产名并使用现有资产字段（`notes`）验证并发更新，支持重复执行
- 资产加密集成测试重构：`tests/integration/crud/test_asset_encryption.py` 全量迁移为异步 CRUD 调用，断言字段对齐当前模型（`address`、`manager_name`），修复旧同步调用导致的协程未等待失败
- Makefile 跨平台 Python 解释器选择修复：Windows 优先使用 `backend/.venv/Scripts/python.exe`（Linux/macOS 继续使用 `backend/.venv/bin/python`），避免 `make dev` 回退到系统 Python 导致缺少 `pydantic_settings` 启动失败
- Asset God Class 拆分：`backend/src/models/asset.py` 仅保留 `Asset`，新增 `asset_history.py`、`project.py`、`project_relations.py`、`system_dictionary.py` 拆分历史/项目/字典模型职责
- 模型依赖彻底去耦：移除 `asset.py` 对 `Project/SystemDictionary/AssetCustomField/AssetHistory` 的兼容回导与扩展 `__all__`，强制按新模块路径导入
- 测试导入路径统一：更新 unit/integration 中残留的 `from src.models.asset import Project/SystemDictionary/AssetCustomField/AssetHistory` 到对应新模型模块，消除对 God Class 兼容层依赖
- 关联表解耦：新增 `backend/src/models/associations.py` 统一维护 `rent_contract_assets` 与 `property_cert_assets`，并更新相关模型/CRUD/服务引用
- 资产自定义字段筛选修复：`/api/v1/assets/custom-fields` 中 `is_required/is_active` 过滤改为布尔值传递，修复字符串比较导致的筛选失效
- 资产自定义字段类型接口修复：路由从错误的 `"/types/list[Any]"` 更正为 `"/types/list"`，恢复前后端契约一致性
- 资产合同快照去冗余：移除 `Asset` 中 `tenant_name/lease_contract_number/contract_start_date/contract_end_date/monthly_rent/deposit` 持久化字段，改为基于 `active_contract` 的只读计算属性
- 资产入参与校验收敛：`Asset` schema、批量校验器、字段白名单同步移除上述冗余字段输入与排序能力；`RentContract` 排序字段改为 `monthly_rent_base/total_deposit`
- 新增迁移：`backend/alembic/versions/20260207_remove_asset_contract_snapshot_fields.py`（rev: `4f9b3b2d6e91`）删除 `assets` 表合同快照列
- 迁移兼容加固：`20260204_add_unique_asset_property_name` 迁移新增 `alembic_version.version_num` 容量检查与扩容（32→64），避免长 revision id 在升级链上触发截断错误
- 迁移幂等加固：`20260206_add_asset_search_index` 在表/索引已存在时跳过创建，避免重复执行迁移时触发 `DuplicateTable`/`DuplicateIndex`
- 前端表单对齐：`AssetForm`、`assetFormSchema`、`useFormFieldVisibility`、`AssetCreateRequest` 移除冗余合同快照字段提交流程与可见性规则
- 单测对齐：更新 `tests/unit/models/test_asset.py`、`tests/unit/crud/test_field_whitelist.py`、`tests/unit/services/asset/test_validators.py` 适配新模型与校验规则
- 异步测试对齐：重写 `tests/unit/crud/test_asset.py`、`tests/unit/crud/test_project.py`、`tests/unit/services/project/test_project_service.py` 为 async/await，移除旧同步 mock 触发的 `coroutine was never awaited`
- 资产服务收敛：`AssetService` 删除历史字段剔除兼容逻辑，创建/更新直接走标准 schema 结果，不再在服务层接收旧字段

- 资产字段统一替换：后端 `AssetCreate/AssetUpdate` 移除历史字段别名兼容，并在请求中显式拒绝 `wuyang_project_name`/`description`（统一使用 `project_name`/`notes`）
- 资产前端字段对齐：`AssetSearchResult` 仅使用 `project_name/notes`，移除旧字段回退逻辑
- 资产导出字段对齐：导出配置改为使用 `project_name`，移除无效导出键 `description`
- 旧可见性规则修复：`useFormFieldVisibility` 将协议字段依赖从 `wuyang_project_name` 调整为 `project_name`
- 权限判定改进：`RBACService` 管理员判定优先基于 RBAC 权限 `system:admin`，仅保留 `admin/super_admin` 名称作为兼容回退
- 权限上下文对齐：`AuthorizationContext.is_admin()` 改为调用 `rbac_service.is_admin()`，移除局部硬编码角色判断
- 启动前配置保护：`backend/run_dev.py` 新增 `DATA_ENCRYPTION_KEY` 的 Base64 格式与解码长度校验（至少 32 bytes），提前暴露配置错误
- 新增/更新测试：
  - 后端 schema 测试覆盖历史字段拒绝（提示改用 `project_name`/`notes`）
  - 后端 RBAC 测试覆盖基于 `system:admin` 的管理员识别
  - 前端 `usePermission` 测试覆盖“仅角色名不应自动视为管理员”
- 回归修复：`TableWithPagination` 恢复透传调用方 `scroll` 配置，仅在未传入时默认 `{ x: 'max-content' }`
- 回归修复：`Pagination` 移除重复触发的 `onShowSizeChange` 绑定，避免分页大小变更导致重复请求
- 回归修复：`Layout.module.css` 将未定义的 `--color-bg-container` 替换为已定义的 `--color-bg-primary`
- 可访问性修复：`AssetBasicInfoSection` 移除输入框错误的 `aria-label={labelId}`，避免屏幕阅读器朗读内部 ID
- 类型修复：`assetFormSchema` 与 `Asset` 类型移除 `wuyang_project_name/description` 历史字段，仅保留 `project_name/notes`
- 资产表单字段彻底统一：`assetFormSchema` 与 `useFormFieldVisibility` 全量替换为 `lease_contract_number / contract_start_date / contract_end_date`，移除 `lease_contract / current_contract_* / current_lease_contract / agreement_* / current_terminal_contract`
- 资产历史展示字段统一：`AssetHistory` 仅使用 `operator / operation_time / change_reason`，移除 `changed_by / changed_at / reason` 回退路径
- 通知未读数接口统一：前端 `notificationService` 改为读取 `unread_count`（不再读取 `count` 兼容字段），并同步更新单测
- 产权证审核字段统一：前端 `PropertyCertificate` 类型与页面全部改为 `is_verified`，移除 `verified` 历史字段兼容
- 产权证导入确认字段统一：前端确认载荷改为 `asset_ids + should_create_new_asset`，移除 `create_new_asset` 历史字段
- 分析服务响应兼容清理：`analyticsService` 移除 `apiData.data?.*` 多形态回退，仅保留当前响应结构与字段适配
- 导出任务字段统一：前端导出任务类型改为 snake_case（`download_url / created_at / completed_at / error_message`），移除 camelCase 兼容路径
- 资产历史字段兼容清理：后端 `history_crud.create_async` 移除 `operator_id -> operator` 桥接，仅接受标准字段 `operator`
- 资产 schema 进一步收敛：删除 `AssetCreate/AssetUpdate/AssetResponse` 中的 `wuyang_project_name` 与 `description` 历史字段定义，仅保留“显式拒绝旧字段并提示替换”的校验逻辑
- 资产创建链路防回归：`asset_crud.create_async/create_with_history_async/update_async/update_with_history_async` 增加旧字段兜底剔除，避免绕过 schema 时触发 `Asset(**kwargs)` 的无效参数异常
- OpenAPI 对齐：`AssetCreate`/`AssetResponse` 文档不再暴露历史字段，修复“文档允许但运行时拒绝”的契约不一致
- 产权证导入确认字段统一：后端 `PropertyCertificateService.confirm_import` 改为仅处理 `extracted_data / asset_ids / should_create_new_asset`，并将资产关联透传到 CRUD 创建流程

### 🔐 安全 (Security)

#### Fixed / 修复

- 将 `backend/config/backend.env.secure` 从 Git 索引移除，阻止继续版本化跟踪明文敏感配置
- 删除工作区中的 `backend/config/backend.env.secure` 明文敏感文件，避免本地误用或二次泄露
- `.gitignore` 增加 `backend/config/*.env.secure` / `backend/config/*.secure` 规则，防止安全配置文件再次被提交
- 清理 `backend/config/` 目录中的遗留跟踪文件（`config.example.yaml`、`quality_monitor_config.yaml`、`pytest_ultra_optimized.ini`、`create_admin_user.sql`）
- `.gitignore` 增加 `backend/config/create_admin_user.sql` 与 `config/create_admin_user.sql` 规则，阻止管理员凭据 SQL 产物被提交
- `README.md` 与 `docs/guides/deployment.md` 增加“密钥一旦出现在 Git 历史即必须视为泄露并强制轮换”的应急处置指引
- 调整 `backend/scripts/devtools/security_key_generator.py`：敏感产物默认输出到系统临时目录 `zcgl-security-artifacts`（可用 `SECURITY_ARTIFACTS_DIR` 覆盖），不再默认写入仓库目录

### 🎨 UI/UX 改进 (UI/UX Improvements)

#### Added / 新增

- **可访问性增强** (Accessibility Enhancement)
  - 为 AssetList 组件的所有操作按钮添加 ARIA 标签（aria-label, title）
  - 为表单字段添加可访问性属性（aria-required, aria-describedby, aria-label）
  - 为 Modal 添加焦点管理（autoFocus, focusTriggerAfterClose）
  - 为错误提示添加 role="alert" 和 aria-invalid 属性
  - 影响 24+ 个交互元素，符合 WCAG 2.1 AA 级标准

- **响应式设计优化** (Responsive Design)
  - 实现表格响应式设计，移动端（< 768px）自动隐藏次要列
  - 动态调整表格滚动配置和尺寸（移动端 y: 400px, 桌面端 y: 600px）
  - 添加窗口 resize 监听，实时响应屏幕尺寸变化
  - 移动端使用 size="small" 优化显示效果

- **样式系统统一** (Style System Unification)
  - 将 Layout.module.css 中 15+ 处硬编码颜色替换为 CSS 变量
  - 统一使用 variables.css 定义的语义化变量
  - 修复内容区域被固定导航栏遮挡的问题（添加适当的 padding-top）

- **性能优化指南** (Performance Optimization Guide)
  - 新增 `frontend/docs/performance-optimization.md` 文档
  - 包含虚拟滚动方案、图片优化、搜索防抖等最佳实践
  - 记录 Web Vitals 性能监控指标和目标

#### Changed / 改进

- AssetList 组件添加响应式状态管理（isMobile）
- 表格列配置支持移动端自动隐藏
- 表单输入框添加完整的可访问性属性
- 按钮和交互元素添加描述性 ARIA 标签

#### Fixed / 修复

- 修复内容区域被固定导航栏遮挡（64px header + 56px breadcrumb）
- **修复页面"蒙层"效果** (Fix Overlay Effect)
  - 移除 Layout.module.css 中的毛玻璃效果（`backdrop-filter: blur(8px)` 和半透明背景）
  - 移除 global.css 中的过渡动画冲突规则
  - 统一使用 CSS 变量 `var(--color-bg-container)` 作为背景色
  - 影响文件：Layout.module.css, global.css, App.tsx, TableWithPagination.tsx 等
  - 修复文件数：12个，修复问题数：15个
- **修复 TypeScript 类型错误** (Fix Type Errors)
  - 修复 AssetSearchResult 组件中 undefined 类型传递问题
  - 修复 AssetSearchFilters 接口类型定义（usage_status, property_nature 等）
  - 修复 ResponsiveTable 和 TableWithPagination 组件类型兼容性
  - 移除未使用的导入和变量（AssetList, EmptyState, Loading, ThemeToggle, VirtualList）
  - ESLint 检查通过（0 错误，0 警告）
- **修复导入路径大小写不一致** (Fix Import Path Case)
  - 统一使用 `Common` 目录（大写C）替代 `common`（小写c）
  - 提高跨平台兼容性（Windows/macOS/Linux）
- **修复 CSS 样式错误** (Fix CSS Style Errors)
  - 修复 EmptyState 组件中 `var-spacing-md)` 缺少开括号的错误
  - 优化 ThemeToggle 组件使用 CSS 类代替内联样式修改
- 修复硬编码样式导致的主题切换不兼容问题

#### 文档 (Documentation)

- 新增 `frontend/docs/ui-ux-improvements-report.md` - UI/UX 改进实施报告
- 新增 `frontend/docs/performance-optimization.md` - 性能优化指南

### 🧪 审核 (Audit)

- **数据库异步化实施核查** (Async DB Migration Audit)
  - 仍存在 run_sync、同步 Session 与 get_db 依赖，异步化未完全收敛

### 🧰 工具与环境 (Tooling & Environment)

#### Fixed / 修复

- 前端 ESLint 启用 `@typescript-eslint/no-explicit-any`（生产代码设为 error，测试文件通过 overrides 保持放行），强化类型安全基线
- 新增 `@typescript-eslint/no-unnecessary-type-assertion`（测试文件关闭）并自动清理一批冗余 `as` 断言，降低类型断言滥用风险
- 明确前端 API 分层边界：新增 `assetImportService` 并让 `AssetImport` 仅通过服务层调接口，新增 ESLint 限制组件/页面/Hook/Context 直接导入 `@/api/client`，避免 HTTP 层与业务层职责混用
- 继续收敛 UI 网络调用边界：`TestCoverageDashboard` 改为复用 `testCoverageService`，`AnalyticsDashboard` 导出改由 `analyticsService` 统一封装，`ErrorBoundary`/`RouteABTesting` 上报请求迁移到独立 service；并新增 ESLint 限制 UI 层直接调用全局 `fetch`
- `testCoverageService` 全量从原生 `fetch` 迁移到 `apiClient`，统一 `ExtractResult` 解包与错误处理语义；新增 `testCoverageService` 单元测试覆盖报告、趋势、模块、阈值与质量门禁等关键接口
- 继续统一前端上报链路：`errorReportService` 与 `abTestingReportService` 改为 `apiClient` 调用（去除硬编码 `/api/*` 与原生 `fetch`）；补充对应单元测试，并在 MSW 中新增 `/errors/report`、`/analytics/abtest-events`、`/analytics/abtest-conversions` handler，消除 ErrorBoundary 测试中的未匹配请求告警
- 前端环境判断收敛：新增 `utils/runtimeEnv`，并将 `ErrorBoundary`、`RouteABTesting`、`AnalyticsDashboard` 的 `process.env.NODE_ENV` 判定统一为运行时 helper（优先兼容 Vite `import.meta.env`，保留测试场景下的 `NODE_ENV` 覆盖能力）
- 修复 `AnalyticsDashboard` 组件测试的历史失稳项：统一 `useAnalytics` 与子组件 mock 路径为 `@/` 别名、`api/config` 改为部分 mock 保留 `API_BASE_URL` 导出、并将导出菜单断言改为 hover 打开下拉后校验文案，恢复测试文件可稳定执行
- 继续清理前端环境判定遗留：`api/client`、`errorHandler`、`colorMap`、`optimization`、`errorMonitoring`、`RoutePerformanceMonitor`、`messageManager`、`responseExtractor`、`versionConfig`、`userExperience`、`useSmartPreload`、`apiPathTest` 全部改为 `runtimeEnv` helper，移除业务代码中的 `process.env.NODE_ENV` 直接判断
- 测试环境一致性补充：`ErrorBoundary` 单测改为 mock `runtimeEnv` 控制开发/生产分支，不再直接读写 `process.env.NODE_ENV`，减少测试对 Node 进程全局状态的耦合
- Node 侧环境变量读取规范化：`vite.config.ts`、`playwright.config.ts`、`tests/e2e/asset-flow.spec.ts` 与 `scripts/diagnose/*` 新增统一读取 helper（字符串/布尔/数值），避免业务逻辑散落直接访问 `process.env.*`；当前 `frontend/` 仅保留 `runtimeEnv` 与 Vite `define` 的 `process.env.NODE_ENV` 注入点
- 修复前端类型检查阻断项：`AssetSearchResult` 对 `ownership_entity` 增加空值兜底；`tsconfig.json` 排除 `src/e2e/**` 与 `*.spec.ts(x)`，避免应用构建类型检查依赖 Playwright 测试类型
- 新增前端 E2E 独立类型检查链路：增加 `tsconfig.e2e.json`、`type-check:e2e` 脚本与 `types/playwright-test.d.ts` 声明；同时修复 `analyticsService` 与 `ContractListPage` 的严格类型报错，并补齐 `src/e2e/auth.spec.ts` 回调参数类型
- 修复 `useContractList` Hook 单测的 `react/display-name` lint 阻断：QueryClient 包装组件补充显式 `displayName`
- 清理前端剩余 3 条 `no-unnecessary-type-assertion` 警告：移除 `useContractList` 与 `AssetListPage` 中冗余 `as Error` 断言
- `Makefile` 的 `type-check` 目标改为串行执行 `pnpm type-check && pnpm type-check:e2e`，并新增 `type-check-e2e` 目标；`make check` 随之默认覆盖前端 E2E 类型检查
- 前端 `package.json` 的 `check`/`audit:ui`/`audit:full` 脚本统一纳入 `type-check:e2e`，确保本地与 CI 流程默认覆盖 E2E 类型检查
- 继续清理 `no-unnecessary-type-assertion`：移除 `UserManagementPage` 与 `RentLedgerPage` 中冗余 `as Error` 断言，减少无效类型收窄噪音
- 执行前端全量 Prettier 规范化并修复 175 处格式漂移，`pnpm check` 现已通过（lint + app type-check + e2e type-check + format:check）
- 开发启动脚本限制 Uvicorn reload 监听目录为 `backend/src`，并支持 `RELOAD` 环境变量关闭重载，避免非源码变更触发重启
- 修复 RBAC 初始化脚本可重复执行并补齐 `property_certificate` 权限，遇到遗留 `users.role` 约束时跳过测试用户创建以保证初始化可完成
- 修复 RBAC 初始化脚本动态权限示例的 `assigned_by` 外键错误，避免插入失败并保持幂等
- RBAC 初始化脚本在测试用户已存在时仍会确保角色分配，并在输出中明确已存在数量
- 修复角色列表/详情接口未预加载权限导致的 `MissingGreenlet` 500 错误
- 调整 CORS 中间件顺序，确保错误响应也包含 CORS 头，避免前端 `system/users` 报 CORS 拒绝
- CORS 允许 `Authorization` 请求头，修复携带登录令牌的预检请求被拒绝
- 本地开发环境补齐 `backend/.env` 基础配置（development 模式 + PostgreSQL 连接信息）
- Makefile 后端命令默认使用 `backend/.venv` 的 Python，避免调用系统 Python 导致依赖缺失
- 修复前端资产查询分页参数漂移：`assetCoreService.getAssets` 兼容 legacy `pageSize` 并统一映射为 `page_size`；同步修正项目详情、权属方详情、合同列表、租金台账中的资产查询调用，避免分页参数被忽略导致结果默认截断
- 统一前端认证状态单一来源：`hooks/useAuth` 改为 `AuthContext` 兼容代理，并在 `AuthContext` 补齐 `permissions`、`refreshUser`、权限判断与清错能力，消除 Context 与本地 Hook 双状态并存风险
- 前端状态管理收敛（P2）：资产列表页移除 `useListData + useEffect` 命令式加载，改为 React Query 单轨查询（分页/排序/筛选驱动 queryKey）；租赁合同列表 Hook 同步迁移到 React Query 单轨（列表/统计/参考数据），并更新对应单测适配 QueryClientProvider
- 前端状态管理收敛（P2 扩展）：租金台账页移除 `useListData`，列表/统计/参考数据统一改为 React Query 单轨查询，并统一支付更新与批量更新后的刷新逻辑（`refetchLedgers + refetchStatistics`）
- 前端状态管理收敛（P2 延伸）：用户管理页移除 `useListData` 与手动 `load*` 副作用，用户列表/组织/角色/统计统一改为 React Query 单轨查询；增删改与锁定/启停操作统一走 `refetchUsers + refetchStatistics` 刷新链路，减少状态漂移
- 前端状态管理收敛（P2 延伸）：角色管理页移除 `useListData` 与手动 `load*` 副作用，角色列表/权限/统计统一改为 React Query 单轨查询；增删改、状态切换与权限保存统一走 `refetchRoles + refetchStatistics` 刷新链路，降低多源状态不一致风险
- 前端状态管理收敛（P2 延伸）：操作日志页移除 `useListData`，日志列表查询统一改为 React Query（筛选/分页驱动 queryKey），并将刷新与分页入口收敛到 `refetchLogs + paginationState`，减少命令式加载分支
- 前端状态管理收敛（P2 延伸）：组织管理页移除 `useListData`，组织列表/树形结构/统计统一改为 React Query 查询；列表分页与刷新统一收敛到本地 `paginationState` + `refetchOrganizations/refetchOrganizationTree/refetchStatistics`
- 前端状态管理收敛（P2 延伸）：通知中心页移除 `useListData`，通知列表改为 React Query 查询并由分页/类型筛选驱动 `queryKey`；已读/全部已读/删除后的刷新统一走 `refetchNotifications`
- 前端状态管理收敛（P2 延伸）：Prompt 管理页移除 `useListData`，Prompt 列表与统计改为 React Query 查询；激活、新建/编辑成功后的刷新统一收敛到 `refetchPrompts + refetchStatistics`
- 修复 `NotificationCenter` 分页回调中的未使用参数 lint 告警，避免 `@typescript-eslint/no-unused-vars` 阻断
- 前端状态管理收敛（P2 组件层）：`OwnershipList` 移除 `useListData`，权属方列表与统计改为 React Query 查询；删除/状态切换/表单提交后统一走 `refetchOwnerships + refetchStatistics`
- 前端状态管理收敛（P2 组件层）：`AssetHistory` 移除 `useListData`，历史列表改为 React Query（按资产/分页/筛选驱动 queryKey），刷新与分页切换统一收敛到 `refetchHistory + paginationState`
- 前端状态管理收敛（P2 组件层）：`ProjectList` 移除 `useListData`，项目列表与权属方下拉改为 React Query 查询；删除/状态切换/表单提交后统一走 `refetchProjects`
- 组件单测对齐 React Query：`OwnershipList.test.tsx`、`AssetHistory.test.tsx`、`ProjectList.test.tsx` 移除 `useListData` 依赖并改为按 `queryKey` mock `useQuery` 返回数据/刷新函数，修复迁移后的断言失配
- 列表 Hook 依赖继续收敛：`useArrayListData` 内部改为独立分页/筛选/加载状态管理并保留原有 `loadList`/`applyFilters`/`resetFilters`/`updatePagination` 契约，移除对 `useListData` 的耦合以降低迁移链路风险
- `AssetListPage` 单测同步对齐 React Query：移除遗留 `useListData` mock，改为基于 `queryKey` 的 `useQuery` mock，并修复测试渲染 helper 递归调用导致的栈溢出（`Maximum call stack size exceeded`）
- 前端 legacy 列表 Hook 正式下线：删除已无引用的 `useListData` 与 `useFilters`，并同步更新 `ProjectList` 测试头注释为 React Query 版本，避免后续误导与死代码回流
- 统一租赁合同 API 模块命名：`backend/src/api/v1/rent_contract/` 重命名为 `backend/src/api/v1/rent_contracts/`，并同步更新路由聚合导入、单元/集成测试 patch 路径与相关文档引用，消除单复数命名不一致
- 命名一致性补充清理：更新 `scripts/test_file_naming.py` 中后端 API 示例路径至当前目录结构（含 `rent_contracts`），修正自定义字段 API 单测注释中的历史路径与接口前缀，并同步更新历史测试重构计划文档中的租赁合同 API 路径引用，避免后续审计误判
- 规范自定义字段类型列表路由：新增标准路径 `/api/v1/asset-custom-fields/types`，并保留旧路径 `/types/list[Any]` 作为隐藏兼容入口，避免历史调用中断
- 统一租赁合同路由前缀命名：将前端测试 Mock 与后端集成测试中的遗留 `/rent-contracts`、`/api/v1/rent/*` 引用收敛到当前标准 `/rental-contracts/*` 路径，避免测试与真实 API 前缀漂移
- 修复租赁合同缓存键失配：将合同创建/续签页中无效的 `invalidateQueries(['rent-contracts'])` 改为实际使用的 `['rent-contract-list']` 与 `['rent-contract-statistics']`，确保列表与统计数据能正确刷新
- 抽取租赁模块 React Query 键常量：新增 `frontend/src/constants/queryKeys/rental.ts`，并将合同列表/详情/续签/创建/统计页与相关失效调用统一改为 `RENTAL_QUERY_KEYS`，降低 key 拼写漂移与局部失效风险
- 继续统一租赁台账 React Query 键：`RentLedgerPage` 的列表/统计/参考数据查询改为 `RENTAL_QUERY_KEYS` 常量，减少页面内字面量 key 并保持租赁模块缓存策略一致
- 统一租赁台账刷新策略：`RentLedgerPage` 将手动 `refetch` 改为基于 `queryClient.invalidateQueries` + `RENTAL_QUERY_KEYS` 的失效刷新，统一与其他租赁页面的缓存更新模式
- 增加 `queryKeys` 统一导出入口：新增 `frontend/src/constants/queryKeys/index.ts` 并将租赁相关页面/Hook 导入切换为 `@/constants/queryKeys`，简化后续扩展与重构成本

#### Removed / 删除

- 删除根目录 `.venv`，统一使用 `backend/.venv` 以避免环境混淆

### 🧹 清理 (Cleanup)

#### Removed / 删除

- **项目临时文件清理** (Project Cleanup)
  - 删除测试输出、缓存目录和构建产物（释放 124.3 MB）
  - 清理临时脚本、报告文档和日志文件
  - 删除过期的 `package/` 目录和 playwright tarball 包
  - 移除测试覆盖率脚本 `test_rent_contract_service_coverage.py`
  - 总计清理 82 个文件，删除 1002 行代码

### 🔍 搜索 (Search)

- **加密字段模糊搜索支持** (Blind Index for Encrypted Search)
  - 新增 `asset_search_index` 表，使用 HMAC n-gram 盲索引支持地址/权属方子串搜索
  - 资产列表搜索与产权证资产匹配改用盲索引（加密启用时），仍保留未加密时 ILIKE
  - 新增重建脚本 `scripts/maintenance/rebuild_asset_search_index.py` 用于回填索引

### 📚 文档 (Docs)

- 明确说明 `SecurityService` 已移除，并指向 `PasswordService`/`AuthenticationService` 作为安全替代路径
- AGENTS.md 增加提示：后端测试应使用项目 `backend/.venv`，避免 `.env` 不生效
- AGENTS.md 增补启动/登录/排查经验与常见故障定位要点

### 🛡️ 稳定性与架构修复 (Stability & Architecture Fixes)

#### Fixed / 修复

- 修复 `core/performance.py` 查询优化器缩进错误导致后端启动失败的问题
- 将数据库初始化移动到应用生命周期中，避免启动时 `asyncio.run()` 在事件循环中触发错误
- 修复通知任务端点异步定义与依赖注入，避免路由注册时报 `await` 语法错误
- 修复 RBAC 角色有效期比较使用时区感知时间导致的数据库错误
- 修复登录接口在权限读取成功时未初始化角色汇总导致的异常
- **RBAC 角色单一来源** (Single Source of Truth for Roles)
  - 认证响应与用户信息统一使用 RBAC 角色汇总（`role_id`/`role_name`/`role_ids`），移除旧 `role` 字段依赖
  - 统一测试与示例数据为 `role_id`，避免遗留角色字符串导致权限误判
- **资产字段补齐** (Asset Field Completeness)
  - 资产新增 `wuyang_project_name` 与 `description` 字段对齐前端，避免提交后静默丢失
  - 新增数据库迁移补齐 `assets` 表字段
- **管理员权限统一** (Admin Permission Unification)
  - 管理员判断改为 `system:admin` 权限，不再依赖角色名
  - 迁移为 `admin`/`super_admin` 角色回填 `system:admin` 权限
- **资产权属字段统一** (Asset Ownership Unification)
  - 资产创建/更新/筛选仅使用 `ownership_id`，`ownership_entity` 改为动态读取（不落库）
  - 资产筛选与统计接口移除 `ownership_entity` 查询参数，前端筛选改用权属方选项与 `ownership_id`
- **组织循环检测查询降为单次递归 CTE** (Organization Cycle Check Single-Query CTE)
  - `_would_create_cycle` 改为递归 CTE 检测父链，避免逐层查询造成的性能放大
- **组织子树路径批量更新** (Organization Subtree Path Batch Update)
  - `_update_children_path` 改为递归 CTE 批量更新层级与路径，移除逐节点递归查询
- **管理员数据库重置异步化** (Admin DB Reset Async)
  - 管理员重置数据库接口改为异步并 await create/drop tables
- **RBAC 服务统一** (RBAC Service Consolidation)
  - 移除 `services/rbac` 旧实现与相关遗留测试引用
  - 统一使用 `services/permission/rbac_service.py`，改用 CRUD + 标准异常
  - 角色管理 API 调用路径切换至新 RBAC 服务
  - RBAC 服务内时间戳改为 `datetime.now(UTC)`，避免 `utcnow()` 弃用警告
- **移除不安全 SecurityService Stub** (Remove Unsafe SecurityService Stub)
  - 删除 `SecurityService` stub 与相关导出，避免 IDE/自动导入误用
  - 新增单元测试防回归，确保 `SecurityService` 不可被导入或导出
- **Ownership 模型抽离与导入统一** (Ownership Model Extraction & Import Unification)
  - Ownership 独立到 `models/ownership.py` 并更新所有引用路径
  - 统一模型导出，避免对 `models.asset` 的隐式依赖与循环导入风险
  - 修复字段白名单初始化中 Ownership 的导入路径，避免运行时警告
- **数据库引擎类型注解修复** (Database Engine Type Hint Fix)
  - 补充 `sqlalchemy.engine.Engine` 导入，避免类型注解在运行时报 NameError
- **枚举初始化异步化** (Enum Init Async Migration)
  - 启动阶段枚举数据初始化改用 AsyncSession + async_session_scope
  - 枚举与遗留值同步逻辑改为异步查询/提交
- **Excel 模板服务去数据库依赖** (Excel Template DB Decoupling)
  - Excel 模板服务移除 Session 依赖，下载路由不再注入未使用的 db
- **移除同步适配层** (Remove Sync Adapter Layer)
  - 删除 `utils/async_db.py` 中的 run_sync/适配器占位实现
- **资产唯一性校验异步化** (Asset Uniqueness Async Validation)
  - AssetValidator 唯一性校验改为 AsyncSession 查询
- **出租率计算去同步 DB** (Occupancy Calc Sync DB Removal)
  - OccupancyRateCalculator 移除同步 Session 聚合路径，仅保留内存计算
- **Excel 导出服务异步收敛** (Excel Export Async Convergence)
  - Excel 导出服务移除同步导出接口与 Session 依赖，保留异步导出路径
- **审计服务异步化** (Audit Service Async Migration)
  - AuditService 改为 AsyncSession + await 调用并更新单元测试为异步
- **分析缓存接口异步化** (Analytics Cache Async Update)
  - 缓存统计/清理服务方法改为异步并在 API 端点 await 调用
  - 分析服务单元测试与统计 API 测试更新为异步 Mock/await
- **分析缓存执行异步化** (Analytics Cache Async Execution)
  - 分析服务缓存读取/写入/清理/统计改为异步线程执行，避免阻塞事件循环
- **Excel 导出服务异步兼容** (Excel Export Async Compatibility)
  - Excel 导出服务支持 AsyncSession 并新增异步导出与预览方法
- **财务统计接口异步化** (Financial Stats Async Migration)
  - 财务汇总统计移除 run_sync，统一 await 异步服务
  - 财务统计单元测试改用 AsyncMock 与 await
- **系统备份与历史接口异步化** (System Backup & History Async Migration)
  - 备份创建/恢复移除 run_sync，统一使用 AsyncSession 获取数据库连接信息
  - 历史记录列表/详情/删除改用异步 CRUD 方法
- **租赁合同统计与生命周期异步化** (Rent Contract Stats & Lifecycle Async Migration)
  - 统计概览/权属/资产/月度/导出移除 run_sync，统一使用 AsyncSession 异步服务
  - 续签/终止流程新增异步服务并在路由 await 调用
- **租赁合同统计服务异步化收敛** (Rent Contract Statistics Async Convergence)
  - 统计服务移除同步方法与 Session 依赖，统一 AsyncSession 路径
- **资产批量/自定义字段/产权证测试异步对齐** (Async Test Alignment for Batch/CustomField/PropertyCert)
  - 资产批量服务与 API 单测改用 AsyncMock/await
  - 自定义字段/产权证 CRUD 与服务单测更新为异步调用
- **租赁合同 CRUD 异步化** (Rent Contract CRUD Async Migration)
  - 合同查询/筛选/台账/条款 CRUD 补齐 AsyncSession 方法，移除 run_sync 包装
  - 合同创建/更新/删除改为异步调用并新增异步更新服务
  - 租赁合同 API 单测改用 AsyncMock/await 适配异步接口
- **租赁合同台账与附件异步补齐** (Rent Contract Ledger & Attachment Async Follow-up)
  - 台账生成/批量更新/更新服务补齐异步实现并移除 run_sync
  - 附件/合同错误用例单测改为异步调用以匹配异步路由
- **租赁合同附件异步收敛** (Rent Contract Attachment Async Convergence)
  - 附件增删查改用 AsyncSession 调用并补齐异步上传服务
  - Excel 异步导入任务创建改为异步 CRUD，移除 run_sync 适配
- **资产附件单测异步对齐** (Asset Attachment Test Async Alignment)
  - 资产附件单元测试改用 AsyncMock/await 适配异步路由实现
- **资产附件字段防丢失对齐** (Asset Attachment Field Preservation)
  - 前端 `assetFormSchema` 补齐接收协议/终端合同附件字段
  - 后端新增 Asset schema 单元测试，确保字段序列化不丢失
  - 资产 API 单测样例补齐附件字段
- **用户组织字段对齐** (User Organization Field Alignment)
  - 后端 User 支持 `organization_id` 兼容别名并返回别名字段
  - 前端统一使用 `default_organization_id`，/auth/me 补齐组织字段

### 🧪 测试 (Tests)

- **RBAC/权属测试对齐** (RBAC/Ownership Test Alignment)
  - 单元/集成/E2E 测试统一使用 `role_id` 与 RBAC 角色分配
  - 资产测试数据改为 `ownership_id`，并为集成场景补齐权属方关联
- **系统监控健康检查测试异步化** (System Health Tests Async)
  - check_component_health 单测改为 async/await 并使用 AsyncMock
- **PDF 批量导入集成测试对齐异步依赖** (PDF Batch API Tests Async Alignment)
  - 移除 get_db 覆盖，使用默认 get_async_db 依赖
- **PDF 批量上传错误响应断言对齐** (PDF Batch Upload Error Assertion)
  - 集成测试改为校验中间件返回的 message 字段
- **出租率计算单测切换为内存计算路径** (Occupancy Calculator Tests In-Memory)
  - 移除 get_db patch，按内存资产列表验证统计结果

### 📚 文档 (Docs)

- 资产与数据库设计文档更新：`ownership_id` 作为唯一来源，`ownership_entity` 动态读取
- 认证与测试示例更新：移除旧 `role` 字段，统一使用 `role_id`
- **异步数据库示例更新** (Async DB Docs Refresh)
  - 后端/数据库/测试指南示例改为 AsyncSession + get_async_db
- **Excel 导出路由单测异步对齐** (Excel Export Route Test Async Alignment)
  - Excel 导出相关单元测试补齐 await 与异步 Mock
- **Excel 预览路由异步对齐** (Excel Preview Route Async Alignment)
  - 预览接口改用 AsyncSession + get_async_db 依赖注入
- **Excel 导入路由异步对齐** (Excel Import Route Async Alignment)
  - 同步/异步导入改为 AsyncSession 调用并移除线程内同步会话
  - 导入后台任务测试改为异步 CRUD 调用与 AsyncMock
- **请求日志异步化** (Request Logging Async Migration)
  - 操作日志记录改为 AsyncSession 异步写入并移除 run_sync 适配
- **产权证服务异步化** (Property Certificate Service Async Migration)
  - 产权证列表/详情/创建/更新/删除移除 run_sync，直接使用 AsyncSession CRUD
- **产权证提取提示词加载** (Property Certificate Prompt Loading)
  - 上传提取流程改用异步 PromptManager 获取激活模板，避免无 PromptManager 时抛配置异常
- **文档提取管理器兼容性** (Document Extraction Manager Compatibility)
  - 产权证提取支持注入 AsyncSession 获取 Prompt，结果字段兼容 extracted_fields/data
- **产权证提取适配器异步兼容** (Property Certificate Adapter Async Compatibility)
  - PromptManager 根据 AsyncSession 调用异步查询，避免同步查询阻塞
- **PDF 导入会话异步化** (PDF Import Session Async Migration)
  - 会话创建/状态查询/处理状态持久化/确认导入/取消流程移除 run_sync
- **PDF 批量会话查询异步化** (PDF Batch Session Async Migration)
  - 批量状态/取消接口改用 AsyncSession 查询会话映射
- **PDF 会话服务异步化** (PDF Session Service Async Migration)
  - PDFSessionService 改为 AsyncSession 并异步化会话查询/创建
- **PDF 处理追踪异步化** (PDF Processing Tracker Async Migration)
  - ProcessingTracker 与步骤追踪装饰器改为 AsyncSession
- **组织列表分页解析修复** (Organization List Pagination Parsing)
  - 前端组织服务解包分页 items，避免用户管理页加载组织列表时报错
  - 组织搜索/高级搜索/历史列表统一兼容分页响应
- **用户列表无限刷新修复** (User List Infinite Refresh Fix)
  - 拆分用户管理页初始化副作用，避免统计刷新触发重复拉取导致渲染深度溢出
- **租赁合同创建提交修复** (Rent Contract Create Submit Fix)
  - 租赁合同表单补齐 onFinish 提交回调，修复创建合同无请求的问题
  - 新增表单提交单元测试覆盖
- **租赁合同创建错误提示与重试优化** (Rent Contract Create Error Messaging & Retry)
  - 创建合同遇到 4xx 冲突不再重试，避免重复提交
  - 失败提示展示后端返回的冲突原因
- **资产重名与删除约束** (Asset Uniqueness & Delete Guard)
  - 资产 `property_name` 全局唯一（服务/导入校验 + 数据库唯一约束）
  - 资产删除时若已关联合同或产权证则阻止删除
- **权属一致性校验** (Ownership Alignment)
  - `ownership_id` 与 `ownership_entity` 不一致时拒绝写入并对齐名称
- **异步会话迁移补齐** (Async Session Migration Completion)
  - 资产/合同/组织/催缴/字典/统计/产权证/PDF批量等接口统一改用 AsyncSession + get_async_db
  - PDF 批量监控改用 async_session_scope，避免同步会话混用
  - PDF 导入会话创建/取消改为异步封装，API 调用统一 await
  - 合同编辑权限检查改为异步 RBAC 适配器调用
  - Excel 同步导入改为线程内创建会话，避免跨线程复用 Session
- **异步会话迁移补充** (Async Session Migration Follow-ups)
  - 枚举字段/权属方/项目/会话管理接口统一改为 AsyncSession + run_sync 适配同步 CRUD/Service
    - 资产导入/提示词/系统设置接口补齐 run_sync 包装与类型适配
- **用户管理服务单测异步化** (User Management Service Test Async Migration)
  - 修复用户管理服务单元测试，统一 AsyncMock/await 与 AsyncSessionService 依赖注入
- **用户管理 API 单测异步对齐** (Users API Test Async Alignment)
  - Users API 单测改为 asyncio.run 调用异步路由，CRUD/Service Mock 全量异步化
  - 锁定/解锁/重置密码测试补齐 AsyncMock commit/refresh/rollback
- **认证集成测试导入清理** (Auth Integration Test Import Cleanup)
  - 集成测试导入改为 AsyncAuthenticationService/AsyncSessionService/AsyncUserManagementService
- **认证 CRUD 异步化** (Auth CRUD Async Migration)
  - auth CRUD 移除同步 Session 接口，补齐用户/会话/审计异步方法
  - 认证 CRUD 单元测试改为 AsyncMock/await，集成测试移除 get_db 同步依赖
- **资产服务异步化** (Asset Service Async Migration)
  - AssetService 统一为 AsyncSession 异步实现，并保留 AsyncAssetService 别名
  - 资产服务单元/集成测试改为异步执行，使用 AsyncMock 与异步会话隔离
  - 资产服务集成测试改用临时异步引擎 + NullPool，避免事件循环关闭导致连接异常
- **组织服务异步化** (Organization Service Async Migration)
  - OrganizationService 全量改为 AsyncSession 调用并修正组织 API 调用命名
  - 组织服务单元/集成测试改为异步执行（AsyncMock + AsyncSession）
  - 组织服务历史/统计查询改为异步路径
  - 遗留的深度/增强集成测试暂时标记跳过，等待异步 API 对齐后恢复
- **操作日志 CRUD 异步化** (Operation Log CRUD Async Migration)
  - OperationLogCRUD 移除同步方法，仅保留异步 CRUD/统计接口
  - 操作日志 CRUD 单元测试改为 AsyncMock + await
  - 组织服务集成测试修正异步引擎引用方式，避免引擎初始化判定失效
  - 组织服务集成测试改用临时异步引擎 + NullPool，避免事件循环关闭导致连接异常
- **系统字典异步化** (System Dictionary Async Migration)
  - system_dictionary CRUD/Service 移除同步 Session 接口，仅保留 AsyncSession 路径
  - 系统字典单元/集成测试改为异步执行并使用 asyncpg 引擎
- **枚举字段与公共字典异步化** (Enum Field & Common Dictionary Async Migration)
  - enum_field CRUD 移除同步方法，仅保留 AsyncSession 异步实现
  - CommonDictionaryService 移除同步入口，单元测试改为 AsyncMock/await 并移除 db_session 依赖
- **通知模块异步化** (Notification Module Async Migration)
  - Notification CRUD/Service 移除同步方法，仅保留 AsyncSession 异步接口
  - 通知定时任务调度器改为异步查询与 async_session_scope 执行
  - 通知调度单元/集成测试改为异步会话与 AsyncMock
- **任务模块异步化** (Task Module Async Migration)
  - Task/ExcelTaskConfig CRUD 移除同步方法，仅保留 AsyncSession 异步实现
  - 任务 API 与服务单元测试统一 AsyncMock/await 调用
  - 任务服务集成测试改用异步会话与当前 TaskService 能力
  - 任务创建单元测试对齐历史记录写入与多次提交行为
- **资产字段值查询测试数据去重** (Asset Field Values Test Data Uniqueness)
  - 资产集成测试字段值查询场景改用唯一 `property_name`，避免唯一约束冲突

### ✨ 文档识别 (Document Extraction)

- **GLM-OCR 预处理接入**
  - 新增 GLM-OCR 服务与 OCR 文本抽取管道
  - 智能识别支持扫描件自动走 OCR，并支持 `force_method=ocr`
  - 产权证/合同提取在 OCR 失败时回退至现有视觉模型
- **PyMuPDF 调用异步化**
  - 文本提取 / PDF 分析 / Vision 转图统一线程池 offload，避免阻塞事件循环
- **LLMService 读取配置对齐**
  - LLM 服务创建优先读取 settings/.env 中的提供商与密钥配置，避免本地脚本无法读取环境变量
- **Vision PDF 转图工具补齐**
  - 补充 `pdf_to_images` 实现，支持 PyMuPDF/pdf2image 转图，修复视觉提取缺失模块
- **证据驱动识别输出**
  - OCR 提取新增字段证据片段与来源（field_evidence/field_sources）
  - PDF 智能合并流程加入一致性校验与置信度扣减
  - API 类型与前端处理选项支持 `force_method=ocr`
  - async_db 适配器补齐构造签名类型约束，修正类型推断错误
- **合同导入审核证据展示**
  - 合同导入确认页支持字段证据弹窗与来源提示
  - 提取警告信息同步展示，便于快速复核异常字段
- **资产批量与历史接口异步化** (Asset Batch & History Async Migration)
  - 资产批量更新/验证/删除/列表与历史接口移除 run_sync，统一使用 AsyncSession 调用
  - 批量更新与删除补齐异步 CRUD 与历史记录写入
- **分析缓存容错** (Analytics Cache Guard)
  - 分析缓存读写失败时降级执行计算，避免测试/运行期 Redis 异常导致 500
- **分析与面积服务单测异步适配** (Analytics/Area Test Async Alignment)
  - 面积聚合/内存计算单测改用 AsyncSession mock 与 await
  - 分析趋势/分布测试补齐异步 patch 与 await 逻辑
- **分析服务清理无用导入** (Analytics Service Import Cleanup)
  - 移除未使用的 SQLAlchemy or_ 导入以通过 lint
- **后台服务会话初始化修复** (Background Service Session Init Fix)
  - 出租率计算、通知任务、租金合同导出改用 session_factory，移除 get_db 生成器调用
- **代码格式统一** (Code Formatting Alignment)
  - 执行 Ruff 格式化以统一最新异步迁移后的代码风格
- **PDF 导入临时文件清理** (PDF Import Temp File Cleanup)
- **催缴服务异步收敛** (Collection Service Async Convergence)
  - CollectionService 移除同步方法与 Session 依赖，统一 AsyncSession
  - 催缴 API 单测改用 AsyncMock/await
- **枚举验证异步收敛** (Enum Validation Async Convergence)
  - 移除同步 EnumValidationService 与便捷函数，仅保留 AsyncEnumValidationService
  - 单测与全局 mock 更新为异步验证接口
- **LLM Prompt 异步收敛** (LLM Prompt Async Convergence)
  - PromptManager/FeedbackService/AutoOptimizer 移除同步接口并改用 AsyncSession
  - LLM Prompt 服务单测重写为异步覆盖
- **产权证提示词适配器收紧** (Property Cert Adapter Async-only)
  - PromptManager 获取提示词仅接受 AsyncSession，移除同步 fallback

### 🧰 工具 (Tooling)

- **开发进程监控脚本** (Dev Watch Script)
  - 新增 `scripts/dev_watch.ps1`，启动前后端并在异常退出时记录退出码与日志尾部
  - 支持后台脱离控制台运行，避免关窗导致进程被中断
- **开发启动默认行为恢复** (Dev Server Defaults Restored)
  - Makefile 恢复默认 shell 行为
  - 前端 Vite 恢复默认启动与 TS 配置，后端 dev server 恢复默认 reload

### 📝 文档更新 (Documentation Updates)

- 更新 `AGENTS.md` 与 `CLAUDE.md`，同步最新命令、规则与更新时间
- 对齐前端状态管理描述：Zustand 用于 UI/资产 UI 状态，认证使用 React Context

### 🐞 Bug Fixes

- **产权证列表 404** (Property Certificate List 404)
  - 修复产权证路由重复前缀导致 `/api/v1/property-certificates/` 404
  - 处理结果/失败后自动清理 `temp_uploads` 与系统临时目录下的源文件
- **产权证字段兼容修复** (Property Certificate Field Compatibility)
  - 响应补齐 `verified` 字段并兼容 `is_verified` 更新
  - 列表查询参数统一使用 `limit`
- **前端路由与组件警告修复** (Frontend Route & Warning Fixes)
  - 产权证导入页面包屑不再误判为详情页
  - Ant Design Steps/Modal 废弃属性警告修复
- **系统字典与统计页面修复** (Dictionary & Stats Page Fixes)
  - 枚举字段列表响应移除 children 懒加载，修复字典页 500
  - 枚举值 children 通过 `set_committed_value` 清理，避免 Pydantic 校验触发 MissingGreenlet
  - 系统设置/组织管理 Tabs 改用 items，移除 TabPane 废弃警告
  - 系统设置密码策略表单修复单子元素渲染警告
  - 个人中心弹窗强制渲染，避免 useForm 未连接提示
  - 租金统计饼图标签改为函数并补齐图表错误边界
  - 租金统计收缴率进度条改用 `size`，移除 Progress width 废弃警告
- **项目/组织响应懒加载防护** (Project/Organization Lazy-load Guard)
  - ProjectResponse/OrganizationResponse 避免在序列化时访问未加载关系字段
- **枚举字段异步加载修复** (Enum Field Async Load Fix)
  - 异步加载枚举值使用 `set_committed_value`，避免 greenlet_spawn 触发 503
- **PDF 批量上传内存风险** (PDF Batch Upload Memory Risk)
  - 批量上传改为流式写入 `temp_uploads`，避免一次性读取到内存
  - 超限/空文件即时清理，失败时回收临时文件
- **Excel 导出临时文件清理** (Excel Export Temp File Cleanup)
  - 异步导出文件统一落盘到 `temp_uploads/excel_exports`
  - 任务删除或清理过期任务时回收导出文件
- **Excel 异步任务状态修复** (Excel Async Task Status Fix)
  - 异步导入/导出任务改用 `TaskService` 更新状态与历史记录
  - 后台任务独立创建数据库会话，避免请求结束后的会话失效
- **Excel 配置创建修复** (Excel Config Create Fix)
  - Excel 配置创建时补齐 `task_type` 与格式配置，避免字段不匹配导致创建失败
  - 默认配置切换时自动取消同类型默认值，避免多默认配置
- **资产创建事务提交修复** (Asset Create Transaction Commit Fix)
  - 修复资产创建在已有事务（权限校验/审计依赖）场景下仅开启嵌套事务导致提交丢失的问题
  - 资产创建现在在检测到已有事务时显式提交，确保写入持久化
- **资产列表缓存失效修复** (Asset List Cache Invalidation Fix)
  - 资产新增/更新/删除后清理资产相关 GET 缓存，避免列表继续显示旧数据
  - API 客户端新增按前缀失效缓存接口，支持粒度清理
- **资产软删除与校验加强** (Asset Soft Delete & Guard)
  - 资产删除改为软删除（data_status=已删除），保留历史记录与附件数据
  - 删除前新增租金台账关联校验，阻止存在台账的资产被删除
  - 查询统计补齐软删除过滤，确保列表与总数一致
  - 管理员新增资产恢复/彻底删除接口，支持软删除恢复与物理清理
  - 资产恢复/彻底删除补齐服务层校验与单元测试覆盖
  - Hard delete now clears asset history to avoid FK violations.
- **资产回收站筛选与前端操作** (Asset Recycle Bin UI)
  - 资产列表支持 `data_status` 筛选查看回收站记录
  - 前端新增资产状态筛选与状态列展示
  - 已删除资产仅显示恢复/彻底删除操作（管理员 + 强确认）
- **任务访问控制修复** (Task Access Control Fix)
  - 任务列表/详情/更新/删除/历史仅允许任务创建者或管理员访问
  - Excel 任务状态与导出下载接口需要认证并校验任务归属
  - 过期任务清理接口限制为管理员调用
- **任务模块异步迁移** (Task Module Async Migration)
  - 任务 CRUD/Service/API 统一使用 AsyncSession，移除 run_sync 适配
  - Excel 任务创建与状态更新改用同步会话专用 CRUD 方法
- **租金台账更新逻辑修复** (Rent Ledger Update Logic Fix)
  - 单条台账更新改为服务层处理，自动计算逾期金额与服务费
  - 统一与批量更新的衍生字段逻辑，避免直接 CRUD 导致数据不一致
- **Excel 历史分页修复** (Excel History Pagination Fix)
  - 修复 Excel 操作历史接口返回总数错误的问题
  - 现在返回真实总数以匹配分页数据
- **合同编辑权限校验修复** (Contract Edit Permission Check Fix)
  - 修复合同更新权限检查未生效的问题，确保 RBAC 校验可用
  - 新增租金条款接口同步执行合同编辑权限校验
- **附件接口修复** (Attachment Endpoints Fix)
  - 修复资产/租金合同附件接口的语法错误，确保路由正常加载
  - 附件接口统一为同步实现，避免测试与运行期不一致
- **API 路由语法修复** (API Route Syntax Fix)
  - 修复统计/组织/催缴/字典/出租率/Excel/PDF 批量接口中的 async/sync 结构错误
  - 清理错误的 `run_sync` 嵌套与 await 使用，恢复路由可加载与运行
  - 产权证接口改用同步会话并修正服务调用方式，避免运行期异常
- **Session 类型引用修复** (Session Type Reference Fix)
  - 修复 PDF 导入服务、系统设置与用户管理路由缺失 `Session` 类型引用导致的导入失败
- **系统设置接口异步化** (System Settings Async Migration)
  - 系统设置/系统信息/备份/恢复接口移除 run_sync，审计日志改为异步写入
- **租金合同路由同步化修复** (Rent Contract Route Sync Fix)
  - 将合同/条款/台账/统计/附件接口改为同步会话，移除错误的 `run_sync` 包装
  - 附件下载/删除改为同步获取附件对象，避免运行期取不到元数据
- **资产附件路由同步化修复** (Asset Attachment Route Sync Fix)
  - 资产附件上传/列表/下载/删除统一为同步会话并匹配文件校验流程
  - 附件列表与下载使用文件系统 API 与校验逻辑，确保测试路径一致
- **资产附件同步依赖补齐** (Asset Attachment Sync Dependency Fix)
  - 资产附件端点改用 `get_db` 同步会话，避免单元测试直接调用时出现未 await 协程
- **Excel 路由同步适配** (Excel Route Sync Alignment)
  - Excel 配置/导出/状态/模板接口恢复同步实现并改用 `get_db`，匹配单元测试直接调用方式
  - 异步导入/导出后台任务补齐可注入 `db_session` 并改用 `task_crud` 更新任务状态
  - 预览与同步导入端点补齐测试参数兼容逻辑，避免 mock 调用缺失导致断言失败
  - 异步导入创建任务时区分 AsyncSession 与 mock db，避免 `run_sync` 误用
- **租金合同 Excel 服务补齐** (Rent Contract Excel Service Restoration)
  - 恢复租金合同模板下载、导入与导出服务，避免模块缺失导致接口不可用
  - 支持合同/条款/台账多表导出与基础导入
- **运行依赖补齐** (Runtime Dependency Completion)
  - 添加 `cryptography` 与 `httpx` 到后端核心依赖，避免运行期导入失败
  - 添加 `asyncpg` 作为异步数据库驱动依赖，避免 AsyncSession 运行期导入失败
- **认证会话写入修复** (Auth Session Insert Fix)
  - `user_sessions` 写入统一使用 naive UTC 时间戳，避免 asyncpg 对 TIMESTAMP WITHOUT TIME ZONE 抛错
  - 审计日志写入改用 naive UTC 时间戳，避免 `audit_logs` 写入失败
  - 用户时间戳字段（`password_last_changed`/`updated_at`/`last_accessed_at`）改为 naive UTC
  - 登录流程不再因会话写入失败返回 500
- **密码过期判断修复** (Password Expiry Comparison Fix)
  - 密码过期时间比较统一为 naive UTC，避免登录时出现 aware/naive 比较异常
- **资产筛选下拉修复** (Asset Filter Dropdown Fix)
  - 资产列表筛选项改为异步 distinct 查询，避免 `run_sync` 调用协程导致 500
- **权属方列表修复** (Ownership List Fix)
  - 权属方列表/搜索改用同步查询封装，修复 `/api/v1/ownerships` 500
- **前端缓存分页提取修复** (Frontend Cache Paginated Extract Fix)
  - GET 缓存命中时继续执行 smartExtract，避免缓存返回原始响应导致列表结构缺失
- **创建接口斜杠兼容** (Create Endpoint Slash Compatibility)
  - 权属方/项目创建同时支持无尾斜杠路径，避免前端 POST 405
- **权属方创建校验修复** (Ownership Create Validation Fix)
  - 前端创建不再提交空字符串编码，允许后端自动生成编码，避免 422
- **权属方/项目异步化** (Ownership & Project Async Migration)
  - 权属方/项目服务与 API 端点改为 AsyncSession 调用，移除 run_sync 适配
  - 权属方财务汇总查询改为异步 SQL，避免同步子查询问题
- **项目响应懒加载修复** (Project Response Lazy-Load Fix)
  - 项目创建/列表/详情响应改为显式映射列字段，避免 ownership_relations 触发 MissingGreenlet
- **前端缓存提取测试** (Frontend Cache Extract Test)
  - 新增单元测试覆盖缓存命中时的分页响应提取
- **时间戳标准化扩展** (UTC Timestamp Standardization Expansion)
  - 模型/服务/CRUD 中写库时间统一使用 `datetime.utcnow()`（naive UTC），避免 asyncpg 对无时区字段报错
  - API 层 Excel 任务与用户更新写入时间统一为 `datetime.utcnow()`（naive UTC）
  - 清理替换后未使用的 `UTC` 导入
- **生产环境路由注册器防降级** (Production Router Registry Guard)
  - 生产环境禁止启用 `ALLOW_MOCK_REGISTRY`，缺失注册器属性时直接报错
  - 新增单元测试覆盖生产环境 Mock 降级保护
- **租金合同删除逻辑修复** (Rent Contract Delete Logic Fix)
  - 删除合同改为服务层软删除并记录历史，避免历史/关联表外键导致的删除失败
  - 租金合同查询默认排除已删除数据
- **前端类型检查修复** (Frontend Type Check Fixes)
  - 修复列表过滤回调与自定义统计卡片类型不匹配导致的 TS 报错
  - 修复 PDF 导入处理方式空字符串比较引发的类型报错
- **角色管理端点异步化** (Role API Async Migration)
  - 角色 CRUD、权限分配、统计与用户列表端点移除 run_sync 适配
  - 角色用户列表查询改为 AsyncSession 直连查询
- **认证会话接口异步化** (Auth Session Async Migration)
  - 会话查询/撤销与可选认证中间件移除 run_sync，改用 AsyncSession 查询
  - 调试认证端点改为 AsyncAuthenticationService 与 AsyncUserManagementService
  - 审计日志统计接口改为 AsyncSession 直连查询
- **组织架构接口异步化** (Organization API Async Migration)
  - 组织列表/搜索/树形/路径/历史等接口移除 run_sync 适配
  - 组织服务与 CRUD 补齐 AsyncSession 查询与历史记录写入
- **操作日志接口异步化** (Operation Log API Async Migration)
  - 操作日志列表/统计/导出/清理改为 AsyncSession 直连查询
  - 用户名补齐查询改用异步批量映射接口
- **安全事件接口异步化** (Security Event API Async Migration)
  - 系统设置安全事件查询改为异步 CRUD 调用
  - 组织权限与安全告警测试改为原生异步安全日志记录
- **枚举字段接口异步化** (Enum Field API Async Migration)
  - 枚举类型/值/使用记录/历史查询改为 AsyncSession 直连调用
  - 枚举 CRUD 补齐异步统计、批量写入与历史记录写入方法
- **提示词与通知接口异步化** (Prompt & Notification API Async Migration)
  - Prompt 模板/版本/统计/反馈接口移除 run_sync 适配
  - 通知列表/未读统计/标记已读/删除改为 AsyncSession 调用
- **系统字典与催缴接口异步化** (System Dictionary & Collection API Async Migration)
  - 系统字典 CRUD/批量更新改为 AsyncSession 直连
  - 催缴汇总/记录列表/读写接口移除 run_sync 适配

#### Changed / 变更

- **AGENTS 测试指引** (AGENTS Low-Memory Test Guidance)
  - 补充低内存运行 pytest 的建议与示例命令
- **资产枚举值更新** (Asset Enum Defaults Update)
  - 更新确权状态/使用状态/物业性质/租户类型的标准枚举值
- **产权证 API 层依赖规范化** (Property Certificate API Layer Normalization)
  - API 端点改为通过 `PropertyCertificateService` 访问 CRUD

#### Added / 新增

- **缺失的 CRUD 类补齐** (Missing CRUD Classes Added)
  - 新增 `collection_crud`、`prompt_template_crud`
- **白名单补齐** (Field Whitelist Coverage)
  - 为 `CollectionRecord`、`PromptTemplate` 增加字段白名单
  - 为 `Project`、`Organization`、`PropertyCertificate`、`PropertyOwner`、`RentTerm`、`RentLedger`、`UserRoleAssignment`、`ResourcePermission`、`PermissionAuditLog` 增加字段白名单
  - 为 `AsyncTask`、`ExcelTaskConfig` 增加字段白名单

### 🧹 代码质量与测试修复 (Lint & Test Fixes)

#### Fixed / 修复

- **Ruff 清理** (Ruff Cleanup)
  - 清理无效 `_sync` 占位逻辑与未使用变量，统一异步/同步辅助代码风格
  - 修正缺失的类型导入与泛型写法（`AsyncDB` 适配器改用 PEP 695 语法）
  - 修复分析接口空行空白、重复导入与未使用导入问题
  - 统一 Excel 导入与产权证 CRUD 的导入排序/来源
  - 补齐资产历史查询缺失的 `Session` 类型导入
  - 密码过期校验改用 `datetime.UTC` 以符合 Ruff 规则
- **MyPy 类型修复** (MyPy Type Fixes)
  - 完善系统日志/字典/联系人/角色等接口的 `run_sync` 适配与 `Session` 类型注解
  - 修复租金合同 Excel 导入/导出类型转换与字段映射模型匹配
  - 统一权限检查/布尔返回类型，减少 `no-any-return` 报错
  - 补齐认证/资产/统计分析/租金合同等模块 `run_sync` 类型标注与导入
  - Excel 异步导入任务创建 helper 增加类型注解
- **附件/日志与导出修复** (Attachment, Logs, Export Fixes)
  - 附件下载补齐元数据获取，避免运行期变量未定义
  - 操作日志导出修复过滤器变量作用域
  - Excel 异步导出补齐 `Session` 类型引用
- **测试稳定性** (Test Stability)
  - 测试环境变量恢复逻辑补齐，避免污染后续用例
  - 清理未使用断言变量并补充必要断言
  - 认证 API 与会话服务单元测试改为 AsyncSession + AsyncMock
  - 认证服务拆分验证测试改用异步服务与 AsyncSession mock
- **可选认证测试清理** (Optional Auth Test Cleanup)
  - 移除可选认证单测中的未使用 mock 逻辑与多余 token 生成，避免 lint 噪音
  - 可选认证单测改为异步调用并使用 AsyncSession 兼容的 stub

### ⚙️ 配置管理优化 (Configuration Management Optimization)

#### Changed / 变更

- **配置文件标准化** (Configuration File Standardization)
  - 整合 `config/environments/backend.env` 到 `backend/.env`
  - 创建标准 `frontend/.env` 文件
  - 统一使用项目根目录的 `.env` 文件，符合工具链最佳实践
- **配置验证去重** (Config Validation Deduplication)
  - 环境覆盖逻辑移入 `Settings` 校验阶段，避免运行时二次修改
  - 移除重复的 SECRET_KEY/Redis 校验，保留 DATABASE_URL 必填检查

#### Removed / 删除

- **完全删除 config/ 目录** (Complete Removal of config/ Directory)
  - 删除 `config/environments/` - 未被代码引用
  - 删除 `config/shared/` - YAML文件无加载机制
  - 删除 `config/templates/` - 已有 `.env.example` 文件作为模板
  - 删除 `config/root/` - API检查脚本已在 `frontend/scripts/` 和 `backend/scripts/`

#### Added / 新增

- `backend/.env` - 完整的后端环境配置（合并所有必要配置）
- `frontend/.env` - 前端Vite环境变量
- 更新 `GEMINI.md` 环境配置文档，包含详细设置指南

### 🎯 代码质量重构 (Code Quality Refactoring)

#### Removed / 删除

- **移除AI风格命名** (Removed AI-Style Naming)
  - 删除 `backend/src/api/v1/CLAUDE.md` - 旧的AI助手文档
  - 移除所有 "Enhanced" 前缀的类名和组件名
  - 移除 "Unified" 前缀的类名

- **简化认证服务层** (Simplified Authentication Services)
  - 合并 `AuthService` 和 `AuthenticationService`
  - 移除冗余的服务包装层

#### Modified / 修改

**后端 (Backend) - 37 files**
- `backend/conftest.py` - 测试配置更新
- `backend/src/api/v1/auth_modules/authentication.py` - 认证端点重构
- `backend/src/api/v1/auth_modules/password.py` - 密码管理端点更新
- `backend/src/api/v1/auth_modules/sessions.py` - 会话管理端点更新
- `backend/src/api/v1/auth_modules/user_management.py` - 用户管理端点重构
- 多个测试文件更新以反映新的服务结构

**前端 (Frontend) - 42 files**
- `frontend/src/api/client.ts` - API客户端重命名
- `frontend/src/utils/responseExtractor.ts` - 响应提取器重命名
- `frontend/src/types/index.ts` - 类型定义更新
- 多个组件和页面更新以使用标准命名

#### Added / 新增

- `frontend/src/pages/Rental/PDFImportPage.tsx` - PDF导入功能页面
- `frontend/src/types/pdfImport.ts` - PDF导入类型定义
- `backend/src/core/database.py` - 数据库核心模块

### 📊 统计 (Statistics)

- **文件修改**: 79 files changed
- **代码增加**: +892 insertions
- **代码删除**: -6,415 deletions
- **净删除**: -5,523 lines (代码更简洁)

### 🎨 改进重点 (Key Improvements)

1. **命名规范化** - 移除AI生成的命名模式，采用业务领域术语
2. **架构简化** - 减少不必要的抽象层和服务包装
3. **代码精简** - 删除了超过6000行冗余代码
4. **可维护性提升** - 更清晰的代码结构和命名

### 🔗 相关文档 (Related Documentation)

- [架构重构分析（归档）](docs/archive/drafts/architecture-refactoring-2026-02.md)
- [测试标准](docs/guides/testing-standards.md)

### 🧹 文档清理 (Documentation Cleanup)

#### Removed / 删除

- 删除过期/未引用的阶段性报告与计划文档:
  - `docs/project-comprehensive-analysis-2026-02-02.md`
  - `docs/project-issues-report-2026-02-02.md`
  - `docs/test-coverage-improvement-phase1-report.md`
  - `docs/test-coverage-improvement-plan.md`
  - `docs/todo-debt-plan.md`
  - `docs/property-certificate-feature-plan.md`
  - `docs/property-certificate-implementation-summary.md`

### 🧩 文档补全 (Documentation Completion)

#### Added / 新增

- 新增缺失的文档与模板，补齐文档树结构:
  - `docs/index.md`
  - `docs/guides/getting-started.md`
  - `docs/guides/deployment.md`
  - `docs/integrations/api-overview.md`
  - `docs/integrations/auth-api.md`
  - `docs/integrations/assets-api.md`
  - `docs/integrations/pdf-processing.md`
  - `docs/features/prd-asset-management.md`
  - `docs/features/prd-rental-management.md`
  - `docs/features/spec-data-models.md`
  - `docs/features/spec-user-permissions.md`
  - `docs/incidents/incident-template.md`
  - `docs/testing/v2-test-cases.md`
  - `docs/v2_upgrade_plan.md`
  - `docs/architecture-refactoring.md`

#### Fixed / 修复

- 修复文档锚点与引用错误，确保内部链接有效

### 🧾 文档与工具维护 (Docs & Tooling Maintenance)

#### Added / 新增

- `backend/docs/API_DOCUMENTATION_ANALYSIS.md` - API 文档分析报告占位与生成说明
- `backend/docs/COVERAGE_IMPROVEMENT_REPORT.md` - 覆盖率报告占位与生成指引
- `docs/tooling/assistant-metadata.md` - 根目录工具元数据说明
- `docs/guides/components.md` - 前端组件导航与入口说明
- `frontend/src/components/index.ts` - 组件统一入口命名空间导出
- `frontend/src/components/Analytics/index.ts` - 分析模块统一入口
- `frontend/src/components/Common/index.ts` - 通用组件统一入口
- `frontend/src/components/Monitoring/index.ts` - 监控模块统一入口
- `frontend/src/components/Router/index.ts` - 路由模块统一入口
- `frontend/src/components/System/index.ts` - 系统管理模块统一入口
- `frontend/src/components/Auth/index.ts` - 认证模块统一入口
- `frontend/src/components/Dashboard/index.ts` - 仪表盘模块统一入口
- `frontend/src/components/Ownership/index.ts` - 权属方模块统一入口
- `frontend/src/components/Project/index.ts` - 项目模块统一入口
- `frontend/src/components/Rental/index.ts` - 租赁模块统一入口

#### Changed / 变更

- 修复文档引用路径:
  - `backend/docs/enhanced_database_guide.md`
  - `frontend/docs/type-safety-fix-summary.md`
  - `scripts/README.md`
- 更新文档入口与前端指南组件导航链接:
  - `docs/index.md`
  - `docs/guides/frontend.md`
- 更新组件导航表，补充 Auth/Dashboard/Ownership/Project/Rental/Router/Monitoring/System 入口:
  - `docs/guides/components.md`
- 修正组件入口导出冲突与默认导出错误:
  - `frontend/src/components/Analytics/index.ts`
  - `frontend/src/components/Common/index.ts`
- `backend/debug_import.py` 迁移至 `backend/scripts/dev/debug_import.py` 并补充路径初始化
- `.gitignore` 允许 `backend/docs` 下的分析/报告占位文档被追踪
- `.gitignore` 忽略前端测试输出文件（`frontend/test-results.txt`、`frontend/test_output.txt`、`frontend/vitest_stdout.txt`）

### 🗑️ 文档站点移除 (Docs Site Removal)

#### Removed / 删除

- 移除 MkDocs 站点构建相关配置与依赖:
  - `backend/mkdocs.yml`
  - `docs/includes/mkdocs.md`
  - `backend/pyproject.toml` 中的 docs 可选依赖
  - `backend/uv.lock` 中的 mkdocs 相关锁定项

### 📝 文档更新 (Documentation Updates)

#### Changed / 变更

- 更新后端与数据库指南，补充 asyncpg 依赖说明、虚拟环境提示与 UTC 时间戳写库约定
- 更新资产管理 PRD，补充范围/对象关系、枚举、唯一性、删除约束、权属对齐与权限预留要求
- 更新资产管理 API 文档，补充业务规则、枚举与关联/删除约束说明

---
## [1.0.0] - 2026-01-15

### 初始版本 (Initial Release)

- ✅ 用户认证与权限管理 (RBAC)
- ✅ 资产基础信息管理
- ✅ 租赁合同基础管理
- ✅ 租金台账基础功能
- ✅ 权属方/项目列表管理
- ✅ 数据字典管理

## [2026-02-22-phase3-plan-v132]

### Changed

- Updated `docs/plans/2026-02-20-phase3-implementation-plan.md` to v1.32.
- Replaced the D10 route mutual-exclusion gate from cross-line `grep -E` regex to a Node structural check command.
- Standardized legacy-field zero-reference scans to `grep -w` in P3b/P3e/§6.2 to reduce regex-boundary portability risk.
- Tightened production feature-flag verification: missing `frontend/.env.production` is now a hard failure; removed auto-write default `VITE_ENABLE_CAPABILITY_GUARD=false` behavior from examples.
- Added `request_id` source-priority contract and PartySelector Level-2 fallback telemetry fields (`result_count`, `is_truncated`, `search_query_length`).
- Corrected section order/numbering: Windows core gate commands are now §6.3 and browser smoke matrix is §6.4.

## [2026-02-23-rent-contract-abac-scope-fix]

### Fixed

- 修复 `rent_contract` 创建鉴权在仅提交 `ownership_id` 场景下强制注入 unscoped `party_id` 导致非管理员被错误拒绝的问题。
- 新增 `ownership_id -> legal_entity party_id` 回退解析链路（通过 `parties.id/external_ref/code/name` 解析），用于创建/变更鉴权上下文补齐主体范围。
- 修复合同变更（update/delete）在 legacy 合同缺失 `owner_party_id/manager_party_id` 时的越权风险：
  - 先尝试基于 `ownership_id` 回退解析主体范围；
  - 无法解析时注入 mutation unscoped sentinel，确保 ABAC fail-closed（管理员仍可走 RBAC bypass）。
- 补充并更新 `backend/tests/unit/api/v1/test_rent_contract_contracts_layering.py` 回归测试，覆盖：
  - create ownership fallback scope 解析；
  - create unresolved ownership 不再强制 unscoped；
  - mutation ownership fallback scope 解析；
  - mutation unresolved ownership fail-closed。
- 新增 `backend/tests/unit/crud/test_party.py` 针对 `resolve_legal_entity_party_id` 的单测，覆盖 external_ref 匹配与空标识输入分支。
- 新增 `backend/tests/unit/services/test_rent_contract_service.py` 针对 `resolve_owner_party_scope_by_ownership_id_async` 的单测，覆盖空 ownership 输入、ownership 元数据透传（code/name）、以及 party 未解析返回 `None` 分支。

## [2026-02-23-authz-write-context-hardening-v2]

### Fixed

- 补齐 10 个 API 模块中 `require_authz(create/update/delete)` 的资源绑定，消除剩余 17 处“无 `resource_id` 且无 `resource_context`”风险点（AST 扫描结果 `TOTAL=0`）：
  - `backend/src/api/v1/party.py`
  - `backend/src/api/v1/assets/asset_batch.py`
  - `backend/src/api/v1/assets/custom_fields.py`
  - `backend/src/api/v1/assets/property_certificate.py`
  - `backend/src/api/v1/auth/organization.py`
  - `backend/src/api/v1/auth/roles.py`
  - `backend/src/api/v1/rent_contracts/ledger.py`
  - `backend/src/api/v1/system/collection.py`
  - `backend/src/api/v1/system/contact.py`
  - `backend/src/api/v1/system/enum_field.py`
- 对无单一资源 ID 的批量/创建写操作统一注入 fail-closed unscoped sentinel `resource_context`，避免 ABAC 在空资源上下文下误放行。
- 对可定位单资源的写操作补充 `resource_id`：
  - `custom_fields.update/delete` 使用 `{field_id}`。
  - `rent_contracts.ledger.update` 使用 `{ledger_id}`。
  - `auth.roles.assign_role_to_user` 使用 `_resolve_user_assignment_resource_id` 从请求体解析 `user_id`。
- `property_certificate.upload_certificate` 改为复用 `_require_property_certificate_create_authz`，与 `confirm_import/create_certificate` 保持一致的组织/主体上下文注入策略。

### Tests

- 更新并增强 10 个 layering 测试文件，新增/收紧 `resource_id` / `resource_context` 断言与 sentinel 常量断言：
  - `backend/tests/unit/api/v1/test_party_layering.py`
  - `backend/tests/unit/api/v1/test_asset_batch_layering.py`
  - `backend/tests/unit/api/v1/test_custom_fields_layering.py`
  - `backend/tests/unit/api/v1/test_property_certificate_authz_layering.py`
  - `backend/tests/unit/api/v1/test_organization_layering.py`
  - `backend/tests/unit/api/v1/test_roles_layering.py`
  - `backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py`
  - `backend/tests/unit/api/v1/test_collection_layering.py`
  - `backend/tests/unit/api/v1/test_contact_layering.py`
  - `backend/tests/unit/api/v1/test_enum_field_layering.py`

### Verification

- `backend/.venv/bin/ruff check backend/src/api/v1/party.py backend/src/api/v1/assets/asset_batch.py backend/src/api/v1/assets/custom_fields.py backend/src/api/v1/assets/property_certificate.py backend/src/api/v1/auth/organization.py backend/src/api/v1/auth/roles.py backend/src/api/v1/rent_contracts/ledger.py backend/src/api/v1/system/collection.py backend/src/api/v1/system/contact.py backend/src/api/v1/system/enum_field.py backend/tests/unit/api/v1/test_party_layering.py backend/tests/unit/api/v1/test_asset_batch_layering.py backend/tests/unit/api/v1/test_custom_fields_layering.py backend/tests/unit/api/v1/test_property_certificate_authz_layering.py backend/tests/unit/api/v1/test_organization_layering.py backend/tests/unit/api/v1/test_roles_layering.py backend/tests/unit/api/v1/test_rent_contract_ledger_layering.py backend/tests/unit/api/v1/test_collection_layering.py backend/tests/unit/api/v1/test_contact_layering.py backend/tests/unit/api/v1/test_enum_field_layering.py` ✅
- `cd backend && .venv/bin/pytest --no-cov -q tests/unit/api/v1/test_party_layering.py tests/unit/api/v1/test_asset_batch_layering.py tests/unit/api/v1/test_custom_fields_layering.py tests/unit/api/v1/test_property_certificate_authz_layering.py tests/unit/api/v1/test_organization_layering.py tests/unit/api/v1/test_roles_layering.py tests/unit/api/v1/test_rent_contract_ledger_layering.py tests/unit/api/v1/test_collection_layering.py tests/unit/api/v1/test_contact_layering.py tests/unit/api/v1/test_enum_field_layering.py` ✅ (`59 passed`)
- AST 扫描：`require_authz(action in {create,update,delete})` 且缺少 `resource_id/resource_context` 结果 `TOTAL=0` ✅

## [2026-03-01-phase4-implementation-continuation]

### Fixed

- 修复 `RentContractLifecycleService.update_contract_async`：更新链路不再写入已废弃别名 `ownership_id`，避免通过别名 setter 覆盖 `owner_party_id`。
- 对齐 QueryBuilder 单测到 Phase4 语义：
  - `Asset` 不再断言 legacy `organization_id` 过滤回退；
  - `PropertyCertificate` 无主体列时断言“跳过过滤而非 fail-closed”。
- 对齐租赁合同相关单测到 Phase4 语义：
  - `ownership_id` 在 owner/manager scope 生效时仅作为兼容别名，不参与 SQL 过滤断言；
  - owner 更新场景断言 `ownership_id` 与 `owner_party_id` 同步别名行为。
- 对齐资产模型/服务单测：
  - `Asset.management_entity` 缺省回填为 `owner_party_id`（Step4 兼容）；
  - `_resolve_ownership` 校验 owner/manager 双主体时，更新为两次 `party_crud.get_party` 调用断言。

### Verification

- `cd backend && .venv/bin/pytest --no-cov -q tests/unit/crud/test_query_builder.py tests/unit/crud/test_rent_contract_crud.py tests/unit/services/rent_contract/test_rent_contract_service_impl.py` ✅ (`60 passed`)
- `cd backend && .venv/bin/pytest --no-cov -q tests/unit/models/test_asset.py` ✅ (`42 passed`)
- `cd backend && .venv/bin/pytest --no-cov -q tests/unit/services/asset/test_asset_service.py::TestOwnershipResolution::test_resolve_ownership_resolves_legacy_ownership_id_to_owner_party_id` ✅
- `cd backend && .venv/bin/pytest --no-cov -m "not slow" -q -x` ✅ (`4816 passed, 6 skipped`)
