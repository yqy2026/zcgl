import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

const DataMappingTest: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>数据映射测试</Title>
      <Card>
        <p>数据映射测试页面正在开发中...</p>
      </Card>
    </div>
  )
}

export default DataMappingTest