# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **资产运营专员（主用户）**：日常在系统内完成资产、合同与协议、经营台账、客户、产权证的全链路维护；以「项目」为默认主工作对象，同时使用资产、合同与协议、主体客户、经营台账、经营分析等全局直接入口。
- **管理层**：通过经营分析与大屏查看终端租户收缴、运营方收入、运营方成本、服务费结算、经营结果与客户双指标。
- **系统管理员 / 权限管理员**：维护用户、角色、组织架构、主体范围、数据字典；权限管理员只可管理系统配置，不可查看业务数据。

## Product Purpose

为土地/物业资产运营方建立统一的资产运营平台，把分散的人工台账（资产、合同与协议、证照、主体信息、经营统计）收拢为可追踪、口径一致、权限可控的系统。支持承租转租与代理运营两种经营模式的并行建模，支撑「扫描件上传 → AI 解析预填 → 人工确认补录 → 经营台账 → 收付流水」的核心经营闭环。成功意味着：资产清晰可见、合同与协议可用、补录与台账可追踪、经营统计口径可用（MVP 一票否决目标）。

## Positioning

以项目为默认主工作对象、以盖章扫描件为事实底座的统一资产运营平台：同一套系统内并行表达承租转租与代理运营两种模式，统计口径严格按租金账期归属并由流水派生实收/实付，逾期与数据质量风险实时派生、不可人工翻改。

## Operating Context

- 纯中文界面，公司内网桌面端使用（Chrome 最新两个大版本 / Edge 最新版），常规列表 P95 ≤ 800ms，统计和搜索 P95 ≤ 2s。
- 业务事实以盖章扫描件为底座：合同/协议与产权证上传扫描件，OCR/AI 解析仅预填，用户逐项确认或修正后落库；解析会话即用即弃，不留草稿。
- 用户按角色（RBAC/ABAC）× 有效主体范围（显式绑定或组织默认）× 视图（owner/manager/all）三维正交访问；无有效范围时失败关闭（403），不返回空列表伪装。
- 企业微信为可选通知推送通道（定向应用消息，非群广播），未接通前仅站内通知。
- 现状为人工台账与线下口径，系统上线后以真实生产数据走通核心链路为验收。

## Capabilities and Constraints

MVP 能力：

- 资产管理：列表、详情、新增、编辑、删除/恢复、导入、附件；关键主数据两步确认（draft → pending → approved）与反审核。
- 项目管理：项目与资产归属、项目经营台账与汇总视图；运营方权威轴在项目，产权方挂资产。
- 租赁管理：承租转租与代理运营双模式；承租合同、转租合同、委托协议、直租合同四类合同/协议；盖章扫描件解析预填、人工确认补录、经营台账生成与维护、轻量收付流水（可分摊多账期）。
- 客户管理：主体（Party）单一主档、建档查重、两步审核、四种业务角色切片；客户主体数 + 客户合同数双指标。
- 搜索：全局搜索覆盖资产、项目、合同/协议、客户，业务编码命中置顶，未授权对象不返回。
- 权限：登录/刷新/退出、RBAC + ABAC 数据策略、主体范围自动注入、关键写操作鉴权与审计。
- 数据分析：概览、趋势、分布、终端租户收缴、运营方收入、运营方成本、服务费结算、经营结果、客户双指标；默认按租金账期归属，支持导出。
- 文档处理：合同与产权证单件扫描件上传、OCR/AI 解析、预填、人工确认、重试与超时处理；附件轻量在线预览、独立下载权限与日志。
- 产权证管理：资产详情内维护证照、权利人、附件、解析预填；5 项硬门槛；数据质量风险仅派生 warning 级提示，不阻断、不可手工关闭。
- 消息通知：五类站内提醒（到期/逾期/系统通知），通知是提醒不是工单。

硬约束：

- 不做财务总账、税票、支付通道、银行对账、独立现金流分析；不做通用 BPM 审批流与合同路由审批（补录即生效，仅保留最小生命周期）；不做 SaaS 多租户；不做高级 BI 建模；不做用户侧物理删除（关键记录只逻辑删除/作废）；不做主体合并；不做批量解析/批量确认；不做催缴工单。
- 单条合同/协议经营事项只能对应一种经营模式；客户双指标必须在单一视图（owner/manager）下计算，混合范围下综合分析硬拒绝、项目分析靶向置空。
- PII（身份证号、手机号）必须加密存储；Asset 乐观锁；批量写路径幂等。
- 代理直租租金进入终端租户收缴但不计入运营方收入；服务费应收按账期×委托协议比例月度生成；逾期只属于终端租户租金收缴。

## Brand Commitments

- 产品名：土地物业资产运营管理系统（英文名 Real Estate Asset Management & Operations System）。
- 用户已确认：无既有品牌/视觉约束（无强制 Logo、VI 色板、设计语言），内部工具视觉可自由发挥。
- 界面语言为纯中文，无国际化需求；不引入英文术语作为页面对象名（用户侧统一「合同与协议」，不以「合同关系」命名）。

## Evidence on Hand

- `docs/prd.md`：产品需求基线（用户、范围、口径、验收标准）——本轮 PRODUCT.md 的主要事实来源。
- `docs/specs/domain-model.md`、`docs/specs/api-contract.md`：字段、状态机、API/鉴权契约。
- `docs/traceability/requirements-trace.md`：REQ 到实现的映射与证据。
- 无真实用户截图、生产数据、证言或案例素材；未来设计不得虚构这些证据。

## Product Principles

1. **事实底座**：盖章扫描件是合同/协议与产权证的事实底座（至少保留一份）；解析只辅助、人工定夺——候选不自动绑定、不自动覆盖，确认动作内不创建主数据。
2. **口径权威不可翻改**：实收/实付由流水汇总派生，统计按租金账期归属；逾期、服务费来源不一致、产权证数据质量风险均由数据实时派生，不设手工标记位，只能修正源数据自然消除。
3. **数据范围即安全**：所有业务查询按统一主体范围解析器过滤；范围缺失、失效必须失败关闭并返回明确 403，绝不以空列表伪装成功。
4. **项目为主、全局并存**：普通运营用户默认从项目进入工作，但资产、合同与协议、客户、台账、分析保留同级直接入口，不设唯一进入路径。
5. **补录即生效、一切留痕**：MVP 不设提审/审核审批流，补录即生效；更正、作废、反审核、收付流水均留痕可审计，关键业务记录只允许逻辑删除。

## Accessibility & Inclusion

用户已确认 MVP 无明确无障碍标准立项。设计保持基本可用基线（合理对比度、键盘可达、语义化结构），不承诺 WCAG 级别达标。
